# YAML Record Review: outdoor air

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/air/outdoor_air.yaml
- Started UTC: 2026-09-21T06:08:00Z
- Finished UTC: 2026-09-21T06:10:22Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/air/outdoor_air.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.d263dfb09c` |
| Label | `outdoor air` |
| Category | `AIR` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `REVIEWED` |
| Maintained status | generated from `data/raw/`, `curation/decisions.tsv`, `curation/term_requests.tsv`, and `data/habitats/PATHS.tsv` |

The generated record is an item-reviewed GOLD term request for the unenclosed
air sibling of `indoor air`:

- GOLD supplies one `Environmental > Air > Outdoor Air` source attestation with
  42 `ORGANISM` assertions.
- `data/raw/gold_ecosystem_paths.tsv` collapses three GOLD ecosystem node IDs,
  `gold.ecosystem:3478`, `gold.ecosystem:3747`, and `gold.ecosystem:3962`,
  into that one canonical GOLD path.
- An item-level `CONFIRM_UNGROUNDED` decision keeps the minted HabitatMech
  identifier and parents the record to `ENVO:00002005` `air`.
- A term-request row authors the definition under `air`, keeps the source
  hierarchy with `parent_mode=ADD`, and adds `ambient air` as an exact synonym.
- No characteristic taxa, environmental parameters, claim-level evidence,
  causal graphs, discussions, datasets, or xrefs are present.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/air/outdoor_air.yaml` | Passed; no LinkML issues. |
| `just validate-strict data/habitats/air/outdoor_air.yaml --quiet` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| `just term-requests-check` | Passed; the generated term-request table is current at 109 terms. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 found, no missing/extra/differing records. |
| `just validate-history` | Passed; 77 history records valid against `src/habitatmech/schema/history.yaml`. |
| Reference validator | Not checked: this record has no DOI/PMID/URL `EvidenceItem` or causal-edge evidence. `scripts/check_report_citations.py` only samples or checks the full research DOI corpus, not one target report. |

## Identity and Grounding

| Claim | Evidence | Assessment |
|---|---|---|
| The record denotes the GOLD `Environmental > Air > Outdoor Air` source concept. | `data/raw/gold_ecosystem_paths.tsv` has exactly that canonical path, leaf `Outdoor Air`, depth `3`, `gold_node_count=3`, `organism_count=42`, `total_assertions=42`, and node IDs `gold.ecosystem:3478\|gold.ecosystem:3747\|gold.ecosystem:3962`. | Supported exactly. |
| The stable file stem is `outdoor_air`. | `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.d263dfb09c` to `outdoor_air`. | Supported exactly. |
| `grounding_status: UNGROUNDED` is correct. | `curation/decisions.tsv` has `habitatmech:GOLD.d263dfb09c` as `CONFIRM_UNGROUNDED`, `review_depth=ITEM`, and nearest parent `ENVO:00002005` `air`. `data/raw/ontology_terms.tsv` verifies `ENVO:00002005` as `air` and the rejected kg-microbe mapping target `ENVO:01000829` as `water vapour saturated air`. | Supported exactly. |
| `ENVO:00002005` `air` is a true parent. | The committed outdoor-air research report proposes `ENVO:00002005` as the genus, verifies `ENVO:01000470` `building envelope` as the differentia term, and rejects the plausible but wrong `ENVO:03501101` `outdoor environment`, `ENVO:01000267` `atmosphere`, `ENVO:01001652` `atmospheric aerosol`, and `ENVO:01000676` `contaminated air` alternatives. | Supported exactly. |
| `mapping_status: REVIEWED` is correct. | The sole source concept feeding this record has an item-level decision row, and the authored term-request row defines the minted concept. | Supported exactly. |
| `ambient air` is a supported exact synonym. | The outdoor-air research report ties `ambient air` to EPA and EU legal definitions and to ENVO issue 980's paired request for an `Outdoor air / ambient air` material term. | Supported. |

## Evidence

No `EvidenceItem` rows or causal graphs are present, so there are no attached
claim-level snippets or causal edges to audit.

The curator-authored definition is supported by the committed outdoor-air
research report's proposed definition and by the maintained
`curation/term_requests.tsv` row. Its scope is intentionally narrower than the
ASHRAE HVAC airstream sense of `outdoor air` and intentionally broader than the
public-access or workplace carve-outs in air-quality regulations; both
distinctions are recorded in the research report and term-request note.

The term request's exact synonym is supportable. `ambient air` appears in the
EPA and EU legal definitions inspected by the report, in the standards summary,
and in the ENVO issue requesting `Outdoor air / ambient air`.

## Completeness

- The record is schema-valid, reproducible, item-reviewed, and backed by a
  committed definition-research report.
- Empty `characteristic_taxa`, `evidence`, `environmental_parameters`,
  `causal_graphs`, `discussions`, `datasets`, and `xrefs` are technically
  acceptable: no maintained input currently supplies them for this record.
- `data/raw/gold_path_triads.tsv` has one exact
  `Environmental > Air > Outdoor Air` MIxS triad, with `atmosphere` as
  `env_broad_scale`, `outdoor environment` as `env_local_scale`, and `air` as
  `env_medium`, but all three slots come from one study. The triad ranking code
  deliberately ranks by independent studies rather than repeated samples, and
  the outdoor-air triad is evidence for future curation rather than a generated
  `EnvironmentalParameter`.
- `data/raw/environment_parameters.tsv` has no row for the exact
  `Environmental > Air > Outdoor Air` path.
- Ignored/hidden-inclusive exact searches covered `data`, `curation`,
  `research`, `docs`, `src`, `.claude`, and `reports`, excluding only generated
  text-map, cache, and build trees, for `habitatmech:GOLD.d263dfb09c`,
  its GOLD ecosystem node IDs, `Outdoor Air`, the exact GOLD path, and
  `outdoor_air`. These found the target, its `PATHS.tsv` row, the raw GOLD path
  and triad rows, the item-level decision row, the term-request row, generated
  term-request output, the dedicated outdoor-air research report, the successful
  research-manifest row, and adjacent records or reports that mention outdoor
  air.
- An ignored/hidden-inclusive exact search found no prior
  `reports/yaml_record_review/` report with
  `- Record: data/habitats/air/outdoor_air.yaml`.
- Bounded `find` searches for `*outdoor*` and `*air*` directly under
  `curation/causal_graphs/` found no candidate causal overlay.
- A bounded `find research/habitats/air -maxdepth 1 -type f -name '*outdoor*'`
  search found exactly one dedicated outdoor-air report.

## Findings

None found.

## Recommended Edits

None.

Future curation may decide whether to add a relationship to
`ENVO:03501101` `outdoor environment` or how to use the GOLD MIxS triad, but
neither is necessary to make the present record valid or internally consistent.

## Follow-up Checks

No curation fix is recommended from this review.

If a later curation edit changes the grounding decision, term request, or any
source-derived field, prove it with:

- `just seed`
- `just seed-canary habitatmech:GOLD.d263dfb09c`
- `just validate data/habitats/air/outdoor_air.yaml`
- `just validate-strict data/habitats/air/outdoor_air.yaml --quiet`
- `just term-requests-check`
- `just verify-corpus --max-diffs 1`

## Additional Notes

- The raw BacDive `Outdoor-Air` lookup row in
  `data/raw/isolation_source_groundings.tsv` is unmapped and does not feed this
  record; the generated YAML correctly has only a GOLD source attestation.
- `reports/habitat_research_manifest.tsv` records two failed outdoor-air
  research attempts and one successful committed report at
  `research/habitats/air/outdoor-air-habitatmech-gold-d263dfb09c-deep-research-claude_code.md`.
- `gold.ecosystem:3478` is the first of the three source node IDs for the
  collapsed path, so using it as the displayed `source_id` while listing all
  three IDs in `data/raw/gold_ecosystem_paths.tsv` matches the seeder's
  existing provenance convention.
