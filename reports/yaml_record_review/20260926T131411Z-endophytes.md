# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/host_associated/endophytes.yaml
- Started UTC: 2026-09-26T13:14:11Z
- Finished UTC: 2026-09-26T13:14:11Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.89d01f437a` |
| Label | `Endophytes` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Generated path | `data/habitats/host_associated/endophytes.yaml` |
| Maintained path lock | `data/habitats/PATHS.tsv:2316` maps `habitatmech:GOLD.89d01f437a` to `endophytes` |
| Source concept | GOLD `gold.ecosystem:5265` |
| Source label | `Endophytes` |
| Source path | `Host-associated > Plants > Phyllosphere > Carposphere > Endophytes` |
| Grounding | `UNGROUNDED` |
| Mapping | `SEEDED` |

The reviewed file is generated from the GOLD source inventory and one
class-level curation decision. It should not be patched directly.

## Validation

| Command | Result |
|---|---|
| `just worklist --limit 0 --status all --out /tmp/habitatmech-worklist.tsv` | Passed; wrote 953 rows. The target appeared as row 237 with three GOLD organism assertions and candidate ontology term `PO:0006051=endotesta`. |
| `just validate data/habitats/host_associated/endophytes.yaml` | Passed with `No issues found`. |
| `just validate-strict data/habitats/host_associated/endophytes.yaml` | Passed; one file scanned, zero files with `ERROR`, zero total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 expected records, 3206 found, 0 missing, 0 extra, 0 differing. |
| `just report` | Passed. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

The generated identifier, GOLD source identity, path, and assertion count agree
with the raw GOLD aggregate:

| Claim | Evidence | Assessment |
|---|---|---|
| The target is the `gold.ecosystem:5265` `Endophytes` leaf under the carposphere branch. | `data/raw/gold_ecosystem_paths.tsv:765` records `Host-associated > Plants > Phyllosphere > Carposphere > Endophytes`, leaf label `Endophytes`, depth 5, one GOLD node ID, three organism assertions, zero study assertions, zero biosample assertions, and total assertion count 3. | Supported exactly. |
| The stable generated filename is pinned for this identifier. | `data/habitats/PATHS.tsv:2316` maps `habitatmech:GOLD.89d01f437a` to `endophytes`. | Supported exactly. |
| The `source_attestations` block preserves the raw GOLD source ID, label, path, assertion count, and unit. | The generated YAML has `source: GOLD`, `source_id: gold.ecosystem:5265`, `source_label: Endophytes`, the full carposphere endophyte `source_path`, `assertion_count: 3`, and `assertion_unit: ORGANISM`. | Supported exactly. |
| The parent edge to `Carposphere` preserves the immediate GOLD path context. | `data/habitats/host_associated/carposphere.yaml` represents `Host-associated > Plants > Phyllosphere > Carposphere` and is the sole generated parent of this record. | Supported exactly. The parent is also still generated and class-level `UNGROUNDED`. |
| The source concept is still owned only by a class-level sweep decision. | `curation/decisions.tsv:811` has `CONFIRM_UNGROUNDED` for `habitatmech:GOLD.89d01f437a` with `review_depth` `CLASS`. Its note says habitat status was not assessed. | Reproducible but incomplete. |

The same `Endophytes` leaf label is used in three different GOLD plant paths:

| Identifier | GOLD path | Assertions | Local path |
|---|---:|---:|---|
| `habitatmech:GOLD.9731c6bb13` | `Host-associated > Plants > Roots > Rhizoplane > Endophytes` | 148 organisms | `data/habitats/host_associated/endophytes__fae60ba7.yaml` |
| `habitatmech:GOLD.89d01f437a` | `Host-associated > Plants > Phyllosphere > Carposphere > Endophytes` | 3 organisms | `data/habitats/host_associated/endophytes.yaml` |
| `habitatmech:GOLD.b8e99e08e5` | `Host-associated > Plants > Phyllosphere > Phylloplane/Leaf > Endophytes` | 0 assertions | `data/habitats/host_associated/endophytes__ca9aaedf.yaml` |

Those are distinct GOLD concepts, not alternate files for this target. The
carposphere source should be item-reviewed in that full source-path context
instead of being merged by the shared bare `Endophytes` label.

The only lexical candidate surfaced by the worklist is not exact:

| CURIE | Label | Definition | Relationship to the GOLD source |
|---|---|---|---|
| `PO:0006051` | `endotesta` | `A portion of seed coat epidermis (PO:0006048) which is part of a seed coat (PO:0009088) and develops from the inner epidermis of the outer integument (PO:0020023).` | Not an exact match. It names a specific seed-coat tissue, while the GOLD source leaf names the endophytic branch of the fruit/carposphere path. |
| `PO:0009001` | `fruit` | `A multi-tissue plant structure (PO:0025496) that develops from a gynoecium (PO:0009062), or a single carpel (PO:0009030), and at maturity may have as parts one or more seeds (PO:0009010).` | A relevant host organ candidate for the `Carposphere` context, but broader than this endophyte leaf and not a plant-interior microbial environment. |
| `PO:0009010` | `seed` | `A multi-tissue plant structure (PO:0025496) that develops from a plant ovule (PO:0020003) and has as parts a plant embryo (PO:0009009) enclosed in a seed coat (PO:0009088).` | Related to fruit biology and to `endotesta`, but still not exact for carposphere endophytes. |

## Evidence

| Record claim | Nearest source | Assessment |
|---|---|---|
| `source: GOLD`, `source_id: gold.ecosystem:5265`, `source_label: Endophytes`, and `source_path: Host-associated > Plants > Phyllosphere > Carposphere > Endophytes` | `data/raw/gold_ecosystem_paths.tsv:765` | Supported exactly. |
| `assertion_count: 3` and `assertion_unit: ORGANISM` | `data/raw/gold_ecosystem_paths.tsv:765` | Supported exactly. This is a GOLD organism count and is not comparable with BacDive strain counts, PREGO taxon counts, or the sibling GOLD records' biosample rows. |
| Parent `habitatmech:GOLD.e898437067` | `data/raw/gold_ecosystem_paths.tsv:348` is the immediate parent path `Host-associated > Plants > Phyllosphere > Carposphere`; `data/raw/gold_ecosystem_paths.tsv:765` is the reviewed child path. | Supported exactly. The parent is a source-path parent, not an ontology grounding. |
| Absence of a curated definition, environmental parameters, evidence objects, causal graph, item-level history event, target-specific research report, term request, and prior exact YAML review | Exact hidden/ignored-inclusive searches for `habitatmech:GOLD.89d01f437a`, `Host-associated > Plants > Phyllosphere > Carposphere > Endophytes`, and `gold.ecosystem:5265` across `data/raw/`, `curation/`, `history/`, `research/habitats/`, `reports/yaml_record_review/`, `data/habitats/`, and `pages/habitats/` found only the expected raw row, path lock, class-level decision, generated record, and generated page. A hidden/ignored-inclusive `find` for `*endophy*` under `data/habitats/` and `research/habitats/` found the three generated `Endophytes` YAML records and no target-specific research report. | Supported. The current record is a generated GOLD leaf with no committed item-level curation. |

## Completeness

The generated record completely preserves the current GOLD source aggregate and
the class-level `CONFIRM_UNGROUNDED` decision. It correctly avoids unsupported
curated definitions, characteristic taxa, environmental parameters, causal
edges, and literature evidence.

The record is incomplete as a grounding and definition decision. The
class-level sweep only tested the bare label `Endophytes` against the then
vendored ontology slice; it did not decide whether
`Host-associated > Plants > Phyllosphere > Carposphere > Endophytes` should be
a novel carposphere-endophytic environment, a narrower child of a broader
endophytic plant environment, or an out-of-scope organismal bin. The worklist
candidate `PO:0006051` `endotesta` reinforces the need for item review because
the local lexical route can see the `endo` string but cannot see that a
seed-coat epidermis is too specific for this GOLD path.

Carposphere's GOLD siblings also show why the path must stay in scope during
review. GOLD separately models `Host-associated > Plants > Phyllosphere >
Carposphere` itself, its `Surface` child, and its `Epiphytes` child. A curator
therefore needs to decide the interior/endophyte reading explicitly rather than
collapse this record to the generic carposphere or to a plant surface term.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| HM-ENDOPHYTES-CARPOSPHERE-001 | Major | `Host-associated > Plants > Phyllosphere > Carposphere > Endophytes` still publishes as `UNGROUNDED` from a `CLASS`-depth `CONFIRM_UNGROUNDED` decision that only says the bare label `Endophytes` had no lexical match. No item-level decision records whether the three-organism GOLD leaf is a novel carposphere endophytic habitat, an instance of a broader plant endophytic habitat, or out of scope, and the only current worklist candidate, `PO:0006051` `endotesta`, is a seed-coat epidermis rather than an exact match for the full source path. | `curation/decisions.tsv`; if no exact ontology term exists after item review, `curation/term_requests.tsv` |

## Recommended Edits

1. Item-review `gold.ecosystem:5265` in the full `Host-associated > Plants >
   Phyllosphere > Carposphere > Endophytes` context, keeping it distinct from
   the roots/rhizoplane and phylloplane/leaf `Endophytes` siblings.
2. Treat `PO:0006051` `endotesta` as too narrow unless source-specific evidence
   proves this GOLD leaf is seed-coat-specific rather than fruit/carposphere
   endophytes in general.
3. If no exact ontology term exists, replace the class-level decision with an
   item-level HabitatMech definition and a term request whose parents preserve
   the plant-associated and carposphere context.
