# YAML Record Review: Beach

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/beach.yaml`
- Started UTC: 2026-09-21T19:57:00Z
- Finished UTC: 2026-09-21T19:58:31Z
- Verdict: pass with minor issues

## Target

| Field | Value |
|---|---|
| Identifier | `ENVO:00000091` |
| Label | `beach` |
| Class | `HabitatRecord` |
| Category | `AQUATIC` |
| Generated or maintained | Generated from `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, `data/raw/gold_ecosystem_paths.tsv`, `data/raw/prego_habitats.tsv`, `data/raw/prego_habitat_taxa.tsv`, `curation/decisions.tsv`, and `data/habitats/PATHS.tsv`; do not hand-edit |
| Grounding status | `EXACT` |
| Mapping status | `REVIEWED` |
| Source concepts | GOLD `gold.ecosystem:4017`, PREGO `ENVO:00000091` |
| Locked stem | `beach` in `data/habitats/PATHS.tsv` |

The record resolves ENVO `ENVO:00000091` `beach`, preserves PREGO's native
`ENVO:00000091` source concept, and merges GOLD
`Environmental > Aquatic > Marine > Intertidal zone > Beach/Shore` through the
item-depth `curation/decisions.tsv` grounding row for
`habitatmech:GOLD.f37e2809fc`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/beach.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/beach.yaml --quiet` | Pass: 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Pass: 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Pass: committed term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: 3,206 expected records, 3,206 found, 0 missing, 0 extra, 0 differing. |
| Reference validator | Not available: `justfile` exposes no reference-specific validator, and this record has no `EvidenceItem`, `EnvironmentalParameter.reference`, `CharacteristicTaxon.reference`, or causal-edge evidence to target. |

## Identity and Grounding

| Claim | Evidence | Assessment |
|---|---|---|
| The record denotes ENVO's `beach` class. | `data/raw/ontology_terms.tsv` has `ENVO:00000091`, ontology `ENVO`, label `beach`, the ENVO shoreline-landform definition, and `directly_referenced=TRUE`. The generated YAML uses the same identifier, label, definition, and `definition_source: ENVO`. | Supported exactly. |
| GOLD `Beach/Shore` is deliberately merged into ENVO `beach`. | `curation/decisions.tsv` has an item-depth `GROUND` row for `habitatmech:GOLD.f37e2809fc` with `object_id=ENVO:00000091`, `object_label=beach`, and `grounding_status=EXACT`; its note names the exact GOLD path `Environmental > Aquatic > Marine > Intertidal zone > Beach/Shore`. | Supported exactly. |
| PREGO's source concept is the same ontology class and has item-level review. | `data/raw/prego_habitats.tsv` has `prego_id=ENVO:00000091`, ontology `ENVO`, `biolink_category=biolink:OntologyClass`, `taxon_count=24`, `direct_assertion_count=25`, `max_prego_score=4`, and `channels=annotated_genomes_isolates`. `curation/decisions.tsv` has an item-depth `REVIEW` row for the corresponding PREGO source concept `habitatmech:PREGO.713e2d516f`. | Supported exactly. |
| The generated `source_attestations` rows preserve the GOLD and PREGO support. | `data/raw/gold_ecosystem_paths.tsv` gives the Beach/Shore path one GOLD node ID, 75 organisms, and 75 total assertions at `gold.ecosystem:4017`; `data/raw/prego_habitats.tsv` gives `ENVO:00000091` 24 associated taxa and maximum PREGO score 4. The generated YAML carries one GOLD attestation with `assertion_count: 75`, `assertion_unit: ORGANISM`, `source_id: gold.ecosystem:4017`, and `mapping_predicate: skos:exactMatch`, plus one PREGO attestation with `assertion_count: 24`, `assertion_unit: TAXON`, and `score: 4.0`. | Supported exactly. |
| The generated parents are strictly broader in their owning source paths. | `data/raw/ontology_subclass_edges.tsv` places ENVO `beach` under `ENVO:01001884` `surface landform`; the GOLD Beach/Shore source is a child of `Environmental > Aquatic > Marine > Intertidal zone`, which is generated as `habitatmech:GOLD.115edc36f8` `Intertidal zone`. | Supported exactly. |

## Evidence

The record has no curator-authored `evidence`, `environmental_parameters`,
`causal_graphs`, `discussions`, or `datasets`.

| Claim | Evidence | Assessment |
|---|---|---|
| The 24 generated taxon rows are the complete PREGO direct taxon set for this habitat. | `data/raw/prego_habitat_taxa.tsv` has exactly 24 `ENVO:00000091` rows with ranks 1 through 24. The generated YAML carries the same rank order, NCBITaxon IDs, labels, scores, and `candidate_pool: 24` on each of those 24 rows. | Supported exactly. |
| The taxa are observational associations, not curated characteristic exemplars. | `src/habitatmech/schema/habitatmech.yaml` documents seeded `characteristic_taxa` as observational, and none of the 24 PREGO rows in the YAML has `is_characteristic: true` or a literature `reference`. | Supported exactly. |
| The generated ENVO, GOLD, and PREGO synonyms are source-owned. | The ENVO exact synonyms in the YAML match the vendored `data/raw/ontology_terms.tsv` synonym column; the PREGO related synonyms match `data/raw/prego_habitats.tsv` `prego_synonyms` values, excluding the preferred `beach` label; and the GOLD `Beach/Shore` exact synonym follows from the reviewed GOLD merge. | Supported exactly, with the synonym-scope caution called out below. |

## Completeness

The record is complete enough as a generated, reviewed GOLD/PREGO/ENVO merge. It
preserves the ENVO identity and definition, both generated source attestations,
the item-depth GOLD grounding event, the item-depth PREGO review event, all 24
PREGO taxon associations, both generated parents, and the seed curation event.

Empty optional fields are correct for the current maintained inputs:

- no `curation/causal_graphs/beach.yaml` overlay exists;
- no `curation/term_requests.tsv`, `curation/term_requests_excluded.tsv`, or
  `curation/external_xrefs.tsv` row is keyed by `ENVO:00000091`;
- the relevant `curation/decisions.tsv` rows are keyed by the GOLD and PREGO
  source concepts, not by the merged ENVO target;
- no `history/` curation-session record names `ENVO:00000091`, the GOLD source
  concept, the PREGO source concept, or the `beach` slug; and
- no target-specific report exists under `research/habitats`.

Hidden- and ignored-file-inclusive searches covered `curation`, `history`,
`research/habitats`, `reports/yaml_record_review`, `data/raw`,
`data/habitats/PATHS.tsv`, and `data/habitats` for `ENVO:00000091`,
`gold.ecosystem:4017`, `habitatmech:GOLD.f37e2809fc`,
`habitatmech:PREGO.713e2d516f`, `habitatmech:GOLD.115edc36f8`, and
`Beach/Shore`; `find` lookups covered the `beach` slug under `curation`,
`history`, `research/habitats`, and `reports/yaml_record_review`. These checks
found the expected raw ontology, raw GOLD, raw PREGO, generated `PATHS.tsv`,
generated YAML, GOLD grounding, and PREGO review entries, and found no
Beach-owned causal overlay, term request, external xref, history record,
research report, or previous `*-beach.md` YAML review.

The vendored ENVO slice carries five direct local `beach` children:
`ENVO:00000092` `sea beach`, `ENVO:00000093` `lacustrine beach`,
`ENVO:00000415` `raised beach`, `ENVO:00000545` `shingle beach`, and
`ENVO:00002199` `sandy beach`. Their labels and definitions are compatible with
`ENVO:00000091` as the generated parent.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| `HM-BEACH-001` | Minor | Some generated synonyms for `beach` are source-owned strings whose exactness is not locally re-reviewed. ENVO contributes exact synonyms that include the plural `beaches` and narrower terms such as `beach berm` and `lagoon beach`, while PREGO contributes mechanical related variants such as `beacheses`, `beachic`, `foreshore flatses`, `foreshore flatsic`, and `lagoon beachic`. The generated record is faithful to the raw and reviewed source rows, but consumers should not treat those exact/related scopes as a HabitatMech-curated synonym audit. | `data/raw/ontology_terms.tsv` lists the ENVO exact synonym payload for `ENVO:00000091`; `data/raw/prego_habitats.tsv` lists the PREGO `prego_synonyms`; `data/habitats/aquatic/beach.yaml` copies them into `synonyms` with `source: ENVO`, `source: GOLD`, or `source: PREGO`. | Upstream ENVO/PREGO extraction into `data/raw/ontology_terms.tsv` and `data/raw/prego_habitats.tsv`; no per-record HabitatMech curation input currently owns synonym suppression. |

No blocker or major findings found.

## Recommended Edits

1. Leave `data/habitats/aquatic/beach.yaml` generated and do not add a
   `curation/decisions.tsv` row merely to restate that PREGO `ENVO:00000091`
   is exactly ENVO `beach`.
2. If broad, narrow, or mechanically inflected synonyms create false-positive
   lookup behavior, add a maintained synonym-suppression mechanism for
   source-owned ontology/PREGO synonyms or fix the upstream vocabulary rows,
   then refresh the raw inventories and regenerate with
   `just seed-canary ENVO:00000091 --force`.

## Follow-up Checks

- `just seed`
- `just seed-canary ENVO:00000091 --force`
- `just validate data/habitats/aquatic/beach.yaml`
- `just validate-strict data/habitats/aquatic/beach.yaml --quiet`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

After any synonym-filtering change, re-read `data/habitats/aquatic/beach.yaml`
and confirm the definition, GOLD attestation, PREGO attestation, parents, and 24
PREGO taxa are unchanged unless the raw PREGO, raw GOLD, or ENVO source rows
changed.

## Additional Notes

This was a read-only YAML review. It did not edit generated habitat YAML, append
curation history, promote the review status, create an issue, or run paid
definition research.

`data/raw/gold_path_triads.tsv` has an unrelated local triad proposal from the
GOLD `Environmental > Terrestrial > Soil > Sand` path to `ENVO:00000091`; no
generated Beach field is owned by that proposal row.
