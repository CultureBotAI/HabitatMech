# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/aerobic_media.yaml
- Started UTC: 2026-09-23T18:09:38Z
- Finished UTC: 2026-09-23T18:12:20Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | habitatmech:GOLD.d548daa79b |
| Label | Aerobic media |
| Class | HabitatRecord |
| Category | ENGINEERED |
| Grounding status | UNGROUNDED |
| Mapping status | SEEDED |
| Maintained owner | generated from `data/raw/gold_ecosystem_paths.tsv`, `curation/decisions.tsv`, and `data/habitats/PATHS.tsv` |

[data/habitats/engineered/aerobic_media.yaml](../../data/habitats/engineered/aerobic_media.yaml) is the generated GOLD record for `Engineered > Lab enrichment > Defined media > Aerobic media`, whose deterministic source-concept key is `habitatmech:GOLD.d548daa79b`.

The target was resolved with ignored-file-inclusive searches:

- `find data/habitats -type f -name 'aerobic_media.yaml' -print` found exactly one generated YAML record.
- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-aerobic_media.md' -print` found no prior exact review report.
- `find curation/causal_graphs -maxdepth 1 -type f -name 'aerobic_media.yaml' -print` found no causal-graph overlay for this record.
- `rg --no-ignore --hidden -n --glob '!data/text_map/**' --glob '!pages/**' --glob '!build/**' --glob '!reports/yaml_record_review/**' 'habitatmech:GOLD\.d548daa79b|Engineered > Lab enrichment > Defined media > Aerobic media|gold\.ecosystem:3855|gold\.ecosystem:4288|aerobic_media' curation history research reports data/habitats/PATHS.tsv data/raw` found the slug lock, the class-level `curation/decisions.tsv` row for this GOLD source concept, and raw GOLD rows; it found no item-level decision, maintained causal overlay, history record, term request, exclusion row, or aerobic-media research report.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/aerobic_media.yaml` | pass; no LinkML issues found |
| `just validate-strict data/habitats/engineered/aerobic_media.yaml` | pass; 1 file scanned, 0 files with ERROR, 0 total ERROR rows |
| `just validate-causal curation/causal_graphs/aerobic_media.yaml` | not applicable; exact `find curation/causal_graphs -maxdepth 1 -type f -name 'aerobic_media.yaml' -print` found no overlay |
| `just validate-causal-all` | pass; 32 causal-graph curation files with 32 graphs validated |
| Reference validator | not applicable; this generated GOLD record has no causal graph and no DOI, PMID, or URL evidence slots to inspect |
| `just term-requests-check` | pass; term-request table is current with 109 terms |
| `just validate-history` | pass; no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | pass; 3206 expected, 3206 found, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | pass; 0 ungrounded records still undecided; 1810 decisions on file |
| `just report` | pass; 3206 records; no global issue specific to `habitatmech:GOLD.d548daa79b` surfaced |
| `git diff --check` | pass |

## Identity and Grounding

The minted identity is faithful to the GOLD source concept. `data/raw/gold_ecosystem_paths.tsv` has the canonical path `Engineered > Lab enrichment > Defined media > Aerobic media` at depth 4 with `leaf_label` `Aerobic media`, 2 collapsed GOLD node ids, first node id `gold.ecosystem:3855`, and 6 direct GOLD organism assertions. `src/habitatmech/seed.py` mints the source concept as the first ten SHA1 hex digits of `GOLD:Engineered > Lab enrichment > Defined media > Aerobic media`, yielding `habitatmech:GOLD.d548daa79b`.

`mapping_status: SEEDED` is correct for the current curation input. `curation/decisions.tsv` has a `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.d548daa79b`, but its `review_depth` is `CLASS`, not `ITEM`; the row records that no vendored ontology term matched by the class-level lexical sweep and explicitly says that whether the concept is a habitat was not assessed.

No exact ontology identity is maintained for the aerobic-media concept. The closest vendored match found by focused search is `BTO:0000316` `culture medium`, defined as a substance used for cultivation, isolation, identification, or storage of microorganisms. That term is strictly broader than GOLD's aerobic-medium source concept and is already used as the exact identity for GOLD's `Engineered > Lab culture > Culture media` path and as the broader parent for several narrower culture-medium paths in `curation/decisions.tsv`.

The current `parent_habitats` value is a generated source-path parent. `habitatmech:GOLD.a60b6f216d` is `Defined media`, generated from `Engineered > Lab enrichment > Defined media`. `Aerobic media` is narrower than that path parent, but the target has no ontology parent anchoring it under `BTO:0000316` `culture medium`.

## Evidence

This record has one GOLD source attestation:

- The `GOLD` attestation mirrors `data/raw/gold_ecosystem_paths.tsv`: source id `gold.ecosystem:3855`, source label `Aerobic media`, source path `Engineered > Lab enrichment > Defined media > Aerobic media`, and `assertion_count: 6` with `assertion_unit: ORGANISM`.
- The raw row's `gold_node_count` is `2` and its `gold_node_ids` field is `gold.ecosystem:3855|gold.ecosystem:4288`, matching the generated de-duplication note.
- Separate raw GOLD study and biosample indexes mention this source path, but the seeder intentionally populates GOLD attestation counts from `gold_ecosystem_paths.tsv` `organism_count`, not those auxiliary counts.

No evidence-backed causal mechanism evidence is present.

Unsupported or over-scoped claims:

- None; the generated source-owned fields agree with the committed GOLD inventory and the current curation input.

## Completeness

The record carries the generated GOLD source-owned content for this source concept: the first GOLD ecosystem id, source label, source path, duplicate-node note, organism assertion count and unit, class-level sweep history, generated seeding history, and generated slug lock.

The hierarchy is incomplete because the record is a culture-medium habitat without `BTO:0000316` as a broader ontology parent. Exact ignored-file-inclusive search found no item-level decision or authored definition for this source concept.

Optional characteristic-taxon, environmental-parameter, discussion, dataset, and causal-graph slots are correctly absent; no maintained causal overlay, history entry, research report, or item-level curated review row adds any of those fields.

## Findings

- **major**: `habitatmech:GOLD.d548daa79b` is missing the broader parent `BTO:0000316` `culture medium`.

  **Evidence**: the source path `Engineered > Lab enrichment > Defined media > Aerobic media` denotes a culture medium constrained by an aerobic lab-enrichment condition. `BTO:0000316` is the vendored `culture medium` term, and `curation/decisions.tsv` already attaches it as the identity or broader parent for item-reviewed GOLD culture-medium paths. The target has only the class-level `CONFIRM_UNGROUNDED` sweep row, so the seeder never adds this safe broader ontology parent or promotes the record to `NARROW`.

  **Maintained owner**: add an item-level `GROUND_AS_PARENT` row for `habitatmech:GOLD.d548daa79b` to `curation/decisions.tsv`.

## Recommended Edits

- Item-review `habitatmech:GOLD.d548daa79b` in `curation/decisions.tsv`; if no exact vendored aerobic-medium term exists, retain the minted identity but set `decision: GROUND_AS_PARENT`, `object_id: BTO:0000316`, `object_label: culture medium`, `grounding_status: NARROW`, and `review_depth: ITEM`.
- Regenerate the affected source concept and inspect [data/habitats/engineered/aerobic_media.yaml](../../data/habitats/engineered/aerobic_media.yaml) to confirm `BTO:0000316` appears in `parent_habitats` and the source-path `Defined media` parent remains.

## Follow-up Checks

The narrow proof path for a future fix is:

- update `curation/decisions.tsv` with one item-level `GROUND_AS_PARENT` row for `habitatmech:GOLD.d548daa79b`
- `just seed`
- `just seed-canary habitatmech:GOLD.d548daa79b`
- inspect [data/habitats/engineered/aerobic_media.yaml](../../data/habitats/engineered/aerobic_media.yaml) and confirm `grounding_status: NARROW`, `mapping_status: REVIEWED`, `BTO:0000316` in `parent_habitats`, and an item-level `GROUND_AS_PARENT` curation-history event
- `just validate-strict data/habitats/engineered/aerobic_media.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- ignored-file-inclusive exact searches for `habitatmech:GOLD.d548daa79b`, `Engineered > Lab enrichment > Defined media > Aerobic media`, and `BTO:0000316`
- `git diff --check`

## Additional Notes

The exact ignored-file-inclusive search also matched the `anaerobic_media` slug lock through the substring `aerobic_media`; that is a distinct GOLD record for `Engineered > Lab enrichment > Defined media > Anaerobic media` and does not affect this review.
