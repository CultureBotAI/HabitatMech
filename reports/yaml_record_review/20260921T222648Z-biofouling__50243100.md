# YAML Record Review: Biofouling

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/biofouling__50243100.yaml`
- Started UTC: 2026-09-21T22:25:55Z
- Finished UTC: 2026-09-21T22:26:48Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.ec116f9a37` |
| Label | `Biofouling` |
| Category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `REVIEWED` |
| Maintained inputs | `data/raw/gold_ecosystem_paths.tsv`, `curation/decisions.tsv`, `curation/term_requests_excluded.tsv`, `data/habitats/PATHS.tsv` |

This is a generated GOLD record. The path lock maps `habitatmech:GOLD.ec116f9a37` to the `biofouling__50243100` slug, and the raw GOLD row maps `Environmental > Aquatic > Non-marine Saline and Alkaline > Biofouling` to the `Biofouling` leaf at depth 4 with two collapsed GOLD ecosystem nodes, `gold.ecosystem:8361|gold.ecosystem:8362`, and zero organisms, studies, biosamples, or total assertions.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/aquatic/biofouling__50243100.yaml` | Pass; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/biofouling__50243100.yaml --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Pass; the committed term-request table is current with 109 terms. |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass; 3,206 expected records, 3,206 records on disk, and 0 missing, extra, or differing records. |

`just validate-products` was not run. This review did not add or change a grounding row, and the record's ontology xref, `ENVO:06105023`, was checked directly in the vendored `data/raw/ontology_terms.tsv` slice during the Biofouling cohort review.

## Identity and Grounding

The generated identity is internally consistent:

- `data/habitats/PATHS.tsv` pins `habitatmech:GOLD.ec116f9a37` to `biofouling__50243100`, matching the reviewed YAML path.
- `data/raw/gold_ecosystem_paths.tsv` carries the exact source path and both collapsed source nodes copied into `source_attestations`: `gold.ecosystem:8361` is the first shown node and `gold.ecosystem:8362` is noted by count.
- `curation/decisions.tsv` has an item-level `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.ec116f9a37` with `ENVO:06105023` retained through `relation: xref`.
- `ENVO:06105023` is the ENVO biofouling process term, so it is correctly excluded from `parent_habitats` and retained only in `xrefs`.

The generated parent is supported. `parent_habitats` points to `habitatmech:GOLD.ce244e62cd`, the reviewed and defined `inland saline or alkaline aquatic environment` record. Its curated definition covers aquatic environments determined by inland water bodies whose water is saline, alkaline, or both, which is broad enough for GOLD's zero-assertion inland saline-or-alkaline Biofouling grouping node.

The record is also correctly left ungrounded to `ENVO:00002034` `biofilm`. Its concrete child, `Environmental > Aquatic > Non-marine Saline and Alkaline > Biofouling > Microfouling/Biofilm`, has its own raw GOLD row and is already grounded to `ENVO:00002034` as part of the generated `data/habitats/aquatic/biofilm__cc6fe05a.yaml` merge.

## Evidence

Supported claims:

- The `Biofouling` label, `GOLD` source, `gold.ecosystem:8361` source id, full source path, and two-node note come from `data/raw/gold_ecosystem_paths.tsv`.
- `habitat_category: AQUATIC` follows from the GOLD `Environmental > Aquatic` source path.
- `parent_habitats: habitatmech:GOLD.ce244e62cd` follows from the curated parent record that owns the immediate GOLD `Environmental > Aquatic > Non-marine Saline and Alkaline` path.
- `grounding_status: UNGROUNDED`, `mapping_status: REVIEWED`, and `xrefs: ENVO:06105023` follow from the `CONFIRM_UNGROUNDED`, `ITEM`, `xref` decision row for `habitatmech:GOLD.ec116f9a37`.
- `curation/term_requests_excluded.tsv` explicitly excludes this parent leaf from HabitatMech term requests because it has zero GOLD organisms, studies, and biosamples, and because its only concrete `Microfouling/Biofilm` child already grounds to `ENVO:00002034` `biofilm`.

Unsupported or over-scoped claims: None found.

The record has no definition, record-level evidence, environmental parameters, characteristic taxa, causal graphs, discussion links, or datasets. That absence is supported: ignored-file-inclusive searches found no exact `Environmental > Aquatic > Non-marine Saline and Alkaline > Biofouling` or `gold.ecosystem:8361` entries in `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, or `data/raw/gold_studies.tsv`, and no exact `habitatmech:GOLD.ec116f9a37` entry in `curation/term_requests.tsv` or `curation/causal_graphs/`.

## Completeness

The record is intentionally sparse and complete enough for the unused GOLD parent leaf it preserves:

- No Biofouling definition is expected because `curation/term_requests_excluded.tsv` intentionally declines a new term request for this zero-assertion grouping bin.
- No GOLD assertion count is expected because the raw `organism_count`, `study_count`, `biosample_count`, and `total_assertions` are all zero.
- No GOLD MIxS triads or study rows are expected; the exact source path and first source id are absent from the committed GOLD API TSVs named above.
- No causal graph is expected; the record has no sample-derived or literature-derived mechanism claims to support.

All negative checks above used `rg --no-ignore --hidden` or `find`, so ignored files were included where they were in the searched path.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

If a future curation change touches this record or its Biofouling cohort, rerun:

- `just validate data/habitats/aquatic/biofouling__50243100.yaml`
- `just validate-strict data/habitats/aquatic/biofouling__50243100.yaml --quiet`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`

## Additional Notes

An ignored-file-inclusive `find` search of `reports/yaml_record_review/` found no prior report for this `biofouling__50243100` record before this file was written.
