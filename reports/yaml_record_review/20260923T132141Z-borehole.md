# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/borehole.yaml`
- Started UTC: 2026-09-23T13:19:00Z
- Finished UTC: 2026-09-23T13:21:41Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/borehole.yaml` |
| Class | `HabitatRecord` |
| Identifier | `ENVO:00002226` |
| Label | `borehole` |
| Category | `AQUATIC` |
| Grounding status | `EXACT` |
| Mapping status | `REVIEWED` |
| Maintained inputs | `curation/decisions.tsv`, `data/raw/gold_ecosystem_paths.tsv`, `data/raw/prego_habitats.tsv`, `data/raw/prego_habitat_taxa.tsv`, `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, `data/habitats/PATHS.tsv` |
| Generated output | Yes. `data/habitats/` records are emitted from the raw inventories and curation decisions by `src/habitatmech/seed.py`; future fixes belong in those maintained inputs or in a new maintained source-path parent suppression surface consumed by the seeder. |

The full record was reviewed. It merges the item-reviewed GOLD source concept `habitatmech:GOLD.61d35511fd` with the item-reviewed PREGO source concept `habitatmech:PREGO.c2d50e2194` on the exact ENVO identity `ENVO:00002226`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/borehole.yaml` | Pass: LinkML validation reported no issues. |
| `just validate-strict data/habitats/aquatic/borehole.yaml` | Pass: 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Pass: 32 causal-graph curation files and 32 graphs validated. |
| `just term-requests-check` | Pass: committed term-request table is current at 109 terms. |
| `just validate-history` | Pass: no issues found; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: 3206 expected records, 3206 records found, 0 missing, 0 extra, 0 differing; the corpus reproduces exactly from `data/raw/`. |
| `just worklist --limit 2000` | Pass: 0 ungrounded records still undecided; 1810 decisions on file. |
| `just report` | Pass: full corpus report completed for 3206 records. |
| `git diff --check` | Pass: no whitespace errors. |
| Reference validator | Not applicable: this record has no reference-bearing `evidence`, `causal_graphs`, `environmental_parameters.reference`, `discussions`, or `datasets`, and HabitatMech exposes no focused reference validator for such a record. |

## Identity and Grounding

| Assertion | Review |
|---|---|
| `identifier: ENVO:00002226` / `label: borehole` | Supported. `data/raw/ontology_terms.tsv` carries `ENVO:00002226` as an ENVO term with canonical label `borehole` and the definition emitted on this record. |
| GOLD source identity | Supported. `data/raw/gold_ecosystem_paths.tsv` has one canonical path, `Environmental > Aquatic > Deep subsurface > Groundwater > Drilled well/Borehole`, with leaf `Drilled well/Borehole`, 13 organism assertions, and node `gold.ecosystem:5958`. |
| GOLD exact grounding | Supported. `curation/decisions.tsv` grounds `habitatmech:GOLD.61d35511fd` to `ENVO:00002226` with `GROUND`, `EXACT`, and `review_depth: ITEM`; the slash leaf explicitly names a borehole alternative. |
| PREGO source identity | Supported. `data/raw/prego_habitats.tsv` has one `ENVO:00002226` row with one distinct taxon, one direct assertion, score 3, channel `annotated_genomes_isolates`, and PREGO strings `boreholar|borehole|boreholes`. |
| PREGO exact review | Supported. PREGO rows self-ground to their ENVO/BTO CURIEs, and `curation/decisions.tsv` carries an item-level `REVIEW` for `habitatmech:PREGO.c2d50e2194`, so every contributing source concept is reviewed and `mapping_status: REVIEWED` is justified. |
| ENVO parents | Supported. `data/raw/ontology_subclass_edges.tsv` asserts `ENVO:00002226 rdfs:subClassOf ENVO:00000070` and `ENVO:00002226 rdfs:subClassOf ENVO:03000117`, matching the generated `human construction` and `channel` parents. |
| GOLD source-path parent | Not supported as an `is-a` parent. The immediate GOLD parent path `Environmental > Aquatic > Deep subsurface > Groundwater` generates `parent_habitats: habitatmech:GOLD.22a80cbd14`, whose record represents the GOLD `Groundwater` bin. A constructed channel is not a kind of groundwater. |
| Path slug | Supported. `data/habitats/PATHS.tsv` maps `ENVO:00002226` to `borehole`. |

The exact identity is the borehole itself, not a recovered core, well sediment, or groundwater material. The GOLD path carries groundwater context, and the GOLD MIxS triads agree by using `ENVO:00002226` `borehole` as the local feature and `ENVO:01001004` `groundwater` as the medium; that supports the exact identity while also showing that the generated `Groundwater` path parent is contextual rather than strictly broader.

## Evidence

| Record field | Nearest maintained evidence | Review |
|---|---|---|
| GOLD `source_attestations[0]` | `data/raw/gold_ecosystem_paths.tsv` row for `Environmental > Aquatic > Deep subsurface > Groundwater > Drilled well/Borehole`; `curation/decisions.tsv` row `habitatmech:GOLD.61d35511fd` | Supported. Source path, source ID, label, exact predicate, count 13, and organism unit match the raw row and reviewed exact grounding. |
| PREGO `source_attestations[1]` | `data/raw/prego_habitats.tsv` row for `ENVO:00002226` | Supported. Source ID, source label, `assertion_count`, score, and channel match the raw PREGO row. |
| `characteristic_taxa[0]` | `data/raw/prego_habitat_taxa.tsv` row for `ENVO:00002226` | Supported as a reported PREGO association only. `NCBITaxon:477974` / `Candidatus Desulforudis audaxviator MP104C`, score 3, rank 1, and candidate pool 1 match the only PREGO taxon row for this habitat; no `is_characteristic` stronger claim is emitted. |
| PREGO synonyms | `data/raw/prego_habitats.tsv` `prego_synonyms` for `ENVO:00002226` | Supported. The seeder suppresses the source synonym equal to the record label and retains `boreholar` and `boreholes` as PREGO `RELATED_SYNONYM` entries. |
| Definition | `data/raw/ontology_terms.tsv` row for `ENVO:00002226` | Supported. The definition and `definition_source: ENVO` are copied from the vendored ENVO row. |
| Causal graph evidence | `curation/causal_graphs/*.yaml` | Correctly absent. Ignored-inclusive searches of `curation/`, `history/`, `research/`, and prior review reports found no maintained borehole causal overlay or borehole-specific research report. |

## Completeness

The exact grounding and two-source merge are complete enough, but the generated hierarchy is not semantically complete while the contextual GOLD groundwater parent is represented as a strict `parent_habitats` edge.

| Area | Review |
|---|---|
| Environmental parameters | None expected; exact searches found no `ENVO:00002226`, `borehole`, or `Drilled well/Borehole` row in a maintained parameter input. |
| Causal graphs | None expected for the current record; no overlay targets `ENVO:00002226`. |
| Discussions or datasets | None expected in the generated record; no curator-authored open question or dataset input targets this habitat. |
| Prior reports | `find reports/yaml_record_review -maxdepth 1 -type f -name '*-borehole.md' -print` found no earlier exact report; the search included ignored files through `find`. |
| Existing curation | Ignored-inclusive exact searches found the two expected `curation/decisions.tsv` rows and no borehole row in `curation/term_requests.tsv` or `curation/causal_graphs/`. |
| External research | Ignored-inclusive exact searches found only near-miss mentions in core-sample and rock-core/sediment reports; those reports distinguish boreholes from recovered cores or subsurface solid material and do not provide borehole-specific mechanism evidence. |

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| HM-BOREHOLE-001 | major | `parent_habitats` asserts that exact ENVO `borehole` is a child of the generated GOLD `Groundwater` record. The raw GOLD path explains where the edge came from, but the relation is contextual: a borehole is a constructed channel that can contain groundwater, not a kind of groundwater. | `data/raw/gold_ecosystem_paths.tsv` nests `Drilled well/Borehole` under `Groundwater`; `data/raw/gold_path_triads.tsv` makes the distinction explicit by annotating the same path with local `ENVO:00002226` `borehole` and medium `ENVO:01001004` `groundwater`; the generated `groundwater.yaml` parent record denotes the GOLD groundwater bin. | `src/habitatmech/seed.py`, or a new maintained source-path parent suppression table consumed by the seeder. |

## Recommended Edits

| Priority | Edit | Owner | Proof |
|---|---|---|---|
| 1 | Add maintained support for retracting false generated GOLD source-path parents independently of exact identity review, then suppress the edge from `habitatmech:GOLD.61d35511fd` / `ENVO:00002226` to `habitatmech:GOLD.22a80cbd14`. | New curation table plus `src/habitatmech/seed.py` support. | `data/habitats/aquatic/borehole.yaml` still keeps `ENVO:00000070` and `ENVO:03000117`, no longer lists `habitatmech:GOLD.22a80cbd14`, and still has both reviewed GOLD and PREGO curation events. |

Do not hand-edit `data/habitats/aquatic/borehole.yaml`; it reproduces from the maintained inputs and `just verify-corpus` would reject a one-off YAML edit.

## Follow-up Checks

After implementing the maintained edge suppression:

- `just seed`
- `just seed-canary ENVO:00002226`
- inspect `data/habitats/aquatic/borehole.yaml`
- `just validate data/habitats/aquatic/borehole.yaml`
- `just validate-strict data/habitats/aquatic/borehole.yaml`
- `just verify-corpus --max-diffs 1`
- `just report`
- `git diff --check`

## Additional Notes

- Exact searches for absence checks used `find` or `rg --no-ignore --hidden`, so ignored files were included.
- `data/raw/prego_habitat_taxa.tsv` also associates `NCBITaxon:477974` with `BTO:0001313`, `ENVO:00001998`, and `ENVO:00002168`; those are separate PREGO observations and do not conflict with the borehole association.
