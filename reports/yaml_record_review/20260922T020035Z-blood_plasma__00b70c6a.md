# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/blood_plasma__00b70c6a.yaml`
- Started UTC: 2026-09-22T02:00:35Z
- Finished UTC: 2026-09-22T02:00:35Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `UBERON:0001969` |
| Label | `blood plasma` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the vendored UBERON ontology slice, `data/raw/gold_ecosystem_paths.tsv`, and `data/habitats/PATHS.tsv`; future curation belongs in `curation/decisions.tsv`, `curation/term_requests.tsv`, or `curation/causal_graphs/*.yaml`, not in this generated YAML |
| Locked slug | `data/habitats/PATHS.tsv:1050` maps `UBERON:0001969` to `blood_plasma__00b70c6a` |

This is the UBERON-grounded GOLD blood-plasma record generated by merging the Mammals: Human and Mammals `Circulatory system > Blood > Plasma` leaves. It is intentionally separate from `data/habitats/host_associated/blood_plasma.yaml`, the BTO `blood plasma` record seeded from PREGO.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/blood_plasma__00b70c6a.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/blood_plasma__00b70c6a.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors |

## Identity and Grounding

The generated exact UBERON identity is supported:

- `data/raw/ontology_terms.tsv:13094` defines `UBERON:0001969` with canonical label `blood plasma`, definition `The liquid component of blood, in which erythrocytes are suspended.`, and exact synonyms `blood plasm`, `plasma`, `portion of blood plasma`, and `portion of plasma`.
- `data/raw/ontology_subclass_edges.tsv:11281` asserts `UBERON:0001969 rdfs:subClassOf UBERON:0000179`; `data/raw/ontology_terms.tsv:12926` labels `UBERON:0000179` as `haemolymphatic fluid`.
- `data/raw/gold_ecosystem_paths.tsv:752` maps `Host-associated > Mammals: Human > Circulatory system > Blood > Plasma` to the `Plasma` leaf, `gold.ecosystem:4416`, and 3 organism assertions.
- `data/raw/gold_ecosystem_paths.tsv:1996` maps `Host-associated > Mammals > Circulatory system > Blood > Plasma` to the `Plasma` leaf, `gold.ecosystem:6817`, and 0 assertions.

The two GOLD source concepts resolve through the host-associated composed-label route. Hashing their full canonical paths gives stable source keys `habitatmech:GOLD.941ff9f450` and `habitatmech:GOLD.b81daaf43c`; there are no item-level rows for either key in `curation/decisions.tsv`, so the generated `mapping_status: SEEDED` is expected.

All parents are also supported. `UBERON:0000179` comes from the vendored UBERON subclass edge. `habitatmech:GOLD.d25ff4a586` is the immediate Mammals: Human `Blood` GOLD parent, `habitatmech:GOLD.6c6aab19d3` is the immediate Mammals `Blood` GOLD parent, and `data/habitats/PATHS.tsv` locks those parent identifiers to the already generated `blood__5ef3f2f4` and `blood__c1b484fe` records.

## Evidence

Every generated scientific claim in this record is source-derived:

| Claim | Nearest source | Review |
|---|---|---|
| UBERON label, definition, exact synonyms, and haemolymphatic-fluid parent | `data/raw/ontology_terms.tsv:12926`, `data/raw/ontology_terms.tsv:13094`, and `data/raw/ontology_subclass_edges.tsv:11281` | Supported exactly |
| Human mammal GOLD attestation for `Plasma` | `data/raw/gold_ecosystem_paths.tsv:752` | Supported exactly; the generated source ID, source path, `skos:exactMatch`, 3-organism count, and host-associated category all match the raw row |
| General mammal GOLD attestation for `Plasma` | `data/raw/gold_ecosystem_paths.tsv:1996` | Supported exactly; the 0-assertion raw row correctly generates no `assertion_count` |
| The generated `Plasma` synonym | Both GOLD rows use the `Plasma` source label | Supported; the source label is retained as a GOLD `EXACT_SYNONYM` |
| The two GOLD `Blood` parent records | `data/raw/gold_ecosystem_paths.tsv:7`, `data/raw/gold_ecosystem_paths.tsv:141`, and `data/habitats/PATHS.tsv:2082,2842` | Supported; blood plasma is a child of each immediate GOLD blood path |

The record has no record-level evidence, environmental parameters, characteristic taxa, causal graphs, discussion links, or datasets. That absence is consistent with the maintained inputs: no row in `curation/term_requests.tsv`, no causal overlay in `curation/causal_graphs/`, no GOLD path-triad row, and no source inventory row supplies environmental parameters, taxa, discussions, or datasets for `UBERON:0001969` or the two exact `Plasma` source keys.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- `source_attestations` captures both GOLD `Plasma` leaves that compose to UBERON `blood plasma`.
- The Mammals: Human row's three GOLD organism assertions are represented once; the Mammals row correctly omits `assertion_count` because the raw count is zero.
- The UBERON and GOLD `Plasma` synonyms are represented once each and do not duplicate the record label.
- The UBERON haemolymphatic-fluid parent and both immediate GOLD blood parents are represented in `parent_habitats`.
- `characteristic_taxa`, `causal_graphs`, `discussions`, and `datasets` are absent because no maintained source row or overlay supplies them.
- `mapping_status` stays `SEEDED`, as expected; there is no item-level curation decision for either GOLD source concept.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research/habitats`, `reports/yaml_record_review`, `docs`, `.claude`, `src`, `data/habitats/PATHS.tsv`, and exact GOLD and ontology TSVs while excluding generated `data/text_map/**` and `pages/**`. They found the raw rows, path lock, and related UBERON notes cited above, plus the previous BTO `blood_plasma` report's sibling-record notes. They found no maintained decision row, term request, history record, causal-graph overlay, target-specific research report, or prior exact YAML review report for `blood_plasma__00b70c6a`, `UBERON:0001969`, `habitatmech:GOLD.941ff9f450`, or `habitatmech:GOLD.b81daaf43c`.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if a future curation change adds reviewed decisions or a causal graph overlay for this UBERON-grounded blood-plasma record:

- `just validate data/habitats/host_associated/blood_plasma__00b70c6a.yaml`
- `just validate-strict data/habitats/host_associated/blood_plasma__00b70c6a.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If future curation touches either GOLD source concept, also recompute the path-hash keys for both `Plasma` paths and confirm the generated `source_attestations` still preserve both full `source_path` values.

## Additional Notes

- `find reports/yaml_record_review -name '*blood_plasma__00b70c6a*' -print` found no pre-existing exact review report for this record before this file was written.
- `data/raw/gold_path_biosamples.tsv` has biosample support for the Mammals: Human `Plasma` leaf; `data/raw/gold_path_triads.tsv` has no exact triad row for either `Plasma` source path. Those rows do not feed this generated record's current fields.
- `docs/HARMONIZATION.md` documents `UBERON:0001969` as one of the host-associated composed-label terms deliberately claimed by more than one GOLD path, so merging the Mammals: Human and Mammals `Plasma` leaves is an explicit seeding rule rather than an accidental same-label collapse.
