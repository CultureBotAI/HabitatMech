# YAML Record Review: Aphotic zone

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/aphotic_zone.yaml
- Started UTC: 2026-09-21T15:23:49Z
- Finished UTC: 2026-09-21T15:23:49Z
- Verdict: needs curation

## Target

The target is the generated `HabitatRecord` at `data/habitats/aquatic/aphotic_zone.yaml`.

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.fcba7e6334` |
| Label | `Aphotic zone` |
| Category | `AQUATIC` |
| Grounding status | `NARROW` |
| Mapping status | `REVIEWED` |
| GOLD source node | `gold.ecosystem:7879` |
| Source path | `Environmental > Aquatic > Freshwater > Lake > Aphotic zone` |
| Locked stem | `aphotic_zone` in `data/habitats/PATHS.tsv` |

This review covers the single GOLD source concept minted from `Environmental > Aquatic > Freshwater > Lake > Aphotic zone`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/aphotic_zone.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/aphotic_zone.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 records on disk, 0 missing, 0 extra, 0 differing. |
| Reference validator | Not checked: this record has no DOI/PMID/URL `EvidenceItem`, no causal-edge evidence, and no target-specific `curation/causal_graphs/` overlay. |

## Identity and Grounding

The record denotes GOLD path `Environmental > Aquatic > Freshwater > Lake > Aphotic zone`. `data/raw/gold_ecosystem_paths.tsv` preserves that path with leaf label `Aphotic zone`, depth 5, one GOLD node, zero GOLD assertions, and source node `gold.ecosystem:7879`.

The minted identity is appropriate for the current ontology slice. Vendored ENVO has `ENVO:00000210` `marine aphotic zone`, but that term's definition is scoped to the ocean below 200 m and the matching GOLD sibling is the separate `Environmental > Aquatic > Marine > Oceanic > Aphotic zone` path. This GOLD source path is explicitly under `Environmental > Aquatic > Freshwater > Lake`, so grounding the record to the marine term would conflate freshwater and marine habitats.

`mapping_status: REVIEWED` follows from the current item-level decision: `curation/decisions.tsv` has a `GROUND_AS_PARENT` row for `habitatmech:GOLD.fcba7e6334` at `ITEM` review depth. The decision's object, `ENVO:00000020` `lake`, is not a strict broader class for a lake aphotic zone. A lake aphotic zone is located within a lake; it is not itself a body of water or other liquid of considerable size contained in a depression on a landmass.

The generated `ENVO:00000021` `freshwater lake` parent is also false. It is inherited from the GOLD parent path `Environmental > Aquatic > Freshwater > Lake`, but `docs/CURATION.md` says `parent_habitats` is an is-a claim and must never be used merely to preserve an upstream link.

Exact hidden/ignored-inclusive searches before this report was created covered `research/habitats/`, `reports/yaml_record_review/`, `history/`, `curation/term_requests.tsv`, `curation/causal_graphs/`, and `data/raw/`; they found only the GOLD inventory row cited here for `habitatmech:GOLD.fcba7e6334`, `gold.ecosystem:7879`, and `Environmental > Aquatic > Freshwater > Lake > Aphotic zone`. A separate hidden/ignored-inclusive search of `reports/yaml_record_review/` for `aphotic` found no prior YAML review report.

## Evidence

| Claim | Evidence | Assessment |
|---|---|---|
| GOLD attests one `Aphotic zone` source concept at `gold.ecosystem:7879`. | `data/raw/gold_ecosystem_paths.tsv` has `Environmental > Aquatic > Freshwater > Lake > Aphotic zone`, leaf label `Aphotic zone`, `gold_node_count=1`, and `gold_node_ids=gold.ecosystem:7879`. | Supported. |
| The generated source attestation correctly omits an `assertion_count`. | The GOLD path row has `organism_count=0`, `study_count=0`, `biosample_count=0`, and `total_assertions=0`, and exact searches found no `data/raw/gold_path_biosamples.tsv` row for this freshwater-lake path. | Supported. |
| The minted record should not exact-ground to `marine aphotic zone`. | `data/raw/ontology_terms.tsv` defines `ENVO:00000210` as the aphotic zone of an ocean; GOLD also has a separate marine/oceanic `Aphotic zone` source path, which is already grounded to `ENVO:00000210` through `habitatmech:GOLD.de5b5dffd1`. | Supported. |
| The current `GROUND_AS_PARENT` decision was applied. | `curation/decisions.tsv` has an `ITEM` row keyed to `habitatmech:GOLD.fcba7e6334`, and the generated record has `grounding_status: NARROW`, a `skos:narrowMatch` source attestation, `ENVO:00000020` in `parent_habitats`, and a matching `GROUND_AS_PARENT` curation-history event. | Supported. |
| `ENVO:00000020` is a valid strict parent for this source concept. | `ENVO:00000020` is `lake`, whose definition denotes a whole body of liquid; GOLD's path denotes the aphotic zone nested under a freshwater lake. | Not supported. |
| `ENVO:00000021` is a valid strict parent for this source concept. | `ENVO:00000021` is `freshwater lake`; GOLD contributes it because it is the parent path record for `Environmental > Aquatic > Freshwater > Lake`, but the aphotic zone is a zone of a freshwater lake rather than a freshwater lake itself. | Not supported. |
| Existing target-specific causal evidence belongs on this record. | Exact hidden/ignored-inclusive searches of `curation/causal_graphs/`, `history/`, prior review reports, and `research/habitats/` found no target-specific overlay, history entry, prior review, or research report for this source concept before this report was created. | Not supported by current maintained inputs. |

## Completeness

The record preserves the current GOLD source identity: the source path, leaf label, node ID, aquatic category, item-level `GROUND_AS_PARENT` decision, generated narrow-match source attestation, and generated curation history are present.

The record is incomplete at the authored-term level. No row in `curation/term_requests.tsv` defines the minted freshwater-lake aphotic-zone concept or repairs the explicit `lake` parent and inherited `freshwater lake` parent.

The empty `environmental_parameters`, `characteristic_taxa`, `evidence`, `causal_graphs`, `discussions`, and `datasets` lists are appropriate for the current maintained inputs. The exact search of `data/raw/gold_path_triads.tsv` found MIxS triad evidence for the marine/oceanic Aphotic zone sibling, but no row for this zero-assertion freshwater-lake Aphotic zone.

## Findings

### Major

- `data/habitats/aquatic/aphotic_zone.yaml` currently asserts `ENVO:00000020` `lake` and `ENVO:00000021` `freshwater lake` as `parent_habitats` values even though the `Aphotic zone` source path denotes a zone within a freshwater lake rather than a lake or freshwater lake. `parent_habitats` is an is-a claim per `docs/CURATION.md`. `ENVO:00000020` is attached by the item-level `GROUND_AS_PARENT` row in `curation/decisions.tsv`, and `ENVO:00000021` is inherited from the GOLD parent path and needs replacement in `curation/term_requests.tsv`.

## Recommended Edits

- Replace the `habitatmech:GOLD.fcba7e6334` `GROUND_AS_PARENT` row in `curation/decisions.tsv` with an item-level decision that no longer attaches `ENVO:00000020` as a parent.
- Add a `curation/term_requests.tsv` row that defines the freshwater-lake aphotic-zone habitat and uses `parent_mode: REPLACE` to remove the inherited `ENVO:00000021` parent after identifying the tightest supported vendored genus.
- Regenerate with `just seed`, inspect `just seed-canary habitatmech:GOLD.fcba7e6334`, and apply with `just seed-apply --force` only after the canary removes both false lake parents while retaining the GOLD source attestation.

## Follow-up Checks

- `just validate data/habitats/aquatic/aphotic_zone.yaml`
- `just validate-strict data/habitats/aquatic/aphotic_zone.yaml --quiet`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- Manual check: confirm `data/habitats/aquatic/aphotic_zone.yaml` no longer lists `ENVO:00000020` or `ENVO:00000021` as a parent after the corrected decision and `REPLACE` term request.

## Additional Notes

GOLD also has `Environmental > Aquatic > Marine > Oceanic > Aphotic zone`, source node `gold.ecosystem:4010`. That sibling path has 146 GOLD assertions and one aggregate `data/raw/gold_path_biosamples.tsv` row reporting 64 biosamples; unlike the zero-assertion freshwater-lake target reviewed here, its MIxS local-scale triad directly names `ENVO:00000210` `marine aphotic zone`.
