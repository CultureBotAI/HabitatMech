# YAML Record Review: Alkaline

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/alkaline__009939a8.yaml
- Started UTC: 2026-09-21T12:48:00Z
- Finished UTC: 2026-09-21T12:51:13Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Path | `data/habitats/aquatic/alkaline__009939a8.yaml` |
| Identifier | `habitatmech:GOLD.d8c427584f` |
| Label | `Alkaline` |
| Category | `AQUATIC` |
| Grounding status | `NOT_APPLICABLE` |
| Mapping status | `REVIEWED` |
| Source | GOLD |
| Source ID | `gold.ecosystem:3983` |
| Source path | `Environmental > Aquatic > Non-marine Saline and Alkaline > Near-boiling (>90C) > Alkaline` |

The target is a generated GOLD-only record. It has no definition, synonyms,
xrefs, environmental parameters, characteristic taxa, record-level evidence,
causal graphs, discussions, or datasets. `data/habitats/PATHS.tsv` pins
`habitatmech:GOLD.d8c427584f` to the stable `alkaline__009939a8` slug.

This review covers the single GOLD source concept keyed by
`habitatmech:GOLD.d8c427584f`. The identifier is the first ten hex characters
of `sha1("GOLD:Environmental > Aquatic > Non-marine Saline and Alkaline > Near-boiling (>90C) > Alkaline")`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/alkaline__009939a8.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/alkaline__009939a8.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just term-requests-check` | Passed; the generated term-request table is current with 109 terms. |
| Reference validator | Not checked: this reviewed `NOT_APPLICABLE` GOLD-only record has no `EvidenceItem` references or causal overlay, and this repository documents no standalone reference validator for a generated record without those objects. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records were present, with 0 missing, 0 extra, and 0 differing files. |

## Identity and Grounding

The reviewed source identity is reproducible:

| Claim | Review |
|---|---|
| Minted identifier | Supported. `mint("GOLD", canonical_path)` hashes the exact GOLD source path to `habitatmech:GOLD.d8c427584f`; `data/habitats/PATHS.tsv` maps that identifier to `alkaline__009939a8`. |
| GOLD source path | Supported. `data/raw/gold_ecosystem_paths.tsv` has the exact canonical path `Environmental > Aquatic > Non-marine Saline and Alkaline > Near-boiling (>90C) > Alkaline`. |
| Source ID | Supported. The exact raw GOLD ecosystem row records `gold.ecosystem:3983`. |
| Source label | Supported. The raw GOLD row sets `leaf_label` to `Alkaline`, matching the generated `label` and `source_label`. |
| Assertion count | Supported. The exact raw GOLD row has `organism_count=0`, `study_count=0`, `biosample_count=0`, and `total_assertions=0`, so the generated attestation correctly omits an `assertion_count`. |
| Category | Supported by the first two GOLD path levels, `Environmental > Aquatic`. |
| `grounding_status: NOT_APPLICABLE` | Supported. The source concept's maintained `curation/decisions.tsv` row is an item-level `NOT_APPLICABLE` decision stating that this `Alkaline` leaf is a pH band qualifying the parent node, not a habitat. |
| `mapping_status: REVIEWED` | Supported. The sole contributing GOLD source has an item-level decision, so the generated record is fully reviewed even though the source concept is rejected as a habitat. |

The non-habitat decision is sound. GOLD is using `Alkaline` as a
specific-ecosystem classifier under
`Environmental > Aquatic > Non-marine Saline and Alkaline > Near-boiling (>90C)`.
The vendored ontology slice also confirms that bare alkalinity is a quality:
`PATO:0001430` has label `alkaline`. A pH band can describe a near-boiling
inland saline or alkaline aquatic environment, but it is not itself the habitat
identity.

The generated `parent_habitats` edge is not sound. The target currently has
`habitatmech:GOLD.12b8a9a4c4` as a parent, but that parent record is the GOLD
`Environmental > Aquatic > Non-marine Saline and Alkaline > Near-boiling (>90C)`
node and has also been item-reviewed `NOT_APPLICABLE` because it denotes a
temperature band. The edge turns one environmental qualifier into a strict child
of another environmental qualifier.

## Evidence

Supported:

| Claim | Evidence |
|---|---|
| The source concept exists and is keyed by one GOLD ecosystem node. | `data/raw/gold_ecosystem_paths.tsv` has one collapsed node, `gold.ecosystem:3983`, for the exact non-marine saline-or-alkaline near-boiling alkaline path. |
| The generated GOLD attestation preserves the absence of GOLD organism assertions. | The exact raw GOLD row has 0 organism assertions, and the generated attestation stores no `assertion_count`, avoiding a misleading zero-valued claim. |
| The exact source path appears in downstream GOLD biosample, triad, and study inventories. | `data/raw/gold_path_biosamples.tsv` has one biosample for the exact path, `data/raw/gold_path_triads.tsv` has exact-path broad/local/medium rows for the same one sample and one study, and `data/raw/gold_studies.tsv` includes the exact path in `Gs0063188`. |
| The MIxS triads for the exact source support hot-spring context, not an alkaline identity. | `data/raw/gold_path_triads.tsv` has exact-path rows whose local term is `ENVO:00000051` `hot spring` and whose medium term is `ENVO:03600065` `spring water`. |
| The nearest GOLD parent is a reviewed qualifier, not a habitat. | `data/habitats/aquatic/near_boiling_90c.yaml` is `habitatmech:GOLD.12b8a9a4c4`, and `curation/decisions.tsv` marks that source concept `NOT_APPLICABLE` because `Near-boiling (>90C)` is a temperature band. |

Unsupported or over-scoped:

| Claim | Issue |
|---|---|
| `habitatmech:GOLD.12b8a9a4c4` `Near-boiling (>90C)` is a broader habitat for this record. | Both child and parent are physicochemical qualifiers rather than habitats. `docs/CURATION.md` and `CLAUDE.md` reserve `parent_habitats` for strictly broader is-a links, so the generated edge overstates GOLD path nesting as habitat subsumption. |

No literature citations, record-level evidence, causal edges, environmental
parameters, characteristic taxa, discussions, or datasets are attached to this
record.

## Completeness

The source concept has been reviewed at item depth, and no term request is
needed because the source concept is not a habitat. The record still has one
consequential generated artifact gap:

- `src/habitatmech/seed.py` adds the next GOLD path level as a parent after
  item-level decisions are applied, so this reviewed `NOT_APPLICABLE` pH band
  still inherits a reviewed `NOT_APPLICABLE` temperature-band parent.

Bounded absence checks:

| Query | Scope | Result |
|---|---|---|
| `habitatmech:GOLD.d8c427584f` | Hidden/ignored-inclusive search over `curation`, `data/raw`, `data/habitats`, `research/habitats`, `reports/yaml_record_review`, and `curation/causal_graphs` | Found the maintained decision row, the PATHS row, and the generated target; found no term request, causal overlay, research report, or prior YAML review for this exact source concept. |
| `gold.ecosystem:3983` and `Environmental > Aquatic > Non-marine Saline and Alkaline > Near-boiling (>90C) > Alkaline` | Hidden/ignored-inclusive search over raw GOLD rows, maintained curation inputs, generated records, research reports, and YAML reviews | Found the exact GOLD ecosystem, biosample, MIxS triad, and study rows, plus the generated target; found no maintained suppression that would prevent the false parent edge. |
| `habitatmech:GOLD.12b8a9a4c4` and `Environmental > Aquatic > Non-marine Saline and Alkaline > Near-boiling (>90C)` | Hidden/ignored-inclusive search over maintained curation inputs, raw GOLD rows, generated records, research reports, and YAML reviews | Found the generated `Near-boiling (>90C)` parent, its `NOT_APPLICABLE` decision row, and the raw parent ecosystem row; found no target-specific research report, causal overlay, or YAML review. |
| `PATO:0001430` | Hidden/ignored-inclusive search over `data/raw/ontology_terms.tsv`, `curation/decisions.tsv`, and the target and parent records | Confirmed `PATO:0001430` is the vendored `alkaline` quality term and is not attached as a generated xref for this source concept. |

## Findings

### Blocker

None found.

### Major

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| M1 | `parent_habitats` keeps a generated edge from a reviewed non-habitat pH band to a reviewed non-habitat temperature band. | `alkaline__009939a8.yaml` lists `habitatmech:GOLD.12b8a9a4c4` as its parent; `near_boiling_90c.yaml` and the maintained decision for `habitatmech:GOLD.12b8a9a4c4` both say the parent source concept is `NOT_APPLICABLE`. The GOLD second pass in `src/habitatmech/seed.py` adds the parent path after decisions resolve both rows to minted identifiers, so the edge is generated reproducibly even though both concepts are environmental-parameter qualifiers. | `src/habitatmech/seed.py`, or a maintained suppression/rewiring input consumed by the GOLD parent-path pass. |

### Minor

None found.

## Recommended Edits

1. Update the maintained GOLD hierarchy pass in `src/habitatmech/seed.py`, or add a maintained suppression/rewiring table consumed by that pass, so item-reviewed `NOT_APPLICABLE` qualifier source concepts are not emitted as strict `parent_habitats`.
2. At minimum, suppress the edge from `Environmental > Aquatic > Non-marine Saline and Alkaline > Near-boiling (>90C) > Alkaline` to `Environmental > Aquatic > Non-marine Saline and Alkaline > Near-boiling (>90C)`.
3. Apply the same review to the near-boiling thermal-spring siblings, including `Environmental > Aquatic > Thermal springs > Near-boiling (>90C) > Alkaline`, so pH and temperature qualifiers are consistently kept out of strict habitat hierarchy edges.
4. Do not hand-edit `data/habitats/aquatic/alkaline__009939a8.yaml`; it is generated from raw GOLD, curation decisions, and the seeder.

## Follow-up Checks

| Edit | Narrowest proving check |
|---|---|
| Suppress or rewire the false GOLD parent edge for this exact source. | Run `just seed-canary habitatmech:GOLD.d8c427584f` and confirm `data/habitats/aquatic/alkaline__009939a8.yaml` no longer has `parent_habitats: habitatmech:GOLD.12b8a9a4c4`. |
| Preserve source provenance while rejecting the pH band as a habitat. | Re-read the canary and confirm the record keeps `grounding_status: NOT_APPLICABLE`, `mapping_status: REVIEWED`, `source_id: gold.ecosystem:3983`, and the exact non-marine near-boiling alkaline GOLD source path. |
| Avoid regressing neighboring non-habitat qualifier records. | Canary `habitatmech:GOLD.12b8a9a4c4` and the thermal-spring near-boiling alkaline source `habitatmech:GOLD.f00984e0e2`, then confirm neither keeps a strict parent edge through another rejected temperature or pH qualifier. |
| Prove the regenerated corpus remains stable. | Run `just validate data/habitats/aquatic/alkaline__009939a8.yaml`, `just validate-strict data/habitats/aquatic/alkaline__009939a8.yaml --quiet`, `just term-requests-check`, `just validate-history`, and `just verify-corpus --max-diffs 1`. |
| Prove downstream exports no longer carry the false edge. | Rebuild KGX and SSSOM products and confirm no exported edge asserts `habitatmech:GOLD.d8c427584f` under `habitatmech:GOLD.12b8a9a4c4`. |

## Additional Notes

- The one biosample and one study in `data/raw/gold_path_biosamples.tsv`,
  `data/raw/gold_path_triads.tsv`, and `data/raw/gold_studies.tsv` are
  downstream GOLD summaries and are intentionally separate from the 0 organism
  assertions in `data/raw/gold_ecosystem_paths.tsv`; the generated GOLD
  `source_attestations` count uses the organism count.
- The exact MIxS triad terms point to hot spring and spring water context for
  a single GOLD sample. They support that GOLD's pH leaf qualifies a
  near-boiling aquatic source, but the current model has no generated field
  that can preserve them as environmental parameters.
