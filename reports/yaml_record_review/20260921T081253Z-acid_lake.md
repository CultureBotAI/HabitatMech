# YAML Record Review: Acid lake

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/acid_lake.yaml
- Started UTC: 2026-09-21T08:12:53Z
- Finished UTC: 2026-09-21T08:12:53Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/acid_lake.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.21e53e7981` |
| Label | `Acid lake` |
| Category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Maintained status | generated from `data/raw/`, `curation/decisions.tsv`, and `data/habitats/PATHS.tsv` |

The generated record is a compact GOLD-only placeholder:

- GOLD supplies one `Environmental > Aquatic > Thermal springs > Acid lake`
  source attestation. The collapsed path has 2 GOLD ecosystem node IDs and 0
  `ORGANISM` assertions, so the generated attestation names the first node,
  `gold.ecosystem:6886`, and does not emit an `assertion_count`.
- The raw GOLD biosample inventory separately reports 1 biosample on the same
  path, attached to `gold.ecosystem:6887`.
- The parent `ENVO:00000051` `hot spring` comes from the preceding GOLD
  `Thermal springs` path step, whose source concept has item-level grounding to
  `ENVO:00000051`.
- GOLD has one direct study row listing only
  `Environmental > Aquatic > Thermal springs > Acid lake`.
- GOLD also has a child `Environmental > Aquatic > Thermal springs > Acid lake
  > Sediment` with 1 `ORGANISM` assertion and one MIxS triad sample: broad
  `ENVO:00000052` `volcanic crater lake`, local `ENVO:00000051` `hot spring`,
  and medium `ENVO:00002007` `sediment`.
- A class-level curation decision rejected automatic lexical grounding but did
  not assess the source concept as an individual habitat.
- No definition, authored term request, item-level grounding decision, taxa,
  environmental parameters, evidence, causal graphs, discussions, datasets,
  synonyms, or xrefs are present.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/acid_lake.yaml` | Passed; no LinkML issues. |
| `just validate-strict data/habitats/aquatic/acid_lake.yaml --quiet` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| `just term-requests-check` | Passed; term-request table is current at 109 terms. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 found, no missing/extra/differing records. |
| `just validate-history` | Passed; 77 history records valid against `src/habitatmech/schema/history.yaml`. |
| Reference validator | Not checked: this record has no DOI/PMID/URL `EvidenceItem` or causal-edge evidence, and no dedicated `research/habitats/aquatic/` report for `habitatmech:GOLD.21e53e7981` requiring `scripts/check_report_citations.py` exists. |

## Identity and Grounding

| Claim | Evidence | Assessment |
|---|---|---|
| The record denotes the GOLD `Environmental > Aquatic > Thermal springs > Acid lake` source concept. | `data/raw/gold_ecosystem_paths.tsv` has exactly that canonical path, leaf `Acid lake`, depth `4`, `gold_node_count=2`, `organism_count=0`, `total_assertions=0`, and node IDs `gold.ecosystem:6886\|gold.ecosystem:6887`. | Supported exactly. |
| The stable file stem is `acid_lake`. | `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.21e53e7981` to `acid_lake`. | Supported exactly. |
| The source path justifies the broad AQUATIC category. | The raw GOLD path is under `Environmental > Aquatic > Thermal springs`. | Supported exactly. |
| The inherited `ENVO:00000051` `hot spring` parent follows from the immediate GOLD path parent. | `curation/decisions.tsv` item-grounds the GOLD `Environmental > Aquatic > Thermal springs` parent to `ENVO:00000051`. `data/raw/ontology_terms.tsv` defines `ENVO:00000051` as a spring through which groundwater heated by geothermal energy flows. | Mechanically supported; item review still needs to confirm that GOLD `Acid lake` means an acidic lake within a hot-spring setting rather than a related volcanic crater lake or a source artifact. |
| `ENVO:00002120` `acidic hot spring` is a plausible grounding target. | The vendored ontology slice has `ENVO:00002120` labeled `acidic hot spring` with synonym `acid hot spring`, and no inspected slice row has an `acid lake` or `acidic lake` label. | Needs item-level curation; the GOLD leaf says `lake`, so the acidic-hot-spring term is plausible but not automatically exact. |
| The child `Acid lake > Sediment` triad is contextual, not identity evidence for the parent by itself. | The only triad row under the exact acid-lake branch belongs to `Environmental > Aquatic > Thermal springs > Acid lake > Sediment`, not to the parent `Acid lake` path. Its broad slot is `volcanic crater lake`, its local slot is `hot spring`, and its medium slot is `sediment`. | Supported exactly. Treat it as a lead that the acid-lake branch may involve volcanic crater lakes, not as a direct grounding for the parent. |
| `grounding_status: UNGROUNDED` is generated from a class-level no-match sweep. | `curation/decisions.tsv` has `habitatmech:GOLD.21e53e7981` as `CONFIRM_UNGROUNDED` with `review_depth=CLASS` and no object ID. Its note says no vendored-slice term matched the label by any search route and explicitly says whether the concept is a habitat was not assessed. | Mechanically supported but incomplete for item-level curation. |
| `mapping_status: SEEDED` is correct. | The only curation row for `habitatmech:GOLD.21e53e7981` has `review_depth=CLASS`; `docs/CURATION.md` states that only item-level decisions promote a merged record to `REVIEWED`. | Supported exactly. |

## Evidence

No claim-level literature evidence was present. The record has no
characteristic taxon rows and no causal graph, so there are no taxon
associations, causal edges, or snippets to audit.

The raw GOLD path, one direct GOLD study row, one biosample on the same path,
and the child sediment triad are source-context rows, not literature evidence.
They show that the `Acid lake` leaf is a real GOLD branch under thermal springs,
but they do not independently decide whether the parent path should ground to
`ENVO:00002120`, become a novel acid-lake term under hot spring, or remain
minted with a broader parent.

## Completeness

- The record is schema-valid and reproducible, but not item-curated.
- Empty `characteristic_taxa`, `evidence`, `environmental_parameters`,
  `causal_graphs`, `discussions`, `datasets`, `synonyms`, and `xrefs` are
  technically acceptable: no maintained input currently supplies them for this
  record.
- `data/raw/environment_parameters.tsv` has no row for
  `Environmental > Aquatic > Thermal springs > Acid lake`.
- `curation/term_requests.tsv`,
  `curation/term_requests/envo_robot_template.tsv`, and
  `reports/habitat_research_manifest.tsv` have no row for
  `habitatmech:GOLD.21e53e7981`.
- Ignored/hidden-inclusive exact searches covered `data/raw`,
  `data/habitats/aquatic`, `data/habitats/PATHS.tsv`, `curation`,
  `research/habitats`, `reports/habitat_research_manifest.tsv`, and
  `reports/yaml_record_review` for `habitatmech:GOLD.21e53e7981`,
  `gold.ecosystem:6886`, `gold.ecosystem:6887`,
  `Environmental > Aquatic > Thermal springs > Acid lake`, `Acid lake`, and
  `acid_lake`. They found the target, the raw GOLD path, the raw GOLD biosample
  row, the raw GOLD direct study row, one child sediment study and its triad
  rows, the stable path entry, the class-level decision row, and a
  `curation/samples` row sampling the child `Sediment` path; they found no
  item-level decision, term-request row, causal overlay, research manifest
  entry, dedicated research report, or prior YAML review report for this target.
- A bounded `find curation/causal_graphs -maxdepth 1` search for `*acid*`,
  `*lake*`, `*6886*`, and `*21e53e7981*` found no candidate causal overlay.
- A bounded `find research/habitats` search for `*acid*`, `*lake*`, `*6886*`,
  and `*21e53e7981*` found no candidate dedicated research report.
- A bounded `find reports/yaml_record_review -maxdepth 1` search for `*acid*`,
  `*lake*`, `*6886*`, and `*21e53e7981*` found no prior review report.

## Findings

### Major

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| `HM-ACID-LAKE-001` | The source concept still needs item-level curation. The generated record treats GOLD `Acid lake` as an ungrounded child of `hot spring`, but its only curation row is a `CLASS`-depth lexical no-match sweep that did not assess the full thermal-spring path, `ENVO:00002120` `acidic hot spring`, the separate GOLD `Environmental > Aquatic > Acidic > Acidic lake` source, or the child `Acid lake > Sediment` triad pointing at `volcanic crater lake`, `hot spring`, and `sediment`. | `curation/decisions.tsv` row `habitatmech:GOLD.21e53e7981` has `review_depth=CLASS`; `data/raw/ontology_terms.tsv` contains the candidate `ENVO:00002120`; GOLD also has a class-depth `Environmental > Aquatic > Acidic > Acidic lake` source at `habitatmech:GOLD.41f771c2b4`; and the only acid-lake MIxS triad in `data/raw/gold_path_triads.tsv` belongs to the child sediment path. | `curation/decisions.tsv`, with a possible follow-on `curation/term_requests.tsv` row if item review confirms the source is a real acid-lake habitat and no ontology term fits exactly. |

## Recommended Edits

1. Item-review `habitatmech:GOLD.21e53e7981` in
   `curation/decisions.tsv`. Resolve whether GOLD's
   `Thermal springs > Acid lake` path is exact to `ENVO:00002120`, a narrower
   acid-lake-in-hot-spring concept, a volcanic-crater-lake context, or a source
   artifact.
2. If `ENVO:00002120` is exact, replace the existing class-depth
   `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.21e53e7981` with an
   item-depth `GROUND` decision to `ENVO:00002120`. Do not ground to
   `ENVO:00000052` `volcanic crater lake` from the child sediment triad without
   evidence that the parent source concept itself has that identity.
3. If the source concept is a real acid-lake habitat but no ontology term fits,
   add a `curation/term_requests.tsv` row with an authored definition and a
   vendored ontology genus. Use default `ADD` parent mode only if item review
   confirms the inherited `hot spring` parent is true; otherwise use `REPLACE`
   with the evidence documented in the definition notes.
4. Regenerate a single-record canary for the resolved identifier that follows
   from the item decision. If the source remains minted, inspect
   `data/habitats/aquatic/acid_lake.yaml`; if it grounds to an ontology CURIE or
   merges, inspect the resolved record path instead. Confirm that the generated
   `grounding_status`, `mapping_status`, label, parent, source attestation, and
   curation history all follow from the maintained rows.

## Follow-up Checks

- `just seed`
- `just seed-canary <resolved-identifier>`
- `just validate <resolved-record-path>`
- `just validate-strict <resolved-record-path> --quiet`
- `just term-requests-check`
- `just verify-corpus --max-diffs 1`

## Additional Notes

- The sibling GOLD `Environmental > Aquatic > Acidic > Acidic lake` path is also
  generated as a minted, class-level ungrounded record. It has 48 `ORGANISM`
  assertions and no item-level decision; item review of `Acid lake` should keep
  the two GOLD source concepts separate unless the source contexts prove they
  have the same identity.
- `curation/samples/narrow-20260814.tsv` marked the child
  `Environmental > Aquatic > Thermal springs > Acid lake > Sediment` path as a
  correct `Sediment` narrow record in a sample, but that is not a decision row
  for the parent `Acid lake` source.
