# YAML Record Review: blast cell

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/blast_cell.yaml`
- Started UTC: 2026-09-21T23:38:00Z
- Finished UTC: 2026-09-21T23:40:45Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `BTO:0000125` |
| Label | `blast cell` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Maintained inputs | `data/raw/ontology_terms.tsv`, `data/raw/prego_habitats.tsv`, `data/raw/prego_habitat_taxa.tsv`, `data/habitats/PATHS.tsv` |

This is a generated BTO-grounded record produced from one PREGO source concept. The path lock maps `BTO:0000125` to `blast_cell`, the vendored ontology slice supplies the `blast cell` label and definition, and PREGO directly asserts `BTO:0000125`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/blast_cell.yaml` | Pass; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/host_associated/blast_cell.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Pass; the committed term-request table is current with 109 terms. |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass; 3,206 expected records, 3,206 records on disk, and 0 missing, extra, or differing records. |
| `git diff --check` | Pass. |

`just validate-products` was not run. This read-only review did not add or change a grounding row, and the BTO identifier on the record was checked directly in `data/raw/ontology_terms.tsv`.

## Identity and Grounding

The generated identity and exact grounding are supported:

- `data/habitats/PATHS.tsv` pins `BTO:0000125` to `blast_cell`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv` labels `BTO:0000125` as `blast cell`, carries the copied definition, and keeps the class active.
- PREGO directly asserts the ontology class `BTO:0000125` with lexical variants `blast`, `blast cell`, `blast cells`, `blastic`, `blasts`, `hematoblast`, `hematoblastic`, `hematoblasts`, `hematocytoblast`, `hematocytoblastic`, `hematocytoblasts`, `hemocytoblast`, `hemocytoblastic`, and `hemocytoblasts`, so `ingest_prego()` resolves the source concept to `BTO:0000125` with `grounding_status: EXACT`.
- The stable PREGO decision key is `habitatmech:PREGO.35c7ff17db`, computed by the seeder from `sha1("PREGO:BTO:0000125")[:10]`.
- There is no `curation/decisions.tsv` row for `habitatmech:PREGO.35c7ff17db`, so the generated `mapping_status: SEEDED` is expected.
- BTO identifiers fall through `PREFIX_CATEGORY` to `HOST_ASSOCIATED`, matching the record's `habitat_category`.

`data/raw/ontology_subclass_edges.tsv` has no exact `BTO:0000125 rdfs:subClassOf ...` or `... rdfs:subClassOf BTO:0000125` edge, so the empty `parent_habitats` slot matches the vendored ontology slice.

## Evidence

Supported claims:

- The definition and `definition_source: BTO` come verbatim from the vendored BTO row for `BTO:0000125`.
- All generated synonyms are the pipe-delimited lexical variants in the PREGO `BTO:0000125` row; `blast cell`, which duplicates the record label, is correctly omitted from the YAML synonyms.
- The PREGO source id, source label, `assertion_count: 18`, `assertion_unit: TAXON`, `score: 3.0`, and `evidence_channels: annotated_genomes_isolates` come from `data/raw/prego_habitats.tsv`.
- The 18 `characteristic_taxa` entries are copied in rank order from `data/raw/prego_habitat_taxa.tsv`; none is promoted with `is_characteristic: true`.

Unsupported or over-scoped claims: None found.

The record has no record-level evidence, environmental parameters, causal graphs, discussion links, or datasets. That absence is supported: ignored-file-inclusive searches found no exact `BTO:0000125`, `blast_cell`, `blast cell`, `blast cells`, or `habitatmech:PREGO.35c7ff17db` entry in `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/causal_graphs/`, `history/`, or prior `reports/yaml_record_review/` reports before this report was written.

## Completeness

The record is complete enough for its current source-backed scope:

- No authored definition or term request is expected because the record uses the existing exact BTO `blast cell` term.
- No BTO subclass parent is expected because the vendored subclass inventory has no exact edge for `BTO:0000125`.
- No item-level curation decision exists yet; with one PREGO-only source concept, `mapping_status: SEEDED` is therefore correct.
- No HabitatMech-authored causal graph is expected because no maintained `curation/causal_graphs/` overlay names this habitat.
- No additional characteristic-taxon evidence is expected because the PREGO taxa are presented only as reported associations from PREGO, not as curator-asserted typifying taxa.

All negative checks above used `rg --no-ignore --hidden`, so ignored files were included in the searched paths.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

If a future curation change touches this record, rerun:

- `just validate data/habitats/host_associated/blast_cell.yaml`
- `just validate-strict data/habitats/host_associated/blast_cell.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

## Additional Notes

An ignored-file-inclusive search of `reports/yaml_record_review/` found no prior report for this `blast_cell` record before this file was written.
