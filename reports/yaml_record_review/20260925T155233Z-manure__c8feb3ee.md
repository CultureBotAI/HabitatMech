# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/manure__c8feb3ee.yaml
- Started UTC: 2026-09-25T15:52:33Z
- Finished UTC: 2026-09-25T15:52:33Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.26717179e1` |
| Label | `Manure` |
| Category | `ENGINEERED` |
| Source path | `Engineered > Bioreactor > Anaerobic > Manure` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv`; future identity curation belongs in `curation/decisions.tsv`, with any minted definition and parent replacement in `curation/term_requests.tsv`, not in `data/habitats/engineered/manure__c8feb3ee.yaml`. |

The generated record is a seeded minted `Manure` leaf under GOLD's reviewed
`Engineered > Bioreactor > Anaerobic` path. It has `grounding_status: NARROW`,
`mapping_status: SEEDED`, parent edges to `ENVO:00002124` `anaerobic
bioreactor` and `ENVO:03501300` `manure`, and one GOLD source attestation
showing `gold.ecosystem:4913` as the representative node for the two GOLD node
IDs that share the canonical path.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/habitats/engineered/manure__c8feb3ee.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/manure__c8feb3ee.yaml` | Pass: scanned 1 file with 0 files containing `ERROR` and 0 total `ERROR` rows. |
| `just validate-causal curation/causal_graphs/<overlay>.yaml` | Not applicable: the target has no `causal_graphs` block, and gitignore-independent exact searches for `habitatmech:GOLD.26717179e1`, `gold.ecosystem:4913`, `gold.ecosystem:4914`, and `manure__c8feb3ee.yaml` under `curation/causal_graphs`, `history`, `research`, and `conf` found no curated overlay or dedicated history record for this target. |
| `just validate-causal-all` | Pass: validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Pass: the term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: expected 3,206 records, found 3,206 on disk, and the corpus reproduces exactly from `data/raw/`. |
| `just worklist --limit 2000 --status all --out /tmp/habitatmech-manure-bioreactor-worklist.tsv` | Pass: reported 953 ungrounded records, 1,810 decisions on file, and wrote 953 rows to `/tmp/habitatmech-manure-bioreactor-worklist.tsv`. |
| `just report` | Pass: completed the corpus report for 3,206 records. |
| `git diff --check` | Pass. |
| Reference validator | Not applicable: this record has no record-level `evidence`, `characteristic_taxa`, `causal_graphs`, `discussions`, or `datasets` with citation-bearing `EvidenceItem` references. |

## Identity and Grounding

The GOLD inventory row is present in `data/raw/gold_ecosystem_paths.tsv` as
`Engineered > Bioreactor > Anaerobic > Manure`, with
`gold_node_ids=gold.ecosystem:4913|gold.ecosystem:4914`,
`gold_node_count=2`, and zero direct organism, study, biosample, or total
assertions. The generated source attestation agrees with that row by carrying
`source: GOLD`, `source_id: gold.ecosystem:4913`, `source_label: Manure`,
`source_path: Engineered > Bioreactor > Anaerobic > Manure`, and
`mapping_predicate: skos:narrowMatch`.

`ENVO:03501300` is present in `data/raw/ontology_terms.tsv` as `manure` with
the definition `An environmental material consisting of organic matter from
plants and/or animals that is used in agriculture as fertilizer.` Its ontology
parent is also supported: `data/raw/ontology_subclass_edges.tsv` asserts
`ENVO:03501300 rdfs:subClassOf ENVO:00010483`.

The generated `ENVO:03501300` broader parent is plausible for this GOLD leaf:
manure in an anaerobic bioreactor is narrower than generic ENVO `manure`, and
the separate same-label GOLD path `Engineered > Solid waste > Animal waste >
Manure` is also preserved as a minted `grounding_status: NARROW` child with
`ENVO:03501300` as a parent. No item-level decision currently records that
judgement for `habitatmech:GOLD.26717179e1`, however, so the record remains
seeded rather than reviewed.

The generated `ENVO:00002124` `anaerobic bioreactor` parent is stricter for the
immediate source parent, `Engineered > Bioreactor > Anaerobic`, than for this
leaf. That parent path has item-level exact `GROUND` decisions to
`ENVO:00002124`, but manure fed to or sampled from an anaerobic bioreactor is a
material in the reactor, not itself a subtype of the reactor vessel.

## Evidence

The GOLD source attestation traces to `data/raw/gold_ecosystem_paths.tsv` and
contains no direct upstream assertion count. A gitignore-independent exact
search for `Engineered > Bioreactor > Anaerobic > Manure` across
`data/raw/gold_path_triads.tsv`, `data/raw/environment_parameters.tsv`,
`data/raw/gold_studies.tsv`, `data/raw/gold_path_biosamples.tsv`, and
`data/raw/gold_ecosystem_paths.tsv` found the path only in the canonical
ecosystem-path table, so the current record has no study, biosample, MIxS
triad, environmental-parameter, taxon, or causal evidence to review.

The material broader parent traces to the committed ENVO inventories:
`data/raw/ontology_terms.tsv` supplies the `ENVO:03501300` `manure` label and
definition, and `data/raw/ontology_subclass_edges.tsv` supplies its asserted
`ENVO:00010483` `environmental material` parent. The inherited
`ENVO:00002124` parent traces to GOLD's source-tree context rather than to any
target-specific evidence about this leaf.

## Completeness

- A gitignore-independent exact search for `habitatmech:GOLD.26717179e1` across `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/causal_graphs`, `history`, `research`, `reports/yaml_record_review`, `conf`, `data/habitats/PATHS.tsv`, and `data/habitats/engineered/manure__c8feb3ee.yaml` found only the generated target and the `PATHS.tsv` row; no maintained item-level decision, term request, causal graph, or history record exists for this target.
- Gitignore-independent exact searches for `gold.ecosystem:4913` and `gold.ecosystem:4914` across `curation`, `history`, `research`, `reports/yaml_record_review`, `data/habitats`, `data/raw`, and `conf` found `gold.ecosystem:4913` only in the canonical raw row and generated target, and `gold.ecosystem:4914` only in the canonical raw row.
- `find reports/yaml_record_review -maxdepth 1 -type f -name '*manure__c8feb3ee*' -print` found no prior review report for this record stem; `find` includes ignored files.
- Optional `environmental_parameters`, `characteristic_taxa`, `evidence`, `causal_graphs`, `discussions`, and `datasets` are correctly absent. No maintained overlay currently adds claim-level mechanism or taxon evidence for this seed-only target.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | `habitatmech:GOLD.26717179e1` has no item-level curation decision. | The generated record still has `mapping_status: SEEDED`, its curation history only contains the seed-from-sources event, and a gitignore-independent exact search for the identifier across `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/causal_graphs`, `history`, `research`, `reports/yaml_record_review`, `conf`, `data/habitats/PATHS.tsv`, and the target file found no maintained curation input for this specific source concept. The current `NARROW` relation to `ENVO:03501300` is plausible but unreviewed. | `curation/decisions.tsv`; if the minted identity is retained, `curation/term_requests.tsv`. |
| Major | The generated `ENVO:00002124` `anaerobic bioreactor` parent is likely contextual rather than a strict broader habitat for `Manure`. | The exact source path says this child is `Manure` nested under `Engineered > Bioreactor > Anaerobic`; that parent context is a reactor type, while `Manure` is a material leaf. The record has no item-level decision, term request, MIxS triad, or causal overlay saying the manure material is itself a kind of anaerobic bioreactor. | `curation/term_requests.tsv`, after an item-level decision confirms the target remains a real minted habitat. |

No blocker findings found.

No minor findings found.

## Recommended Edits

1. Add an item-level `curation/decisions.tsv` row for `habitatmech:GOLD.26717179e1` after inspecting the exact `Engineered > Bioreactor > Anaerobic > Manure` path. Preserve `ENVO:03501300` only as a broader term if the concept is confirmed as manure material in an anaerobic-bioreactor context.
2. If item review keeps the minted identifier, add a `curation/term_requests.tsv` definition for the bioreactor-scoped manure material. Use `ENVO:03501300` as the strict genus and `parent_mode: REPLACE` if the review confirms `ENVO:00002124` is only contextual.
3. Regenerate `data/habitats/engineered/manure__c8feb3ee.yaml` through the seeder and inspect the generated diff rather than editing the generated YAML directly.

## Follow-up Checks

| Edit | Proof |
| --- | --- |
| Record an item-level decision for `habitatmech:GOLD.26717179e1` | `just seed-canary habitatmech:GOLD.26717179e1 --force`, followed by inspection of `data/habitats/engineered/manure__c8feb3ee.yaml` showing `mapping_status: REVIEWED` when every contributing source concept has item-level review. |
| Replace the contextual `ENVO:00002124` parent if needed | The canary-generated `parent_habitats` list keeps `ENVO:03501300` and drops `ENVO:00002124` when the term request uses `parent_mode: REPLACE`. |
| Preserve corpus reproduction | `just seed-apply --force` and `just verify-corpus --max-diffs 1`. |
| Validate the focused record and maintained inputs | `just validate data/habitats/engineered/manure__c8feb3ee.yaml`, `just validate-strict data/habitats/engineered/manure__c8feb3ee.yaml`, `just term-requests-check`, `just validate-history`, and `git diff --check`. |

## Additional Notes

- The GOLD `Engineered > Solid waste > Animal waste > Manure` path is a same-label sibling with 124 direct organism assertions; it remains a separate minted `NARROW` record with `ENVO:03501300` as a parent. The two same-label `Manure` records are therefore not evidence for each other beyond confirming that GOLD uses context paths to distinguish narrower manure concepts.
- The nearby reviews for `Inoculum`, `Biofilm`, `Digestate`, and `Leachate` under `Engineered > Bioreactor > Anaerobic` found the same issue with leaf materials or biofilms inheriting `anaerobic bioreactor` as an `is-a` parent from the source path.
