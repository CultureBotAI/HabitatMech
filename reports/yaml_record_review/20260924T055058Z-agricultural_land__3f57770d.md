# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/terrestrial/agricultural_land__3f57770d.yaml`
- Started UTC: 2026-09-24T05:43:30Z
- Finished UTC: 2026-09-24T05:50:58Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/terrestrial/agricultural_land__3f57770d.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.74d360dcf3` |
| Label | `Agricultural land` |
| Category | `TERRESTRIAL` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| GOLD source-concept key | `habitatmech:GOLD.74d360dcf3` |
| Maintained owners | Generated from `data/raw/gold_ecosystem_paths.tsv`, `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, `data/habitats/PATHS.tsv`, and `src/habitatmech/seed.py`; `data/habitats/` is generated and remains read-only |

This generated GOLD-only record represents `gold.ecosystem:5770`:

```text
Environmental > Terrestrial > Soil > Greenhouse > Agricultural land
```

It is one of the collision-pinned GOLD `Agricultural land` leaves. This review
covers only the `Greenhouse`-scoped source concept with stable minted
identifier `habitatmech:GOLD.74d360dcf3`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/terrestrial/agricultural_land__3f57770d.yaml` | Passed: LinkML reported no issues |
| `just validate-strict data/habitats/terrestrial/agricultural_land__3f57770d.yaml` | Passed: 1 file scanned, 0 files with errors, 0 total error rows |
| `just validate-causal <overlay>` | Not applicable: the record has no `causal_graphs` slot, and ignored/hidden-inclusive searches of `curation/causal_graphs/` found no overlay for `habitatmech:GOLD.74d360dcf3`, `gold.ecosystem:5770`, or `agricultural_land__3f57770d` |
| `just validate-causal-all` | Passed: 32 causal-graph curation files with 32 graphs |
| `just term-requests-check` | Passed: term-request table is current with 109 terms |
| `just validate-history` | Passed: no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Passed: expected 3,206 records, found 3,206, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | Passed: 0 ungrounded records still undecided, 1,810 decisions on file |
| `just report` | Passed: corpus report completed for 3,206 records |
| Reference validator | Not applicable: this record has no DOI, PMID, URL, `EvidenceItem`, or causal-edge evidence that would need citation checking |
| `git diff --check` | Passed before this report was written |

## Identity and Grounding

| Claim | Maintained evidence | Assessment |
|---|---|---|
| The output identifier is `habitatmech:GOLD.74d360dcf3`. | `data/habitats/PATHS.tsv` pins that identifier to `agricultural_land__3f57770d`. | Supported. |
| The GOLD source ID is `gold.ecosystem:5770`. | `data/raw/gold_ecosystem_paths.tsv` has one row for `Environmental > Terrestrial > Soil > Greenhouse > Agricultural land` with one GOLD node ID and zero organism, study, biosample, or total assertions. | Supported. |
| The source belongs in the terrestrial category. | `src/habitatmech/seed.py` infers the category from GOLD's `Environmental > Terrestrial` prefix for this raw row. | Supported. |
| `ENVO:00000077` `agricultural ecosystem` is a broader habitat for this source concept. | The reused GOLD leaf `Agricultural land` matches a synonym on the vendored `ENVO:00000077` row; the seeder keeps the exact ontology term as a parent because other GOLD paths reuse the same leaf. | Supported. |
| `ENVO:03600087` `greenhouse` is a broader habitat for this source concept. | The GOLD parent-path pass links the child path to `Environmental > Terrestrial > Soil > Greenhouse`, and that collapsed parent source resolves to `ENVO:03600087` `greenhouse`. | Not supported: this is a containment/context edge. A greenhouse is a human construction containing agricultural soil or land-use contexts, not a superclass of agricultural land. |
| `mapping_status` is `SEEDED`. | Ignored/hidden-inclusive exact searches found no item-level `curation/decisions.tsv` decision for `habitatmech:GOLD.74d360dcf3`, `gold.ecosystem:5770`, `Environmental > Terrestrial > Soil > Greenhouse > Agricultural land`, `agricultural_land__3f57770d`, or exact `Agricultural land` label. | Supported. |

The generated minted identity is internally consistent, but one generated
hierarchy edge is too strong. The source path places `Agricultural land` under
GOLD's `Greenhouse` bin, yet `parent_habitats` is an is-a assertion and
`ENVO:03600087` is a physical greenhouse construction rather than a kind of
agricultural ecosystem or soil.

## Evidence

The source attestation is an accurate transcription of the committed GOLD row:

- `source: GOLD`
- `source_id: gold.ecosystem:5770`
- `source_label: Agricultural land`
- `source_path: Environmental > Terrestrial > Soil > Greenhouse > Agricultural land`
- `mapping_predicate: skos:narrowMatch`

The attestation correctly omits `assertion_count` and `assertion_unit`: the
maintained GOLD ecosystem-path row for `gold.ecosystem:5770` has
`organism_count`, `study_count`, `biosample_count`, and `total_assertions` all
set to `0`.

Additional GOLD-derived evidence is consistent with a greenhouse-soil source:

| Source | Maintained row | Assessment |
|---|---|---|
| Biosamples | `data/raw/gold_path_biosamples.tsv` maps the same exact path and node `5770` to 17 biosamples. | Supported; biosample counts are independent of the `ORGANISM` count in the generated source attestation and are not emitted on this record. |
| MIxS broad scale | `data/raw/gold_path_triads.tsv` summarizes 17 samples from 1 study and ranks `ENVO:00000446` `terrestrial biome` as the broad-scale top term with 100% sample share. | Supported as contextual evidence; the single agreeing study makes it weaker than multi-study corroboration. |
| MIxS local scale | The same triad block ranks `ENVO:00000078` `farm` as the local-scale top term with 100% sample share from 1 study. | Supports an agricultural local context. |
| MIxS medium | The same triad block ranks `ENVO:00005780` `greenhouse soil` as the medium top term with 100% sample share from 1 study. | Supports greenhouse soil as the submitted material context; it does not make `greenhouse` an is-a parent. |

## Completeness

- The generated target contains every field supported by the maintained GOLD
  and ENVO inputs: minted identity, terrestrial category, seeded grounding
  status, two generated parents, one GOLD source attestation, and one generated
  curation event.
- Empty `synonyms`, `environmental_parameters`, `characteristic_taxa`,
  `evidence`, `causal_graphs`, `discussion`, and `datasets` are appropriate for
  the current maintained inputs. The reviewed GOLD triad rows provide
  contextual evidence for this source path; no maintained input emits them as
  `environmental_parameters`, and no maintained input supplies an output
  synonym, taxon association, claim-level evidence item, causal overlay,
  discussion item, or dataset reference for this source concept.
- Ignored/hidden-inclusive exact searches covered `curation/decisions.tsv`,
  `curation/term_requests.tsv`, `curation/external_xrefs.tsv`, and
  `curation/causal_graphs/` for `habitatmech:GOLD.74d360dcf3`,
  `gold.ecosystem:5770`, `Environmental > Terrestrial > Soil > Greenhouse >
  Agricultural land`, `agricultural_land__3f57770d`, and `Agricultural land`;
  no maintained curation row or overlay was found.
- Ignored/hidden-inclusive `find` searches covered
  `reports/yaml_record_review/` before report creation; no exact
  `*-agricultural_land__3f57770d.md` report was found.

## Findings

### Major

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| `HM-AGR-LAND-3F57770D-001` | `parent_habitats` asserts `ENVO:03600087` `greenhouse` as an is-a parent, but the source concept is agricultural land in GOLD's greenhouse context, not a kind of greenhouse construction. | `data/raw/gold_ecosystem_paths.tsv` places `gold.ecosystem:5770` under `Environmental > Terrestrial > Soil > Greenhouse > Agricultural land`; the GOLD parent-path second pass in `src/habitatmech/seed.py` turns that path containment into a `parent_habitats` edge; `data/raw/ontology_terms.tsv` defines `ENVO:03600087` as a human construction that provides regulated climatic conditions for plant growth; `data/raw/gold_path_triads.tsv` points the sampled material at `ENVO:00005780` `greenhouse soil`, which is found in a greenhouse rather than subclassed to greenhouse. | `src/habitatmech/seed.py` needs a maintained way to suppress or demote this one GOLD parent-path edge without hand-editing `data/habitats/terrestrial/agricultural_land__3f57770d.yaml`. |

No blocker or minor findings found.

## Recommended Edits

1. Add a maintained curation route that can remove or demote a generated GOLD
   parent-path edge when the path parent is contextual, locative, or a
   container rather than strictly broader. Then apply it to
   `Environmental > Terrestrial > Soil > Greenhouse > Agricultural land` so
   `ENVO:03600087` no longer appears in `parent_habitats` for
   `habitatmech:GOLD.74d360dcf3`.

   Maintain the fix in `src/habitatmech/seed.py` plus the relevant curation
   input, then regenerate the corpus. Do not edit
   `data/habitats/terrestrial/agricultural_land__3f57770d.yaml` directly.

## Follow-up Checks

| Scope | Command or check |
|---|---|
| Target schema | `just validate data/habitats/terrestrial/agricultural_land__3f57770d.yaml` |
| Closed-schema validation | `just validate-strict data/habitats/terrestrial/agricultural_land__3f57770d.yaml` |
| Corpus reproduction | `just verify-corpus --max-diffs 1` |
| Worklist/report sanity | `just worklist --limit 2000` and `just report` |
| Causal overlays | `just validate-causal-all` |
| Term-request exports | `just term-requests-check` |
| History | `just validate-history` |
| Manual hierarchy check | Confirm `data/habitats/terrestrial/agricultural_land__3f57770d.yaml` keeps `ENVO:00000077` and drops `ENVO:03600087` from `parent_habitats` after regeneration |
| Diff hygiene | `git diff --check` |

## Additional Notes

This defect matches the edge shape found in
`reports/yaml_record_review/20260924T052832Z-chemocline.md`: a GOLD parent path
can be a correct source-vocabulary context while still being too strong as a
LinkML `parent_habitats` is-a claim.
