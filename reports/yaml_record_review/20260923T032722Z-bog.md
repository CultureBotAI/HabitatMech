# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/bog.yaml
- Started UTC: 2026-09-23T03:23:00Z
- Finished UTC: 2026-09-23T03:27:22Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/bog.yaml` |
| Identifier | `habitatmech:GOLD.42a3b7b2d3` |
| Label | `Bog` |
| Category | `AQUATIC` |
| Grounding | `NARROW` |
| Mapping status | `SEEDED` |
| Source | `GOLD` / `gold.ecosystem:4185` |
| Source path | `Environmental > Aquatic > Freshwater > Wetlands > Bog` |

The record is generated from the committed GOLD inventory rather than
maintained by hand. `data/habitats/PATHS.tsv:1793` pins
`habitatmech:GOLD.42a3b7b2d3` to the `bog` slug, and the identifier matches
`sha1("GOLD:Environmental > Aquatic > Freshwater > Wetlands > Bog")[:10]`.

The full generated record was read. It contains the GOLD attestation, two
strictly broader parents, and a single seed history event; it has no authored
definition, characteristic taxa, environmental parameters, causal graphs,
discussions, or datasets.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/aquatic/bog.yaml` | Passed: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/bog.yaml` | Passed: 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Passed: 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed: the generated term-request table is current with 109 terms. |
| `just validate-history` | Passed: 77 history records validate against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed: 3,206 expected records, 3,206 found, 0 missing, 0 extra, 0 differing. |
| `just worklist --limit 2000` | Passed: 0 ungrounded records still undecided; 1,810 decisions on file. |
| `just report` | Passed: the corpus report completed and still reports 0 risky unreviewed groundings, 0 contradicted class-level sweeps, 0 swept path compounds, and 0 records claiming a non-habitat is a habitat. |

## Identity and Grounding

The record denotes the GOLD leaf `Bog` under
`Environmental > Aquatic > Freshwater > Wetlands`, not every upstream bog use.
The raw source row at `data/raw/gold_ecosystem_paths.tsv:324` records the same
canonical path, leaf label `Bog`, one GOLD node, node id
`gold.ecosystem:4185`, and 38 organism assertions. The generated
`source_attestations` entry preserves that path and count with
`assertion_unit: ORGANISM`, which is the source-specific unit GOLD uses.

The minted identifier is appropriate. The seeder mints GOLD records from the
canonical path, and the broader `ENVO:00000044` match is deliberately not an
exact identity here because this corpus has multiple non-host GOLD leaves named
`Bog`, including `Environmental > Terrestrial > Soil > Peat > Bog`.

The two generated parents are both broader than the GOLD path:

| Parent | Support |
|---|---|
| `ENVO:00000043` / wetland area | The parent GOLD path `Environmental > Aquatic > Freshwater > Wetlands` is item-reviewed in `curation/decisions.tsv` and grounded exactly to wetland area; `data/raw/ontology_terms.tsv:6639` defines wetland area as a vegetated area overlapping a wetland ecosystem. |
| `ENVO:00000044` / peatland | `data/raw/ontology_terms.tsv:6640` defines peatland as a wetland ecosystem that accumulates acidic peat and lists `bog` and `peat bog` as synonyms. Because the GOLD leaf match is broader than this exact path, the seeder keeps the GOLD identity minted and hangs it under `peatland` with `skos:narrowMatch`. |

The `AQUATIC` category follows the GOLD top-level path
`Environmental > Aquatic` and is therefore consistent with this source concept.

## Evidence

The GOLD source attestation is supported by the committed raw inventory:

- `data/raw/gold_ecosystem_paths.tsv:324` has
  `Environmental > Aquatic > Freshwater > Wetlands > Bog`, leaf `Bog`,
  `organism_count: 38`, `total_assertions: 38`, and
  `gold.ecosystem:4185`.
- `data/raw/gold_path_biosamples.tsv:373` separately records 42 biosamples for
  ecosystem path id `4185`; the generated attestation correctly keeps GOLD's
  organism assertion count rather than substituting the biosample count.
- `data/raw/gold_path_triads.tsv:419-421` records MIxS broad, local, and
  medium slots for 14 samples from two studies. Those terms are contextual
  environmental-triad evidence only; no generated field incorrectly promotes
  the broad aquatic biome, local wetland area, or medium forest soil slot to the
  `Bog` identity.

No external citation, quoted snippet, characteristic-taxon claim,
environmental parameter, causal edge, discussion, or dataset is present in the
record, so there was no claim-level literature evidence to verify.

## Completeness

For a seeded, GOLD-only, NARROW record, the current minimal form is complete:
it preserves the source path and assertion count, keeps the ambiguous leaf as a
minted HabitatMech concept, and records the two broader parents the seeder can
support.

Hidden/ignored-inclusive exact searches found no maintained curation row, term
request, causal-graph overlay, history record, or target-specific research
report for `habitatmech:GOLD.42a3b7b2d3` or `gold.ecosystem:4185`. A
gitignore-independent `find` over `reports/yaml_record_review` found no exact
prior `*-bog.md` review report; the only filename hit for `*bog*` in the
review reports was the unrelated `blanket_bog` report.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

None required for this pass. If a later curator adds an item-level decision for
`habitatmech:GOLD.42a3b7b2d3`, the maintained owner is
`curation/decisions.tsv`; the narrowest proof would be `just seed`, `just
seed-canary habitatmech:GOLD.42a3b7b2d3`, `just validate
data/habitats/aquatic/bog.yaml`, `just validate-strict
data/habitats/aquatic/bog.yaml`, and `just verify-corpus --max-diffs 1`.

## Additional Notes

The vendored ontology slice also contains `bog` as a synonym of several toilet
fixture and latrine terms. Those rows are not directly referenced and were not
used by this record; the generated parent came from the habitat sense on
`ENVO:00000044`.
