# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/cave_water__c4d82240.yaml
- Started UTC: 2026-09-24T01:52:00Z
- Finished UTC: 2026-09-24T01:54:49Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.6251bef319` |
| Label | `Cave water` |
| Habitat category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Generated path | `data/habitats/aquatic/cave_water__c4d82240.yaml` |
| Maintained owner | `curation/decisions.tsv`, `curation/term_requests.tsv`, and GOLD source inventories under `data/raw/` |

`data/habitats/aquatic/cave_water__c4d82240.yaml` is generated. Future
curation must change maintained inputs and regenerate the YAML rather than
patching this record directly.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/cave_water__c4d82240.yaml` | Pass - LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/cave_water__c4d82240.yaml` | Pass - 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Target causal overlay validator | Not applicable - ignored-independent `find` searches found no `*cave*` overlay under `curation/causal_graphs`; `*water*` only found generic overlays for `fresh_water`, `liquid_water`, `sea_water`, `waste_water`, `hypersaline_water`, `brackish_water`, and `marine_water_body`. |
| `just validate-causal-all` | Pass - 32 causal-graph curation files with 32 graphs validated. |
| Reference validator | Not applicable - this seeded record has no `evidence`, `causal_graphs`, `discussions`, or `datasets` reference block to validate with a focused record-level reference checker. |
| `just term-requests-check` | Pass - committed term-request table is current with 109 terms. |
| `just validate-history` | Pass - 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass - expected and found 3,206 records; the corpus reproduces exactly from `data/raw/`. |
| `just worklist --limit 2000` | Pass - worklist generation completed and reported 0 ungrounded records still undecided, with 1,810 decisions on file. |
| `just report` | Pass - corpus report completed for 3,206 records. |
| `git diff --check` | Pass - no whitespace errors. |

## Identity and Grounding

The generated record denotes the intended GOLD source concept:

| Claim | Review |
|---|---|
| Minted ID | Supported. `habitatmech:GOLD.6251bef319` is the generated identifier for the exact GOLD path `Environmental > Aquatic > Freshwater > Groundwater > Cave water`; `data/habitats/PATHS.tsv` maps the ID to `cave_water__c4d82240`. |
| GOLD source identity | Supported. `gold.ecosystem:4167` is the sole collapsed GOLD ecosystem id for this path in `data/raw/gold_ecosystem_paths.tsv`. |
| Habitat meaning | Supported. The source path denotes groundwater in a cave; GOLD records 2 KGX organism assertions and the newer GOLD path inventory records 12 biosamples for the same path. |
| Exact grounding | Supported as absent. Ignored-inclusive exact searches found no exact `Cave water` term in the vendored ontology slice, `curation/term_requests.tsv`, or `curation/external_xrefs.tsv`. |
| Broader grounding | Needs curation. The target is narrower than vendored `ENVO:01001004` `groundwater`, so the maintained decision should be item-level `GROUND_AS_PARENT`, not class-level `CONFIRM_UNGROUNDED`. |
| Current source-path parent | Supported. `habitatmech:GOLD.7f8e268653` is the generated `Environmental > Aquatic > Freshwater > Groundwater` parent and is a strict broader concept for this GOLD Cave water leaf. |

The GOLD MIxS triad inventory reinforces the freshwater-groundwater context but
does not ground the identity exactly. For this path, `data/raw/gold_path_triads.tsv`
reports 11 triad-bearing biosamples across 2 studies: all 11 use
`ENVO:00000873` `freshwater biome` as the broad term, while local scale is split
across 2 terms led by `ENVO:00012408` `aquifer` at 0.64 share and medium is split
across 2 terms led by `ENVO:01000156` `biofilm material` at 0.64 share. Those
rows show that some submitters sampled cave-water-associated groundwater or
biofilm contexts, but the triad terms are contextual and should not be promoted
to exact Cave water identity.

## Evidence

| Claim | Nearest evidence | Review |
|---|---|---|
| `source_attestations` uses `gold.ecosystem:4167` and the Cave water path | `data/raw/gold_ecosystem_paths.tsv` row for `gold.ecosystem:4167` | Supported. The generated `source_id`, `source_label`, `source_path`, `assertion_count: 2`, and `assertion_unit: ORGANISM` agree with the KGX-backed GOLD ecosystem-path row. |
| The path has newer GOLD biosample/triad context | `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_studies.tsv`, and `data/raw/gold_path_triads.tsv` | Present but not yet a generated attestation. The raw path inventory records 12 biosamples and 3 GOLD studies, including one study shared with `Environmental > Terrestrial > Deep subsurface > Cave > Wall biofilm`. |
| Class-level `CONFIRM_UNGROUNDED` curation history | `curation/decisions.tsv` row for `habitatmech:GOLD.6251bef319` | Mechanically reproduced, but the row is too weak after item review because the source concept is a groundwater habitat with a broader vendored ENVO term. |

The target record has no record-level `evidence`, environmental parameters,
characteristic taxa, causal graph, discussions, or datasets. That is not a
schema error: GOLD is the only generated source attestation for this exact
minted record, and the raw GOLD organism count is represented on
`source_attestations`.

## Completeness

- A future item-level curation row is needed in `curation/decisions.tsv`; the
  existing `CLASS` decision deliberately did not assess whether the concept was
  a habitat and cannot promote this source concept to `REVIEWED`.
- A future definition/term request is needed in `curation/term_requests.tsv` if
  Cave water remains minted. ENVO `groundwater` is a strict broader genus, and
  default `parent_mode=ADD` can preserve the true GOLD Freshwater Groundwater
  source-path parent.
- Exact, ignored-inclusive searches found no prior
  `*-cave_water__c4d82240.md` or `*-cave_water.md` review report, no target
  causal overlay, no research/history filename for `6251bef319`, and no
  research/history filename matching a cave-water slug.

## Findings

| Severity | Finding |
|---|---|
| Major | **Cave water has only a class-level ungrounded decision even though a strict broader ENVO parent is available.** The current `curation/decisions.tsv` row records `CONFIRM_UNGROUNDED` with `review_depth=CLASS`, which only records that no lexical route matched the label. Item review confirms the GOLD concept is a cave groundwater habitat and is narrower than `ENVO:01001004` `groundwater`, so the maintained row should be item-level `GROUND_AS_PARENT` and the minted term should receive a `curation/term_requests.tsv` definition. Owner: `curation/decisions.tsv` and `curation/term_requests.tsv`. |

No blocker findings.

No minor findings.

## Recommended Edits

1. In `curation/decisions.tsv`, replace the `habitatmech:GOLD.6251bef319`
   class-level row with an item-level `GROUND_AS_PARENT` row that points to
   `ENVO:01001004` `groundwater`.
2. In `curation/term_requests.tsv`, add a Cave water definition with
   `ENVO:01001004` as the genus parent and default `parent_mode=ADD`, preserving
   the generated `habitatmech:GOLD.7f8e268653` Freshwater Groundwater parent.
3. Regenerate through `just seed`, `just seed-canary
   habitatmech:GOLD.6251bef319`, and `just seed-apply --force`; then verify
   that `data/habitats/aquatic/cave_water__c4d82240.yaml` has
   `grounding_status: NARROW`, `mapping_status: REVIEWED`, an authored
   definition, item-level curation history, `ENVO:01001004` in
   `parent_habitats`, and no loss of the true GOLD source-path parent.

## Follow-up Checks

- `just validate data/habitats/aquatic/cave_water__c4d82240.yaml`
- `just validate-strict data/habitats/aquatic/cave_water__c4d82240.yaml`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

## Additional Notes

- Searches establishing absence included ignored files: exact `find` checks
  covered `reports/yaml_record_review`, `curation/causal_graphs`, `research`,
  `reports`, and `history`; exact `rg --no-ignore --hidden` checks covered
  source identifiers, labels, ontology CURIEs, and the GOLD source path while
  excluding high-volume generated `pages/`, `build/`, `data/text_map/`, and
  `.git/` data where appropriate.
- A separate BacDive `Cave-water` record exists at
  `data/habitats/other/cave_water.yaml`. It has a different source concept,
  `habitatmech:BACDIVE.9ae6cfc178`, and was not reviewed here.
- `ENVO:00012408` `aquifer` appears as the local-scale triad for 7 of 11
  triad-bearing biosamples from one study, but it denotes a water-bearing
  geologic layer, not the cave water itself. The aquifer record already has a
  separate YAML review covering its inherited GOLD groundwater parent.
