# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/adipose_tissue__28f7c202.yaml`
- Started UTC: 2026-09-23T06:33:56Z
- Finished UTC: 2026-09-23T06:37:01Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.9ddd7a2cf6` |
| Label | `Adipose tissue` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Source concept | `Host-associated > Fish > Integumentary system > Adipose tissue` |
| Maintained owner | Generated from the GOLD path inventory and `data/habitats/PATHS.tsv`; generated YAML remains read-only |
| Locked slug | `data/habitats/PATHS.tsv:2465` maps `habitatmech:GOLD.9ddd7a2cf6` to `adipose_tissue__28f7c202` |

This is the generated GOLD record for the fish-specific adipose-tissue path. Its
source key is deterministic: `sha1("GOLD:Host-associated > Fish > Integumentary
system > Adipose tissue")[:10]` is `9ddd7a2cf6`, matching the generated
`habitatmech:GOLD.9ddd7a2cf6` identifier.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/adipose_tissue__28f7c202.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/adipose_tissue__28f7c202.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | Pass; 0 ungrounded records are still undecided and 1810 decisions are on file |
| `just report` | Pass; the report completed and kept the corpus at 3206 records |
| `git diff --check` | Pass; no whitespace errors |

## Identity and Grounding

The record's identity is internally consistent:

- `data/raw/gold_ecosystem_paths.tsv:1938` directly attests the exact canonical path `Host-associated > Fish > Integumentary system > Adipose tissue`, with leaf label `Adipose tissue`, `depth: 4`, `gold_node_count: 2`, `total_assertions: 0`, and `gold.ecosystem:7547|gold.ecosystem:7548`.
- The generated `source_id: gold.ecosystem:7547` is the first source node in that raw `gold_node_ids` list, and the generated note explicitly points to `data/raw/gold_ecosystem_paths.tsv` for the second node.
- `data/habitats/PATHS.tsv:2465` pins `habitatmech:GOLD.9ddd7a2cf6` to `adipose_tissue__28f7c202`.
- `data/raw/ontology_terms.tsv:12993` gives `UBERON:0001013` the primary label `adipose tissue`, supporting the ontology parent used for the generic tissue meaning.
- `data/habitats/PATHS.tsv:2899` pins the generated fish-specific `Integumentary system` parent `habitatmech:GOLD.d980919c41` to `integumentary_system__bd82bc93`, and `data/raw/gold_ecosystem_paths.tsv:1036` supports that parent path as `Host-associated > Fish > Integumentary system`.

`grounding_status: NARROW` is appropriate. The label-only match to
`UBERON:0001013` is broader than this fish-scoped GOLD concept, and the GOLD
inventory contains same-depth `Adipose tissue` leaves under mammals: human,
birds, fish, and mammals at `data/raw/gold_ecosystem_paths.tsv:1077`, `1881`,
`1938`, and `2055`. Because those leaves tie at the shallowest path depth,
HabitatMech's anti-conflation rule must mint distinct child concepts rather
than let any one of them claim exact identity to generic UBERON adipose tissue.

`mapping_status: SEEDED` is expected. Ignored/hidden-inclusive exact searches
for `habitatmech:GOLD.9ddd7a2cf6`, `adipose_tissue__28f7c202`, and the full
GOLD source path found no maintained `curation/decisions.tsv` row, term request,
causal-graph overlay, or target-specific research artifact that would promote
the record to `REVIEWED`.

## Evidence

| Claim | Nearest source | Review |
|---|---|---|
| GOLD contains a fish integumentary-system child path labelled `Adipose tissue`. | `data/raw/gold_ecosystem_paths.tsv:1938` | Supported exactly |
| Two GOLD ecosystem nodes share that collapsed path, with `gold.ecosystem:7547` used as the representative `source_id`. | `data/raw/gold_ecosystem_paths.tsv:1938` | Supported exactly |
| The fish adipose-tissue path has zero committed upstream assertions. | `data/raw/gold_ecosystem_paths.tsv:1938` | Supported exactly; the generated attestation correctly omits `assertion_count` rather than serializing a zero |
| The record is narrower than generic UBERON adipose tissue. | `data/raw/ontology_terms.tsv:12993`; equal-depth GOLD sibling paths at `data/raw/gold_ecosystem_paths.tsv:1077`, `1881`, `1938`, and `2055` | Supported exactly |
| The record is narrower than the fish integumentary-system GOLD parent. | `data/raw/gold_ecosystem_paths.tsv:1036`, `1938`; `data/habitats/PATHS.tsv:2899` | Supported by the GOLD source hierarchy |

The record has no definition, synonyms, xrefs, environmental parameters,
characteristic taxa, record-level evidence, causal graphs, discussion links,
datasets, or quality flags. Those absences match the source shape: the GOLD
path row supplies an inventory concept with no assertions, no triads, no PREGO
taxa, and no curated overlay.

Unsupported or over-scoped claims: None found.

## Completeness

The GOLD source attestation is complete for
`data/raw/gold_ecosystem_paths.tsv:1938`: the generated record carries the
representative source node, source label, full GOLD source path,
`skos:narrowMatch` predicate, and the multi-node note for the two raw
`gold_node_ids`. The absent `assertion_count` is faithful to the row's zero
`organism_count`, zero `study_count`, zero `biosample_count`, and zero
`total_assertions`.

No curated term request or definition is expected because this source concept
already has a valid broader ontology anchor in `UBERON:0001013`. No causal graph
is expected because this is a generated seed record and the repository requires
independent literature evidence before mechanism edges can be authored.

Ignored/hidden-inclusive exact searches covered the target minted id, the record
stem, the full GOLD source path, `gold.ecosystem:7547`, `gold.ecosystem:7548`,
the parent minted id, and `UBERON:0001013`, excluding generated `build`,
`data/text_map`, and `pages` trees for broad content searches. They found the
cited GOLD rows, UBERON row, `PATHS.tsv` lock rows, generated YAML records, and
related generated fish integumentary-system children, and no maintained row
that should already have changed this target.

## Findings

None found.

No blocker findings.

No major findings.

No minor findings.

## Recommended Edits

None required.

If a future curator wants to promote this seeded GOLD record to reviewed status,
add an item-level `REVIEW` row for `habitatmech:GOLD.9ddd7a2cf6` in
`curation/decisions.tsv` and rerun a canary for
`habitatmech:GOLD.9ddd7a2cf6`. That should be a status-only change: keep the
identifier minted, keep `UBERON:0001013` and
`habitatmech:GOLD.d980919c41` as broader parents, and keep the GOLD attestation
unchanged unless `data/raw/gold_ecosystem_paths.tsv` changes upstream.

## Follow-up Checks

None required beyond the validators already run for this report.

For any future status-only review row, run:

- `just seed`
- `just seed-canary habitatmech:GOLD.9ddd7a2cf6`
- `just validate data/habitats/host_associated/adipose_tissue__28f7c202.yaml`
- `just validate-strict data/habitats/host_associated/adipose_tissue__28f7c202.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-adipose_tissue__28f7c202.md' -print` found no pre-existing exact review report for this record before this file was written, and `find` included ignored files under the searched directory.
- Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.9ddd7a2cf6`, `adipose_tissue__28f7c202`, `Host-associated > Fish > Integumentary system > Adipose tissue`, `gold.ecosystem:7547`, `gold.ecosystem:7548`, `habitatmech:GOLD.d980919c41`, and `UBERON:0001013` found the cited raw, path-lock, and generated-record rows, and no target-specific curated input rows.
