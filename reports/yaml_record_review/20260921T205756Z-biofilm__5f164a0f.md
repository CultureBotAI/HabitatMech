# YAML Record Review: Biofilm

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/biofilm__5f164a0f.yaml`
- Started UTC: 2026-09-21T20:57:56Z
- Finished UTC: 2026-09-21T20:58:18Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/biofilm__5f164a0f.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.5298ffd023` |
| Label | `Biofilm` |
| Category | `AQUATIC` |
| Grounding | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained owner | generated from `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/PATHS.tsv`, and the GOLD parent-path hierarchy |

The record is a generated GOLD-only child for `Environmental > Aquatic > Freshwater > Aquifer > Biofilm`. It has no authored definition, synonyms, xrefs, environmental parameters, taxa, evidence items, causal graphs, discussions, external datasets, or item-level curation history.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/aquatic/biofilm__5f164a0f.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/biofilm__5f164a0f.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| Reference validator | Not available: `justfile` exposes no reference-specific validator, and this record has no `EvidenceItem`, `EnvironmentalParameter.reference`, `CharacteristicTaxon.reference`, or causal-edge evidence to target. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the committed ENVO term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 found, 0 missing, 0 extra, 0 differing. |

## Identity and Grounding

| Claim | Trace | Assessment |
|---|---|---|
| `habitatmech:GOLD.5298ffd023` is the pinned identifier for `biofilm__5f164a0f`. | `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.5298ffd023` to `biofilm__5f164a0f`. | Supported. |
| The GOLD source is Aquifer > Biofilm. | `data/raw/gold_ecosystem_paths.tsv` has the exact row `Environmental > Aquatic > Freshwater > Aquifer > Biofilm`, leaf label `Biofilm`, depth 5, and node id `gold.ecosystem:5446`; the YAML carries the same source id, label, and path. | Supported. |
| `ENVO:00002034` is the seeded ambiguous-leaf parent. | `data/raw/ontology_terms.tsv` has `ENVO:00002034` `biofilm`. The exact ontology-grounded `ENVO:00002034` record is the generic multi-source biofilm record, while this Aquifer > Biofilm row is a deeper path-qualified GOLD leaf. | Supported as a broader biofilm parent. |
| `habitatmech:GOLD.d61fa6c9ee` is inherited from the GOLD `Aquifer` parent path. | `data/raw/gold_ecosystem_paths.tsv` has the parent row `Environmental > Aquatic > Freshwater > Aquifer` as `gold.ecosystem:5373`, and `data/habitats/PATHS.tsv` maps that parent to `aquifer__f620c815`. | Mechanically supported as source context, but likely false as an `is-a` parent because an aquifer biofilm is a biofilm in an aquifer rather than a kind of aquifer. |
| `mapping_status: SEEDED` follows from maintained inputs. | Ignored-file-inclusive exact searches over `curation/decisions.tsv`, `curation/term_requests.tsv`, `history/`, and `curation/causal_graphs/` found no row for `habitatmech:GOLD.5298ffd023`; the only curation-history event is the generated 2026-08-16 `seed_from_sources` event. | Supported. |

## Evidence

| Area | Status |
|---|---|
| Source attestation | Supported by the collapsed GOLD path row. The row reports one GOLD node and `organism_count=0`, so the record correctly omits `assertion_count`. |
| GOLD API context | Empty for this exact path. Ignored-file-inclusive exact searches of `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, and `data/raw/gold_studies.tsv` found no `Environmental > Aquatic > Freshwater > Aquifer > Biofilm` row. |
| Definition | Empty. No curation decision or term request defines this path-qualified biofilm. |
| Synonyms and xrefs | Empty. No maintained xref or synonym row exists for this minted child. |
| Environmental parameters | Empty. The environments table has generic `ENVO:00002034` biofilm bands, but no maintained input provides Aquifer > Biofilm-specific physicochemical values. |
| Characteristic taxa | Empty. The record has no PREGO, BacDive, or Madin source attestation. |
| Causal graphs | Empty. `curation/causal_graphs/biofilm.yaml` belongs to the ontology-grounded `ENVO:00002034` generic biofilm record, not this Aquifer > Biofilm child. |
| Discussions and datasets | Empty. No maintained input currently targets this record. |

No evidence item is over-scoped because the record has no curator-authored evidence-bearing claim.

## Completeness

- An ignored-file-inclusive `find` found no pre-existing exact `*-biofilm__5f164a0f.md` report before this report was created.
- Ignored-file-inclusive exact searches for `habitatmech:GOLD.5298ffd023`, `gold.ecosystem:5446`, `Environmental > Aquatic > Freshwater > Aquifer > Biofilm`, `habitatmech:GOLD.d61fa6c9ee`, and `biofilm__5f164a0f` covered `data/raw`, `curation`, `history`, `research/habitats`, `reports/yaml_record_review`, `data/habitats/PATHS.tsv`, and generated `data/habitats`.
- Those searches found the raw GOLD path, PATHS, and generated YAML rows for the Aquifer > Biofilm target; they found no item-level `curation/decisions.tsv` row, no term request, no target-owned causal overlay, no target-owned history record, no target-specific research report, and no prior exact YAML review.
- The exact parent-path searches found one earlier review for the generated Aquifer parent. That report references this child as generated evidence for the Aquifer source hierarchy, but it is not a review of this Aquifer > Biofilm record.
- Broader `biofilm` searches found maintained curation for other path-qualified biofilm records. In particular, `habitatmech:GOLD.7f436f8aff` `air scrubber biofilm` already uses `ENVO:00002034` as the genus and `parent_mode=REPLACE` to drop its contextual Air scrubber source parent because the biofilm is inside the scrubber, not a kind of scrubber.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| HM-BIOFILM-5F164A0F-001 | Major | `Environmental > Aquatic > Freshwater > Aquifer > Biofilm` is still only a seeded ambiguous-leaf child, and its generated `habitatmech:GOLD.d61fa6c9ee` Aquifer parent is likely a contextual location rather than a true `is-a` parent. The exact GOLD path says the leaf is a `Biofilm` nested under the GOLD Aquifer path; the maintained Air scrubber biofilm curation uses the same pattern to define a path-qualified biofilm under `ENVO:00002034` while replacing a false inherited source-path parent. | `data/raw/gold_ecosystem_paths.tsv` has `Environmental > Aquatic > Freshwater > Aquifer > Biofilm` as a child path of `Environmental > Aquatic > Freshwater > Aquifer`; `curation/decisions.tsv` has no item row for `habitatmech:GOLD.5298ffd023`; `curation/term_requests.tsv` defines `air scrubber biofilm` under `ENVO:00002034` with `parent_mode=REPLACE` because its source parent is contextual. | `curation/decisions.tsv` and `curation/term_requests.tsv`. |

No blocker or minor findings.

## Recommended Edits

| Priority | Edit | Owner | Follow-up validator |
|---:|---|---|---|
| 1 | Add an item-level decision for `habitatmech:GOLD.5298ffd023` that confirms the record is an aquifer-specific biofilm rather than generic ENVO biofilm or an aquifer. | `curation/decisions.tsv` | `just seed`, `just seed-canary habitatmech:GOLD.5298ffd023 --force`, then `just validate data/habitats/aquatic/biofilm__5f164a0f.yaml`. |
| 2 | If item review confirms the aquifer-specific reading, add a term request whose genus is `ENVO:00002034` `biofilm` and whose `parent_mode` is `REPLACE`, so the generated record drops the inherited `habitatmech:GOLD.d61fa6c9ee` Aquifer parent. | `curation/term_requests.tsv` | `just term-requests-check`, `just validate-strict data/habitats/aquatic/biofilm__5f164a0f.yaml --quiet`, and `just verify-corpus --max-diffs 1`. |

## Follow-up Checks

- Re-run `just validate data/habitats/aquatic/biofilm__5f164a0f.yaml` on the regenerated target.
- Re-run `just validate-strict data/habitats/aquatic/biofilm__5f164a0f.yaml --quiet` to enforce closed-schema output.
- Re-run `just term-requests-check` after adding the term request.
- Re-run `just validate-history` after adding the required append-only curation history.
- Re-run `just verify-corpus --max-diffs 1` to prove the generated YAML still reproduces from `data/raw/` and `curation/`.

## Additional Notes

- The generated YAML should remain read-only; the future fix belongs in `curation/decisions.tsv` and `curation/term_requests.tsv`.
- The exact GOLD collapsed path has zero GOLD organism assertions and does not appear in the committed later GOLD API biosample, study, or MIxS-triad inventories, so item review will need external aquifer-biofilm evidence before it can author a definition.
