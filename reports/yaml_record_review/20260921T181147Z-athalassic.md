# YAML Record Review: Athalassic

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/athalassic.yaml
- Started UTC: 2026-09-21T18:11:47Z
- Finished UTC: 2026-09-21T18:11:47Z
- Verdict: needs curation

## Target

The target is the generated `HabitatRecord` at `data/habitats/aquatic/athalassic.yaml`.

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.06e9da107b` |
| Label | `Athalassic` |
| Category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Source concepts | GOLD `gold.ecosystem:3971` |
| Locked stem | `athalassic` in `data/habitats/PATHS.tsv` |

This review covers GOLD `Environmental > Aquatic > Non-marine Saline and Alkaline > Saline > Athalassic`, a level-5 specific-ecosystem source concept emitted as one HabitatMech-minted record.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/athalassic.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/athalassic.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 records on disk, 0 missing, 0 extra, 0 differing. |
| Reference validator | Not checked: the generated record has no DOI/PMID/URL `EvidenceItem`, no causal-edge evidence, and no curated causal overlay. |

## Identity and Grounding

| Claim | Evidence | Assessment |
|---|---|---|
| The generated record denotes the GOLD `Athalassic` source concept. | `data/raw/gold_ecosystem_paths.tsv` has canonical path `Environmental > Aquatic > Non-marine Saline and Alkaline > Saline > Athalassic`, leaf `Athalassic`, depth `5`, one GOLD node ID `gold.ecosystem:3971`, `organism_count=0`, and `total_assertions=0`. | Supported exactly. |
| The locked generated identifier is stable. | `sha1("GOLD:Environmental > Aquatic > Non-marine Saline and Alkaline > Saline > Athalassic")` starts with `06e9da107b`; `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.06e9da107b` to stem `athalassic`. | Supported exactly. |
| The record has not been item-reviewed yet. | `curation/decisions.tsv` has one row for `habitatmech:GOLD.06e9da107b`, but its `review_depth` is `CLASS`, and the note says the class-level sweep did not assess whether the concept is a habitat. | Needs curation; the final interpretation of the bare GOLD `Athalassic` leaf has to distinguish a real athalassic saline-water habitat from an ionic-composition qualifier. |
| The vendored ontology slice has no exact `athalassic` term. | A hidden/ignored-inclusive search for `athal` across `data/raw/ontology_terms.tsv`, `curation`, `data/habitats/PATHS.tsv`, and generated records found no ontology label or synonym for `athalassic` or `athalassohaline`. | Supports keeping the source concept minted if item review confirms it denotes a real habitat subtype. |
| The inherited `ENVO:01001319` parent comes from the reviewed GOLD parent path. | `Environmental > Aquatic > Non-marine Saline and Alkaline > Saline` resolves to `ENVO:01001319` `saline water body`; its source-specific decision row `habitatmech:GOLD.a5eb3e9d4f` is `REVIEW` at `ITEM` depth, and `ENVO:01001319` is defined as a body of water primarily composed of saline water. | Mechanically and probably semantically supported, but the target itself still needs an item-level decision before curators decide whether to retain this parent through a term request. |

## Evidence

The generated record has no record-level `evidence` entries and no `causal_graphs`.

| Claim | Evidence | Assessment |
|---|---|---|
| The GOLD source attestation preserves the source path and source ID. | `data/raw/gold_ecosystem_paths.tsv` has `gold.ecosystem:3971` on the exact source path, and the generated `source_attestations` entry carries that `source_id` and `source_path`. | Supported exactly. |
| The generated GOLD attestation correctly omits `assertion_count`. | The canonical GOLD ecosystem row has `organism_count=0`, and `src/habitatmech/seed.py` emits a GOLD `assertion_count` only when the row's organism count is nonzero. | Mechanically supported. The direct GOLD path still has 4 biosamples, 3 MIxS triads, and 2 study rows in the later raw path tables; the seeder does not expose those counts on source attestations. |
| The source path is a terminal GOLD path. | Hidden/ignored-inclusive exact searches found no raw GOLD row with prefix `Environmental > Aquatic > Non-marine Saline and Alkaline > Saline > Athalassic >`. | Supported exactly; there are no generated child records to inspect. |
| The coordinate GOLD `Thalassic` sibling is not a maintained precedent. | The sibling `Environmental > Aquatic > Non-marine Saline and Alkaline > Saline > Thalassic` is generated as exact `ENVO:01001667` `thalassic`, but ENVO defines that term as a physical quality rather than as a habitat. | Do not copy that identity path to `Athalassic`; the target needs item-level review rather than another bare quality-label match. |

## Completeness

- The generated record is complete enough for a class-swept source concept: it has a stable HabitatMech identifier, the GOLD source attestation, the generated class-sweep event, and the generated seed event.
- The record correctly omits record-level evidence, causal graphs, characteristic taxa, and environmental parameters because there is no maintained target-specific item decision, term request, or causal overlay yet.
- The record is not complete enough for a defended `UNGROUNDED` curation state because the only decision is a class-depth lexical no-match sweep, not an item-level assessment of GOLD `Athalassic`, ENVO `thalassic`, ENVO `saline water body`, ENVO `hypersaline lake`, ENVO `hypersaline water`, and the parent concept's existing saline-or-alkaline research.
- Hidden/ignored-inclusive exact searches covered `data/raw`, `data/habitats/PATHS.tsv`, `curation`, `history`, `curation/causal_graphs`, prior `reports/yaml_record_review`, and `research/habitats` for `habitatmech:GOLD.06e9da107b`, `gold.ecosystem:3971`, `athalassic`, `Athalassic`, and `Environmental > Aquatic > Non-marine Saline and Alkaline > Saline > Athalassic`. They found the raw GOLD ecosystem, biosample, MIxS triad, and study rows, the locked slug, the class-sweep decision, and contextual discussion in the committed research report for `Non-marine Saline and Alkaline`; they found no target-specific term request, causal overlay, history record, generated child path, or prior YAML review for `athalassic`.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| `HM-ATHALASSIC-001` | Major | `Athalassic` is still only class-reviewed. The current `CONFIRM_UNGROUNDED` row records a lexical no-match cohort, explicitly says it did not assess whether the concept is a habitat, and cannot defend the final `UNGROUNDED` state for this specific GOLD source path. | `curation/decisions.tsv` row `habitatmech:GOLD.06e9da107b` has `review_depth=CLASS`; `docs/HARMONIZATION.md` states that class decisions do not establish whether the concept is a real habitat and do not promote a record to `REVIEWED`; the record remains `mapping_status: SEEDED`. | Replace the class-sweep row with an item-depth row in `curation/decisions.tsv` after checking the GOLD path against available thalassic, saline-water, hypersaline-water, and hypersaline-lake terms. |
| `HM-ATHALASSIC-002` | Major | The minted record has no maintained HabitatMech definition or explicit genus review. GOLD supplies only the terse leaf `Athalassic`, and the existing parent-concept research discusses the chemistry-vs-geography ambiguity but is not itself a target-specific curation input for `habitatmech:GOLD.06e9da107b`. | There is no `curation/term_requests.tsv` row for `habitatmech:GOLD.06e9da107b`; hidden/ignored-inclusive searches found no `athalassic` label in `data/raw/ontology_terms.tsv`; the generated record has no `definition`; the only exact ENVO `thalassic` hit is a physical quality, not a habitat class. | If item review confirms the source is a real habitat subtype, add a `curation/term_requests.tsv` row defining the requested athalassic saline-water class, probably under `ENVO:01001319` `saline water body` with `parent_mode=ADD`. |

No blocker or minor findings found.

## Recommended Edits

1. Replace the class-sweep decision in `curation/decisions.tsv` for `habitatmech:GOLD.06e9da107b` with an item-level decision that records whether GOLD `Environmental > Aquatic > Non-marine Saline and Alkaline > Saline > Athalassic` denotes an athalassic saline-water habitat or only an ionic-composition qualifier.
2. If the source denotes a habitat, add a curated `curation/term_requests.tsv` row for `habitatmech:GOLD.06e9da107b` with an authored label, definition, synonyms or rejected synonyms, and `ENVO:01001319` as the checked parent unless item review finds a tighter existing genus.
3. Regenerate `data/habitats/aquatic/athalassic.yaml` with `just seed-canary habitatmech:GOLD.06e9da107b --force` and inspect the generated target before any wider seed run.
4. Review `data/habitats/aquatic/thalassic.yaml` separately, because the sibling GOLD leaf currently grounds to an ENVO physical-quality class and may be a quality-labeled habitat subtype with the same modeling question.

## Follow-up Checks

| Edit | Narrowest proving check |
|---|---|
| Promote the source path from class sweep to item review. | Re-read the new `curation/decisions.tsv` row and confirm it keys `habitatmech:GOLD.06e9da107b`, uses `review_depth=ITEM`, and explains how the bare `Athalassic` leaf was interpreted in context. |
| Add an athalassic definition. | Re-read the new `curation/term_requests.tsv` row, run `just term-requests-check`, then run `just seed-canary habitatmech:GOLD.06e9da107b --force` and confirm `data/habitats/aquatic/athalassic.yaml` gains the authored definition while retaining the GOLD source attestation. |
| Validate the regenerated record and corpus. | `just validate data/habitats/aquatic/athalassic.yaml`, `just validate-strict data/habitats/aquatic/athalassic.yaml --quiet`, `just validate-history`, `just term-requests-check`, and `just verify-corpus --max-diffs 1`. |

## Additional Notes

- The direct GOLD path has rows in `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, and `data/raw/gold_studies.tsv`; the generated missing `assertion_count` comes from `data/raw/gold_ecosystem_paths.tsv` having zero organism assertions for this node.
- The committed `Non-marine Saline and Alkaline` research report has useful Athalassic-vs-Thalassic notes, but it scoped its curated output to parent record `habitatmech:GOLD.ce244e62cd`; future item review can read it as a lead without treating it as a maintained input for this target.
