# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/terrarium.yaml
- Started UTC: 2026-09-25T12:43:00Z
- Finished UTC: 2026-09-25T12:48:34Z
- Verdict: pass

## Target

Reviewed the complete generated record at `data/habitats/engineered/terrarium.yaml`. The record is a GOLD-only engineered habitat for `Engineered > Artificial ecosystem > Vivarium > Terrarium`, grounded exactly to `ENVO:00000349` `terrarium`.

The generated file has `definition_source: ENVO`, `habitat_category: ENGINEERED`, `grounding_status: EXACT`, and `mapping_status: SEEDED`. Its only parent is the ENVO ontology parent `ENVO:00010622` `vivarium`. Its only source attestation shows `gold.ecosystem:8029` and correctly notes that two GOLD ecosystem node IDs share this canonical path.

The file is generated from `data/raw/`; no item-level decision, authored term request, source extractor override, or causal-graph overlay currently contributes to this record. Future grounding changes belong in `curation/decisions.tsv` keyed by source concept `habitatmech:GOLD.aa6ba5aec0`; future authored definition or parent-mode changes would belong in `curation/term_requests.tsv`; future causal overlays would belong under `curation/causal_graphs/`.

## Validation

All required local checks passed:

- `just validate data/habitats/engineered/terrarium.yaml`
- `just validate-strict data/habitats/engineered/terrarium.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000 --status all --out /tmp/habitatmech-terrarium-worklist.tsv`
- `just report`

No validator was skipped.

## Identity and Grounding

The generated identifier, label, category, source attestation, and path slug agree with the maintained inputs. `data/raw/gold_ecosystem_paths.tsv` has the exact `Engineered > Artificial ecosystem > Vivarium > Terrarium` row at depth 4, with two GOLD node IDs, zero `organism_count`, `study_count`, `biosample_count`, and `total_assertions` values in this bulk-export summary, and `gold.ecosystem:8029|gold.ecosystem:8030` as the node list. `data/habitats/PATHS.tsv` maps `ENVO:00000349` to the stable `terrarium` slug.

The `EXACT` grounding and `SEEDED` mapping follow from the GOLD leaf-label route. `data/raw/ontology_terms.tsv` defines `ENVO:00000349` with canonical label `terrarium`; the generated definition is copied from that vendored ENVO row. The deterministic GOLD source-concept key for the path is `habitatmech:GOLD.aa6ba5aec0`, and exact ignored-inclusive searches found no row for that key in `curation/decisions.tsv`, so the record remains `SEEDED` rather than `REVIEWED`.

The broader term is traceable. `data/raw/ontology_subclass_edges.tsv` has `ENVO:00000349 rdfs:subClassOf ENVO:00010622`, and `data/raw/ontology_terms.tsv` defines `ENVO:00010622` as `vivarium`. The immediate GOLD path prefix `Engineered > Artificial ecosystem > Vivarium` also currently resolves to `data/habitats/engineered/vivarium.yaml`, which is exactly grounded to the same ENVO parent.

## Evidence

- Supported as generated source provenance: exact ignored-inclusive searches found `gold.ecosystem:8029`, `gold.ecosystem:8030`, and `Engineered > Artificial ecosystem > Vivarium > Terrarium` in `data/raw/gold_ecosystem_paths.tsv`; `gold.ecosystem:8029` is the first node shown in the generated source attestation.
- Supported as generated ontology grounding: `ENVO:00000349` `terrarium` is present in `data/raw/ontology_terms.tsv`, and the record's definition and `ENVO:00010622` parent are vendored ENVO content.
- Supported as generated hierarchy: `ENVO:00010622` is both the asserted ontology parent of `ENVO:00000349` and the grounded identifier for the immediate GOLD parent path, `Engineered > Artificial ecosystem > Vivarium`.
- Supported as generated status: the contributing GOLD source concept has no item-level decision, so `mapping_status: SEEDED` follows from the current decision table.

The record has no causal graphs, environmental parameters, characteristic taxa, datasets, discussions, or claim-level `EvidenceItem` objects to inspect.

## Completeness

The generated attestation is complete for the committed GOLD ecosystem inventory. `data/raw/gold_ecosystem_paths.tsv` lists both source node IDs, and the generated record's first-node note accounts for the second node ID.

Exact ignored-inclusive searches of the GOLD API side tables found no `gold.ecosystem:8029`, `gold.ecosystem:8030`, or exact `Engineered > Artificial ecosystem > Vivarium > Terrarium` rows in `data/raw/gold_studies.tsv`, `data/raw/gold_path_biosamples.tsv`, or `data/raw/gold_path_triads.tsv`; no GOLD study, biosample, or MIxS triad context is committed for this exact path.

Exact ignored-inclusive searches over `history/`, `research/habitats/`, `curation/causal_graphs/`, `curation/term_requests.tsv`, `curation/term_requests/`, `curation/term_requests_excluded.tsv`, `conf/id_label_targets.yaml`, and `reports/yaml_record_review/` found no target-specific append-only history record, research report, causal overlay, authored term request, generated ENVO term-request row, term-request exclusion, label-correspondence residual, or prior review for `habitatmech:GOLD.aa6ba5aec0`, `ENVO:00000349`, `gold.ecosystem:8029`, `gold.ecosystem:8030`, or `Engineered > Artificial ecosystem > Vivarium > Terrarium`.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

None required. If future curation changes this grounding or adds an authored definition, rerun `just seed`, a focused `just seed-canary` for `ENVO:00000349`, `just validate-strict data/habitats/engineered/terrarium.yaml`, `just term-requests-check`, `just validate-history`, `just validate-causal-all`, `just verify-corpus --max-diffs 1`, and `just report`.

## Additional Notes

All absence checks in this review used ignored-inclusive `rg --no-ignore --hidden`, exact full-file scans of the committed raw side tables, or `find`, bounded to maintained curation, history, research, raw inventory, generated habitat, and review-report paths. No search depended on rendered `pages/` or text-map JSON.

The generated `just worklist --status all` table is scoped to ungrounded records. It did not include the direct `ENVO:00000349` Terrarium target, because this record is exactly grounded to an ENVO term.
