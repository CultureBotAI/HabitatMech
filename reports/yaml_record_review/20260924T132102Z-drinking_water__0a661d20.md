# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/drinking_water__0a661d20.yaml
- Started UTC: 2026-09-24T13:21:02Z
- Finished UTC: 2026-09-24T13:21:02Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Path | `data/habitats/engineered/drinking_water__0a661d20.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.cfa077b4b9` |
| Label | `Drinking water` |
| Category | `ENGINEERED` |
| Generated or maintained | Generated from committed GOLD source inventories, vendored ontology terms, and `data/habitats/PATHS.tsv`; `data/habitats/` remains read-only |
| Locked slug | `data/habitats/PATHS.tsv:2817` maps `habitatmech:GOLD.cfa077b4b9` to `drinking_water__0a661d20` |
| Grounding | `NARROW` |
| Mapping status | `SEEDED` |

## Validation

| Check | Result |
| --- | --- |
| `find reports/yaml_record_review -maxdepth 1 -type f -name '*-drinking_water__0a661d20.md' -print` | Passed; no prior exact `drinking_water__0a661d20` review was present before this report was written. `find` included ignored files under `reports/yaml_record_review`. |
| `just validate data/habitats/engineered/drinking_water__0a661d20.yaml` | Passed; no LinkML issues found. |
| `just validate-strict data/habitats/engineered/drinking_water__0a661d20.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Reference validator | Not applicable; the target record has no record-level `evidence`, no causal graph, and no DOI, PMID, or URL evidence items to validate. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 records were expected, 3206 were present, and there were 0 missing, extra, or differing records. |
| `just worklist --limit 2000` | Passed; 0 ungrounded records are still undecided and 1810 decisions are on file. |
| `just report` | Passed; completed the corpus report for 3206 records. |
| `git diff --check` | Passed before report creation. |

## Identity and Grounding

The minted identity is traceable to the GOLD path `Engineered > Built environment > Drinking water treatment plant > Drinking water`. `data/raw/gold_ecosystem_paths.tsv:254` lists that canonical path, leaf label `Drinking water`, depth 4, `gold_node_count` 2, `organism_count` 68, `total_assertions` 68, and node ids `gold.ecosystem:5499|gold.ecosystem:5500`. The generated source attestation repeats the path, uses first node id `gold.ecosystem:5499`, records 68 `ORGANISM` assertions, and states that two GOLD nodes share the path.

The `NARROW` ENVO parent is explainable. `data/raw/ontology_terms.tsv:7376` labels `ENVO:00003064` as `drinking water` and defines it as potable water. This engineered GOLD child is not the general ENVO class because it is scoped to drinking water inside the built-environment treatment-plant branch; retaining the ENVO term as a broader parent avoids merging it with the aquatic `Environmental > Aquatic > Freshwater > Drinking water` GOLD branch or the broad ENVO `data/habitats/aquatic/drinking_water.yaml` record.

The record also inherits its immediate GOLD parent path as `ENVO:03600004`. That edge follows the raw GOLD tree, but `data/raw/ontology_terms.tsv:10053` marks `ENVO:03600004` `drinking water treatment plant` deprecated. A curation decision for the parent source concept `habitatmech:GOLD.1a2ab4986f` should redirect that parent path to a current replacement or a minted HabitatMech record, and this child should inherit the regenerated parent identifier from that maintained fix.

An ignored/hidden-inclusive exact search for `habitatmech:GOLD.cfa077b4b9` over `curation`, `history`, `research`, `reports`, `data/habitats/PATHS.tsv`, and `data/habitats` found no maintained curation decision or history event for this exact source concept. `mapping_status: SEEDED` is therefore consistent.

## Evidence

| Claim | Evidence | Status |
| --- | --- | --- |
| The generated record denotes the GOLD source path `Engineered > Built environment > Drinking water treatment plant > Drinking water`. | `data/raw/gold_ecosystem_paths.tsv:254` lists that canonical path; `data/habitats/engineered/drinking_water__0a661d20.yaml:10-17` repeats it in the GOLD source attestation. | Supported exactly. |
| The GOLD aggregate has two node ids and 68 organism assertions. | `data/raw/gold_ecosystem_paths.tsv:254` reports `gold_node_count` 2, `organism_count` 68, `total_assertions` 68, and node ids `gold.ecosystem:5499|gold.ecosystem:5500`. | Supported exactly. |
| `ENVO:00003064` is a current broader drinking-water term. | `data/raw/ontology_terms.tsv:7376` labels `ENVO:00003064` `drinking water`, gives the exact synonym `potable water`, and does not mark the term deprecated. | Supported exactly. |
| `ENVO:00003064` is an ENVO child of fresh water. | `data/raw/ontology_subclass_edges.tsv:5495` asserts `ENVO:00003064 rdfs:subClassOf ENVO:00002011`; `data/raw/ontology_terms.tsv:7173` labels `ENVO:00002011` as `fresh water`. | Supported exactly. |
| The generated `ENVO:03600004` parent represents the immediate GOLD parent path. | The generated parent record `data/habitats/engineered/drinking_water_treatment_plant.yaml` is attested by `Engineered > Built environment > Drinking water treatment plant`, the immediate prefix of the target GOLD path. | Supported, but the inherited identifier is obsolete. |
| The target has no MIxS triads to emit as `environmental_parameters`. | An ignored/hidden-inclusive exact search for the target path in `data/raw/gold_path_triads.tsv` found no rows. | Supported exactly. |
| No causal overlay is attached to `habitatmech:GOLD.cfa077b4b9`. | Ignored/hidden-inclusive exact search over `curation/causal_graphs` found no `identifier: habitatmech:GOLD.cfa077b4b9`, and `find curation/causal_graphs -maxdepth 1 -type f -name '*drinking*' -print` returned no files. | Supported exactly. |

## Completeness

The generated record is intentionally sparse but complete for the current committed inputs for this GOLD concept. The exact source path has the GOLD aggregate row, one `data/raw/gold_path_biosamples.tsv` row for `gold.ecosystem:5500`, one `data/raw/gold_studies.tsv` row, no GOLD MIxS triads, no record-level evidence, no causal graph, no characteristic taxa, and no environmental parameters.

Ignored/hidden-inclusive exact searches covered `data/raw`, `curation`, `history`, `research`, `reports`, `data/habitats/PATHS.tsv`, `data/habitats`, and `conf/id_label_targets.yaml`. They found the target source path, locked slug, generated record, the environmental `drinking water` siblings, the deprecated treatment-plant parent and sibling filter child, three existing decisions that also use `ENVO:03600004` as a broad water-treatment parent, and no target-specific maintained decision, term request, history event, causal overlay, or id-label residual exception.

The sibling `Drinking water filter` record at `data/habitats/engineered/drinking_water_filter.yaml` inherits the same deprecated `ENVO:03600004` treatment-plant parent from `Engineered > Built environment > Drinking water treatment plant > Drinking water filter`. It is out of scope for this report, but the same parent-path curation for `habitatmech:GOLD.1a2ab4986f` should repair both generated children.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| major | The generated record inherits a deprecated immediate parent from the GOLD treatment-plant parent path. | `data/habitats/engineered/drinking_water__0a661d20.yaml:6-8` lists `ENVO:03600004` as a parent; `data/raw/ontology_terms.tsv:10053` marks that class deprecated; and `data/raw/gold_ecosystem_paths.tsv:254` places the target path directly under `Engineered > Built environment > Drinking water treatment plant`, so the obsolete parent enters through the generated GOLD parent-path edge rather than through a target-specific curation row. | Add an item-level row for parent source `habitatmech:GOLD.1a2ab4986f` in `curation/decisions.tsv`, using a current ontology replacement if one exists or minting the treatment-plant source with `ENVO:03600004` demoted to an xref if no current identity term fits. |

## Recommended Edits

Review the parent source concept `habitatmech:GOLD.1a2ab4986f` against the current ENVO release and add an item-level `curation/decisions.tsv` row so the GOLD `Drinking water treatment plant` path no longer resolves to deprecated `ENVO:03600004`.

After that parent-path fix, rerun a canary for `habitatmech:GOLD.cfa077b4b9` and confirm that `data/habitats/engineered/drinking_water__0a661d20.yaml` keeps `ENVO:00003064` as the drinking-water parent while inheriting a current treatment-plant parent.

Run an exact ignored/hidden-inclusive search for `ENVO:03600004` across `curation`, `data/habitats`, and `data/raw` so the generic water-treatment records and both treatment-plant child records can either switch to the replacement parent or retain the obsolete term only as a related xref.

## Follow-up Checks

Rerun `just seed-canary habitatmech:GOLD.1a2ab4986f` and `just seed-canary habitatmech:GOLD.cfa077b4b9` after adding the parent decision or term request, then inspect the regenerated treatment-plant and drinking-water child records.

Rerun `just validate <record>`, `just validate-strict <record>`, `just verify-corpus --max-diffs 1`, `just term-requests-check`, `just validate-history`, `just worklist --limit 2000`, and `just report` to prove the maintained curation and regenerated corpus agree.

## Additional Notes

`conf/id_label_targets.yaml` tracks known obsolete ENVO residuals accepted by the label-correspondence gate, but an exact ignored/hidden-inclusive search found no entry for `ENVO:03600004` there. The label-correspondence workflow should therefore not be treated as acceptance of this deprecated treatment-plant parent on the target record.
