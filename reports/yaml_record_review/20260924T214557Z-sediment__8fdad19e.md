# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/engineered/sediment__8fdad19e.yaml`
- Started UTC: `2026-09-24T21:44:02Z`
- Finished UTC: `2026-09-24T21:45:57Z`
- Verdict: needs curation

## Target

Reviewed the complete generated record at `data/habitats/engineered/sediment__8fdad19e.yaml`. The record is a GOLD-only engineered habitat for the canonical path `Engineered > Artificial ecosystem > Aquaculture > Crustaceans raceway > Sediment`, minted as `habitatmech:GOLD.f18dafe21d`.

The generated file has `grounding_status: NARROW` and `mapping_status: SEEDED`. It has one true ontology parent, `ENVO:00002007` `sediment`, one inherited GOLD source-path parent, `habitatmech:GOLD.a904b6974e` `Crustaceans raceway`, and one `GOLD` source attestation for `gold.ecosystem:8016`.

## Validation

| Check | Result |
|---|---|
| `find reports/yaml_record_review -maxdepth 1 -type f -name '*-sediment__8fdad19e.md' -print` | Passed before this report was written; no existing exact `sediment__8fdad19e` review was found. `find` is not `.gitignore`-filtered. |
| `just validate data/habitats/engineered/sediment__8fdad19e.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/sediment__8fdad19e.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Focused causal overlay validator | Not applicable; an exact ignored-inclusive search in `curation/causal_graphs/` found no `habitatmech:GOLD.f18dafe21d`, `gold.ecosystem:8016`, `sediment__8fdad19e`, `Crustaceans raceway`, or exact source-path overlay. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| Reference validator | Not applicable; the target record has no record-level `evidence` entries and no causal graph edges with citation references. |
| `just term-requests-check` | Passed; the generated ENVO term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records all exist on disk, with 0 missing, 0 extra, and 0 differing records. |
| `just worklist --limit 2000` | Passed; it reported 0 still-undecided ungrounded records and 1,810 decisions on file. |
| `just report` | Passed; the corpus report completed for 3,206 records. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

The generated identifier, label, category, source attestation, and ontology grounding agree with the committed GOLD inventory. `data/raw/gold_ecosystem_paths.tsv` has the exact child path at depth 5 with `gold_node_count` 1, no direct aggregate organism, study, or biosample assertions, and source ID `gold.ecosystem:8016`. `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.f18dafe21d` to the stable `sediment__8fdad19e` slug.

The `NARROW` grounding is scientifically plausible: this source path denotes sediment in a crustaceans raceway, and the vendored ontology contains `ENVO:00002007` `sediment` as a broader material term. The record is still only seeded because an exact ignored-inclusive search across `data/raw/`, `data/habitats/`, `curation/`, `history/`, `research/`, `conf/`, and `reports/yaml_record_review/` found the generated record, the `PATHS.tsv` row, the GOLD inventory row, and the prior parent review's mentions of this child, but no item-level decision row, term request, history entry, research report, label-correspondence residual, or prior exact review for `habitatmech:GOLD.f18dafe21d`, `gold.ecosystem:8016`, `sediment__8fdad19e`, or the exact source path.

The second generated parent is not a strict broader material parent. The GOLD path provides raceway context for the leaf `Sediment`, while `parent_habitats` is an `is_a` claim. Sediment contained in an engineered aquaculture raceway is not a subtype of the raceway itself, and this target already keeps `ENVO:00002007` as its true broader material parent.

## Evidence

- Supported: the `GOLD` source attestation is traceable to `data/raw/gold_ecosystem_paths.tsv`, including the single upstream node ID `gold.ecosystem:8016`.
- Supported: the generated `ENVO:00002007` parent follows from the anti-conflation rule for a deep GOLD `Sediment` leaf; the ontology slice contains `ENVO:00002007` as a valid broader sediment material class.
- Supported side-table context: the exact ignored-inclusive search across the GOLD biosample, triad, study, and ecosystem-path side tables found only the canonical `gold_ecosystem_paths.tsv` row for `gold.ecosystem:8016` or the exact source path. No committed auxiliary GOLD side table adds target-specific biosample, study, or MIxS triad claims for this child.
- Unsupported hierarchy: the generated edge from `habitatmech:GOLD.f18dafe21d` to `habitatmech:GOLD.a904b6974e` is an inherited GOLD source-path edge. The child record denotes sediment in a raceway, not a subtype of the containing raceway.
- Under-reviewed: the record has no item-level decision, authored definition, term request, record-level evidence, discussion, dataset, characteristic taxon, environmental parameter, or causal graph.

## Completeness

The committed source inventories are sufficient to explain the current generated record. Exact ignored-inclusive searches covering `data/raw/`, `data/habitats/`, `curation/`, `history/`, `research/`, `conf/`, `reports/yaml_record_review/`, and `curation/causal_graphs/` found no maintained item-level decision, no term request, and no target causal overlay to audit.

The empty optional slots are appropriate for a seeded GOLD path with no curated overlay: no maintained input currently provides characteristic taxa, environmental parameters, causal edges, or record-level cited evidence for the target. The parent `Crustaceans raceway` record was reviewed separately and its report already identified this generated child as a separate sediment-in-raceway concept.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| Major | `Sediment` inherits `Crustaceans raceway` as an `is_a` parent even though the child denotes sediment contained in the engineered raceway, not a subtype of that raceway. | The generated YAML has both `ENVO:00002007` and `habitatmech:GOLD.a904b6974e` in `parent_habitats`. The exact GOLD source path is `Engineered > Artificial ecosystem > Aquaculture > Crustaceans raceway > Sediment`, and the generated target already keeps the true broader `ENVO:00002007` sediment material parent. | `curation/decisions.tsv`; `curation/term_requests.tsv` |

## Recommended Edits

1. Item-review `habitatmech:GOLD.f18dafe21d` in `curation/decisions.tsv` against the exact `Engineered > Artificial ecosystem > Aquaculture > Crustaceans raceway > Sediment` source path, the existing `ENVO:00002007` sediment parent, and the generated `habitatmech:GOLD.a904b6974e` Crustaceans raceway parent.
2. If the item review keeps this as a narrower minted sediment concept under `ENVO:00002007`, add a definition in `curation/term_requests.tsv` whose genus is the true `sediment` material parent and use `parent_mode=REPLACE` so regeneration drops the inherited `Crustaceans raceway` source-path parent.
3. Keep raceway or aquaculture context out of `parent_habitats` unless a future maintained input has a non-`is_a` slot for local source-path context.

## Follow-up Checks

- Run `just seed`, then `just seed-canary habitatmech:GOLD.f18dafe21d`, and inspect `data/habitats/engineered/sediment__8fdad19e.yaml` before any wider `just seed-apply --force`.
- Run `just validate data/habitats/engineered/sediment__8fdad19e.yaml`, `just validate-strict data/habitats/engineered/sediment__8fdad19e.yaml`, `just term-requests-check`, `just validate-history`, `just verify-corpus --max-diffs 1`, `just worklist --limit 2000`, `just report`, and `git diff --check` after maintained input changes.

## Additional Notes

The review did not find a target causal overlay, so there was no focused `just validate-causal curation/causal_graphs/<slug>.yaml` invocation to run. The whole-overlay validator still passed.

This review left generated products and curation inputs unchanged. Fixing the false parent belongs in the maintained decision and term-request inputs, followed by a normal seed regeneration.
