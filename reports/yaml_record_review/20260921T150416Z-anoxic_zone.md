# YAML Record Review: Anoxic zone

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/anoxic_zone.yaml
- Started UTC: 2026-09-21T15:04:16Z
- Finished UTC: 2026-09-21T15:04:16Z
- Verdict: needs curation

## Target

The target is the generated `HabitatRecord` at `data/habitats/aquatic/anoxic_zone.yaml`.

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.b75e4aa08f` |
| Label | `Anoxic zone` |
| Category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| GOLD source node | `gold.ecosystem:5392` |
| Source path | `Environmental > Aquatic > Freshwater > Lake > Anoxic zone` |
| Locked stem | `anoxic_zone` in `data/habitats/PATHS.tsv` |

This review covers the single GOLD source concept minted from `Environmental > Aquatic > Freshwater > Lake > Anoxic zone`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/anoxic_zone.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/anoxic_zone.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 records on disk, 0 missing, 0 extra, 0 differing. |
| Reference validator | Not checked: this record has no DOI/PMID/URL `EvidenceItem`, no causal-edge evidence, and no target-specific `curation/causal_graphs/` overlay. |

## Identity and Grounding

The record denotes GOLD path `Environmental > Aquatic > Freshwater > Lake > Anoxic zone`. `data/raw/gold_ecosystem_paths.tsv` preserves that path with leaf label `Anoxic zone`, depth 5, one GOLD node, and source node `gold.ecosystem:5392`.

The minted identity is appropriate for the current ontology slice. Vendored ENVO has the related `ENVO:01000066` `marine anoxic zone`, but the GOLD source path is a freshwater-lake zone rather than a marine zone. Vendored ENVO also has `ENVO:01000173` `anoxic water`, but GOLD uses that term in the MIxS `env_medium` slot for this path; the source habitat is the anoxic zone nested under a freshwater-lake path, not the water material alone.

`mapping_status: SEEDED` is correct for the current decision depth. `curation/decisions.tsv` has a target-specific `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.b75e4aa08f`, but its `review_depth` is `CLASS`, and `docs/CURATION.md` says `CLASS` records membership in a mechanical screen and does not promote a record to `REVIEWED`.

Exact hidden/ignored-inclusive searches before this report was created covered the maintained `curation/`, `history/`, `data/raw/`, and `research/habitats/` surfaces plus `data/habitats/PATHS.tsv` and prior `reports/yaml_record_review/` reports; they found the GOLD path, biosample, triad, PATHS, and class-level decision rows cited here plus contextual mentions on the `anoxic_water`, `sludge`, and `waste_water` records, but no item-level decision, term request, target-specific causal overlay, history record, prior YAML review, or target-specific research report for `habitatmech:GOLD.b75e4aa08f`, `gold.ecosystem:5392`, `anoxic_zone`, `Anoxic zone`, or the full GOLD path.

## Evidence

| Claim | Evidence | Assessment |
|---|---|---|
| GOLD attests one `Anoxic zone` source concept at `gold.ecosystem:5392`. | `data/raw/gold_ecosystem_paths.tsv` has `Environmental > Aquatic > Freshwater > Lake > Anoxic zone`, leaf label `Anoxic zone`, `gold_node_count=1`, and `gold_node_ids=gold.ecosystem:5392`. | Supported. |
| The generated source attestation correctly omits an `assertion_count`. | The GOLD path row has `organism_count=0`, `study_count=0`, `biosample_count=0`, and `total_assertions=0`; the separate `data/raw/gold_path_biosamples.tsv` count of 27 is not the organism count consumed by GOLD `SourceAttestation`. | Supported. |
| The minted record should not exact-ground to `marine anoxic zone`. | `data/raw/ontology_terms.tsv` defines `ENVO:01000066` as `marine anoxic zone`; the source path is explicitly under `Environmental > Aquatic > Freshwater > Lake`. | Supported. |
| The minted record should not exact-ground to `anoxic water`. | GOLD assigns `ENVO:01000173` `anoxic water` to this path's MIxS `medium` role, while the source leaf is a zone within the freshwater-lake path. | Supported. |
| The current class-level `CONFIRM_UNGROUNDED` row was applied. | `curation/decisions.tsv` has a `CLASS` row keyed to `habitatmech:GOLD.b75e4aa08f`, and the generated record has `grounding_status: UNGROUNDED` plus a matching class-level curation-history event. | Supported. |
| `ENVO:00000021` is a valid strict parent for this source concept. | The record inherits `ENVO:00000021` from the GOLD parent path `Environmental > Aquatic > Freshwater > Lake`. `data/raw/ontology_terms.tsv` labels `ENVO:00000021` as `freshwater lake`, which is the containing lake rather than a class that an anoxic zone instantiates. | Not supported. |
| Existing target-specific causal evidence belongs on this record. | Exact hidden/ignored-inclusive searches of `curation/`, `history/`, and prior review reports found no target-specific overlay, term request, prior review, or item-level decision for the source concept before this report was created. | Not supported by current maintained inputs. |

## Completeness

The record preserves the current GOLD source identity: the source path, leaf label, node ID, aquatic category, class-level ungrounding decision, and generated seed history are present.

The record is incomplete at the item level. The class-level decision explicitly says whether the `Anoxic zone` source concept is a habitat was not assessed, and no row in `curation/term_requests.tsv` defines the minted freshwater-lake anoxic-zone concept or repairs its inherited `freshwater lake` parent.

The empty `environmental_parameters`, `characteristic_taxa`, `evidence`, `causal_graphs`, `discussions`, and `datasets` lists are appropriate for the current maintained inputs. The GOLD MIxS triad rows are contextual candidates for curation, but they are not causal-edge evidence and are not generated directly into those optional fields.

## Findings

### Major

- `data/habitats/aquatic/anoxic_zone.yaml` currently asserts `ENVO:00000021` `freshwater lake` as a `parent_habitats` value even though `Anoxic zone` denotes a zone within a freshwater lake rather than a freshwater lake itself. `parent_habitats` is an is-a claim per `docs/CURATION.md`, and this edge is a generated GOLD parent-path link that needs item-level replacement in `curation/term_requests.tsv`.

## Recommended Edits

- Add item-level curation for `habitatmech:GOLD.b75e4aa08f`: replace the existing `CLASS` row in `curation/decisions.tsv` with an `ITEM` `CONFIRM_UNGROUNDED` review, then add a `curation/term_requests.tsv` row that defines the freshwater-lake anoxic-zone habitat and uses `parent_mode: REPLACE` to remove the inherited `ENVO:00000021` parent after identifying the tightest supported vendored genus.
- Regenerate with `just seed`, inspect `just seed-canary habitatmech:GOLD.b75e4aa08f`, and apply with `just seed-apply --force` only after the canary removes the false parent while retaining the GOLD source attestation.

## Follow-up Checks

- `just validate data/habitats/aquatic/anoxic_zone.yaml`
- `just validate-strict data/habitats/aquatic/anoxic_zone.yaml --quiet`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- Manual check: confirm `data/habitats/aquatic/anoxic_zone.yaml` no longer lists `ENVO:00000021` as a parent after the `REPLACE` term request.

## Additional Notes

`data/raw/gold_path_triads.tsv` maps the same GOLD path to `ENVO:01000252` `freshwater lake biome` as the MIxS broad scale, `ENVO:00000021` `freshwater lake` as the local scale, and `ENVO:01000173` `anoxic water` as the medium. Those rows are useful cues for a future definition, but none of the three role fillers is an exact identity for the zone itself.

`curation/causal_graphs/sludge.yaml` and `curation/causal_graphs/waste_water.yaml` mention anoxic zones in mechanisms for their own target records. Those mentions are not a target-specific overlay for `habitatmech:GOLD.b75e4aa08f`.
