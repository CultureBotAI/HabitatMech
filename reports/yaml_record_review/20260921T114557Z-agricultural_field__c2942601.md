# YAML Record Review: Agricultural field

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/agricultural_field__c2942601.yaml`
- Started UTC: 2026-09-21T11:38:00Z
- Finished UTC: 2026-09-21T11:45:57Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Path | `data/habitats/aquatic/agricultural_field__c2942601.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.a16a79bd0c` |
| Label | `Agricultural field` |
| Category | `AQUATIC` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained or generated | Generated from `data/raw/` plus curation inputs |

The record contains one GOLD source attestation:

| Source | Source ID | Source label | Source path | Assertion count |
|---|---|---|---|---|
| `GOLD` | `gold.ecosystem:5741` | `Agricultural field` | `Environmental > Aquatic > Freshwater > Agricultural field` | `8` `ORGANISM` assertions |

Current hierarchy:

| Parent | Label in vendored ontology | Basis |
|---|---|---|
| `ENVO:00000114` | `agricultural field` | Ambiguous GOLD leaf retained as a broader parent |
| `ENVO:00002011` | `fresh water` | GOLD parent path `Environmental > Aquatic > Freshwater` grounded to ENVO fresh water |

The record has no definition, synonyms, xrefs, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussions, or datasets.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/agricultural_field__c2942601.yaml` | Passed; LinkML reported no issues. |
| `just validate-strict data/habitats/aquatic/agricultural_field__c2942601.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just term-requests-check` | Passed; the generated term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records were present, with 0 missing, 0 extra, and 0 differing files. |

## Identity and Grounding

The generated source identity and provenance are reproducible:

| Claim | Review |
|---|---|
| GOLD source path | Supported. `data/raw/gold_ecosystem_paths.tsv` has the exact canonical path `Environmental > Aquatic > Freshwater > Agricultural field`. |
| Source ID | Supported. The raw GOLD ecosystem row records two collapsed node IDs, `gold.ecosystem:5741|gold.ecosystem:5742`; the generated attestation uses the first and adds the standard multi-node note. |
| Source label | Supported. The raw GOLD path row sets `leaf_label` to `Agricultural field`, matching the generated `label` and `source_label`. |
| Assertion count | Supported. The exact raw GOLD ecosystem row has `organism_count=8`, `study_count=0`, `biosample_count=0`, and `total_assertions=8`; the generated record stores `assertion_count: 8` with `assertion_unit: ORGANISM`. |
| Slug | Supported. `data/habitats/PATHS.tsv` pins `habitatmech:GOLD.a16a79bd0c` to `agricultural_field__c2942601`. |
| Category | Supported by the first two GOLD path levels, `Environmental > Aquatic`. |
| `mapping_status: SEEDED` | Supported. There is no item-level `curation/decisions.tsv` row for `habitatmech:GOLD.a16a79bd0c`, so the seeder's lexical and path-derived mapping has not been curated. |

The record's `NARROW` status is defensible: the GOLD leaf label matches `ENVO:00000114` `agricultural field`, but `Environmental > Terrestrial > Agricultural field` is the shallow GOLD claimant for that ontology identity, and `data/habitats/terrestrial/agricultural_field.yaml` already owns the exact ENVO record with the terrestrial GOLD attestation and PREGO's `ENVO:00000114` attestation.

## Evidence

Supported:

| Claim | Evidence |
|---|---|
| The source path is a distinct aquatic GOLD concept, not a second exact attestation on the terrestrial agricultural-field record. | `data/raw/gold_ecosystem_paths.tsv` has separate rows for `Environmental > Terrestrial > Agricultural field` and `Environmental > Aquatic > Freshwater > Agricultural field`. The aquatic row has its own minted slug in `data/habitats/PATHS.tsv`. |
| The concept is attested by GOLD organism assertions. | The exact `gold_ecosystem_paths.tsv` row carries `organism_count=8` and `total_assertions=8`. |
| One GOLD biosample independently used the same aquatic source path. | `data/raw/gold_path_biosamples.tsv` has the exact path with ecosystem path ID `5742` and biosample count `1`. |
| GOLD study `Gs0121636` used this path. | `data/raw/gold_studies.tsv` lists the exact path alongside `Engineered > Lab culture > Culture media > Bacteria > Contaminated`, `Environmental > Aquatic > Marine > Coastal`, and `Environmental > Aquatic > Thermal springs > Hot (42-90C)`. |
| The sole current MIxS triad for this path treated `agricultural field` as the local scale, not the sampled medium. | `data/raw/gold_path_triads.tsv` has one exact-path `local` row with `ENVO:00000114` `agricultural field`; the same source path has `broad` = `ENVO:00000873` `freshwater biome` and `medium` = `ENVO:00002011` `fresh water`. |

Unsupported or over-scoped:

| Claim | Issue |
|---|---|
| `ENVO:00000114` `agricultural field` is a broader habitat for this record. | The source is filed under GOLD's aquatic freshwater branch and the matching MIxS row uses agricultural field as `env_local_scale`; a local feature can contextualize the sample without becoming a material or system the aquatic sample is a subclass of. The fresh-water `env_medium` row is the closer source hint for what was sampled. |

No literature citations, snippets, record-level evidence, causal edges, environmental parameters, characteristic taxa, discussions, or datasets are attached to this record.

## Completeness

Consequential gaps:

- There is no item-level `curation/decisions.tsv` row for `habitatmech:GOLD.a16a79bd0c`.
- There is no `curation/term_requests.tsv` definition that names the minted aquatic concept and puts it under the true material parent, `ENVO:00002011` `fresh water`.
- There is no curated xref preserving the relevant local-scale field context without asserting that this aquatic concept is a kind of agricultural field.
- There is no causal graph overlay or record-level evidence; this is acceptable for a seeded, uncurated GOLD-only record.
- There are no seeded environmental parameters, characteristic taxa, discussions, or datasets; the searched maintained inputs provide no claim to emit for the minted aquatic record.

Bounded absence checks:

| Query | Scope | Result |
|---|---|---|
| `habitatmech:GOLD.a16a79bd0c`, `gold.ecosystem:5741`, `gold.ecosystem:5742`, and the exact GOLD source path | Hidden/ignored-inclusive search over `data/raw`, `data/habitats`, `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/causal_graphs`, `research/habitats`, and `reports/yaml_record_review`, excluding `data/text_map/**` | Found only the generated target, the slug row, and the raw GOLD ecosystem, study, biosample, and MIxS triad rows listed above. |
| `agricultural_field__c2942601` | Hidden/ignored-inclusive search over `data/habitats/PATHS.tsv`, `data/raw`, `curation`, `research/habitats`, and `reports/yaml_record_review` | Found only the slug row before this report was created. |
| `Agricultural field` / `agricultural field` | Hidden/ignored-inclusive search over raw ontology terms, curation tables, research reports, and generated path pins | Found the exact ENVO term, the terrestrial exact record, this aquatic GOLD source path, MIxS local-scale rows that use `ENVO:00000114`, grassland decisions rejecting `ENVO:00000114`, and incidental research prose; found no item-level row for `habitatmech:GOLD.a16a79bd0c`. |

## Findings

### Major

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| M1 | `ENVO:00000114` is stored as a `parent_habitats` entry, but the source inventory supports agricultural field as local-scale context for a fresh-water sample, not as a broader habitat for the sampled aquatic material. | `docs/CURATION.md` and `CLAUDE.md` require `parent_habitats` to be strictly broader is-a links. The raw GOLD path places the source under `Environmental > Aquatic > Freshwater`, and `data/raw/gold_path_triads.tsv` splits the matching biosample into `broad` = `freshwater biome`, `local` = `agricultural field`, and `medium` = `fresh water`. That is enough to keep `ENVO:00000114` as context or an xref, but not enough to say this aquatic concept is a kind of land field. | `curation/decisions.tsv` for item-level review plus `curation/term_requests.tsv` for a minted definition under `ENVO:00002011` with `parent_mode=REPLACE`. |

### Minor

None found.

### Blockers

None found.

## Recommended Edits

1. Add an item-level curation decision for `habitatmech:GOLD.a16a79bd0c` in `curation/decisions.tsv` after reviewing the exact GOLD path and triad rows. Keep the identity minted, and retain `ENVO:00000114` only as an xref or closely related context rather than as a `GROUND_AS_PARENT` parent.
2. Add a `curation/term_requests.tsv` row for a minted fresh-water agricultural-field class under `ENVO:00002011` `fresh water` with `parent_mode` set to `REPLACE`. The replacement is needed because the inherited `ENVO:00000114` agricultural-field parent is contextual rather than strictly broader.
3. Run `just term-requests` to regenerate `curation/term_requests/envo_robot_template.tsv`.
4. Regenerate only through the seeder: run `just seed`, canary `just seed-canary habitatmech:GOLD.a16a79bd0c --force`, inspect `data/habitats/aquatic/agricultural_field__c2942601.yaml`, then run `just seed-apply --force`.
5. Scaffold an append-only history record with `just new-history`, filling in the reviewed GOLD path, MIxS triad rows, near matches, and validation commands.

## Follow-up Checks

- `just validate data/habitats/aquatic/agricultural_field__c2942601.yaml`
- `just validate-strict data/habitats/aquatic/agricultural_field__c2942601.yaml --quiet`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just qc`
- Manual post-regeneration check: confirm the record keeps a minted identity, keeps `grounding_status: UNGROUNDED`, gains a `HabitatMech` definition, retains `ENVO:00002011` as its sole `parent_habitats` entry, and no longer treats `ENVO:00000114` as an is-a parent.

## Additional Notes

- All absence searches described above included hidden and ignored files.
- The existing `data/habitats/terrestrial/agricultural_field.yaml` record is not a duplicate of this target. It is the exact `ENVO:00000114` record for terrestrial agricultural fields and carries the shallow `Environmental > Terrestrial > Agricultural field` GOLD path plus PREGO's agricultural-field assertions.
- The generated aquatic record correctly avoids inheriting PREGO taxa and environmental parameters from the broader `ENVO:00000114` and `ENVO:00002011` records. Its current issue is hierarchy, not cross-record taxon leakage.
