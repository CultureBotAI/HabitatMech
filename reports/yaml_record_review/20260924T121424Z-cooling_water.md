# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/cooling_water.yaml
- Started UTC: 2026-09-24T12:14:24Z
- Finished UTC: 2026-09-24T12:14:24Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Path | `data/habitats/engineered/cooling_water.yaml` |
| Class | `HabitatRecord` |
| Identifier | `ENVO:03600002` |
| Label | `cooling water` |
| Category | `ENGINEERED` |
| Generated or maintained | Generated from committed source inventories, the vendored ontology slice, and `data/habitats/PATHS.tsv`; `data/habitats/` remains read-only |
| Locked slug | `data/habitats/PATHS.tsv:924` maps `ENVO:03600002` to `cooling_water` |
| Grounding | `EXACT` |
| Mapping status | `SEEDED` |

## Validation

| Check | Result |
| --- | --- |
| `find reports/yaml_record_review -maxdepth 1 -type f -name '*-cooling_water.md' -print` | Passed; no prior exact `cooling_water` review was present before this report was written. `find` included ignored files under `reports/yaml_record_review`. |
| `just validate data/habitats/engineered/cooling_water.yaml` | Passed; no LinkML issues found. |
| `just validate-strict data/habitats/engineered/cooling_water.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Reference validator | Not applicable; the target record has no record-level `evidence`, no causal graph, and no DOI, PMID, or URL evidence items to validate. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 records were expected, 3206 were present, and there were 0 missing, extra, or differing records. |
| `just worklist --limit 2000` | Passed; 0 ungrounded records are still undecided and 1810 decisions are on file. |
| `just report` | Passed; completed the corpus report for 3206 records. |
| `git diff --check` | Passed before report creation. |

## Identity and Grounding

The identifier, label, and definition are copied directly from the vendored ENVO row at `data/raw/ontology_terms.tsv:10051`: `ENVO:03600002`, ontology `ENVO`, label `cooling water`, non-deprecated, and definition `Liquid water which is a participant in a water cooling process.`

The ENVO-derived parent is correct on its own: `data/raw/ontology_subclass_edges.tsv:8446` asserts `ENVO:03600002 rdfs:subClassOf ENVO:00002006`, and `data/raw/ontology_terms.tsv:7169` identifies `ENVO:00002006` as `liquid water`.

The source attestation is also traceable. `data/raw/gold_ecosystem_paths.tsv:1273` has leaf label `Cooling water`, canonical path `Engineered > Built environment > Nuclear test reactor > Cooling water`, `gold_node_count` 2, zero organism, study, and biosample assertions, and node ids `gold.ecosystem:5709|gold.ecosystem:5710`. The generated attestation reports `gold.ecosystem:5709` as the first node id and explains that two GOLD nodes share the path.

The generated `skos:exactMatch` overstates the GOLD source path. GOLD did not assert a top-level cooling-water source; it asserted a `Cooling water` child under `Engineered > Built environment > Nuclear test reactor`. The inherited source-path parent is `Engineered > Built environment > Nuclear test reactor`, which `data/raw/gold_ecosystem_paths.tsv:1272` maps to `ENVO:02000136` `research nuclear reactor` in `data/habitats/engineered/research_nuclear_reactor.yaml`. In the current record, `ENVO:02000136` is emitted as a `parent_habitats` edge even though cooling water is not a kind of nuclear reactor. That is the sign the leaf denotes a material in, or associated with, a nuclear test reactor rather than the generic ENVO class for every water-cooling process.

`mapping_status: SEEDED` is appropriate. The exact source key for this GOLD path is `habitatmech:GOLD.01305045f6`, and an ignored/hidden-inclusive exact search over `curation`, `history`, `research`, `reports`, `data/habitats/PATHS.tsv`, and `data/habitats` found no maintained curation decision, term request, causal overlay, history record, research report, prior YAML review, or generated target that mentions that source key. This review is read-only and does not promote the record to `REVIEWED`.

## Evidence

| Claim | Evidence | Status |
| --- | --- | --- |
| The HabitatMech record denotes the ENVO class `ENVO:03600002` `cooling water`. | `data/raw/ontology_terms.tsv:10051` and `data/habitats/engineered/cooling_water.yaml:1` | Supported exactly. |
| ENVO `cooling water` is narrower than `liquid water`. | `data/raw/ontology_subclass_edges.tsv:8446` asserts `ENVO:03600002 rdfs:subClassOf ENVO:00002006`. | Supported exactly. |
| The sole GOLD source concept is `Engineered > Built environment > Nuclear test reactor > Cooling water`. | `data/raw/gold_ecosystem_paths.tsv:1273` lists that canonical path with leaf label `Cooling water`. | Supported exactly. |
| The GOLD source concept has two GOLD ecosystem node ids and `gold.ecosystem:5709` is the first shown. | `data/raw/gold_ecosystem_paths.tsv:1273` lists `gold.ecosystem:5709|gold.ecosystem:5710`. | Supported exactly. |
| No GOLD assertion count is emitted for this source attestation. | The raw row reports `organism_count`, `study_count`, `biosample_count`, and `total_assertions` as 0. | Supported exactly. |
| The GOLD source concept exactly matches every ENVO `cooling water`. | The only GOLD row is path-qualified under `Nuclear test reactor`, while the ENVO definition covers liquid water participating in any water-cooling process. | Unsupported; at most, the path supports a narrower nuclear-test-reactor cooling-water concept with `ENVO:03600002` as a broader material parent. |
| `ENVO:02000136` `research nuclear reactor` is a broader habitat of this record. | The parent is inherited from the immediate GOLD parent path `Engineered > Built environment > Nuclear test reactor`; the parent record at `data/habitats/engineered/research_nuclear_reactor.yaml` confirms that path maps to `ENVO:02000136`. | Unsupported; the reactor is the context for the material, not an is-a superclass of cooling water. |

## Completeness

The record is intentionally sparse for the inspected inputs. It has no synonyms, xrefs, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussions, datasets, or quality flags. Its only raw source concept is GOLD's zero-assertion aggregate path, and the only ontology claim comes from the vendored ENVO term and subclass row.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports/yaml_record_review`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated target. They found the expected ENVO term, ENVO subclass edge, locked slug, GOLD aggregate path, and related cooling-tower discussion in `research/habitats/other/cooling-tower-habitatmech-bacdive-71d5f01e03-deep-research-claude_code.md`; they found no maintained row, term request, causal overlay, history record, prior YAML review, GOLD biosample row, GOLD triad row, or GOLD study row specific to `habitatmech:GOLD.01305045f6`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| major | The GOLD source is over-grounded as an exact match to generic ENVO `cooling water`. | `data/raw/gold_ecosystem_paths.tsv:1273` qualifies the `Cooling water` leaf under `Engineered > Built environment > Nuclear test reactor`, but `data/habitats/engineered/cooling_water.yaml:1-17` publishes the generic ENVO identifier with `skos:exactMatch`. | Add an item-level `curation/decisions.tsv` row for `habitatmech:GOLD.01305045f6`; if the concept remains distinct, add a `curation/term_requests.tsv` definition keyed to the minted source concept and regenerate. |
| major | The generated `ENVO:02000136` parent is not an is-a parent of a cooling-water material. | `data/raw/gold_ecosystem_paths.tsv:1272` is the immediate GOLD parent path and `data/habitats/engineered/research_nuclear_reactor.yaml:1-29` shows that path resolves to `research nuclear reactor`; `src/habitatmech/seed.py` adds immediate GOLD parent paths to `parent_habitats`, whose schema meaning is broader habitats. | Use the same source-specific curation to keep the concept minted and use `curation/term_requests.tsv` with `parent_mode=REPLACE`, or adjust the GOLD parent-path rule if this is a broader class of material-in-container paths rather than a one-record exception. |

## Recommended Edits

Add an item-level `curation/decisions.tsv` row keyed to `habitatmech:GOLD.01305045f6` so the seeder stops publishing `Engineered > Built environment > Nuclear test reactor > Cooling water` as an automatic exact match to all `ENVO:03600002` cooling water.

If item review confirms the source concept denotes nuclear-test-reactor cooling water rather than generic cooling water, keep the source concept minted, retain `ENVO:03600002` as its broader material class, and add a `curation/term_requests.tsv` definition with `parent_mode=REPLACE` so the generated record has `cooling water` as its genus and does not inherit `research nuclear reactor` as an is-a parent.

After that input-level edit, rerun `just seed-canary habitatmech:GOLD.01305045f6` if the source stays minted or `just seed-canary ENVO:03600002` if curation deliberately keeps the ENVO identity, inspect the regenerated YAML, then run `just seed-apply --force` without pruning.

## Follow-up Checks

Rerun `just validate data/habitats/engineered/cooling_water.yaml` if the ENVO record survives, or the regenerated minted YAML path if curation splits the source concept away from `ENVO:03600002`.

Rerun `just validate-strict <record>`, `just verify-corpus --max-diffs 1`, `just term-requests-check`, `just validate-history`, `just worklist --limit 2000`, and `just report` to prove the curation decision, optional term request, generated record, and corpus outputs agree.

Manually inspect the regenerated `source_attestations` and `parent_habitats` to confirm the GOLD path remains traceable, `ENVO:03600002` is no longer asserted as an exact identity unless the item review explicitly justifies it, and `ENVO:02000136` is absent from `parent_habitats` unless a true is-a relationship is added to a maintained input.

## Additional Notes

The related cooling-tower research report discusses `ENVO:03600002` only as a near miss for BacDive's cooling-tower source, and an ignored/hidden-inclusive exact search found `cooling tower water` only in that report. It does not curate this zero-assertion GOLD nuclear-test-reactor cooling-water path and should be treated as a lead for any future material-specific definition rather than as evidence that the current record is correct.
