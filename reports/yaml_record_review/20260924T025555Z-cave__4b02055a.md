# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/terrestrial/cave__4b02055a.yaml
- Started UTC: 2026-09-24T02:48:10Z
- Finished UTC: 2026-09-24T02:55:55Z
- Verdict: pass with minor issues

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/terrestrial/cave__4b02055a.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.46abb9a153` |
| Label | `Cave` |
| Category | `TERRESTRIAL` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv`, GOLD path-derived parentage, `curation/decisions.tsv`, and `data/habitats/PATHS.tsv`; do not edit `data/habitats/terrestrial/cave__4b02055a.yaml` directly. |

`data/habitats/terrestrial/cave__4b02055a.yaml` is the generated GOLD-only
record for `Environmental > Terrestrial > Deep subsurface > Cave`. Its minted
identifier recomputes to `habitatmech:GOLD.46abb9a153`, and
`data/habitats/PATHS.tsv` pins that identifier to slug `cave__4b02055a`.

An ignored-file-inclusive pre-report check,
`find reports/yaml_record_review -maxdepth 1 -type f -name '*-cave__4b02055a.md' -print`,
found no prior exact report. A second ignored-file-inclusive
`find data/habitats -type f -name 'cave__4b02055a.yaml' -print` found exactly
this generated YAML target.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/terrestrial/cave__4b02055a.yaml` | Pass - LinkML reported `No issues found`. |
| `just validate-strict data/habitats/terrestrial/cave__4b02055a.yaml` | Pass - 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Target causal overlay validator | Not applicable - ignored-file-inclusive `find` found no `curation/causal_graphs/*cave*` overlay, and exact ignored/hidden-inclusive `rg` found no causal overlay keyed to `habitatmech:GOLD.46abb9a153` or the `Environmental > Terrestrial > Deep subsurface > Cave` path. |
| Reference validator | Not applicable - the record has no `evidence` entries or causal graph edges with reference identifiers. |
| `just validate-causal-all` | Pass - validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Pass - committed ENVO batch term-request table is current with 109 terms. |
| `just validate-history` | Pass - 77 history records valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass - expected 3206 generated records, found 3206 on disk, with 0 missing, 0 extra, and 0 differing. |
| `just worklist --limit 2000` | Pass - 0 ungrounded records still undecided; 1810 decisions on file. |
| `just report` | Pass - report completed. |
| `git diff --check` | Pass - no whitespace errors before this report was written. |

## Identity and Grounding

The generated source identity is stable. Recomputing the GOLD mint for
`Environmental > Terrestrial > Deep subsurface > Cave` yields
`habitatmech:GOLD.46abb9a153`, and `data/raw/gold_ecosystem_paths.tsv` has that
canonical path at depth 4 with leaf `Cave`, two collapsed GOLD ecosystem node
ids, and zero older KG-Microbe `ORGANISM` assertions.

The `ENVO:00000067` parent is mechanically justified. The vendored ontology
row for `ENVO:00000067` labels it `cave` and defines it as a naturally formed,
subterranean open area or chamber. GOLD also has a shallower depth-3
`Environmental > Terrestrial > Cave` path whose minted source id is
`habitatmech:GOLD.db94b84352`; per the seeder's shallowest-leaf rule, that
generic path claims `ENVO:00000067` exactly while this depth-4
Deep-subsurface-qualified path is minted as `NARROW` with `ENVO:00000067` as a
broader parent.

The source-path parent is supported by GOLD hierarchy. The direct parent path
`Environmental > Terrestrial > Deep subsurface` recomputes to
`habitatmech:GOLD.151ba43519`, has its own GOLD inventory row with 60 older
`ORGANISM` assertions, and is the immediate prefix of this target's GOLD path.
That supports the generated `habitatmech:GOLD.151ba43519` parent as a
source-derived broader grouping.

The newer GOLD API-derived context is compatible with the generated identity
but does not by itself settle the final curation decision. `data/raw/gold_path_biosamples.tsv`
has two biosamples for node `gold.ecosystem:7086` on the exact target path, and
`data/raw/gold_path_triads.tsv` has one complete target-path MIxS triad with
`local` = `ENVO:00000067` `cave` and `medium` = `ENVO:01000255` `mineral
deposit`. Those rows corroborate a cave sampling context inside GOLD's deep
subsurface branch, but a curator still needs to decide whether GOLD intended a
distinct deep-subsurface cave concept or a duplicate placement of generic
`cave`.

The generated `mapping_status: SEEDED` is correct. Exact
ignored/hidden-inclusive searches found no `curation/decisions.tsv` row for
`habitatmech:GOLD.46abb9a153`; the only curation history event is the generated
`seed_from_sources` event from the raw inventories. The current row is therefore
not yet item-reviewed.

## Evidence

| Claim | Source | Assessment |
|---|---|---|
| The record represents GOLD `Environmental > Terrestrial > Deep subsurface > Cave`. | `data/raw/gold_ecosystem_paths.tsv`; generated `source_attestations` | Supported exactly. |
| The generated identifier is the correct mint for the exact GOLD path. | `habitatmech.seed.mint("GOLD", "Environmental > Terrestrial > Deep subsurface > Cave")`; generated `identifier` | Supported exactly. |
| `ENVO:00000067` `cave` is a strict broader parent for the target path. | GOLD source path; `data/raw/ontology_terms.tsv`; seeder shallowest-leaf rule | Supported as an automatically generated `skos:narrowMatch`; the target has not been item-reviewed. |
| `habitatmech:GOLD.151ba43519` `Deep subsurface` is the GOLD source-path parent. | `data/raw/gold_ecosystem_paths.tsv`; minted GOLD parent-path id | Supported exactly as source hierarchy. |
| The target path had zero older KG-Microbe organism assertions. | `data/raw/gold_ecosystem_paths.tsv`; generated omission of `assertion_count` | Supported exactly; the seeder omits `assertion_count` when the inventory count is zero. |
| Newer GOLD API tables include two biosamples and a cave/mineral-deposit triad for the target path. | `data/raw/gold_path_biosamples.tsv`; `data/raw/gold_path_triads.tsv` | Supported exactly; these rows are context for future item-level review, not currently reflected as organism assertions. |
| `habitatmech:GOLD.46abb9a153` has been reviewed. | `curation/decisions.tsv` | Not supported. Exact ignored/hidden-inclusive search found no target row. |

Exact ignored/hidden-inclusive searches found no target-specific maintained
causal overlay, no exact review report under
`reports/yaml_record_review/*-cave__4b02055a.md`, no curation decision or term
request for `habitatmech:GOLD.46abb9a153`, and no `research` or `history`
filename containing `46abb9a153`.

## Completeness

| Area | Assessment |
|---|---|
| Definition | Complete as empty for a raw seeded record; incomplete if item-level review keeps the source as a distinct minted habitat. |
| Synonyms | Complete as empty. The raw GOLD row contributes only leaf label `Cave`, which is the primary label. |
| Parents | Complete enough for the seeded state. `ENVO:00000067` follows the ambiguous-leaf rule and `habitatmech:GOLD.151ba43519` follows the GOLD source path. |
| Source attestations | Complete. The source id, label, path, `skos:narrowMatch`, and two-node note agree with `data/raw/gold_ecosystem_paths.tsv`; zero organism assertions are correctly omitted. |
| Environmental parameters | Complete as empty. Exact ignored/hidden-inclusive searches found no maintained row for `habitatmech:GOLD.46abb9a153` or the exact target source path. |
| Characteristic taxa | Complete as empty. No PREGO, BacDive, or Madin source attests this exact record. |
| Evidence | Complete as empty for now. There are no authored claims or causal edges needing claim-level references. |
| Causal graphs | Complete as empty. No target overlay exists. |
| Status and audit | Minor cleanup needed. The source concept has no item-level curation row, so the record correctly remains `SEEDED`. |

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Blocker | None found. | The YAML validates and the generated identifier, source attestation, parents, and `SEEDED` status all follow from maintained inputs. | Not applicable |
| Major | None found. | The target is not grounded to a wrong ontology identity, and no false direct parent was found. | Not applicable |
| Minor | **The GOLD source concept still needs item-level disposition.** The current `NARROW` record is a plausible generated representation for a GOLD path one level below generic `Cave`, but `habitatmech:GOLD.46abb9a153` has no `curation/decisions.tsv` row. A curator should decide whether to endorse the distinct deep-subsurface-cave record, ground the path directly to `ENVO:00000067` `cave` so it merges with `data/habitats/terrestrial/cave.yaml`, or keep it minted with an authored definition. | Exact ignored/hidden-inclusive searches found no decision row or term request for `habitatmech:GOLD.46abb9a153`; `data/raw/gold_path_triads.tsv` adds one cave/mineral-deposit triad but does not disambiguate duplicate `cave` placement versus a novel narrower class. | `curation/decisions.tsv`; possibly `curation/term_requests.tsv` |

## Recommended Edits

1. Add an item-level `curation/decisions.tsv` row for
   `habitatmech:GOLD.46abb9a153` after deciding the source path's extension:
   use `REVIEW` if the current minted `NARROW` placement is intentionally kept,
   or `GROUND` to `ENVO:00000067` if
   `Environmental > Terrestrial > Deep subsurface > Cave` should be merged into
   the generic exact cave record.

2. If the path remains a distinct minted habitat, add a maintained
   `curation/term_requests.tsv` definition that labels the record explicitly,
   for example as a deep-subsurface cave, and records why it is narrower than
   generic `ENVO:00000067` `cave` instead of a duplicate GOLD placement.

## Follow-up Checks

After item-level disposition, run:

- `just seed`
- `just seed-canary habitatmech:GOLD.46abb9a153` if the record remains minted
- `just seed-canary ENVO:00000067` if the GOLD source is grounded to generic
  `cave`
- `just seed-apply --force`
- `just validate data/habitats/terrestrial/cave__4b02055a.yaml` if the record
  remains minted
- `just validate data/habitats/terrestrial/cave.yaml` if the source merges into
  generic `cave`
- `just validate-strict data/habitats/terrestrial/cave__4b02055a.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

If item-level curation merges this source into `ENVO:00000067`, also rerun an
ignored/hidden-inclusive search for `habitatmech:GOLD.46abb9a153` and
`cave__4b02055a` to confirm the old generated YAML disappeared, an appropriate
redirect was generated after commit, and no stale source-specific page remains.

## Additional Notes

- The source path's newer GOLD study context is `Gs0149395`, which spans
  `Environmental > Aquatic > Deep subsurface > Groundwater > Cave stream sediment`,
  `Environmental > Terrestrial > Deep subsurface > Cave`, and
  `Environmental > Terrestrial > Deep subsurface > Cave > Wall biofilm`.
- Child paths under this target already show more specific cave-surface
  contexts in raw GOLD: `Wall biofilm`, `Floor sediment`, `Speleothems`, `Lava
  tube`, `Spicules`, and `Wall surface`.
- The committed cave-spicule research mentions
  `Environmental > Terrestrial > Deep subsurface > Cave > Spicules` and reads
  that child as a cave-mineral speleothem morphotype. It is child-specific
  evidence and does not item-review this target path.
