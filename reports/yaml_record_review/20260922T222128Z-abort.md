# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/other/abort.yaml`
- Started UTC: 2026-09-22T22:12:00Z
- Finished UTC: 2026-09-22T22:21:28Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/other/abort.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:BACDIVE.b61f30c070` |
| Label | `Abort` |
| Category | `OTHER` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Record status | Generated from one BacDive isolation-source concept with an empty upstream mapping row |

This is the generated record for BacDive isolation source `bacdive.isolation_source:abort`. HabitatMech mints `habitatmech:BACDIVE.b61f30c070` for this source because the imported kg-microbe BacDive mapping table has an explicit `Abort` row with no ontology target; the seeder honors that row and treats it as intentionally ungrounded rather than re-grounding by lexical fallback.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/other/abort.yaml` | Passed; LinkML validation reported no issues. |
| `just validate-strict data/habitats/other/abort.yaml` | Passed; one file scanned with 0 error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 generated records, found 3206, with 0 missing, 0 extra, and 0 differing. |
| `just report` | Passed; the report regenerated and left the reviewed concept out of the risky-grounding, stale-sweep, and current non-habitat inventory sections. |
| `git diff --check` | Passed; no whitespace errors. |

## Identity and Grounding

The BacDive source identity is mechanically supported:

| Raw field | Value |
|---|---|
| `source_id` | `bacdive.isolation_source:abort` |
| `label` | `Abort` |
| `normalized_label` | `abort` |
| `strain_count` | `39` |
| `distinct_taxa` | `24` |

The generated `source_attestations` block matches `data/raw/bacdive_isolation_sources.tsv:99`: it keeps the BacDive source id and `Abort` source label exactly, records `assertion_count: 39` with `assertion_unit: STRAIN`, and omits any source path because BacDive isolation sources are flat labels rather than path-bearing source concepts.

The generated `characteristic_taxa` block matches the 24 raw rows in `data/raw/bacdive_source_taxa.tsv:2-25`. Each generated row preserves the raw taxon id, label, rank, and association count, and every row has `candidate_pool: 24`, matching the raw source's distinct-taxon count. These are observed BacDive source/taxon associations only; no row sets `is_characteristic`, so the record does not make a stronger curated characteristic-taxon claim.

The ungrounded minted identity is expected for the current inputs. `data/raw/isolation_source_groundings.tsv:3` records `Abort` with `source_dataset=bacdive` and no object id, object label, or predicate, and `src/habitatmech/seed.py` routes that explicit empty row through `bacdive_declined_upstream` instead of using lexical ontology matching. `tests/test_seed_harmonization.py` has a dedicated regression test that asserts the same outcome for `bacdive.isolation_source:abort`.

The curation status is weaker than the source needs. `curation/decisions.tsv` carries only a `CLASS`-depth `CONFIRM_UNGROUNDED` row for `habitatmech:BACDIVE.b61f30c070`, and that row explicitly says no term in the vendored slice matched the label but "whether the concept is a habitat at all was NOT assessed".

## Evidence

| Claim | Nearest support | Review |
|---|---|---|
| The generated record represents BacDive isolation source `bacdive.isolation_source:abort`. | `data/raw/bacdive_isolation_sources.tsv:99` | Supported exactly. |
| BacDive contributed 39 strain assertions and 24 distinct taxa for this source. | `data/raw/bacdive_isolation_sources.tsv:99`; `data/raw/bacdive_source_taxa.tsv:2-25` | Supported exactly. |
| The 24 generated taxon associations preserve the raw BacDive ranks, taxon ids, labels, and association counts. | `data/raw/bacdive_source_taxa.tsv:2-25` | Supported exactly. |
| The raw kg-microbe BacDive mapping intentionally leaves `Abort` without an ontology target. | `data/raw/isolation_source_groundings.tsv:3`; `src/habitatmech/seed.py`; `tests/test_seed_harmonization.py` | Supported exactly. The imported empty row prevents lexical remapping. |
| The class-level sweep did not assess whether `Abort` is a habitat. | `curation/decisions.tsv`; `data/habitats/other/abort.yaml` | Supported exactly. The generated curation-history text repeats the decision note verbatim. |

Unsupported or over-scoped claims: one found. The generated record still presents `Abort` as an ungrounded `HabitatRecord`, but `Abort` names a source artifact, clinical event, or pregnancy outcome rather than a microbial habitat, material, or host-associated site.

## Completeness

The generated BacDive provenance is complete for the current raw tables: the record carries the isolation-source id, source label, 39-strain assertion count, and all 24 ranked source/taxon associations.

The maintained curation decision is not complete. `Abort` should not remain in the class-level ungrounded bucket because that bucket only proves that lexical lookup found no target; it explicitly defers the separate habitatness decision. This source concept now has enough local evidence for an item-level `NOT_APPLICABLE` decision.

Ignored/hidden-inclusive exact searches for `habitatmech:BACDIVE.b61f30c070` and `bacdive.isolation_source:abort` found no maintained curation inputs beyond the existing `curation/decisions.tsv` row and no generated record besides `data/habitats/other/abort.yaml`; generated `data/text_map`, `pages`, and `build` outputs were excluded from broad exact content searches.

No maintained causal overlay, history file, raw research report, or prior exact YAML review report was found by filename for `abort` under `curation/causal_graphs`, `history`, `research/habitats`, or `reports/yaml_record_review`; the `find` search included ignored files beneath those checked directories.

## Findings

One major finding.

No blocker findings.

Major finding: `habitatmech:BACDIVE.b61f30c070` should be marked `NOT_APPLICABLE`. BacDive `Abort` has no source path that would turn the label into a body site or material, the upstream mapping already declined to map it to an ontology term, and the only local review is a class-level `CONFIRM_UNGROUNDED` sweep that explicitly did not assess habitatness. The remaining curation work is to pull this row out of the ungrounded term-candidate bucket and decide it as an item-level non-habitat source artifact.

No minor findings.

## Recommended Edits

Promote the `habitatmech:BACDIVE.b61f30c070` row in `curation/decisions.tsv` from:

| Field | Current |
|---|---|
| `decision` | `CONFIRM_UNGROUNDED` |
| `review_depth` | `CLASS` |

to:

| Field | Recommended |
|---|---|
| `decision` | `NOT_APPLICABLE` |
| `review_depth` | `ITEM` |
| `notes` | `Abort names a BacDive isolation-source source artifact or clinical event rather than a microbial habitat, material, or host-associated site. Pulled out of the class-level sweep and decided individually.` |

Then regenerate the source-derived records and confirm that `data/habitats/other/abort.yaml` disappears, because `NOT_APPLICABLE` BacDive sources should be filtered out of the generated corpus rather than kept as ungrounded habitat records.

## Follow-up Checks

For the recommended curation edit, run:

- `just seed-canary habitatmech:BACDIVE.b61f30c070`
- `just validate-schema`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just report`
- `git diff --check`

Do not run record-specific validation against `data/habitats/other/abort.yaml` after a successful `NOT_APPLICABLE` seed diff removes it; generated `NOT_APPLICABLE` records are absent by design.

## Additional Notes

- The pre-report exact filename check `find reports/yaml_record_review -maxdepth 1 -type f -name '*abort*' -print` returned no previous exact report; `find` includes ignored files.
- `just worklist` did not list `habitatmech:BACDIVE.b61f30c070`, `bacdive.isolation_source:abort`, or `Abort`, consistent with a source concept that already has a decision row.
- `just report` completed successfully and did not flag `habitatmech:BACDIVE.b61f30c070` in any risky-grounding, stale-sweep, current non-habitat, same-label, GOLD-triad-compatibility, minted-GOLD-triad, unsupported-prefix, or organism-identity section.
