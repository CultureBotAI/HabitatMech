# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/bone__234001be.yaml`
- Started UTC: 2026-09-22T03:24:58Z
- Finished UTC: 2026-09-22T03:24:58Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.67f49f8b15` |
| Label | `Bone` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the GOLD ecosystem-path inventory plus the vendored ontology slice; future item-level review belongs in `curation/decisions.tsv`, future authored definitions belong in `curation/term_requests.tsv`, and future mechanism evidence belongs in `curation/causal_graphs/*.yaml`, not in this generated YAML |
| Locked slug | `data/habitats/PATHS.tsv:2044` maps `habitatmech:GOLD.67f49f8b15` to `bone__234001be` |

This is the minted GOLD record for `Host-associated > Mammals: Human > Skeletal system > Bone`. The GOLD path scopes the otherwise generic `Bone` leaf to the human skeletal system and keeps it narrower than the vendored BTO `bone` term.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/bone__234001be.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/bone__234001be.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors |

## Identity and Grounding

The minted identity and its broader parents are supported by the GOLD inventory, the path lock, and the vendored ontology slice:

- `data/raw/gold_ecosystem_paths.tsv:361` is the exact raw GOLD path `Host-associated > Mammals: Human > Skeletal system > Bone`, gives leaf label `Bone`, records depth `4`, records two GOLD ecosystem nodes, aggregates 29 organism assertions, and names `gold.ecosystem:5249|gold.ecosystem:5250` as the upstream nodes.
- `data/habitats/PATHS.tsv:2044` pins the minted identifier `habitatmech:GOLD.67f49f8b15` to slug `bone__234001be`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:142` defines `BTO:0000140` with canonical label `bone`, a connective-tissue definition, `directly_referenced: FALSE`, and `label_only: FALSE`.
- `data/raw/gold_ecosystem_paths.tsv:2438` is the immediate GOLD parent path `Host-associated > Mammals: Human > Skeletal system`, and `data/habitats/PATHS.tsv:3036` pins its generated identifier `habitatmech:GOLD.ea67ca30cb` to `skeletal_system__437a4825`.
- Exact ignored/hidden-inclusive searches found no maintained `curation/decisions.tsv` row for either `habitatmech:GOLD.67f49f8b15` or its minted `Skeletal system` parent, so the target record correctly remains `mapping_status: SEEDED`.

`grounding_status: NARROW` is consistent with the path context: the source concept is human skeletal-system bone, which should not be merged into the generic BTO `bone` identity. Keeping the GOLD path as a minted record and attaching `BTO:0000140` as a broader term preserves the source distinction while retaining the anatomical/material parent. The other parent, `habitatmech:GOLD.ea67ca30cb`, follows directly from the next broader GOLD path step.

## Evidence

Every generated scientific claim in this record is source-derived:

| Claim | Nearest source | Review |
|---|---|---|
| Minted identifier and locked filename | `data/habitats/PATHS.tsv:2044` | Supported exactly |
| GOLD `source_id: gold.ecosystem:5249`, `source_label: Bone`, `source_path`, `assertion_count: 29`, `assertion_unit: ORGANISM`, and the two-node note | `data/raw/gold_ecosystem_paths.tsv:361` | Supported; `gold.ecosystem:5249` is the first of two node IDs that share the exact path |
| Broader BTO `bone` parent | `data/raw/ontology_terms.tsv:142` | Supported as a broader term for the human skeletal-system bone source concept, not as an exact identity |
| Broader GOLD `Skeletal system` parent | `data/raw/gold_ecosystem_paths.tsv:2438`, `data/habitats/PATHS.tsv:3036` | Supported exactly |

The record has no definition, synonyms, xrefs, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussion links, or datasets. Exact ignored/hidden-inclusive searches of the committed raw GOLD tables for `gold.ecosystem:5249`, `gold.ecosystem:5250`, and the full target source path found the cited `gold_ecosystem_paths.tsv` aggregate row, one `gold_path_biosamples.tsv` row for node `5250`, and one `gold_studies.tsv` row for the exact path, but no exact committed GOLD triad row that would feed an environmental-parameter field.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- The sole `source_attestations` entry captures the complete target GOLD path, first upstream node id, aggregate organism count, organism assertion unit, and two-node provenance note.
- Both `parent_habitats` values are expected: the BTO parent records the matched broader ontology term, and the minted `Skeletal system` parent records the next broader GOLD path.
- The record has no environmental parameter: committed `data/raw/gold_path_triads.tsv` rows under this branch belong to child paths such as `Scull: Ethmoid`, not to the exact target path.
- `mapping_status` stays `SEEDED` because no item-level curation decision exists for `habitatmech:GOLD.67f49f8b15`.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research/habitats`, `reports/yaml_record_review`, `docs`, `.claude`, `src`, `tests`, `build`, `data/raw`, and `data/habitats/PATHS.tsv`, while excluding generated `data/text_map/**` and `pages/**`. They found the GOLD and BTO rows, pinned slugs, generated text-map projections, and the historical `curation/samples/narrow-20260814.tsv:31` sample verdict for the `Skeletal system` parent, but no maintained decision row, term request, history record, causal-graph overlay, target-specific research report, or prior exact YAML review report for `habitatmech:GOLD.67f49f8b15`.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if a future curation change adds an item-level decision, authored definition, or causal graph overlay for this human skeletal-system bone record:

- `just validate data/habitats/host_associated/bone__234001be.yaml`
- `just validate-strict data/habitats/host_associated/bone__234001be.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If future curation signs off the exact GOLD path, add the maintained item-level row to `curation/decisions.tsv`, run `just seed`, preview `just seed-canary habitatmech:GOLD.67f49f8b15`, and rerun the corpus-level checks after regeneration.

## Additional Notes

- `find reports/yaml_record_review -name '*bone__234001be*' -print` found no pre-existing exact review report for this record before this file was written.
- `find research/habitats reports/yaml_record_review curation/causal_graphs -iname '*bone*' -print` found the already-reviewed `bone` and `bone__169c3d9e` sibling reports and no target-specific research report, prior exact review report, or causal overlay before this file was written.
- The generated text-map hits for `bone__234001be` and `skeletal_system__437a4825` are search projections, not maintained inputs.
