# YAML Record Review: Biofilm

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/biofilm__599a99de.yaml`
- Started UTC: 2026-09-21T20:38:57Z
- Finished UTC: 2026-09-21T20:39:21Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/biofilm__599a99de.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.e331fc84ae` |
| Label | `Biofilm` |
| Category | `AQUATIC` |
| Grounding | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained owner | generated from `data/raw/gold_ecosystem_paths.tsv`, `data/raw/gold_path_*.tsv`, and `data/habitats/PATHS.tsv` |

The record is a generated GOLD-only child for `Environmental > Aquatic > Freshwater > Creek > Biofilm`. It has no authored definition, synonyms, xrefs, environmental parameters, taxa, evidence items, causal graphs, discussions, external datasets, or item-level curation history.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/aquatic/biofilm__599a99de.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/biofilm__599a99de.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| Reference validator | Not available: `justfile` exposes no reference-specific validator, and this record has no `EvidenceItem`, `EnvironmentalParameter.reference`, `CharacteristicTaxon.reference`, or causal-edge evidence to target. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the committed ENVO term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 found, 0 missing, 0 extra, 0 differing. |

## Identity and Grounding

| Claim | Trace | Assessment |
|---|---|---|
| `habitatmech:GOLD.e331fc84ae` is the pinned identifier for `biofilm__599a99de`. | `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.e331fc84ae` to `biofilm__599a99de`. | Supported. |
| The GOLD source is Creek > Biofilm. | `data/raw/gold_ecosystem_paths.tsv` has the exact row `Environmental > Aquatic > Freshwater > Creek > Biofilm`, leaf label `Biofilm`, depth 5, and node id `gold.ecosystem:8389`; the YAML carries the same source id, label, and path. | Supported. |
| `ENVO:00002034` is the seeded ambiguous-leaf parent. | `data/raw/ontology_terms.tsv` has `ENVO:00002034` `biofilm`, and the Creek/Biofilm exact path appears deeper than generic ontology-grounded biofilm source rows. `docs/HARMONIZATION.md` documents that deeper ambiguous GOLD leaves keep minted identifiers with the matched term as a broader parent instead of all claiming the same ontology identity. | Supported as a broader biofilm parent. |
| `ENVO:00000023` is inherited from the GOLD `Creek` parent path. | `data/habitats/PATHS.tsv` maps the parent `Creek` record to `ENVO:00000023` `stream`, and the exact GOLD triad row for Creek > Biofilm places `ENVO:00000023` in the `local` MIxS role. | Mechanically supported as source context, but likely false as an `is-a` parent because a creek biofilm is on or in a stream rather than a kind of stream. |
| `mapping_status: SEEDED` follows from maintained inputs. | Ignored-file-inclusive searches over `curation/decisions.tsv` found no row for `habitatmech:GOLD.e331fc84ae`; the only curation-history event is the generated 2026-08-16 `seed_from_sources` event. | Supported. |

## Evidence

| Area | Status |
|---|---|
| Source attestation | Supported by the collapsed GOLD path row. The record has no `assertion_count` because that row reports `organism_count=0`. |
| GOLD API context | `data/raw/gold_path_biosamples.tsv` reports 72 biosamples for this exact path, `data/raw/gold_studies.tsv` lists it in one study, and `data/raw/gold_path_triads.tsv` records one broad/local/medium triad with `freshwater biome`, `stream`, and `biofilm`. These rows support item-level curation but are not currently represented as positive source assertion counts. |
| Definition | Empty. No term request defines this path-qualified biofilm. |
| Synonyms and xrefs | Empty. No maintained xref or synonym row exists for this minted child. |
| Environmental parameters | Empty. The environments table has generic `ENVO:00002034` biofilm bands, and the exact GOLD triad uses `ENVO:00002034` as a MIxS `medium`; neither source provides Creek > Biofilm-specific physicochemical values. |
| Characteristic taxa | Empty. The record has no PREGO, BacDive, or Madin source attestation. |
| Causal graphs | Empty. `curation/causal_graphs/biofilm.yaml` belongs to the ontology-grounded `ENVO:00002034` generic biofilm record, not this Creek > Biofilm child. |
| Discussions and datasets | Empty. No maintained input currently targets this record. |

No evidence item is over-scoped because the record has no curator-authored evidence-bearing claim.

## Completeness

- An ignored-file-inclusive `find` found no pre-existing exact `*-biofilm__599a99de.md` report before this report was created.
- Ignored-file-inclusive exact searches for `habitatmech:GOLD.e331fc84ae`, `gold.ecosystem:8389`, `Environmental > Aquatic > Freshwater > Creek > Biofilm`, `ENVO:00000023`, and `ENVO:00002034` covered `data/raw`, `curation`, `history`, `research/habitats`, `reports/yaml_record_review`, `data/habitats/PATHS.tsv`, and the generated target.
- Those searches found the raw GOLD path, exact biosample, exact triad, exact study, ontology, PATHS, and generated YAML rows for the Creek > Biofilm target; they found no item-level `curation/decisions.tsv` row, no term request, no target-owned causal overlay, no target-owned history record, no target-specific research report, and no prior exact YAML review.
- Broader `biofilm` searches found maintained curation for other path-qualified biofilm records. In particular, `habitatmech:GOLD.7f436f8aff` `air scrubber biofilm` already uses `ENVO:00002034` as the genus and `parent_mode=REPLACE` to drop its contextual Air scrubber source parent because the biofilm is inside the scrubber, not a kind of scrubber.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| HM-BIOFILM-599A99DE-001 | Major | `Creek > Biofilm` is still only a seeded ambiguous-leaf child, and its generated `ENVO:00000023` `stream` parent is likely a contextual local-scale link rather than a true `is-a` parent. The exact GOLD triad identifies the Creek as `local` and the Biofilm as `medium`; the sibling Air scrubber biofilm curation uses the same pattern to define a path-qualified biofilm under `ENVO:00002034` while replacing a false inherited source-path parent. | `data/raw/gold_path_triads.tsv` rows for this exact path contain `local ENVO:00000023 stream` and `medium ENVO:00002034 biofilm`; `curation/decisions.tsv` has no item row for `habitatmech:GOLD.e331fc84ae`; `curation/term_requests.tsv` defines `air scrubber biofilm` under `ENVO:00002034` with `parent_mode=REPLACE` because its source parent is contextual. | `curation/decisions.tsv` and `curation/term_requests.tsv`. |

No blocker or minor findings.

## Recommended Edits

| Priority | Edit | Owner | Follow-up validator |
|---:|---|---|---|
| 1 | Add an item-level decision for `habitatmech:GOLD.e331fc84ae` that confirms the record is a creek-specific biofilm rather than generic ENVO biofilm or a stream. | `curation/decisions.tsv` | `just seed`, `just seed-canary habitatmech:GOLD.e331fc84ae --force`, then `just validate data/habitats/aquatic/biofilm__599a99de.yaml`. |
| 2 | If item review confirms the creek-specific reading, add a term request whose genus is `ENVO:00002034` `biofilm` and whose `parent_mode` is `REPLACE`, so the generated record drops the inherited `ENVO:00000023` stream parent. | `curation/term_requests.tsv` | `just term-requests-check`, `just validate-strict data/habitats/aquatic/biofilm__599a99de.yaml --quiet`, and `just verify-corpus --max-diffs 1`. |

## Follow-up Checks

- Re-run `just validate data/habitats/aquatic/biofilm__599a99de.yaml` on the regenerated target.
- Re-run `just validate-strict data/habitats/aquatic/biofilm__599a99de.yaml --quiet` to enforce closed-schema output.
- Re-run `just term-requests-check` after adding the term request.
- Re-run `just validate-history` after adding the required append-only curation history.
- Re-run `just verify-corpus --max-diffs 1` to prove the generated YAML still reproduces from `data/raw/` and `curation/`.

## Additional Notes

- The generated YAML should remain read-only; the future fix belongs in `curation/decisions.tsv` and `curation/term_requests.tsv`.
- The exact GOLD collapsed path has zero GOLD organism assertions, but the later GOLD API inventories still provide useful curation context: 72 biosamples, one study, and one MIxS triad are attached to the exact Creek > Biofilm path.
