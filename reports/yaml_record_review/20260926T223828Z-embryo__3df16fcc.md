# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/embryo__3df16fcc.yaml`
- Started UTC: 2026-09-26T22:30:00Z
- Finished UTC: 2026-09-26T22:38:28Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.32330da5da` |
| Label | `brown algal embryo` |
| Category | `HOST_ASSOCIATED` |
| Grounding | `UNGROUNDED` |
| Mapping | `REVIEWED` |
| Status | Generated from `data/raw/`, `curation/decisions.tsv`, and `curation/term_requests.tsv` |
| Source path | `Host-associated > Algae > Brown Algae > Embryo` |

I read the full generated record. The source concept is a GOLD host-associated path with two node ids, `gold.ecosystem:7227` and `gold.ecosystem:7228`, and three organism assertions in `data/raw/gold_ecosystem_paths.tsv`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/embryo__3df16fcc.yaml` | Pass; LinkML reported no issues. |
| `just validate-strict data/habitats/host_associated/embryo__3df16fcc.yaml` | Pass; 1 file scanned, 0 files with errors. |
| `just validate-causal-all` | Pass; 32 curated causal-graph files validated. |
| `just validate-history` | Pass; 77 history records validated. |
| `just verify-corpus` | Pass; 3,206 generated records are present with no missing, extra, or differing files. |
| `just term-requests-check` | Pass; the generated ENVO robot template is current for 109 curated terms. |

## Identity and Grounding

The grounding decision in `curation/decisions.tsv` is sound: this is a real host-associated habitat with no exact ontology identity yet, and `UBERON:0000922` names the whole organism at a life stage rather than a host-associated environment. Keeping `UBERON:0000922` as an xref and adding `ENVO:01001000` as the broad organism-determined parent matches `docs/CURATION.md`, which says whole hosts keep minted identities, keep taxon or whole-organism terms as xrefs, attach defensible associated-environment parents, and request a specific associated-environment term.

The parent set is otherwise defensible. `ENVO:01001000` is broad but true because brown algae are stramenopiles rather than green plants or animals, and the retained `habitatmech:GOLD.3e4ecbed63` parent preserves the source hierarchy through `Host-associated > Algae > Brown Algae`.

The generated primary label and exact synonyms do not yet agree with that habitat identity. `curation/term_requests.tsv` row 56 asks for `brown algal embryo` with exact synonyms `Embryo` and `Phaeophyceae embryo`. Those names denote the embryo organism or a reusable GOLD life-stage leaf, not the `<X>-associated environment` slot required by the local host rule. The committed deep-research report made the same distinction and suggested `brown algal embryo-associated environment` for the term-request label.

## Evidence

The GOLD attestation is internally consistent: `data/raw/gold_ecosystem_paths.tsv` records the exact source path with two GOLD node ids, three organism assertions, zero study assertions, and zero biosample assertions. A hidden/ignored-inclusive search of `data/raw/gold_path_triads.tsv`, `data/raw/gold_studies.tsv`, `data/raw/gold_path_biosamples.tsv`, and `data/raw/environment_parameters.tsv` for `GOLD.32330da5da`, both GOLD node ids, the full path, and `brown algal embryo` found no target-specific triad, study, biosample, or environment-parameter rows.

The `CONFIRM_UNGROUNDED` curation event is backed by the maintained `curation/decisions.tsv` row. The `DEFINED` event is backed by `curation/term_requests.tsv`, but that same row owns the label and synonym defect described below.

The committed research report supports retaining the concept as a habitat and leaving it ungrounded rather than mapping it to `UBERON:0000922`. It explicitly describes the GOLD path as a sampling context for microbial communities from an entire young brown alga, notes that the embryo is a whole organism rather than a part of a parent alga, and recommends a brown-algal-embryo associated-environment term for ENVO.

## Completeness

The record has no causal graphs, discussions, public datasets, environmental parameters, or characteristic taxa. Those slots are acceptable as empty for this generated record: the source concept has only three GOLD organism assertions, no GOLD study or biosample rows in the source-path inventory, no matching raw triad or environment-parameter rows in the hidden/ignored-inclusive search described above, and no curated overlay was found by either:

- `find curation/causal_graphs -maxdepth 1 -type f -iname '*embryo*' -print`
- `rg --no-ignore --hidden 'GOLD\.32330da5da|gold\.ecosystem:7227|gold\.ecosystem:7228|brown algal embryo|embryo__3df16fcc' history curation/causal_graphs`

No target-specific append-only history file was found by the same hidden/ignored-inclusive search. The generated record still carries the embedded decision and term-request curation events generated from maintained curation TSV rows.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Major | The outbound term request names the host organism, not the microbial habitat environment. `curation/term_requests.tsv` asks for label `brown algal embryo`, then publishes that same organism name to the generated YAML and `curation/term_requests/envo_robot_template.tsv`. The local whole-host rule and the record's committed research both require an `<X>-associated environment` class, and the report's suggested text was `brown algal embryo-associated environment`. | `curation/term_requests.tsv` row for `habitatmech:GOLD.32330da5da` |
| Major | Exact synonyms `Embryo` and `Phaeophyceae embryo` repeat the same type error. `Phaeophyceae embryo` denotes the brown-algal embryo organism. Bare `Embryo` is broader still: the research report and the term-request note both state that GOLD reuses `Embryo` under Brown algae, Red algae, and Fish, so it must not be treated as a standalone habitat name. | `curation/term_requests.tsv` row for `habitatmech:GOLD.32330da5da` |

No blocker findings were found. The record validates, its GOLD source attestation is counted correctly, and the maintained grounding row correctly avoids using `UBERON:0000922` as the habitat identity or as a broader parent.

## Recommended Edits

1. In `curation/term_requests.tsv`, change the `habitatmech:GOLD.32330da5da` label from `brown algal embryo` to `brown algal embryo-associated environment`.
2. In the same row, remove `Embryo|Phaeophyceae embryo` from `exact_synonym` unless exact environment-denoting synonyms are available from inspected sources.
3. Keep the decision row as `CONFIRM_UNGROUNDED` with `UBERON:0000922` as an xref, keep parent mode `ADD`, and keep `ENVO:01001000` as the direct ontology parent unless a new algae-associated ENVO class is available in the slice.
4. Regenerate the term-request template, generated records, redirect map, and rendered pages so the YAML, ENVO robot row, live page slug, and redirect table all agree with the curated row.

## Follow-up Checks

After the maintained row is edited, run:

| Command | Purpose |
|---|---|
| `just term-requests` | Rebuild `curation/term_requests/envo_robot_template.tsv` from the curated request. |
| `just seed-apply` | Regenerate `data/habitats/**` from the maintained raw and curation inputs. |
| `just redirects` | Preserve the old `brown-algal-embryo` page URL after the label-driven slug changes. |
| `just render` | Regenerate `pages/**` from the corrected corpus. |
| `just validate data/habitats/host_associated/embryo__3df16fcc.yaml` | Revalidate this record after generation. |
| `just validate-strict data/habitats/host_associated/embryo__3df16fcc.yaml` | Confirm the corrected generated record remains closed-schema valid. |
| `just verify-corpus` | Confirm no generated YAML was patched by hand. |
| `just term-requests-check` | Confirm the ENVO robot template was regenerated from the curated table. |

## Additional Notes

The exact hidden/ignored-inclusive search over `curation`, `history`, `research`, `reports`, and `data/raw` found the expected failed and successful research-manifest attempts, the maintained decision row, the maintained term-request row, the generated ENVO robot row, the raw GOLD source-path inventory, the committed research report, and the related red-algal and fish embryo curation records. It did not find a second maintained row for `habitatmech:GOLD.32330da5da`.
