# YAML Record Review: Biofouling

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/biofouling__fa010eab.yaml`
- Started UTC: 2026-09-21T22:45:23Z
- Finished UTC: 2026-09-21T22:45:41Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.4bd2954af9` |
| Label | `Biofouling` |
| Category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `REVIEWED` |
| Maintained inputs | `data/raw/gold_ecosystem_paths.tsv`, `curation/decisions.tsv`, `curation/term_requests_excluded.tsv`, `data/habitats/PATHS.tsv` |

This is a generated GOLD record. The path lock maps `habitatmech:GOLD.4bd2954af9` to the `biofouling__fa010eab` slug, and the raw GOLD row maps `Environmental > Aquatic > Marine > Biofouling` to the `Biofouling` leaf at depth 4 with two collapsed GOLD ecosystem nodes, `gold.ecosystem:8355|gold.ecosystem:8356`, and zero organisms, studies, biosamples, or total assertions.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/aquatic/biofouling__fa010eab.yaml` | Pass; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/biofouling__fa010eab.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Pass; the committed term-request table is current with 109 terms. |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass; 3,206 expected records, 3,206 records on disk, and 0 missing, extra, or differing records. |
| `git diff --check` | Pass. |

`just validate-products` was not run. This review did not add or change a grounding row, and the record's ontology xref, `ENVO:06105023`, and current parent, `ENVO:00001999`, were checked directly in the vendored `data/raw/ontology_terms.tsv` slice during the Biofouling cohort review.

## Identity and Grounding

The generated identity and grounding are internally consistent:

- `data/habitats/PATHS.tsv` pins `habitatmech:GOLD.4bd2954af9` to `biofouling__fa010eab`, matching the reviewed YAML path.
- `data/raw/gold_ecosystem_paths.tsv` carries the exact source path and both collapsed source nodes copied into `source_attestations`: `gold.ecosystem:8355` is the first shown node and `gold.ecosystem:8356` is noted by count.
- `curation/decisions.tsv` has an item-level `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.4bd2954af9` with `ENVO:06105023` retained through `relation: xref`.
- `ENVO:06105023` is the ENVO biofouling process term, so it is correctly excluded from `parent_habitats` and retained only in `xrefs`.

The generated parent is not supported. `parent_habitats` points to `ENVO:00001999`, which the vendored ontology slice labels `marine water body` and defines as a lentic water body composed primarily of marine water. A marine biofouling layer occurs in a marine setting, but it is attached biological matter on a solid surface, not a water body. GOLD's immediate `Environmental > Aquatic > Marine` path level is therefore contextual rather than a strict genus for this zero-assertion Biofouling grouping node.

The record is correctly left ungrounded to `ENVO:00002034` `biofilm`. Its concrete child, `Environmental > Aquatic > Marine > Biofouling > Microfouling/Biofilm`, has its own raw GOLD row and is already grounded to `ENVO:00002034` as part of the generated `data/habitats/aquatic/biofilm__cc6fe05a.yaml` merge.

## Evidence

Supported claims:

- The `Biofouling` label, `GOLD` source, `gold.ecosystem:8355` source id, full source path, and two-node note come from `data/raw/gold_ecosystem_paths.tsv`.
- `habitat_category: AQUATIC` follows from the GOLD `Environmental > Aquatic` source path.
- `grounding_status: UNGROUNDED`, `mapping_status: REVIEWED`, and `xrefs: ENVO:06105023` follow from the `CONFIRM_UNGROUNDED`, `ITEM`, `xref` decision row for `habitatmech:GOLD.4bd2954af9`.
- `curation/term_requests_excluded.tsv` explicitly excludes this parent leaf from HabitatMech term requests because it has zero GOLD organisms, studies, and biosamples, and because its only concrete `Microfouling/Biofilm` child already grounds to `ENVO:00002034` `biofilm`.

Unsupported or over-scoped claims:

- `parent_habitats: ENVO:00001999` overstates the GOLD `Marine` source-path context. A Biofouling fouling layer under the marine GOLD bin is not a kind of ENVO `marine water body`, and a sibling curation precedent in `curation/term_requests.tsv` already used `parent_mode=REPLACE` for `macroalgal bed` because a bed underlies a water body rather than being a kind of one.

The record has no definition, record-level evidence, environmental parameters, characteristic taxa, causal graphs, discussion links, or datasets. That absence is supported: ignored-file-inclusive searches found no exact `Environmental > Aquatic > Marine > Biofouling` or `gold.ecosystem:8355` entries in `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, or `data/raw/gold_studies.tsv`, and no exact `habitatmech:GOLD.4bd2954af9` entry in `curation/term_requests.tsv` or `curation/causal_graphs/`.

## Completeness

The record is intentionally sparse enough for the unused GOLD parent leaf it preserves, except for the unsupported inherited parent:

- No Biofouling definition is expected because `curation/term_requests_excluded.tsv` intentionally declines a new term request for this zero-assertion grouping bin.
- No GOLD assertion count is expected because the raw `organism_count`, `study_count`, `biosample_count`, and `total_assertions` are all zero.
- No GOLD MIxS triads or study rows are expected; the exact source path and first source id are absent from the committed GOLD API TSVs named above.
- No causal graph is expected; the record has no sample-derived or literature-derived mechanism claims to support.

All negative checks above used `rg --no-ignore --hidden` or `find`, so ignored files were included where they were in the searched path.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Major | `parent_habitats: ENVO:00001999` asserts that the GOLD `Environmental > Aquatic > Marine > Biofouling` grouping node is a kind of `marine water body`. The reviewed decision text says the source concept is the fouling layer rather than the fouling process; the vendored ontology defines `ENVO:00001999` as a water body, and an attached fouling layer is not itself a water body. | Missing maintained input for source-path parent retractions; the bad edge is generated from the GOLD path through `scripts/seed_from_sources.py`, while `curation/term_requests_excluded.tsv` deliberately keeps this zero-assertion parent leaf out of `curation/term_requests.tsv`. |

## Recommended Edits

1. Add a maintained curation surface for retracting false generated source-path parents independently of `curation/term_requests.tsv`.
2. Use that surface to retract `habitatmech:GOLD.4bd2954af9 -> ENVO:00001999` with evidence that the marine Biofouling source concept is an attached fouling layer under a marine context, not a strict subclass of `marine water body`.
3. Regenerate the target from the maintained input instead of hand-editing `data/habitats/aquatic/biofouling__fa010eab.yaml`.

## Follow-up Checks

After implementing the parent retraction, rerun:

- `just seed`
- `just seed-canary habitatmech:GOLD.4bd2954af9`
- `just seed-apply --force`
- `just validate data/habitats/aquatic/biofouling__fa010eab.yaml`
- `just validate-strict data/habitats/aquatic/biofouling__fa010eab.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`

Then inspect the regenerated target and confirm it no longer carries `ENVO:00001999` as a parent.

## Additional Notes

An ignored-file-inclusive `find` search of `reports/yaml_record_review/` found no prior report for this `biofouling__fa010eab` record before this file was written.

An ignored-file-inclusive `find` search of `curation/causal_graphs/` found no Biofouling causal overlay.
