# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/bryozoa.yaml`
- Started UTC: 2026-09-22T15:35:19Z
- Finished UTC: 2026-09-22T15:35:19Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.dfaedfff09` |
| Label | `Bryozoa` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Maintained owner | `curation/decisions.tsv:1237` records only a `CLASS`-depth ungrounded sweep for this GOLD source concept |
| Locked slug | `data/habitats/PATHS.tsv:2949` maps `habitatmech:GOLD.dfaedfff09` to `bryozoa` |

This is the seeded GOLD record for the top-level `Host-associated > Bryozoa` path. The generated YAML faithfully preserves the current inputs: it keeps the source path minted, inherits `ENVO:01001000` as the host-associated parent, records the first of three GOLD node ids, and stays `SEEDED` because the only decision row is a class-level lexical sweep. The record still needs item-level curation because the bare host-phylum label `Bryozoa` is not itself the habitat identity, and GOLD separately has a `Host-associated > Invertebrates > Bryozoans` branch that currently mints a duplicate unreviewed bryozoan host-clade record.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/bryozoa.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/bryozoa.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The generated identifier, label, category, grounding status, source attestation, and seed status are supported by maintained inputs:

- `data/raw/gold_ecosystem_paths.tsv:1906` is the raw GOLD row for `Host-associated > Bryozoa`. It has leaf label `Bryozoa`, depth 2, three GOLD node ids, zero organism assertions, zero study assertions, zero biosample assertions, and total assertion count 0.
- `curation/decisions.tsv:1237` is a `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.dfaedfff09`, but its `review_depth` is `CLASS` and its note says whether the concept is a habitat was not assessed.
- `data/raw/ontology_terms.tsv:8495` provides `ENVO:01001000` `environmental system determined by an organism`, and `data/raw/ontology_subclass_edges.tsv:6795` places it under `ENVO:01000254` `environmental system`.
- `data/habitats/PATHS.tsv:2949` pins the generated target to `data/habitats/host_associated/bryozoa.yaml`.
- `data/raw/gold_ecosystem_paths.tsv:1907` and `:1908` are the GOLD descendants of this top-level path, `Reproductive system` and `Ovicells`. The `Ovicells` child has supplemental one-biosample and one-study rows in `data/raw/gold_path_biosamples.tsv:1005` and `data/raw/gold_studies.tsv:2557`.
- `data/raw/gold_ecosystem_paths.tsv:842`, `curation/decisions.tsv:1165`, `data/habitats/PATHS.tsv:2847`, and `data/habitats/host_associated/bryozoans.yaml` show that `Host-associated > Invertebrates > Bryozoans` is a separate seeded source concept for the same host phylum; that branch has two organism assertions.

The current `UNGROUNDED` status is a reproducible lexical-sweep result, not a reviewed habitat decision. The source concept should likely remain minted because a whole bryozoan taxon is not the habitat identity, but the minted record should be a bryozoan-associated environment and it should not be split from the plural GOLD `Bryozoans` branch if both source paths assert the same host scope.

## Evidence

Every generated claim is traceable to a raw row or maintained curation input:

| Claim | Nearest source | Review |
|---|---|---|
| Minted identifier `habitatmech:GOLD.dfaedfff09` | GOLD path minting from `Host-associated > Bryozoa` in `src/habitatmech/seed.py` | Supported exactly |
| Label `Bryozoa`, category `HOST_ASSOCIATED`, source label, source path, and three-node note | `data/raw/gold_ecosystem_paths.tsv:1906` | Supported exactly |
| `source_id: gold.ecosystem:5180` | first node id in `data/raw/gold_ecosystem_paths.tsv:1906` | Supported exactly; `gold.ecosystem:5180\|gold.ecosystem:5181\|gold.ecosystem:5182` share the path |
| Absence of `assertion_count` and `assertion_unit` | `organism_count` is `0` in `data/raw/gold_ecosystem_paths.tsv:1906`; `src/habitatmech/seed.py` emits GOLD assertion counts only when the organism count is nonzero | Supported exactly |
| Parent `ENVO:01001000` | Host-associated category inference in the GOLD seeder, backed by the ENVO vendored slice | Supported as the broad host-associated genus |
| `grounding_status: UNGROUNDED` | `curation/decisions.tsv:1237` | Supported exactly |
| `mapping_status: SEEDED` | `curation/decisions.tsv:1237` has `review_depth` `CLASS`; only `ITEM` decisions count as reviewed | Supported exactly |
| Absence of a definition, synonym, xref, environmental parameter, characteristic taxon, causal graph, record-level evidence, discussion, and dataset | No maintained input row or overlay found for these slots | Supported by bounded exact searches |

The one GOLD biosample and study side-table rows under `Host-associated > Bryozoa > Reproductive system > Ovicells` are correctly absent from this parent record. They belong to the generated Ovicells descendant unless curation later decides to merge or retire that leaf.

## Completeness

The generated fields are complete for the maintained inputs that currently feed this record:

- The single GOLD source attestation captures the path, the first GOLD node id, and the multi-node note.
- The host-associated `ENVO:01001000` parent is present.
- The generated corpus keeps this node in use as a parent of the seeded `Host-associated > Bryozoa > Reproductive system` record.
- No item-level decision, authored term request, target-specific causal graph overlay, append-only history record, deep-research report, GOLD side-table row on the exact `Host-associated > Bryozoa` path, term-request exclusion row, or previous exact YAML review report exists for `habitatmech:GOLD.dfaedfff09`.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated target, excluding generated `build`, `data/text_map`, and `pages` trees. They found the raw top-level Bryozoa row, the child Reproductive system and Ovicells rows, Ovicells GOLD biosample and study side rows, the duplicate `Bryozoans` source concept under GOLD Invertebrates, the class-level decision rows, the generated YAML cited above, and path locks; they found no Bryozoa-specific term request, term-request exclusion, causal overlay, history record, deep-research report, GOLD side-table row on `Host-associated > Bryozoa` itself, BacDive or PREGO row, or prior exact YAML review report.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Major | `habitatmech:GOLD.dfaedfff09` remains a `SEEDED` host-associated GOLD record because its only decision row is a class-level ungrounded sweep that did not assess habitat validity. The current record therefore publishes the bare host phylum `Bryozoa` with no definition, and the semantically equivalent `Host-associated > Invertebrates > Bryozoans` branch remains a separate seeded record even though both source concepts denote bryozoans as hosts. | Add `ITEM`-depth rows in `curation/decisions.tsv` for `habitatmech:GOLD.dfaedfff09` and `habitatmech:GOLD.d2ef97a849`; if retained, add one associated-environment definition in `curation/term_requests.tsv`, then regenerate `bryozoa` and `bryozoans`. |

## Recommended Edits

- Add an item-level `curation/decisions.tsv` row for `habitatmech:GOLD.dfaedfff09` that explicitly reviews `Host-associated > Bryozoa`.
- Review `habitatmech:GOLD.d2ef97a849` for `Host-associated > Invertebrates > Bryozoans` in the same curation pass and merge it with `habitatmech:GOLD.dfaedfff09` if both paths have the same bryozoan host-clade scope.
- If the source concepts should publish, keep the surviving identity minted and define `bryozoan-associated environment` in `curation/term_requests.tsv` with parent `ENVO:01001000`; keep any bryozoan taxon term only as a related xref.
- Regenerate the target with `just seed` and preview `habitatmech:GOLD.dfaedfff09` with `just seed-canary habitatmech:GOLD.dfaedfff09` before applying the full generated corpus.

## Follow-up Checks

After item-level Bryozoa curation, re-run the same focused validation set and confirm the `Host-associated > Bryozoa` and `Host-associated > Invertebrates > Bryozoans` attestations land on one reviewed generated identity or on deliberately distinct reviewed records:

- `just validate data/habitats/host_associated/bryozoa.yaml`
- `just validate-strict data/habitats/host_associated/bryozoa.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

Also verify that the child `Host-associated > Bryozoa > Reproductive system` record keeps a strictly broader parent and that the `Ovicells` biosample and study rows remain attached to the surviving child record.

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -name '*bryozoa*' -print` found no pre-existing exact review report for this record before this file was written.
- Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.dfaedfff09`, `gold.ecosystem:5180`, and `Host-associated > Bryozoa`, plus a case-insensitive hidden-inclusive search for `bryozo`, found no target-specific maintained artifact beyond the raw, curation, path-lock, and generated rows cited above.
