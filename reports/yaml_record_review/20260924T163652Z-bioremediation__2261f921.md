# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/engineered/bioremediation__2261f921.yaml`
- Started UTC: 2026-09-24T16:31:42Z
- Finished UTC: 2026-09-24T16:36:52Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.5f0e9e816a` |
| Label | `Bioremediation` |
| Category | `ENGINEERED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Generated path | `data/habitats/engineered/bioremediation__2261f921.yaml` |
| Source concept | GOLD `Engineered > Bioremediation` |

The target is the depth-2 GOLD `Bioremediation` source path directly under GOLD's top-level `Engineered` branch. It is not one of the pollutant-specific or material-specific children under `Engineered > Bioremediation`.

The file is generated. `data/habitats/PATHS.tsv:1988` maps `habitatmech:GOLD.5f0e9e816a` to the stable `bioremediation__2261f921` slug, so future fixes belong in `curation/decisions.tsv`, `curation/term_requests.tsv`, or future source-path parent suppression support rather than in this YAML.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/bioremediation__2261f921.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/bioremediation__2261f921.yaml` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| Target causal overlay validator | Not applicable; an ignored-inclusive exact search over `curation/causal_graphs` for the target identifier and exact source path, plus `find curation/causal_graphs -maxdepth 1 -type f -name '*bioremediation*' -print`, found no target overlay. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files and 32 graphs validated. |
| Reference validator | Not applicable; this generated record has no record-level `evidence` or `causal_graphs`, and HabitatMech has no dedicated per-record reference check. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 records were expected, 3206 were found, and `data/habitats/` reproduced exactly from `data/raw/`. |
| `just worklist --limit 2000` | Passed; 0 ungrounded records remain undecided and 1810 decisions are on file. |
| `just report` | Passed; the corpus report regenerated successfully. |
| `git diff --check` | Passed. |

## Identity and Grounding

`data/raw/gold_ecosystem_paths.tsv:600` is the maintained GOLD row for this exact source concept. It has canonical path `Engineered > Bioremediation`, source leaf `Bioremediation`, depth `2`, four collapsed GOLD node IDs, six direct organism assertions, no direct study or biosample assertions in that aggregate row, and `gold.ecosystem:2893|gold.ecosystem:3527|gold.ecosystem:3866|gold.ecosystem:4304` as source node IDs.

The generated source attestation mirrors that row with GOLD `gold.ecosystem:2893`, source label `Bioremediation`, source path `Engineered > Bioremediation`, `assertion_count: 6`, `assertion_unit: ORGANISM`, and a note that four GOLD ecosystem node IDs share this path.

The exact GOLD path also appears in `data/raw/gold_path_biosamples.tsv:942` with one direct biosample for path ID `4304` and in `data/raw/gold_studies.tsv:2045` with the single-path study row `Gs0135004`. An ignored-inclusive exact raw search found no MIxS triad row for the exact path in `data/raw/gold_path_triads.tsv`.

`curation/decisions.tsv:583` is the only target decision row. It is a class-level `CONFIRM_UNGROUNDED` row from the lexical sweep and explicitly says the sweep did not assess whether the concept is a habitat at all, so `UNGROUNDED` has not been confirmed by item-level review of this source concept.

The target's only parent, `habitatmech:GOLD.2acb39dd08`, is GOLD's top-level `Engineered` source path. `data/raw/gold_ecosystem_paths.tsv:253` defines that parent path and `curation/decisions.tsv:330` gives it the same class-level `CONFIRM_UNGROUNDED` decision shape, so no maintained input currently proves that `Bioremediation` is a strict subclass of a reviewed `Engineered` habitat.

## Evidence

The generated target has no record-level `evidence`, no `definition`, no `environmental_parameters`, no `characteristic_taxa`, and no target-specific `causal_graphs`.

Supported:

- The source-path identity and four-node collapse are reproducible from `data/raw/gold_ecosystem_paths.tsv:600`.
- The generated parent edge to `habitatmech:GOLD.2acb39dd08` is reproducible from the source path prefix `Engineered`.
- The exact path has limited auxiliary GOLD evidence: one direct biosample row in `data/raw/gold_path_biosamples.tsv:942` and one direct study row in `data/raw/gold_studies.tsv:2045`.

Unsupported or over-scoped:

- No item-level decision has inspected whether this GOLD `Bioremediation` branch denotes a microbial habitat, a bioremediation process or program, a grouping node for downstream contaminant classes, or a broad environmental material being remediated.
- No maintained term request defines what a generic `Bioremediation` habitat would be if the concept is retained as a novel habitat.
- No item-level review has shown that the inherited top-level `Engineered` parent is a strict habitat parent instead of a source category.

Related curation:

- Several reviewed children under `Engineered > Bioremediation` are treated as contaminant-specific remediation materials. For example, `curation/term_requests.tsv:67`, `curation/term_requests.tsv:83`, and `curation/term_requests.tsv:92` request persistent-organic-pollutant, methane, and xylene bioremediation material terms under `ENVO:00010483` `environmental material`.
- That child-level pattern is useful context, but it does not itself review the generic `Bioremediation` node or the inherited `Engineered` parent.

## Completeness

Consequential gaps:

- The target has no item-level decision or term request.
- The target has no definition, environmental parameters, characteristic taxa, causal graph, or target-specific literature evidence.
- The target's only parent is also class-level reviewed rather than item-reviewed.
- `just report` lists `bioremediation` as one of 15 novel labels held by records from different sources, with GOLD and BacDive records still separate; that is a same-label curation lead, not a reviewed equivalence claim.

Bounded searches:

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-bioremediation__2261f921.md' -print` found no earlier YAML review report for this exact stem. `find` is not gitignore-filtered.
- An ignored-inclusive exact search over `curation`, `history`, `research`, `data/habitats/PATHS.tsv`, `data/habitats/engineered`, `reports/yaml_record_review`, `data/raw`, and `conf/id_label_targets.yaml` for `habitatmech:GOLD.5f0e9e816a` found the path lock, the class-level decision row, generated child records that use this target as a parent, this target YAML, and the earlier `Thiocyanate` YAML review's parent note; it found no target-specific term request, term-request exclusion, history record, deep-research report, id-label residual, or causal overlay.
- Ignored-inclusive exact searches over the same corpus for `gold.ecosystem:2893`, `gold.ecosystem:3527`, `gold.ecosystem:3866`, and `gold.ecosystem:4304` found the raw aggregate row and this generated target, and found no target-specific curation inputs.
- An ignored-inclusive exact raw search found `data/raw/gold_ecosystem_paths.tsv:600`, `data/raw/gold_path_biosamples.tsv:942`, and `data/raw/gold_studies.tsv:2045` for the exact `Engineered > Bioremediation` path and found no exact MIxS triad row.
- An ignored-inclusive `data/raw/ontology_terms.tsv` search for `bioremediat|remediat` found no vendored ontology term for bioremediation or remediation.
- Ignored-inclusive searches of `curation/term_requests.tsv`, `curation/term_requests_excluded.tsv`, `curation/decisions.tsv`, and `curation/samples` for `bioremediat|remediat` found child-level pollutant/material curation rows and no item-level row for this target.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The target has only a class-level `CONFIRM_UNGROUNDED` row, so `UNGROUNDED` has not been item-reviewed against the full `Engineered > Bioremediation` source path. | `curation/decisions.tsv:583` records `review_depth=CLASS` and says habitathood was not assessed. The exact path has one direct biosample row and one direct study row in the raw GOLD auxiliary tables, so this is not simply an unattested empty branch in all committed raw GOLD tables. | `curation/decisions.tsv`; if item review confirms a real novel habitat, `curation/term_requests.tsv`. |
| Major | The target's only parent, `habitatmech:GOLD.2acb39dd08`, is GOLD's top-level `Engineered` source grouping and has not been item-reviewed as a strict broader habitat. | `data/raw/gold_ecosystem_paths.tsv:253` defines the parent as the depth-1 `Engineered` branch; `curation/decisions.tsv:330` records the same `review_depth=CLASS` "habitathood was NOT assessed" decision for that parent. | `curation/decisions.tsv`; if item review rejects the source-derived edge, future parent-suppression support in `src/habitatmech/seed.py` or a `curation/term_requests.tsv` row with `parent_mode=REPLACE`. |

## Recommended Edits

1. Item-review `habitatmech:GOLD.5f0e9e816a` in `curation/decisions.tsv` against the exact `Engineered > Bioremediation` path and the GOLD `4304` biosample.
2. Decide whether the source node is a generic bioremediation environmental material with no ontology term, a non-habitat process or administrative branch, or the same concept as the BacDive `bioremediation` record flagged by `just report`.
3. If the GOLD concept is retained as a novel habitat, add a definition to `curation/term_requests.tsv` under a strict material parent and use `parent_mode=REPLACE` if the inherited `Engineered` parent remains only contextual.
4. Item-review `habitatmech:GOLD.2acb39dd08` `Engineered` in `curation/decisions.tsv` before relying on it as an is-a parent for this or other depth-2 engineered GOLD records.

## Follow-up Checks

- After adding a decision or term request, run `just seed`, `just seed-canary habitatmech:GOLD.5f0e9e816a`, inspect `data/habitats/engineered/bioremediation__2261f921.yaml`, then run `just seed-apply --force`.
- Run `just validate data/habitats/engineered/bioremediation__2261f921.yaml` and `just validate-strict data/habitats/engineered/bioremediation__2261f921.yaml`.
- Run `just term-requests-check`, `just validate-history`, `just verify-corpus --max-diffs 1`, `just worklist --limit 2000`, `just report`, and `git diff --check`.
- Run `just validate-causal-all` if any overlay changes.

## Additional Notes

- The exact ignored-inclusive miss searches included hidden and gitignored files.
- The existing child-level remediation-material term requests justify treating contaminant branches cautiously; they do not prove that `Bioremediation` itself is a strict habitat class.
