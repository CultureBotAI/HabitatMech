# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/bone__71064238.yaml`
- Started UTC: 2026-09-22T03:42:17Z
- Finished UTC: 2026-09-22T03:42:17Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.9d0dd3ece9` |
| Label | `Bone` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the GOLD ecosystem-path inventory plus the vendored ontology slice; future item-level review belongs in `curation/decisions.tsv`, future authored definitions belong in `curation/term_requests.tsv`, and future mechanism evidence belongs in `curation/causal_graphs/*.yaml`, not in this generated YAML |
| Locked slug | `data/habitats/PATHS.tsv:2457` maps `habitatmech:GOLD.9d0dd3ece9` to `bone__71064238` |

This is the minted GOLD record for `Host-associated > Mammals > Remains > Fossilized remains > Bone`. The GOLD path scopes the generic `Bone` leaf to fossilized mammal remains, so the record is correctly retained as a child of the BTO `bone` term and the minted GOLD `Fossilized remains` parent instead of being merged into the generic BTO identity.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/bone__71064238.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/bone__71064238.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors |

## Identity and Grounding

The minted identity and its broader parents are supported by the GOLD inventory, the path lock, and the vendored ontology slice:

- `data/raw/gold_ecosystem_paths.tsv:2092` is the exact raw GOLD path `Host-associated > Mammals > Remains > Fossilized remains > Bone`, gives leaf label `Bone`, records depth `5`, records one GOLD ecosystem node, aggregates zero organism, study, biosample, and total assertions, and names `gold.ecosystem:8087` as the upstream node.
- `data/habitats/PATHS.tsv:2457` pins the minted identifier `habitatmech:GOLD.9d0dd3ece9` to slug `bone__71064238`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:142` defines `BTO:0000140` with canonical label `bone`, a connective-tissue definition, `directly_referenced: FALSE`, and `label_only: FALSE`.
- `data/raw/gold_ecosystem_paths.tsv:2091` is the immediate GOLD parent path `Host-associated > Mammals > Remains > Fossilized remains`, and `data/habitats/PATHS.tsv:2739` pins its generated identifier `habitatmech:GOLD.c5640eadf7` to `fossilized_remains__eec3c6e8`.
- Exact ignored/hidden-inclusive searches found no maintained `curation/decisions.tsv` row for `habitatmech:GOLD.9d0dd3ece9`. The generated `Fossilized remains` parent has only a class-level no-lexical-match `CONFIRM_UNGROUNDED` decision, so neither the target nor that parent has item-level curation yet.

`grounding_status: NARROW` is consistent with the path context: the source concept is fossilized-remains bone, which should not be merged into the generic BTO `bone` identity. Keeping the GOLD path as a minted record and attaching `BTO:0000140` as a broader term preserves the source distinction while retaining the anatomical/material parent. The other parent, `habitatmech:GOLD.c5640eadf7`, follows directly from the next broader GOLD path step.

## Evidence

Every generated scientific claim in this record is source-derived:

| Claim | Nearest source | Review |
|---|---|---|
| Minted identifier and locked filename | `data/habitats/PATHS.tsv:2457` | Supported exactly |
| GOLD `source_id: gold.ecosystem:8087`, `source_label: Bone`, `source_path`, and omitted assertion count/unit | `data/raw/gold_ecosystem_paths.tsv:2092` | Supported; the exact raw GOLD aggregate row has one node and zero total assertions |
| Broader BTO `bone` parent | `data/raw/ontology_terms.tsv:142` | Supported as a broader term for the fossilized-remains bone source concept, not as an exact identity |
| Broader GOLD `Fossilized remains` parent | `data/raw/gold_ecosystem_paths.tsv:2091`, `data/habitats/PATHS.tsv:2739` | Supported exactly |

The record has no definition, synonyms, xrefs, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussion links, or datasets. Exact ignored/hidden-inclusive searches of the committed raw GOLD tables for `gold.ecosystem:8087` and the full target source path found the cited `gold_ecosystem_paths.tsv` aggregate row, one `gold_path_biosamples.tsv` row for two biosamples, three exact `gold_path_triads.tsv` rows for one-sample broad, local, and medium MIxS context, and one `gold_studies.tsv` row listing the exact path among four paths in study `Gs0161452`.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- The sole `source_attestations` entry captures the complete target GOLD path and first upstream node id. It correctly omits `assertion_count` and `assertion_unit` because the exact `gold_ecosystem_paths.tsv` aggregate has zero total assertions.
- Both `parent_habitats` values are expected: the BTO parent records the matched broader ontology term, and the minted `Fossilized remains` parent records the next broader GOLD path.
- The record has no environmental parameter: the exact committed GOLD triad rows are source context rows and are not currently promoted to generated `environmental_parameters`.
- `mapping_status` stays `SEEDED` because no item-level curation decision exists for `habitatmech:GOLD.9d0dd3ece9`.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `docs`, `.claude`, `src`, `tests`, `build`, `data/raw`, and `data/habitats/PATHS.tsv`, while excluding generated `data/text_map/**` and `pages/**`. They found the GOLD and BTO rows, pinned slugs, generated text-map projections, and the class-level `Fossilized remains` parent decision, but no maintained target decision row, term request, history record, causal-graph overlay, target-specific research report, or prior exact YAML review report for `habitatmech:GOLD.9d0dd3ece9`.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if a future curation change adds an item-level decision, authored definition, or causal graph overlay for this fossilized-remains bone record:

- `just validate data/habitats/host_associated/bone__71064238.yaml`
- `just validate-strict data/habitats/host_associated/bone__71064238.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If future curation signs off the exact GOLD path, add the maintained item-level row to `curation/decisions.tsv`, run `just seed`, preview `just seed-canary habitatmech:GOLD.9d0dd3ece9`, and rerun the corpus-level checks after regeneration.

## Additional Notes

- `find reports/yaml_record_review -name '*bone__71064238*' -print` found no pre-existing exact review report for this record before this file was written.
- `find curation history research reports -name '*bone__71064238*' -o -name '*9d0dd3ece9*' -o -name '*8087*'` found no maintained target-specific decision, history, research, report, or causal overlay artifacts before this file was written.
- The generated text-map hits for `bone__71064238` and `fossilized_remains__eec3c6e8` are search projections, not maintained inputs.
