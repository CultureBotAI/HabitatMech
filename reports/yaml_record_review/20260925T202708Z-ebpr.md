# YAML Record Review

- Repository: HabitatMech
- Record: `data/habitats/engineered/ebpr.yaml`
- Started UTC: 2026-09-25T20:27:08Z
- Finished UTC: 2026-09-25T20:27:13Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Identifier | `habitatmech:GOLD.b5081d36b9` |
| Label | `EBPR` |
| Class | `HabitatRecord` |
| Category | `ENGINEERED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Source concept | GOLD `Engineered > Bioreactor > EBPR` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/PATHS.tsv`, and `curation/decisions.tsv`; the YAML record is read-only |
| Locked slug | `data/habitats/PATHS.tsv:2620` maps `habitatmech:GOLD.b5081d36b9` to `ebpr` |

The generated record is the collapsed GOLD depth-3 `EBPR` node under the
`Bioreactor` branch. `data/raw/gold_ecosystem_paths.tsv:1184` lists canonical
path `Engineered > Bioreactor > EBPR`, leaf label `EBPR`, depth `3`, no direct
organism, study, biosample, or total assertions, and three equivalent GOLD node
ids: `gold.ecosystem:7758`, `gold.ecosystem:7759`, and `gold.ecosystem:7760`.

The YAML at `data/habitats/engineered/ebpr.yaml:1-13` preserves that source
identity with `identifier: habitatmech:GOLD.b5081d36b9`, `label: EBPR`, the
generated `ENVO:00002123` `bioreactor` parent, `source_id:
gold.ecosystem:7758`, and a note explaining that three GOLD node ids share the
same path.

## Validation

| Command | Result |
| --- | --- |
| `just validate data/habitats/engineered/ebpr.yaml` | Passed; LinkML reported no issues. |
| `just validate-strict data/habitats/engineered/ebpr.yaml` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; `curation/term_requests.tsv` is current for 109 generated terms. |
| `just validate-history` | Passed; 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected and found 3206 records, with 0 missing, 0 extra, and 0 differing files. |
| `just worklist --limit 0 --status all --out /tmp/habitatmech-ebpr-worklist.tsv` | Passed; wrote 953 ungrounded rows and ranked `habitatmech:GOLD.b5081d36b9` at row 515 with no lexical candidate term. |
| `just report` | Passed; the corpus report completed and kept the class-level sweep separate from individually examined term requests. |

No validator was skipped.

## Identity and Grounding

`data/raw/gold_ecosystem_paths.tsv:1184` supports the generated GOLD identity
exactly. The raw row is `Engineered > Bioreactor > EBPR`, has leaf label
`EBPR`, and collapses source nodes `gold.ecosystem:7758`,
`gold.ecosystem:7759`, and `gold.ecosystem:7760`. The generated source
attestation at `data/habitats/engineered/ebpr.yaml:8-13` repeats
`gold.ecosystem:7758`, `EBPR`, and the exact path, with a note pointing back to
the raw table for the other two node ids.

The current curated grounding decision is only a lexical class sweep.
`curation/decisions.tsv:1024` records `CONFIRM_UNGROUNDED` with no object term,
no object label, no relation, and `review_depth` `CLASS`. The decision note says
no vendored-slice term matched the label by the class-level search routes and
explicitly says habitathood was not assessed. `docs/CURATION.md` and
`docs/HARMONIZATION.md` define `CLASS` as a mechanically defined screen that
does not promote a record to `REVIEWED`.

The generated `parent_habitats` edge to `ENVO:00002123` is traceable to the
GOLD source parent `Engineered > Bioreactor`. The vendored ontology slice has
`ENVO:00002123` `bioreactor`, `ENVO:00002124` `anaerobic bioreactor`,
`ENVO:00002126` `aerobic bioreactor`, and a small set of other bioreactor
subtypes, but ignored/hidden-inclusive searches found no exact EBPR,
enhanced-biological-phosphorus-removal, or biological-phosphorus-removal
ontology term in `data/raw/ontology_terms.tsv`.

## Evidence

| Claim | Evidence | Status |
| --- | --- | --- |
| The record denotes the GOLD path `Engineered > Bioreactor > EBPR`. | `data/raw/gold_ecosystem_paths.tsv:1184` lists that canonical path, leaf label `EBPR`, and node ids `gold.ecosystem:7758|gold.ecosystem:7759|gold.ecosystem:7760`; `data/habitats/engineered/ebpr.yaml:8-13` repeats the first source node and the path. | Supported exactly. |
| The raw GOLD EBPR path has no direct source assertions. | `data/raw/gold_ecosystem_paths.tsv:1184` reports `organism_count` 0, `study_count` 0, `biosample_count` 0, and `total_assertions` 0. | Supported exactly; the generated source attestation correspondingly omits `assertion_count` and `assertion_unit`. |
| EBPR has child GOLD paths that hold the EBPR/Anaerobic-Aerobic branch. | `data/raw/gold_ecosystem_paths.tsv:1185-1187` lists `Anaerobic-Aerobic`, `Activated sludge`, and `Sludge` below `Engineered > Bioreactor > EBPR`. | Supported exactly. |
| The only committed GOLD study, biosample, and MIxS-triad evidence under this branch is attached to `Engineered > Bioreactor > EBPR > Anaerobic-Aerobic > Sludge`, not to `Engineered > Bioreactor > EBPR` itself. | Ignored/hidden-inclusive exact searches in `data/raw/gold_studies.tsv`, `data/raw/gold_path_biosamples.tsv`, and `data/raw/gold_path_triads.tsv` found rows for the child `Sludge` path and no rows for the exact EBPR parent path. | Supported for the committed GOLD source tables. |
| No EBPR-specific causal overlay is attached. | An ignored/hidden-inclusive search over `curation/causal_graphs` found no `identifier: habitatmech:GOLD.b5081d36b9`, and `find curation/causal_graphs -maxdepth 1 -type f -name 'ebpr.yaml' -print` returned no files. | Supported exactly. |

## Completeness

The record is complete for the current generated input state but materially
incomplete as curation. It has the exact GOLD path, the locked `ebpr` slug, one
generated `ENVO:00002123` parent, one class-level `CONFIRM_UNGROUNDED`
decision, no definition, no synonyms, no xrefs, no assertion count, no
environmental parameters, no characteristic taxa, no causal graph, no
discussion, and no dataset.

Ignored/hidden-inclusive exact searches covered `curation`, `history`,
`research`, `reports/yaml_record_review`, `conf/id_label_targets.yaml`,
`data/habitats/PATHS.tsv`, `data/habitats/engineered/ebpr.yaml`,
`data/raw/gold_ecosystem_paths.tsv`, `data/raw/gold_path_biosamples.tsv`,
`data/raw/gold_studies.tsv`, `data/raw/gold_path_triads.tsv`, and
`data/raw/ontology_terms.tsv`. They found the generated target, the locked
slug, the source inventory row, the `curation/decisions.tsv` class-level row,
child EBPR source paths, one unrelated deep-research report that mentions EBPR
as wastewater-treatment context, and prior child-record YAML reviews. They
found no EBPR-specific term-request row, excluded term-request row, history
record, causal graph overlay, direct GOLD biosample row, direct GOLD study row,
direct GOLD MIxS-triad row, or pre-existing `*-ebpr.md` review report.

`reports/yaml_record_review/20260924T140857Z-anaerobic_aerobic.md` had already
flagged `habitatmech:GOLD.b5081d36b9` as an unreviewed source-path parent for
the EBPR/Anaerobic-Aerobic record. Reviewing EBPR directly confirms the upstream
cause: GOLD contributes an intermediate zero-assertion path, but HabitatMech has
not item-reviewed whether that path is itself an enhanced-biological-
phosphorus-removal habitat or only a contextual folder for the material records
below it.

## Findings

| ID | Severity | Finding | Maintained owner |
| --- | --- | --- | --- |
| HM-EBPR-001 | Major | `EBPR` remains at `review_depth` `CLASS`. The only maintained decision at `curation/decisions.tsv:1024` explicitly did not assess whether the GOLD path is a habitat at all, while the raw GOLD row has zero direct organisms, studies, biosamples, or MIxS triads. The generated `ENVO:00002123` parent is traceable source hierarchy, but no item-level curation has established that this zero-assertion intermediate path should remain minted as a bioreactor habitat instead of being retired as a non-habitat operating context. | `curation/decisions.tsv`; if the concept stays minted, add the definition and authored hierarchy in `curation/term_requests.tsv`. |

## Recommended Edits

1. Reopen `habitatmech:GOLD.b5081d36b9` in `curation/decisions.tsv` with
   `review_depth` `ITEM` and decide whether `Engineered > Bioreactor > EBPR`
   denotes an enhanced-biological-phosphorus-removal bioreactor habitat, a
   wastewater-treatment process folder, or another non-habitat context.
2. If the concept is a real habitat with no exact ontology term, keep it minted
   with `CONFIRM_UNGROUNDED`, add an evidence-backed definition in
   `curation/term_requests.tsv`, and preserve `ENVO:00002123` only if the item
   review confirms `bioreactor` is a true genus.
3. If the concept is not itself a habitat, mark it `NOT_APPLICABLE` and rerun
   seeding to verify that child records such as
   `data/habitats/engineered/anaerobic_aerobic.yaml` do not keep a non-habitat
   `parent_habitats` edge.

## Follow-up Checks

- `rg --no-ignore --hidden -n 'habitatmech:GOLD\.b5081d36b9|Engineered > Bioreactor > EBPR|\bEBPR\b' curation/decisions.tsv curation/term_requests.tsv history research data/habitats/engineered`
- `just seed`
- `just seed-canary habitatmech:GOLD.b5081d36b9`
- `just seed-apply --force`
- `just validate data/habitats/engineered/ebpr.yaml`
- `just validate-strict data/habitats/engineered/ebpr.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just report`

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-ebpr.md' -print` found no pre-existing exact review report before this file was written; `find` included ignored files under the searched directory.
- `find curation/causal_graphs -maxdepth 1 -type f -name 'ebpr.yaml' -print` found no slug-matched causal overlay before this report was written; `find` included ignored files under the searched directory.
- The ignored/hidden-inclusive search for `EBPR`, `enhanced biological phosphorus`, `biological phosphorus`, and `phosphorus removal` in `curation/term_requests.tsv`, `curation/term_requests_excluded.tsv`, `research`, `history`, and `conf/id_label_targets.yaml` found one dissolved-organics research report that discusses EBPR as adjacent wastewater-treatment context, and no EBPR curation input or history artifact.
