# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/sheet_of_paper.yaml
- Started UTC: 2026-09-24T10:42:51Z
- Finished UTC: 2026-09-24T10:42:51Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `HabitatRecord` |
| Identifier | `ENVO:03501256` |
| Label | `sheet of paper` |
| Category | `ENGINEERED` |
| Source concept | `habitatmech:GOLD.b2d1395135` |
| Source path | `Engineered > Paper` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv`; future curation belongs in `curation/decisions.tsv` or in a maintained parent-override input, not in `data/habitats/engineered/sheet_of_paper.yaml`. |

The generated record grounds GOLD's `Engineered > Paper` concept to ENVO's `sheet of paper` with `grounding_status: CLOSE` and `mapping_predicate: skos:closeMatch`. The GOLD canonical path row is `gold.ecosystem:4387` and contributes no GOLD organism, study, or biosample assertions directly to this record.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/habitats/engineered/sheet_of_paper.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/sheet_of_paper.yaml` | Pass: scanned 1 file with 0 files containing `ERROR` and 0 total `ERROR` rows. |
| `just validate-causal curation/causal_graphs/<overlay>.yaml` | Not applicable: a gitignore-independent `rg --no-ignore --hidden` search for `ENVO:03501256`, `sheet_of_paper`, `sheet of paper`, `GOLD.b2d1395135`, `gold.ecosystem:4387`, and `Engineered > Paper` under `curation/causal_graphs` and `curation/term_requests.tsv` found no curated causal overlay for this record, and the target has no `causal_graphs` block. |
| `just validate-causal-all` | Pass: validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Pass: the term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: expected 3,206 records, found 3,206 on disk, and the corpus reproduces exactly from `data/raw/`. |
| `just worklist --limit 2000` | Pass: reported 0 undecided ungrounded records and 1,810 decisions on file. The first sandboxed run failed because `uv` could not open `/Users/marcin/.cache/uv/sdists-v9/.git`; the same command passed outside the sandbox. |
| `just report` | Pass: completed the corpus report for 3,206 records. The first sandboxed run failed because `uv` could not open `/Users/marcin/.cache/uv/sdists-v9/.git`; the same command passed outside the sandbox. |
| `git diff --check` | Pass before writing this report. |
| Reference validator | Not applicable: this record has no record-level `evidence`, `characteristic_taxa`, `causal_graphs`, `discussions`, or `datasets` with citation-bearing `EvidenceItem` references. |

## Identity and Grounding

`ENVO:03501256` is present in `data/raw/ontology_terms.tsv` as `sheet of paper` with definition `A paper product which is a thin sheet material made of processed cellulose fibres.` and exact synonyms `paper` and `paper sheet`. The ontology-derived parent `ENVO:00003895` is supported by `data/raw/ontology_subclass_edges.tsv`, which asserts `ENVO:03501256 rdfs:subClassOf ENVO:00003895`; `data/raw/ontology_terms.tsv` labels that parent `paper product` and defines it as a product composed primarily of paper.

The GOLD source row `Engineered > Paper` maps to `gold.ecosystem:4387` with no direct organism, study, or biosample assertions. The item-level row for `habitatmech:GOLD.b2d1395135` in `curation/decisions.tsv` explicitly endorses the seeder's `CLOSE` result rather than promoting the source concept to exact identity, so the generated `grounding_status: CLOSE`, `mapping_predicate: skos:closeMatch`, and `mapping_status: REVIEWED` agree with the maintained curation input.

The repeated GOLD leaf `Paper` under `Engineered > Industrial production > Engineered product > Material > Paper` remains a separate minted `NARROW` record, `data/habitats/engineered/paper.yaml`. That agrees with the anti-conflation rule: only the shallowest unmatched GOLD leaf claims the ontology term, while the deeper source path keeps a minted identifier and treats `sheet of paper` as a broader parent.

The source-derived parent `habitatmech:GOLD.2acb39dd08`, however, is GOLD's top-level `Engineered` source grouping. Its own only decision row is a class-level `CONFIRM_UNGROUNDED` whose note says whether the concept is a habitat at all was not assessed. That row does not support making `Engineered` a strictly broader habitat class for `sheet of paper`; see the major finding below.

## Evidence

The ENVO label, definition, synonyms, and `paper product` parent trace to committed ontology inventories: `data/raw/ontology_terms.tsv` and `data/raw/ontology_subclass_edges.tsv`.

The GOLD attestation traces to `data/raw/gold_ecosystem_paths.tsv`, where `Engineered > Paper` has `gold_node_count=1`, all assertion counts are 0, and `gold_node_ids=gold.ecosystem:4387`. The generated source attestation agrees with that row by carrying `source: GOLD`, `source_id: gold.ecosystem:4387`, `source_label: Paper`, and `source_path: Engineered > Paper`, with no `assertion_count`.

The item-level curation event traces to `curation/decisions.tsv`: source concept `habitatmech:GOLD.b2d1395135` has decision `REVIEW`, curator `claude-opus-5`, date `2026-08-16`, `review_depth: ITEM`, and notes naming the same close-grounding review shown in the generated record's curation history.

No record-level `evidence` block is required for this seeded record. It contains only the source-vocabulary provenance above and no curator-authored literature-backed mechanism, characteristic-taxon, discussion, or dataset claims.

## Completeness

- A gitignore-independent exact search for `ENVO:03501256`, `sheet_of_paper`, `sheet of paper`, `gold.ecosystem:4387`, `Engineered > Paper`, `GOLD.b2d1395135`, and `GOLD.2acb39dd08` across `data/raw`, `curation`, and `data/habitats/PATHS.tsv` found the expected path-lockfile row, the GOLD source row, the `REVIEW` decision, the ENVO ontology rows, and the GOLD parent decision.
- A gitignore-independent exact search for `ENVO:03501256`, `sheet_of_paper`, `sheet of paper`, `GOLD.b2d1395135`, `gold.ecosystem:4387`, and `Engineered > Paper` across `curation/causal_graphs` and `curation/term_requests.tsv` found no term request or curated causal overlay for this record.
- A gitignore-independent exact search for `Paper` and `paper` across `data/raw`, `curation`, `data/habitats/engineered/paper.yaml`, and `data/habitats/engineered/sheet_of_paper.yaml` found the sibling GOLD `Paper` path under `Engineered > Industrial production > Engineered product > Material`, the `Currency notes` child of the target path, ENVO's `paper product` descendants, and the target ENVO synonym.
- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-sheet_of_paper.md' -print` found no prior top-level review report for the same record stem; `find` includes ignored files.
- Optional `environmental_parameters`, `characteristic_taxa`, `evidence`, `causal_graphs`, `discussions`, and `datasets` are correctly absent. No maintained overlay adds mechanism, taxon, discussion, or dataset claims to this record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | `parent_habitats` contains `habitatmech:GOLD.2acb39dd08`, the GOLD `Engineered` root, but `parent_habitats` must contain strictly broader habitats. | `sheet of paper` already has its true ontology parent, `ENVO:00003895` `paper product`. The additional minted parent comes only from GOLD filing the source path under `Engineered`; the `Engineered` record is a top-level GOLD grouping with source path `Engineered` and only a class-level decision that explicitly did not assess whether the concept is a habitat. | The fix needs a curator-maintained way to suppress or replace false source-path parents for ontology-grounded GOLD records. The current generated edge comes from `src/habitatmech/seed.py` applying GOLD parent paths; do not hand-edit `data/habitats/engineered/sheet_of_paper.yaml`. |

No blocker findings found.

No minor findings found.

## Recommended Edits

1. Add a maintained parent-suppression or parent-override input for ontology-grounded GOLD records and use it to drop `habitatmech:GOLD.2acb39dd08` from `ENVO:03501256`. The row should name source concept `habitatmech:GOLD.b2d1395135`, rejected parent `habitatmech:GOLD.2acb39dd08`, and the fact that `Engineered` is a GOLD source grouping rather than a strictly broader habitat for a sheet of paper.
2. Update `src/habitatmech/seed.py` to apply that curated override before serializing `parent_habitats`.
3. Add a regression test that preserves `ENVO:00003895` on `sheet of paper` while rejecting only the source-derived `Engineered` parent.

## Follow-up Checks

| Edit | Proof |
| --- | --- |
| Drop only the false `Engineered` parent | `just seed-canary ENVO:03501256`, followed by an inspection of `data/habitats/engineered/sheet_of_paper.yaml` showing `ENVO:00003895` in `parent_habitats` and no `habitatmech:GOLD.2acb39dd08`. |
| Preserve corpus reproduction | `just seed-apply --force` and `just verify-corpus --max-diffs 1`. |
| Validate the focused record and new curated override | `just validate data/habitats/engineered/sheet_of_paper.yaml`, `just validate-strict data/habitats/engineered/sheet_of_paper.yaml`, and the new parent-override validator or regression test added with the maintained input. |
| Confirm no unrelated curation loss | `just term-requests-check`, `just validate-history`, `just worklist --limit 2000`, and `git diff --check`. |

## Additional Notes

- The GOLD `Engineered > Paper > Currency notes` child and the sibling `Engineered > Industrial production > Engineered product > Material > Paper` path do not change the target identity. `Currency notes` is a child path with its own `GROUND ENVO:00003896` decision, and the deeper `Paper` source path stays minted as a `NARROW` child of `sheet of paper`.
- The target GOLD row carries zero direct assertions, but that is a faithful representation of `data/raw/gold_ecosystem_paths.tsv`. The record is not incomplete merely because the generated `source_attestations` entry omits `assertion_count`.
