# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/bryophytes.yaml`
- Started UTC: 2026-09-22T15:15:54Z
- Finished UTC: 2026-09-22T15:15:54Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.35beb98884` |
| Label | `Bryophytes` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Maintained owner | `curation/decisions.tsv:392` records only a `CLASS`-depth ungrounded sweep for this GOLD concept |
| Locked slug | `data/habitats/PATHS.tsv:1691` maps `habitatmech:GOLD.35beb98884` to `bryophytes` |

This is the seeded GOLD record for `Host-associated > Plants > Bryophytes`. The generated YAML faithfully preserves the current maintained inputs: a class-level `CONFIRM_UNGROUNDED` decision, the plant-associated parent inherited from the item-reviewed `Host-associated > Plants` source path, and one zero-count GOLD source attestation. The record still needs item-level curation because the only decision row explicitly says the sweep did not assess whether the concept is a habitat, and the published label remains the host clade rather than an authored `bryophyte-associated environment` term.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/bryophytes.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/bryophytes.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The generated identifier, label, category, grounding status, parent, source attestation, and seed status are supported by maintained inputs:

- `data/raw/gold_ecosystem_paths.tsv:2497` is the raw GOLD row for `Host-associated > Plants > Bryophytes`. It has leaf label `Bryophytes`, depth 3, three GOLD node ids, zero organism assertions, zero study assertions, zero biosample assertions, and total assertion count 0.
- `curation/decisions.tsv:392` is a `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.35beb98884`, but its `review_depth` is `CLASS` and its note says whether the concept is a habitat was not assessed.
- `data/habitats/PATHS.tsv:1691` pins the generated target to `data/habitats/host_associated/bryophytes.yaml`.
- `data/raw/gold_ecosystem_paths.tsv:5` is the raw `Host-associated > Plants` parent path, and `curation/decisions.tsv:251` item-grounds that parent exactly to `ENVO:01001001` `plant-associated environment`.
- `data/raw/ontology_terms.tsv:8496` provides `ENVO:01001001` `plant-associated environment`, and `data/raw/ontology_subclass_edges.tsv:6796` places it under `ENVO:01001000` `environmental system determined by an organism`.
- `data/raw/gold_ecosystem_paths.tsv:2498` through `:2515` are all GOLD descendants of the Bryophytes branch. Their raw assertion counts are also all zero, so this source concept is currently a taxonomy placeholder, not the ancestor of attested GOLD bryophyte samples.

The host-associated category and ENVO parent are therefore mechanically reproducible. The identity itself is still not reviewed: a host organism clade can scope a microbial habitat, but the clade term is not the habitat identity and needs either an authored associated-environment term or an explicit exclusion.

## Evidence

Every generated claim is traceable to a raw row or maintained curation input:

| Claim | Nearest source | Review |
|---|---|---|
| Minted identifier `habitatmech:GOLD.35beb98884` | GOLD path minting from `Host-associated > Plants > Bryophytes` in `src/habitatmech/seed.py` | Supported exactly |
| Label `Bryophytes`, category `HOST_ASSOCIATED`, source label, source path, and three-node note | `data/raw/gold_ecosystem_paths.tsv:2497` | Supported exactly |
| `source_id: gold.ecosystem:8107` | first node id in `data/raw/gold_ecosystem_paths.tsv:2497` | Supported exactly; `gold.ecosystem:8107\|gold.ecosystem:8108\|gold.ecosystem:8109` share the path |
| Absence of `assertion_count` and `assertion_unit` | `organism_count` is `0` in `data/raw/gold_ecosystem_paths.tsv:2497`; `src/habitatmech/seed.py` emits GOLD assertion counts only when the organism count is nonzero | Supported exactly |
| `grounding_status: UNGROUNDED` | `curation/decisions.tsv:392` | Supported exactly |
| `mapping_status: SEEDED` | `curation/decisions.tsv:392` has `review_depth` `CLASS`; only `ITEM` decisions count as reviewed | Supported exactly |
| Parent `ENVO:01001001` | GOLD parent-path emission plus `curation/decisions.tsv:251` for `Host-associated > Plants` | Supported exactly |
| Absence of a definition, synonym, xref, environmental parameter, characteristic taxon, causal graph, record-level evidence, discussion, and dataset | No maintained input row or overlay found for these slots | Supported by bounded exact searches |

There is no evidence or causal graph on the record, so there is no claim-level citation to challenge. The source attestation does not claim any organism, study, biosample, or taxon volume for this branch, which is the right projection of the raw zero-count GOLD row.

## Completeness

The generated fields are complete for the maintained inputs that currently feed this record:

- The single GOLD source attestation captures the path, the first GOLD node id, and the multi-node note.
- The source-inherited `plant-associated environment` parent is present.
- No item-level decision, authored term request, target-specific causal graph overlay, append-only history record, deep-research report, GOLD side-table row, term-request exclusion row, or previous exact YAML review report exists for `habitatmech:GOLD.35beb98884`.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated target, excluding generated `build`, `data/text_map`, and `pages` trees. They found the raw GOLD Bryophytes row and zero-count descendants, the class-level decision row, the reviewed `Host-associated > Plants` parent row, the path lock, the generated YAML, and related reports about aquatic macrophytes, algae, peat moss, and red-algal embryos; they found no Bryophytes-specific maintained term request, term-request exclusion, causal overlay, history record, research report, GOLD biosample side-table row, GOLD MIxS triad row, GOLD study row, BacDive or PREGO row, or prior exact YAML review report.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Major | `habitatmech:GOLD.35beb98884` remains a `SEEDED` host-associated GOLD record because its only decision row is a class-level ungrounded sweep that did not assess habitat validity. The record therefore publishes the bare host clade label `Bryophytes` with no definition even though a whole host-taxon term is not a microbial habitat identity. The raw GOLD row and all GOLD descendants under `Host-associated > Plants > Bryophytes` also have zero assertions, so item review needs to decide whether this is a useful empty host branch to retain and define or an unused GOLD placeholder to exclude. | Add an `ITEM`-depth row in `curation/decisions.tsv`; if retained, add the associated-environment definition in `curation/term_requests.tsv`, then regenerate `bryophytes`. |

## Recommended Edits

- Add an item-level `curation/decisions.tsv` row for `habitatmech:GOLD.35beb98884` that explicitly decides the `Host-associated > Plants > Bryophytes` source path rather than inheriting the class-level ungrounded sweep.
- If the branch should publish, keep it minted and define `bryophyte-associated environment` in `curation/term_requests.tsv` with parent `ENVO:01001001` and synonym `Bryophytes`; keep any taxon term only as a related xref.
- If the zero-count GOLD branch should not publish, record the reason in the maintained curation inputs instead of relying on the current class-level placeholder state.
- Regenerate the target with `just seed` and preview `habitatmech:GOLD.35beb98884` with `just seed-canary habitatmech:GOLD.35beb98884` before applying the full generated corpus.

## Follow-up Checks

After item-level Bryophytes curation, re-run the same focused validation set and confirm either that `bryophytes.yaml` is defined and `REVIEWED`, or that the source path has been deliberately removed or redirected. If `bryophytes.yaml` is retired, validate the absorbing generated record in place of the deleted target:

- `just validate data/habitats/host_associated/bryophytes.yaml`
- `just validate-strict data/habitats/host_associated/bryophytes.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -name '*bryophytes*' -print` and `find reports/yaml_record_review -maxdepth 1 -name '*bryophyte*' -print` found no pre-existing exact review report for this record before this file was written.
- Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.35beb98884`, `gold.ecosystem:8107`, and `Host-associated > Plants > Bryophytes`, plus a case-insensitive hidden-inclusive search for `bryophy`, found no target-specific maintained artifact beyond the raw, curation, path-lock, and generated rows cited above.
