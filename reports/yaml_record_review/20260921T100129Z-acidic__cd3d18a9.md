# YAML Record Review: Acidic

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/acidic__cd3d18a9.yaml
- Started UTC: 2026-09-21T10:01:29Z
- Finished UTC: 2026-09-21T10:01:37Z
- Verdict: needs curation

## Target

The target is the generated `HabitatRecord` at `data/habitats/aquatic/acidic__cd3d18a9.yaml`.

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.ebbc8b20a7` |
| Label | `Acidic` |
| Category | `AQUATIC` |
| Grounding status | `NOT_APPLICABLE` |
| Mapping status | `REVIEWED` |
| Source concept | `gold.ecosystem:3995` |
| Source path | `Environmental > Aquatic > Thermal springs > Hot (42-90C) > Acidic` |
| Locked stem | `acidic__cd3d18a9` in `data/habitats/PATHS.tsv` |

This review covers the single GOLD source concept represented by `habitatmech:GOLD.ebbc8b20a7`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/acidic__cd3d18a9.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/acidic__cd3d18a9.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 records on disk, 0 missing, 0 extra, 0 differing. |
| Reference validator | Not checked: this record has no DOI/PMID/URL `EvidenceItem`, no causal-edge evidence, and no target-specific `curation/causal_graphs/` overlay. |

## Identity and Grounding

The reviewed `NOT_APPLICABLE` grounding is correct. The source leaf `Acidic` is a pH-band qualifier under `Environmental > Aquatic > Thermal springs > Hot (42-90C)`, not a microbial habitat class in its own right.

`curation/decisions.tsv` has the item-level decision for `habitatmech:GOLD.ebbc8b20a7`:

| Decision field | Value |
|---|---|
| `decision` | `NOT_APPLICABLE` |
| `review_depth` | `ITEM` |
| `note` | `A pH band qualifying the parent node, not a habitat in its own right. Belongs in environmental_parameters. Path: Environmental > Aquatic > Thermal springs > Hot (42-90C) > Acidic` |

The generated `grounding_status`, `mapping_status`, and curation-history `NOT_APPLICABLE` event agree with that maintained row.

The BacDive source-inventory grounding for the bare label `Acidic` maps it to `PATO:0001429` with label `acidic`, and `data/raw/ontology_terms.tsv` vendors that PATO quality term. That independent source mapping reinforces the reviewed HabitatMech judgment: an acid pH condition is a quality of a habitat, not a habitat identity.

The identity problem is confined to hierarchy. The generated record lists `parent_habitats: habitatmech:GOLD.c085f6141a`, whose own YAML record is `Hot (42-90C)`, `grounding_status: NOT_APPLICABLE`, and `mapping_status: REVIEWED`. The maintained decision for that parent says it is a temperature band qualifying thermal springs, not a habitat.

## Evidence

The GOLD source inventory supports the source attestation exactly:

| Source file | Target evidence |
|---|---|
| `data/raw/gold_ecosystem_paths.tsv` | The exact source path has leaf `Acidic`, depth 5, `gold_node_count=1`, `organism_count=68`, `total_assertions=68`, and node `gold.ecosystem:3995`. |
| `data/raw/gold_path_biosamples.tsv` | The exact source path is listed with node `3995` and 350 biosamples. |
| `data/raw/gold_path_triads.tsv` | The exact source path has MIxS triad context for aquatic biome, hot spring, and sampled water. |
| `data/raw/gold_studies.tsv` | Multiple GOLD study rows include the exact `Environmental > Aquatic > Thermal springs > Hot (42-90C) > Acidic` path. |

Those source rows prove that GOLD used this path and that the generated `assertion_count: 68` matches the GOLD organism count. They do not override the item-level curation decision, because GOLD occurrence under a hot-spring branch is compatible with interpreting `Acidic` as an environmental pH qualifier.

Ignored and hidden files were included in exact searches across `curation`, `data`, `research`, and `reports`, excluding only the generated `data/text_map/` cache, for `habitatmech:GOLD.ebbc8b20a7`, `gold.ecosystem:3995`, the source path, and `acidic__cd3d18a9`. The searches found the maintained decision, generated path lock row, generated YAML, raw GOLD rows, and prior sibling review mentions. They did not find target-specific literature research or causal-graph curation.

The only `*hot*` causal overlay found was `curation/causal_graphs/hot_spring.yaml`, which targets `ENVO:00000051` and is not attached to this rejected GOLD pH-band source concept.

## Completeness

The record is intentionally sparse and is complete enough as a reviewed non-habitat source concept:

- No `definition` is needed because `Acidic` is not a HabitatMech habitat here.
- No term request is needed because this source concept should not be minted as a new habitat term.
- No `environmental_parameters` entry is needed; GOLD supplied a qualitative pH band, not a numeric pH measurement with a value or unit.
- No characteristic taxa, causal graph, discussion, or dataset entries are needed for a rejected pH qualifier.
- `source_attestations` correctly preserves the GOLD node ID, label, full source path, and organism assertion count needed to audit why the source concept exists.

The generated `parent_habitats` edge is the consequential gap. The edge converts GOLD source-path nesting into an is-a assertion even after both the child pH band and the parent temperature band were reviewed as environmental qualifiers rather than habitats.

## Findings

- **Major - `parent_habitats` keeps a generated edge to a reviewed non-habitat qualifier.** `data/habitats/aquatic/acidic__cd3d18a9.yaml` declares `habitatmech:GOLD.c085f6141a` as its parent, but that parent is the reviewed `NOT_APPLICABLE` `Hot (42-90C)` source concept. `docs/CURATION.md` says `parent_habitats` is an is-a claim that must not be used merely to avoid losing an upstream link. This generated edge makes a pH qualifier a kind of temperature-band qualifier. The maintained owner is the GOLD parent-path pass in `src/habitatmech/seed.py`, which currently adds the next GOLD path level to `parent_habitats` whenever it resolves to a different identifier.

## Recommended Edits

Update the maintained GOLD hierarchy logic in `src/habitatmech/seed.py`, or a maintained suppression table consumed by it, so reviewed environmental-parameter qualifiers do not get emitted as strict habitat parents or children.

At minimum, suppress the edge from `Environmental > Aquatic > Thermal springs > Hot (42-90C) > Acidic` to `Environmental > Aquatic > Thermal springs > Hot (42-90C)`.

The same rule should be evaluated against the hot thermal-spring pH siblings:

- `Environmental > Aquatic > Thermal springs > Hot (42-90C) > Alkaline`
- `Environmental > Aquatic > Thermal springs > Hot (42-90C) > Neutral`

When the source child is a real habitat, such as the hot thermal-spring `Microbial mats` or `Sediment` branch, do not retain `Hot (42-90C)` as a strict parent. Either suppress that qualifier parent or rewire through it to the nearest true habitat ancestor so valid hot-spring context is not lost.

Add a regression that fails if an item-reviewed `NOT_APPLICABLE` pH or temperature qualifier is emitted as a `parent_habitats` target.

No hand edit is recommended for `data/habitats/aquatic/acidic__cd3d18a9.yaml`.

## Follow-up Checks

| Edit | Narrowest proving check |
|---|---|
| Suppress or rewire the generated GOLD path edge. | Run `just seed-canary habitatmech:GOLD.ebbc8b20a7` and confirm `data/habitats/aquatic/acidic__cd3d18a9.yaml` no longer has `parent_habitats: habitatmech:GOLD.c085f6141a`. |
| Preserve source provenance for the rejected GOLD concept. | Re-read the canary and confirm the `source_attestations` entry still points to `gold.ecosystem:3995` and the exact hot acidic source path. |
| Avoid losing legitimate hot-spring ancestry for real children under the temperature band. | Canary `habitatmech:GOLD.086ba0a00d` and `habitatmech:GOLD.686adf1872`, then inspect the generated `Microbial mats` and `Sediment` records. |
| Prove the regenerated corpus remains stable. | Run `just validate data/habitats/aquatic/acidic__cd3d18a9.yaml`, `just validate-strict data/habitats/aquatic/acidic__cd3d18a9.yaml --quiet`, `just term-requests-check`, `just validate-history`, and `just verify-corpus --max-diffs 1`. |
| Prove downstream exports no longer carry the false edge. | Rebuild KGX and SSSOM products and confirm no exported edge asserts `habitatmech:GOLD.ebbc8b20a7` under `habitatmech:GOLD.c085f6141a`. |

## Additional Notes

This review intentionally leaves the generated YAML and curation history unchanged. The reviewed pH-quality disposition is already represented in the maintained decision row; the remaining defect is the generated hierarchy edge that ignores the reviewed non-habitat status of the source path levels.
