# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/acne_vulgaris.yaml`
- Started UTC: 2026-09-23T04:29:00Z
- Finished UTC: 2026-09-23T04:33:10Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/host_associated/acne_vulgaris.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.70fd1acd87` |
| Label | `Acne vulgaris` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NOT_APPLICABLE` |
| Mapping status | `REVIEWED` |
| Record status | Generated from one GOLD source concept plus an item-level `curation/decisions.tsv` review |

This is the generated record for the GOLD path `Host-associated > Mammals: Human > Integumentary system > Skin > Acne vulgaris`. The source concept is keyed by `habitatmech:GOLD.70fd1acd87`, computed as `sha1("GOLD:Host-associated > Mammals: Human > Integumentary system > Skin > Acne vulgaris")[:10]`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/acne_vulgaris.yaml` | Passed; LinkML validation reported no issues. |
| `just validate-strict data/habitats/host_associated/acne_vulgaris.yaml` | Passed; 1 file scanned with 0 error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 generated records, found 3206, with 0 missing, 0 extra, and 0 differing. |
| `just worklist --limit 2000` | Passed; 0 ungrounded records are still undecided and 1810 decisions are on file. |
| `just report` | Passed; the report completed and did not flag `habitatmech:GOLD.70fd1acd87` in the risky-grounding, stale-sweep, non-habitat-claim, GOLD-triad, unsupported-prefix, or organism-identity sections. |
| `git diff --check` | Passed; no whitespace errors. |

## Identity and Grounding

The GOLD source identity is supported:

| Raw field | Value |
|---|---|
| `canonical_path` | `Host-associated > Mammals: Human > Integumentary system > Skin > Acne vulgaris` |
| `ecosystem` | `Host-associated` |
| `ecosystem_category` | `Mammals: Human` |
| `ecosystem_type` | `Integumentary system` |
| `ecosystem_subtype` | `Skin` |
| `specific_ecosystem` | `Acne vulgaris` |
| `leaf_label` | `Acne vulgaris` |
| `depth` | `5` |
| `gold_node_count` | `1` |
| `organism_count` | `1` |
| `study_count` | `0` |
| `biosample_count` | `0` |
| `total_assertions` | `1` |
| `gold_node_ids` | `gold.ecosystem:7011` |

`data/raw/gold_ecosystem_paths.tsv` records exactly one source row for this path, and `source_attestations` preserves the GOLD node id, leaf label, full source path, `assertion_count: 1`, and `assertion_unit: ORGANISM`. The record correctly omits a multi-node note because `gold_node_count` is 1.

The item-level decision row in `curation/decisions.tsv` marks `habitatmech:GOLD.70fd1acd87` as `NOT_APPLICABLE`, with no object term, no relation, and `review_depth: ITEM`. That matches the decision note: the GOLD leaf names an acne disease state rather than skin, hair follicle, sebum, or another habitat that a microbe inhabits.

`grounding_status: NOT_APPLICABLE` and `mapping_status: REVIEWED` are mechanically expected from the item-level decision. The remaining problem is the generated hierarchy: the reviewed non-habitat record still inherits `habitatmech:GOLD.4b37ab4b76` from GOLD's immediate `Skin` parent path even though acne vulgaris is a disease of skin structures, not a strict subtype of skin.

## Evidence

| Claim | Nearest support | Review |
|---|---|---|
| The generated record represents the GOLD path `Host-associated > Mammals: Human > Integumentary system > Skin > Acne vulgaris`. | `data/raw/gold_ecosystem_paths.tsv`; `data/habitats/PATHS.tsv` | Supported exactly. The path hashes to `habitatmech:GOLD.70fd1acd87`, and `PATHS.tsv` pins that identifier to `acne_vulgaris`. |
| The GOLD source row contributes 1 organism assertion from one GOLD ecosystem node. | `data/raw/gold_ecosystem_paths.tsv` | Supported exactly. |
| `Acne vulgaris` is intentionally reviewed as non-applicable. | `curation/decisions.tsv` | Supported exactly. The item-level decision says the source concept is a disease state, not the skin, follicle, or sebum that would be inhabited. |
| `habitatmech:GOLD.4b37ab4b76` is the mechanical parent path for the immediate GOLD `Skin` node. | `data/raw/gold_ecosystem_paths.tsv`; `data/habitats/PATHS.tsv`; `data/habitats/host_associated/skin__621b7871.yaml` | Supported as source-path provenance but not as a HabitatMech `parent_habitats` is-a edge. Acne vulgaris is not a subtype of skin. |

Unsupported or over-scoped claims: the inherited `Skin` parent is too broad for a disease record. The record makes no definition, environmental-parameter, characteristic-taxon, causal-mechanism, record-level evidence, or dataset claim.

## Completeness

The direct GOLD source attestation is complete for the single-node, one-organism raw row. It carries the source name, source id, source label, source path, assertion count, and assertion unit, and correctly omits score and evidence-channel fields that GOLD does not supply.

No term request is expected: the item-level review decided that the source concept is not a habitat at all. Exact ignored/hidden-inclusive searches for `habitatmech:GOLD.70fd1acd87`, `gold.ecosystem:7011`, `Acne vulgaris`, and `acne_vulgaris` found the maintained decision, the raw GOLD row, the locked `PATHS.tsv` row, and the generated YAML. The same target-specific search found no `curation/term_requests.tsv`, `curation/term_requests_excluded.tsv`, `curation/causal_graphs`, `history`, or `research/habitats` artifact for the Acne vulgaris source concept; generated `data/text_map`, `pages`, and `build` outputs were excluded only from broad exact content searches.

No prior exact YAML review report was found before this file was created: `find reports/yaml_record_review -maxdepth 1 -type f -name '*-acne_vulgaris.md' -print` returned no rows, and `find` includes ignored files under the searched directory.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| HM-ACNE-VULGARIS-001 | major | `parent_habitats` still contains `habitatmech:GOLD.4b37ab4b76` even though the item-level decision confirms Acne vulgaris is a disease state, not a subtype of Skin. `parent_habitats` denotes strictly broader habitats, so the GOLD source-path parent is being promoted into a false habitat hierarchy edge. | There is no existing item-level curation input that can retract only this inherited source-path parent. The seeder's GOLD parent emission in `src/habitatmech/seed.py`, or a new curation table it reads, needs to own this suppression before reseeding. |

No blocker findings.

No minor findings.

## Recommended Edits

1. Add a maintained way to suppress false inherited GOLD source-path parents for item-level reviewed non-habitats, then apply it to `habitatmech:GOLD.70fd1acd87` so a reseed removes `habitatmech:GOLD.4b37ab4b76` from `data/habitats/host_associated/acne_vulgaris.yaml`.
2. Keep the existing `curation/decisions.tsv` `NOT_APPLICABLE` row for `habitatmech:GOLD.70fd1acd87`; it correctly records that the source concept is a disease rather than a habitat.
3. Rerun `just seed-canary habitatmech:GOLD.70fd1acd87`, then inspect `data/habitats/host_associated/acne_vulgaris.yaml` to confirm that the GOLD source attestation and generated `NOT_APPLICABLE` status remain while `parent_habitats` is absent.

## Follow-up Checks

After a future parent-suppression fix, run:

- `just seed`
- `just seed-canary habitatmech:GOLD.70fd1acd87`
- `just validate data/habitats/host_associated/acne_vulgaris.yaml`
- `just validate-strict data/habitats/host_associated/acne_vulgaris.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just report`
- `git diff --check`

## Additional Notes

- The exact source-path search for `Host-associated > Mammals: Human > Integumentary system > Skin > Acne vulgaris` found only the expected generated record and `data/raw/gold_ecosystem_paths.tsv` row in maintained/raw inputs.
- The immediate parent `habitatmech:GOLD.4b37ab4b76` is itself generated from `Host-associated > Mammals: Human > Integumentary system > Skin`, aggregates two GOLD node ids, and contributes 754 organism assertions. That raw GOLD context explains where the parent came from; it does not make Acne vulgaris a kind of skin.
- Broad exact content searches used `rg --no-ignore --hidden` and excluded generated `data/text_map`, `pages`, and `build` outputs. Exact filename searches used `find` and therefore included ignored files in the searched directories.
