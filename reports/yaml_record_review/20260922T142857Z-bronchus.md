# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/bronchus.yaml`
- Started UTC: 2026-09-22T14:28:57Z
- Finished UTC: 2026-09-22T14:28:57Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `UBERON:0002185` |
| Label | `bronchus` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `CLOSE` |
| Mapping status | `REVIEWED` |
| Maintained owner | `curation/decisions.tsv:1544` item-reviews the BacDive source concept `habitatmech:BACDIVE.d37a7e5c01`; raw BacDive source rows feed the attestation and taxa |
| Locked slug | `data/habitats/PATHS.tsv:1067` maps `UBERON:0002185` to `bronchus` |

This is the reviewed BacDive-backed `UBERON:0002185` anatomy record for a `Bronchial-wash` isolation source. Its ontology identity, source attestation, strain count, and taxon associations are supported by the maintained BacDive mapping and the item-level `REVIEW` row. The generated record still needs one generator-side correction because the BacDive source label is emitted as an `EXACT_SYNONYM` of `bronchus` even though the source mapping is deliberately only `skos:closeMatch`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/bronchus.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/bronchus.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The generated anatomy identity and BacDive attestation are supported by maintained inputs:

- `data/raw/isolation_source_groundings.tsv:52` maps BacDive `Bronchial-wash` / `bronchial wash` to `UBERON:0002185` `bronchus` with predicate `skos:closeMatch`, confidence `medium`, mapping method `semapv:ManualMappingCuration`, provenance `manual_review`, source `bacdive`, and date `2026-05-02`.
- `curation/decisions.tsv:1544` records an item-level `REVIEW` decision for `habitatmech:BACDIVE.d37a7e5c01`; the note confirms the seeder's close mapping because a bronchial wash samples the bronchus and the bronchus is the habitat site.
- `data/raw/bacdive_isolation_sources.tsv:119` provides the BacDive source id `bacdive.isolation_source:bronchial-wash`, label `Bronchial-wash`, slug `bronchial-wash`, 18 strains, and 18 taxa.
- `data/raw/ontology_terms.tsv:13131` provides `UBERON:0002185` `bronchus`, its definition, and the UBERON exact synonyms `bronchi`, `bronchial tissue`, and `bronchial trunk`.
- `data/raw/ontology_subclass_edges.tsv:11345` makes `UBERON:0000117` the ontology parent of `UBERON:0002185`; `data/raw/ontology_terms.tsv:12908` labels that parent `respiratory tube`.
- `data/habitats/PATHS.tsv:1067` pins the ontology record to the generated slug `bronchus`.

The close-match grounding is appropriate: the source concept is a bronchial-wash sampling label, while the resolved identity is the bronchus anatomical site. The maintained review row intentionally endorses the close anatomical-site interpretation rather than asserting that the specimen label and ontology label are exact lexical equivalents.

## Evidence

Most generated claims are direct projections of BacDive, UBERON, or the item-level decision row:

| Claim | Nearest source | Review |
|---|---|---|
| Identifier `UBERON:0002185`, label `bronchus`, definition, and UBERON exact synonyms | `data/raw/ontology_terms.tsv:13131` | Supported exactly |
| `habitat_category: HOST_ASSOCIATED` | Prefix-category inference for UBERON anatomy in `src/habitatmech/seed.py` | Supported |
| `grounding_status: CLOSE` and BacDive `mapping_predicate: skos:closeMatch` | `data/raw/isolation_source_groundings.tsv:52` | Supported exactly |
| `mapping_status: REVIEWED` | `curation/decisions.tsv:1544` has an `ITEM`-depth `REVIEW` decision | Supported exactly |
| Parent `UBERON:0000117` | `data/raw/ontology_subclass_edges.tsv:11345` | Supported exactly |
| BacDive source id, label, 18-strain count, and `assertion_unit: STRAIN` | `data/raw/bacdive_isolation_sources.tsv:119` | Supported exactly |
| Eighteen BacDive taxa, ranks 1 through 18, single-strain association counts, and `candidate_pool: 18` | `data/raw/bacdive_source_taxa.tsv:396` through `:413`, `data/raw/bacdive_isolation_sources.tsv:119` | Supported exactly; these are BacDive strain associations and none is upgraded with `is_characteristic` |
| Synonym `Bronchial-wash` with `synonym_type: EXACT_SYNONYM` | `src/habitatmech/seed.py:948` unconditionally calls `concept.add_synonym(row["label"], "EXACT_SYNONYM", "BacDive")` for every BacDive source row | Unsupported as exact synonymy for this record; the maintained source mapping is only `skos:closeMatch` |

The generated taxon list accurately preserves BacDive strain observations for this source. Because each taxon has only one associated strain and no PREGO corroboration, the entries should continue to be read as observed associations, not as curator-backed characteristic bronchus taxa.

## Completeness

The record is complete for the BacDive source and UBERON ontology rows that currently feed it:

- The sole source attestation preserves the BacDive isolation-source id, label, close-match predicate, 18-strain assertion count, and strain assertion unit.
- All 18 `data/raw/bacdive_source_taxa.tsv` rows for `bacdive.isolation_source:bronchial-wash` appear in the generated record with their exact ranks, NCBITaxon IDs, labels, single-strain counts, `BACDIVE` source, and candidate pool.
- The ontology definition, UBERON exact synonyms, and `respiratory tube` parent all trace to the vendored ontology slice.
- The item-level `REVIEW` decision is sufficient to make this single-source record `REVIEWED`.
- No xref, environmental parameter, record-level evidence item, causal graph, discussion item, dataset, term request, or append-only history file is required by the current maintained inputs.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated target, excluding generated `build`, `data/text_map`, and `pages` trees. They found the ontology term and subclass rows, the BacDive isolation-source row, the BacDive taxa rows, the source-grounding row, the item-level decision row, the path lock, the generated YAML, and earlier `bronchi` reports that cite this record; they found no target-specific term request, causal overlay, append-only history record, or pre-existing exact YAML review report.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Major | The generated synonym list says `Bronchial-wash` is an `EXACT_SYNONYM` of `UBERON:0002185` `bronchus`, but the inspected BacDive grounding row and reviewed curation row both model `Bronchial-wash` as only a `skos:closeMatch`: it is a wash specimen/source term that samples the bronchus anatomical habitat. Exact synonymy therefore overstates the source label and contradicts the record's own close-match source attestation. | Change the BacDive synonym emission in `src/habitatmech/seed.py:948` so source labels from non-exact BacDive mappings are not emitted as `EXACT_SYNONYM`, then regenerate `UBERON:0002185`. |

## Recommended Edits

- Change `src/habitatmech/seed.py:948` to derive the BacDive synonym scope from the resolved mapping rather than always assigning `EXACT_SYNONYM`. At minimum, `skos:closeMatch` rows such as `Bronchial-wash` should be downgraded to `RELATED_SYNONYM` or omitted from the `synonyms` slot in favor of the existing source attestation.
- Regenerate the target with `just seed` and preview `UBERON:0002185` with `just seed-canary UBERON:0002185` before applying the full generated corpus.
- Add or update generator coverage proving that a close BacDive mapping preserves the source label in `source_attestations` without asserting exact synonymy against the ontology identity.

## Follow-up Checks

After correcting BacDive synonym emission, re-run the focused validators and confirm the regenerated `bronchus.yaml` still has its reviewed close-match attestation but no longer presents `Bronchial-wash` as an `EXACT_SYNONYM`:

- `just validate data/habitats/host_associated/bronchus.yaml`
- `just validate-strict data/habitats/host_associated/bronchus.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -name '*bronchus*' -print` found no pre-existing exact review report for this record before this file was written.
- Exact ignored/hidden-inclusive searches for `UBERON:0002185`, `UBERON:0000117`, `bacdive.isolation_source:bronchial-wash`, `habitatmech:BACDIVE.d37a7e5c01`, `Bronchial-wash`, and `bronchial-wash` found only the maintained source, ontology, decision, path-lock, generated, research-context, and prior-report rows cited above for this target.
