# YAML Record Review: Bar

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/bar.yaml`
- Started UTC: 2026-09-21T18:37:00Z
- Finished UTC: 2026-09-21T18:55:21Z
- Verdict: pass with minor issues

## Target

| Field | Value |
|---|---|
| Identifier | `ENVO:00000167` |
| Label | `bar` |
| Class | `HabitatRecord` |
| Category | `AQUATIC` |
| Generated or maintained | Generated from `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, `data/raw/prego_habitats.tsv`, `data/raw/prego_habitat_taxa.tsv`, and `data/habitats/PATHS.tsv`; do not hand-edit |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Source concepts | PREGO `ENVO:00000167` |
| Locked stem | `bar` in `data/habitats/PATHS.tsv` |

The record resolves the ENVO ontology class `ENVO:00000167` `bar`, which PREGO
also uses as its source habitat identifier. `data/habitats/PATHS.tsv` maps that
identifier to the stable corpus stem `bar`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/bar.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/bar.yaml --quiet` | Pass: 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Pass: 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Pass: committed term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: 3,206 expected records, 3,206 found, 0 missing, 0 extra, 0 differing. |
| Reference validator | Not available: `justfile` exposes no reference-specific validator, and this record has no `EvidenceItem`, `EnvironmentalParameter.reference`, `CharacteristicTaxon.reference`, or causal-edge evidence to target. |

## Identity and Grounding

| Claim | Evidence | Assessment |
|---|---|---|
| The record denotes ENVO's `bar` class. | `data/raw/ontology_terms.tsv` has `ENVO:00000167`, ontology `ENVO`, label `bar`, the ENVO definition for a linear shoaling landform feature within a body of water, and `directly_referenced=TRUE`. The generated YAML uses the same identifier, label, definition, and `definition_source: ENVO`. | Supported exactly. |
| PREGO's source concept is the same ontology class. | `data/raw/prego_habitats.tsv` has `prego_id=ENVO:00000167`, ontology `ENVO`, `biolink_category=biolink:OntologyClass`, `taxon_count=4`, `direct_assertion_count=4`, `max_prego_score=3`, and `channels=annotated_genomes_isolates`. | Supported exactly. |
| The generated `source_attestations` row preserves the PREGO association volume. | The single generated attestation carries `source: PREGO`, `source_id: ENVO:00000167`, `source_label: bar`, `assertion_count: 4`, `assertion_unit: TAXON`, `score: 3.0`, and `evidence_channels: annotated_genomes_isolates`, matching the PREGO habitat row. | Supported exactly. |
| `EXACT` grounding is mechanically and semantically appropriate. | The HabitatMech identifier and the PREGO source identifier are both `ENVO:00000167`; this is not a lexical GOLD/BacDive guess or a broader-parent mapping. | Supported exactly. |
| The `AQUATIC` category and `ENVO:00000012` parent fit the ENVO hierarchy. | `data/raw/ontology_subclass_edges.tsv` places `ENVO:00000167` under `ENVO:00000012`; `data/raw/ontology_terms.tsv` labels that parent `hydrographic feature` and defines it as a geographical feature associated with water. | Supported exactly. |

## Evidence

The record has no curator-authored `evidence`, `environmental_parameters`,
`causal_graphs`, `discussions`, or `datasets`.

| Claim | Evidence | Assessment |
|---|---|---|
| The four generated taxon rows are the complete PREGO direct taxon set for this habitat. | `data/raw/prego_habitat_taxa.tsv` has exactly four `ENVO:00000167` rows: `NCBITaxon:1156985` `Epibacterium ulvae`, `NCBITaxon:118166` `Nodosilinea nodulosa PCC 7104`, `NCBITaxon:317936` `Nostoc sp. PCC 7107`, and `NCBITaxon:710685` `Mycolicibacterium rhodesiae NBB3`. Their PREGO scores are all `3` and their ranks are `1` through `4` out of the habitat row's `taxon_count=4`. | Supported exactly. |
| The taxa are observational associations, not curated characteristic exemplars. | `src/habitatmech/schema/habitatmech.yaml` documents seeded `characteristic_taxa` as observational, and none of the four PREGO rows has `is_characteristic: true` or a literature `reference`. | Supported exactly. |
| The generated ENVO and PREGO synonyms are source-owned. | The ENVO exact synonyms in the YAML match the vendored `data/raw/ontology_terms.tsv` synonym column; the PREGO related synonyms match `data/raw/prego_habitats.tsv` `prego_synonyms` values, excluding the preferred `bar` label. | Supported exactly, with the synonym-scope caution called out below. |

## Completeness

The record is complete enough as a generated PREGO/ENVO seed. It preserves the
ontology identity, ENVO definition, ENVO exact synonyms, the PREGO attestation,
all four PREGO taxon associations, and the generated seed curation event.

Empty optional fields are correct for the current maintained inputs:

- no `curation/causal_graphs/bar.yaml` overlay exists;
- no `curation/decisions.tsv`, `curation/term_requests.tsv`,
  `curation/term_requests_excluded.tsv`, or `curation/external_xrefs.tsv` row is
  keyed by `ENVO:00000167`;
- no `history/` curation-session record names `ENVO:00000167`; and
- no target-specific report exists under `research/habitats`.

Hidden- and ignored-file-inclusive searches covered `curation`, `history`,
`research/habitats`, `reports/yaml_record_review`, `data/raw`,
`data/habitats/PATHS.tsv`, and `data/habitats` for `ENVO:00000167`; `find`
lookups covered the `bar` slug under `research/habitats`,
`reports/yaml_record_review`, and `history`. These checks found the expected
raw ontology, raw PREGO, raw subclass, generated `PATHS.tsv`, and generated
YAML entries, and found no Bar-owned curation decision, term request, causal
overlay, history record, research report, or previous `*-bar.md` YAML review.

The vendored ontology also carries the two expected local children,
`ENVO:00000306` `spit` and `ENVO:00000420` `tombolo`, as subclasses of
`ENVO:00000167`. Their definitions both describe kinds of bars or bar-like
depositional landforms, so the parent edge is not contradicted by the immediate
child context in the vendored slice.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| `HM-BAR-001` | Minor | Some generated synonyms for `bar` are source-owned strings whose exactness is not locally re-reviewed. ENVO contributes broad or ambiguous `EXACT_SYNONYM` values such as `seafloor feature`, `barrier island`, and `point`, while PREGO contributes mechanical related variants such as `baric`, `shoal patcheses`, and `shoalsic`. The generated record is faithful to the two raw rows, but consumers should not treat those exact/related scopes as a HabitatMech-curated synonym audit. | `data/raw/ontology_terms.tsv` lists the ENVO exact synonym payload for `ENVO:00000167`; `data/raw/prego_habitats.tsv` lists the PREGO `prego_synonyms`; `data/habitats/aquatic/bar.yaml` copies them into `synonyms` with `source: ENVO` or `source: PREGO`. | Upstream ENVO/PREGO extraction into `data/raw/ontology_terms.tsv` and `data/raw/prego_habitats.tsv`; no per-record HabitatMech curation input currently owns synonym suppression. |

No blocker or major findings found.

## Recommended Edits

1. Leave `data/habitats/aquatic/bar.yaml` generated and do not add a
   `curation/decisions.tsv` row merely to restate that PREGO `ENVO:00000167`
   is exactly ENVO `bar`.
2. If broad or mechanically inflected synonyms create false-positive lookup
   behavior, add a maintained synonym-suppression mechanism for source-owned
   ontology/PREGO synonyms or fix the upstream vocabulary rows, then refresh the
   raw inventories and regenerate with `just seed-canary ENVO:00000167 --force`.

## Follow-up Checks

- `just seed`
- `just seed-canary ENVO:00000167 --force`
- `just validate data/habitats/aquatic/bar.yaml`
- `just validate-strict data/habitats/aquatic/bar.yaml --quiet`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

After any synonym-filtering change, re-read `data/habitats/aquatic/bar.yaml` and
confirm the definition, PREGO attestation, parent `ENVO:00000012`, and four
PREGO taxa are unchanged unless the raw PREGO or ENVO source rows changed.

## Additional Notes

This was a read-only YAML review. It did not edit generated habitat YAML, append
curation history, promote the review status, create an issue, or run paid
definition research.

The exact label `bar` is too short for a useful global absence query: a
hidden/ignored-inclusive literal search returns unrelated terms such as `bare`,
`bark`, and `cattle_barn`. Absence claims above therefore rely on exact
identifier searches plus slug-shaped `find` lookups under bounded owner
directories.
