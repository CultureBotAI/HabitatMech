# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/breviatea.yaml`
- Started UTC: 2026-09-22T12:07:52Z
- Finished UTC: 2026-09-22T12:07:52Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.8a09449b58` |
| Label | `Breviatea` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv` plus the class-level `curation/decisions.tsv` row |
| Locked slug | `data/habitats/PATHS.tsv:2318` maps `habitatmech:GOLD.8a09449b58` to `breviatea` |

This is the GOLD path record for `Host-associated > Protists > Breviatea`. The generated YAML faithfully retains it as an ungrounded, minted HabitatMech source concept because the maintained decision is still only a class-level lexical sweep row. The leaf now needs item-level curation: the parent `Host-associated > Protists` concept and nearby Oomycetes, Dinoflagellates, and Amoebozoa children have already been reviewed as protist host-clade habitats or duplicates of equivalent host-clade habitats, while Breviatea is still in the legacy class-sweep bucket.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/breviatea.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/breviatea.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The generated identifier, label, host-associated category, GOLD path parent, path lock, zero-assertion source attestation, and class-level grounding decision are supported by maintained inputs:

- `data/raw/gold_ecosystem_paths.tsv:2534` is the raw GOLD row for this record. Its canonical path is `Host-associated > Protists > Breviatea`; its leaf label is `Breviatea`; it is a depth-3 path with 3 GOLD node ids, `gold.ecosystem:4596|gold.ecosystem:4597|gold.ecosystem:4598`, and zero organism, study, biosample, and total assertions.
- `curation/decisions.tsv:813` addresses the same minted source concept, `habitatmech:GOLD.8a09449b58`, and records a `CLASS`-depth `CONFIRM_UNGROUNDED` decision by `claude-opus-5` on 2026-08-12.
- `data/habitats/PATHS.tsv:2318` pins `habitatmech:GOLD.8a09449b58` to slug `breviatea`, matching the reviewed YAML path.
- The parent path `Host-associated > Protists` mints to `habitatmech:GOLD.eee7408afd`; `data/habitats/PATHS.tsv:3059` pins that parent to `protists`.
- `data/raw/gold_ecosystem_paths.tsv:322` is the parent Protists path that feeds `habitatmech:GOLD.eee7408afd` and carries GOLD node ids `gold.ecosystem:3384|gold.ecosystem:3603|gold.ecosystem:3719|gold.ecosystem:4738`.
- `curation/decisions.tsv:1315` independently reviews the immediate GOLD `Protists` parent as `CONFIRM_UNGROUNDED`, with `NCIT:C77914` retained only as a taxon xref rather than as the habitat identity.
- `curation/term_requests.tsv:78` defines that parent as `protist-associated environment` under `ENVO:01001000`.

`mapping_status: SEEDED` is mechanically correct because the only maintained target decision has `review_depth: CLASS`; by design, class-level rows do not promote generated records to `REVIEWED`. `grounding_status: UNGROUNDED` is also faithful to the current `CONFIRM_UNGROUNDED` decision: that sweep established that no vendored ontology term matched the label by the documented lexical routes, not that the Breviatea host-clade leaf had been item-reviewed as a term-request candidate.

## Evidence

Every generated claim in this record is source-derived or maintained:

| Claim | Nearest source | Review |
|---|---|---|
| Minted record identifier `habitatmech:GOLD.8a09449b58` | `src/habitatmech/seed.py`, `data/raw/gold_ecosystem_paths.tsv:2534` | Supported exactly by `mint("GOLD", canonical_path)` |
| Label `Breviatea`, host-associated category, GOLD source id, and source path | `data/raw/gold_ecosystem_paths.tsv:2534` | Supported exactly |
| Omitted `assertion_count` and `assertion_unit` | `data/raw/gold_ecosystem_paths.tsv:2534`, `src/habitatmech/seed.py` | Supported; GOLD attestation counts are emitted from nonzero `organism_count`, and this exact path has `organism_count=0` |
| `UNGROUNDED` status and the first curation-history event | `curation/decisions.tsv:813` | Supported exactly |
| `SEEDED` mapping status | `curation/decisions.tsv:813`, `docs/HARMONIZATION.md` | Supported; the decision is class-depth, not item-depth |
| Parent `habitatmech:GOLD.eee7408afd` | GOLD parent-path emission in `src/habitatmech/seed.py`, backed by `data/raw/gold_ecosystem_paths.tsv:322` | Supported exactly |
| Locked filename | `data/habitats/PATHS.tsv:2318` | Supported exactly |

The record has no xrefs, definition, synonyms, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussion links, assertion count, assertion unit, or datasets. Those absences are consistent with the class-level decision and zero-organism raw GOLD row. Exact side-table lookups found `data/raw/gold_path_biosamples.tsv:1037`, which records one biosample for ecosystem path `4598`, and `data/raw/gold_studies.tsv:1714`, which records the single-path GOLD study `Gs0132936` for the target path; they found no exact target row in `data/raw/gold_path_triads.tsv`.

## Completeness

The generated slots are faithful to the maintained inputs that feed this record, but one maintained input is consequentially underfilled:

- The sole `source_attestations` entry captures the complete target GOLD path and the first of three upstream GOLD node ids.
- No `assertion_count` is emitted because `data/raw/gold_ecosystem_paths.tsv:2534` records 0 organism assertions for the canonical GOLD path; supplemental biosample and study rows do not currently materialize into generated record fields.
- The parent list preserves the immediate reviewed GOLD path parent, `protist-associated environment`, so this sparse leaf remains browsable in its source hierarchy.
- `mapping_status` stays `SEEDED` because the class-level row is a reproducible lexical miss, not an item-level habitat judgement; this is correct generated output, but not complete curation for a protist host-clade GOLD leaf.
- The nearby `Dinoflagellates`, `Oomycetes`, and `Amoebozoa` host-clade children have already received item-level decisions, at `curation/decisions.tsv:858`, `:939`, and `:206`.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated target, excluding generated `build`, `data/text_map`, and `pages` trees. They found the maintained target decision, raw GOLD row, biosample side-table row, study side-table row, path lock, generated YAML, Protists parent record, sibling item-level decisions, and contextual committed research that treats GOLD's `Host-associated > Protists` branch as protists acting as hosts; they found no target-specific term request, causal overlay, history record, GOLD triad row, or prior exact YAML review report. An ignored/hidden-inclusive exact search of `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, and `curation/external_xrefs.tsv` found no exact `Breviatea` or `breviate` xref already available in the maintained ontology slice.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Major | `Breviatea` remains under a `CLASS`-depth `CONFIRM_UNGROUNDED` row even though the GOLD path is a direct child of the item-reviewed `protist-associated environment` parent and the reviewed Host-associated > Protists pattern treats microbial eukaryote clades as host contexts rather than as `NOT_APPLICABLE` taxon labels. The generated `UNGROUNDED` / `SEEDED` state is faithful to the table, but it leaves a source concept that should be a term-request candidate in the lexical-miss bucket. | Replace `curation/decisions.tsv:813` for `habitatmech:GOLD.8a09449b58` with an `ITEM`-depth curation row, most likely `CONFIRM_UNGROUNDED`; if an exact Breviatea taxon is added to `curation/external_xrefs.tsv`, retain it as an `xref` rather than using it as the habitat identity. |

## Recommended Edits

- Replace the `CLASS`-depth `curation/decisions.tsv` row for `habitatmech:GOLD.8a09449b58` with an item-level curation row. The expected disposition is `CONFIRM_UNGROUNDED` if the curator confirms GOLD's `Host-associated > Protists > Breviatea` path denotes breviates acting as microbial hosts.
- Add a `curation/term_requests.tsv` row for a Breviatea-specific environment only after the item-level decision is confirmed, with the already reviewed `protist-associated environment` parent preserved through the GOLD path hierarchy.
- Decide the exact taxon xref as part of item-level curation. No tracked `Breviatea` or `breviate` xref is present in `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, or `curation/external_xrefs.tsv` today.
- Regenerate from maintained inputs with `just seed` and preview `data/habitats/host_associated/breviatea.yaml` with `just seed-canary habitatmech:GOLD.8a09449b58` before applying the full generated corpus.

## Follow-up Checks

After replacing the class-level Breviatea decision, re-run the same focused validation set and confirm the regenerated YAML is `mapping_status: REVIEWED` with a first curation-history event that points at an `ITEM`-depth `curation/decisions.tsv` row:

- `just validate data/habitats/host_associated/breviatea.yaml`
- `just validate-strict data/habitats/host_associated/breviatea.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

Also verify the GOLD attestation, supplemental biosample/study context, and Protists parent stay attached to the surviving record or its replacement.

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -name '*breviatea*' -print` found no pre-existing exact review report for this record before this file was written.
- Exact ignored/hidden-inclusive content searches for `Breviatea`, `breviatea`, `Host-associated > Protists > Breviatea`, `gold.ecosystem:4596`, `gold.ecosystem:4597`, `gold.ecosystem:4598`, and `habitatmech:GOLD.8a09449b58` found the maintained source and decision rows cited above. The same broader GOLD Protists search found reviewed host-clade siblings; those rows share the same source parent but do not feed this target.
