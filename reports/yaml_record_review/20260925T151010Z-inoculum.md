# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/engineered/inoculum.yaml`
- Started UTC: 2026-09-25T15:06:00Z
- Finished UTC: 2026-09-25T15:10:10Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/engineered/inoculum.yaml` |
| Identifier | `habitatmech:GOLD.94e1475c5e` |
| Label | `Inoculum` |
| Category | `ENGINEERED` |
| Grounding | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Source | `GOLD` |
| Source path | `Engineered > Bioreactor > Anaerobic > Inoculum` |

The target is a generated `HabitatRecord`; its maintained inputs are the GOLD inventories in `data/raw/`, the file pin in `data/habitats/PATHS.tsv`, and any future curator-authored decision, definition, or parent override in `curation/decisions.tsv` and `curation/term_requests.tsv`.

The record collapses two GOLD ecosystem node IDs, `gold.ecosystem:7096` and `gold.ecosystem:7097`, under one canonical path. The generated source attestation exposes `gold.ecosystem:7096` as the first ID and points at `data/raw/gold_ecosystem_paths.tsv` for the full collapsed pair.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/engineered/inoculum.yaml` | Passed; LinkML reported no issues. |
| `just validate-strict data/habitats/engineered/inoculum.yaml` | Passed; 1 file scanned, 0 files with errors, and 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the generated term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validated. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 records on disk, no missing/extra/differing records. |
| `just worklist --limit 2000 --status all --out /tmp/habitatmech-inoculum-worklist.tsv` | Passed; wrote 953 rows and retained `habitatmech:GOLD.94e1475c5e` as a class-swept ungrounded item with 0 direct assertions. |
| `just report` | Passed; the corpus-level diagnostic report completed. |

## Identity and Grounding

`data/raw/gold_ecosystem_paths.tsv` has the exact row:

| Canonical path | Leaf | Depth | Node count | Organisms | Studies | Biosamples | Total assertions | Node IDs |
|---|---|---:|---:|---:|---:|---:|---:|---|
| `Engineered > Bioreactor > Anaerobic > Inoculum` | `Inoculum` | 4 | 2 | 0 | 0 | 0 | 0 | `gold.ecosystem:7096\|gold.ecosystem:7097` |

The generated ID, label, source path, and source ID agree with that row and `data/habitats/PATHS.tsv` pins `habitatmech:GOLD.94e1475c5e` to slug `inoculum`.

The grounding, however, is not item-reviewed. The only maintained row for `habitatmech:GOLD.94e1475c5e` in `curation/decisions.tsv` is the 2026-08-12 class-level `CONFIRM_UNGROUNDED` sweep. Its note explicitly says no vendored-slice term matched the label by the sweep's lexical routes and that whether the concept is a habitat was not assessed. `mapping_status: SEEDED` is therefore expected, and the worklist row still carries the `TRUE` class-sweep flag.

The sole generated parent is `ENVO:00002124` `anaerobic bioreactor`, inherited from the GOLD parent path. That parent is strict for the parent record `Engineered > Bioreactor > Anaerobic`, which has item-level exact `GROUND` decisions to `ENVO:00002124`. It is probably not strict for `Inoculum`: a bioreactor inoculum is normally the starting material added to an anaerobic bioreactor, not a subtype of the reactor itself. Because no item-level Inoculum decision or definition has been written, the record has no curator-reviewed basis for either retaining `ENVO:00002124` as a broader habitat or replacing it with a material or microbial-culture genus.

Ignored and hidden files were included while checking for supporting maintained inputs. Exact searches for `habitatmech:GOLD.94e1475c5e`, `gold.ecosystem:7096`, `gold.ecosystem:7097`, `inoculum.yaml`, and the exact source path across `curation/`, `history/`, `research/`, `reports/yaml_record_review/`, `conf/`, `data/habitats/`, and `data/raw/` found only the generated record, `PATHS.tsv`, the class-level decision row, and the raw GOLD rows described here. A `find` over `reports/yaml_record_review/` found no prior `*inoculum*` review report.

## Evidence

The record has no `evidence` entries, no `causal_graphs`, and no source-specific study or biosample dataset objects. Its only support is the generated GOLD source attestation.

Two committed GOLD study side rows mention the exact path:

| Study | Path count | Paths |
|---|---:|---|
| `Gs0156593` | 2 | `Engineered > Bioreactor > Anaerobic > Digestate\|Engineered > Bioreactor > Anaerobic > Inoculum` |
| `Gs0161731` | 2 | `Engineered > Bioreactor > Anaerobic > Biomass\|Engineered > Bioreactor > Anaerobic > Inoculum` |

`data/raw/gold_path_biosamples.tsv` also has an exact row for ecosystem path ID `7097` with 22 biosamples. These rows prove GOLD uses the path in more than the ecosystem tree, but they do not by themselves define the intended habitat identity or prove the generated `anaerobic bioreactor` parent.

An ignored/hidden-inclusive search for the exact path in `data/raw/gold_path_triads.tsv`, `data/raw/environment_parameters.tsv`, `data/raw/gold_studies.tsv`, `data/raw/gold_path_biosamples.tsv`, and `data/raw/gold_ecosystem_paths.tsv` found matches only in `gold_studies.tsv`, `gold_path_biosamples.tsv`, and `gold_ecosystem_paths.tsv`. The review therefore found no source MIxS triads or environmental-parameter rows to support a narrower grounding, environmental parameter, or parent change.

## Completeness

- `definition` and `definition_source` are absent. That is incomplete for a class-swept novel term because the record has not yet been reviewed as a real habitat or entered into `curation/term_requests.tsv`.
- `synonyms` and `xrefs` are absent. No exact candidate xref was found during this bounded review.
- `environmental_parameters` is absent, and the exact path has no GOLD triad row or raw environment-parameter row.
- `characteristic_taxa` is absent, with no BacDive or PREGO source attestation on this generated record.
- `causal_graphs` is absent; no `curation/causal_graphs/` overlay references this identifier.
- `discussions` and `datasets` are absent even though the exact path has two GOLD study rows and one 22-biosample side row that a future item-level curation pass should inspect.

The supporting absence searches used `rg --no-ignore --hidden` and included ignored and hidden files. Lowercase `inoculum` hits in `research/habitats/` were unrelated prose in reports for other records, not a term request, causal overlay, history entry, or prior YAML-record review for `habitatmech:GOLD.94e1475c5e`.

## Findings

### Major: the record has only a class-level grounding decision

`curation/decisions.tsv` has a single `CLASS` `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.94e1475c5e`. The generated curation history faithfully repeats that this sweep only established the lack of a lexical match in the vendored slice and did not assess whether the source concept is a habitat at all. Leaving the row at `CLASS` also leaves `mapping_status: SEEDED`, so the record is not yet a reviewed ENVO term-request candidate.

Maintained owner: `curation/decisions.tsv`, followed by `curation/term_requests.tsv` if item-level review confirms this is a real habitat lacking a fitting vendored term.

### Major: `ENVO:00002124` is probably a contextual parent, not a strict broader habitat

The generated `parent_habitats` list contains `ENVO:00002124` only because the GOLD source path places `Inoculum` under `Engineered > Bioreactor > Anaerobic`, and the parent record is grounded exactly to `anaerobic bioreactor`. The review found no item-level definition, decision note, term request, MIxS triad, or causal/evidence overlay saying that the inoculum material itself is a kind of anaerobic bioreactor. A starter material added to an anaerobic bioreactor is related to that reactor context but is not strictly a subtype of it.

Maintained owner: an item-level `curation/decisions.tsv` row plus a `curation/term_requests.tsv` definition with a strict reviewed genus. Use `parent_mode: REPLACE` only if the item-level evidence confirms that every inherited source parent should be removed.

## Recommended Edits

1. Inspect the two GOLD study IDs, `Gs0156593` and `Gs0161731`, plus the 22 biosamples for `gold.ecosystem:7097` to establish whether GOLD's `Inoculum` denotes a seed sludge, an enriched microbial culture, a reactor input material, or another narrower concept.
2. Replace the `CLASS` row in `curation/decisions.tsv` with an `ITEM`-level decision that records the inspected GOLD path, rejects exact grounding to `ENVO:00002124`, and either confirms the concept as ungrounded or marks it not applicable if the upstream path proves to be a procedure or sample role rather than a habitat/material.
3. If the item-level decision confirms a real ungrounded habitat or material, add a `curation/term_requests.tsv` definition and a strict parent. Choose `ADD` only if the source-derived `ENVO:00002124` parent survives as a true `is-a`; otherwise choose `REPLACE` and document why `anaerobic bioreactor` is contextual.
4. Rerun `just seed`, canary `just seed-canary habitatmech:GOLD.94e1475c5e`, inspect `data/habitats/engineered/inoculum.yaml`, then apply the seed and run the corpus validators.

## Follow-up Checks

After curation, run:

- `just validate data/habitats/engineered/inoculum.yaml`
- `just validate-strict data/habitats/engineered/inoculum.yaml`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000 --status all --out /tmp/habitatmech-inoculum-worklist.tsv`
- `just report`

Manually re-check that `mapping_status` remains `SEEDED` until the underlying GOLD source concept has an `ITEM` row, and that `parent_habitats` contains only strict broader concepts after any `term_requests.tsv` parent override.

## Additional Notes

- A label search for `Inoculum` over `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/engineered/`, `curation/decisions.tsv`, and `reports/yaml_record_review/` found no same-label sibling leaf to review alongside this one.
- The related parent `data/habitats/engineered/anaerobic_bioreactor.yaml` is a multi-source reviewed record. Its exact grounding does not settle whether all generated GOLD children of `Engineered > Bioreactor > Anaerobic` are `is-a` children of `anaerobic bioreactor`.
