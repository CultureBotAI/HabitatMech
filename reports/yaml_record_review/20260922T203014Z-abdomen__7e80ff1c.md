# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/abdomen__7e80ff1c.yaml`
- Started UTC: 2026-09-22T20:27:00Z
- Finished UTC: 2026-09-22T20:30:14Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/host_associated/abdomen__7e80ff1c.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.e02956e1eb` |
| Label | `Abdomen` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Record status | Generated from one unreviewed GOLD source concept |

This is the minted GOLD record for `Host-associated > Arthropoda: Insects > Larva > Abdomen`. The identifier is the deterministic source-concept id `habitatmech:GOLD.e02956e1eb`, computed from `sha1("GOLD:Host-associated > Arthropoda: Insects > Larva > Abdomen")[:10]`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/abdomen__7e80ff1c.yaml` | Passed; LinkML validation reported no issues. |
| `just validate-strict data/habitats/host_associated/abdomen__7e80ff1c.yaml` | Passed; one file scanned with 0 error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 generated records, found 3206, with 0 missing, 0 extra, and 0 differing. |
| `git diff --check` | Passed; no whitespace errors. |

## Identity and Grounding

The direct GOLD source identity is supported:

| Raw field | Value |
|---|---|
| `canonical_path` | `Host-associated > Arthropoda: Insects > Larva > Abdomen` |
| `ecosystem` | `Host-associated` |
| `ecosystem_category` | `Arthropoda: Insects` |
| `ecosystem_type` | `Larva` |
| `ecosystem_subtype` | `Abdomen` |
| `specific_ecosystem` | empty |
| `leaf_label` | `Abdomen` |
| `path_depth` | `4` |
| `gold_node_count` | `2` |
| `organism_count` | `0` |
| `metagenome_count` | `0` |
| `biosample_count` | `0` |
| `total_assertions` | `0` |
| `gold_node_ids` | `gold.ecosystem:7599\|gold.ecosystem:7600` |

`source_attestations` matches `data/raw/gold_ecosystem_paths.tsv:1818`: the record keeps `gold.ecosystem:7599` as the first source id, records the GOLD label and canonical path exactly, omits `assertion_count` because the raw organism count is zero, and includes the expected note that two GOLD node ids share the path.

The GOLD source-path parent is supported. `data/raw/gold_ecosystem_paths.tsv:208` records the immediate parent path `Host-associated > Arthropoda: Insects > Larva`, whose deterministic minted id is `habitatmech:GOLD.1cbfc76870`; `data/habitats/host_associated/larva__aa08751d.yaml` is the reviewed record for that parent and defines it as `insect larva-associated environment`.

The seeded `NARROW` parent `UBERON:0000916` is not supported. `src/habitatmech/seed.py` mints an identifier for non-claimant rows when several GOLD paths share the leaf label, and `data/raw/gold_ecosystem_paths.tsv` contains four GOLD rows whose `leaf_label` is `Abdomen`: `Host-associated > Arthropoda: Insects > Abdomen`, `Host-associated > Mammals > Lymphatic system > Lymph nodes > Abdomen`, `Host-associated > Arthropoda: Insects > Larva > Abdomen`, and `Host-associated > Fish > Abdomen`. Minting this larval row instead of exactly grounding it is therefore expected, but the lexical match still has to be a strictly broader parent. `data/raw/ontology_terms.tsv:12962` defines `UBERON:0000916` as the subdivision of a vertebrate body between the thorax and pelvis, so it is not broader than an insect larva abdomen.

`BTO:0000020 abdomen` is present in the vendored ontology slice and its definition explicitly includes "The posterior section of the body behind the thorax in an arthropod." That makes BTO a plausible broader parent to review for an item-level `GROUND_AS_PARENT` decision. It is already present as the direct PREGO record `data/habitats/host_associated/abdomen.yaml`, but no decision row currently tells GOLD `habitatmech:GOLD.e02956e1eb` to use BTO instead of the seeded UBERON match.

## Evidence

| Claim | Nearest support | Review |
|---|---|---|
| The generated record represents GOLD node ids `gold.ecosystem:7599` and `gold.ecosystem:7600` at `Host-associated > Arthropoda: Insects > Larva > Abdomen`. | `data/raw/gold_ecosystem_paths.tsv:1818` | Supported exactly. |
| The GOLD source row has no upstream organism, metagenome, or biosample assertions. | `data/raw/gold_ecosystem_paths.tsv:1818` | Supported exactly. |
| The GOLD path is a child of `Host-associated > Arthropoda: Insects > Larva`. | `data/raw/gold_ecosystem_paths.tsv:208`; `src/habitatmech/seed.py` | Supported. `ingest_gold()` links each GOLD row to the generated record for the next path level up, so the source-path parent `habitatmech:GOLD.1cbfc76870` is expected. |
| `Host-associated > Arthropoda: Insects > Larva > Abdomen` is narrower than `UBERON:0000916 abdomen`. | `data/raw/ontology_terms.tsv:12962`; `data/raw/gold_ecosystem_paths.tsv:1818` | Unsupported. The source path is an insect abdomen under larva; the UBERON term is vertebrate-scoped and is therefore a lexical near miss, not a broader habitat. |
| `mapping_status: SEEDED` | Ignored/hidden-inclusive search for `habitatmech:GOLD.e02956e1eb` in `curation/decisions.tsv` | Supported. No item-level curation decision exists for the source concept. |

Unsupported or over-scoped claims: only the `UBERON:0000916` parent and the `skos:narrowMatch` relation to that parent are unsupported. The minted identifier, GOLD source attestation, zero-count assertion omission, host-associated category, and reviewed source-path parent are consistent with their maintained inputs.

## Completeness

No direct source attestation is missing for the GOLD source row: the record carries the source name, first source id, source label, canonical source path, and the note explaining that two GOLD node ids share this path. Its omitted `assertion_count` is correct for a GOLD row with `organism_count: 0`.

The consequential completeness gap is the missing item-level decision for the insect-larval abdomen concept. Exact ignored/hidden-inclusive searches found no `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/external_xrefs.tsv`, `curation/causal_graphs`, `history`, or raw research artifact keyed by `habitatmech:GOLD.e02956e1eb`; the target is present only in `data/habitats/PATHS.tsv` and the generated YAML.

No maintained causal overlay, history file, raw research report, or prior exact YAML review report was found by filename for `abdomen__7e80ff1c` or `abdomen-7e80ff1c` under `curation/causal_graphs`, `history`, `research`, or `reports/yaml_record_review`; the `find` search included ignored files beneath those checked directories.

The sibling `Host-associated > Arthropoda: Insects > Larva` records `Head`, `Thorax`, `Gut`, and `Whole body` are likewise still seeded and should be screened when this branch is curated. That sibling screen is separate from this record's direct finding, because `Thorax` is seeded to BTO, whose definition covers insect thorax, while `Head` and `Whole body` need their own target-specific taxonomy checks before any conclusion.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | The seeded `NARROW` grounding attaches `UBERON:0000916` as a broader parent of an insect-larval abdomen. `UBERON:0000916` is vertebrate-scoped, so `parent_habitats` contains a false is-a edge and the source attestation's `skos:narrowMatch` overstates the lexical match. | `data/raw/gold_ecosystem_paths.tsv:1818` scopes the source path to `Arthropoda: Insects > Larva > Abdomen`; `data/raw/ontology_terms.tsv:12962` defines `UBERON:0000916` as vertebrate abdomen; exact ignored/hidden-inclusive search found no overriding row for `habitatmech:GOLD.e02956e1eb` in `curation/decisions.tsv`. | Add an item-level row for `habitatmech:GOLD.e02956e1eb` in `curation/decisions.tsv`, most likely `GROUND_AS_PARENT` to `BTO:0000020` if curator review accepts the BTO arthropod abdomen sense as a broader parent. |

No blocker findings.

No minor findings.

## Recommended Edits

1. Add an item-level `curation/decisions.tsv` row for `habitatmech:GOLD.e02956e1eb`.
   Re-review `Host-associated > Arthropoda: Insects > Larva > Abdomen` against `BTO:0000020 abdomen`. If the BTO arthropod abdomen sense is accepted as strictly broader, use `GROUND_AS_PARENT` with `object_id: BTO:0000020`, `object_label: abdomen`, and `review_depth: ITEM`; otherwise use `CONFIRM_UNGROUNDED` without `UBERON:0000916`.
2. Run `just seed-canary habitatmech:GOLD.e02956e1eb` and inspect the generated YAML to confirm `UBERON:0000916` is no longer emitted as a parent.
3. Screen the other seeded insect-larval body-part records under `Host-associated > Arthropoda: Insects > Larva`: `head__ef8439be`, `thorax__ec1cb573`, and `whole_body__07339b29`.
4. If a later edit adds a term request, external xref, or causal graph for this minted record, scaffold the corresponding append-only `history/` entry and keep `just validate-history` green.

## Follow-up Checks

- `just seed-canary habitatmech:GOLD.e02956e1eb`
- `just seed`
- `just validate data/habitats/host_associated/abdomen__7e80ff1c.yaml`
- `just validate-strict data/habitats/host_associated/abdomen__7e80ff1c.yaml`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- Exact ignored/hidden-inclusive searches for `habitatmech:GOLD.e02956e1eb`, `Host-associated > Arthropoda: Insects > Larva > Abdomen`, `BTO:0000020`, and `UBERON:0000916` across `curation`, `data/raw`, and `data/habitats`, excluding generated text maps and pages, to verify that the insect-larval abdomen concept uses only taxonomically compatible parents.

## Additional Notes

- The pre-report exact filename check `find reports/yaml_record_review -maxdepth 1 -type f -name '*abdomen__7e80ff1c*' -print` returned no previous exact report; `find` includes ignored files.
- Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.e02956e1eb`, `Host-associated > Arthropoda: Insects > Larva > Abdomen`, `gold.ecosystem:7599`, `habitatmech:GOLD.1cbfc76870`, `BTO:0000020`, and `UBERON:0000916` found the generated target, raw GOLD row, raw ontology rows, source-path parent, BTO abdomen record, and sibling abdomen paths cited above. No maintained curation row currently names `habitatmech:GOLD.e02956e1eb`.
- The exact source-path search also found prior `abdomen__0b37a453` and `abdomen__0eb11b74` review reports that mention this insect-larval path only as an ambiguous GOLD sibling. Those are not prior reviews of `data/habitats/host_associated/abdomen__7e80ff1c.yaml`.
