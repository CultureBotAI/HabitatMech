# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/breast.yaml`
- Started UTC: 2026-09-22T10:18:57Z
- Finished UTC: 2026-09-22T10:18:57Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `BTO:0000149` |
| Label | `breast` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the PREGO inventory plus the vendored ontology slice; generated YAML remains read-only |
| Locked slug | `data/habitats/PATHS.tsv:32` maps `BTO:0000149` to `breast` |

This is the seeded PREGO record for the BRENDA Tissue Ontology `breast` class. PREGO uses `BTO:0000149` directly as its habitat identifier, so the seeder treats the row as direct `EXACT` self-grounding and emits the BTO term rather than minting a HabitatMech-only identifier.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/breast.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/breast.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The exact identity, label, definition, parent hierarchy, path lock, PREGO attestation, and emitted taxa are supported by the vendored ontology and PREGO source tables:

- `data/habitats/PATHS.tsv:32` pins `BTO:0000149` to slug `breast`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:151` gives `BTO:0000149` the ontology label `breast`, the generated BTO definition, `directly_referenced: TRUE`, and `label_only: FALSE`.
- `data/raw/ontology_terms.tsv:256` and `:433` provide the two emitted parent labels, `female reproductive gland` for `BTO:0000254` and `excretory gland` for `BTO:0000431`.
- `data/raw/ontology_subclass_edges.tsv:103-104` records `BTO:0000149 rdfs:subClassOf BTO:0000254` and `BTO:0000149 rdfs:subClassOf BTO:0000431`; no other exact subclass edge in the vendored table names `BTO:0000149` as subject.
- `data/raw/prego_habitats.tsv:317` records PREGO source id `BTO:0000149`, vocabulary `BTO`, Biolink type `GrossAnatomicalStructure`, 13 taxon assertions, 13 direct assertions, maximum PREGO score `3`, evidence channel `annotated_genomes_isolates`, and synonyms `breast|breastic|breasts`.
- `data/raw/prego_habitat_taxa.tsv:190-202` is the complete PREGO taxon block for `BTO:0000149`; it emits ranks 1 through 13, the same taxon ids and labels, score `3`, `direct_flag: TRUE`, evidence channel `annotated_genomes_isolates`, and no corroborating source.

`grounding_status: EXACT` is correct because the PREGO row is self-grounded to the same BTO class. `mapping_status: SEEDED` is also expected because the PREGO decision key for this source is `habitatmech:PREGO.8d1440c273`, and ignored/hidden-inclusive exact searches found no item-level decision row for that key or for `BTO:0000149`.

## Evidence

Every generated scientific claim in this record is source-derived:

| Claim | Nearest source | Review |
|---|---|---|
| Ontology identity, label, definition, and host-associated category inference | `data/raw/ontology_terms.tsv:151` | Supported exactly |
| Locked filename | `data/habitats/PATHS.tsv:32` | Supported exactly |
| Parent `BTO:0000254` / `female reproductive gland` | `data/raw/ontology_subclass_edges.tsv:103`, `data/raw/ontology_terms.tsv:256` | Supported exactly |
| Parent `BTO:0000431` / `excretory gland` | `data/raw/ontology_subclass_edges.tsv:104`, `data/raw/ontology_terms.tsv:433` | Supported exactly |
| PREGO `source_id`, `source_label`, 13-taxon assertion count, score `3.0`, and evidence channel | `data/raw/prego_habitats.tsv:317` | Supported exactly |
| Related synonyms `breastic` and `breasts` | `data/raw/prego_habitats.tsv:317` | Supported; the canonical `breast` synonym collapses against the record label |
| The 13 `characteristic_taxa` entries | `data/raw/prego_habitat_taxa.tsv:190-202` | Supported exactly, including ranks, scores, labels, and the 13-taxon candidate pool |

The record has no xrefs, environmental parameters, record-level evidence, causal graphs, discussion links, or datasets. Those absences are consistent with exact ignored/hidden-inclusive searches that found no maintained decision, term-request row, causal-graph overlay, history entry, target-specific research artifact, external xref, environment-parameter row, dataset row, or prior exact YAML review report for `BTO:0000149` or `habitatmech:PREGO.8d1440c273`.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- The BTO ontology term supplies the generated definition and `definition_source: BTO`.
- Both vendored subclass parents are emitted as `parent_habitats`, so the source hierarchy is fully represented for `BTO:0000149`.
- The sole `source_attestations` entry captures the complete PREGO source id, source label, assertion count, `TAXON` unit, maximum score, and evidence-channel set.
- The synonym list carries the two non-canonical PREGO alternate forms after de-duplicating the raw canonical label `breast`.
- PREGO reports 13 taxa for `BTO:0000149`, and all 13 are emitted with their taxon ids, labels, scores, ranks, source, and 13-taxon candidate pool.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated target, excluding generated `build`, `data/text_map`, and `pages` trees. They found the expected PREGO habitat row, PREGO taxon rows, ontology row, parent subclass edges, parent ontology labels, path lock, and generated YAML, and no target-specific maintained curation, history, research, xref, environmental-parameter, dataset, or exact prior YAML review report.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if future curation changes the PREGO rollup, BTO vendored terms, ontology parent handling, path lock, or characteristic-taxon emission:

- `just validate data/habitats/host_associated/breast.yaml`
- `just validate-strict data/habitats/host_associated/breast.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If future review redirects `BTO:0000149` away from this direct PREGO self-grounding, add an explicit `curation/decisions.tsv` row for `habitatmech:PREGO.8d1440c273` first, regenerate with `just seed`, and verify that the PREGO attestation and all 13 characteristic taxa remain attached to the surviving record.

## Additional Notes

- `find reports/yaml_record_review -name '*breast*' -print` found no pre-existing exact review report for this record before this file was written.
- Exact ignored/hidden-inclusive content searches for `BTO:0000149`, `habitatmech:PREGO.8d1440c273`, `breast`, and the emitted taxon ids found several contextual `breast` or shared-taxon matches, including generated breast disease paths, the causal graph for milk, and unrelated BTO cell-line terms; the only rows that feed this target are the `BTO:0000149` rows cited above.
