# YAML Record Review: Biofouling

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/biofouling__438bcaa2.yaml`
- Started UTC: 2026-09-21T22:04:30Z
- Finished UTC: 2026-09-21T22:09:24Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.3ec896865d` |
| Label | `Biofouling` |
| Category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `REVIEWED` |
| Maintained inputs | `data/raw/gold_ecosystem_paths.tsv`, `curation/decisions.tsv`, `curation/term_requests_excluded.tsv`, `data/habitats/PATHS.tsv` |

This is a generated GOLD record. The path lock maps `habitatmech:GOLD.3ec896865d` to the `biofouling__438bcaa2` slug, and the raw GOLD row maps `Environmental > Aquatic > Freshwater > Biofouling` to the `Biofouling` leaf at depth 4 with two collapsed GOLD ecosystem nodes, `gold.ecosystem:8358|gold.ecosystem:8360`, and zero organisms, studies, biosamples, or total assertions.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/aquatic/biofouling__438bcaa2.yaml` | Pass; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/biofouling__438bcaa2.yaml --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Pass; the committed term-request table is current with 109 terms. |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass; 3,206 expected records, 3,206 records on disk, and 0 missing, extra, or differing records. |

`just validate-products` was not run. This review did not add or change a grounding row, and the record's ontology terms, `ENVO:06105023` and `ENVO:00002011`, were checked directly in the vendored `data/raw/ontology_terms.tsv` slice.

## Identity and Grounding

The minted identity, GOLD source identity, and xref grounding are consistent:

- `data/habitats/PATHS.tsv` pins `habitatmech:GOLD.3ec896865d` to `biofouling__438bcaa2`, matching the reviewed YAML path.
- `data/raw/gold_ecosystem_paths.tsv` carries the exact source path and both collapsed source nodes copied into `source_attestations`: `gold.ecosystem:8358` is the first shown node and `gold.ecosystem:8360` is noted by count.
- `curation/decisions.tsv` has an item-level `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.3ec896865d` with `ENVO:06105023` retained through `relation: xref`.
- `ENVO:06105023` is present in `data/raw/ontology_terms.tsv` as the process term `biofouling`, so it is correctly excluded from `parent_habitats` and retained only in `xrefs`.

The generated ontology parent is not supported as an is-a parent. `ENVO:00002011` is the material `fresh water`; a biofouling layer in a freshwater GOLD bin is not itself fresh water. That parent is a source-path context link, not a strictly broader habitat.

The record is also correctly left ungrounded to `ENVO:00002034` `biofilm`. Its concrete child, `Environmental > Aquatic > Freshwater > Biofouling > Microfouling/Biofilm`, has its own raw GOLD row and is already grounded to `ENVO:00002034` as part of the generated `data/habitats/aquatic/biofilm__cc6fe05a.yaml` merge.

## Evidence

Supported claims:

- The `Biofouling` label, `GOLD` source, `gold.ecosystem:8358` source id, full source path, and two-node note come from `data/raw/gold_ecosystem_paths.tsv`.
- `habitat_category: AQUATIC` follows from the GOLD `Environmental > Aquatic` source path.
- `grounding_status: UNGROUNDED`, `mapping_status: REVIEWED`, and `xrefs: ENVO:06105023` follow from the `CONFIRM_UNGROUNDED`, `ITEM`, `xref` decision row for `habitatmech:GOLD.3ec896865d`.
- `curation/term_requests_excluded.tsv` explicitly excludes this parent leaf from HabitatMech term requests because it has zero GOLD organisms, studies, and biosamples, and because its only concrete `Microfouling/Biofilm` child already grounds to `ENVO:00002034` `biofilm`.

Unsupported or over-scoped claims:

- `parent_habitats: ENVO:00002011` is broader only in the GOLD path sense. The vendored ENVO row labels `ENVO:00002011` as `fresh water` and defines it as water with a low dissolved-solute concentration, which does not subsume a fouling layer accumulated on a solid surface.

The record has no definition, record-level evidence, environmental parameters, characteristic taxa, causal graphs, discussion links, or datasets. That absence is supported: ignored-file-inclusive searches found no exact `Environmental > Aquatic > Freshwater > Biofouling` or `gold.ecosystem:8358` entries in `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, or `data/raw/gold_studies.tsv`, and no exact `habitatmech:GOLD.3ec896865d` entry in `curation/term_requests.tsv` or `curation/causal_graphs/`.

## Completeness

The record is intentionally sparse because it preserves an unused GOLD parent leaf:

- No Biofouling definition is expected because `curation/term_requests_excluded.tsv` intentionally declines a new term request for this zero-assertion grouping bin.
- No GOLD assertion count is expected because the raw `organism_count`, `study_count`, `biosample_count`, and `total_assertions` are all zero.
- No GOLD MIxS triads or study rows are expected; the exact source path and first source id are absent from the committed GOLD API TSVs named above.
- No causal graph is expected; the record has no sample-derived or literature-derived mechanism claims to support.

The one consequential gap is an authoritative way to suppress a false source-path parent while keeping a minted, ungrounded source concept excluded from term requests. All negative checks above used `rg --no-ignore --hidden` or `find`, so ignored files were included where they were in the searched path.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | `parent_habitats` asserts a false is-a edge from Freshwater Biofouling to `ENVO:00002011` `fresh water`. | The source path places `Biofouling` under GOLD's `Freshwater` bin, but the decision row explicitly says the concept is the fouling layer rather than the process, and the vendored `ENVO:00002011` definition denotes low-solute water rather than a surface layer. | No current TSV owns selective removal of source-path parents for a term-request-excluded minted record; add a curated source-parent suppression input consumed by `src/habitatmech/seed.py`, or extend an existing maintained input to express that edge retraction. |

## Recommended Edits

1. Add maintained support for retracting false generated source-path parents independently of `curation/term_requests.tsv`, then retract the generated edge from `habitatmech:GOLD.3ec896865d` to `ENVO:00002011`.

## Follow-up Checks

- Re-seed the one canary with `just seed-canary habitatmech:GOLD.3ec896865d`, then inspect `data/habitats/aquatic/biofouling__438bcaa2.yaml` for the removed `ENVO:00002011` parent.
- Run `just validate data/habitats/aquatic/biofouling__438bcaa2.yaml`.
- Run `just validate-strict data/habitats/aquatic/biofouling__438bcaa2.yaml --quiet`.
- Run `just term-requests-check`.
- Run `just validate-history`.
- Run `just verify-corpus --max-diffs 1`.

## Additional Notes

An ignored-file-inclusive `find` search of `reports/yaml_record_review/` found no prior report for this `biofouling__438bcaa2` record before this file was written.
