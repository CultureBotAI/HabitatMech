# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/abdomen__0b37a453.yaml`
- Started UTC: 2026-09-22T19:33:13Z
- Finished UTC: 2026-09-22T19:34:46Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/host_associated/abdomen__0b37a453.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.c6b0b54290` |
| Label | `Abdomen` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Record status | Generated from the GOLD inventory without an item-level curation decision |

This is the generated GOLD record for the path `Host-associated > Arthropoda: Insects > Abdomen`. Its identifier is the deterministic minted id `habitatmech:GOLD.c6b0b54290`, computed from `sha1("GOLD:Host-associated > Arthropoda: Insects > Abdomen")[:10]`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/abdomen__0b37a453.yaml` | Passed; LinkML validation reported no issues. |
| `just validate-strict data/habitats/host_associated/abdomen__0b37a453.yaml` | Passed; one file scanned with 0 error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 generated records, found 3206, with 0 missing, 0 extra, and 0 differing. |
| `git diff --check` | Passed; no whitespace errors. |

## Identity and Grounding

The direct GOLD identity is supported:

| Raw field | Value |
|---|---|
| `canonical_path` | `Host-associated > Arthropoda: Insects > Abdomen` |
| `leaf_label` | `Abdomen` |
| `path_depth` | `3` |
| `gold_node_count` | `3` |
| `organism_count` | `3` |
| `metagenome_count` | `0` |
| `biosample_count` | `0` |
| `total_assertions` | `3` |
| `gold_node_ids` | `gold.ecosystem:7090\|gold.ecosystem:7091\|gold.ecosystem:7092` |

`source_attestations` matches `data/raw/gold_ecosystem_paths.tsv:735`: the generated record keeps `gold.ecosystem:7090` as the first source id, records the source label and path exactly, emits `assertion_count: 3` with `assertion_unit: ORGANISM`, and includes the expected note that three GOLD node ids share this path.

The GOLD path-parent edge is also supported. `data/raw/gold_ecosystem_paths.tsv:26` records the immediate parent path `Host-associated > Arthropoda: Insects`, `data/habitats/host_associated/arthropoda_insects.yaml` is the generated reviewed record for `habitatmech:GOLD.dba2a83b95`, and `parent_habitats` correctly names that parent.

The ontology-derived parent is not supported. The reviewed record also asserts `parent_habitats: [UBERON:0000916]`; `data/raw/ontology_terms.tsv:12962` defines `UBERON:0000916 abdomen` as the subdivision of the vertebrate body between the thorax and pelvis. That is not a broader habitat for the GOLD insect-abdomen path under `Arthropoda: Insects`.

This false parent is the expected output of the automatic ambiguous-leaf rule rather than a maintained curation row. `leaf_claimants()` leaves a tied shallowest label unclaimed; GOLD has both `Host-associated > Arthropoda: Insects > Abdomen` and `Host-associated > Fish > Abdomen` at depth 3, so `resolve_gold()` keeps this source path minted and attaches the lexical `abdomen` match as a `skos:narrowMatch` parent. An ignored/hidden-inclusive exact search found no row for `habitatmech:GOLD.c6b0b54290` in `curation/decisions.tsv`, so `mapping_status: SEEDED` is mechanically expected.

## Evidence

| Claim | Nearest support | Review |
|---|---|---|
| The generated record represents GOLD node ids `gold.ecosystem:7090`, `gold.ecosystem:7091`, and `gold.ecosystem:7092` at `Host-associated > Arthropoda: Insects > Abdomen`. | `data/raw/gold_ecosystem_paths.tsv:735` | Supported exactly. |
| The GOLD path has three organism assertions and no metagenome or biosample assertions. | `data/raw/gold_ecosystem_paths.tsv:735` | Supported exactly. |
| The GOLD `Abdomen` path is a child of the generated `Arthropoda: Insects` source path. | `data/raw/gold_ecosystem_paths.tsv:26`; `src/habitatmech/seed.py` | Supported. `ingest_gold()` links each GOLD path to the concept generated from the next path level up. |
| `UBERON:0000916` is a broader habitat for this insect abdomen source path. | `data/raw/ontology_terms.tsv:12962`; `data/raw/gold_ecosystem_paths.tsv:735` | Unsupported. `UBERON:0000916` is vertebrate-scoped; the source path is insect-scoped. |
| `mapping_status: SEEDED` | Exact ignored/hidden-inclusive search for `habitatmech:GOLD.c6b0b54290` in `curation/decisions.tsv` | Supported. No item-level decision exists for this source concept. |

Unsupported or over-scoped claims: the `parent_habitats` edge to `UBERON:0000916` and the `source_attestations.mapping_predicate` value `skos:narrowMatch` overstate a lexical match to a vertebrate abdomen class as a broader-parent relationship for an insect abdomen habitat.

## Completeness

No direct source attestation is missing for the GOLD source row: the record carries the source name, first source id, source label, canonical source path, organism assertion count and unit, and the note explaining that three GOLD node ids share this path.

The consequential completeness gap is the missing item-level curation row for this ambiguous GOLD label. This path has enough context to rule out `UBERON:0000916`, but without `curation/decisions.tsv` overriding the automatic ambiguous-leaf result, the record stays `SEEDED`, `NARROW`, and parented to the vertebrate abdomen term.

No maintained causal overlay, history file, raw research report, or prior exact YAML review report was found by filename for `abdomen__0b37a453` or `abdomen-0b37a453` under `curation/causal_graphs`, `history`, `research`, or `reports/yaml_record_review`; the `find` search included ignored files beneath those checked directories.

Existing curated descendants show that the surrounding insect-abdomen branch is already being curated away from false vertebrate or generic abdomen groundings. `curation/decisions.tsv:856` confirms `Host-associated > Arthropoda: Insects > Abdomen > Metasoma` as a real insect body-region habitat, not `UBERON:0000916`; `curation/term_requests.tsv:109` and `data/habitats/host_associated/metasoma.yaml` retain the GOLD abdomen parent path while defining a novel `metasoma environment`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | `abdomen__0b37a453.yaml` asserts `UBERON:0000916` as a broader habitat for GOLD's insect abdomen path, but `UBERON:0000916` is the vertebrate abdomen. This is a wrong ontology parent and an over-scoped `skos:narrowMatch` attestation on an unreviewed source path. | The reviewed record contains `parent_habitats: [UBERON:0000916, habitatmech:GOLD.dba2a83b95]` and `mapping_predicate: skos:narrowMatch`; `data/raw/gold_ecosystem_paths.tsv:735` scopes the GOLD source path to `Arthropoda: Insects`; `data/raw/ontology_terms.tsv:12962` scopes `UBERON:0000916` to the vertebrate abdomen. | Add an item-level row for `habitatmech:GOLD.c6b0b54290` in `curation/decisions.tsv`; then regenerate `data/habitats/host_associated/abdomen__0b37a453.yaml`. |

No blocker findings.

No minor findings.

## Recommended Edits

1. Add an item-level `curation/decisions.tsv` row for `habitatmech:GOLD.c6b0b54290`.
   Confirm `Host-associated > Arthropoda: Insects > Abdomen` as an insect body-region habitat and explicitly reject `UBERON:0000916` as a parent for this source path. If no exact or broader vendored ontology term fits, use `CONFIRM_UNGROUNDED` without `UBERON:0000916`; the GOLD source-path parent will still keep it under `habitatmech:GOLD.dba2a83b95`.
2. Regenerate the corpus and verify that `data/habitats/host_associated/abdomen__0b37a453.yaml` no longer lists `UBERON:0000916` in `parent_habitats` and no longer reports `mapping_predicate: skos:narrowMatch` against that false lexical parent.
3. Screen the sibling GOLD `Abdomen` records emitted by the same ambiguous-leaf route, especially `data/habitats/host_associated/abdomen__7e80ff1c.yaml`, to make sure no other insect abdomen path still uses vertebrate `UBERON:0000916` as a broader parent.

## Follow-up Checks

- `just seed-canary habitatmech:GOLD.c6b0b54290`
- `just verify-corpus --max-diffs 1`
- `just validate data/habitats/host_associated/abdomen__0b37a453.yaml`
- `just validate-strict data/habitats/host_associated/abdomen__0b37a453.yaml`
- Exact ignored/hidden-inclusive searches for `habitatmech:GOLD.c6b0b54290`, `Host-associated > Arthropoda: Insects > Abdomen`, and `UBERON:0000916` across `curation`, `data/raw`, and `data/habitats`, excluding generated text maps and pages, to verify that the target source concept is now item-reviewed and no longer has the false UBERON parent.

## Additional Notes

- The pre-report exact filename check `find reports/yaml_record_review -maxdepth 1 -name '*abdomen__0b37a453*' -print` returned no previous exact report.
- Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.c6b0b54290`, `Host-associated > Arthropoda: Insects > Abdomen`, `gold.ecosystem:7090`, `gold.ecosystem:7091`, `gold.ecosystem:7092`, `UBERON:0000916`, and `habitatmech:GOLD.dba2a83b95` found the generated target, raw GOLD rows, raw ontology rows, generated parent and descendant records, and curated metasoma/gaster descendant rows cited above.
- The exact ignored/hidden-inclusive search for the other GOLD abdomen paths found `Host-associated > Fish > Abdomen`, `Host-associated > Mammals > Lymphatic system > Lymph nodes > Abdomen`, and `Host-associated > Arthropoda: Insects > Larva > Abdomen`; those rows explain why no shallowest GOLD `Abdomen` path can claim the lexical ontology match outright.
