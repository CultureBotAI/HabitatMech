# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/nutrient_rich.yaml
- Started UTC: 2026-09-25T06:14:00Z
- Finished UTC: 2026-09-25T06:28:08Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| LinkML class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.d97bda50b6` |
| Label | `Nutrient-rich` |
| File | `data/habitats/engineered/nutrient_rich.yaml` |
| Category | `ENGINEERED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Generated or maintained | Generated from `data/raw/` inventories by `scripts/seed_from_sources.py` |

The record is the GOLD path `Engineered > Artificial ecosystem >
Nutrient-rich`. Its source row is `data/raw/gold_ecosystem_paths.tsv:1141`, a
depth-3 row with leaf label `Nutrient-rich`, one GOLD ecosystem node ID
`gold.ecosystem:6278`, and zero organism, study, biosample, and total
assertion counts in the core kg-microbe inventory.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/nutrient_rich.yaml` | Passed; LinkML reported no issues. |
| `just validate-strict data/habitats/engineered/nutrient_rich.yaml` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current for 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against the vendored history schema. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 records, found 3206, with 0 missing, 0 extra, and 0 differing records. |
| `just worklist --limit 2000 --status all` | Passed; included `Nutrient-rich` with 0 upstream assertions and the target plus child `R2A agar` source paths. |
| `just report` | Passed; rebuilt the corpus report over 3206 records. |

No validator was skipped.

## Identity and Grounding

- The record's minted identifier `habitatmech:GOLD.d97bda50b6` is a generated,
  content-hashed GOLD identifier and is pinned to slug `nutrient_rich` in
  `data/habitats/PATHS.tsv:2898`.
- The only upstream attestation is GOLD, with `source_id: gold.ecosystem:6278`,
  `source_label: Nutrient-rich`, and `source_path: Engineered > Artificial
  ecosystem > Nutrient-rich`. This agrees with
  `data/raw/gold_ecosystem_paths.tsv:1141`.
- The parent `habitatmech:GOLD.0acae9a1a4` is the generated record for the
  next broader source path `Engineered > Artificial ecosystem`, so the
  hierarchy preserves GOLD's source-path structure.
- The child path `Engineered > Artificial ecosystem > Nutrient-rich > R2A agar`
  generates `data/habitats/engineered/r2a_agar.yaml`, whose sole
  `parent_habitats` value is this record's identifier.
- `grounding_status: UNGROUNDED` is traceable to the maintained class-level
  decision in `curation/decisions.tsv:1201`. That row confirms that no term in
  the vendored slice matched this label by any search route, but does not
  promote the source concept to a term request or to item-level review.
- `mapping_status: SEEDED` is therefore consistent with the `CLASS`
  `review_depth` of the only decision row for `habitatmech:GOLD.d97bda50b6`.

## Evidence

- The record has no curator-authored `evidence` entries, causal graphs,
  environmental parameters, characteristic taxa, discussions, or datasets.
- No claim-level literature evidence is required for the generated GOLD source
  attestation itself. The attestation is backed by the committed raw GOLD path
  row.
- An ignored-inclusive exact search of `data/raw/gold_studies.tsv`,
  `data/raw/gold_path_biosamples.tsv`, and `data/raw/gold_path_triads.tsv` for
  `Engineered > Artificial ecosystem > Nutrient-rich` found no supplemental
  GOLD rows for the exact path.
- The narrower child path
  `Engineered > Artificial ecosystem > Nutrient-rich > R2A agar` has
  supplemental GOLD rows: 334 biosamples in
  `data/raw/gold_path_biosamples.tsv:95` and one study path mention in
  `data/raw/gold_studies.tsv:4065`. Those rows belong to the child habitat
  and do not change this parent record.

## Completeness

- The empty optional slots are acceptable for this generated GOLD path. The
  core GOLD inventory has no organism assertions for the path, and
  `src/habitatmech/seed.py` currently emits GOLD `assertion_count` only from
  the nonzero `organism_count` in `data/raw/gold_ecosystem_paths.tsv`.
- An ignored-inclusive exact search of `curation`, `history`, `research`,
  `curation/term_requests.tsv`, `curation/term_requests`,
  `curation/causal_graphs`, and `data/habitats/PATHS.tsv` for
  `habitatmech:GOLD.d97bda50b6` and `Nutrient-rich` found only the maintained
  class-level decision row and the PATHS slug row before this report was
  added.
- An ignored-inclusive exact search of `data/raw/gold_ecosystem_paths.tsv`,
  `data/raw/gold_studies.tsv`, `data/raw/gold_path_biosamples.tsv`, and
  `data/raw/gold_path_triads.tsv` for
  `Engineered > Artificial ecosystem > Nutrient-rich` and
  `gold.ecosystem:6278` found only the raw GOLD ecosystem row for the target.
- `find reports/yaml_record_review -name '*nutrient_rich.md'` found no prior
  review report for this record; `find` included gitignored entries below
  `reports/yaml_record_review`.

## Findings

None found.

## Recommended Edits

None found.

## Follow-up Checks

None found.

## Additional Notes

- The supplemental GOLD biosample and study inventories are used to decide
  which GOLD study IDs are worth querying for API-only MIxS triads. The direct
  `Nutrient-rich` path has no committed exact row in
  `data/raw/gold_path_triads.tsv`, so there is no exact triad evidence for
  this review to assess.
- The `R2A agar` child record has supplemental GOLD biosamples, but its
  narrower path is not evidence for broadening this parent beyond the source
  hierarchy already represented in `parent_habitats`.
