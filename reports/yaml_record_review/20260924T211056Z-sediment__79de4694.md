# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/engineered/sediment__79de4694.yaml`
- Started UTC: `2026-09-24T21:06:33Z`
- Finished UTC: `2026-09-24T21:10:56Z`
- Verdict: needs curation

## Target

Reviewed the complete generated record at `data/habitats/engineered/sediment__79de4694.yaml`. The record is a GOLD-only engineered habitat for the canonical path `Engineered > Artificial ecosystem > Aquaculture > Crustaceans pond > Sediment`, minted as `habitatmech:GOLD.70845c6a72`.

The generated file has `grounding_status: NARROW` and `mapping_status: SEEDED`. It has one true ontology parent, `ENVO:00002007` `sediment`, one inherited GOLD source-path parent, `habitatmech:GOLD.be4a7c95b6` `Crustaceans pond`, and one `GOLD` source attestation for `gold.ecosystem:8006`.

## Validation

| Check | Result |
|---|---|
| `find reports/yaml_record_review -maxdepth 1 -type f -name '*-sediment__79de4694.md' -print` | Passed before this report was written; no existing exact `sediment__79de4694` review was found. `find` is not `.gitignore`-filtered. |
| `just validate data/habitats/engineered/sediment__79de4694.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/sediment__79de4694.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Focused causal overlay validator | Not applicable; an exact ignored-inclusive search in `curation/causal_graphs/` found no `habitatmech:GOLD.70845c6a72`, `gold.ecosystem:8006`, `sediment__79de4694`, `Crustaceans pond`, or exact `Engineered > Artificial ecosystem > Aquaculture > Crustaceans pond > Sediment` overlay. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| Reference validator | Not applicable; the target record has no record-level `evidence` entries and no causal graph edges with citation references. |
| `just term-requests-check` | Passed. |
| `just validate-history` | Passed. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records all exist on disk, with 0 missing, 0 extra, and 0 differing records. |
| `just worklist --limit 2000` | Passed. |
| `just report` | Passed; the corpus report completed for 3,206 records. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

The generated identifier, label, category, source attestation, and ontology grounding agree with the committed GOLD inventory. `data/raw/gold_ecosystem_paths.tsv` has the exact child path at depth 5 with `gold_node_count` 1, no direct aggregate organism, study, or biosample assertions, and source ID `gold.ecosystem:8006`. `data/raw/gold_path_biosamples.tsv` has six biosamples for the same path at upstream ecosystem path ID `8006`. `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.70845c6a72` to the stable `sediment__79de4694` slug.

The `NARROW` grounding is scientifically plausible: this source path denotes sediment in a crustaceans pond, and the vendored ontology contains `ENVO:00002007` `sediment` as a broader material term. The record is still only seeded because an exact ignored-inclusive search across `data/raw/`, `data/habitats/`, `curation/`, `history/`, `research/`, `conf/`, and `reports/yaml_record_review/` found the generated record and GOLD inventory rows but no item-level decision row, term request, history entry, research report, label-correspondence residual, or prior exact review for `habitatmech:GOLD.70845c6a72`, `gold.ecosystem:8006`, `sediment__79de4694`, or the exact source path.

The second generated parent is not a strict broader material parent. `data/raw/gold_path_triads.tsv` represents `ENVO:00002007` `sediment` as the MIxS `env_medium` term, `ENVO:00000033` `pond` as the local-scale site, and `ENVO:03600074` `aquaculture farm` as the broad-scale context for this path. That supports treating the source leaf as sediment in an engineered aquaculture pond, not treating the sediment material as a subtype of the `Crustaceans pond` container inherited from the GOLD source path.

## Evidence

- Supported: the `GOLD` source attestation is traceable to `data/raw/gold_ecosystem_paths.tsv`, including the single upstream node ID `gold.ecosystem:8006`.
- Supported: the generated `ENVO:00002007` parent is the MIxS medium term for the exact GOLD path in `data/raw/gold_path_triads.tsv`; that side table records one sample and one study with share 1.00 for the medium row.
- Supported context: the raw triad rows also record one sample and one study with share 1.00 for `ENVO:03600074` as the broad-scale term and `ENVO:00000033` as the local-scale term.
- Supported side-table context: `data/raw/gold_path_biosamples.tsv` has six biosamples for the same exact path, and `data/raw/gold_studies.tsv` lists study `Gs0161494` across five paths, including the parent `Crustaceans pond` path and this sediment child.
- Unsupported hierarchy: the generated edge from `habitatmech:GOLD.70845c6a72` to `habitatmech:GOLD.be4a7c95b6` is an inherited GOLD source-path edge. The raw MIxS roles instead place the child on the sediment medium and the parent path on an engineered pond site.
- Under-reviewed: the record has no item-level decision, authored definition, term request, record-level evidence, discussion, dataset, characteristic taxon, environmental parameter, or causal graph.

## Completeness

The committed source inventories are sufficient to explain the current generated record. The exact ignored-inclusive searches covering `data/raw/`, `data/habitats/`, `curation/`, `history/`, `research/`, `conf/`, `reports/yaml_record_review/`, and `curation/causal_graphs/` found no maintained item-level decision, no term request, and no target causal overlay to audit.

The empty optional slots are appropriate for a seeded GOLD path with no curated overlay: no maintained input currently provides characteristic taxa, environmental parameters, causal edges, or record-level cited evidence for the target. The raw MIxS triads should remain side-table context until a curator decides which terms describe identity, containment, and broader setting.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| Major | `Sediment` inherits `Crustaceans pond` as an `is_a` parent even though the child denotes sediment contained in the engineered pond, not a subtype of that pond. | The generated YAML has both `ENVO:00002007` and `habitatmech:GOLD.be4a7c95b6` in `parent_habitats`. The exact GOLD triad rows assign `sediment` to `env_medium`, `pond` to local scale, and `aquaculture farm` to broad scale for this source path, separating the material identity from the pond context. | `curation/decisions.tsv`; `curation/term_requests.tsv` |

## Recommended Edits

1. Item-review `habitatmech:GOLD.70845c6a72` in `curation/decisions.tsv` against the exact `Engineered > Artificial ecosystem > Aquaculture > Crustaceans pond > Sediment` source path, `ENVO:00002007` `sediment`, `ENVO:00000033` `pond`, `ENVO:03600074` `aquaculture farm`, and the already reviewed parent `habitatmech:GOLD.be4a7c95b6`.
2. If the item review keeps this as a narrower minted sediment concept under `ENVO:00002007`, add a definition in `curation/term_requests.tsv` whose genus is the true `sediment` material parent and use `parent_mode=REPLACE` so regeneration drops the inherited `Crustaceans pond` source-path parent.
3. Keep `ENVO:00000033` and `ENVO:03600074` out of `parent_habitats` unless a future maintained input has a non-`is_a` slot for local and broad MIxS context.

## Follow-up Checks

- Run `just seed`, then `just seed-canary habitatmech:GOLD.70845c6a72`, and inspect `data/habitats/engineered/sediment__79de4694.yaml` before any wider `just seed-apply --force`.
- Run `just validate data/habitats/engineered/sediment__79de4694.yaml`, `just validate-strict data/habitats/engineered/sediment__79de4694.yaml`, `just term-requests-check`, `just validate-history`, `just verify-corpus --max-diffs 1`, `just worklist --limit 2000`, `just report`, and `git diff --check` after maintained input changes.

## Additional Notes

The review did not find a target causal overlay, so there was no focused `just validate-causal curation/causal_graphs/<slug>.yaml` invocation to run. The whole-overlay validator still passed.

This review left generated products and curation inputs unchanged. Fixing the false parent belongs in the maintained decision and term-request inputs, followed by a normal seed regeneration.
