# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/bronchi.yaml`
- Started UTC: 2026-09-22T12:26:26Z
- Finished UTC: 2026-09-22T12:26:26Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.4885b989e7` |
| Label | `Bronchi` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv` through the automatic GOLD ambiguous-leaf route |
| Locked slug | `data/habitats/PATHS.tsv:1831` maps `habitatmech:GOLD.4885b989e7` to `bronchi` |

This is the GOLD path record for `Host-associated > Mammals > Respiratory system > Bronchi`. The generated YAML faithfully preserves it as a path-specific minted record with `skos:narrowMatch` to `UBERON:0002185` because several GOLD rows share the `Bronchi` leaf label. The leaf now needs item-level curation: the record has no maintained decision row, the matched ontology target is the exact anatomical class for bronchi, and the corpus already has a reviewed `UBERON:0002185` `bronchus` record carrying BacDive's bronchial-wash attestation.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/bronchi.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/bronchi.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The generated identifier, label, host-associated category, UBERON and GOLD parents, path lock, and GOLD source attestation are supported by maintained inputs:

- `data/raw/gold_ecosystem_paths.tsv:1058` is the raw GOLD row for this record. Its canonical path is `Host-associated > Mammals > Respiratory system > Bronchi`; its leaf label is `Bronchi`; it is a depth-4 path with 2 GOLD node ids, `gold.ecosystem:6835|gold.ecosystem:6836`, one organism assertion, and no study or biosample assertions.
- No `curation/decisions.tsv` row addresses `habitatmech:GOLD.4885b989e7`; the generated `NARROW` status comes from the unresolved ambiguous `Bronchi` leaf rather than from a curator-authored decision.
- `data/habitats/PATHS.tsv:1831` pins `habitatmech:GOLD.4885b989e7` to slug `bronchi`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:13131` provides `UBERON:0002185` `bronchus`, whose exact synonym list includes `bronchi`; `data/habitats/PATHS.tsv:1067` pins that exact ontology record to `bronchus`.
- `data/raw/isolation_source_groundings.tsv:52` maps BacDive `Bronchial-wash` to `UBERON:0002185` with `skos:closeMatch`, and `curation/decisions.tsv:1544` item-reviews that BacDive source concept because a bronchial wash samples the bronchus.
- `data/raw/bacdive_isolation_sources.tsv:119` and `data/raw/bacdive_source_taxa.tsv:396-413` feed the reviewed BacDive attestation and 18 characteristic-taxon rows in `data/habitats/host_associated/bronchus.yaml`.
- The source path parent `Host-associated > Mammals > Respiratory system` mints to `habitatmech:GOLD.fac4f63f3b`; `data/habitats/PATHS.tsv:3158` pins that parent to `respiratory_system__ef8617fd`, and `data/raw/gold_ecosystem_paths.tsv:149` is the raw parent row.

`grounding_status: NARROW` and `mapping_status: SEEDED` are reproducible seeder output, but they are not a reviewed anatomical decision. The exact `Bronchi` leaf has three same-depth GOLD claimants under Mammals, Mammals: Human, and Birds, so the automatic ambiguous-leaf guard correctly avoided collapsing them without item-level review. For this Mammals path, the source concept is not materially narrower than `UBERON:0002185`: it is an explicitly mammalian bronchus branch with one organism assertion and no child paths.

## Evidence

Every generated claim in this record is source-derived or inferred by the seeder:

| Claim | Nearest source | Review |
|---|---|---|
| Minted record identifier `habitatmech:GOLD.4885b989e7` | `src/habitatmech/seed.py`, `data/raw/gold_ecosystem_paths.tsv:1058` | Supported exactly by `mint("GOLD", canonical_path)` |
| Label `Bronchi`, host-associated category, GOLD source id, source path, 2-node note, one-organism count, and `ORGANISM` unit | `data/raw/gold_ecosystem_paths.tsv:1058` | Supported exactly |
| `NARROW` status, `skos:narrowMatch`, and parent `UBERON:0002185` | GOLD leaf-label matching in `src/habitatmech/seed.py`, backed by `data/raw/ontology_terms.tsv:13131` | Supported as an automatic ambiguity fallback; not item-reviewed |
| Parent `habitatmech:GOLD.fac4f63f3b` | GOLD parent-path emission in `src/habitatmech/seed.py`, backed by `data/raw/gold_ecosystem_paths.tsv:149` | Supported exactly |
| `SEEDED` mapping status and only a seed-from-sources history event | Absence of a `curation/decisions.tsv` row for `habitatmech:GOLD.4885b989e7` | Supported exactly |
| Locked filename | `data/habitats/PATHS.tsv:1831` | Supported exactly |

The record has no definition, synonyms, xrefs, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussion links, or datasets. Those absences are consistent with this unreviewed single-source GOLD record. Exact side-table lookups found no exact `Host-associated > Mammals > Respiratory system > Bronchi` row in `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_studies.tsv`, or `data/raw/gold_path_triads.tsv`.

## Completeness

The generated slots are faithful to the maintained inputs that feed this record, but one curation decision is missing:

- The sole `source_attestations` entry captures the complete target GOLD path, both upstream node ids through the multi-node note, and the sole GOLD organism assertion.
- The UBERON parent preserves the automatic `Bronchi` synonym match without letting the seeder conflate all GOLD `Bronchi` rows.
- The GOLD parent list preserves the exact source parent `Host-associated > Mammals > Respiratory system`, so the record remains browsable in the GOLD host hierarchy while unreviewed.
- The same-depth GOLD siblings at `data/raw/gold_ecosystem_paths.tsv:570` and `:1901` feed `habitatmech:GOLD.67ad6b3f36` and `habitatmech:GOLD.f7cc0ef1b4`; they explain why the seeder did not claim `UBERON:0002185` automatically, but they do not feed this target.
- The reviewed `UBERON:0002185` `bronchus` record already proves the corpus can merge bronchus-like sources onto that anatomical identity when a maintained decision confirms the habitat reading.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated target, excluding generated `build`, `data/text_map`, and `pages` trees. They found the target raw GOLD row, path lock, generated YAML, exact `UBERON:0002185` ontology row, BacDive bronchial-wash grounding and source rows, the reviewed `bronchus.yaml` record, the Mammals: Human and Birds `Bronchi` siblings, and a prior narrow-sample judgement for the Birds sibling; they found no target-specific maintained decision, term request, causal overlay, history record, research report, GOLD side-table row, or prior exact YAML review report.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Major | `Host-associated > Mammals > Respiratory system > Bronchi` remains a minted `NARROW` child of `UBERON:0002185` even though the exact matched ontology class is `bronchus`, its exact synonyms include `bronchi`, and the existing reviewed BacDive bronchial-wash source is already attached to the `UBERON:0002185` record. The generated state is a correct ambiguity fallback, but without an item-level decision it leaves a duplicate Mammals GOLD bronchus attestation outside the reviewed bronchus record. | Add an `ITEM`-depth `curation/decisions.tsv` row for `habitatmech:GOLD.4885b989e7`, most likely `GROUND UBERON:0002185 bronchus EXACT`, after confirming the Mammals GOLD path is the bronchial anatomy and not a distinct bronchial material or child airway concept. |

## Recommended Edits

- Add an item-level `curation/decisions.tsv` row for `habitatmech:GOLD.4885b989e7`. The expected disposition is `GROUND` to `UBERON:0002185` with exact relation because `Bronchi` is an exact synonym of the `bronchus` class and the GOLD path adds only the mammalian host context.
- Regenerate from maintained inputs with `just seed` and preview `UBERON:0002185` with `just seed-canary UBERON:0002185` before applying the full generated corpus.
- Confirm `data/habitats/host_associated/bronchi.yaml` is retired, the GOLD attestation from `gold.ecosystem:6835` lands on `data/habitats/host_associated/bronchus.yaml`, and no GOLD child source path becomes orphaned.

## Follow-up Checks

After adding the item-level Bronchi decision, re-run the same focused validation set and confirm the regenerated corpus merges this source into `UBERON:0002185`:

- `just validate data/habitats/host_associated/bronchus.yaml`
- `just validate-strict data/habitats/host_associated/bronchus.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

Also verify the retired `habitatmech:GOLD.4885b989e7` source concept appears in `bronchus.yaml` curation history and that `data/habitats/PATHS.tsv` no longer preserves a duplicate `bronchi` record.

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -name '*bronchi*' -print` found no pre-existing exact review report for this record before this file was written.
- Exact ignored/hidden-inclusive content searches for `Host-associated > Mammals > Respiratory system > Bronchi`, `habitatmech:GOLD.4885b989e7`, and `gold.ecosystem:6835` found only the maintained source and generated rows cited above for the target. The broader exact `Bronchi` sibling search found the Mammals: Human and Birds GOLD rows; those sibling rows share the same leaf label but do not feed this target.
