# YAML Record Review: Biofilm

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/biofilm__e72e4f05.yaml`
- Started UTC: 2026-09-21T21:31:11Z
- Finished UTC: 2026-09-21T21:31:19Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/biofilm__e72e4f05.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.748b381af4` |
| Label | `Biofilm` |
| Category | `AQUATIC` |
| Grounding | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained owner | generated from `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/PATHS.tsv`, and the GOLD parent-path hierarchy |

The record is a generated GOLD-only child for `Environmental > Aquatic > Freshwater > Lake > Biofilm`. It has no authored definition, synonyms, xrefs, environmental parameters, taxa, evidence items, causal graphs, discussions, external datasets, or item-level curation history.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/aquatic/biofilm__e72e4f05.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/biofilm__e72e4f05.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| Reference validator | Not available: `justfile` exposes no reference-specific validator, and this record has no `EvidenceItem`, `EnvironmentalParameter.reference`, `CharacteristicTaxon.reference`, or causal-edge evidence to target. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the committed ENVO term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 found, 0 missing, 0 extra, 0 differing. |

## Identity and Grounding

| Claim | Trace | Assessment |
|---|---|---|
| `habitatmech:GOLD.748b381af4` is the pinned identifier for `biofilm__e72e4f05`. | `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.748b381af4` to `biofilm__e72e4f05`. | Supported. |
| The GOLD source is Lake > Biofilm. | `data/raw/gold_ecosystem_paths.tsv` has the exact row `Environmental > Aquatic > Freshwater > Lake > Biofilm`, leaf label `Biofilm`, depth 5, and node id `gold.ecosystem:7640`; the YAML carries the same source id, label, and path. | Supported. |
| `ENVO:00002034` is the seeded ambiguous-leaf parent. | `data/raw/ontology_terms.tsv` has `ENVO:00002034` `biofilm`, and the Lake/Biofilm exact path appears deeper than the generic ontology-grounded biofilm source rows. `docs/HARMONIZATION.md` documents that deeper ambiguous GOLD leaves keep minted identifiers with the matched term as a broader parent instead of all claiming the same ontology identity. | Supported as a broader biofilm parent. |
| `ENVO:00000021` is inherited from the GOLD `Lake` parent path. | `data/habitats/PATHS.tsv` maps `ENVO:00000021` to the parent `freshwater_lake` record; `data/raw/ontology_terms.tsv` labels it `freshwater lake`. | Mechanically supported as source context, but false as an `is-a` parent because a lake biofilm is on or in a freshwater lake rather than a kind of freshwater lake. |
| `mapping_status: SEEDED` follows from maintained inputs. | Ignored-file-inclusive exact searches over `curation/decisions.tsv`, `curation/term_requests.tsv`, `history/`, and `curation/causal_graphs/` found no row for `habitatmech:GOLD.748b381af4`; the only curation-history event is the generated 2026-08-16 `seed_from_sources` event. | Supported. |

## Evidence

| Area | Status |
|---|---|
| Source attestation | Supported by the collapsed GOLD path row. The row reports one GOLD ecosystem node id and six GOLD organism assertions, so the record correctly reports `assertion_count: 6` and `assertion_unit: ORGANISM`. |
| GOLD API context | The later GOLD API inventories have 2 biosamples and 2 studies for the exact Lake > Biofilm path. Ignored-file-inclusive exact searches found no `data/raw/gold_path_triads.tsv` row for this exact path. |
| Definition | Empty. No curation decision or term request defines this path-qualified biofilm. |
| Synonyms and xrefs | Empty. No maintained xref or synonym row exists for this minted child. |
| Environmental parameters | Empty. The environments table has generic `ENVO:00002034` biofilm bands, but no maintained input provides Lake > Biofilm-specific physicochemical values. |
| Characteristic taxa | Empty. The record has no PREGO, BacDive, or Madin source attestation. |
| Causal graphs | Empty. `curation/causal_graphs/biofilm.yaml` belongs to the ontology-grounded `ENVO:00002034` generic biofilm record, not this Lake > Biofilm child. |
| Discussions and datasets | Empty. No maintained input currently targets this record. |

No evidence item is over-scoped because the record has no curator-authored evidence-bearing claim.

## Completeness

- An ignored-file-inclusive `find` found no pre-existing exact `*-biofilm__e72e4f05.md` report before this report was created.
- Ignored-file-inclusive exact searches for `habitatmech:GOLD.748b381af4`, `gold.ecosystem:7640`, `Environmental > Aquatic > Freshwater > Lake > Biofilm`, `ENVO:00000021`, and `ENVO:00002034` covered `data/raw`, `curation`, `history`, `research/habitats`, `reports/yaml_record_review`, `data/habitats/PATHS.tsv`, and the generated target.
- Those searches found the raw GOLD path, exact biosample, exact study, ontology, PATHS, and generated YAML rows for the Lake > Biofilm target; they found no item-level `curation/decisions.tsv` row, no term request, no target-owned causal overlay, no target-owned history record, no target-specific research report, and no prior exact YAML review.
- Exact sibling searches found `data/habitats/aquatic/lake_biofilm.yaml`, generated from `gold.ecosystem:7787` at `Environmental > Aquatic > Freshwater > Lake > Lake biofilm`. That sibling has 5 biosamples and a complete MIxS triad in the later GOLD API inventories, but its only decision is a class-level no-lexical-match `CONFIRM_UNGROUNDED` row.
- Broader `biofilm` searches found maintained curation for other path-qualified biofilm records. In particular, `habitatmech:GOLD.7f436f8aff` `air scrubber biofilm` already uses `ENVO:00002034` as the genus and `parent_mode=REPLACE` to drop its contextual Air scrubber source parent because the biofilm is inside the scrubber, not a kind of scrubber.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| HM-BIOFILM-E72E4F05-001 | Major | `Environmental > Aquatic > Freshwater > Lake > Biofilm` is still only a seeded ambiguous-leaf child, and its generated `ENVO:00000021` freshwater-lake parent is a contextual location rather than a true `is-a` parent. A lake biofilm is a biofilm in a freshwater lake, not a freshwater lake. | `data/raw/gold_ecosystem_paths.tsv` has `Environmental > Aquatic > Freshwater > Lake > Biofilm` as a child path of `Environmental > Aquatic > Freshwater > Lake`; `curation/decisions.tsv` has no item row for `habitatmech:GOLD.748b381af4`; `curation/term_requests.tsv` defines `air scrubber biofilm` under `ENVO:00002034` with `parent_mode=REPLACE` because its source parent is contextual. | `curation/decisions.tsv` and `curation/term_requests.tsv`. |
| HM-BIOFILM-E72E4F05-002 | Major | The Lake > Biofilm and Lake > Lake biofilm GOLD leaves are likely duplicate source concepts still published as two HabitatMech records. Both source paths sit under the same `Environmental > Aquatic > Freshwater > Lake` parent and differ only by whether the leaf repeats `Lake`; the existing `Lake biofilm` class-level row did not assess that duplicate boundary. | `data/raw/gold_ecosystem_paths.tsv` has sibling rows for `Environmental > Aquatic > Freshwater > Lake > Biofilm` and `Environmental > Aquatic > Freshwater > Lake > Lake biofilm`; `data/habitats/aquatic/lake_biofilm.yaml` keeps `habitatmech:GOLD.d3da9a2ed0` as a separate `UNGROUNDED` record; `curation/decisions.tsv` row `habitatmech:GOLD.d3da9a2ed0` is only `review_depth=CLASS`. | `curation/decisions.tsv`; possibly `curation/term_requests.tsv` after one survivor is selected. |

No blocker or minor findings.

## Recommended Edits

| Priority | Edit | Owner | Follow-up validator |
|---:|---|---|---|
| 1 | Compare `habitatmech:GOLD.748b381af4` against `habitatmech:GOLD.d3da9a2ed0`; if item review confirms they are the same freshwater-lake biofilm concept, merge one into the other with `SAME_AS` and choose one survivor for any definition. | `curation/decisions.tsv` | `just seed`, `just seed-canary habitatmech:GOLD.748b381af4 --force`, `just seed-canary habitatmech:GOLD.d3da9a2ed0 --force`, then inspect both generated outputs or the expected redirect. |
| 2 | Add an item-level decision for whichever minted lake-biofilm identity survives, confirming that it is a path-qualified biofilm rather than generic ENVO biofilm or a lake. | `curation/decisions.tsv` | `just validate-history` after recording the append-only curation session, then `just verify-corpus --max-diffs 1` after regeneration. |
| 3 | If item review confirms the lake-biofilm reading, add a term request whose genus is `ENVO:00002034` `biofilm` and whose `parent_mode` is `REPLACE`, so regeneration drops the inherited `ENVO:00000021` freshwater-lake parent. | `curation/term_requests.tsv` | `just term-requests-check`, `just validate-strict data/habitats/aquatic/biofilm__e72e4f05.yaml --quiet`, and `just verify-corpus --max-diffs 1`. |

## Follow-up Checks

- Re-run `just validate data/habitats/aquatic/biofilm__e72e4f05.yaml` on the regenerated target, or inspect its redirect if it is retired by `SAME_AS`.
- Re-run `just validate data/habitats/aquatic/lake_biofilm.yaml` if `habitatmech:GOLD.d3da9a2ed0` is the survivor or remains a separate record.
- Re-run `just validate-strict data/habitats/aquatic/biofilm__e72e4f05.yaml --quiet` to enforce closed-schema output.
- Re-run `just term-requests-check` after adding a lake-biofilm term request.
- Re-run `just validate-history` after adding the required append-only curation history.
- Re-run `just verify-corpus --max-diffs 1` to prove the generated YAML still reproduces from `data/raw/` and `curation/`.

## Additional Notes

- The generated YAML should remain read-only; the future fix belongs in `curation/decisions.tsv`, `curation/term_requests.tsv`, and the seed/regenerate workflow.
- GOLD's later API inventories give the exact Lake > Biofilm path 2 biosamples and two studies, and give the Lake > Lake biofilm sibling 5 biosamples plus one complete broad/local/medium triad. Those rows are useful duplicate-review context, but the source-attestation `assertion_count` still comes from `data/raw/gold_ecosystem_paths.tsv`.
