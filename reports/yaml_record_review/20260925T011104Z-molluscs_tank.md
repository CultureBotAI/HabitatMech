# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/engineered/molluscs_tank.yaml`
- Started UTC: `2026-09-25T01:11:04Z`
- Finished UTC: `2026-09-25T01:11:36Z`
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.f9af2e0686` |
| Label | `Molluscs tank` |
| Category | `ENGINEERED` |
| Grounding | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Maintained source | generated from `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/PATHS.tsv`, and the class-level row in `curation/decisions.tsv` |

Reviewed the complete generated record at `data/habitats/engineered/molluscs_tank.yaml`. The record is a GOLD-only engineered habitat for `Engineered > Artificial ecosystem > Aquaculture > Molluscs tank`, with one displayed `gold.ecosystem:8009` source attestation and two collapsed GOLD ecosystem node IDs in the raw inventory. The file is generated output; any future grounding or parent fix belongs in the maintained row for `habitatmech:GOLD.f9af2e0686`.

## Validation

| Check | Result |
|---|---|
| `find reports/yaml_record_review -maxdepth 1 -type f -name '*-molluscs_tank.md' -print` | Passed before this report was written; no existing exact `molluscs_tank` review was found. `find` is not `.gitignore`-filtered. |
| `just validate data/habitats/engineered/molluscs_tank.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/molluscs_tank.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Focused causal overlay validator | Not applicable; exact ignored-inclusive searches in `curation/causal_graphs/` found no `habitatmech:GOLD.f9af2e0686`, `gold.ecosystem:8009`, `gold.ecosystem:8019`, `gold.ecosystem:8010`, `molluscs_tank`, or exact source-path overlay. |
| Reference validator | Not applicable; the generated GOLD-only record has no record-level `evidence` entries, no causal graph, and no environmental-parameter references. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the generated ENVO term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; the corpus reproduces exactly. |
| `just worklist --limit 2000` | Passed; 0 ungrounded records remain undecided and 1,810 decisions are present. |
| `just report` | Passed; the corpus report completed for 3,206 records. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

The generated identifier, label, category, source attestation, and source-derived parent agree with the committed GOLD inventory. `data/raw/gold_ecosystem_paths.tsv` has the collapsed path `Engineered > Artificial ecosystem > Aquaculture > Molluscs tank`, with `depth` 4, `gold_node_count` 2, no direct organism, study, biosample, or total assertions, and `gold_node_ids` `gold.ecosystem:8009|gold.ecosystem:8019`. `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.f9af2e0686` to the stable `molluscs_tank` slug.

The current `UNGROUNDED` status is under-reviewed. The only maintained decision for `habitatmech:GOLD.f9af2e0686` is a 2026-08-12 class-level `CONFIRM_UNGROUNDED` row whose note explicitly says the habitat identity was not assessed. An exact ignored-inclusive search of `data/raw/ontology_terms.tsv` found no exact `molluscs tank`, `mollusc tank`, `mollusks tank`, `mollusk tank`, or `shellfish tank` term, but the vendored slice contains `ENVO:03600074` `aquaculture farm`, "An agricultural aquatic ecosystem in which fish, crustaceans, mollusks, aquatic plants and algae are cultivated." A molluscs tank is a narrower kind of aquaculture farm, so the source concept should be item-reviewed for a `GROUND_AS_PARENT` decision to `ENVO:03600074` unless a curator finds a still narrower term outside the current slice.

Two vendored ENVO aquaculture terms are near misses rather than identities. `ENVO:01001251` `mollusc farming process` and `ENVO:01001669` `mariculture process` are processes, not built or managed ecosystems. `ENVO:01001671` `open cage mariculture facility` is a facility term, but it is still too narrow for generic GOLD `Molluscs tank` because it requires open cages and marine water.

The current generated parent, `habitatmech:GOLD.0287e1b2a9`, points to the direct GOLD source-path parent `Engineered > Artificial ecosystem > Aquaculture`. The Aquaculture record itself is seeded `NARROW` under `ENVO:03600074`, but it is also still unreviewed. Its existing review found that `habitatmech:GOLD.0287e1b2a9` needs item-level review against the exact engineered Aquaculture source path and the same `ENVO:03600074` `aquaculture farm` broader term.

## Evidence

- Supported: the source path, source leaf, representative `gold.ecosystem:8009` source ID, two-node collapse, and zero direct assertion counts match `data/raw/gold_ecosystem_paths.tsv`.
- Supported: the raw `gold.ecosystem:8019` node is part of the same collapsed Molluscs tank GOLD path, which explains why the generated record displays the first node ID and carries the note that two GOLD ecosystem node IDs share the path.
- Supported: no direct GOLD biosample, MIxS triad, or study side table adds source-path-specific claims for `gold.ecosystem:8009`, `gold.ecosystem:8019`, or the anchored Molluscs tank source path; ignored-inclusive exact searches over `data/raw/` found only the canonical `gold_ecosystem_paths.tsv` row.
- Under-reviewed: the class-level `CONFIRM_UNGROUNDED` decision missed the vendored strict broader `ENVO:03600074` `aquaculture farm` term because the class sweep did not assess source-path habitat identity.
- No record-level citations, characteristic taxa, environmental parameters, or causal edges are present, so there are no attached snippets or literature references to validate.

## Completeness

The record is structurally sparse but not missing any required generated field. It has the expected GOLD source attestation and source-path parent for a Molluscs tank concept with no committed GOLD organism, biosample, triad, or study rows.

Bounded ignored-inclusive searches covered `data/habitats/`, `data/raw/`, `curation/`, `history/`, `conf/`, `reports/yaml_record_review/`, `pages/habitats/`, and `research/` for `habitatmech:GOLD.f9af2e0686`, `habitatmech:GOLD.a4fe9adfc7`, `gold.ecosystem:8009`, `gold.ecosystem:8019`, `gold.ecosystem:8010`, `sediment__0f723c05`, and `Engineered > Artificial ecosystem > Aquaculture > Molluscs tank`. They found the path lock, the generated target page, the class-level `curation/decisions.tsv` row, the canonical GOLD ecosystem-path rows for the target and child, and the generated Sediment child; they found no existing review report for this exact stem before this report was written and no item-level decision, term request, history entry, label-correspondence residual, auxiliary GOLD biosample/triad/study row, target-specific research report, or target-specific causal overlay.

The generated child at `data/habitats/engineered/sediment__0f723c05.yaml` inherits `habitatmech:GOLD.f9af2e0686` as a parent. That edge is only source-path-derived; because sediment contained in a molluscs tank is not itself a kind of molluscs tank, a later `sediment__0f723c05` review should decide whether the source-path parent needs suppression instead of preserving it by default.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `molluscs_tank.yaml` is still `UNGROUNDED` under a class-level sweep even though the vendored ontology contains a strict broader `ENVO:03600074` `aquaculture farm` parent. | The only maintained decision for `habitatmech:GOLD.f9af2e0686` has `review_depth` `CLASS` and says the concept's habitat identity was not assessed. Ignored-inclusive exact searches found no exact Molluscs tank ontology term in `data/raw/ontology_terms.tsv`, but did find `ENVO:03600074` `aquaculture farm`, which is broader than a molluscs tank and already the ontology parent of the generated Aquaculture source-path parent. | `curation/decisions.tsv` |

## Recommended Edits

1. Item-review `habitatmech:GOLD.f9af2e0686` in `curation/decisions.tsv` against the exact `Engineered > Artificial ecosystem > Aquaculture > Molluscs tank` path, `ENVO:03600074` `aquaculture farm`, `ENVO:01001251` `mollusc farming process`, `ENVO:01001669` `mariculture process`, `ENVO:01001671` `open cage mariculture facility`, and the existing generated `habitatmech:GOLD.0287e1b2a9` Aquaculture parent.
2. If no exact Molluscs tank ontology term is found, replace the class-level `CONFIRM_UNGROUNDED` row with an item-level `GROUND_AS_PARENT` row to `ENVO:03600074` `aquaculture farm`.
3. If a curator chooses to author an exact Molluscs tank term instead of only accepting the broader aquaculture farm parent, add the definition in `curation/term_requests.tsv` under `ENVO:03600074` with `parent_mode=ADD` so regeneration keeps the true source-path Aquaculture parent.

## Follow-up Checks

- `just seed`
- `just seed-canary habitatmech:GOLD.f9af2e0686`
- Inspect `data/habitats/engineered/molluscs_tank.yaml` and `data/habitats/engineered/sediment__0f723c05.yaml`
- `just validate data/habitats/engineered/molluscs_tank.yaml`
- `just validate-strict data/habitats/engineered/molluscs_tank.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

## Additional Notes

- No blocker finding was found: the current generated YAML validates, its GOLD source attestation is internally consistent, and the record denotes an engineered aquaculture habitat rather than a non-habitat.
- No minor findings were found.
