# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/host_associated/adrenal_gland.yaml
- Started UTC: 2026-09-23T16:28:45Z
- Finished UTC: 2026-09-23T16:33:09Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | BTO:0000047 |
| Label | adrenal gland |
| Class | HabitatRecord |
| Category | HOST_ASSOCIATED |
| Grounding status | EXACT |
| Mapping status | REVIEWED |
| Maintained owner | generated from the BTO ontology slice, `data/raw/gold_ecosystem_paths.tsv`, PREGO raw tables, `curation/decisions.tsv`, and `data/habitats/PATHS.tsv`; BTO subclass parents and GOLD source-path parents are added by `src/habitatmech/seed.py` |

[data/habitats/host_associated/adrenal_gland.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/host_associated/adrenal_gland.yaml) is the merged exact `BTO:0000047` record for adrenal gland, with two GOLD adrenal-gland source concepts and one PREGO/BTO source concept feeding the same record.

The target was resolved with ignored-file-inclusive searches:

- `find data/habitats -type f -name 'adrenal_gland.yaml' -print` found exactly one generated YAML record.
- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-adrenal_gland.md' -print` found no prior exact review report.
- `find curation/causal_graphs -maxdepth 1 -type f -name 'adrenal_gland.yaml' -print` found no causal-graph overlay for this record.
- `rg --no-ignore --hidden -n 'BTO:0000047|gold\.ecosystem:6551|gold\.ecosystem:6782|habitatmech:GOLD\.5b2265e44e|habitatmech:GOLD\.e9371b4536|habitatmech:PREGO\.a6ae0d69a2|Host-associated > Mammals(: Human)? > Endocrine system > Endocrine gland > Adrenal glands' data/raw curation data/habitats/PATHS.tsv history research reports -g '!reports/yaml_record_review/**' -g '!data/text_map/**' -g '!pages/**' -g '!build/**'` found the authoritative BTO, GOLD, PREGO, and curation-decision rows and no maintained causal overlay, history record, research report, or auxiliary report for this path outside the raw and curation inputs.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/host_associated/adrenal_gland.yaml` | pass; no LinkML issues found |
| `just validate-strict data/habitats/host_associated/adrenal_gland.yaml` | pass; 1 file scanned, 0 files with ERROR, 0 total ERROR rows |
| `just validate-causal curation/causal_graphs/adrenal_gland.yaml` | not applicable; exact `find curation/causal_graphs -maxdepth 1 -type f -name 'adrenal_gland.yaml' -print` found no overlay |
| `just validate-causal-all` | pass; 32 causal-graph curation files with 32 graphs validated |
| Reference validator | not applicable; this generated ontology/GOLD/PREGO record has no causal graph and no DOI, PMID, or URL evidence slots to inspect |
| `just term-requests-check` | pass; term-request table is current with 109 terms |
| `just validate-history` | pass; no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | pass; 3206 expected, 3206 found, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | pass; 0 ungrounded records still undecided; 1810 decisions on file |
| `just report` | pass; 3206 records; no global issue specific to `BTO:0000047` surfaced |
| `git diff --check` | pass |

## Identity and Grounding

The merged record identity is well supported:

- `data/raw/ontology_terms.tsv` gives `BTO:0000047` primary label `adrenal gland`, the BTO definition copied into the generated record, and no ontology synonyms in the vendored slice.
- `data/raw/prego_habitats.tsv` has a PREGO `BTO:0000047` row with `taxon_count` `2`, `direct_assertion_count` `2`, `max_prego_score` `3`, `annotated_genomes_isolates`, and the related synonyms emitted in the record.
- `data/raw/gold_ecosystem_paths.tsv` contains `Host-associated > Mammals: Human > Endocrine system > Endocrine gland > Adrenal glands` with `gold.ecosystem:6551`.
- `data/raw/gold_ecosystem_paths.tsv` also contains `Host-associated > Mammals > Endocrine system > Endocrine gland > Adrenal glands` with `gold.ecosystem:6782`.
- `data/habitats/PATHS.tsv` pins the shared resolved identifier `BTO:0000047` to slug `adrenal_gland`.

The source-concept decisions all target this generic BTO identity at item depth:

- `GOLD:Host-associated > Mammals: Human > Endocrine system > Endocrine gland > Adrenal glands` deterministically hashes to `habitatmech:GOLD.e9371b4536` and has a `GROUND` decision to exact `BTO:0000047`.
- `GOLD:Host-associated > Mammals > Endocrine system > Endocrine gland > Adrenal glands` deterministically hashes to `habitatmech:GOLD.5b2265e44e` and has a `GROUND` decision to exact `BTO:0000047`.
- `PREGO:BTO:0000047` deterministically hashes to `habitatmech:PREGO.a6ae0d69a2` and has an item-level `REVIEW` row endorsing the seeder's self-grounding.

Those three item-level rows explain `mapping_status: REVIEWED`: every source concept feeding this merged record has been manually read.

The two ontology parents are coherent. `data/raw/ontology_subclass_edges.tsv` asserts `BTO:0000047 rdfs:subClassOf BTO:0001488` and `BTO:0000047 rdfs:subClassOf BTO:0001491`; `data/raw/ontology_terms.tsv` labels those parents `endocrine gland` and `viscus`.

The two GOLD source-path parents are not coherent for the merged generic BTO record:

- `habitatmech:GOLD.c26b92ece6` denotes GOLD path `Host-associated > Mammals: Human > Endocrine system > Endocrine gland`.
- `habitatmech:GOLD.2f5a0b5455` denotes GOLD path `Host-associated > Mammals > Endocrine system > Endocrine gland`.
- Both are valid parent paths for their own GOLD child concepts, but after both child concepts resolve exactly to generic `BTO:0000047`, their host-scoped context parents become asserted as broader habitats of every adrenal gland. `BTO:0000047` is broader than a human adrenal gland, not narrower than a human endocrine-gland context.

## Evidence

This record has three source attestations and two PREGO taxon associations:

- The first `GOLD` attestation mirrors `data/raw/gold_ecosystem_paths.tsv` row `2240`: representative source node `gold.ecosystem:6551`, leaf `Adrenal glands`, path `Host-associated > Mammals: Human > Endocrine system > Endocrine gland > Adrenal glands`, one GOLD node, and zero organism, study, and biosample counts.
- The second `GOLD` attestation mirrors `data/raw/gold_ecosystem_paths.tsv` row `2044`: representative source node `gold.ecosystem:6782`, leaf `Adrenal glands`, path `Host-associated > Mammals > Endocrine system > Endocrine gland > Adrenal glands`, one GOLD node, and zero organism, study, and biosample counts.
- The `PREGO` attestation mirrors `data/raw/prego_habitats.tsv`: source id `BTO:0000047`, source label `adrenal gland`, two direct taxon assertions, score `3`, and the `annotated_genomes_isolates` evidence channel.
- `NCBITaxon:243365` and `NCBITaxon:331112` mirror ranks `1` and `2` in `data/raw/prego_habitat_taxa.tsv` for `BTO:0000047`, including score `3` and candidate pool `2`.

The record correctly does not emit GOLD `assertion_count` values because both exact GOLD paths have `organism_count`, `study_count`, `biosample_count`, and `total_assertions` equal to zero. Ignored/hidden-inclusive exact search found no rows for either GOLD adrenal-gland path in `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, or `data/raw/gold_studies.tsv`.

No evidence-backed causal mechanism evidence is present.

Unsupported or over-scoped claims: the host-scoped `Endocrine gland` source-path parents are not supported as strict broader habitats of generic `BTO:0000047`.

## Completeness

The record carries the source-owned BTO definition, PREGO synonyms, exact GOLD source labels, source paths, representative GOLD node ids, PREGO taxon assertions, BTO superclass parents, generated curation history entries from each item-level decision, and the generated `SEEDED_FROM_SOURCES` history event.

No curated term request, external definition, environmental-parameter row, or causal graph is expected for this exact BTO anatomy record. Exact ignored/hidden-inclusive searches covered the target BTO id, the two GOLD source ids, the two GOLD source paths, the three source-concept minted identifiers, the PREGO-associated NCBITaxon ids, the two BTO parent ids, the two GOLD source-path parent ids, the target slug, and the exact review-report/causal-overlay filenames. They found the cited BTO, GOLD, PREGO, decision, PATHS, and generated-record rows and no maintained overlay, history record, research report, triad, biosample, study, or parameter row that should already have changed this record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | `parent_habitats` incorrectly asserts host-scoped GOLD `Endocrine gland` contexts as broader habitats of generic `BTO:0000047`. | The record's identity is the generic BTO `adrenal gland` anatomy term. The generated GOLD parent records are for `Host-associated > Mammals > Endocrine system > Endocrine gland` and `Host-associated > Mammals: Human > Endocrine system > Endocrine gland`; each is only broader for the matching GOLD adrenal-gland source path before exact merge into the shared BTO record. `src/habitatmech/seed.py` still adds both source-path parents to `BTO:0000047`, making generic adrenal gland narrower than the human and mammal GOLD endocrine-gland records. | Add a maintained parent-edge override or teach `src/habitatmech/seed.py` to suppress host-scoped GOLD path parents that would be inherited by a generic ontology record after exact source-concept merges. |

## Recommended Edits

1. Add maintained support for suppressing the false `BTO:0000047 -> habitatmech:GOLD.2f5a0b5455` and `BTO:0000047 -> habitatmech:GOLD.c26b92ece6` source-path parent edges. The current generated-input model has source-concept decisions but no edge-level override for this GOLD parent-path inheritance, so the fix belongs either in a new maintained edge table or in `src/habitatmech/seed.py`.
2. Re-seed `BTO:0000047` after that maintained input exists and confirm the generated record keeps `BTO:0001488` and `BTO:0001491` as ontology parents while no longer claiming human- or mammal-scoped GOLD endocrine-gland records as strict broader habitats.
3. Screen other exact anatomy merges that share host-clade GOLD leaves for the same leak from a source-path parent into a generic ontology identity. Exact ignored-file-inclusive search for `habitatmech:GOLD.2f5a0b5455` and `habitatmech:GOLD.c26b92ece6` found the same parent ids on generated thyroid, parathyroid, hypophysis, thymus, and pineal-gland records.

## Follow-up Checks

The narrowest checks for the recommended fix are:

- `just seed`
- `just seed-canary BTO:0000047`
- inspect [data/habitats/host_associated/adrenal_gland.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/host_associated/adrenal_gland.yaml) and confirm the two false host-scoped GOLD endocrine-gland parents are gone
- `just validate data/habitats/host_associated/adrenal_gland.yaml`
- `just validate-strict data/habitats/host_associated/adrenal_gland.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

## Additional Notes

The parent-edge finding does not call the three item-level identity decisions wrong. The two GOLD leaf paths plausibly denote the same anatomical site as BTO adrenal gland, and the record preserves their human and mammal source contexts in `source_attestations`. The defect is specifically that `parent_habitats` is narrower after those exact host-scoped source concepts merge onto a host-generic ontology term.
