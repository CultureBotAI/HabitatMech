# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/freshwater__0ce4d27d.yaml
- Started UTC: 2026-09-25T04:58:00Z
- Finished UTC: 2026-09-25T05:02:03Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| LinkML class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.d482ddbf80` |
| Label | `Freshwater` |
| File | `data/habitats/engineered/freshwater__0ce4d27d.yaml` |
| Category | `ENGINEERED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Generated or maintained | Generated from `data/raw/` inventories by `scripts/seed_from_sources.py` |

The record is the GOLD path `Engineered > Artificial ecosystem > Microcosm > Freshwater`.
Its source row is `data/raw/gold_ecosystem_paths.tsv:1137`, a depth-4 row with
leaf label `Freshwater`, two collapsed GOLD ecosystem node IDs
`gold.ecosystem:8263|gold.ecosystem:8264`, and zero bulk-export organism,
study, biosample, and total assertion counts.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/freshwater__0ce4d27d.yaml` | Passed; LinkML reported no issues. |
| `just validate-strict data/habitats/engineered/freshwater__0ce4d27d.yaml` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current for 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against the vendored history schema. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 records, found 3206, with 0 missing, 0 extra, and 0 differing records. |
| `just worklist --limit 2000` | Passed; 0 ungrounded records remain undecided, with 1810 decisions on file. |
| `just report` | Passed; rebuilt the corpus report over 3206 records. |

No validator was skipped.

## Identity and Grounding

- The record's minted identifier `habitatmech:GOLD.d482ddbf80` is a generated,
  content-hashed GOLD identifier and is pinned to slug
  `freshwater__0ce4d27d` in `data/habitats/PATHS.tsv:2863`.
- The only upstream attestation is GOLD, with `source_id:
  gold.ecosystem:8263`, `source_label: Freshwater`, and `source_path:
  Engineered > Artificial ecosystem > Microcosm > Freshwater`. This agrees
  with `data/raw/gold_ecosystem_paths.tsv:1137`.
- The attestation note is consistent with the raw source row: two GOLD node IDs
  collapse to the same canonical path, and `scripts/seed_from_sources.py`
  intentionally records the first ID and points back to
  `data/raw/gold_ecosystem_paths.tsv` rather than emitting the full ID list.
- `mapping_predicate: skos:narrowMatch` and `grounding_status: NARROW` are
  appropriate for a depth-4 GOLD path whose leaf matched the broader ENVO
  material term `ENVO:00002011` `fresh water`. The committed ontology slice
  defines that parent at `data/raw/ontology_terms.tsv:7173`, and
  `data/raw/ontology_subclass_edges.tsv:5272` gives its ENVO superclass.
- The second broader parent, `ENVO:01000621` `microcosm`, is the exact grounded
  record for the parent GOLD path `Engineered > Artificial ecosystem >
  Microcosm`. The target's source-path parent is therefore preserved as a
  strict broader habitat.
- `mapping_status: SEEDED` is consistent with the absence of an item-level
  curation decision for `habitatmech:GOLD.d482ddbf80`; the lexical NARROW
  grounding is not represented as curator-reviewed.

## Evidence

- The record has no curator-authored `evidence` entries, causal graphs,
  environmental parameters, characteristic taxa, discussions, or datasets.
- No claim-level literature evidence is required for the generated GOLD source
  attestation itself. The attestation is backed by the committed raw GOLD path
  row.
- Ignored-inclusive exact searches of `data/raw/gold_studies.tsv`,
  `data/raw/gold_path_biosamples.tsv`, and `data/raw/gold_path_triads.tsv`
  for `Engineered > Artificial ecosystem > Microcosm > Freshwater` found no
  matching study, biosample, or MIxS triad rows that should have populated
  additional generated fields.

## Completeness

- The empty optional slots are acceptable for this zero-assertion raw GOLD
  path. There are no upstream organism counts, study counts, biosamples,
  MIxS triads, PREGO taxa, BacDive mappings, Madin associations, causal graph
  overlays, or curated datasets to represent.
- An ignored-inclusive exact search for `habitatmech:GOLD.d482ddbf80` across
  `data`, `curation`, `reports`, `history`, and `research` found only this
  generated record and its `PATHS.tsv` slug row before this report was added.
  That is consistent with an uncurated generated GOLD-only record.
- An ignored-inclusive exact search for
  `Engineered > Artificial ecosystem > Microcosm > Freshwater` across `data`,
  `curation`, `reports`, `history`, and `research` found only the raw GOLD row
  and this generated record before this report was added.
- `find reports/yaml_record_review -maxdepth 1 -type f -name
  '*-freshwater__0ce4d27d.md'` found no prior review report for this record;
  `find` included gitignored entries below `reports/yaml_record_review`.

## Findings

None found.

## Recommended Edits

None found.

## Follow-up Checks

None found.

## Additional Notes

- The parent-path record `data/habitats/engineered/microcosm.yaml` currently
  grounds `Engineered > Artificial ecosystem > Microcosm` exactly to
  `ENVO:01000621` and preserves the `Engineered > Artificial ecosystem` source
  path as a second broader parent.
- The broad `Artificial ecosystem` parent record is still a seeded
  class-level term-request concept, but that upstream review gap does not make
  this Microcosm/Freshwater child internally inconsistent.
