# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/bone_marrow.yaml`
- Started UTC: 2026-09-22T06:37:38Z
- Finished UTC: 2026-09-22T06:37:38Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `BTO:0000141` |
| Label | `bone marrow` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the PREGO inventory and the vendored BTO slice; a future item-level review belongs in `curation/decisions.tsv`, and future mechanism evidence belongs in `curation/causal_graphs/*.yaml` |
| Locked slug | `data/habitats/PATHS.tsv:28` maps `BTO:0000141` to `bone_marrow` |

This is the PREGO-attested exact BTO record for `bone marrow`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/bone_marrow.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/bone_marrow.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was staged |

## Identity and Grounding

The generated exact BTO identity, PREGO attestation, and locked filename are supported by committed inventory inputs:

- `data/habitats/PATHS.tsv:28` pins `BTO:0000141` to slug `bone_marrow`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:143` defines `BTO:0000141` with canonical label `bone marrow`, a soft fatty vascular tissue definition, `directly_referenced: TRUE`, and `label_only: FALSE`.
- `data/raw/prego_habitats.tsv:400` is the exact raw PREGO `BTO:0000141` row, classifies the term as a `BTO` `biolink:GrossAnatomicalStructure`, records 4 taxa, `max_prego_score` 3, evidence channel `annotated_genomes_isolates`, and all 12 PREGO labels that appear as the generated primary label plus related synonyms.
- `data/raw/prego_habitat_taxa.tsv:159` through `data/raw/prego_habitat_taxa.tsv:162` are the complete ranked PREGO taxa for `BTO:0000141`.
- The PREGO source-concept hash matches the seeder's route: `sha1("PREGO:BTO:0000141")[:10]` is `fa8211230c`.

`grounding_status: EXACT` is consistent with PREGO directly using `BTO:0000141` as its source identifier, and `mapping_status: SEEDED` is expected because exact ignored/hidden-inclusive searches found no item-level `curation/decisions.tsv` row for `habitatmech:PREGO.fa8211230c`.

## Evidence

Every generated scientific claim in this record is source-derived:

| Claim | Nearest source | Review |
|---|---|---|
| BTO identifier, canonical label, and BTO definition | `data/raw/ontology_terms.tsv:143` | Supported exactly |
| PREGO `source_id: BTO:0000141`, `source_label: bone marrow`, 4-taxon attestation, score `3.0`, `TAXON` unit, and `annotated_genomes_isolates` evidence channel | `data/raw/prego_habitats.tsv:400` | Supported exactly |
| PREGO related synonyms | `data/raw/prego_habitats.tsv:400` | Supported; the generated related synonyms are the non-primary labels from PREGO's pipe-delimited label set |
| Four characteristic-taxon associations, all with score `3.0`, ranks 1-4, and candidate pool 4 | `data/raw/prego_habitat_taxa.tsv:159` through `data/raw/prego_habitat_taxa.tsv:162` | Supported exactly; no entry overclaims `is_characteristic` |

The record has no parent habitats, xrefs, environmental parameters, record-level evidence, causal graphs, discussion links, or datasets. Exact ignored/hidden-inclusive searches found no `BTO:0000141` row in `curation/decisions.tsv` or `curation/term_requests.tsv`, and the only `data/raw/ontology_subclass_edges.tsv` rows for `BTO:0000141` are child edges from `BTO:0000635` and `BTO:0001160`, not parent edges from the target.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- The sole `source_attestations` entry captures PREGO's complete source identifier, primary label, 4-taxon count, maximum score, and evidence channel.
- The `characteristic_taxa` list captures all four PREGO taxon rows for `BTO:0000141`; the candidate pool of 4 makes each rank fully interpretable.
- The record correctly omits `parent_habitats` because the vendored BTO slice has no outgoing `rdfs:subClassOf` row for `BTO:0000141`.
- The record has no authored causal graph. Exact ignored/hidden-inclusive searches found no maintained `curation/causal_graphs/` overlay for `bone_marrow`, `BTO:0000141`, or `habitatmech:PREGO.fa8211230c`.
- Exact ignored/hidden-inclusive searches found no target-specific `history/`, `research/`, `reports/`, or `curation/term_requests.tsv` artifact for this record before this report was written.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated target. They found the expected PREGO rows, BTO row, two generated GOLD sibling slugs, and unrelated research prose mentions of bone marrow, but no maintained input that changes the reviewed PREGO record.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if a future curation change adds an item-level decision, PREGO extraction change, BTO slice change, or causal graph overlay for this record:

- `just validate data/habitats/host_associated/bone_marrow.yaml`
- `just validate-strict data/habitats/host_associated/bone_marrow.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If future curation signs off the exact PREGO source concept, add the maintained item-level row for `habitatmech:PREGO.fa8211230c` to `curation/decisions.tsv`, run `just seed`, preview `just seed-canary BTO:0000141`, and rerun the corpus-level checks after regeneration.

## Additional Notes

- `find reports/yaml_record_review -name '*bone_marrow*' -print` found no pre-existing exact review report for this record before this file was written.
- `find curation history research reports -name '*bone_marrow*' -o -name '*fa8211230c*' -o -name '*0000141*' -o -name '*BTO_0000141*'` found no maintained target-specific decision-adjacent, history, research, or report artifacts before this file was written.
- `data/habitats/PATHS.tsv` also contains generated GOLD sibling slugs `bone_marrow__9387f1a6` and `bone_marrow__c1fc5dec`; those are separate GOLD path records and do not feed this exact PREGO/BTO target.
