# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/engineered/biofilm__59810f2b.yaml`
- Started UTC: 2026-09-24T16:11:40Z
- Finished UTC: 2026-09-24T16:14:58Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.0759307c55` |
| Label | `Biofilm` |
| Category | `ENGINEERED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Generated path | `data/habitats/engineered/biofilm__59810f2b.yaml` |
| Source concept | GOLD `Engineered > Bioremediation > Thiocyanate > Biofilm` |

The target is the path-qualified GOLD `Biofilm` leaf under engineered thiocyanate bioremediation. It is not the generic ontology-grounded `ENVO:00002034` `biofilm` record and not the curated `air scrubber biofilm` sibling `habitatmech:GOLD.7f436f8aff`.

The file is generated. `data/habitats/PATHS.tsv:1323` maps `habitatmech:GOLD.0759307c55` to the stable `biofilm__59810f2b` slug, so future fixes belong in `curation/decisions.tsv`, `curation/term_requests.tsv`, or seeder support for source-path parent suppression rather than in this YAML.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/biofilm__59810f2b.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/biofilm__59810f2b.yaml` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| Target causal overlay validator | Not applicable; an ignored-inclusive exact search over `curation/causal_graphs` for the target identifier, exact GOLD path, source node IDs, and `biofilm__59810f2b` slug found no target overlay. |
| `just validate-causal curation/causal_graphs/biofilm.yaml` | Passed for the generic `ENVO:00002034` biofilm overlay found by the broader Biofilm search. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files and 32 graphs validated. |
| Reference validator | Not applicable; this generated child record has no record-level `evidence` or `causal_graphs`, and HabitatMech has no dedicated per-record reference check. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 records were expected, 3206 were found, and `data/habitats/` reproduced exactly from `data/raw/`. |
| `just worklist --limit 2000` | Passed; 0 ungrounded records remain undecided and 1810 decisions are on file. |
| `just report` | Passed; the corpus report regenerated successfully. |
| `git diff --check` | Passed. |

## Identity and Grounding

`data/raw/gold_ecosystem_paths.tsv:1238` is the maintained GOLD row for this exact source concept. It has canonical path `Engineered > Bioremediation > Thiocyanate > Biofilm`, source leaf `Biofilm`, depth `4`, two collapsed GOLD node IDs, and zero direct organism, project, biosample, or study assertions in the ecosystem-path inventory.

The generated source attestation mirrors that row with GOLD `gold.ecosystem:5497`, source label `Biofilm`, source path `Engineered > Bioremediation > Thiocyanate > Biofilm`, `mapping_predicate: skos:narrowMatch`, and a note that two GOLD ecosystem node IDs share this path.

The `NARROW` status is mechanically plausible for an ambiguous GOLD `Biofilm` leaf: `ENVO:00002034` is present in `data/raw/ontology_terms.tsv:7184`, and several deeper GOLD `Biofilm` leaves use it as a broader parent rather than claiming exact identity. This target has no item-level `curation/decisions.tsv` row, so that plausible broader biofilm grounding remains unreviewed for the thiocyanate-specific path.

The second generated parent, `habitatmech:GOLD.a006851e0a`, is the immediate GOLD source-path prefix `Engineered > Bioremediation > Thiocyanate`. The YAML review in `reports/yaml_record_review/20260924T155643Z-thiocyanate.md` found that this parent still has only a class-level `CONFIRM_UNGROUNDED` decision, so this child currently inherits an unreviewed contextual contaminant parent.

## Evidence

The generated target has no record-level `evidence`, no `environmental_parameters`, no `characteristic_taxa`, and no target-specific `causal_graphs`.

Supported:

- The source-path identity and two-node collapse are reproducible from `data/raw/gold_ecosystem_paths.tsv:1238`.
- `ENVO:00002034` `biofilm` is a broader class for this path-qualified GOLD `Biofilm` child.
- The absence of target-specific direct GOLD biosample, study, or triad support is reproducible from ignored-inclusive exact searches over `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_studies.tsv`, and `data/raw/gold_path_triads.tsv`.

Unsupported or over-scoped:

- No item-level decision has inspected whether the exact thiocyanate child denotes a distinct thiocyanate bioremediation biofilm, an unused GOLD grouping leaf, or the same concept as an adjacent thiocyanate-remediating biofilm class that should be merged later.
- No maintained input currently shows that a thiocyanate bioremediation biofilm is a strict subclass of the upstream `Thiocyanate` branch rather than a biofilm growing in a thiocyanate bioremediation setting.

Related curation:

- `curation/term_requests.tsv:76` defines the already-reviewed `air scrubber biofilm` as a child of `ENVO:00002034` and uses `parent_mode=REPLACE` to drop the inherited source-path `Air scrubber` parent because the biofilm is inside the scrubber, not a kind of scrubber.
- `curation/causal_graphs/biofilm.yaml` belongs to `ENVO:00002034`, not this path-qualified child.

## Completeness

Consequential gaps:

- The target has no item-level decision or term request.
- The exact child path has zero direct GOLD auxiliary biosamples, triads, and study rows, so any future definition will probably have to reason from the 23 direct biosamples on the parent `Engineered > Bioremediation > Thiocyanate` path or from external thiocyanate-biofilm literature.
- The inherited `Thiocyanate` parent is unresolved and probably contextual rather than strict.

Bounded searches:

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-biofilm__59810f2b.md' -print` found no earlier YAML review report for this exact stem. `find` is not gitignore-filtered.
- An ignored-inclusive exact search over `curation`, `history`, `research`, `data/habitats/PATHS.tsv`, `data/habitats/engineered`, `reports/yaml_record_review`, and `conf/id_label_targets.yaml` for `habitatmech:GOLD.0759307c55`, `gold.ecosystem:5497`, `gold.ecosystem:5498`, `biofilm__59810f2b`, and the exact source path found only the path lock, the generated target, the raw GOLD path row, one adjacent air-scrubber Biofilm research mention, and the immediately preceding Thiocyanate review; it found no target-specific decision row, term request, term-request exclusion, history record, deep-research report, or id-label residual.
- An ignored-inclusive exact search of `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, and `data/raw/gold_studies.tsv` found no direct auxiliary GOLD rows for `Engineered > Bioremediation > Thiocyanate > Biofilm`.
- Broader ignored-inclusive `ENVO:00002034` and `biofilm` searches found the generic ontology term, the generic `curation/causal_graphs/biofilm.yaml` overlay, several curated `Microfouling/Biofilm` decisions, and the reviewed `air scrubber biofilm` term request, but no maintained curation for this exact target.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The exact thiocyanate `Biofilm` child has no item-level review, so `mapping_status: SEEDED`, the minted identifier, and the retained source-path parents are still unreviewed for this source concept. | Exact ignored-inclusive searches found no `curation/decisions.tsv`, `curation/term_requests.tsv`, or `curation/term_requests_excluded.tsv` row for `habitatmech:GOLD.0759307c55`; the generated curation history contains only `SEEDED_FROM_SOURCES`. | `curation/decisions.tsv`; if the child is retained as a novel path-qualified biofilm, `curation/term_requests.tsv`. |
| Major | The inherited `habitatmech:GOLD.a006851e0a` `Thiocyanate` parent is likely a contextual contaminant branch, not a strict parent of a biofilm. | The exact GOLD path is `Engineered > Bioremediation > Thiocyanate > Biofilm`: its leaf says the habitat is a `Biofilm`, and its parent path says the biofilm sits under GOLD's thiocyanate bioremediation branch. The reviewed `air scrubber biofilm` row in `curation/term_requests.tsv:76` already uses `parent_mode=REPLACE` to drop an inherited device parent for the same "biofilm inside X" pattern. | `curation/term_requests.tsv`, or future source-path parent suppression support in `src/habitatmech/seed.py`. |

## Recommended Edits

1. Add an item-level row for `habitatmech:GOLD.0759307c55` to `curation/decisions.tsv`. If this exact source concept is a real thiocyanate bioremediation biofilm, use `GROUND_AS_PARENT` to attach `ENVO:00002034` as the verified broader biofilm term.
2. If retained, define a path-qualified thiocyanate bioremediation biofilm in `curation/term_requests.tsv` under `ENVO:00002034` `biofilm`, using `parent_mode=REPLACE` if item review confirms the inherited `Thiocyanate` parent is contextual.
3. Review this child together with `habitatmech:GOLD.a006851e0a` `Thiocyanate`, `habitatmech:GOLD.7a8691bc23` `Thiocyanate-remediating`, and `habitatmech:GOLD.23007d6432` `Cyanide/thiocyanate` to avoid minting multiple equivalent thiocyanate-remediation biofilm/material records.

## Follow-up Checks

- After adding a decision or term request, run `just seed`, `just seed-canary habitatmech:GOLD.0759307c55`, inspect `data/habitats/engineered/biofilm__59810f2b.yaml`, then run `just seed-apply --force`.
- Run `just validate data/habitats/engineered/biofilm__59810f2b.yaml` and `just validate-strict data/habitats/engineered/biofilm__59810f2b.yaml`.
- Run `just term-requests-check`, `just validate-history`, `just verify-corpus --max-diffs 1`, `just worklist --limit 2000`, `just report`, and `git diff --check`.
- Run `just validate-causal-all` if any overlay changes.

## Additional Notes

- The generic `ENVO:00002034` biofilm mechanism overlay can inform future curation, but it is not a target-specific thiocyanate bioremediation mechanism and does not prove that the `Thiocyanate` parent is an is-a parent.
- The committed air-scrubber Biofilm deep-research report mentions this source path only as a sibling ambiguous Biofilm leaf; it is not evidence for this target's definition.
