# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/buccal_mucosa.yaml`
- Started UTC: 2026-09-22T16:16:20Z
- Finished UTC: 2026-09-22T16:16:51Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `BTO:0003833` |
| Label | `buccal mucosa` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Maintained owner | Raw PREGO and ontology rows only; no item-level curation overlay is present |
| Locked slug | `data/habitats/PATHS.tsv:376` maps `BTO:0003833` to `buccal_mucosa` |

This is the generated PREGO self-grounded record for the BTO anatomical class `buccal mucosa`. The generated YAML faithfully preserves the BTO label and definition, records PREGO's one direct taxon assertion, carries the two additional PREGO spellings as related synonyms, and leaves the GOLD mammal and human `Buccal mucosa` source paths in separate narrower generated records because those GOLD paths are host- and path-qualified.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/buccal_mucosa.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/buccal_mucosa.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The generated identifier, label, definition, category, grounding status, source attestation, PREGO synonyms, characteristic taxon, and seed status are supported by maintained inputs:

- `data/raw/ontology_terms.tsv:3833` is the imported BTO row for `BTO:0003833`, with label `buccal mucosa`, definition `The inner lining of the cheeks and lips.`, and no BTO synonym strings. Those fields exactly match the target's identifier, label, definition, and `definition_source`.
- `data/raw/prego_habitats.tsv:642` is the PREGO habitat row for `BTO:0003833`. It classifies the term as `biolink:GrossAnatomicalStructure`, has one direct taxon assertion, records maximum PREGO score 3, records `annotated_genomes_isolates`, and supplies the synonym string `buccal mucosa|buccal mucosal|buccal mucosas`.
- `data/raw/prego_habitat_taxa.tsv:2787` is the single PREGO taxon row for this term, ranking `NCBITaxon:388919` `Streptococcus sanguinis SK36` at score 3 with direct evidence from `annotated_genomes_isolates`.
- `src/habitatmech/seed.py`'s `ingest_prego` transform keeps PREGO concepts self-grounded, labels them from the target ontology, adds PREGO synonym strings as `RELATED_SYNONYM`, emits `TAXON` assertion counts from `taxon_count`, and emits characteristic taxa with score, rank, and candidate pool values from the PREGO taxon table.
- `data/habitats/PATHS.tsv:376` pins the generated target to `data/habitats/host_associated/buccal_mucosa.yaml`.

The exact identifier is a BTO class instead of the exact same UBERON anatomical class because the PREGO source row is already keyed to `BTO:0003833`. `data/raw/ontology_terms.tsv:13316` also imports `UBERON:0006956` with the same label and definition, but no maintained decision redirects the PREGO BTO source term to that UBERON id.

The two same-label GOLD records are correctly separate from this generic BTO target:

- `data/raw/gold_ecosystem_paths.tsv:846` is `Host-associated > Mammals > Digestive system > Oral cavity > Buccal mucosa`, which generates the narrower `habitatmech:GOLD.4caaf1fb4f` record with parents `UBERON:0006956` and the GOLD `Oral cavity` path.
- `data/raw/gold_ecosystem_paths.tsv:2218`, `data/raw/gold_path_biosamples.tsv:159`, and `data/raw/gold_studies.tsv:378`, `:2102`, and `:2209` are for `Host-associated > Mammals: Human > Digestive system > Oral cavity > Buccal mucosa`, which generates the narrower `habitatmech:GOLD.1a8d5400cc` record with parents `UBERON:0006956` and the GOLD human `Oral cavity` path.

## Evidence

Every generated claim is traceable to a raw row or maintained transform:

| Claim | Nearest source | Review |
|---|---|---|
| Identifier `BTO:0003833` | PREGO self-grounding from `data/raw/prego_habitats.tsv:642` | Supported exactly |
| Label `buccal mucosa`, definition, and `definition_source: BTO` | `data/raw/ontology_terms.tsv:3833` | Supported exactly |
| Category `HOST_ASSOCIATED` | `biolink:GrossAnatomicalStructure` plus the host-associated ontology route in PREGO ingestion | Supported exactly |
| Synonyms `buccal mucosal` and `buccal mucosas` | PREGO synonym string in `data/raw/prego_habitats.tsv:642` | Supported exactly; the label duplicate `buccal mucosa` is correctly not repeated as a synonym |
| PREGO `source_id`, `source_label`, one taxon assertion, score 3, and `annotated_genomes_isolates` evidence channel | `data/raw/prego_habitats.tsv:642` | Supported exactly |
| `NCBITaxon:388919` `Streptococcus sanguinis SK36`, score 3, rank 1, and candidate pool 1 | `data/raw/prego_habitat_taxa.tsv:2787` | Supported exactly |
| Absence of parent habitats, environmental parameters, causal graphs, record-level evidence, discussion, and datasets | No maintained input row or overlay found for these slots | Supported by bounded exact searches |

The same PREGO strain appears under the generated `dental_plaque`, `pharyngeal_mucosa`, and `pharynx` records, but each of those records has its own source row in `data/raw/prego_habitat_taxa.tsv`; no unsupported cross-record taxon leakage is visible in this target.

## Completeness

The generated fields are complete for the maintained inputs that currently feed this record:

- The PREGO source attestation captures the source id, source label, taxon assertion count, assertion unit, maximum score, and evidence channel.
- The generated synonyms carry PREGO's two non-label lexical variants and omit only the exact label duplicate.
- The single characteristic taxon row captures the PREGO taxon id, label, score, rank, and one-term candidate pool.
- No item-level decision, authored term request, target-specific causal graph overlay, append-only history record, dedicated deep-research report, term-request exclusion row, BacDive row, Madin row, GOLD row for the exact BTO id, or previous exact YAML review report exists for `BTO:0003833`.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `curation/causal_graphs`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated buccal mucosa records, excluding generated `build`, `data/text_map`, and `pages` trees. They found the raw BTO ontology row, PREGO habitat row, PREGO taxon row, locked path row, the exact generated BTO record, the same-label UBERON term, the two narrower GOLD Buccal mucosa paths, three GOLD side-table rows for the human path, and three additional PREGO rows that reuse `NCBITaxon:388919`; they found no target-specific maintained artifact beyond the raw, path-lock, and generated rows cited above.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

No target-specific follow-up is required. If upstream PREGO or ontology inputs change for `BTO:0003833`, regenerate the target and re-run:

- `just validate data/habitats/host_associated/buccal_mucosa.yaml`
- `just validate-strict data/habitats/host_associated/buccal_mucosa.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -name '*buccal_mucosa*' -print` found no pre-existing exact review report for this record before this file was written.
- Exact ignored/hidden-inclusive content searches for `BTO:0003833`, `buccal_mucosa`, `buccal mucosa`, `UBERON:0006956`, `habitatmech:GOLD.4caaf1fb4f`, `habitatmech:GOLD.1a8d5400cc`, `Host-associated > Mammals > Digestive system > Oral cavity > Buccal mucosa`, `Host-associated > Mammals: Human > Digestive system > Oral cavity > Buccal mucosa`, and `NCBITaxon:388919` found no unsupported assertion, target-specific curation overlay, or prior exact review.
