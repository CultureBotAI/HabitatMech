# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/biofilm__5327749c.yaml
- Started UTC: 2026-09-25T13:22:00Z
- Finished UTC: 2026-09-25T13:26:17Z
- Verdict: needs curation

## Target

Reviewed the complete generated record at `data/habitats/engineered/biofilm__5327749c.yaml`. The record is the GOLD-only engineered child for `Engineered > Artificial ecosystem > Water channel system > Indoor > Biofilm`, currently carried as the minted identifier `habitatmech:GOLD.905d58ea72`.

The generated file has `label: Biofilm`, `habitat_category: ENGINEERED`, `grounding_status: NARROW`, and `mapping_status: SEEDED`. Its generated parents are `ENVO:00002034` `biofilm`, from the ambiguous GOLD leaf match, and `habitatmech:GOLD.b4e93f5d66` `indoor artificial water channel system`, from the immediate GOLD source-path parent. Its only source attestation is `gold.ecosystem:8494` with `mapping_predicate: skos:narrowMatch`.

The file is generated from `data/raw/` and seeded path logic. No `curation/decisions.tsv` row, authored term request, source extractor override, or target causal-graph overlay currently contributes to this record. Future item review belongs in `curation/decisions.tsv`; a future path-qualified label, definition, or parent replacement belongs in `curation/term_requests.tsv`; future causal overlays belong under `curation/causal_graphs/`.

## Validation

All required local checks passed:

- `just validate data/habitats/engineered/biofilm__5327749c.yaml`
- `just validate-strict data/habitats/engineered/biofilm__5327749c.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000 --status all --out /tmp/habitatmech-water-channel-biofilm-worklist.tsv`
- `just report`

No validator was skipped.

## Identity and Grounding

The generated identifier, label, category, source attestation, and path slug agree with the maintained inputs. `data/raw/gold_ecosystem_paths.tsv` has the exact `Engineered > Artificial ecosystem > Water channel system > Indoor > Biofilm` row at depth 5, with one GOLD node ID, zero `organism_count`, `study_count`, `biosample_count`, and `total_assertions` values in this bulk-export summary, and `gold.ecosystem:8494` as the node list. `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.905d58ea72` to the stable `biofilm__5327749c` slug.

The `ENVO:00002034` parent is a plausible broader term. The vendored ontology slice defines `ENVO:00002034` with canonical label `biofilm`, and the GOLD source path adds the indoor water-channel context under that generic leaf. `docs/HARMONIZATION.md` documents that deeper repeated GOLD leaves keep a minted identifier and use the matched leaf term as a broader parent instead of claiming the ontology term as an exact identity for every context-specific child.

The inherited `habitatmech:GOLD.b4e93f5d66` `indoor artificial water channel system` parent is not defensible as a strict `is-a` parent. The raw row places this `Biofilm` leaf under the reviewed Indoor water-channel system, and the committed Indoor research report reads benthic biofilm on channel substrata as the canonical sampled material in experimental stream work, but an attached biofilm inside an indoor artificial water-channel system is not itself a kind of indoor artificial water-channel system. This is the same path-qualified Biofilm pattern that already led the curated air-scrubber Biofilm term request to use `ENVO:00002034` as the genus and `parent_mode=REPLACE` to drop its inherited equipment parent.

This target has no item-level decision. Exact ignored-inclusive searches found no `habitatmech:GOLD.905d58ea72`, `gold.ecosystem:8494`, `source_id: gold.ecosystem:8494`, or exact path-specific term request in maintained curation inputs.

## Evidence

- Supported as generated source provenance: exact ignored-inclusive searches found `gold.ecosystem:8494` and the exact tab-delimited `Engineered > Artificial ecosystem > Water channel system > Indoor > Biofilm` row in `data/raw/gold_ecosystem_paths.tsv`.
- Supported as broader ontology context: `ENVO:00002034` is present in the vendored ontology slice as `biofilm`, and a path-qualified GOLD Biofilm child is narrower than the generic ontology class.
- Supported as generated context but not as a strict parent: `habitatmech:GOLD.b4e93f5d66` is the immediate parent source path, which explains why it is generated, but the GOLD containment/context path has not been item-reviewed into a true `parent_habitats` claim.
- Not yet supported as individually reviewed habitat identity: no item-level `curation/decisions.tsv` row decides whether this source concept should stay as a specific indoor water-channel biofilm, become a different exact ontology grounding, or be handled some other way.

The record has no definition, environmental parameters, characteristic taxa, claim-level `EvidenceItem` objects, causal graphs, discussions, or datasets to inspect.

## Completeness

The generated source attestation is complete for the committed GOLD ecosystem inventory. `data/raw/gold_ecosystem_paths.tsv` lists a single source node ID for this exact path, so no generated note is needed to account for collapsed sibling nodes.

Exact ignored-inclusive searches of the GOLD API side tables found no `gold.ecosystem:8494` and no exact tab-delimited `Engineered > Artificial ecosystem > Water channel system > Indoor > Biofilm` row in `data/raw/gold_studies.tsv`, `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, or `data/raw/environment_parameters.tsv`; no GOLD study, biosample, MIxS triad context, or environmental-parameter row is committed for this exact path.

Before this report was written, exact ignored-inclusive searches over `history/`, `research/habitats/`, `curation/causal_graphs/`, `curation/term_requests.tsv`, `curation/term_requests/`, `curation/term_requests_excluded.tsv`, `conf/id_label_targets.yaml`, and `reports/yaml_record_review/` found no target-specific append-only history record, causal overlay, authored term request, generated ENVO term-request row, term-request exclusion, label-correspondence residual, or prior exact review report for `habitatmech:GOLD.905d58ea72`, `gold.ecosystem:8494`, or the exact source path. The Indoor water-channel research report mentions this child record while reviewing its parent; it is useful context but not a target-specific curation input. Before this report was written, `find reports/yaml_record_review -maxdepth 1 -type f -name '*biofilm__5327749c*.md'` found no prior review report for this exact generated stem.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `Engineered > Artificial ecosystem > Water channel system > Indoor > Biofilm` is still only a seeded ambiguous-leaf child, and its generated `habitatmech:GOLD.b4e93f5d66` Indoor water-channel parent is a contextual location rather than a true `is-a` parent. | `data/raw/gold_ecosystem_paths.tsv` has this source concept as the `Biofilm` child of `Engineered > Artificial ecosystem > Water channel system > Indoor`; `curation/decisions.tsv` has no item-level row for `habitatmech:GOLD.905d58ea72`; `curation/term_requests.tsv` defines `air scrubber biofilm` under `ENVO:00002034` with `parent_mode=REPLACE` because its source parent is contextual. | `curation/decisions.tsv` and `curation/term_requests.tsv`. |

No blockers or minor findings were found.

## Recommended Edits

1. Item-review `habitatmech:GOLD.905d58ea72` against the exact `Engineered > Artificial ecosystem > Water channel system > Indoor > Biofilm` GOLD path, `ENVO:00002034` `biofilm`, and the immediate `habitatmech:GOLD.b4e93f5d66` Indoor water-channel context.
2. If the source concept is an indoor water-channel biofilm, add an `ITEM` `CONFIRM_UNGROUNDED` decision in `curation/decisions.tsv` for `habitatmech:GOLD.905d58ea72`, because no exact term in the vendored slice currently names the path-qualified child.
3. In that same future curation session, add a `curation/term_requests.tsv` row that defines the record under `ENVO:00002034` `biofilm`; use `parent_mode=REPLACE` to remove the generated Indoor water-channel parent unless item review finds evidence that the parent is truly broader rather than contextual.
4. Add an append-only `history/` record for that future curation session.

## Follow-up Checks

- After updating `curation/decisions.tsv` and `curation/term_requests.tsv`, run `just seed`, `just seed-canary habitatmech:GOLD.905d58ea72`, and inspect the regenerated `data/habitats/engineered/biofilm__5327749c.yaml`.
- If the canary is correct, run `just seed-apply --force`, then `just validate-strict data/habitats/engineered/biofilm__5327749c.yaml`, `just term-requests-check`, `just validate-history`, `just validate-causal-all`, `just verify-corpus --max-diffs 1`, and `just report`.
- If the future item review adds a causal overlay, run the focused `just validate-causal curation/causal_graphs/<slug>.yaml` before `just validate-causal-all`.

## Additional Notes

All absence checks in this review used ignored-inclusive `rg --no-ignore --hidden`, exact scans of the committed raw GOLD side tables, or `find`, bounded to maintained curation, history, research, raw inventory, generated habitat, label-residual, and review-report paths. No search depended on rendered `pages/` or text-map JSON.

The generic `ENVO:00002034` Biofilm record and its `curation/causal_graphs/biofilm.yaml` overlay remain out of scope for this single-record review. The generic mechanism overlay can inform future curation, but it does not prove a mechanism for the Indoor-water-channel-specific child and does not decide whether the Indoor source-path parent is a true superclass.

The immediate `indoor artificial water channel system` parent already has an item-level decision and a committed term request. That parent work should not block item review of this child: the generated child still needs a strict-parent check of the Biofilm-to-water-channel edge.
