# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/engineered/air_scrubber.yaml`
- Started UTC: `2026-09-23T20:55:00Z`
- Finished UTC: `2026-09-23T20:59:34Z`
- Verdict: `pass with minor issues`

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/engineered/air_scrubber.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.ebf95a8a4a` |
| Label | `air scrubber` |
| Definition source | `HabitatMech` |
| Category | `ENGINEERED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `REVIEWED` |

This is a generated record owned by upstream inputs, not by hand edits under
`data/habitats/`. Its path is pinned by `data/habitats/PATHS.tsv`, which maps
`habitatmech:GOLD.ebf95a8a4a` to `air_scrubber`.

## Validation

| Check | Result |
|---|---|
| `find reports/yaml_record_review -maxdepth 1 -type f -name '*-air_scrubber.md' -print` | Passed; no prior exact report was present. |
| `find curation/causal_graphs -maxdepth 1 -type f -name '*air_scrubber*' -print` | Passed; no candidate causal overlay was present. |
| `rg --no-ignore --hidden -n -e "habitatmech:GOLD\.ebf95a8a4a" -e "air_scrubber" -e "air scrubber" curation/causal_graphs` | Passed; no causal overlay references the identifier, slug, or label. Ignored and hidden files were included. |
| `just validate data/habitats/engineered/air_scrubber.yaml` | Passed; `linkml-validate` reported no issues. |
| `just validate-strict data/habitats/engineered/air_scrubber.yaml` | Passed; 1 file scanned, 0 files with errors, and 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| Focused causal-graph validation | Not applicable; the record has no maintained overlay under `curation/causal_graphs/`. |
| Reference validator | Not applicable; the record has no DOI, PMID, URL, causal-edge evidence, dataset, or discussion references to check. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; no issues found and 77 history records validated. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 on disk, 0 missing, 0 extra, and 0 differing. |
| `just worklist --limit 2000` | Passed after rerunning outside the managed workspace sandbox; the first run could not read `~/.cache/uv/sdists-v9/.git`. The command reported 0 ungrounded records still undecided and 1,810 decisions on file. |
| `just report` | Passed; the corpus report completed for 3,206 records. |
| `git diff --check` | Passed before report creation. |

## Identity and Grounding

The GOLD source concept is `gold.ecosystem:5805`, label `Air scrubber`, with
path `Environmental > Air > Indoor Air > Air scrubber`. In
`data/raw/gold_ecosystem_paths.tsv` this leaf has depth 4, one GOLD node, and
zero upstream organism, study, biosample, or assertion counts; its child
`Air scrubber > Biofilm` is likewise present but carries zero upstream
assertions.

The item-level row in `curation/decisions.tsv` confirms
`habitatmech:GOLD.ebf95a8a4a` as `UNGROUNDED`, classifies it as `ENGINEERED`,
and records the key identity correction: although GOLD files the source under
air, the leaf denotes a piece of equipment rather than a portion of air.

The term-request row in `curation/term_requests.tsv` authors the definition
under `ENVO:00003074` `manufactured product`, adds the exact synonyms
`air washer`, `gas scrubber`, and `wet scrubber`, and uses
`parent_mode=REPLACE` to drop GOLD's inherited `Indoor Air` parent. That is
consistent with the committed `Air scrubber` research report, which reads the
GOLD leaf as an agricultural or industrial wet exhaust scrubber whose wetted
packing, attached biofilm, and recirculating wash water form microbial
habitats.

The maintained `ENVO:00002152` `biofilter` term remains too narrow and
cross-cutting for exact grounding because it asserts biological pollutant
degradation. The current vendored `ENVO:00003968` `air filter` term is a closer
device near miss: it is now directly under `ENVO:00003074`, but it still names
a generic filter rather than the wet gas-liquid contactor this GOLD record
denotes. No vendored ontology term names wet scrubbers, bioscrubbers,
biotrickling filters, or air-pollution-control devices directly.

## Evidence

The generated `definition`, three exact synonyms, `ENVO:00003074`
`parent_habitats` entry, `DEFINED` curation-history event, and
`REVIEWED` mapping status all come from the single maintained
`curation/term_requests.tsv` row for `habitatmech:GOLD.ebf95a8a4a`.

The GOLD source attestation exactly preserves `gold.ecosystem:5805`, the source
label, and the four-level source path from `data/raw/gold_ecosystem_paths.tsv`.
The zero upstream assertion count in that same raw row explains why this record
correctly has no `characteristic_taxa`: GOLD declares the ecosystem
classification leaf, but this extract has no organisms, studies, or biosamples
filed under it.

No causal graph, claim-level evidence block, environmental parameter,
discussion item, or dataset usage is asserted for this record.

## Completeness

The record is complete enough for its current GOLD and HabitatMech-authored
term-request scope:

- It has an item-level `CONFIRM_UNGROUNDED` decision for its only source
  concept.
- It has a maintained HabitatMech definition and synonym set.
- Its retained parent is the defensible vendored genus for a new wet-scrubber
  term.
- It intentionally emits no characteristic taxa because the GOLD source leaf
  has no upstream assertions in `data/raw/gold_ecosystem_paths.tsv`.
- It correctly leaves causal graphs, environmental parameters, discussions, and
  datasets empty because no maintained input asserts them.

Bounded, ignored-inclusive searches for `habitatmech:GOLD.ebf95a8a4a`,
`gold.ecosystem:5805`, `Air scrubber`, `air scrubber`, and `air_scrubber`
across curation inputs, raw inputs, `PATHS.tsv`, history, reports, research,
source, tests, and configuration found the item-level decision, the term
request, the ENVO ROBOT request copy, the committed research report and
manifest entries, the GOLD raw rows, the `PATHS.tsv` slug row, and the
append-only mapping-history entry. Generated `data/text_map/`, `pages/`, and
`build/` files were excluded; ignored and hidden files were included.

## Findings

### Minor

| Finding | Evidence | Maintained owner |
|---|---|---|
| The term-request note still rejects `ENVO:00003968` `air filter` for a stale HVAC-specific reason. | The generated curation-history event says `ENVO:00003968` is a near miss that over-claims "HVAC semantics." In the current vendored slice `ENVO:00003968` is directly under `ENVO:00003074` `manufactured product`, not under `ENVO:00002874` `air conditioning unit`. The term is still adjacent rather than exact, but the note should reject it because it names a generic air-filter device rather than a wet scrubber. | `curation/term_requests.tsv` |

## Recommended Edits

1. In `curation/term_requests.tsv`, update the `air scrubber` notes cell so the
   `ENVO:00003968` near-miss rationale matches the current vendored ENVO
   hierarchy. Preserve `ENVO:00003074`, the definition, synonyms, and
   `parent_mode=REPLACE`; only the explanatory note needs to change.

## Follow-up Checks

- Run `just term-requests-check` to prove the regenerated ENVO ROBOT request is
  current after the note edit.
- Re-seed `habitatmech:GOLD.ebf95a8a4a` and inspect
  `data/habitats/engineered/air_scrubber.yaml`.
- Run `just verify-corpus --max-diffs 1` to prove the generated corpus has no
  unexpected drift.
- Run `just validate-strict data/habitats/engineered/air_scrubber.yaml` to
  prove the regenerated record still satisfies the closed LinkML schema.

## Additional Notes

The separate child leaf `Environmental > Air > Indoor Air > Air scrubber >
Biofilm` is already minted as `habitatmech:GOLD.7f436f8aff`; defining the
current record as the device leaves that child record free to denote the
attached biomass rather than duplicating the whole scrubber.
