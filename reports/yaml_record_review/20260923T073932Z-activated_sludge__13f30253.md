# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/engineered/activated_sludge__13f30253.yaml`
- Started UTC: 2026-09-23T07:35:30Z
- Finished UTC: 2026-09-23T07:39:32Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.6ba6dd1c82` |
| Label | `Activated sludge` |
| Category | `ENGINEERED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Source concept | `Engineered > Wastewater > Nutrient removal > Dissolved organics (anaerobic) > Activated sludge` |
| Maintained owner | Generated from the GOLD path inventory and `data/habitats/PATHS.tsv`; generated YAML remains read-only |
| Locked slug | `data/habitats/PATHS.tsv:2073` maps `habitatmech:GOLD.6ba6dd1c82` to `activated_sludge__13f30253` |

This is the generated GOLD record for one path-qualified `Activated sludge`
leaf below the curated `Dissolved organics (anaerobic)` parent. Its source key
is deterministic: `sha1("GOLD:Engineered > Wastewater > Nutrient removal >
Dissolved organics (anaerobic) > Activated sludge")[:10]` is `6ba6dd1c82`,
matching the generated `habitatmech:GOLD.6ba6dd1c82` identifier.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/engineered/activated_sludge__13f30253.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/engineered/activated_sludge__13f30253.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | Pass; 0 ungrounded records are still undecided and 1810 decisions are on file |
| `just report` | Pass; the report completed and kept the corpus at 3206 records |
| `git diff --check` | Pass; no whitespace errors |

## Identity and Grounding

The record's generated identity and direct source trace are internally
consistent:

- `data/raw/gold_ecosystem_paths.tsv:1413` directly attests the canonical path
  `Engineered > Wastewater > Nutrient removal > Dissolved organics (anaerobic)
  > Activated sludge`, with leaf label `Activated sludge`, `depth: 5`, one GOLD
  ecosystem node, zero organism-level assertions, and node id
  `gold.ecosystem:4259`.
- `data/habitats/PATHS.tsv:2073` pins `habitatmech:GOLD.6ba6dd1c82` to
  `activated_sludge__13f30253`.
- The generated `source_id: gold.ecosystem:4259`, `source_label`, and
  `source_path` match the raw GOLD row exactly.
- `data/raw/ontology_terms.tsv:7191` gives `ENVO:00002046` the canonical label
  `activated sludge`, and `data/raw/ontology_subclass_edges.tsv:5293` places it
  under `ENVO:00002044` `sludge`, supporting the generic ontology parent
  retained by the ambiguous-leaf rule.

`grounding_status: NARROW` is appropriate for a path-qualified GOLD leaf. GOLD
has many `Activated sludge` leaves under engineered subpaths, while
`ENVO:00002046` names generic activated sludge; keeping this source concept
minted and using `ENVO:00002046` as a broader parent avoids merging all GOLD
activated-sludge contexts into one ontology identity.

`mapping_status: SEEDED` is expected for the target itself. Ignored/hidden-
inclusive exact searches for `habitatmech:GOLD.6ba6dd1c82`,
`activated_sludge__13f30253`, `gold.ecosystem:4259`, and the full GOLD source
path under `curation/`, `history/`, `research/`, `reports/`, `data/raw`,
`data/habitats/PATHS.tsv`, and generated habitat records found the raw GOLD
rows, the generated target, the slug lock, and no item-level decision, term
request, causal overlay, or history entry for `habitatmech:GOLD.6ba6dd1c82`.

The record does inherit one false parent edge. The GOLD source path is a child
of `Engineered > Wastewater > Nutrient removal > Dissolved organics
(anaerobic)`, but that parent is now item-curated and defined in
`curation/term_requests.tsv:68` as an `anaerobic wastewater treatment reactor`
under `ENVO:00002124` `anaerobic bioreactor`. The vendored ENVO slice places
`activated sludge` under `ENVO:00002044` `sludge`, not under a reactor class.

## Evidence

| Claim | Nearest source | Review |
|---|---|---|
| GOLD contains the child path `Engineered > Wastewater > Nutrient removal > Dissolved organics (anaerobic) > Activated sludge`. | `data/raw/gold_ecosystem_paths.tsv:1413` | Supported exactly |
| The generated source node `gold.ecosystem:4259` is the only GOLD ecosystem node collapsed into this canonical path. | `data/raw/gold_ecosystem_paths.tsv:1413` | Supported exactly |
| The source concept has zero GOLD organism-level assertions in the seeder's source table. | `data/raw/gold_ecosystem_paths.tsv:1413` | Supported exactly; `src/habitatmech/seed.py` serializes GOLD `assertion_count` from `organism_count`, so the generated attestation correctly omits `assertion_count` |
| The source concept is narrower than generic ENVO activated sludge. | `data/raw/ontology_terms.tsv:7191`; `data/raw/ontology_subclass_edges.tsv:5293`; sibling GOLD `Activated sludge` paths in `data/raw/gold_ecosystem_paths.tsv` | Supported exactly |
| The source concept is narrower than `habitatmech:GOLD.bb0eb00ccb` `anaerobic wastewater treatment reactor`. | GOLD source hierarchy plus `curation/term_requests.tsv:68` | Not supported after curation; the source hierarchy places the leaf below that parent, but the curated parent denotes a reactor rather than sludge material |

The record has no definition, synonyms, xrefs, environmental parameters,
characteristic taxa, record-level evidence, causal graphs, discussion links,
datasets, or quality flags. No target-specific maintained input currently
supplies those fields.

Unsupported or over-scoped claims:

- `parent_habitats` includes `habitatmech:GOLD.bb0eb00ccb`, which now denotes
  the anaerobic treatment reactor rather than its sludge contents.

## Completeness

The direct GOLD source attestation is complete for
`data/raw/gold_ecosystem_paths.tsv:1413`: the generated record carries the
representative source node, source label, full GOLD source path, and
`skos:narrowMatch` predicate. The absent `assertion_count` is faithful to the
row's zero `organism_count`, zero `study_count`, zero `biosample_count`, and
zero `total_assertions`. The supplemental
`data/raw/gold_path_biosamples.tsv:622` row reports ten GOLD API biosamples for
`gold.ecosystem:4259`, but that supplement is not the organism-assertion column
that seeds GOLD source-attestation counts.

The target is not complete enough for review signoff because the maintained
curation that redefined its immediate parent made the source-derived parent edge
false. The committed deep-research report for the parent already warned that
this child `Activated sludge` path is internally inconsistent upstream noise;
the child needs its own maintained decision so the seeder can stop regenerating
the false reactor-as-parent edge.

No causal graph is expected until the target identity and hierarchy are fixed.
Upstream GOLD occurrence is not mechanism evidence.

Ignored/hidden-inclusive exact searches covered the target minted id, the
record stem, the full GOLD source path, `gold.ecosystem:4259`, the curated
parent id `habitatmech:GOLD.bb0eb00ccb`, parent node ids
`gold.ecosystem:3830` and `gold.ecosystem:4260`, `ENVO:00002046`,
`ENVO:00002044`, `ENVO:00002123`, and `ENVO:00002124`, excluding generated
`build`, `data/text_map`, and `pages`
trees for broad content searches. They found the cited raw, path-lock,
maintained parent curation, generated-record, history, and parent research
rows, and no maintained target-specific curation row.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| HM-ACTSLUDGE-13F30253-001 | major | `parent_habitats` still asserts `habitatmech:GOLD.6ba6dd1c82` is a kind of `habitatmech:GOLD.bb0eb00ccb`, but that parent is curated as an anaerobic wastewater-treatment reactor. The child is labelled `Activated sludge` and kept under `ENVO:00002046`, so the generated hierarchy now conflates a sludge material with the reactor that may contain sludge. | Add an item-level target decision in `curation/decisions.tsv`; if the concept stays minted, add `curation/term_requests.tsv` with `parent_mode=REPLACE` so the child keeps `ENVO:00002046` and drops the false GOLD parent. |

No blocker findings.

No minor findings.

## Recommended Edits

1. Add an item-level row for `habitatmech:GOLD.6ba6dd1c82` in
   `curation/decisions.tsv`.

2. Decide the source identity explicitly:
   - If `gold.ecosystem:4259` is just an erroneous placement of generic
     activated sludge, ground the source concept to `ENVO:00002046`
     `activated sludge` so its GOLD attestation merges into the exact
     ontology-backed record.
   - If the path should remain a separate anaerobic-dissolved-organics-specific
     child, keep it minted with `GROUND_AS_PARENT` to `ENVO:00002046` and add a
     `curation/term_requests.tsv` definition with `parent_mode=REPLACE` to
     remove the inherited `habitatmech:GOLD.bb0eb00ccb` parent.

3. Rerun `just seed`, then a canary for `habitatmech:GOLD.6ba6dd1c82`, and
   inspect the regenerated `data/habitats/engineered/activated_sludge__13f30253.yaml`.

## Follow-up Checks

For the hierarchy fix, run:

- `just seed`
- `just seed-canary habitatmech:GOLD.6ba6dd1c82`
- `just validate data/habitats/engineered/activated_sludge__13f30253.yaml`
- `just validate-strict data/habitats/engineered/activated_sludge__13f30253.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

Manually confirm the regenerated record no longer lists
`habitatmech:GOLD.bb0eb00ccb` under `parent_habitats` unless future curation
substantively changes that parent's denotation away from an anaerobic reactor.

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-activated_sludge__13f30253.md' -print` found no pre-existing exact review report for this record before this file was written, and `find` included ignored files under the searched directory.
- Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.6ba6dd1c82`, `activated_sludge__13f30253`, `gold.ecosystem:4259`, `Engineered > Wastewater > Nutrient removal > Dissolved organics (anaerobic) > Activated sludge`, `habitatmech:GOLD.bb0eb00ccb`, `gold.ecosystem:3830`, `gold.ecosystem:4260`, `ENVO:00002046`, `ENVO:00002044`, `ENVO:00002123`, and `ENVO:00002124` found the cited raw, path-lock, maintained curation, history, research, and generated-record rows, and no target-specific curated input rows.
