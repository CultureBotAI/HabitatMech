# YAML Record Review: Abyssal plane

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/abyssal_plane.yaml
- Started UTC: 2026-09-21T07:03:31Z
- Finished UTC: 2026-09-21T07:03:37Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/abyssal_plane.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.5520b30ba7` |
| Label | `Abyssal plane` |
| Category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Maintained status | generated from `data/raw/`, `curation/decisions.tsv`, and `data/habitats/PATHS.tsv` |

The generated record is a compact GOLD-only placeholder:

- GOLD supplies one `Environmental > Aquatic > Marine > Oceanic > Abyssal
  plane` source attestation with 15 `ORGANISM` assertions.
- The parent `ENVO:00000207` `oceanic zone` comes from the preceding GOLD path
  step, `Oceanic`, which was item-reviewed and merged with the PREGO
  `ENVO:00000207` source.
- GOLD's MIxS triad inventory separately reports 77 biosamples on this path
  with `ENVO:01000038` `oceanic abyssopelagic zone biome` as broad scale,
  `ENVO:01000105` `marine benthic feature` as the top local scale, and
  `ENVO:00002149` `sea water` as the top medium.
- A class-level curation decision rejected automatic lexical grounding but did
  not assess the source concept as an individual habitat.
- No definition, authored term request, item-level grounding decision, taxa,
  environmental parameters, evidence, causal graphs, discussions, datasets,
  synonyms, or xrefs are present.

## Validation

| Check | Result |
|---|---|
| `.venv/bin/python -c "from linkml.validator.cli import cli; cli()" --schema src/habitatmech/schema/habitatmech.yaml --target-class HabitatRecord data/habitats/aquatic/abyssal_plane.yaml` | Passed; no LinkML issues. |
| `.venv/bin/python scripts/validate_strict.py data/habitats/aquatic/abyssal_plane.yaml --quiet` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| `.venv/bin/python scripts/build_term_requests.py --check` | Passed; term-request table is current at 109 terms. |
| `.venv/bin/python scripts/verify_corpus.py --max-diffs 1` | Passed; 3,206 expected records, 3,206 found, no missing/extra/differing records. |
| `.venv/bin/python scripts/validate_history.py` | Passed; 77 history records valid against `src/habitatmech/schema/history.yaml`. |
| Reference validator | Not checked: this record has no DOI/PMID/URL `EvidenceItem` or causal-edge evidence, and no dedicated `research/habitats/aquatic/` report for `habitatmech:GOLD.5520b30ba7` requiring `scripts/check_report_citations.py` exists. |

## Identity and Grounding

| Claim | Evidence | Assessment |
|---|---|---|
| The record denotes the GOLD `Environmental > Aquatic > Marine > Oceanic > Abyssal plane` source concept. | `data/raw/gold_ecosystem_paths.tsv` has exactly that canonical path, leaf `Abyssal plane`, depth `5`, `gold_node_count=1`, `organism_count=15`, `total_assertions=15`, and node ID `gold.ecosystem:4006`. | Supported exactly. |
| The stable file stem is `abyssal_plane`. | `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.5520b30ba7` to `abyssal_plane`. | Supported exactly. |
| The source path justifies the broad AQUATIC category. | The raw GOLD path is under `Environmental > Aquatic > Marine > Oceanic`. The 77-biosample GOLD triad rows also put all 6 inspected studies in `oceanic abyssopelagic zone biome` as broad scale and put `sea water` as the top medium across 5 of those 6 studies. | Supported exactly. |
| The inherited `ENVO:00000207` `oceanic zone` parent is strictly broader. | `ENVO:00000207` is the item-reviewed `Oceanic` path parent and denotes an ocean water mass outside the continental shelf. GOLD's triads are mixed: their top medium is `sea water`, but their top local scale is the benthic term `ENVO:01000105` `marine benthic feature`. | Plausible if the GOLD child denotes abyssal-zone seawater; unresolved if it denotes `abyssal plain` seafloor. |
| `ENVO:00000244` `abyssal plain` is a near spelling-correction candidate. | `data/raw/ontology_terms.tsv` contains `ENVO:00000244` `abyssal plain` with definition "A flat or very gently sloping areas of the deep ocean basin floor." That label differs from GOLD's `Abyssal plane` leaf by one vowel and matches the expected deep-ocean seafloor spelling. | Needs item-level curation; the class sweep could not decide this misspelling from label matching alone. |
| `grounding_status: UNGROUNDED` is generated from a class-level no-match sweep. | `curation/decisions.tsv` has `habitatmech:GOLD.5520b30ba7` as `CONFIRM_UNGROUNDED` with `review_depth=CLASS` and no object ID. Its note says no vendored-slice term matched the label by any search route and explicitly says whether the concept is a habitat was not assessed. | Mechanically supported but incomplete for item-level curation. |
| `mapping_status: SEEDED` is correct. | The only curation row for `habitatmech:GOLD.5520b30ba7` has `review_depth=CLASS`; `docs/CURATION.md` states that only item-level decisions promote a merged record to `REVIEWED`. | Supported exactly. |

## Evidence

No claim-level literature evidence was present. The record has no
characteristic taxon rows and no causal graph, so there are no taxon
associations, causal edges, or snippets to audit.

The raw GOLD MIxS triads are source-context rows, not literature evidence or a
curated definition. They are strong enough to make the spelling-corrected
`abyssal plain` candidate worth item review, but they do not prove whether GOLD
used `Abyssal plane` for a seafloor feature, the adjacent abyssal water mass,
or a source artifact.

## Completeness

- The record is schema-valid and reproducible, but not item-curated.
- Empty `characteristic_taxa`, `evidence`, `environmental_parameters`,
  `causal_graphs`, `discussions`, `datasets`, `synonyms`, and `xrefs` are
  technically acceptable: no maintained input currently supplies them for this
  record.
- `data/raw/environment_parameters.tsv` has no row for
  `Environmental > Aquatic > Marine > Oceanic > Abyssal plane`.
- `curation/term_requests.tsv`,
  `curation/term_requests/envo_robot_template.tsv`, and
  `reports/habitat_research_manifest.tsv` have no row for
  `habitatmech:GOLD.5520b30ba7`.
- Ignored/hidden-inclusive exact searches covered `data/raw`,
  `data/habitats/aquatic`, `curation`, `research/habitats`,
  `reports/habitat_research_manifest.tsv`, and `reports/yaml_record_review`,
  excluding only generated text-map, cache, and build trees, for
  `habitatmech:GOLD.5520b30ba7`, `gold.ecosystem:4006`, `Abyssal plane`,
  `abyssal plane`, `abyssal plain`, and `abyssal_plane`. They found the target,
  the raw GOLD path, raw GOLD biosamples and triads, six raw GOLD study rows,
  the class-level decision row, the vendored `ENVO:00000244` `abyssal plain`
  term, and an adjacent `Plankton` research report mentioning `Abyssal plane`
  as an `Oceanic` sibling; they found no item-level decision, term-request row,
  causal overlay, research manifest entry, or dedicated research report for
  this target.
- A bounded `find curation/causal_graphs -maxdepth 1 -type f -name '*abyss*'`
  search found no candidate causal overlay.
- A bounded `find research/habitats -type f -name '*abyss*'` search found no
  candidate dedicated research report.
- A bounded `find reports/yaml_record_review -maxdepth 1 -type f -name
  '*abyssal*'` search and an exact ignored/hidden-inclusive `rg` search under
  `reports/yaml_record_review/` found no prior review for
  `data/habitats/aquatic/abyssal_plane.yaml`,
  `habitatmech:GOLD.5520b30ba7`, or `gold.ecosystem:4006`.

## Findings

### Major

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| `HM-ABYSSAL-PLANE-001` | The source concept still needs item-level curation. The generated record treats GOLD `Abyssal plane` as an ungrounded child of `oceanic zone`, but its only decision is a `CLASS`-depth lexical no-match sweep that did not assess the path, the MIxS triads, or the spelling-corrected `ENVO:00000244` `abyssal plain` candidate. If that candidate is the intended identity, the generated `ENVO:00000207` water-mass parent is also not strictly broader than the seafloor feature. | `curation/decisions.tsv` row `habitatmech:GOLD.5520b30ba7` has `review_depth=CLASS`; `data/raw/ontology_terms.tsv` contains the candidate `ENVO:00000244` `abyssal plain`; the raw GOLD triads for this path mix abyssopelagic broad scale, benthic local scale, and sea-water medium. | `curation/decisions.tsv`, with a possible follow-on `curation/term_requests.tsv` row if item review rejects the spelling-corrected ENVO candidate but confirms the GOLD source concept is a real habitat. |

## Recommended Edits

1. Item-review `habitatmech:GOLD.5520b30ba7` in
   `curation/decisions.tsv`. Resolve whether GOLD `Abyssal plane` is a
   misspelling of the seafloor term `ENVO:00000244` `abyssal plain`,
   abyssopelagic sea water above an abyssal plain, or another source artifact.
2. If `ENVO:00000244` is exact, ground the source concept to that term and
   remove the inherited `ENVO:00000207` water-mass parent through the maintained
   decision that the seeder reads.
3. If the GOLD source concept is real but not exact to `ENVO:00000244`, add a
   `curation/term_requests.tsv` row with an authored definition and a vendored
   ontology genus. Use default `ADD` parent mode only if item review confirms
   the inherited `oceanic zone` parent is true; otherwise use `REPLACE` with the
   evidence documented in the definition notes.
4. Regenerate a single-record canary for `habitatmech:GOLD.5520b30ba7`, inspect
   `data/habitats/aquatic/abyssal_plane.yaml`, and confirm that the generated
   `grounding_status`, `mapping_status`, label, parent, source attestation, and
   curation history all follow from the maintained rows.

## Follow-up Checks

- `.venv/bin/python scripts/seed_from_sources.py`
- `.venv/bin/python scripts/seed_from_sources.py --apply --only habitatmech:GOLD.5520b30ba7`
- `.venv/bin/python -c "from linkml.validator.cli import cli; cli()" --schema src/habitatmech/schema/habitatmech.yaml --target-class HabitatRecord data/habitats/aquatic/abyssal_plane.yaml`
- `.venv/bin/python scripts/validate_strict.py data/habitats/aquatic/abyssal_plane.yaml --quiet`
- `.venv/bin/python scripts/build_term_requests.py --check`
- `.venv/bin/python scripts/verify_corpus.py --max-diffs 1`

## Additional Notes

- `research/habitats/aquatic/plankton-habitatmech-gold-3fb6b22200-deep-research-claude_code.md`
  mentions `Abyssal plane` only as an `Oceanic` sibling while reasoning about
  GOLD `Plankton`; it is not an item-level review of
  `habitatmech:GOLD.5520b30ba7`.
- Six raw GOLD studies list this path. Two list only `Abyssal plane`, three
  pair it with other `Oceanic` water-column or seafloor siblings, and one pairs
  it with `Aquifer > Formation fluid`. Those mixed neighboring paths reinforce
  the need to inspect GOLD's study context before deciding between seafloor and
  water-mass identities.
