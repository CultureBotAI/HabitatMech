# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/brine__791bb429.yaml
- Started UTC: 2026-09-23T15:08:00Z
- Finished UTC: 2026-09-23T15:33:15Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | habitatmech:GOLD.fd7b992495 |
| Label | Brine |
| Class | HabitatRecord |
| Category | AQUATIC |
| Grounding status | NARROW |
| Mapping status | SEEDED |
| Maintained owner | generated from `data/raw/gold_ecosystem_paths.tsv` and `data/habitats/PATHS.tsv`, with the source-path parent added by `src/habitatmech/seed.py` |

[data/habitats/aquatic/brine__791bb429.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/aquatic/brine__791bb429.yaml) is the generated GOLD record for `Environmental > Aquatic > Marine > Coastal > Brine`.

The target was resolved with ignored-file-inclusive searches:

- `find data/habitats -type f -name 'brine__791bb429.yaml' -print` found exactly one generated YAML record.
- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-brine__791bb429.md' -print` found no prior exact review report.
- `rg --no-ignore --hidden -n 'gold\.ecosystem:5391|Environmental > Aquatic > Marine > Coastal > Brine|habitatmech:GOLD\.fd7b992495' data/raw curation data/habitats/PATHS.tsv history research reports -g '!reports/yaml_record_review/**' -g '!data/text_map/**' -g '!pages/**' -g '!build/**'` found the authoritative GOLD source rows and no maintained curation overlay, history record, research report, or auxiliary report for this path outside raw generated inputs.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/brine__791bb429.yaml` | pass; no LinkML issues found |
| `just validate-strict data/habitats/aquatic/brine__791bb429.yaml` | pass; 1 file scanned, 0 files with ERROR, 0 total ERROR rows |
| `just validate-causal curation/causal_graphs/brine__791bb429.yaml` | not applicable; exact `find curation/causal_graphs -maxdepth 1 -type f -name 'brine__791bb429.yaml' -print` found no overlay |
| `just validate-causal-all` | pass; 32 causal-graph curation files with 32 graphs validated |
| Reference validator | not applicable; this generated GOLD record has no causal graph and no DOI, PMID, or URL evidence slots to inspect |
| `just term-requests-check` | pass; term-request table is current with 109 terms |
| `just validate-history` | pass; no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | pass; 3206 expected, 3206 found, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | pass; 0 ungrounded records still undecided; 1810 decisions on file |
| `just report` | pass; 3206 records; no global issue specific to `habitatmech:GOLD.fd7b992495` surfaced |
| `git diff --check` | pass |

## Identity and Grounding

The minted record identity matches its GOLD source path:

- `data/raw/gold_ecosystem_paths.tsv` contains canonical path `Environmental > Aquatic > Marine > Coastal > Brine`, leaf label `Brine`, depth `5`, one GOLD node, `17` organism assertions, and source node `gold.ecosystem:5391`.
- `data/raw/gold_path_biosamples.tsv` maps the same path to ecosystem path id `5391` with `12` biosamples.
- `data/habitats/PATHS.tsv` pins the deterministic identifier `habitatmech:GOLD.fd7b992495` to the collision-resolved slug `brine__791bb429`.

The `NARROW` grounding to `ENVO:00003044` is well supported. The GOLD leaf `Brine` matches ENVO's `brine` label, and this depth-5 GOLD path is narrower than the generic ENVO material because it denotes brine in the context of a marine coastal environment. The same path's GOLD triads put `ENVO:00003044` in the `medium` slot and `ENVO:02000049` coastal water body in the `local` slot, agreeing that brine is the sampled material rather than the coastal feature itself.

The generated `parent_habitats` list is only partly coherent:

- `ENVO:00003044` is a correct broader parent for a coastal brine material.
- `ENVO:00002150` is inherited from the immediate GOLD parent path `Environmental > Aquatic > Marine > Coastal`, whose own curation decision grounds that zone to `coastal sea water` with `CLOSE` status because the GOLD node is a zone but the ENVO term names the water body. The target is brine in a coastal context, not a kind of coastal sea water.

`mapping_status: SEEDED` is expected. Exact ignored-file-inclusive search found no `curation/decisions.tsv` row for `habitatmech:GOLD.fd7b992495`, and exact search for the minted identifier, `gold.ecosystem:5391`, `brine__791bb429`, and the full source path across `curation`, `history`, `research`, and non-review `reports` found no maintained review artifact.

## Evidence

This record has one source attestation and no causal mechanism evidence:

- The `GOLD` source attestation mirrors `data/raw/gold_ecosystem_paths.tsv`: first and only source node `gold.ecosystem:5391`, leaf `Brine`, the full canonical path, `17` source organisms, and assertion unit `ORGANISM`.
- The attestation correctly uses `skos:narrowMatch`: the record keeps its minted GOLD identifier and records the matching generic brine term as a broader parent instead of merging the coastal GOLD path into the generic ENVO material.
- No characteristic taxa, environmental parameters, causal graphs, discussion items, or external datasets are generated for this source path.

## Completeness

The record carries all currently generated source-owned GOLD content: the source path, source node, organism assertion count, mapping predicate, generated parent links, category, and generated curation history.

It is missing a maintained way to suppress or qualify a false source-path parent. The second GOLD seeding pass in `src/habitatmech/seed.py` adds the parent path's resolved identifier when it differs from the child identifier. For `Coastal > Brine`, that turns a source hierarchy context into an `is-a` claim and cannot currently be repaired in `curation/decisions.tsv`, which decides source concepts rather than individual generated parent edges.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | `parent_habitats` incorrectly says the `Brine` medium is a narrower kind of `coastal sea water`. | The target source path is `Environmental > Aquatic > Marine > Coastal > Brine`, and GOLD triads for that exact path put `ENVO:02000049` coastal water body in the `local` slot and `ENVO:00003044` brine in the `medium` slot. `src/habitatmech/seed.py` nevertheless adds `ENVO:00002150` from the immediate `Environmental > Aquatic > Marine > Coastal` source-path parent, whose existing item decision only considers `coastal sea water` a `CLOSE` grounding for GOLD's broader coastal zone. | Add a maintained parent-edge override or equivalent source-path relation table, then make `src/habitatmech/seed.py` consult it before adding the inherited GOLD parent path as a `parent_habitats` edge. |

## Recommended Edits

1. Add a maintained override for the false `habitatmech:GOLD.fd7b992495 -> ENVO:00002150` source-path parent edge instead of hand-editing [data/habitats/aquatic/brine__791bb429.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/aquatic/brine__791bb429.yaml). The current generated-input model has no edge-level table for this, so the fix needs either a new curation input or a seeder rule that can distinguish source-path containment from strict `parent_habitats`.
2. Re-seed `habitatmech:GOLD.fd7b992495` after that maintained input exists and confirm the generated record keeps `ENVO:00003044` as its broader material parent while no longer claiming `coastal sea water` as an `is-a` parent.

## Follow-up Checks

The narrowest checks for the recommended fix are:

- `just seed`
- `just seed-canary habitatmech:GOLD.fd7b992495`
- `just validate data/habitats/aquatic/brine__791bb429.yaml`
- `just validate-strict data/habitats/aquatic/brine__791bb429.yaml`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- an ignored-file-inclusive exact search for `habitatmech:GOLD.fd7b992495`, `ENVO:00002150`, and the new override row to confirm the false parent edge has a maintained explanation

## Additional Notes

There are two exact GOLD `Brine` records plus the generic PREGO/ENVO lowercase `brine` record in the corpus, and they are not conflated:

- [data/habitats/aquatic/brine.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/aquatic/brine.yaml) is the generic PREGO/ENVO `ENVO:00003044` brine record.
- [data/habitats/aquatic/brine__2299825d.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/aquatic/brine__2299825d.yaml) is the minted GOLD record for non-marine saline-and-alkaline lake brine.
- [data/habitats/aquatic/brine__791bb429.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/aquatic/brine__791bb429.yaml) is this minted GOLD record for marine coastal brine.

The nearby `brine_pool.yaml` path is unrelated to the same-label GOLD `Brine` split.
