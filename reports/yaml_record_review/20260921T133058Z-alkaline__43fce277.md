# YAML Record Review: Alkaline

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/alkaline__43fce277.yaml
- Started UTC: 2026-09-21T13:28:30Z
- Finished UTC: 2026-09-21T13:30:58Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Path | `data/habitats/aquatic/alkaline__43fce277.yaml` |
| Identifier | `habitatmech:GOLD.f00984e0e2` |
| Label | `Alkaline` |
| Category | `AQUATIC` |
| Grounding status | `NOT_APPLICABLE` |
| Mapping status | `REVIEWED` |
| Source | GOLD |
| Source ID | `gold.ecosystem:3993` |
| Source path | `Environmental > Aquatic > Thermal springs > Near-boiling (>90C) > Alkaline` |

The target is a generated GOLD-only record. It has no definition, synonyms,
xrefs, environmental parameters, characteristic taxa, record-level evidence,
causal graph, discussions, or datasets. `data/habitats/PATHS.tsv` pins
`habitatmech:GOLD.f00984e0e2` to the stable `alkaline__43fce277` slug.

This review covers the single GOLD source concept keyed by
`habitatmech:GOLD.f00984e0e2`. The identifier is the first ten hex characters
of `sha1("GOLD:Environmental > Aquatic > Thermal springs > Near-boiling (>90C) > Alkaline")`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/alkaline__43fce277.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/alkaline__43fce277.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just term-requests-check` | Passed; the generated term-request table is current with 109 terms. |
| Reference validator | Not checked: this reviewed `NOT_APPLICABLE` GOLD-only record has no `EvidenceItem` references or causal overlay, and this repository documents no standalone reference validator for a generated record without those objects. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records were present, with 0 missing, 0 extra, and 0 differing files. |

## Identity and Grounding

The reviewed source identity is reproducible:

| Claim | Review |
|---|---|
| Minted identifier | Supported. `mint("GOLD", canonical_path)` hashes the exact GOLD source path to `habitatmech:GOLD.f00984e0e2`; `data/habitats/PATHS.tsv` maps that identifier to `alkaline__43fce277`. |
| GOLD source path | Supported. `data/raw/gold_ecosystem_paths.tsv` has the exact canonical path `Environmental > Aquatic > Thermal springs > Near-boiling (>90C) > Alkaline`. |
| Source ID | Supported. The exact raw GOLD row carries the single node `gold.ecosystem:3993`, matching the generated attestation. |
| Source label | Supported. The raw GOLD row sets `leaf_label` to `Alkaline`, matching the generated `label` and `source_label`. |
| Category | Supported by the first two GOLD path levels, `Environmental > Aquatic`. |
| `grounding_status: NOT_APPLICABLE` | Supported. The source concept's maintained `curation/decisions.tsv` row is an item-level `NOT_APPLICABLE` decision stating that this `Alkaline` node is a pH band qualifying the parent node, not a habitat. |
| `mapping_status: REVIEWED` | Supported. The sole contributing GOLD source has an item-level decision, so the generated record is fully reviewed even though the source concept is rejected as a habitat. |

The non-habitat decision is also consistent with the exact GOLD context. The
nearest MIxS rows for this path support hot-spring and microbial-mat context:
`data/raw/gold_path_triads.tsv` reports `ENVO:00000051` `hot spring` as the top
local term and `ENVO:01000157` `microbial mat material` as the top medium term.
Those contextual terms do not make the bare `Alkaline` leaf a habitat identity;
they only confirm that this GOLD source path is a near-boiling alkaline
thermal-spring setting.

The generated `parent_habitats` edge is not sound. The target currently has
`habitatmech:GOLD.8b53067bc5` as a parent, but that parent is itself
item-reviewed as `NOT_APPLICABLE` because `Near-boiling (>90C)` is a
temperature band qualifying `Thermal springs`. `parent_habitats` is a strict
broader-habitat relation, not a relation between environmental parameter
qualifiers.

## Evidence

Supported:

| Claim | Evidence |
|---|---|
| The source concept exists in GOLD with the expected path and identifier. | `data/raw/gold_ecosystem_paths.tsv` has the exact thermal-spring `Alkaline` path with `gold_node_count=1` and `gold_node_ids=gold.ecosystem:3993`. |
| The generated GOLD attestation preserves the upstream source label and source path. | `src/habitatmech/seed.py` emits `source_label` from `leaf_label` and `source_path` from `canonical_path`; the generated record matches the exact raw GOLD row. |
| The exact source path appears in downstream GOLD biosample, triad, and study inventories. | `data/raw/gold_path_biosamples.tsv` has 5 biosamples for `gold.ecosystem:3993`; `data/raw/gold_path_triads.tsv` has exact-path broad, local, and medium rows summarizing 5 samples from 4 studies; and `data/raw/gold_studies.tsv` has five study rows that include the exact path. |
| The exact-path MIxS triads support near-boiling hot-spring context, not a bare alkaline identity. | The highest-share local term for the exact path is `ENVO:00000051` `hot spring`, while the highest-share medium term is `ENVO:01000157` `microbial mat material`. |
| The immediate GOLD parent is a curated non-habitat qualifier. | `data/habitats/aquatic/near_boiling_90c__f7b76591.yaml` has `grounding_status: NOT_APPLICABLE`, and the maintained decision for `habitatmech:GOLD.8b53067bc5` says `Near-boiling (>90C)` is a temperature band qualifying the parent node, not a habitat. |

Unsupported or over-scoped:

| Claim | Issue |
|---|---|
| `habitatmech:GOLD.8b53067bc5` `Near-boiling (>90C)` is a broader habitat for this record. | Both ends of the generated edge are reviewed non-habitat qualifiers: `Alkaline` is a pH band and `Near-boiling (>90C)` is a temperature band. The second GOLD pass in `src/habitatmech/seed.py` links each GOLD source path to its parent path after decisions are applied, so the target reproducibly emits a strict `parent_habitats` edge to a source concept that does not denote a habitat. |

No literature citations, record-level evidence, causal edges, environmental
parameters, characteristic taxa, discussions, or datasets are attached to this
record.

## Completeness

The source concept has been reviewed at item depth, and no term request is
needed because the source concept is not a habitat. The record still has one
consequential generated hierarchy gap:

- `src/habitatmech/seed.py` adds the next GOLD path level as a parent after
  item-level decisions are applied, so this reviewed `NOT_APPLICABLE` pH band
  still inherits another reviewed `NOT_APPLICABLE` temperature band as a strict
  broader habitat.

The target has no generated child records, but the same temperature-band parent
is also inherited by the `Sediment` and `Microbial mats` siblings under
`Environmental > Aquatic > Thermal springs > Near-boiling (>90C)`. A maintained
fix should suppress or rewire false edges around the non-habitat
`Near-boiling (>90C)` node without dropping each sibling's defensible
hot-spring, sediment, or microbial-mat context.

Bounded absence checks:

| Query | Scope | Result |
|---|---|---|
| `habitatmech:GOLD.f00984e0e2` | Hidden/ignored-inclusive search over `curation`, `data/raw`, `data/habitats`, `src`, and `reports/yaml_record_review` | Found the maintained decision row, the `PATHS` row, the generated target, and one earlier review canary mention; found no term request, causal overlay, generated child record, or prior YAML review for this exact source concept. |
| `gold.ecosystem:3993` and `Environmental > Aquatic > Thermal springs > Near-boiling (>90C) > Alkaline` | Hidden/ignored-inclusive search over raw GOLD rows, maintained curation inputs, generated records, and YAML reviews | Found the exact GOLD ecosystem, biosample, MIxS triad, and study rows, plus the generated target; found no maintained suppression that would prevent the false parent edge. |
| `alkaline__43fce277` | Hidden/ignored-inclusive search over `reports/yaml_record_review` before this report was written | Found no existing report for this record stem. |
| `habitatmech:GOLD.8b53067bc5` | Hidden/ignored-inclusive search over `data/habitats` | Found the `Near-boiling (>90C)` parent record and the generated `Alkaline`, `Sediment`, and `Microbial mats` children that inherit it as a parent. |

## Findings

### Blocker

None found.

### Major

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| M1 | `parent_habitats` keeps a generated edge from a reviewed non-habitat pH band to a reviewed non-habitat temperature band, asserting an is-a relationship between environmental-parameter qualifiers. | `alkaline__43fce277.yaml` lists `habitatmech:GOLD.8b53067bc5` as its parent even though maintained item-level decisions mark the child `Alkaline` source concept and the parent `Near-boiling (>90C)` source concept `NOT_APPLICABLE`. The GOLD parent-path second pass in `src/habitatmech/seed.py` adds this edge after decisions resolve both rows, so the edge is generated reproducibly from a classification path rather than a strict broader-habitat relation. | `src/habitatmech/seed.py`, or a maintained suppression/rewiring input consumed by the GOLD parent-path pass. |

### Minor

None found.

## Recommended Edits

1. Update the maintained GOLD hierarchy pass in `src/habitatmech/seed.py`, or add a maintained suppression/rewiring table consumed by that pass, so item-reviewed `NOT_APPLICABLE` qualifier source concepts are not emitted as strict `parent_habitats`.
2. At minimum, suppress the edge from `Environmental > Aquatic > Thermal springs > Near-boiling (>90C) > Alkaline` to `Environmental > Aquatic > Thermal springs > Near-boiling (>90C)`.
3. Canary the reviewed parent and siblings, including `habitatmech:GOLD.8b53067bc5`, `habitatmech:GOLD.7d5544155b`, and `habitatmech:GOLD.056292f510`, so the future fix does not keep any hierarchy edge that routes through the non-habitat `Near-boiling (>90C)` temperature band.
4. Do not hand-edit `data/habitats/aquatic/alkaline__43fce277.yaml`; it is generated from raw GOLD, curation decisions, and the seeder.

## Follow-up Checks

| Edit | Narrowest proving check |
|---|---|
| Suppress or rewire the false GOLD parent edge for this exact source. | Run `just seed-canary habitatmech:GOLD.f00984e0e2` and confirm `data/habitats/aquatic/alkaline__43fce277.yaml` no longer has `parent_habitats: habitatmech:GOLD.8b53067bc5`. |
| Preserve source provenance while rejecting the pH band as a habitat. | Re-read the canary and confirm the record keeps `grounding_status: NOT_APPLICABLE`, `mapping_status: REVIEWED`, `source_id: gold.ecosystem:3993`, and the exact thermal-spring alkaline GOLD source path. |
| Avoid regressing siblings below the same non-habitat temperature band. | Canary `habitatmech:GOLD.7d5544155b` and `habitatmech:GOLD.056292f510`, then confirm they either keep a defensible broader habitat or intentionally replace the inherited `Near-boiling (>90C)` temperature-band node. |
| Prove the regenerated corpus remains stable. | Run `just validate data/habitats/aquatic/alkaline__43fce277.yaml`, `just validate-strict data/habitats/aquatic/alkaline__43fce277.yaml --quiet`, `just term-requests-check`, `just validate-history`, and `just verify-corpus --max-diffs 1`. |
| Prove downstream exports no longer carry the false edge. | Rebuild KGX and SSSOM products and confirm no exported edge asserts `habitatmech:GOLD.f00984e0e2` under `habitatmech:GOLD.8b53067bc5`. |

## Additional Notes

- `gold_ecosystem_paths.tsv` reports 0 organism, study, biosample, and total
  assertions on the exact ecosystem row, so the generated GOLD attestation
  intentionally has no `assertion_count`. `gold_path_biosamples.tsv` and
  `gold_path_triads.tsv` preserve separate biosample and MIxS summaries for
  the same `gold.ecosystem:3993` path.
- The exact-path MIxS triads include one broad term, two local terms, and two
  medium terms across four studies. They support environmental context for the
  source path, not a narrower maintained habitat identity for the rejected
  `Alkaline` source node.
