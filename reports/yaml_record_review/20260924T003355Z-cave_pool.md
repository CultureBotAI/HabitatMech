# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/aquatic/cave_pool.yaml`
- Started UTC: 2026-09-24T00:29:00Z
- Finished UTC: 2026-09-24T00:33:55Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.afb49b0d4b` |
| Label | `Cave pool` |
| File | `data/habitats/aquatic/cave_pool.yaml` |
| Category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Source | `GOLD` |
| Source ID | `gold.ecosystem:5966` |
| Source path | `Environmental > Aquatic > Deep subsurface > Groundwater > Cave pool` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/PATHS.tsv`, and `curation/decisions.tsv`; do not edit `data/habitats/aquatic/cave_pool.yaml` directly. |

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/cave_pool.yaml` | Passed; `linkml-validate` reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/cave_pool.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| Target causal overlay | Not applicable. Exact ignored-inclusive searches for `habitatmech:GOLD.afb49b0d4b`, `Cave pool`, `cave_pool`, and `*cave*` causal-graph filenames found no target overlay in `curation/causal_graphs/`. |
| Reference validator | Not applicable. This target has no record-level `evidence`, no `characteristic_taxa.reference`, and no `causal_graphs` edges requiring cited evidence. |
| `just term-requests-check` | Passed; the ENVO term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 records expected, 3,206 found, 0 missing, 0 extra, 0 differing. |
| `just worklist --limit 2000` | Passed; 0 ungrounded records are still undecided and 1,810 decisions are on file. |
| `just report` | Passed; the corpus has 3,206 records, 686 `REVIEWED` records, and 2,520 `SEEDED` records. |
| `git diff --check` | Passed before writing this report. |

## Identity and Grounding

The generated identifier is stable and correct for this GOLD path:

| Claim | Check |
|---|---|
| Minted identifier | `mint("GOLD", "Environmental > Aquatic > Deep subsurface > Groundwater > Cave pool")` returned `habitatmech:GOLD.afb49b0d4b`, matching the generated record. |
| Slug lock | `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.afb49b0d4b` to `cave_pool`. |
| GOLD source row | `data/raw/gold_ecosystem_paths.tsv` has exactly one node for the path, source id `gold.ecosystem:5966`, depth 5, label `Cave pool`, and zero GOLD KGX organism, sample, and study assertions. |
| Curation row | `curation/decisions.tsv` has one class-level `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.afb49b0d4b`; the row records that the lexical sweep did not assess whether the concept is a habitat or term-request candidate. |

The current direct source-path parent,
`habitatmech:GOLD.22a80cbd14` `Groundwater`, is generated from the GOLD parent
path `Environmental > Aquatic > Deep subsurface > Groundwater`. That parent
record is itself only `NARROW` under `ENVO:01001004` `groundwater`, and ENVO
defines groundwater as underground water in pore spaces of rock or
unconsolidated deposits. A cave pool is a standing water body in a cave, not a
kind of pore-space groundwater.

The vendored ontology slice has no exact `cave pool` class. It does have
`ENVO:00000033` `pond`, defined as a body of water usually smaller than a lake,
with `pool` as a synonym. That term is strictly broader than a cave pool and is
the candidate parent the class-level sweep missed because its search used the
whole leaf label rather than the head noun.

## Evidence

The generated record is traceable to maintained inputs:

| Claim | Maintained input or inventory |
|---|---|
| GOLD source id and exact source path | `data/raw/gold_ecosystem_paths.tsv` row for `gold.ecosystem:5966` |
| Stable HabitatMech identifier and slug | `data/habitats/PATHS.tsv` row for `habitatmech:GOLD.afb49b0d4b` |
| Class-level `CONFIRM_UNGROUNDED` status | `curation/decisions.tsv` row for `habitatmech:GOLD.afb49b0d4b` |
| Generated source-path parent | `data/raw/gold_ecosystem_paths.tsv` row for the parent path `Environmental > Aquatic > Deep subsurface > Groundwater` plus `data/habitats/PATHS.tsv` mapping `habitatmech:GOLD.22a80cbd14` to `groundwater` |

The record has no record-level literature entry, and the committed GOLD rows do
not attach any biosamples, studies, MIxS triads, or environmental parameters to
`gold.ecosystem:5966`. Exact ignored-inclusive searches for
`gold.ecosystem:5966` and the full source path found the target row in
`data/raw/gold_ecosystem_paths.tsv` and the generated target record, but no
`data/raw/gold_path_biosamples.tsv`, `data/raw/gold_studies.tsv`, or
`data/raw/gold_path_triads.tsv` rows for this source path.

No causal graph is attached to this record, so there are no mechanism edges
needing claim-level `EvidenceItem` support.

## Completeness

The required generated identity, category, source attestation, source-derived
parent, class-level curation history, and seeded mapping status are present.

The empty optional slots are acceptable for the generated record as it stands:

| Slot | Review |
|---|---|
| `environmental_parameters` | Empty. No committed GOLD or curator-authored environment-parameter row was found for `gold.ecosystem:5966`. |
| `characteristic_taxa` | Empty. The GOLD aggregate records zero organism assertions for this path, and HabitatMech does not infer characteristic taxa from a source-tree label. |
| `evidence` | Empty. The class-level sweep did not inspect cave-pool literature, and no maintained literature artifact was found for this record. |
| `causal_graphs` | Empty. Exact ignored-inclusive searches found no target overlay in `curation/causal_graphs/`. |
| `discussions` and `datasets` | Empty. No maintained discussion or dataset input exists for this record. |

Exact ignored-inclusive searches for `habitatmech:GOLD.afb49b0d4b`,
`gold.ecosystem:5966`, `cave_pool`, `Cave pool`, and the GOLD source path found
the generated record, its `PATHS.tsv` row, the raw GOLD path row, and the
class-level decision row. They did not find a target-specific term request,
causal overlay, history record, research report, or prior exact YAML review
report. The exact report check used `find` under `reports/yaml_record_review`,
which includes ignored files.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | None found | The YAML validates, the generated identifier matches the GOLD source path, and the record correctly remains `SEEDED` because its only curation decision is class-level. | Not applicable |
| Major | The class-level `CONFIRM_UNGROUNDED` row missed `ENVO:00000033` `pond` as a strictly broader term for Cave pool and left the generated record under the non-strict `Groundwater` source-path parent. | `data/raw/ontology_terms.tsv` includes `ENVO:00000033` `pond`, with `pool` as a synonym and a definition covering smaller standing bodies of water. `data/raw/ontology_subclass_edges.tsv` places `ENVO:00000033` under `ENVO:01000617`, not under `ENVO:01001004` `groundwater`. The source path places the concept in a cave-pool context, so the GOLD `Groundwater` parent is provenance context rather than a strict broader `is-a` parent. | `curation/decisions.tsv`; probably `curation/term_requests.tsv` if Cave pool remains a novel minted child of `ENVO:00000033` |
| Minor | None found | The remaining empty optional slots are consistent with the absence of maintained item-level curation, source biosample rows, or a causal overlay. | Not applicable |

## Recommended Edits

| Priority | Edit | Maintained path |
|---|---|---|
| Major | Replace the class-level row for `habitatmech:GOLD.afb49b0d4b` with an item-level `GROUND_AS_PARENT` decision to `ENVO:00000033` `pond`, yielding `grounding_status` `NARROW`. | `curation/decisions.tsv` |
| Major | If Cave pool stays distinct from generic `ENVO:00000033`, add an authored `cave pool` term request with parent `ENVO:00000033` and `parent_mode=REPLACE` so the regenerated record drops the inherited `habitatmech:GOLD.22a80cbd14` `Groundwater` parent. | `curation/term_requests.tsv` |

Do not hand-edit `data/habitats/aquatic/cave_pool.yaml`; regenerate it from the
maintained inputs after item-level curation changes.

## Follow-up Checks

| Edit | Narrowest proving checks |
|---|---|
| Item-level Cave pool grounding | Exact ignored-inclusive `rg` for `habitatmech:GOLD.afb49b0d4b`, `gold.ecosystem:5966`, and `ENVO:00000033` should find the new maintained decision and any term request; `just seed-canary habitatmech:GOLD.afb49b0d4b`, `just term-requests-check`, `just validate data/habitats/aquatic/cave_pool.yaml`, `just validate-strict data/habitats/aquatic/cave_pool.yaml`, `just validate-history`, and `just verify-corpus --max-diffs 1` should still pass. |
| Parent replacement | The regenerated `data/habitats/aquatic/cave_pool.yaml` should list `ENVO:00000033`, not `habitatmech:GOLD.22a80cbd14`, under `parent_habitats`; run `just validate data/habitats/aquatic/cave_pool.yaml`, `just validate-strict data/habitats/aquatic/cave_pool.yaml`, and `just verify-corpus --max-diffs 1`. |

## Additional Notes

- Searches used to establish absence included ignored and hidden files. Broad
  exact content searches excluded `data/text_map/**`, `pages/**`, `build/**`,
  and `.git/**` so generated text-map, rendered-page, build-product, and git
  object matches did not dominate the results.
- The exact source-path search also hit `data/habitats/aquatic/cave_pool_sediment.yaml`
  because its YAML line wraps after the shared `Cave pool` prefix; that record
  has source id `gold.ecosystem:5968` and is the sibling GOLD path
  `Environmental > Aquatic > Deep subsurface > Groundwater > Cave pool sediment`,
  not an additional source attestation for this Cave pool target.
