# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/blood__d6f5ae05.yaml`
- Started UTC: 2026-09-22T01:26:13Z
- Finished UTC: 2026-09-22T01:26:35Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `UBERON:0000178` |
| Label | `blood` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the vendored ontology slice, `data/raw/environment_parameters.tsv`, and Madin raw tables; future curation belongs in `curation/decisions.tsv`, `curation/term_requests.tsv`, or `curation/causal_graphs/*.yaml`, not in this generated YAML |
| Locked slug | `data/habitats/PATHS.tsv:1011` maps `UBERON:0000178` to `blood__d6f5ae05` |

This is the ontology-grounded generic blood record. It is intentionally separate from the path-specific GOLD `Blood` leaves for Mammals: Human, Mammals, Birds, and Fish, which remain minted narrower children and cite `UBERON:0000178` as a broader parent.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/blood__d6f5ae05.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/blood__d6f5ae05.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors |

## Identity and Grounding

The exact ontology identity is supported by the vendored UBERON slice:

- `data/raw/ontology_terms.tsv:12925` defines `UBERON:0000178` with canonical label `blood`, definition `A fluid that is composed of blood plasma and erythrocytes.`, and exact synonyms `portion of blood`, `vertebrate blood`, and `whole blood`.
- `data/raw/ontology_subclass_edges.tsv:11037` asserts `UBERON:0000178 rdfs:subClassOf UBERON:0000179`.
- `data/raw/ontology_terms.tsv:12926` labels `UBERON:0000179` as `haemolymphatic fluid`.

The generated `identifier`, `label`, `definition`, `definition_source`, `synonyms`, and sole `parent_habitats` entry all agree with those ontology rows. `grounding_status: EXACT` is therefore appropriate for this record.

The two non-ontology source surfaces also point to the same blood identity:

| Source surface | Raw support | Record slot |
|---|---|---|
| kg-microbe environment table | `data/raw/environment_parameters.tsv:344-355` all use `env_type` `host_animal_endotherm_blood`, term `UBERON:0000178`, and term label `blood` | `source_attestations[ENVIRONMENTS_TABLE]` plus 12 `environmental_parameters` |
| Madin habitat table | `data/raw/madin_habitats.tsv:16` has `UBERON:0000178`, vocabulary `UBERON`, label `blood`, and `taxon_count` 440 | `source_attestations[MADIN]` with `assertion_count: 440` and `assertion_unit: TAXON` |
| Madin habitat-taxon table | `data/raw/madin_habitat_taxa.tsv:1151-1175` list the 25 emitted taxa for `UBERON:0000178` | 25 generated `characteristic_taxa` entries, each with `candidate_pool: 440` |

## Evidence

Every generated scientific claim in this record is source-derived:

| Claim | Nearest source | Review |
|---|---|---|
| Blood label, definition, synonyms, and haemolymphatic-fluid parent | `data/raw/ontology_terms.tsv:12925-12926`; `data/raw/ontology_subclass_edges.tsv:11037` | Supported exactly |
| 12 physicochemical parameter bands for `host_animal_endotherm_blood` | `data/raw/environment_parameters.tsv:344-355` | Supported; raw labels normalize to `GRADIENTS`, `NUTRIENTS`, `ORGANIC_MATTER`, `PRESSURE`, `SALINITY`, `STRUCTURAL_COMPLEXITY`, `TEMPERATURE`, `WATER_AVAILABILITY`, `PH`, `SALINITY_VARIABILITY`, `TEMPERATURE_VARIABILITY`, and `WATER_VARIABILITY` |
| Madin attests 440 taxa for blood | `data/raw/madin_habitats.tsv:16` | Supported |
| The inline Madin taxa and labels | `data/raw/madin_habitat_taxa.tsv:1151-1175` | Supported; all 25 emitted rows retain source `MADIN` and `candidate_pool: 440` |

The record has no curator-authored causal graph. That is consistent with `curation/causal_graphs/` containing no `*blood*` overlay, and no upstream source in this record supplies causal edges.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- `source_attestations` captures the two source surfaces that seed this record: `ENVIRONMENTS_TABLE` and `MADIN`.
- All 12 raw environment-parameter rows for `host_animal_endotherm_blood` are represented once in `environmental_parameters`.
- All 25 inline Madin taxon rows for `UBERON:0000178` are represented once in `characteristic_taxa`.
- `characteristic_taxa.corroborated_by` is absent because the raw `corroborated_by` column is blank for all 25 rows.
- `causal_graphs`, `discussions`, and `datasets` are absent; no maintained overlay or source row supplies them for this record.
- `mapping_status` stays `SEEDED`, as expected; there is no item-level curation decision that would promote these source attestations to `REVIEWED`.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if a future curation change adds a reviewed decision or causal graph overlay for this ontology-grounded blood record:

- `just validate data/habitats/host_associated/blood__d6f5ae05.yaml`
- `just validate-strict data/habitats/host_associated/blood__d6f5ae05.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If the future change touches Madin extraction, also confirm that `data/raw/madin_habitats.tsv` and `data/raw/madin_habitat_taxa.tsv` still agree on the `UBERON:0000178` count and emitted taxa.

## Additional Notes

- `find reports/yaml_record_review -name '*blood__d6f5ae05*' -print` found no pre-existing exact review report for this record before this file was written.
- Ignored/hidden-inclusive exact searches with `rg --no-ignore --hidden` covered `data/raw`, `data/habitats`, `curation`, `history`, `research`, `reports`, `docs`, `.claude`, and `src`, while excluding generated `data/text_map/**` and `pages/**`; they found no maintained decision row, term request, history record, causal-graph overlay, or prior exact YAML review report for `blood__d6f5ae05`.
- Related research reports mention `UBERON:0000178` and `host_animal_endotherm_blood` while evaluating broader endotherm host concepts; those reports are leads only and do not feed this generated record.
