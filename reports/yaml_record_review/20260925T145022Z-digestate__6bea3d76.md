# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/digestate__6bea3d76.yaml
- Started UTC: 2026-09-25T14:50:22Z
- Finished UTC: 2026-09-25T14:50:22Z
- Verdict: needs curation

## Target

Reviewed the complete generated record at `data/habitats/engineered/digestate__6bea3d76.yaml`. The record is the GOLD-only engineered child for `Engineered > Bioreactor > Anaerobic > Digestate`, currently carried as the minted identifier `habitatmech:GOLD.43db7aa909`.

The generated file has `label: Digestate`, `habitat_category: ENGINEERED`, `grounding_status: UNGROUNDED`, and `mapping_status: SEEDED`. Its generated parent is the already reviewed `ENVO:00002124` `anaerobic bioreactor`, inherited from the immediate `Engineered > Bioreactor > Anaerobic` source-path parent. Its only source attestation is `gold.ecosystem:5582`.

The file is generated from `data/raw/` plus seeded path logic. A class-level `curation/decisions.tsv` sweep row confirms that no vendored ontology term lexically matched `Digestate`, but it deliberately did not item-review this GOLD path or make it a term-request candidate. Future item review belongs in `curation/decisions.tsv`; a future path-qualified label, definition, and material parent belong in `curation/term_requests.tsv`; any future target-specific mechanism overlay belongs under `curation/causal_graphs/`.

## Validation

All required local checks passed:

- `just validate data/habitats/engineered/digestate__6bea3d76.yaml`
- `just validate-strict data/habitats/engineered/digestate__6bea3d76.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000 --status all --out /tmp/habitatmech-digestate-worklist.tsv`
- `just report`

No validator was skipped.

## Identity and Grounding

The generated identifier, label, category, source attestation, and path slug agree with the maintained inputs. `data/raw/gold_ecosystem_paths.tsv` has the exact `Engineered > Bioreactor > Anaerobic > Digestate` row at depth 4, with two collapsed GOLD node IDs, zero direct `organism_count`, `study_count`, `biosample_count`, and `total_assertions` values in the bulk ecosystem-path inventory, and `gold.ecosystem:5582|gold.ecosystem:5583` as the source node list. `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.43db7aa909` to the stable `digestate__6bea3d76` slug.

The `UNGROUNDED` status is inherited only from the class-level sweep row for `habitatmech:GOLD.43db7aa909`; that row says no term in the vendored slice matched the `Digestate` label by the sweep's lexical routes and explicitly leaves the item's habitat identity unassessed. Exact ignored-inclusive searches found no vendored ontology `digestate` term, target-specific authored term request, term-request exclusion, deep-research report, causal overlay, append-only history record, label residual, or prior YAML-record review for `habitatmech:GOLD.43db7aa909`, `gold.ecosystem:5582`, `gold.ecosystem:5583`, `digestate__6bea3d76`, or the exact source path.

The generated `ENVO:00002124` parent is the item-reviewed exact grounding for GOLD `Engineered > Bioreactor > Anaerobic`. That parent has an item-level `curation/decisions.tsv` row and a generated exact record at `data/habitats/engineered/anaerobic_bioreactor.yaml`, so the upstream context term is settled. The remaining issue is that `Digestate` denotes material in or from anaerobic digestion rather than a kind of anaerobic bioreactor; if item review retains the source concept, the inherited bioreactor parent should probably be replaced with a true material genus.

## Evidence

- Supported as generated source provenance: exact ignored-inclusive searches found `gold.ecosystem:5582`, `gold.ecosystem:5583`, and the exact tab-delimited `Engineered > Bioreactor > Anaerobic > Digestate` row in `data/raw/gold_ecosystem_paths.tsv`.
- Supported as API side-table context: `data/raw/gold_path_biosamples.tsv` has 406 biosamples for numeric GOLD path ID `5583`, and `data/raw/gold_studies.tsv` has nine rows for the exact target path.
- Supported as the current reason for `grounding_status: UNGROUNDED`: `curation/decisions.tsv` has a `CLASS` `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.43db7aa909`.
- Supported as generated context but not as a strict parent: `ENVO:00002124` is the reviewed exact grounding for the immediate `Engineered > Bioreactor > Anaerobic` source path, but the target row's leaf is `Digestate`; the raw path does not establish that digestate is a subtype of the bioreactor that produced it.
- Not supported as individually reviewed habitat identity: no item-level `curation/decisions.tsv` row decides whether this source concept is a digestate material, a sample artifact, an exact match to some unvendored ontology term, or equivalent to either of the two more specific GOLD `Digestate` siblings.

The record has no definition, environmental parameters, characteristic taxa, claim-level `EvidenceItem` objects, target-specific causal graphs, discussions, or datasets to inspect.

## Completeness

The generated source attestation is complete for the committed GOLD ecosystem inventory. `data/raw/gold_ecosystem_paths.tsv` lists exactly two source node IDs for this exact path, and the generated note says two GOLD ecosystem node IDs share the path.

The GOLD API side tables add direct study and biosample context but no MIxS triad terms or environmental parameters. Exact ignored-inclusive searches over `data/raw` found the target path in `data/raw/gold_studies.tsv` and `data/raw/gold_path_biosamples.tsv`, but not in `data/raw/gold_path_triads.tsv` or `data/raw/environment_parameters.tsv`.

Before this report was written, exact ignored-inclusive searches over maintained curation, append-only history, research, generated habitat YAML, label residuals, and existing YAML-record reviews found no target-specific item decision, authored term request, generated ENVO term-request row, term-request exclusion, history record, deep-research report, causal overlay, label-correspondence residual, or prior exact review report for `habitatmech:GOLD.43db7aa909`, `gold.ecosystem:5582`, `gold.ecosystem:5583`, `digestate__6bea3d76`, or `Engineered > Bioreactor > Anaerobic > Digestate`. `find reports/yaml_record_review -maxdepth 1 -type f -name '*digestate__6bea3d76*' -print` also found no prior review report for this exact generated stem.

Broader ignored-inclusive `Digestate` searches found two sibling generated records: `Engineered > Bioreactor > Semi-continuous > Anaerobic > Digestate` and `Engineered > Bioreactor > SSF (Solid state fermentation) > Anaerobic > Digestate`. Both siblings are also minted, `UNGROUNDED`, `SEEDED`, and backed only by class-level `CONFIRM_UNGROUNDED` decisions, so future curation should review the three same-label leaves together.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `Engineered > Bioreactor > Anaerobic > Digestate` still has no item-level review, definition, or term request despite being backed by direct GOLD API rows. | `curation/decisions.tsv` has only a `CLASS` row for `habitatmech:GOLD.43db7aa909`; exact ignored-inclusive searches found no target-specific row in `curation/term_requests.tsv` or `curation/term_requests/`; the exact path has nine `data/raw/gold_studies.tsv` rows and 406 biosamples in `data/raw/gold_path_biosamples.tsv`. | `curation/decisions.tsv` and `curation/term_requests.tsv`. |
| Major | The generated `ENVO:00002124` `anaerobic bioreactor` parent is contextual and likely false as an `is-a` parent for digestate material. | The exact GOLD path says this child is `Digestate` nested under `Engineered > Bioreactor > Anaerobic`; `parent_habitats` means a strict broader relationship, but digestate is the material associated with anaerobic digestion rather than a subtype of the bioreactor vessel. | `curation/term_requests.tsv`. |

No blockers or minor findings were found.

## Recommended Edits

1. Item-review `habitatmech:GOLD.43db7aa909` against the exact `Engineered > Bioreactor > Anaerobic > Digestate` GOLD path and the two same-label `Digestate` siblings.
2. If the source concept is a real digestate material absent from the vendored slice, upgrade its row in `curation/decisions.tsv` to an `ITEM` `CONFIRM_UNGROUNDED` decision.
3. In that same future curation session, add a `curation/term_requests.tsv` row that defines digestate under a true material parent and uses `parent_mode=REPLACE` if item review confirms that the generated anaerobic-bioreactor parent is contextual.
4. Add an append-only `history/` record for that future curation session.

## Follow-up Checks

- After updating `curation/decisions.tsv` and `curation/term_requests.tsv`, run `just seed`, `just seed-canary habitatmech:GOLD.43db7aa909`, and inspect the regenerated `data/habitats/engineered/digestate__6bea3d76.yaml`.
- If the canary is correct, run `just seed-apply --force`, then `just validate-strict data/habitats/engineered/digestate__6bea3d76.yaml`, `just term-requests-check`, `just validate-history`, `just validate-causal-all`, `just verify-corpus --max-diffs 1`, and `just report`.
- If the future item review adds a causal overlay, run the focused `just validate-causal curation/causal_graphs/<slug>.yaml` before `just validate-causal-all`.

## Additional Notes

All absence checks in this review used ignored-inclusive `rg --no-ignore --hidden`, exact scans of the committed raw GOLD side tables, or `find`, bounded to maintained curation, history, research, raw inventory, generated habitat, label-residual, and review-report paths. The review did not depend on rendered `pages/` or text-map JSON.

The class-level sweep remains useful negative lexical evidence: `just report` retested every swept class with the same classifier and found zero swept concepts that the current ontology slice has since contradicted. That check is not a substitute for item review because it does not decide whether a source concept is a habitat, what true parent should replace the source-path parent, or which same-label GOLD source paths should stay separate.
