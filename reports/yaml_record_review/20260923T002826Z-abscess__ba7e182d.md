# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/abscess__ba7e182d.yaml`
- Started UTC: 2026-09-23T00:08:00Z
- Finished UTC: 2026-09-23T00:28:26Z
- Verdict: pass

## Target

| Field | Value |
| --- | --- |
| Path | `data/habitats/host_associated/abscess__ba7e182d.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.6d1b8fc8fc` |
| Label | `Abscess` |
| Habitat category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Record status | Generated from one GOLD source concept with no item-level curation decision |
| GOLD source id | `gold.ecosystem:6338` |
| GOLD source path | `Host-associated > Mammals: Human > Respiratory system > Nasal cavity > Abscess` |
| Locked slug | `data/habitats/PATHS.tsv:2092` maps `habitatmech:GOLD.6d1b8fc8fc` to `abscess__ba7e182d` |

This is the generated record for the GOLD nasal-cavity abscess path. The GOLD source-concept key is `habitatmech:GOLD.6d1b8fc8fc`, computed as `sha1("GOLD:Host-associated > Mammals: Human > Respiratory system > Nasal cavity > Abscess")[:10]`. The record is correctly minted and `NARROW`: the source concept is narrower than the generic `mesh:D000038` `Abscess` term because the full GOLD path supplies the nasal-cavity context.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/habitats/host_associated/abscess__ba7e182d.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/host_associated/abscess__ba7e182d.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Passed; validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Passed; the term-request table is current at 109 terms. |
| `just validate-history` | Passed; 77 history records validated. |
| `just verify-corpus --max-diffs 1` | Passed; all 3,206 generated records reproduced exactly from `data/raw/`, with 0 missing, 0 extra, and 0 differing. |
| `just worklist` filtered for `Abscess`, `abscess`, `GOLD\.6d1b8fc8fc`, and `mesh:D000038` | Completed with no matching rows, as expected for a seeded `NARROW` GOLD record that is not in the class-level ungrounded backlog. |
| `just report` | Passed; the corpus summary still reports 0 risky groundings not yet reviewed, 0 contradicted class-level sweeps, and 0 current records claiming a non-habitat organism/process term as a habitat. |

## Identity and Grounding

The record identity is the GOLD leaf `Abscess` under `Host-associated > Mammals: Human > Respiratory system > Nasal cavity`. `data/raw/gold_ecosystem_paths.tsv:655` carries exactly that path with source id `gold.ecosystem:6338`, 5 organism assertions, 0 study assertions, and 0 biosample assertions. `data/habitats/host_associated/abscess__ba7e182d.yaml:1` preserves the minted `habitatmech:GOLD.6d1b8fc8fc` identifier and `:11-16` preserve the same GOLD source id, source label, source path, `skos:narrowMatch` predicate, and 5 `ORGANISM` assertions.

The `habitatmech:GOLD.bcb2f8b7f3` parent is supported. The parent source path is `Host-associated > Mammals: Human > Respiratory system > Nasal cavity` in `data/raw/gold_ecosystem_paths.tsv:108`, and `data/habitats/host_associated/nasal_cavity__ae9d5dba.yaml:1-17` is the generated parent record for that same GOLD path.

The `mesh:D000038` parent is also supported. `data/raw/ontology_terms.tsv:13554` labels `mesh:D000038` as `Abscess`, and the nasal-cavity abscess source concept is a nasal-cavity-specific subtype of a generic abscess. Other raw GOLD paths reuse the leaf `Abscess` under skin, brain, spinal cord, joint capsule, abdominal/peritoneal cavity, and soft-tissue contexts, so the ambiguous-leaf rule correctly prevents this one GOLD path from becoming exactly `mesh:D000038`.

`grounding_status: NARROW` is correct: the target has two broader parents, but neither parent is an exact identity for a GOLD nasal-cavity abscess. `mapping_status: SEEDED` is also correct because exact ignored/hidden-inclusive searches found no maintained `curation/decisions.tsv`, `curation/term_requests`, `history`, or raw research artifact that reviews `habitatmech:GOLD.6d1b8fc8fc` or `gold.ecosystem:6338`.

## Evidence

| Claim | Source | Assessment |
| --- | --- | --- |
| The record represents the GOLD path `Host-associated > Mammals: Human > Respiratory system > Nasal cavity > Abscess`. | `data/raw/gold_ecosystem_paths.tsv:655`; `data/habitats/host_associated/abscess__ba7e182d.yaml:11-16` | Supported exactly. |
| The path is attested by 5 GOLD organism assertions and no study or biosample assertions. | `data/raw/gold_ecosystem_paths.tsv:655`; `data/habitats/host_associated/abscess__ba7e182d.yaml:15-17` | Supported exactly. |
| `habitatmech:GOLD.bcb2f8b7f3` is the immediate GOLD source-path parent. | `data/raw/gold_ecosystem_paths.tsv:108`; `data/habitats/host_associated/nasal_cavity__ae9d5dba.yaml:1-17`; `data/habitats/host_associated/abscess__ba7e182d.yaml:6-8` | Supported. A nasal-cavity abscess is narrower than the parent nasal-cavity GOLD path. |
| `mesh:D000038` names the broader generic abscess concept. | `data/raw/ontology_terms.tsv:13554`; `data/habitats/host_associated/abscess__ba7e182d.yaml:6-8` | Supported. The MeSH term is a valid broader parent but not an exact target for the path-specific GOLD record. |
| `abscess__ba7e182d` is the locked generated slug for this minted identifier. | `data/habitats/PATHS.tsv:2092` | Supported exactly. |

Unsupported or over-scoped claims: none found. The record carries only GOLD source provenance, a generic abscess parent, the nasal-cavity source-path parent, and seeded audit metadata. It does not claim characteristic taxa, environmental parameters, causal mechanisms, cross references, synonyms, or literature-backed definition text.

## Completeness

The record intentionally has no definition, synonyms, environmental parameters, characteristic taxa, causal graphs, discussion links, datasets, or record-level evidence. Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.6d1b8fc8fc`, `gold.ecosystem:6338`, the full GOLD nasal-cavity abscess path, and `abscess__ba7e182d` found no maintained curation, history, research, test, or report artifact outside generated data, `data/raw/gold_ecosystem_paths.tsv`, and `data/habitats/PATHS.tsv`.

Filename searches also found no target-specific maintained overlay or prior report: `find curation/causal_graphs history research/habitats reports/yaml_record_review -name '*abscess__ba7e182d*' -print` and the same search for `*6d1b8fc8fc*` returned no rows before this report was written. Those `find` checks include ignored files beneath the checked directories.

## Findings

None.

## Recommended Edits

None.

No GitHub issue is needed from this YAML review because the generated record is internally valid, reproduced from maintained inputs, and semantically consistent with its raw GOLD path.

## Follow-up Checks

Re-run this focused validation set if future curation changes the GOLD nasal-cavity abscess decision, source-path parent emission, generic abscess grounding, or path lock:

- `just validate data/habitats/host_associated/abscess__ba7e182d.yaml`
- `just validate-strict data/habitats/host_associated/abscess__ba7e182d.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist`
- `just report`
- `git diff --check`

## Additional Notes

- The pre-report exact filename check `find reports/yaml_record_review -maxdepth 1 -type f -name '*-abscess__ba7e182d.md' -print` returned no previous exact report; `find` includes ignored files.
- The pre-report exact record check `find data/habitats -name 'abscess__ba7e182d.yaml' -print` found exactly one generated target; `find` includes ignored files.
- Exact ignored/hidden-inclusive searches found sibling abscess decisions and review reports, but none for `habitatmech:GOLD.6d1b8fc8fc`, `gold.ecosystem:6338`, or `abscess__ba7e182d`.
