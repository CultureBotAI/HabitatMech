# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/engineered/dairy_farm.yaml`
- Started UTC: 2026-09-24T09:35:05Z
- Finished UTC: 2026-09-24T09:39:20Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/engineered/dairy_farm.yaml` |
| Class | `HabitatRecord` |
| Identifier | `ENVO:03501120` |
| Label | `dairy farm` |
| Category | `ENGINEERED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| GOLD source-concept key | `habitatmech:GOLD.2a19ce80e2` |
| Maintained owners | Generated from `data/raw/gold_ecosystem_paths.tsv`, `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, `data/habitats/PATHS.tsv`, and `src/habitatmech/seed.py`; `data/habitats/` is generated and remains read-only |

This generated GOLD-only record reviews the path:

```text
Engineered > Wastewater > Industrial wastewater > Dairy farm
```

The uncurated GOLD source path has the curation key
`habitatmech:GOLD.2a19ce80e2`. The seeder grounded it to the ENVO `dairy farm`
facility term because the GOLD leaf label exactly matched ENVO's label.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/dairy_farm.yaml` | Passed: LinkML reported no issues |
| `just validate-strict data/habitats/engineered/dairy_farm.yaml` | Passed: 1 file scanned, 0 files with errors, 0 total error rows |
| `just validate-causal <overlay>` | Not applicable: the record has no `causal_graphs` slot, and ignored/hidden-inclusive exact searches found no `curation/causal_graphs/` overlay for `ENVO:03501120`, `habitatmech:GOLD.2a19ce80e2`, `Engineered > Wastewater > Industrial wastewater > Dairy farm`, or `dairy_farm` |
| `just validate-causal-all` | Passed: 32 causal-graph curation files with 32 graphs |
| `just term-requests-check` | Passed: term-request table is current with 109 terms |
| `just validate-history` | Passed: no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Passed: expected 3,206 records, found 3,206, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | Passed: 0 ungrounded records still undecided, 1,810 decisions on file |
| `just report` | Passed: corpus report completed for 3,206 records |
| Reference validator | Not applicable: this record has no DOI, PMID, URL, `EvidenceItem`, or causal-edge evidence that would need citation checking |
| `git diff --check` | Passed before and after this report was written |

## Identity and Grounding

| Claim | Maintained evidence | Assessment |
|---|---|---|
| The output identifier is `ENVO:03501120`. | `data/habitats/PATHS.tsv` pins `ENVO:03501120` to `dairy_farm`. | Supported. |
| The generated label and definition are ENVO-derived. | `data/raw/ontology_terms.tsv` has `term_id` `ENVO:03501120`, `ontology` `ENVO`, label `dairy farm`, and definition `A farm which produces milk or products derived from milk.` | Supported for the ENVO term, but not for the GOLD source concept. |
| The GOLD path denotes a dairy-farm industrial wastewater bin, not the farm facility itself. | `data/raw/gold_ecosystem_paths.tsv` has the exact path `Engineered > Wastewater > Industrial wastewater > Dairy farm`, with `ecosystem_type` `Industrial wastewater` and leaf `Dairy farm`. | Not supported by the generated identity: an industrial-wastewater child is not exactly the same thing as a dairy farm facility. |
| The source path maps to `gold.ecosystem:5513`. | `data/raw/gold_ecosystem_paths.tsv` has one row for `Engineered > Wastewater > Industrial wastewater > Dairy farm` with two GOLD node IDs, `gold.ecosystem:5513|gold.ecosystem:5514`; the generated attestation uses the first node and records the duplicate-node note. | Supported. |
| `mapping_predicate: skos:exactMatch` and `grounding_status: EXACT` are curated. | There is no `curation/decisions.tsv` row for `habitatmech:GOLD.2a19ce80e2`; `src/habitatmech/seed.py` selected the unreviewed `gold_leaf_label` route from the leaf text `Dairy farm`. | Not supported. |
| `ENVO:00000078` `farm` is a strict parent of `ENVO:03501120` `dairy farm`. | `data/raw/ontology_subclass_edges.tsv` asserts `ENVO:03501120 rdfs:subClassOf ENVO:00000078`. | Supported. |
| `ENVO:01000964` `industrial wastewater` is a strict parent of the generated record. | `src/habitatmech/seed.py` links each GOLD concept to the concept of its parent path; here the source parent `Engineered > Wastewater > Industrial wastewater` is grounded to `ENVO:01000964`. | Not supported for the current `ENVO:03501120` identity: a dairy farm is not a kind of industrial wastewater. |
| The source attestation has `assertion_count: 5` and `assertion_unit: ORGANISM`. | The exact `data/raw/gold_ecosystem_paths.tsv` row records `organism_count` 5 for the collapsed GOLD path. | Supported. |

The maintained raw rows support GOLD's dairy-farm industrial wastewater path:
the collapsed GOLD row carries five organism assertions, `data/raw/gold_path_biosamples.tsv`
links the same path through node `5514` to eight biosamples, and
`data/raw/gold_studies.tsv` lists one study, `Gs0164320`, that includes the
path. They do not support the generated `skos:exactMatch` to ENVO's farm
facility class.

## Evidence

The source attestation is an accurate transcription of the committed GOLD row:

- `source: GOLD`
- `source_id: gold.ecosystem:5513`
- `source_label: Dairy farm`
- `source_path: Engineered > Wastewater > Industrial wastewater > Dairy farm`
- `mapping_predicate: skos:exactMatch`
- `assertion_count: 5`
- `assertion_unit: ORGANISM`
- `notes: 2 GOLD ecosystem node ids share this path; first shown. See data/raw/gold_ecosystem_paths.tsv.`

The attestation proves that GOLD uses this path and has five organism
assertions there. It does not prove that the path is the same concept as
`ENVO:03501120` `dairy farm`; the path context narrows the leaf label to an
industrial-wastewater category.

Ignored/hidden-inclusive exact searches found no row for
`Engineered > Wastewater > Industrial wastewater > Dairy farm`,
`gold.ecosystem:5513`, or `gold.ecosystem:5514` in
`data/raw/gold_path_triads.tsv`; no MIxS broad, local, or medium triad is
available for this exact GOLD path.

There is no claim-level evidence, causal graph, or reference-bearing assertion
on this reviewed GOLD record.

## Completeness

- The generated target contains every slot currently emitted from the
  maintained exact GOLD and ENVO rows: ontology identity, ENVO definition,
  engineered category, exact grounding, the GOLD source attestation, ontology
  and source-path parents, and generated curation history.
- The empty `synonyms`, `environmental_parameters`, `characteristic_taxa`,
  `evidence`, `causal_graphs`, `discussions`, and `datasets` slots are
  appropriate for the current maintained inputs. No maintained exact MIxS
  triad row, taxon association, claim-level evidence item, causal overlay,
  discussion item, or dataset reference was found for this source concept.
- Ignored/hidden-inclusive exact searches covered `curation/decisions.tsv`,
  `curation/term_requests.tsv`, `curation/external_xrefs.tsv`,
  `curation/causal_graphs/`, `history/`, `reports/yaml_record_review/`,
  `research/`, and the committed `data/raw/` inventories for `ENVO:03501120`,
  `habitatmech:GOLD.2a19ce80e2`,
  `Engineered > Wastewater > Industrial wastewater > Dairy farm`,
  `gold.ecosystem:5513`, `gold.ecosystem:5514`, and `dairy_farm`; they found
  the cited GOLD, ENVO, and `PATHS.tsv` rows, and no maintained target
  decision, term request, external xref, causal overlay, history file, prior
  exact review, or research report.
- A gitignore-independent `find` search covered `reports/yaml_record_review/`
  before report creation; no exact `*-dairy_farm.md` report was found.

## Findings

### Blocker

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| `HM-DAIRY-FARM-001` | The record claims `ENVO:03501120` `dairy farm` as an exact identity for a GOLD source concept whose full path denotes industrial wastewater from a dairy farm. This makes the record denote the farm facility rather than the wastewater habitat described by GOLD. | `data/raw/ontology_terms.tsv` defines `ENVO:03501120` as a farm; the exact GOLD row is `Engineered > Wastewater > Industrial wastewater > Dairy farm`; `src/habitatmech/seed.py` selected `ENVO:03501120` through the uncurated `gold_leaf_label` route, keyed for future curation as `habitatmech:GOLD.2a19ce80e2`. | Add an item-level row for `habitatmech:GOLD.2a19ce80e2` in `curation/decisions.tsv` that demotes `ENVO:01000964` `industrial wastewater` to a parent of a minted source concept, then re-seed the generated record. |

### Major

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| `HM-DAIRY-FARM-002` | `parent_habitats` currently asserts both `ENVO:00000078` `farm` and `ENVO:01000964` `industrial wastewater` as strict parents of the same `ENVO:03501120` record. That parent set is coherent only if the source concept is split away from the ENVO farm facility term. | `ENVO:03501120 rdfs:subClassOf ENVO:00000078` is asserted in `data/raw/ontology_subclass_edges.tsv`; `ENVO:01000964` is introduced only by `src/habitatmech/seed.py` from the GOLD source parent path `Engineered > Wastewater > Industrial wastewater`. | After adding the `curation/decisions.tsv` demotion, add a `curation/term_requests.tsv` definition for `habitatmech:GOLD.2a19ce80e2`, using `ENVO:01000964` as the broader parent and a label such as `dairy farm industrial wastewater` so the regenerated record no longer names the facility itself. |

No minor findings found.

## Recommended Edits

1. Add an item-level `curation/decisions.tsv` row for
   `habitatmech:GOLD.2a19ce80e2` that rejects the automatic exact
   `dairy farm` grounding and keeps `ENVO:01000964` `industrial wastewater` as
   a `GROUND_AS_PARENT` target.

2. Add a `curation/term_requests.tsv` row for `habitatmech:GOLD.2a19ce80e2`
   with a scoped label and definition for industrial wastewater generated by a
   dairy farm. The definition should use `ENVO:01000964` as its genus and
   explain why `ENVO:03501120` is the source facility, not the habitat
   identity.

3. Regenerate the target from maintained inputs. Do not hand-edit
   `data/habitats/engineered/dairy_farm.yaml`; `data/habitats/` is generated
   output.

## Follow-up Checks

| Scope | Command or check |
|---|---|
| Target schema | `just validate data/habitats/engineered/dairy_farm.yaml` |
| Closed-schema validation | `just validate-strict data/habitats/engineered/dairy_farm.yaml` |
| Corpus reproduction | `just verify-corpus --max-diffs 1` |
| Worklist/report sanity | `just worklist --limit 2000` and `just report` |
| Causal overlays | `just validate-causal-all` |
| Term-request exports | `just term-requests-check` |
| History | `just validate-history` |
| Manual identity check | Confirm the regenerated record uses `habitatmech:GOLD.2a19ce80e2`, keeps `ENVO:01000964` as a broader parent, and does not claim `ENVO:03501120` `dairy farm` as `skos:exactMatch` |
| Diff hygiene | `git diff --check` |

## Additional Notes

- The generated duplicate-node note is correct: the collapsed raw row lists
  `gold.ecosystem:5513|gold.ecosystem:5514`, while `seed_from_sources` emits
  only the first node as `source_id` and records the duplicate-node note.
- This is a path-context defect rather than an absent-vocabulary defect. The
  ENVO `industrial wastewater` parent already exists in the vendored slice;
  the missing HabitatMech concept is the narrower wastewater category scoped
  to dairy-farm provenance.
