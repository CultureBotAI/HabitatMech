# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/agricultural_waste_material.yaml
- Started UTC: 2026-09-23T19:05:50Z
- Finished UTC: 2026-09-23T19:09:09Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | ENVO:01000371 |
| Label | agricultural waste material |
| Class | HabitatRecord |
| Category | ENGINEERED |
| Grounding status | CLOSE |
| Mapping status | REVIEWED |
| Maintained owner | generated from the vendored ontology slice, `data/raw/gold_ecosystem_paths.tsv`, `data/raw/prego_habitats.tsv`, `data/raw/prego_habitat_taxa.tsv`, `curation/decisions.tsv`, and `data/habitats/PATHS.tsv`; the false `mesh:D062611` parent is generated by the GOLD parent-path pass in `src/habitatmech/seed.py` |

[data/habitats/engineered/agricultural_waste_material.yaml](../../data/habitats/engineered/agricultural_waste_material.yaml) is the merged record for GOLD `Engineered > Solid waste > Agricultural waste` and PREGO `ENVO:00002265`; the GOLD source-concept key is `habitatmech:GOLD.2021aa3231`, and the obsolete PREGO source-concept key is `habitatmech:PREGO.ae82b5486e`.

The target was resolved with ignored-file-inclusive searches:

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-agricultural_waste_material.md' -print` found no prior exact review report.
- `find curation/causal_graphs -maxdepth 1 -type f -name 'agricultural_waste_material.yaml' -print` found no causal-graph overlay for this record.
- `rg --no-ignore --hidden -n --fixed-strings 'ENVO:01000371' curation data/habitats data/raw history research --glob '!data/text_map/**' --glob '!pages/**' --glob '!build/**' --glob '!.venv/**'` found the ontology term and subclass edge, the slug lock, this generated record, two generated agricultural-waste child records, the PREGO `BROAD` decision, the obsolete `ENVO:00002265` retired-URL row, and the obsolete-replacement history entry.
- `rg --no-ignore --hidden -n --fixed-strings 'habitatmech:GOLD.2021aa3231' curation data/habitats data/raw history research --glob '!data/text_map/**' --glob '!pages/**' --glob '!build/**' --glob '!.venv/**'` found only the GOLD item-level `REVIEW` row and this record's generated history.
- `rg --no-ignore --hidden -n --fixed-strings 'habitatmech:PREGO.ae82b5486e' curation data/habitats data/raw history research --glob '!data/text_map/**' --glob '!pages/**' --glob '!build/**' --glob '!.venv/**'` found only the PREGO item-level `BROAD` grounding row and this record's generated history.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/agricultural_waste_material.yaml` | pass; no LinkML issues found |
| `just validate-strict data/habitats/engineered/agricultural_waste_material.yaml` | pass; 1 file scanned, 0 files with ERROR, 0 total ERROR rows |
| `just validate-causal curation/causal_graphs/agricultural_waste_material.yaml` | not applicable; exact `find curation/causal_graphs -maxdepth 1 -type f -name 'agricultural_waste_material.yaml' -print` found no overlay |
| `just validate-causal-all` | pass; 32 causal-graph curation files with 32 graphs validated |
| Reference validator | not applicable; this generated GOLD/PREGO merge has no causal graph and no DOI, PMID, or URL evidence slots to inspect |
| `just term-requests-check` | pass; term-request table is current with 109 terms |
| `just validate-history` | pass; no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | pass; 3206 expected, 3206 found, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | pass; 0 ungrounded records still undecided; 1810 decisions on file |
| `just report` | pass; 3206 records; no global issue specific to `ENVO:01000371` surfaced |
| `git diff --check` | pass |

## Identity and Grounding

The record's identifier, label, and ENVO definition agree. `data/raw/ontology_terms.tsv` has `ENVO:01000371` with label `agricultural waste material`, synonym `agricultural waste`, and definition text identical to this record. The vendored subclass slice asserts `ENVO:01000371 rdfs:subClassOf ENVO:00002264`, which accounts for the valid `waste material` parent.

The GOLD source concept is reviewed at item depth. `GOLD:Engineered > Solid waste > Agricultural waste` hashes to `habitatmech:GOLD.2021aa3231`, and `curation/decisions.tsv` has an item-level `REVIEW` row saying the seeded `CLOSE` grounding is correct because the GOLD solid-waste path and the ENVO term differ in framing or specificity.

The PREGO source concept is also reviewed at item depth. `PREGO:ENVO:00002265` hashes to `habitatmech:PREGO.ae82b5486e`; its `GROUND` row maps obsolete `ENVO:00002265` to `ENVO:01000371` with `BROAD` status because the obsolete term denoted agricultural wastewater while the ENVO replacement covers manure, slaughterhouse waste, harvest waste, run-off, salt, and silt as well. The merged record is therefore `REVIEWED` because both feeding source concepts have `ITEM` decisions.

`mesh:D062611` `Solid Waste` is not a true broader term for the record identity. It comes from GOLD's source tree, where the narrower GOLD leaf sits below `Engineered > Solid waste`; ENVO's `agricultural waste material` also covers non-solid material such as run-off and dissolved pesticides in water, air, or soils.

## Evidence

This record has one GOLD and one PREGO source attestation:

- The `GOLD` attestation mirrors `data/raw/gold_ecosystem_paths.tsv`: source id `gold.ecosystem:4908`, source label `Agricultural waste`, source path `Engineered > Solid waste > Agricultural waste`, three collapsed GOLD node ids, and `2` direct organism assertions.
- The `PREGO` attestation mirrors `data/raw/prego_habitats.tsv`: source id `ENVO:00002265`, source label `ENVO:00002265`, `5` direct TAXON assertions, maximum score `4.0`, and `annotated_genomes_isolates` as the evidence channel.
- The five `characteristic_taxa` entries exactly mirror the five ranked `data/raw/prego_habitat_taxa.tsv` rows for obsolete `ENVO:00002265`; none is curator-upgraded with `is_characteristic: true`.

No evidence-backed causal mechanism evidence is present.

Unsupported or over-scoped claims:

- `parent_habitats: mesh:D062611` overstates a source-tree placement for the close GOLD source as an `is-a` parent of the broader ENVO record.

## Completeness

The record carries the generated source-owned and ontology-owned content for this merge: the ENVO definition and synonym, the valid ENVO superclass, the GOLD synonym and source attestation, the PREGO synonyms, PREGO taxon associations, item-level curation events for both source concepts, and the generated slug lock.

The hierarchy is materially incomplete because one generated parent is false. Exact ignored-file-inclusive searches found no causal-graph overlay, term-request row, or other maintained input that overrides the inherited GOLD `Solid Waste` parent for `ENVO:01000371`.

Optional environmental-parameter, discussion, dataset, and causal-graph slots are correctly absent; the GOLD and PREGO inventories provide source occurrence and taxon-association evidence, not causal mechanism edges or environmental parameter rows.

## Findings

- **major**: `parent_habitats` asserts `agricultural waste material` `is-a` `mesh:D062611` `Solid Waste`.

  **Evidence**: The `mesh:D062611` edge is generated from the GOLD path `Engineered > Solid waste > Agricultural waste`. That source-path placement is valid for the narrower GOLD concept, but the record has resolved to `ENVO:01000371`, whose definition includes manure, slaughterhouse and harvest waste, fertilizer run-off, pesticides that enter water, air or soils, and salt and silt drained from fields. ENVO asserts only `ENVO:00002264` `waste material` as the direct superclass for `ENVO:01000371`; the committed PREGO obsolete-replacement history also says the ENVO term covers more than agricultural wastewater. `CLAUDE.md` requires every `parent_habitats` edge to be strictly broader, and solid waste is not strictly broader than the whole ENVO class.

  **Maintained owner**: `src/habitatmech/seed.py` currently adds the GOLD parent-path edge after source concepts are resolved. No current `curation/decisions.tsv` or `curation/term_requests.tsv` row can retain the GOLD `CLOSE` grounding while suppressing only this inherited source-path parent on an ontology-grounded record.

## Recommended Edits

- Add either a maintained source-path parent exclusion or a seeder guard that keeps a close/broad source concept's narrower source-path parent from becoming a parent of the broader ontology identity it resolves to.
- Apply that path to `habitatmech:GOLD.2021aa3231` so `ENVO:01000371` retains the GOLD `CLOSE` attestation but drops `mesh:D062611`.
- Regenerate `ENVO:01000371` and inspect [data/habitats/engineered/agricultural_waste_material.yaml](../../data/habitats/engineered/agricultural_waste_material.yaml) to confirm `ENVO:00002264` is retained, `mesh:D062611` is gone, and the generated `Composting` and `Spent mushroom substrate` children remain intentional children of the agricultural-waste material record.

## Follow-up Checks

The narrow proof path for a future fix is:

- add or update focused tests proving the GOLD source-path parent from `Engineered > Solid waste` is not copied onto `ENVO:01000371`
- `just seed`
- `just seed-canary ENVO:01000371`
- inspect [data/habitats/engineered/agricultural_waste_material.yaml](../../data/habitats/engineered/agricultural_waste_material.yaml) and confirm `mesh:D062611` is no longer in `parent_habitats`
- inspect [data/habitats/engineered/composting__fe5ff5cf.yaml](../../data/habitats/engineered/composting__fe5ff5cf.yaml) and [data/habitats/engineered/spent_mushroom_substrate.yaml](../../data/habitats/engineered/spent_mushroom_substrate.yaml) for the intended child relationships to `ENVO:01000371`
- `just validate-strict data/habitats/engineered/agricultural_waste_material.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- ignored-file-inclusive exact searches for `ENVO:01000371`, `mesh:D062611`, `habitatmech:GOLD.2021aa3231`, and any new parent-exclusion input
- `git diff --check`

## Additional Notes

The sibling `Engineered > Wastewater > Industrial wastewater > Agricultural wastewater` GOLD source is already a separate generated record at [data/habitats/engineered/agricultural_wastewater.yaml](../../data/habitats/engineered/agricultural_wastewater.yaml). Keeping that record separate is consistent with the PREGO correction that refused to treat agricultural wastewater as exact to all `agricultural waste material`.

`mesh:D062611` remains a true identity for the standalone GOLD `Engineered > Solid waste` record at [data/habitats/engineered/solid_waste.yaml](../../data/habitats/engineered/solid_waste.yaml). The problem is only inheriting that parent onto the broader ENVO agricultural-waste class.
