# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/soil__5aefc937.yaml
- Started UTC: 2026-09-25T07:04:45Z
- Finished UTC: 2026-09-25T07:07:25Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| LinkML class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.e9844fc187` |
| Label | `Soil` |
| File | `data/habitats/engineered/soil__5aefc937.yaml` |
| Category | `ENGINEERED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Generated or maintained | Generated from `data/raw/` inventories by `scripts/seed_from_sources.py` |

The record is the GOLD path `Engineered > Artificial ecosystem > Plant growth
chamber > Soil`. Its source row is `data/raw/gold_ecosystem_paths.tsv:1143`, a
depth-4 row with leaf label `Soil`, two collapsed GOLD ecosystem node IDs
`gold.ecosystem:7692|gold.ecosystem:7693`, and zero organism, study,
biosample, and total assertion counts in the core kg-microbe inventory.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/soil__5aefc937.yaml` | Passed; LinkML reported no issues. |
| `just validate-strict data/habitats/engineered/soil__5aefc937.yaml` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current for 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against the vendored history schema. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 records, found 3206, with 0 missing, 0 extra, and 0 differing records. |
| `just worklist --limit 2000 --status all --out /tmp/habitatmech-plant-growth-worklist.tsv` | Passed; wrote 953 ungrounded worklist rows. An exact target search found no row because this is a seeded `NARROW` record, not an `UNGROUNDED` backlog entry. |
| `just report` | Passed; rebuilt the corpus report over 3206 records. |

No validator was skipped.

## Identity and Grounding

- The record's minted identifier `habitatmech:GOLD.e9844fc187` is a generated,
  content-hashed GOLD identifier and is pinned to slug `soil__5aefc937` in
  `data/habitats/PATHS.tsv:3030`.
- The only upstream attestation is GOLD, with `source_id: gold.ecosystem:7692`,
  `source_label: Soil`, `source_path: Engineered > Artificial ecosystem >
  Plant growth chamber > Soil`, and `mapping_predicate: skos:narrowMatch`.
  This agrees with `data/raw/gold_ecosystem_paths.tsv:1143`.
- The attestation note is consistent with the raw source row: two GOLD node
  IDs collapse to the same canonical path, and `src/habitatmech/seed.py`
  intentionally records the first ID and points back to
  `data/raw/gold_ecosystem_paths.tsv` rather than emitting the full ID list.
- The `ENVO:00001998` parent is the ontology-grounded `soil` record. The
  ambiguous-leaf rule keeps this deeper engineered GOLD `Soil` path minted and
  records generic soil only as a broader parent, so the `skos:narrowMatch`
  predicate and `grounding_status: NARROW` are consistent.
- The parent `habitatmech:GOLD.86cf49cefb` is the generated record for the
  next broader source path `Engineered > Artificial ecosystem > Plant growth
  chamber`, so the hierarchy preserves GOLD's source-path structure.
- An ignored-inclusive exact search of the committed GOLD path inventories for
  `Engineered > Artificial ecosystem > Plant growth chamber > Soil >` found no
  narrower GOLD descendants below this path.
- `mapping_status: SEEDED` is consistent with the absence of an item-level
  curation decision for `habitatmech:GOLD.e9844fc187`.

## Evidence

- The record has no curator-authored `evidence` entries, causal graphs,
  environmental parameters, characteristic taxa, discussions, or datasets.
- No claim-level literature evidence is required for the generated GOLD source
  attestation itself. The attestation is backed by the committed raw GOLD path
  row.
- The supplemental GOLD biosample inventory records 88 biosamples for
  `Engineered > Artificial ecosystem > Plant growth chamber > Soil` at
  `data/raw/gold_path_biosamples.tsv:260`.
- The supplemental GOLD study inventory lists three studies containing this
  path at `data/raw/gold_studies.tsv:2142`, `:2341`, and `:4295`.
- `data/raw/gold_path_triads.tsv:17-19` records one-study MIxS triad leads for
  this exact path: `env_broad_scale` mapped to `ENVO:00000446`, `env_local_scale`
  mapped to `ENVO:01000621`, and `env_medium` mapped to `ENVO:00001998`. The
  `env_medium` row is compatible with the broader soil parent, but the MIxS
  triad rows are contextual evidence and are not automatic identity,
  parameter, or hierarchy inputs for this generated record.

## Completeness

- The empty optional record slots are acceptable for this generated GOLD path.
  The core GOLD inventory has no organism assertions for the path, and
  `src/habitatmech/seed.py` currently emits GOLD `assertion_count` only from
  the nonzero `organism_count` in `data/raw/gold_ecosystem_paths.tsv`.
- An ignored-inclusive exact search of `curation`, `history`, `research`,
  `data/habitats/PATHS.tsv`, and `data/raw` for
  `habitatmech:GOLD.e9844fc187`, `gold.ecosystem:7692`,
  `gold.ecosystem:7693`, `soil__5aefc937`, and the exact source path found the
  PATHS slug row, the raw GOLD ecosystem row, three supplemental study rows,
  one supplemental biosample row, and three supplemental MIxS triad rows before
  this report was added.
- `rg --no-ignore --hidden` found no direct decision, term-request, causal
  overlay, history, or research entries for `habitatmech:GOLD.e9844fc187`
  before this report was added.
- `find reports/yaml_record_review -name '*soil__5aefc937.md'` found no prior
  review report for this record; `find` included gitignored entries below
  `reports/yaml_record_review`.
- An ignored-inclusive exact search of prior review reports for the full source
  path `Engineered > Artificial ecosystem > Plant growth chamber > Soil` found
  no prior report covering this generated record.

## Findings

None found.

## Recommended Edits

None found.

## Follow-up Checks

None found.

## Additional Notes

- This `Soil` record is intentionally distinct from `ENVO:00001998`, because
  grounding every deeper GOLD leaf named `Soil` to ENVO's generic soil term
  would conflate engineered, terrestrial, and other source contexts.
- The committed MIxS triads for this path come from one independent study.
  They are useful curation leads, but they are not enough to make the path
  `REVIEWED` or to promote a direct identity grounding.
