# YAML Record Review: biofilm

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/biofilm__cc6fe05a.yaml`
- Started UTC: 2026-09-21T21:12:17Z
- Finished UTC: 2026-09-21T21:12:43Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/biofilm__cc6fe05a.yaml` |
| Class | `HabitatRecord` |
| Identifier | `ENVO:00002034` |
| Label | `biofilm` |
| Category | `AQUATIC` |
| Grounding | `EXACT` |
| Mapping status | `SEEDED` |
| Maintained owner | generated from the vendored ENVO slice, `curation/decisions.tsv`, GOLD/PREGO/Madin/environment-table inventories, and `curation/causal_graphs/biofilm.yaml` |

This is the generic ontology-grounded biofilm record. It merges three curated GOLD `Microfouling/Biofilm` source concepts, PREGO `ENVO:00002034`, Madin `ENVO:00002034`, generic `kg-microbe` environment-table parameter bands, and one curated causal-graph overlay.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/aquatic/biofilm__cc6fe05a.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/biofilm__cc6fe05a.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| Reference validator | Not available: `justfile` exposes no reference-specific validator. The causal graph references resolve to the expected three Nature Reviews Microbiology review articles: PMID:27510863 / DOI:10.1038/nrmicro.2016.94, PMID:20676145 / DOI:10.1038/nrmicro2415, and PMID:35149841 / DOI:10.1038/s41579-022-00692-2. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the committed ENVO term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 found, 0 missing, 0 extra, 0 differing. |

## Identity and Grounding

| Claim | Trace | Assessment |
|---|---|---|
| `ENVO:00002034` is pinned to `biofilm__cc6fe05a`. | `data/habitats/PATHS.tsv` maps `ENVO:00002034` to `biofilm__cc6fe05a`; `data/raw/ontology_terms.tsv` labels the term `biofilm` and provides the generated definition. | Supported. |
| The direct ontology parent is `ENVO:01000549`. | `data/raw/ontology_subclass_edges.tsv` has `ENVO:00002034 rdfs:subClassOf ENVO:01000549`; the generated record carries that parent. | Supported. |
| The three GOLD `Microfouling/Biofilm` rows are exact biofilm sources. | `curation/decisions.tsv` item-reviewed `habitatmech:GOLD.00cc26ac8b`, `habitatmech:GOLD.3911678ce4`, and `habitatmech:GOLD.f8048a6e2a` as `GROUND` `EXACT` to `ENVO:00002034`; the three exact paths exist in `data/raw/gold_ecosystem_paths.tsv` as `gold.ecosystem:8359`, `gold.ecosystem:8357`, and `gold.ecosystem:8364`. | Supported. |
| PREGO contributes an exact reviewed `biofilm` source. | `data/raw/prego_habitats.tsv` has `ENVO:00002034` with 325 taxa, score `4`, and evidence channels `annotated_genomes_isolates|environmental_samples`; `curation/decisions.tsv` item-reviewed `habitatmech:PREGO.94bda4d522` with `REVIEW`. | Supported. |
| Madin contributes `biofilm` source taxa. | `data/raw/madin_habitats.tsv` has `ENVO:00002034` `biofilm` with `taxon_count=39`; `data/raw/madin_habitat_taxa.tsv` contains the 25 rows emitted into the generated record. | Supported. |
| `mapping_status: SEEDED` follows from maintained inputs. | The four item-reviewed source concepts listed above feed the record, but the ENVIRONMENTS_TABLE and Madin exact `ENVO:00002034` attestations are source-owned rows with no item-level `curation/decisions.tsv` entries. | Supported. |
| The three generated `Biofouling` parents are source-path parents from the three GOLD rows. | The GOLD `Microfouling/Biofilm` rows are nested under Marine, Freshwater, and Non-marine Saline and Alkaline `Biofouling` paths; `data/habitats/PATHS.tsv` maps those source-path parents to `habitatmech:GOLD.4bd2954af9`, `habitatmech:GOLD.3ec896865d`, and `habitatmech:GOLD.ec116f9a37`. | Mechanically supported as source context, but false as global parents for generic `ENVO:00002034` `biofilm`. |

## Evidence

| Area | Status |
|---|---|
| Source attestations | The ENVIRONMENTS_TABLE, three GOLD, Madin, and PREGO attestations all match maintained input rows. The GOLD rows report zero organisms and therefore correctly omit `assertion_count`. |
| Synonyms | `Microfouling/Biofilm` is generated from the reviewed GOLD labels as an exact synonym; `biofilms` is generated from the PREGO row as a related synonym. |
| Environmental parameters | The six generated bands are the six generic `biofilm` rows in `data/raw/environment_parameters.tsv`. Multi-term rows such as `biofilm_fresh`, `biofilm_marine`, and `petroleum_biofilm` are correctly not attached to generic `ENVO:00002034`. |
| Characteristic taxa | The first 25 PREGO rows and all 25 emitted Madin rows match `data/raw/prego_habitat_taxa.tsv` and `data/raw/madin_habitat_taxa.tsv`. None is promoted to `is_characteristic`. |
| Causal graph | `curation/causal_graphs/biofilm.yaml` owns the emitted `biofilm_eps_gradient_structuring` graph and its 16 evidence-bearing edges. The six PMID/DOI reference ids on those edges resolve to three biofilm review papers whose scopes match the generic matrix, adhesion, and gradient claims. |
| Discussions and datasets | Empty. No maintained input currently targets this record with a discussion thread or external dataset row. |

No evidence item is over-scoped in the curated causal graph.

## Completeness

- An ignored-file-inclusive `find` found no pre-existing exact `*-biofilm__cc6fe05a.md` report before this report was created.
- Ignored-file-inclusive exact searches for `ENVO:00002034`, `biofilm__cc6fe05a`, `Microfouling/Biofilm`, all three GOLD Microfouling source ids, `habitatmech:PREGO.94bda4d522`, and the three generated Biofouling parent ids covered `data/raw`, `curation`, `history`, `research/habitats`, `reports/yaml_record_review`, `data/habitats/PATHS.tsv`, and generated `data/habitats`.
- Those searches found the expected ontology, GOLD, PREGO, Madin, environment-table, decision, term-request exclusion, causal-graph, and generated YAML rows; they found no prior exact YAML review and no existing maintained parent-edge suppression that would remove the three generated Biofouling parents from generic `ENVO:00002034`.
- Broader exact searches for `biofilm` in `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, and `data/raw/gold_studies.tsv` found later GOLD API context for several other path-qualified biofilms, but not for the three exact `Microfouling/Biofilm` source paths that feed this record.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| HM-BIOFILM-CC6FE05A-001 | Major | Generic `ENVO:00002034` `biofilm` inherits three false `Biofouling` parents from path-local GOLD source contexts. `parent_habitats` says all biofilms are kinds of the freshwater, marine, and inland saline/alkaline GOLD Biofouling grouping nodes, but each of those reviewed parent records says Biofouling includes macrofouling and is broader than `ENVO:00002034`; `curation/term_requests_excluded.tsv` also records them as unused grouping bins whose concrete `Microfouling/Biofilm` child already grounds to `ENVO:00002034`. | `data/habitats/aquatic/biofilm__cc6fe05a.yaml` lists `habitatmech:GOLD.3ec896865d`, `habitatmech:GOLD.4bd2954af9`, and `habitatmech:GOLD.ec116f9a37` as parents; `curation/decisions.tsv` rows for those three ids keep `ENVO:06105023` as an xref and explicitly say they are not `ENVO:00002034` biofilm because biofouling includes macrofouling; `curation/term_requests_excluded.tsv` says requesting habitat terms for those grouping nodes would mint unused bins. | `src/habitatmech/seed.py`, or a future maintained GOLD source-path parent suppression table consumed by the seeder. |

No blocker or minor findings.

## Recommended Edits

| Priority | Edit | Owner | Follow-up validator |
|---:|---|---|---|
| 1 | Add a maintained GOLD source-path parent suppression for the three exact-grounded `Microfouling/Biofilm` sources so their Biofouling parent paths do not become strict parents of generic `ENVO:00002034`. | New curation table plus `src/habitatmech/seed.py` support | `just seed`, `just seed-canary ENVO:00002034 --force`, then `just validate data/habitats/aquatic/biofilm__cc6fe05a.yaml`. |
| 2 | Re-run the same suppression path against the three generated Biofouling parent records and confirm those `CONFIRM_UNGROUNDED` xref decisions remain unchanged. | New curation table plus `curation/decisions.tsv` | `just verify-corpus --max-diffs 1` and `just validate-history`. |

## Follow-up Checks

- Re-run `just validate data/habitats/aquatic/biofilm__cc6fe05a.yaml` on the regenerated target.
- Re-run `just validate-strict data/habitats/aquatic/biofilm__cc6fe05a.yaml --quiet` to enforce closed-schema output.
- Re-run `just validate-causal-all` to confirm the generic biofilm causal overlay still attaches.
- Re-run `just validate-history` after adding the required append-only history.
- Re-run `just verify-corpus --max-diffs 1` to prove the generated YAML still reproduces from `data/raw/` and `curation/`.

## Additional Notes

- The generated YAML should remain read-only; the hierarchy fix belongs in the GOLD parent-linking curation path, not in `data/habitats/aquatic/biofilm__cc6fe05a.yaml`.
- The exact `Microfouling/Biofilm` GOLD rows have zero GOLD organism assertions and no committed later GOLD API biosample, study, or MIxS-triad rows. Their exact grounding is already curator-owned in `curation/decisions.tsv`; the unsupported part is only the inherited source-path parent edge after exact-identity merging.
