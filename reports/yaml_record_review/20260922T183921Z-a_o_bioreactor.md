# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/engineered/a_o_bioreactor.yaml`
- Started UTC: 2026-09-22T18:36:54Z
- Finished UTC: 2026-09-22T18:39:33Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.bf15464676` |
| Label | `A/O bioreactor` |
| Category | `ENGINEERED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the GOLD inventory plus `curation/decisions.tsv`; generated YAML remains read-only |
| Locked slug | `data/habitats/PATHS.tsv:2691` maps `habitatmech:GOLD.bf15464676` to `a_o_bioreactor` |

This is the generated GOLD record for the path `Engineered > WWTP > A/O treatment system > A/O bioreactor`. Its identifier is the deterministic minted id `habitatmech:GOLD.bf15464676`, computed from `sha1("GOLD:Engineered > WWTP > A/O treatment system > A/O bioreactor")[:10]`. The record is `SEEDED` because its `CONFIRM_UNGROUNDED` decision in `curation/decisions.tsv` is a `CLASS`-depth sweep, not an item-level review of the source path.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/engineered/a_o_bioreactor.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/engineered/a_o_bioreactor.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

Most direct fields agree with committed inputs:

- `data/habitats/PATHS.tsv:2691` pins `habitatmech:GOLD.bf15464676` to slug `a_o_bioreactor`, matching the reviewed YAML path.
- `data/raw/gold_ecosystem_paths.tsv:1384` records canonical path `Engineered > WWTP > A/O treatment system > A/O bioreactor`, leaf label `A/O bioreactor`, path depth `4`, two GOLD node ids, zero organism assertions, zero metagenome assertions, zero biosample assertions, and zero total assertions.
- `curation/decisions.tsv:1068` records the `CONFIRM_UNGROUNDED` class-level sweep for `habitatmech:GOLD.bf15464676`, with no object term and no category override.
- `data/raw/gold_ecosystem_paths.tsv:1383` records the immediate parent path `Engineered > WWTP > A/O treatment system`; its deterministic minted id is `habitatmech:GOLD.7c91485eb8`, computed from `sha1("GOLD:Engineered > WWTP > A/O treatment system")[:10]`.
- `data/raw/gold_ecosystem_paths.tsv:575` records the broader `Engineered > WWTP` path. `curation/decisions.tsv:1336` grounds that source concept to `ENVO:00002043` / `wastewater treatment plant`.

The target's `habitat_category: ENGINEERED` is source-derived from GOLD's top-level `Engineered` ecosystem. Its class-level `CONFIRM_UNGROUNDED` decision is also represented accurately: the generated `curation_history` carries the source concept id and the class-level note that no term in the vendored slice matched the label by the sweep routes, while explicitly warning that whether the concept is a habitat at all was not assessed.

The one identity-and-grounding defect is the generated parent. `parent_habitats` points to `habitatmech:GOLD.7c91485eb8`, but that parent record is generated from an item-level `NOT_APPLICABLE` decision in `curation/decisions.tsv:739`. `NOT_APPLICABLE` confirms that a source concept itself is not a habitat, whereas the schema defines `parent_habitats` as broader habitats.

## Evidence

| Claim | Nearest source | Review |
|---|---|---|
| Record identifier, label, engineered category, and `UNGROUNDED` class-level status | `data/raw/gold_ecosystem_paths.tsv:1384`; `curation/decisions.tsv:1068` | Supported exactly |
| Locked filename | `data/habitats/PATHS.tsv:2691` | Supported exactly |
| GOLD `source_id` | `data/raw/gold_ecosystem_paths.tsv:1384` | Supported; the raw `gold_node_ids` cell is `gold.ecosystem:8224|gold.ecosystem:8226`, and `ingest_gold()` emits the first id |
| GOLD `source_label` and `source_path` | `data/raw/gold_ecosystem_paths.tsv:1384` | Supported exactly |
| Duplicate-node note | `data/raw/gold_ecosystem_paths.tsv:1384`; `src/habitatmech/seed.py` | Supported; the raw row has two GOLD ecosystem node ids, and the importer emits the duplicate-node note when a canonical path collapses more than one id |
| Absence of `assertion_count` and `assertion_unit` on the source attestation | `data/raw/gold_ecosystem_paths.tsv:1384` | Supported; the raw GOLD row has zero organisms, zero metagenomes, zero biosamples, and zero total assertions |
| Parent `habitatmech:GOLD.7c91485eb8` | `data/raw/gold_ecosystem_paths.tsv:1383-1384`; `curation/decisions.tsv:739` | The source path supports `A/O treatment system` as the next broader GOLD path, but the generated parent record is explicitly `NOT_APPLICABLE`; see Findings |

The record has no synonyms, xrefs, definition, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussion links, datasets, or quality flags. Those absences match ignored/hidden-inclusive searches that found no authored definition, term request, external xref, causal-graph overlay, target-specific history entry, target-specific research artifact, or prior exact YAML review report for `habitatmech:GOLD.bf15464676`, `A/O bioreactor`, `a_o_bioreactor`, `gold.ecosystem:8224`, or `gold.ecosystem:8226`.

Unsupported or over-scoped claims: the `parent_habitats` entry overclaims `habitatmech:GOLD.7c91485eb8` as a broader habitat record while the parent itself is maintained as non-habitat.

## Completeness

The direct GOLD source attestation is complete for the available maintained input:

- The one `source_attestations` entry captures the first GOLD node id, leaf label, canonical source path, and duplicate-node note for a two-node canonical path.
- No assertion count is expected because the raw A/O bioreactor GOLD row has no organism, metagenome, or biosample assertions.
- No authored definition or term request is expected yet because the target only has a class-level sweep; `docs/HARMONIZATION.md#class-level-sweep` says this sweep did not determine whether the concept is a real habitat or a term-request candidate.
- No causal graph, record-level evidence, xref, environmental parameter, or characteristic taxon is expected from the current maintained inputs.

The immediate broader path needs item-level attention before this branch of the hierarchy is complete. Either `A/O treatment system` is a valid engineered habitat context and `curation/decisions.tsv:739` should stop marking it `NOT_APPLICABLE`, or the GOLD parent-path linker in `src/habitatmech/seed.py` should skip that non-habitat node and attach descendants such as `A/O bioreactor` to the next valid ancestor, currently the `Engineered > WWTP` concept grounded to `ENVO:00002043`.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and generated records on this path, excluding generated `build`, `data/text_map`, and `pages` trees where broad searches would otherwise hit generated artifacts. They found the expected GOLD row, decision row, generated YAML, path lock, parent GOLD row, and child `Activated sludge` GOLD row; they found no target-specific maintained curation or research artifacts beyond `curation/decisions.tsv:1068`.

## Findings

### Major: `A/O bioreactor` inherits a parent that is explicitly marked non-habitat

- Affected generated field: `data/habitats/engineered/a_o_bioreactor.yaml:6`
- Maintained inputs involved: `data/raw/gold_ecosystem_paths.tsv:1384`, `data/raw/gold_ecosystem_paths.tsv:1383`, `curation/decisions.tsv:739`, and `src/habitatmech/seed.py`
- Evidence: the reviewed record emits `parent_habitats: [habitatmech:GOLD.7c91485eb8]`, because `ingest_gold()` links each GOLD path to the next level up. That parent id is `A/O treatment system`, but `curation/decisions.tsv:739` says the `A/O treatment system` source concept is `NOT_APPLICABLE`.
- Impact: `parent_habitats` is a broader-habitat claim. Linking a habitat to a record that the maintained curation marks as non-habitat makes this local hierarchy internally inconsistent and lets descendants inherit a non-habitat ancestor.

## Recommended Edits

1. Revisit `curation/decisions.tsv:739` for `habitatmech:GOLD.7c91485eb8`.

   If `A/O treatment system` is a real engineered habitat with no exact ontology term, replace the `NOT_APPLICABLE` row with an item-level `CONFIRM_UNGROUNDED` decision and reasoning scoped to the source path `Engineered > WWTP > A/O treatment system`.

2. If `A/O treatment system` is intentionally non-habitat, update the GOLD parent-path logic in `src/habitatmech/seed.py` so descendants skip parent records whose grounding status is `NOT_APPLICABLE` and reattach to the nearest valid habitat ancestor, then regenerate the corpus from `data/raw/`.

## Follow-up Checks

- `just seed`
- `just seed-canary habitatmech:GOLD.bf15464676`
- `just seed-canary habitatmech:GOLD.7c91485eb8`
- `just seed-apply --force`
- `just validate data/habitats/engineered/a_o_bioreactor.yaml`
- `just validate data/habitats/engineered/a_o_treatment_system.yaml`
- `just validate-strict data/habitats/engineered/a_o_bioreactor.yaml data/habitats/engineered/a_o_treatment_system.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

After regeneration, verify that `a_o_bioreactor.yaml` no longer names any `NOT_APPLICABLE` record in `parent_habitats`, and exact-search the sibling rows under `Engineered > WWTP > A/O treatment system` to make sure `activated_sludge__e116af8f.yaml`, `settling_tank__54e3a311.yaml`, and `effluent__b6295023.yaml` have consistent parents too.

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -name '*a_o_bioreactor*' -print` found no pre-existing exact review report for this record before this file was written.
- `find curation/causal_graphs history research reports/yaml_record_review -name '*a_o_bioreactor*' -print` found no target-specific causal overlay, history record, research artifact, or prior exact review report before this file was written.
- Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.bf15464676`, `bf15464676`, `A/O bioreactor`, `a_o_bioreactor`, `gold.ecosystem:8224`, `gold.ecosystem:8226`, `habitatmech:GOLD.7c91485eb8`, and `A/O treatment system` found the generated target, raw GOLD rows, path lock, parent decision, generated parent, and generated child/sibling records cited above, and no other target-specific maintained inputs.
