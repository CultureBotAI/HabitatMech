# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/vivarium.yaml
- Started UTC: 2026-09-25T10:22:01Z
- Finished UTC: 2026-09-25T10:22:01Z
- Verdict: pass

## Target

Reviewed the complete generated record at `data/habitats/engineered/vivarium.yaml`. The record is a GOLD-only engineered habitat for `Engineered > Artificial ecosystem > Vivarium`, grounded exactly to `ENVO:00010622` `vivarium`.

The generated file has `definition_source: ENVO`, `habitat_category: ENGINEERED`, `grounding_status: EXACT`, and `mapping_status: SEEDED`. Its parents are the ENVO ontology parent `ENVO:01001816` `biomaterial containment unit` and the immediate GOLD source-path parent `habitatmech:GOLD.0acae9a1a4` `Artificial ecosystem`. Its only source attestation shows `gold.ecosystem:8021` and correctly notes that three GOLD ecosystem node IDs share this canonical path.

The file is generated from `data/raw/`; no item-level decision, authored term request, source extractor override, or causal-graph overlay currently contributes to this record. Future grounding changes belong in `curation/decisions.tsv`; future authored definition or parent-mode changes would belong in `curation/term_requests.tsv`; future causal overlays would belong under `curation/causal_graphs/`.

## Validation

All required local checks passed:

- `just validate data/habitats/engineered/vivarium.yaml`
- `just validate-strict data/habitats/engineered/vivarium.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000 --status all --out /tmp/habitatmech-vivarium-worklist.tsv`
- `just report`

No validator was skipped.

## Identity and Grounding

The generated identifier, label, category, source attestation, and path slug agree with the maintained inputs. `data/raw/gold_ecosystem_paths.tsv` has the exact `Engineered > Artificial ecosystem > Vivarium` row at depth 3, with three GOLD node IDs, zero `organism_count`, `study_count`, `biosample_count`, and `total_assertions` values in this bulk-export summary, and `gold.ecosystem:8021|gold.ecosystem:8022|gold.ecosystem:8023` as the node list. `data/habitats/PATHS.tsv` maps `ENVO:00010622` to the stable `vivarium` slug.

The `EXACT` grounding and `SEEDED` mapping follow from the GOLD leaf-label route. `data/raw/ontology_terms.tsv` defines `ENVO:00010622` with canonical label `vivarium`; the generated definition is copied from that vendored ENVO row. There is no target row in `curation/decisions.tsv`, so the record remains `SEEDED` rather than `REVIEWED`.

Both broader terms are traceable. `data/raw/ontology_subclass_edges.tsv` has `ENVO:00010622 rdfs:subClassOf ENVO:01001816`, and `data/raw/ontology_terms.tsv` defines `ENVO:01001816` as `biomaterial containment unit`. The `habitatmech:GOLD.0acae9a1a4` parent comes from the immediate GOLD path prefix `Engineered > Artificial ecosystem`, which currently resolves to `data/habitats/engineered/artificial_ecosystem.yaml`.

## Evidence

- Supported as generated source provenance: exact ignored-inclusive searches found `gold.ecosystem:8021`, `gold.ecosystem:8022`, `gold.ecosystem:8023`, and `Engineered > Artificial ecosystem > Vivarium` in `data/raw/gold_ecosystem_paths.tsv`; `gold.ecosystem:8021` is the first node shown in the generated source attestation.
- Supported as generated ontology grounding: `ENVO:00010622` `vivarium` is present in `data/raw/ontology_terms.tsv`, and the record's definition and `ENVO:01001816` parent are vendored ENVO content.
- Supported as generated source hierarchy: `habitatmech:GOLD.0acae9a1a4` is the generated identifier for the immediate GOLD parent path, `Engineered > Artificial ecosystem`.
- Supported as generated status: no contributing source concept has an item-level decision, so `mapping_status: SEEDED` follows from the current decision table.

The record has no causal graphs, environmental parameters, characteristic taxa, datasets, discussions, or claim-level `EvidenceItem` objects to inspect.

## Completeness

The generated attestation is complete for the committed GOLD ecosystem inventory. `data/raw/gold_ecosystem_paths.tsv` lists all three source node IDs, and the generated record's first-node note accounts for the second and third node IDs.

Exact ignored-inclusive searches of the GOLD API side tables found no `gold.ecosystem:8021`, `gold.ecosystem:8022`, `gold.ecosystem:8023`, or exact `Engineered > Artificial ecosystem > Vivarium` rows in `data/raw/gold_studies.tsv`, `data/raw/gold_path_biosamples.tsv`, or `data/raw/gold_path_triads.tsv`; no GOLD study, biosample, or MIxS triad context is committed for this exact path.

Exact ignored-inclusive searches over `history/`, `research/habitats/`, `curation/causal_graphs/`, `curation/term_requests.tsv`, `curation/term_requests/`, `curation/term_requests_excluded.tsv`, `conf/id_label_targets.yaml`, and `reports/yaml_record_review/` found no target-specific append-only history record, research report, causal overlay, authored term request, generated ENVO term-request row, term-request exclusion, label-correspondence residual, or prior review for `ENVO:00010622`, `gold.ecosystem:8021`, `gold.ecosystem:8022`, `gold.ecosystem:8023`, or `Engineered > Artificial ecosystem > Vivarium`. Before this report was written, `find reports/yaml_record_review -maxdepth 1 -type f \( -name '*vivarium*.md' -o -name '*Vivarium*.md' \) -print` found no prior Vivarium review report.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

None required. If future curation changes this grounding or adds an authored definition, rerun `just seed`, a focused `just seed-canary` for the affected generated identifier, `just validate-strict` on the regenerated record path, `just term-requests-check`, `just validate-history`, `just validate-causal-all`, `just verify-corpus --max-diffs 1`, and `just report`.

## Additional Notes

All absence checks in this review used ignored-inclusive `rg --no-ignore --hidden`, exact full-file scans of the committed raw side tables, or `find`, bounded to maintained curation, history, research, raw inventory, generated habitat, and review-report paths. No search depended on rendered `pages/` or text-map JSON.

The `habitatmech:GOLD.0acae9a1a4` `Artificial ecosystem` parent has a prior `needs curation` review because the parent record itself only has a class-level sweep decision. That is a parent-record curation gap, not a `vivarium` identity defect: this record is exactly grounded to ENVO, and the `Artificial ecosystem` edge is reproducibly inherited from the immediate GOLD source path.

The generated `just worklist --status all` table is scoped to ungrounded records. It did not include the direct `ENVO:00010622` Vivarium target, but did list ungrounded child rows under the broader GOLD Vivarium subtree; those child records remain future review targets and were out of scope for this single-record review.
