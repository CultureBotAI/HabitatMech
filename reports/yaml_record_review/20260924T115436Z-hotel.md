# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/hotel.yaml
- Started UTC: 2026-09-24T11:54:36Z
- Finished UTC: 2026-09-24T11:54:36Z
- Verdict: pass

## Target

| Field | Value |
| --- | --- |
| Path | `data/habitats/engineered/hotel.yaml` |
| Class | `HabitatRecord` |
| Identifier | `ENVO:03501347` |
| Label | `hotel` |
| Category | `ENGINEERED` |
| Generated or maintained | Generated from committed source inventories, the vendored ontology slice, and `data/habitats/PATHS.tsv`; `data/habitats/` remains read-only |
| Locked slug | `data/habitats/PATHS.tsv:923` maps `ENVO:03501347` to `hotel` |
| Grounding | `EXACT` |
| Mapping status | `SEEDED` |

## Validation

| Check | Result |
| --- | --- |
| `find reports/yaml_record_review -maxdepth 1 -type f -name '*-hotel.md' -print` | Passed; no prior exact hotel review was present before this report was written. `find` included ignored files under `reports/yaml_record_review`. |
| `just validate data/habitats/engineered/hotel.yaml` | Passed; no LinkML issues found. |
| `just validate-strict data/habitats/engineered/hotel.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Reference validator | Not applicable; the target record has no record-level `evidence`, no causal graph, and no DOI, PMID, or URL evidence items to validate. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 records were expected, 3206 were present, and there were 0 missing, extra, or differing records. |
| `just worklist --limit 2000` | Passed; 0 ungrounded records are still undecided and 1810 decisions are on file. |
| `just report` | Passed; completed the corpus report for 3206 records. |
| `git diff --check` | Passed before report creation; `git diff --cached --check` also passed after staging this report. |

## Identity and Grounding

The identifier, label, and definition are copied directly from the vendored ENVO row at `data/raw/ontology_terms.tsv:9966`: `ENVO:03501347`, ontology `ENVO`, label `hotel`, non-deprecated, and definition "A building which 1) containing multiple dwellings of pre-furnished accomodation and 2) is rented on a short term and/or longer-term basis."

The record's one parent, `ENVO:00000073`, is exactly the asserted ENVO superclass in `data/raw/ontology_subclass_edges.tsv:8351`, and the parent term is the vendored ENVO `building` term at `data/raw/ontology_terms.tsv:6667`.

The GOLD attestation is the exact source path `Engineered > Built environment > Building > Hotel` at `data/raw/gold_ecosystem_paths.tsv:1245`. Its leaf label is `Hotel`, its canonical path is a child of `Engineered > Built environment > Building`, and that immediate GOLD parent is the `Building` path at `data/raw/gold_ecosystem_paths.tsv:438`; the generated record's ENVO building parent is therefore consistent with both the ontology and the source hierarchy.

The generated `skos:exactMatch` is appropriate. GOLD's leaf is a hotel under a building path, ENVO's `hotel` is a kind of building, and no inspected row suggests that GOLD is using the label for a broader lodging or hospitality-process concept.

`mapping_status: SEEDED` is also appropriate: ignored/hidden-inclusive exact searches found no hotel-specific `curation/decisions.tsv` row, term request, causal overlay, or history record. This review is read-only and does not promote the record to `REVIEWED`.

## Evidence

| Claim | Evidence | Status |
| --- | --- | --- |
| The HabitatMech record denotes `ENVO:03501347` `hotel`. | `data/raw/ontology_terms.tsv:9966` and `data/habitats/engineered/hotel.yaml:1` | Supported exactly. |
| A hotel is narrower than a building. | `data/raw/ontology_subclass_edges.tsv:8351` asserts `ENVO:03501347 rdfs:subClassOf ENVO:00000073`. | Supported exactly. |
| The GOLD source concept is `Engineered > Built environment > Building > Hotel`. | `data/raw/gold_ecosystem_paths.tsv:1245` lists that canonical path with leaf label `Hotel`. | Supported exactly. |
| Two GOLD ecosystem node ids share the hotel path and `gold.ecosystem:8281` is the first shown. | `data/raw/gold_ecosystem_paths.tsv:1245` lists `gold.ecosystem:8281|gold.ecosystem:8283`. | Supported exactly. |
| No GOLD assertion count is emitted for this source attestation. | The raw hotel row reports `organism_count`, `study_count`, `biosample_count`, and `total_assertions` as 0. | Supported exactly. |

No unsupported or over-scoped claim was found.

## Completeness

The sparse record is complete for the inspected inputs. It has no synonyms, xrefs, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussions, datasets, or quality flags; the only raw hotel input is GOLD's zero-assertion aggregate path plus the ENVO term and subclass rows.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports/yaml_record_review`, `data/raw`, and `data/habitats/PATHS.tsv`, while excluding generated `pages`, `build`, and `data/text_map` outputs. They found the expected ENVO term, ENVO subclass edge, locked slug, and GOLD aggregate path; they found no hotel-specific maintained curation row, term request, causal overlay, history file, research report, prior YAML review report, GOLD biosample row, GOLD triad row, or GOLD study row.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

No correctness edit is required. If a future curation pass chooses to promote this exact seeded result, it should add an item-level `REVIEW` row for the hotel source concept in `curation/decisions.tsv`, rerun `just seed-canary ENVO:03501347`, inspect the regenerated `data/habitats/engineered/hotel.yaml`, run `just seed-apply --force`, and then rerun the record and corpus validators listed above.

## Additional Notes

The exact search for `gold.ecosystem:8281` found the expected aggregate row in `data/raw/gold_ecosystem_paths.tsv`; the exact search for `gold.ecosystem:8283` found the same row; an exact search for `gold.ecosystem:8282` found nothing because 8282 is not one of the hotel node ids in the committed aggregate row. All three searches included ignored and hidden files under the bounded searched directories.
