# YAML Record Review: algal bloom

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/algal_bloom.yaml`
- Started UTC: 2026-09-21T11:59:00Z
- Finished UTC: 2026-09-21T12:01:40Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Path | `data/habitats/aquatic/algal_bloom.yaml` |
| Class | `HabitatRecord` |
| Identifier | `ENVO:2000004` |
| Label | `algal bloom` |
| Category | `AQUATIC` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Maintained or generated | Generated from `data/raw/` plus curation inputs |

The record contains one GOLD source attestation:

| Source | Source ID | Source label | Source path | Assertion count |
|---|---|---|---|---|
| `GOLD` | `gold.ecosystem:5349` | `Algal bloom` | `Environmental > Aquatic > Freshwater > Lake > Algal bloom` | `8` `ORGANISM` assertions |

Current hierarchy:

| Parent | Label in vendored ontology | Basis |
|---|---|---|
| `ENVO:00000012` | `hydrographic feature` | Vendored ENVO edge `ENVO:2000004 rdfs:subClassOf ENVO:00000012` |
| `ENVO:00000021` | `freshwater lake` | GOLD parent path `Environmental > Aquatic > Freshwater > Lake` grounded to the ENVO freshwater-lake identity |

The record has no synonyms, xrefs, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussions, or datasets.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/algal_bloom.yaml` | Passed; LinkML reported no issues. |
| `just validate-strict data/habitats/aquatic/algal_bloom.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just term-requests-check` | Passed; the generated term-request table is current with 109 terms. |
| Reference validator | Not checked: this seeded GOLD-only record has no `EvidenceItem` references or causal overlay, and this repository documents no standalone reference validator for a generated record without those objects. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records were present, with 0 missing, 0 extra, and 0 differing files. |
| `git diff --check` | Passed; no whitespace errors. |

## Identity and Grounding

The generated source identity and provenance are reproducible:

| Claim | Review |
|---|---|
| GOLD source path | Supported. `data/raw/gold_ecosystem_paths.tsv` has the exact canonical path `Environmental > Aquatic > Freshwater > Lake > Algal bloom`. |
| Source ID | Supported. The raw GOLD ecosystem row records `gold.ecosystem:5349`. |
| Source label | Supported. The raw GOLD row sets `leaf_label` to `Algal bloom`, which case-insensitively matches the generated `label` and `source_label`. |
| Assertion count | Supported. The exact raw GOLD ecosystem row has `organism_count=8`, `study_count=0`, `biosample_count=0`, and `total_assertions=8`; the generated record stores `assertion_count: 8` with `assertion_unit: ORGANISM`. |
| Slug | Supported. `data/habitats/PATHS.tsv` pins `ENVO:2000004` to `algal_bloom`. |
| Category | Supported by the first two GOLD path levels, `Environmental > Aquatic`. |
| `mapping_status: SEEDED` | Supported. There is no item-level `curation/decisions.tsv` row for the source concept, `habitatmech:GOLD.b3f000fa4a`, so the lexical exact mapping has not been curated. |

The exact source-concept key for future curation is `habitatmech:GOLD.b3f000fa4a`, computed by the seeder's `mint("GOLD", canonical_path)` helper as the first ten hex characters of `sha1("GOLD:Environmental > Aquatic > Freshwater > Lake > Algal bloom")`.

The current exact grounding is over-broad. `ENVO:2000004` denotes any algal bloom in an aquatic system, but the sole GOLD attestation is not a bare algal-bloom source: it is a lake child under GOLD's freshwater branch. The matching MIxS rows also keep the same context: all 8 samples use `ENVO:01000252` `freshwater lake biome` as `env_broad_scale`, all 8 use `ENVO:04000007` `lake water` as `env_medium`, and 7 of 8 use the closer `ENVO:2000005` `freshwater algal bloom` as `env_local_scale`.

## Evidence

Supported:

| Claim | Evidence |
|---|---|
| The source path is an asserted GOLD freshwater-lake algal bloom concept, not a generic algal-bloom source. | The raw GOLD row is five levels deep: `Environmental > Aquatic > Freshwater > Lake > Algal bloom`. |
| The exact GOLD path is attested by organism assertions and biosamples. | `data/raw/gold_ecosystem_paths.tsv` and `data/raw/gold_path_biosamples.tsv` both record 8 assertions or biosamples for the exact path and `gold.ecosystem:5349`. |
| Three GOLD studies used this exact path. | `data/raw/gold_studies.tsv` lists the exact path for `Gs0060830`, `Gs0113966`, and `Gs0114880`; each also lists the parent `Environmental > Aquatic > Freshwater > Lake` path. |
| The ENVO slice has a closer vendored term than generic `algal bloom`. | `data/raw/ontology_terms.tsv` contains `ENVO:2000005` `freshwater algal bloom`, and `data/raw/ontology_subclass_edges.tsv` records it as a subclass of `ENVO:2000004`. |
| GOLD MIxS triads independently point at a freshwater lake bloom. | `data/raw/gold_path_triads.tsv` has exact-path rows for `freshwater lake biome`, `freshwater algal bloom`, and `lake water`. |

Unsupported or over-scoped:

| Claim | Issue |
|---|---|
| The source concept is identical to `ENVO:2000004` generic `algal bloom`. | The source is under `Freshwater > Lake`, while `ENVO:2000004` also has marine and other aquatic subclasses. The existing `ENVO:2000005` `freshwater algal bloom` term is a closer broader class, and even it does not carry the lake-specific context of the exact GOLD source path. |
| `ENVO:00000021` `freshwater lake` is a broader habitat for this record. | A freshwater-lake algal bloom is a feature in a freshwater lake, not a kind of freshwater lake. The parent path is contextual for the bloom and should not become a generated is-a parent on an algal-bloom record. |

No literature citations, snippets, record-level evidence, causal edges, environmental parameters, characteristic taxa, discussions, or datasets are attached to this record.

## Completeness

Consequential gaps:

- There is no item-level `curation/decisions.tsv` row for `habitatmech:GOLD.b3f000fa4a`.
- There is no `curation/term_requests.tsv` definition that can keep the GOLD concept minted as a freshwater lake algal bloom under `ENVO:2000005`.
- The slug `algal_bloom` is still pinned to `ENVO:2000004`; moving the GOLD source concept to its minted identifier also requires replacing that `data/habitats/PATHS.tsv` row with `habitatmech:GOLD.b3f000fa4a`.
- There is no maintained input that can suppress the inherited `ENVO:00000021` lake parent while preserving the true bloom genus.
- There is no causal graph overlay or record-level evidence; this is acceptable for a seeded, uncurated GOLD-only record.
- There are no seeded environmental parameters, characteristic taxa, discussions, or datasets; the searched maintained inputs provide no claim to emit for this exact record.

Bounded absence checks:

| Query | Scope | Result |
|---|---|---|
| `habitatmech:GOLD.b3f000fa4a` | Hidden/ignored-inclusive search over `data/raw`, `data/habitats`, `data/habitats/PATHS.tsv`, `curation`, `research/habitats`, and `reports/yaml_record_review`, excluding `data/text_map/**` | Before this report was created, no current generated record, decision row, term request, causal overlay, research report, or prior review report was found for the source concept's minted key. |
| `ENVO:2000004`, `ENVO:2000005`, `gold.ecosystem:5349`, and the exact GOLD source path | Hidden/ignored-inclusive search over `data/raw`, `data/habitats`, `data/habitats/PATHS.tsv`, `curation`, `research/habitats`, and `reports/yaml_record_review`, excluding `data/text_map/**` | Found the generated target, the slug row, the raw GOLD ecosystem, biosample, study, and MIxS triad rows, the ENVO ontology terms and subclass row, and incidental research prose about non-bloom algae and plankton records; found no maintained curation row for this GOLD path. |
| `Algal bloom` / `algal bloom` | Hidden/ignored-inclusive search over raw ontology terms, raw GOLD rows, curation inputs, research reports, and generated path pins | Found the exact ENVO algal-bloom family, the exact GOLD source path, and incidental deep-research prose for algae, seaweed, and plankton records that treats algal bloom as an adjacent non-match; found no prior review or item-level curation for `gold.ecosystem:5349`. |

## Findings

### Major

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| M1 | GOLD's lake-specific source path is grounded onto generic `ENVO:2000004` and contributes the contextual `ENVO:00000021` lake path as an is-a parent. The record therefore makes two over-broad claims at once: that a freshwater-lake bloom is identical to all algal blooms, and that the bloom is a kind of freshwater lake. | `docs/CURATION.md` and `CLAUDE.md` require `parent_habitats` to be strictly broader is-a links. The exact GOLD path is `Environmental > Aquatic > Freshwater > Lake > Algal bloom`; the exact MIxS rows place the samples in `freshwater lake biome` and `lake water`, with 7 of 8 samples naming `ENVO:2000005` `freshwater algal bloom` as their local feature. `ENVO:2000005` is vendored and is a subclass of `ENVO:2000004`, making it a closer genus for a minted freshwater-lake bloom than generic `algal bloom` or `freshwater lake`. | `curation/decisions.tsv` for item-level source review, `data/habitats/PATHS.tsv` for the `algal_bloom` slug migration, and `curation/term_requests.tsv` for a minted lake-bloom definition under `ENVO:2000005` with `parent_mode=REPLACE`. |

### Minor

None found.

### Blockers

None found.

## Recommended Edits

1. Add an item-level `CONFIRM_UNGROUNDED` decision for `habitatmech:GOLD.b3f000fa4a` in `curation/decisions.tsv`, with `ENVO:2000005` `freshwater algal bloom` as the verified broader parent. Keep the GOLD concept minted rather than grounding it exactly to generic `ENVO:2000004`.
2. Add a `curation/term_requests.tsv` row for a minted freshwater-lake algal-bloom class under `ENVO:2000005` with `parent_mode` set to `REPLACE`. The replacement is needed because the inherited `ENVO:00000021` freshwater-lake parent is contextual rather than strictly broader.
3. Replace the `ENVO:2000004` row in `data/habitats/PATHS.tsv` with a row whose identifier is `habitatmech:GOLD.b3f000fa4a` and whose slug is `algal_bloom`, so the minted record reuses the existing `data/habitats/aquatic/algal_bloom.yaml` path instead of creating an unpinned label-derived file.
4. Regenerate only through the seeder: run `just seed`, canary `just seed-canary habitatmech:GOLD.b3f000fa4a --force`, then inspect `data/habitats/aquatic/algal_bloom.yaml` to confirm the slug migration overwrote the old `ENVO:2000004` record.
5. Run `just term-requests` after the canary has written the minted record so `scripts/build_term_requests.py` can validate the new row against `load_corpus()`, then run `just seed-apply --force`.
6. Scaffold an append-only history record with `just new-history`, filling in the reviewed GOLD path, MIxS triad rows, near matches, the `PATHS.tsv` migration, and validation commands.

## Follow-up Checks

- `just validate data/habitats/aquatic/algal_bloom.yaml`
- `just validate-strict data/habitats/aquatic/algal_bloom.yaml --quiet`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just qc`
- Manual post-regeneration check: confirm the record keeps a minted identity, keeps `grounding_status: UNGROUNDED`, gains a `HabitatMech` definition, retains `ENVO:2000005` as its sole `parent_habitats` entry, and no longer asserts exact identity with `ENVO:2000004` or an is-a edge to `ENVO:00000021`.

## Additional Notes

- All absence searches described above included hidden and ignored files.
- The record correctly avoids inheriting any taxa or environmental parameters from the generic `ENVO:2000004` and `ENVO:00000021` concepts. Its issue is limited to identity and hierarchy.
- The incidental `Host-associated > Algae > Haptophytes` MIxS broad-scale row that uses `ENVO:2000004` is unrelated to `gold.ecosystem:5349` and does not support this freshwater-lake bloom source path.
