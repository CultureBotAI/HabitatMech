# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/food/soy_sauce.yaml
- Started UTC: 2026-09-25T22:41:00Z
- Finished UTC: 2026-09-25T22:50:47Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.b22544c1b5` |
| Label | `Soy sauce` |
| Class | `HabitatRecord` |
| Category | `FOOD` |
| Generated path | `data/habitats/food/soy_sauce.yaml` |
| Maintained path lock | `data/habitats/PATHS.tsv:2597` maps `habitatmech:GOLD.b22544c1b5` to `soy_sauce` |
| Source concept | GOLD `gold.ecosystem:5598` / `gold.ecosystem:5599` |
| Source path | `Engineered > Food production > Fermented food > Soy sauce` |
| Grounding | `UNGROUNDED` |
| Mapping | `SEEDED` |

The reviewed file is a generated GOLD-only record. It is not a maintained
curation input and should not be patched directly.

## Validation

| Command | Result |
|---|---|
| `just worklist --limit 0 --status all --out /tmp/habitatmech-worklist.tsv` | Passed; wrote 953 rows and kept `habitatmech:GOLD.b22544c1b5` as the next row after `Pulp and paper wastewater`. |
| `just validate data/habitats/food/soy_sauce.yaml` | Passed with `No issues found`. |
| `just validate-strict data/habitats/food/soy_sauce.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 expected records, 3206 found, 0 missing, 0 extra, 0 differing. |
| `just report` | Passed; the corpus report did not list `Soy sauce` in the GOLD triad ranking. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

The generated identifier, label, category, and source attestation agree with the
GOLD source inventory:

| Claim | Evidence | Assessment |
|---|---|---|
| The target is the GOLD `Soy sauce` leaf under fermented food. | `data/raw/gold_ecosystem_paths.tsv:629` gives the canonical path `Engineered > Food production > Fermented food > Soy sauce`, leaf label `Soy sauce`, depth `4`, and `gold.ecosystem:5598|gold.ecosystem:5599`. | Supported. |
| The generated source attestation uses the first collapsed GOLD node ID and records the duplicate-node note. | The same raw row lists two GOLD ecosystem node IDs; `data/habitats/food/soy_sauce.yaml` emits `source_id: gold.ecosystem:5598` and says two GOLD ecosystem node IDs share the path. | Supported. |
| The direct GOLD row has five organism-level assertions. | `data/raw/gold_ecosystem_paths.tsv:629` has `organism_count=5`, `study_count=0`, `biosample_count=0`, and `total_assertions=5`. | Supported; the count is a GOLD `ORGANISM` aggregate and is not study or biosample evidence. |
| The only generated parent is the broader GOLD source-path parent `Fermented food`. | The target has `parent_habitats: habitatmech:GOLD.8f5ec46a28`; `data/habitats/food/fermented_food.yaml` is the generated record for `Engineered > Food production > Fermented food`. | Supported as a strict broader parent for brewed soy sauce, while the parent record itself remains class-level and unreviewed. |
| `UNGROUNDED` is currently explained only by a class-level no-match sweep. | `curation/decisions.tsv:1007` has `CONFIRM_UNGROUNDED` with `review_depth` `CLASS`; the generated curation history repeats that no ontology term fit by any lexical route and that habitat status was not assessed. | Reproducible but incomplete. `CLASS` depth does not establish that Soy sauce is a real microbial habitat or that no manual broader FOODON parent should be attached. |

A case-insensitive ignored/hidden-inclusive search of
`data/raw/ontology_terms.tsv`, `curation/term_requests.tsv`,
`curation/term_requests_excluded.tsv`, `conf/id_label_targets.yaml`,
`history/`, `research/`, `curation/causal_graphs/`, and prior
`reports/yaml_record_review/` files for `soy sauce` found no exact ontology row,
no term request, no term-request exclusion, no label-correspondence residual, no
append-only history entry, no causal overlay, and no prior YAML review. A broader
ignored/hidden-inclusive search for `soy`, `shoyu`, and `soya` found broader or
adjacent soybean terms in the vendored slice, including
`FOODON:00002266` `soybean food product` and `FOODON:03310002`
`soybean paste`, but no exact soy-sauce term.

## Evidence

| Record claim | Nearest source | Assessment |
|---|---|---|
| `source: GOLD`, `source_id: gold.ecosystem:5598`, `source_label: Soy sauce`, and the full GOLD path | `data/raw/gold_ecosystem_paths.tsv:629` | Supported exactly. |
| The two-GOLD-node collapse note | `data/raw/gold_ecosystem_paths.tsv:629` lists `gold.ecosystem:5598|gold.ecosystem:5599`. | Supported exactly. |
| `assertion_count: 5`, `assertion_unit: ORGANISM` | `data/raw/gold_ecosystem_paths.tsv:629` records 5 organism assertions. | Supported exactly. |
| Parent `habitatmech:GOLD.8f5ec46a28` | The GOLD path places `Soy sauce` below `Engineered > Food production > Fermented food`; the parent generated record has identifier `habitatmech:GOLD.8f5ec46a28`. | Supported as a source-path broader edge. |
| Absence of characteristic taxa, environmental parameters, record evidence, and causal graphs | Exact path searches across `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, and `data/raw/gold_studies.tsv` found no row for `Engineered > Food production > Fermented food > Soy sauce`; `find curation/causal_graphs -maxdepth 1 -type f -name 'soy_sauce.yaml' -print` returned no files. | Supported. GOLD organism aggregates are not characteristic-taxon claims, and no curated causal overlay exists. |

The adjacent fermented-food research report for the BacDive/Madin fermented-food
record mentions that "non-brewed soy sauce" is outside ISAPP's fermented-food
scope, but it does not inspect `gold.ecosystem:5598` or
`gold.ecosystem:5599`; it is useful background for future item review, not
evidence that the GOLD Soy sauce leaf is exact, over-broad, or not applicable.

## Completeness

The generated record is structurally complete for the maintained inputs that
currently exist: it preserves the GOLD aggregate source, keeps a broader GOLD
source-path parent, and avoids unsupported optional fields.

The consequential gap is in maintained curation depth. The only decision row is
the 2026-08-12 class-level sweep. The 2026-08-14 advisory sample row for this
source concept in `curation/samples/class_swept_unscreened-20260814.tsv:25`
classified it as an `ok` habitat, but that TSV is advisory sampling evidence,
not the item-level decision row that would make the generated record reviewed or
add broader/term-request curation.

Bounded ignored/hidden-inclusive searches covered `data/raw/`,
`curation/decisions.tsv`, `curation/term_requests.tsv`,
`curation/term_requests/`, `curation/term_requests_excluded.tsv`,
`curation/causal_graphs/`, `history/`, `research/`, `conf/id_label_targets.yaml`,
`reports/yaml_record_review/`, `data/habitats/PATHS.tsv`, and the generated
target for:

- `habitatmech:GOLD.b22544c1b5`
- `gold.ecosystem:5598`
- `gold.ecosystem:5599`
- `Soy sauce`
- `soy_sauce`
- `Engineered > Food production > Fermented food > Soy sauce`

Those searches found the expected generated record, path lock, raw GOLD
aggregate row, class-level decision row, and advisory sample row. They found no
target-specific term request, exclusion, causal overlay, append-only history
entry, label residual, exact-path GOLD biosample/triad/study row, or prior
review report.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| HM-SOY-SAUCE-001 | Major | Soy sauce is still backed only by `review_depth` `CLASS`. No maintained item-level decision has confirmed that the GOLD leaf denotes brewed soy sauce as a real fermented-food microbial habitat, that the vendored slice lacks an exact identity, or that `FOODON:00002266` `soybean food product` is the right broader FOODON parent rather than `FOODON:03310002` `soybean paste` or no broader FOODON term. Because `CLASS` rows do not promote a record to `REVIEWED`, the generated `mapping_status: SEEDED` is correct but materially incomplete for an asserted term-request candidate. | `curation/decisions.tsv`; if a novel term is requested, `curation/term_requests.tsv` |

## Recommended Edits

1. Item-review `habitatmech:GOLD.b22544c1b5` in
   `curation/decisions.tsv`.
2. Inspect GOLD nodes `gold.ecosystem:5598` and `gold.ecosystem:5599`, including
   the five organism assertions behind the aggregate row, to confirm whether the
   leaf means brewed fermented soy sauce rather than non-brewed soy sauce or a
   mixed sauce bucket.
3. If the source concept is true fermented soy sauce with no exact ontology
   term, keep the minted identity, replace the class-level row with an
   item-level `CONFIRM_UNGROUNDED`, and consider `FOODON:00002266` as a
   `GROUND_AS_PARENT` candidate or a genus for a term request.
4. If the GOLD node is instead a non-fermented or mixed product bucket, use an
   item-level `NOT_APPLICABLE` or discussion of the unresolved upstream
   ambiguity instead of promoting the current row.

Do not hand-edit `data/habitats/food/soy_sauce.yaml`; regenerate it from the
maintained TSVs.

## Follow-up Checks

After adding item-level curation, rerun the narrow target checks:

- `just seed`
- `just seed-canary habitatmech:GOLD.b22544c1b5`
- inspect `data/habitats/food/soy_sauce.yaml`
- `just validate-strict data/habitats/food/soy_sauce.yaml`
- `just term-requests-check`
- `just verify-corpus --max-diffs 1`
- `just report`

If a causal overlay is added later, also run
`just validate-causal curation/causal_graphs/soy_sauce.yaml` and
`just validate-causal-all`.

## Additional Notes

The direct parent `Fermented food` (`habitatmech:GOLD.8f5ec46a28`) is also
class-level and unreviewed. Its generated parent is `FOODON:03530206`
`food production`, which the vendored slice defines as a planned process rather
than a food material. That parent-record issue is not a direct Soy sauce record
defect because Soy sauce's `parent_habitats` edge points only to the minted
`Fermented food` record, but resolving the food hierarchy should keep this
process/material distinction in view.
