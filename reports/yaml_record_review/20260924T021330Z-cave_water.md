# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/other/cave_water.yaml
- Started UTC: 2026-09-24T02:09:00Z
- Finished UTC: 2026-09-24T02:13:30Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/other/cave_water.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:BACDIVE.9ae6cfc178` |
| Label | `Cave-water` |
| Category | `OTHER` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from `data/raw/bacdive_isolation_sources.tsv`, `data/raw/bacdive_source_taxa.tsv`, `data/raw/isolation_source_groundings.tsv`, `curation/decisions.tsv`, and `data/habitats/PATHS.tsv`; do not edit `data/habitats/other/cave_water.yaml` directly. |

`data/habitats/other/cave_water.yaml` is the generated record for the BacDive
isolation source `bacdive.isolation_source:cave-water`. The seeder minting
function resolves that source id to `habitatmech:BACDIVE.9ae6cfc178`, and the
slug lock maps the same identifier to `cave_water`.

An ignored-file-inclusive pre-report check,
`find reports/yaml_record_review -maxdepth 1 -type f -name '*-cave_water.md' -print`,
found no prior exact report for this record. A second ignored-file-inclusive
`find data/habitats -type f -name 'cave_water.yaml' -print` found exactly this
generated YAML target.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/other/cave_water.yaml` | Pass - LinkML reported `No issues found`. |
| `just validate-strict data/habitats/other/cave_water.yaml` | Pass - 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Target causal overlay validator | Not applicable - ignored-file-inclusive `find` found no `curation/causal_graphs/*cave*` overlay; the `*water*` overlays are generic records for `fresh_water`, `liquid_water`, `sea_water`, `waste_water`, `hypersaline_water`, `brackish_water`, and `marine_water_body`, and exact ignored/hidden-inclusive `rg` found no causal overlay keyed to `habitatmech:BACDIVE.9ae6cfc178` or `bacdive.isolation_source:cave-water`. |
| Reference validator | Not applicable - the record has no `evidence` entries or causal graph edges with reference identifiers. |
| `just validate-causal-all` | Pass - validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Pass - committed ENVO batch term-request table is current with 109 terms. |
| `just validate-history` | Pass - 77 history records valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass - expected 3206 generated records, found 3206 on disk, with 0 missing, 0 extra, and 0 differing. |
| `just worklist --limit 2000` | Pass - 0 ungrounded records still undecided; 1810 decisions on file. |
| `just report` | Pass - report completed; this record remains in the class-level-sweep population and `cave water` remains one of the 15 normalized labels shared by records from different sources. |
| `git diff --check` | Pass - no whitespace errors. |

## Identity and Grounding

The source identity is internally consistent. `data/raw/bacdive_isolation_sources.tsv`
has one row for `bacdive.isolation_source:cave-water` with label `Cave-water`,
source slug `cave-water`, 2 strains, and 2 taxa. `data/raw/bacdive_source_taxa.tsv`
then supplies the two generated BacDive taxon associations:
`NCBITaxon:1665626` `Deinococcus budaensis` at rank 1 and `NCBITaxon:647`
`Aeromonas sp.` at rank 2.

The empty upstream mapping is reproduced correctly. `data/raw/isolation_source_groundings.tsv`
has a reviewed BacDive `Cave-water` row whose object id, object label,
object source, predicate, and confidence fields are all empty; `src/habitatmech/seed.py`
honors such rows as `bacdive_declined_upstream` rather than replacing them with
a lexical guess, and the generated source-attestation note says the same thing.

The maintained HabitatMech decision is too shallow. `curation/decisions.tsv`
marks `habitatmech:BACDIVE.9ae6cfc178` as `CONFIRM_UNGROUNDED` with
`review_depth=CLASS`, so the row only records that no vendored ontology label
matched `Cave-water` by the class-sweep search routes. The generated
`mapping_status: SEEDED` is correct for that class-level row, but the row does
not represent item-level review of whether the BacDive concept is a habitat or
whether a strict broader term exists.

The vendored slice has no exact cave-water class. Exact ignored/hidden-inclusive
searches for `Cave-water`, `cave-water`, and `cave water` in
`data/raw/ontology_terms.tsv`, `curation/decisions.tsv`,
`curation/term_requests.tsv`, `curation/external_xrefs.tsv`, and
`data/habitats/` found only this BacDive source concept and the separate GOLD
`Cave water` source concept.

A strict broader term is available. The BacDive label denotes a water material
from a cave, and the vendored slice includes `ENVO:00002006` `liquid water`,
defined as an environmental material primarily composed of dihydrogen oxide in
its liquid form. `ENVO:01001004` `groundwater` is also present, but the BacDive
label lacks the GOLD source path's `Freshwater > Groundwater` context, so the
tighter groundwater parent would overread this source label. `ENVO:00000067`
`cave` is the setting, not a material broader than cave water.

## Evidence

| Claim | Source | Assessment |
|---|---|---|
| The record represents the BacDive isolation source `Cave-water`. | `data/raw/bacdive_isolation_sources.tsv`; `data/habitats/other/cave_water.yaml` | Supported exactly. |
| The source has 2 BacDive strain assertions. | `data/raw/bacdive_isolation_sources.tsv`; generated `source_attestations` | Supported exactly; the assertion unit is correctly `STRAIN`. |
| BacDive reports one `Deinococcus budaensis` and one `Aeromonas sp.` association for this source. | `data/raw/bacdive_source_taxa.tsv`; generated `characteristic_taxa` | Supported as reported-from associations. The record does not overclaim `is_characteristic`. |
| The KG-Microbe isolation-source mapping table left `Cave-water` ungrounded. | `data/raw/isolation_source_groundings.tsv`; generated attestation note | Supported exactly. |
| HabitatMech has item-reviewed the record and found no broader ontology term. | `curation/decisions.tsv` | Not supported. The current decision explicitly says it was a class-level lexical sweep and that habitatness was not assessed. |

The only research content found by exact ignored/hidden-inclusive search was an
unrelated Geologic report that names `Cave-water` as a separate neighboring
BacDive tag while distinguishing cave water from cave rock. That report is
useful negative context for `Geologic`, but it is not a target-specific
definition or grounding review for this record.

## Completeness

| Area | Assessment |
|---|---|
| Definition | Incomplete. A novel `habitatmech:` cave-water term needs a curator-authored `curation/term_requests.tsv` definition if the item-level decision keeps it minted under `ENVO:00002006`. |
| Parents | Incomplete. No generated `parent_habitats` are present, but `ENVO:00002006` is a strict broader class for cave water. |
| Category | Incomplete. The record is currently `OTHER`; after a water-material parent is attached, the maintained decision should also move this BacDive water source into `AQUATIC`. |
| Xrefs | Complete as empty. The BacDive row has no upstream ontology target to preserve as an xref, and exact searches found no external xref for `habitatmech:BACDIVE.9ae6cfc178`. |
| Environmental parameters | Complete as empty for this source. The BacDive aggregate inventory contributes no pH, salinity, temperature, or oxygen parameter rows. |
| Characteristic taxa | Complete for seeded BacDive assertions. The two taxa exactly match the two `bacdive_source_taxa.tsv` rows and are left as reported associations, not curator claims of characteristic cave-water taxa. |
| Evidence | Complete as empty for now. No curator-authored definition or mechanism edge exists yet, and no target-specific literature artifact was found by ignored-inclusive filename searches under `research` or `history`. |
| Causal graphs | Complete as empty. Generic water overlays exist, but no causal graph overlay is keyed to this BacDive record. |

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Blocker | None found. |  |  |
| Major | **BacDive Cave-water has only a class-level ungrounded decision even though a strict broader water-material parent is available.** The current `CONFIRM_UNGROUNDED` row does not establish that this two-strain BacDive source concept was item-reviewed; it also leaves the generated record without the broader `ENVO:00002006` parent and in the fallback `OTHER` category. | `curation/decisions.tsv` has `review_depth=CLASS` for `habitatmech:BACDIVE.9ae6cfc178`; `data/raw/bacdive_isolation_sources.tsv` names the source `Cave-water`; `data/raw/ontology_terms.tsv` includes `ENVO:00002006` `liquid water`; exact ignored/hidden-inclusive searches found no exact `cave water` ontology term or term request. | `curation/decisions.tsv`; `curation/term_requests.tsv` |
| Minor | None found. |  |  |

## Recommended Edits

1. Replace the `curation/decisions.tsv` row for
   `habitatmech:BACDIVE.9ae6cfc178` with an item-level `GROUND_AS_PARENT`
   decision to `ENVO:00002006` `liquid water`, `grounding_status=NARROW`, and
   `category=AQUATIC`. Its note should explain that `Cave-water` is a cave
   water material, that no exact class exists in the vendored slice, that
   `liquid water` is a strict broader parent, and that `groundwater` is not
   claimed without GOLD's groundwater path context.

2. Add a `curation/term_requests.tsv` row for the novel cave-water concept with
   `ENVO:00002006` as the parent. The default `ADD` parent mode is sufficient
   because this BacDive record has no inherited parent that needs replacing.

3. Regenerate the target with `just seed-canary habitatmech:BACDIVE.9ae6cfc178`
   and confirm that the regenerated `data/habitats/aquatic/cave_water.yaml`
   has `grounding_status: NARROW`, `mapping_status: REVIEWED`, category
   `AQUATIC`, and `ENVO:00002006` under `parent_habitats`. Because the category
   change moves the record out of `data/habitats/other/`, follow the canary with
   a full `just seed-apply --force --prune` so the stale old path is deleted by
   the seeder.

## Follow-up Checks

After the maintained curation inputs change, run:

- `just seed`
- `just seed-canary habitatmech:BACDIVE.9ae6cfc178`
- `just seed-apply --force --prune`
- `just term-requests-check`
- `just validate data/habitats/aquatic/cave_water.yaml`
- `just validate-strict data/habitats/aquatic/cave_water.yaml`
- `just validate-causal-all`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

Also rerun exact ignored/hidden-inclusive searches for
`habitatmech:BACDIVE.9ae6cfc178`, `bacdive.isolation_source:cave-water`,
`Cave-water`, `cave_water`, and `ENVO:00002006` to confirm the new decision and
term request are the only new maintained inputs for this target unless a causal
overlay is intentionally added.

## Additional Notes

- The separate GOLD Cave water record is
  `data/habitats/aquatic/cave_water__c4d82240.yaml` with source concept
  `habitatmech:GOLD.6251bef319`. It was already reviewed in
  `reports/yaml_record_review/20260924T015449Z-cave_water__c4d82240.md`.
- `just report` still lists normalized label `cave water` among the
  multi-source novel labels, backed by 2 GOLD organism assertions and 2 BacDive
  strains. That is a useful future SAME_AS lead, but the source labels alone do
  not prove the BacDive isolation source and GOLD groundwater path are exact
  synonyms.
- Ignored-file-inclusive `find` searches found no `research` or `history`
  filename containing `9ae6cfc178` or `cave*water`, and exact
  ignored/hidden-inclusive content searches found no target-specific causal
  overlay.
