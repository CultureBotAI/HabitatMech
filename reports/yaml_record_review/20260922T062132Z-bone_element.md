# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/bone_element.yaml`
- Started UTC: 2026-09-22T06:21:32Z
- Finished UTC: 2026-09-22T06:21:32Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `UBERON:0001474` |
| Label | `bone element` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `CLOSE` |
| Mapping status | `REVIEWED` |
| Maintained owner | Generated from the BacDive and GOLD inventories, `data/raw/isolation_source_groundings.tsv`, `curation/decisions.tsv`, and the vendored ontology slice; future mechanism evidence belongs in `curation/causal_graphs/*.yaml`, and generated YAML remains read-only |
| Locked slug | `data/habitats/PATHS.tsv:1033` maps `UBERON:0001474` to `bone_element` |

This is the reviewed close merge of BacDive `Bone` and the GOLD ecosystem path `Host-associated > Birds > Skeletal system > Bones` into the vendored UBERON `bone element` term.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/bone_element.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/bone_element.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was staged |

## Identity and Grounding

The generated UBERON identity, source attestations, source-concept decisions, and parent edges are supported by committed inventory and curation inputs:

- `data/habitats/PATHS.tsv:1033` pins `UBERON:0001474` to slug `bone_element`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:13046` defines `UBERON:0001474` with canonical label `bone element`, UBERON definition `Skeletal element that is composed of bone tissue.`, and synonyms `bone|bone organ|bones`.
- `data/raw/ontology_subclass_edges.tsv:11204` and `data/raw/ontology_subclass_edges.tsv:11205` make `FOODON:03420168` and `UBERON:0004765` direct ontology parents of `UBERON:0001474`; `data/raw/ontology_terms.tsv:10720` and `data/raw/ontology_terms.tsv:13243` provide their labels `bone or shell` and `skeletal element`.
- `data/raw/bacdive_isolation_sources.tsv:66` is the raw BacDive `bacdive.isolation_source:bone` row with source label `Bone`, normalized label `bone`, 125 strains, and 96 candidate taxa.
- `data/raw/isolation_source_groundings.tsv:46` maps BacDive `Bone` to `UBERON:0001474` / `bone element` using `skos:closeMatch`.
- `data/raw/gold_ecosystem_paths.tsv:1904` is the exact raw GOLD path `Host-associated > Birds > Skeletal system > Bones`, gives leaf label `Bones`, records depth `4`, aggregates two GOLD ecosystem nodes with zero organism, study, biosample, and total assertions, and names `gold.ecosystem:7479|gold.ecosystem:7480` as the upstream nodes.
- `data/raw/gold_ecosystem_paths.tsv:1903` is the immediate GOLD parent path `Host-associated > Birds > Skeletal system`, and `data/habitats/PATHS.tsv:1698` pins its generated identifier `habitatmech:GOLD.3756d4bb39` to `skeletal_system__5ac43c2f`.
- The source-concept hashes match the seeder's `sha1(f"{source}:{key}")[:10]` route: `BACDIVE:bacdive.isolation_source:bone` hashes to `16280b13f2`, `GOLD:Host-associated > Birds > Skeletal system > Bones` hashes to `6296c1e38f`, and `GOLD:Host-associated > Birds > Skeletal system` hashes to `3756d4bb39`.
- `curation/decisions.tsv:12` and `curation/decisions.tsv:600` contain item-level `REVIEW` decisions for `habitatmech:BACDIVE.16280b13f2` and `habitatmech:GOLD.6296c1e38f`.

`grounding_status: CLOSE` and `mapping_status: REVIEWED` are consistent with those inputs. Both upstream source concepts have item-level decisions endorsing the seeder's close match to `UBERON:0001474`; the generated record therefore has no minted identity and is eligible for `REVIEWED`.

## Evidence

Every generated scientific claim in this record traces to an inspected source row:

| Claim | Nearest source | Review |
|---|---|---|
| UBERON identifier, canonical label, definition, UBERON synonyms, FOODON and UBERON ontology parents | `data/raw/ontology_terms.tsv:13046`, `data/raw/ontology_subclass_edges.tsv:11204`, `data/raw/ontology_subclass_edges.tsv:11205` | Supported exactly by the vendored ontology slice |
| BacDive synonym `Bone`, `source_id: bacdive.isolation_source:bone`, 125-strain attestation, and 96-taxon candidate pool | `data/raw/bacdive_isolation_sources.tsv:66` | Supported exactly |
| BacDive-to-UBERON close match | `data/raw/isolation_source_groundings.tsv:46` plus `curation/decisions.tsv:12` | Supported; the item-level review endorses the generated close match |
| GOLD synonym `Bones`, `source_id: gold.ecosystem:7479`, full bird skeletal-system source path, and two-node note | `data/raw/gold_ecosystem_paths.tsv:1904` | Supported; `gold.ecosystem:7479` is the first of two node IDs that share the exact path |
| Broader GOLD `Skeletal system` parent | `data/raw/gold_ecosystem_paths.tsv:1903`, `data/habitats/PATHS.tsv:1698`, and `data/habitats/host_associated/skeletal_system__5ac43c2f.yaml` | Supported exactly |
| Mapping status `REVIEWED` and the two curation-history events | `curation/decisions.tsv:12`, `curation/decisions.tsv:600` | Supported by one item-level decision for each contributing source concept |
| Top 25 characteristic taxa, their ranks, and association counts | `data/raw/bacdive_source_taxa.tsv:296` through `data/raw/bacdive_source_taxa.tsv:320` | Supported exactly; all 25 entries are BacDive-derived and use the 96-taxon candidate pool from `data/raw/bacdive_isolation_sources.tsv:66` |

The record has no xrefs, environmental parameters, record-level evidence, causal graphs, discussion links, or datasets. Exact ignored/hidden-inclusive searches of the committed raw GOLD tables found the cited `gold_ecosystem_paths.tsv` aggregate row and no exact `gold_path_biosamples.tsv`, `gold_path_triads.tsv`, or `gold_studies.tsv` side-table row for `Host-associated > Birds > Skeletal system > Bones`.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- Both `source_attestations` entries capture their complete raw provenance. The BacDive attestation includes the 125-strain count and `STRAIN` unit; the GOLD attestation correctly omits `assertion_count` and `assertion_unit` because the exact bird `Bones` aggregate has zero total assertions.
- The generated 25-entry `characteristic_taxa` list matches the raw BacDive top-25 rows for `bacdive.isolation_source:bone` in rank order and does not overclaim `is_characteristic`.
- The FOODON and UBERON parents are direct ontology parents of `UBERON:0001474`, and the minted `habitatmech:GOLD.3756d4bb39` parent records the next broader GOLD path.
- The reviewed record has no authored causal graph. Exact ignored/hidden-inclusive searches found no maintained `curation/causal_graphs/` overlay for `bone_element`, `UBERON:0001474`, `habitatmech:BACDIVE.16280b13f2`, or `habitatmech:GOLD.6296c1e38f`.
- Exact ignored/hidden-inclusive searches found no target-specific `history/`, `research/`, `reports/`, or `curation/term_requests.tsv` artifact for this record before this report was written.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, the generated target, and the generated GOLD parent. They found the expected decisions, raw rows, ontology rows, and path locks, but no uncited maintained input that changes the review outcome.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if a future curation change adds an item-level decision, authored definition, BacDive extraction change, GOLD inventory change, or causal graph overlay for this record:

- `just validate data/habitats/host_associated/bone_element.yaml`
- `just validate-strict data/habitats/host_associated/bone_element.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If future curation changes one of the two source-level decisions, add or update the maintained item-level row in `curation/decisions.tsv`, run `just seed`, preview `just seed-canary UBERON:0001474`, and rerun the corpus-level checks after regeneration.

## Additional Notes

- `find reports/yaml_record_review -name '*bone_element*' -print` found no pre-existing exact review report for this record before this file was written.
- `find curation history research reports -name '*bone_element*' -o -name '*16280b13f2*' -o -name '*6296c1e38f*' -o -name '*UBERON_0001474*' -o -name '*0001474*'` found no maintained target-specific decision-adjacent, history, research, or report artifacts before this file was written.
- The generated text-map hit for `bone_element` is a search projection, not a maintained input.
