# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/engineered/seawater_ras.yaml`
- Started UTC: `2026-09-25T02:12:57Z`
- Finished UTC: `2026-09-25T02:13:55Z`
- Verdict: needs curation

## Target

Reviewed the complete generated record at `data/habitats/engineered/seawater_ras.yaml`. The record is a GOLD-only engineered habitat for `Engineered > Artificial ecosystem > Aquaculture > Seawater RAS`, minted as `habitatmech:GOLD.95eace5af2`.

The generated file has `grounding_status: UNGROUNDED` and `mapping_status: SEEDED`. Its only parent is the immediate GOLD source-path parent, `habitatmech:GOLD.0287e1b2a9` `Aquaculture`. Its only source attestation shows `gold.ecosystem:8318`, and the note correctly points out that two GOLD ecosystem node IDs share this canonical path.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/seawater_ras.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/seawater_ras.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Focused causal overlay validator | Not applicable; an exact ignored-inclusive search in `curation/causal_graphs/` found no `habitatmech:GOLD.95eace5af2`, `seawater_ras`, `Seawater RAS`, or exact Seawater RAS source-path overlay. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| Reference validator | Not applicable; the target record has no record-level `evidence` entries and no causal graph edges with citation references. |
| `just term-requests-check` | Passed; the generated ENVO term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records all exist on disk, with 0 missing, 0 extra, and 0 differing records. |
| `just worklist --limit 2000` | Passed; it reported 0 still-undecided ungrounded records and 1,810 decisions on file. |
| `just report` | Passed; the corpus report completed for 3,206 records. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

The generated identifier, label, category, parent, and source attestation agree with the committed GOLD inventory. `data/raw/gold_ecosystem_paths.tsv` has the row `Engineered > Artificial ecosystem > Aquaculture > Seawater RAS` at depth 4 with `gold_node_count` 2, no GOLD organism, study, or biosample assertions, and node IDs `gold.ecosystem:8318|gold.ecosystem:8319`. `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.95eace5af2` to the stable `seawater_ras` slug.

The record's `UNGROUNDED` status comes from `curation/decisions.tsv`, which has only a class-level `CONFIRM_UNGROUNDED` decision for `habitatmech:GOLD.95eace5af2`. That row records a reproducible lexical miss against the vendored slice, but its note explicitly says the source concept's habitathood was not assessed.

The local ontology slice did not provide an exact recirculating-aquaculture term in a bounded ignored-inclusive search for `recirculating`, `seawater`, `sea water`, `aquarium`, `aquaculture`, and `fish farm` across `data/raw/ontology_terms.tsv`, `curation/decisions.tsv`, `curation/term_requests.tsv`, and `conf/id_label_targets.yaml`. Nearby terms do need item review before this record is promoted: `ENVO:03600074` `aquaculture farm` is a plausible broader aquaculture target; `ENVO:00000294` `fish farm` may be too fish-specific for an unqualified RAS label; `ENVO:00002196` `aquarium` and `ENVO:00002197` `saline water aquarium` are aquarium terms rather than explicit aquaculture-system terms; and `ENVO:01000906` `aquaculture process` plus `ENVO:01001669` `mariculture process` are processes rather than habitat identities.

## Evidence

- Supported: the `GOLD` source attestation is traceable to `data/raw/gold_ecosystem_paths.tsv`, including the two collapsed GOLD node IDs and the absence of an assertion count in the generated YAML because the canonical path carries zero organism, study, biosample, and total assertions.
- Supported: the sole generated parent, `habitatmech:GOLD.0287e1b2a9`, is the immediate GOLD path parent for `Engineered > Artificial ecosystem > Aquaculture`. It is itself a generated `Aquaculture` record, left `SEEDED` and `NARROW` under `ENVO:03600074` by the ambiguous-leaf seeding rule.
- Supported: the `CONFIRM_UNGROUNDED` history event in the generated record mirrors the class-level decision row from `curation/decisions.tsv`.
- Under-reviewed: the record has no item-level decision, authored definition, term request, record-level evidence, discussion, dataset, characteristic taxon, environmental parameter, or causal graph.

## Completeness

The zero source count is faithful to the GOLD source inventory, so the absence of `assertion_count`, `assertion_unit`, characteristic taxa, and environmental parameters is not a generated-data omission for this path. An exact ignored-inclusive search across `data/raw/` for `gold.ecosystem:8318`, `gold.ecosystem:8319`, and the exact source path found only the parent Seawater RAS row and the child Seawater RAS Biofilter row in `gold_ecosystem_paths.tsv`; it found no direct `gold_path_triads.tsv`, `gold_path_biosamples.tsv`, or `gold_studies.tsv` row to audit.

Exact ignored-inclusive searches in `research/`, `history/`, `curation/causal_graphs/`, `curation/term_requests.tsv`, `curation/term_requests/`, and `conf/id_label_targets.yaml` found no target-specific research report, history entry, causal overlay, minted definition, generated term-request row, or label-correspondence residual for `seawater_ras`, `habitatmech:GOLD.95eace5af2`, `Seawater RAS`, or the exact source path.

`find reports/yaml_record_review -maxdepth 1 -type f -name '*-seawater_ras.md' -print` found no existing `seawater_ras` review before this report was written. `find` is not `.gitignore`-filtered, so ignored files were included in that absence check.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| Major | `Seawater RAS` remains only class-reviewed even though the GOLD path places it under Aquaculture and nearby ontology terms require an item-level broader-term decision. | The only maintained decision is a `CLASS` `CONFIRM_UNGROUNDED` row whose note says habitathood was not assessed. The vendored slice has no exact RAS term in the bounded ignored-inclusive search, but it does have aquaculture-farm, fish-farm, aquarium, saline-water-aquarium, aquaculture-process, and mariculture-process near misses that a curator should explicitly accept or reject before promotion from `SEEDED`. | `curation/decisions.tsv`; if no exact term fits, `curation/term_requests.tsv` |

## Recommended Edits

1. Item-review `habitatmech:GOLD.95eace5af2` in `curation/decisions.tsv` against the exact `Engineered > Artificial ecosystem > Aquaculture > Seawater RAS` source path, `ENVO:03600074` `aquaculture farm`, `ENVO:00000294` `fish farm`, the aquarium terms, and the aquaculture-process terms.
2. If the item review confirms a real habitat with no exact ontology term, retain a minted identifier and either record a `GROUND_AS_PARENT` decision for a strictly broader term or leave a reviewed `CONFIRM_UNGROUNDED` decision with reasoning that rejects the broader candidates.
3. If no exact ontology term fits, define the minted Seawater RAS concept in `curation/term_requests.tsv` with a true engineered-aquaculture genus and `parent_mode=ADD`, so regeneration keeps the source-path Aquaculture parent alongside any authored ontology parent.

## Follow-up Checks

- Run `just seed`, then `just seed-canary habitatmech:GOLD.95eace5af2`, and inspect `data/habitats/engineered/seawater_ras.yaml` before any wider `just seed-apply --force`.
- Run `just validate data/habitats/engineered/seawater_ras.yaml`, `just validate-strict data/habitats/engineered/seawater_ras.yaml`, `just term-requests-check`, `just validate-history`, `just verify-corpus --max-diffs 1`, `just worklist --limit 2000`, `just report`, and `git diff --check` after the maintained input changes.

## Additional Notes

The review did not find a target causal overlay, so there was no focused `just validate-causal curation/causal_graphs/<slug>.yaml` invocation to run. The whole-overlay validator still passed.

The existing Biofilter review at `reports/yaml_record_review/20260924T191000Z-biofilter__9cdb9879.md` already called out that the Seawater RAS child `Biofilter` is generated with its container system as an inherited source-path parent. That is a downstream hierarchy issue in the child record. The narrower finding here is that the parent Seawater RAS concept itself has not yet received item-level grounding or definition review.
