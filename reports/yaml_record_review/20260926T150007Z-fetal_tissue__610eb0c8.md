# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/fetal_tissue__610eb0c8.yaml`
- Started UTC: 2026-09-26T14:55:00Z
- Finished UTC: 2026-09-26T15:00:07Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.baffd7f6c7` |
| Label | `Fetal tissue` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding | `UNGROUNDED` |
| Mapping | `SEEDED` |
| Maintained source | `data/raw/gold_ecosystem_paths.tsv:746`, plus `curation/decisions.tsv:1051` for the current class-level decision |
| Generated path | `data/habitats/host_associated/fetal_tissue__610eb0c8.yaml` |
| Stable slug | `data/habitats/PATHS.tsv:2659` maps `habitatmech:GOLD.baffd7f6c7` to `fetal_tissue__610eb0c8` |

This is the generated record for the GOLD concept `Host-associated > Mammals > Fetus > Fetal tissue`, not the separate human-specific `Host-associated > Mammals: Human > Fetus > Fetal tissue` record at `data/habitats/host_associated/fetal_tissue.yaml`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/fetal_tissue__610eb0c8.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/host_associated/fetal_tissue__610eb0c8.yaml` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the generated term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validate against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 records are present and the corpus reproduces exactly from `data/raw/`. |
| `just report` | Passed; the corpus report completed with 3,206 total records and 953 `UNGROUNDED` records. |
| `git diff --check` | Passed. |

## Identity and Grounding

`data/raw/gold_ecosystem_paths.tsv:746` is the maintained GOLD inventory row for the exact target path. It records:

| Column | Value |
|---|---|
| `canonical_path` | `Host-associated > Mammals > Fetus > Fetal tissue` |
| `leaf_label` | `Fetal tissue` |
| `depth` | `4` |
| `gold_node_count` | `2` |
| `organism_count` | `3` |
| `total_assertions` | `3` |
| `gold_node_ids` | `gold.ecosystem:7612|gold.ecosystem:7613` |

The generated `source_attestations` entry mirrors that row: it uses first GOLD node id `gold.ecosystem:7612`, source label `Fetal tissue`, the exact source path, `assertion_count: 3`, `assertion_unit: ORGANISM`, and the generated note that two GOLD ecosystem node ids share the path.

The source-path parent `Host-associated > Mammals > Fetus` is generated as `habitatmech:GOLD.c8a2e380c1` from `data/raw/gold_ecosystem_paths.tsv:745` and is a broader GOLD bin for the target. That parent is not item-reviewed; it is still a seeded `NARROW` match that places the minted `Fetus` habitat beneath `BTO:0000449` `fetus`.

The human-specific sibling is a different source concept:

| Identifier | Record | GOLD row | Source path |
|---|---|---|---|
| `habitatmech:GOLD.1ad500c492` | `data/habitats/host_associated/fetal_tissue.yaml` | `data/raw/gold_ecosystem_paths.tsv:2247` | `Host-associated > Mammals: Human > Fetus > Fetal tissue` |

`curation/decisions.tsv:1051` is the only maintained curation row for the target and records the August 2026 `CLASS`-level `CONFIRM_UNGROUNDED` sweep. The sweep established that no vendored ENVO, UBERON, FOODON, or BTO label matched `Fetal tissue` by its lexical routes; it explicitly did not establish that this source concept had been read as a real habitat or as a term-request candidate.

The nearest local ontology rows do not exactly ground this record:

| CURIE | Label | Why it is not exact |
|---|---|---|
| `UBERON:0005291` | `embryonic tissue` | Defined as tissue that is part of an embryo, not a fetus. |
| `BTO:0000449` | `fetus` | Whole unborn or unhatched vertebrate, not tissue and not an associated-environment identity. |
| `UBERON:0000323` | `late embryo` | Whole late embryo; `fetus` is only a synonym. |
| `BTO:0000473` | `fetal membrane` | Any membrane for fetal protection, nourishment, respiration, or excretion; narrower and extraembryonic. |
| `UBERON:0000478` | `extraembryonic structure` | Extraembryonic structures do not contribute to the embryo proper. |
| `UBERON:0007106` | `chorionic villus` | Child placental projection, not all fetal tissue. |

## Evidence

| Claim | Maintained evidence | Assessment |
|---|---|---|
| The generated record denotes GOLD `Host-associated > Mammals > Fetus > Fetal tissue`. | `data/raw/gold_ecosystem_paths.tsv:746` and `data/habitats/PATHS.tsv:2659`. | Supported exactly. |
| The source attestation has GOLD source id `gold.ecosystem:7612` and 3 organism assertions. | `data/raw/gold_ecosystem_paths.tsv:746`. | Supported exactly; `gold.ecosystem:7612` is the first node in the collapsed `gold_node_ids` list. |
| The target has no exact vendored ontology identity. | `curation/decisions.tsv:1051`, the absence of exact fetal-tissue rows in `data/raw/ontology_terms.tsv`, and the false near-miss rows listed above. | The negative ontology search is plausible, but the curation row is only a class-level lexical sweep, so this has not been endorsed at item depth. |
| The only parent is `habitatmech:GOLD.c8a2e380c1`. | `data/raw/gold_ecosystem_paths.tsv:745`, `data/habitats/PATHS.tsv:2762`, and `data/habitats/host_associated/fetus.yaml`. | The direct GOLD parent is broader than fetal tissue, but its own seeded `BTO:0000449` parent has not been item-reviewed. |

The record has no `definition`, `definition_source`, `synonyms`, `xrefs`, `environmental_parameters`, `characteristic_taxa`, `evidence`, `causal_graphs`, `discussions`, or `datasets`. That sparseness is consistent with a seeded GOLD-only record, but it is incomplete for a finished `REVIEWED` term-request candidate.

## Completeness

- Exact ignored/hidden-inclusive searches for `habitatmech:GOLD.baffd7f6c7`, `gold.ecosystem:7612`, `gold.ecosystem:7613`, and `Host-associated > Mammals > Fetus > Fetal tissue` covered `data/raw`, `data/habitats/PATHS.tsv`, `curation`, `history`, `research/habitats`, and `reports/yaml_record_review`. They found the generated target, its GOLD inventory row, its `PATHS.tsv` slug, the class-level curation decision, and sibling or child GOLD rows. They found no item-level target decision, target term request, target history record, target causal graph, committed target research report, or prior target review report.
- `find reports/yaml_record_review -maxdepth 1 -type f -iname '*fetal*' -print` covered the review-report directory including ignored files and found no pre-existing fetal-tissue review report before this report was written.
- Exact ignored/hidden-inclusive searches for `gold.ecosystem:7612` and `gold.ecosystem:7613` in `data/raw/gold_path_triads.tsv`, `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_studies.tsv`, and `data/raw/environment_parameters.tsv` found no extra committed per-triad, biosample, study, or environmental-parameter support for this exact GOLD path.
- The target is not merged with the zero-assertion `Mammals: Human` sibling. That sibling should be compared during future curation, but its source path is not identical and should not be collapsed into the target without an item-level SAME_AS decision.

## Findings

### Major: HM-FETAL-TISSUE-001 - `Fetal tissue` is still only class-swept, not curated as a real fetal-tissue habitat

The target represents a plausible mammalian fetal-tissue-associated habitat with three GOLD organism assertions, but `curation/decisions.tsv:1051` is still the August `CLASS`-level row whose note says whether the concept is a habitat at all was not assessed. `docs/HARMONIZATION.md#class-level-sweep` says those decisions do not promote records to `REVIEWED`; `docs/CURATION.md` requires an `ITEM` decision for confirmed term requests. The current YAML therefore correctly remains `mapping_status: SEEDED`, but it is not curation-ready: no maintained row defines the missing fetal-tissue-associated-environment concept, records the embryo/fetus/fetal-membrane near misses, or says why the broad `Mammals > Fetus > Fetal tissue` source is a host-associated habitat.

Maintained owner: replace `curation/decisions.tsv:1051` with an item-level `CONFIRM_UNGROUNDED` for `habitatmech:GOLD.baffd7f6c7`, and add a `curation/term_requests.tsv` row for a mammalian fetal-tissue-associated environment.

### Major: HM-FETAL-TISSUE-002 - the target inherits an unreviewed whole-fetus ontology parent

The target's sole parent is `habitatmech:GOLD.c8a2e380c1` `Fetus`, and that parent is generated as `NARROW` beneath `BTO:0000449` `fetus`. Under `CLAUDE.md` and `docs/CURATION.md`, whole host organisms and organism-stage terms are not identities for their microbial associated environments; they should remain minted habitat concepts with the organism or stage term retained as an xref. Until the `Mammals > Fetus` parent is item-reviewed, the target's broader hierarchy says, transitively, that a fetal-tissue-associated environment is-a whole fetus.

Maintained owner: curate the `habitatmech:GOLD.c8a2e380c1` parent through `curation/decisions.tsv` and, if no exact vendored associated-environment term exists, `curation/term_requests.tsv`, so `BTO:0000449` is not a `parent_habitats` edge.

## Recommended Edits

1. In `curation/decisions.tsv`, update the target row `habitatmech:GOLD.baffd7f6c7` from a class-level sweep to an item-level `CONFIRM_UNGROUNDED` decision. The note should name the exact GOLD path, record that `UBERON:0005291` `embryonic tissue`, `BTO:0000473` `fetal membrane`, `UBERON:0000478` `extraembryonic structure`, `UBERON:0007106` `chorionic villus`, and the generic tissue candidates are false exact identities, and keep the `Mammals: Human` fetal-tissue sibling out of scope unless it is reviewed at the same time.
2. Add a `curation/term_requests.tsv` row for `habitatmech:GOLD.baffd7f6c7`, with a requested label such as `mammalian fetal tissue-associated environment`, exact synonym `Fetal tissue`, a host-associated ENVO genus such as `ENVO:01001002` until a mammal-associated or fetus-associated environment term exists upstream, and `ADD` parent mode so the true GOLD source-path parent is retained.
3. Curate `habitatmech:GOLD.c8a2e380c1` separately before relying on the target's finished hierarchy. The parent should retain `BTO:0000449` only as an xref and request a mammalian fetus-associated-environment term if the vendored slice still lacks one.
4. Re-run the seeder from maintained inputs and inspect the regenerated fetal-tissue record before any bulk write.

## Follow-up Checks

For the direct target fix:

```bash
just seed
just seed-canary habitatmech:GOLD.baffd7f6c7 --force
just validate data/habitats/host_associated/fetal_tissue__610eb0c8.yaml
just validate-strict data/habitats/host_associated/fetal_tissue__610eb0c8.yaml
just term-requests-check
just validate-history
just verify-corpus --max-diffs 1
just seed-apply --force
just verify-corpus
just render
just qc
```

For the parent-record hierarchy fix, run the same canary flow with `habitatmech:GOLD.c8a2e380c1` before checking that `data/habitats/host_associated/fetal_tissue__610eb0c8.yaml` still has only strictly broader `parent_habitats`.

After regeneration, manually confirm:

- the target remains the GOLD `Host-associated > Mammals > Fetus > Fetal tissue` record and has not been conflated with `Host-associated > Mammals: Human > Fetus > Fetal tissue`;
- the target's `curation_history` names an `ITEM`-level curation event, not the August class sweep;
- the generated record has the requested definition, exact synonym, source attestation for `gold.ecosystem:7612`, and no BTO/UBERON embryo, fetus, fetal-membrane, or placental term as an exact identity;
- the Fetus parent no longer asserts a whole-organism BTO term as an is-a parent after that parent is curated.

## Additional Notes

The target's zero-assertion children at `data/raw/gold_ecosystem_paths.tsv:2050-2054` cover `Amniotic fluid`, `Amniotic sac`, `Chorionic villi`, `Umbilical cord`, and `Umbilical cord blood`. Several of those have exact anatomy or fluid terms in the vendored slice, so child curation should not be inferred from the generic fetal-tissue decision.
