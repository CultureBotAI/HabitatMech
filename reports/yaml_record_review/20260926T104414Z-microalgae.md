# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/host_associated/microalgae.yaml
- Started UTC: 2026-09-26T10:44:14Z
- Finished UTC: 2026-09-26T10:44:24Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.6759cf557e` |
| Label | `Microalgae` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Generated path | `data/habitats/host_associated/microalgae.yaml` |
| Maintained path lock | `data/habitats/PATHS.tsv:2037` maps `habitatmech:GOLD.6759cf557e` to `microalgae` |
| Source concept | GOLD `gold.ecosystem:4813`, `gold.ecosystem:4814`, and `gold.ecosystem:4815` |
| Source path | `Host-associated > Algae > Microalgae` |
| Grounding | `UNGROUNDED` |
| Mapping | `SEEDED` |

The reviewed file is generated from the GOLD source inventory and one
class-level curation decision. GOLD side tables provide additional review
context for this path but are not represented inline. It should not be patched
directly.

## Validation

| Command | Result |
|---|---|
| `just worklist --limit 0 --status all --out /tmp/habitatmech-worklist.tsv` | Passed; wrote 953 rows and placed `habitatmech:GOLD.6759cf557e` immediately after the already reviewed `Landfill leachate`. |
| `just validate data/habitats/host_associated/microalgae.yaml` | Passed with `No issues found`. |
| `just validate-strict data/habitats/host_associated/microalgae.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 expected records, 3206 found, 0 missing, 0 extra, 0 differing. |
| `just report` | Passed; no stale class-level sweep, contradictory path term, or non-habitat label contradiction was reported for this record. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

The generated identifier, label, category, source attestation, and assertion
count agree with the GOLD source inventory:

| Claim | Evidence | Assessment |
|---|---|---|
| The target is the collapsed GOLD `Microalgae` path. | `data/raw/gold_ecosystem_paths.tsv:675` records `Host-associated > Algae > Microalgae` with leaf label `Microalgae`, depth 3, three GOLD nodes, four organism assertions, and source IDs `gold.ecosystem:4813|gold.ecosystem:4814|gold.ecosystem:4815`. | Supported exactly. |
| The generated `source_attestations` block preserves the representative raw source ID, path, count, and unit. | The generated YAML carries `source: GOLD`, `source_id: gold.ecosystem:4813`, the same full source path, `assertion_count: 4`, `assertion_unit: ORGANISM`, and a note that three GOLD ecosystem node IDs share the path. | Supported exactly. |
| The generated `HOST_ASSOCIATED` category agrees with GOLD's top-level path. | The source path begins with `Host-associated`, and the target file has `habitat_category: HOST_ASSOCIATED`. | Supported exactly. |
| The generated `habitatmech:GOLD.02383c20a7` parent is inherited from the immediate GOLD `Host-associated > Algae` path parent. | The generated parent record `data/habitats/host_associated/algae.yaml` is the item-reviewed and defined `alga-associated environment` record for the GOLD parent path plus the merged MADIN `alga` source. | Supported exactly. This is a habitat parent, not the previous false `FOODON:03411301` organism-group parent. |
| `Microalgae` is not an exact identity match to current lexical candidates. | `FOODON:03412266` is `seaweed` with synonym `macroalgae`; `BTO:0000078` is `microglia`; `ENVO:03600064` is `microbialite`; and `FOODON:00001184` `algae material` is a material derived from algae. | Supported; all are adjacent or lexical near misses rather than an alga-associated microbial habitat. |
| `UNGROUNDED` is currently explained only by a class-level no-match sweep. | `curation/decisions.tsv:616` has `CONFIRM_UNGROUNDED` with `review_depth` `CLASS`; the generated curation history repeats that no ontology term fit by any lexical route and that habitat status was not assessed. | Reproducible but incomplete. `CLASS` depth does not establish that the leaf denotes a habitat or identify which novel alga-associated term should be requested. |

A bounded hidden/ignored-inclusive search of `data/raw/`, `curation/`,
`history/`, `research/`, `reports/yaml_record_review/`, `data/habitats/`, and
`pages/habitats/` for `habitatmech:GOLD.6759cf557e`,
`gold.ecosystem:4813`, `gold.ecosystem:4814`, `gold.ecosystem:4815`,
`microalgae`, `Microalgae`, and the exact source path found the expected
generated record, page, path lock, raw GOLD aggregate row, GOLD side-table
rows, and class-level decision row. It found contextual mentions in other
research reports, but no target-specific term request, generated term-request
row, causal overlay, append-only history entry, deep-research report, or prior
exact YAML review for this target.

## Evidence

| Record claim | Nearest source | Assessment |
|---|---|---|
| `source: GOLD`, `source_id: gold.ecosystem:4813`, `source_label: Microalgae`, and the full source path | `data/raw/gold_ecosystem_paths.tsv:675` | Supported exactly. |
| The note that three GOLD ecosystem node IDs share this path | `data/raw/gold_ecosystem_paths.tsv:675` lists `gold.ecosystem:4813|gold.ecosystem:4814|gold.ecosystem:4815`. | Supported exactly. |
| `assertion_count: 4`, `assertion_unit: ORGANISM` | `data/raw/gold_ecosystem_paths.tsv:675` has `organism_count=4` and `total_assertions=4`. | Supported; this count is the GOLD organism aggregate, not the path-level biosample side table. |
| Parent `habitatmech:GOLD.02383c20a7` | GOLD places the source concept under `Host-associated > Algae`; the maintained decision and term-request rows for `habitatmech:GOLD.02383c20a7` define that GOLD parent as `alga-associated environment`. | Supported. |
| Presence of GOLD side-table rows | `data/raw/gold_path_biosamples.tsv:433` records 30 biosamples for `Host-associated > Algae > Microalgae` at path ID `4815`, and `data/raw/gold_studies.tsv` has three one-path study rows for the same exact path. | Supported. Item review should inspect these before deciding the source's scope. |
| Absence of committed MIxS triads | An exact hidden/ignored-inclusive search of `data/raw/gold_path_triads.tsv` found no row for the target source path or its three GOLD node IDs. | Supported. No generated environmental-parameter claim is missing from the YAML. |
| Absence of characteristic taxa, record evidence, and causal graphs | Exact hidden/ignored-inclusive searches across `curation/causal_graphs/` and the generated YAML found no target-specific taxa or curated overlay, and GOLD path aggregates do not emit inline characteristic-taxon claims. | Supported. |

The vendored ontology slice has useful near terms but no exact
`microalga-associated environment` identity:

| CURIE | Label | Relationship to the GOLD leaf |
|---|---|---|
| `ENVO:01001000` | `environmental system determined by an organism` | Current genus for the item-reviewed `alga-associated environment`, brown-alga, red-alga, and diatom siblings; a plausible ontology genus if item review keeps the target under a minted identity. |
| `ENVO:01001001` | `plant-associated environment` | Current genus for the green-alga sibling because green algae are Viridiplantae; only true for the subset of microalgae that are green algae. |
| `FOODON:03411301` | `algae` | An organism-group identity rejected from the `alga-associated environment` parent and related siblings; not a microbial habitat. |
| `FOODON:03412266` | `seaweed` | A macroscopic-algae class with synonym `macroalgae`, so it is the wrong size class for `Microalgae`. |
| `FOODON:00001184` | `algae material` | A material derived from algae, not the alga-associated environment named by the GOLD host-associated path. |
| `BTO:0000078` | `microglia` | A mammalian central-nervous-system cell type and only a string near miss. |
| `ENVO:03600064` | `microbialite` | A carbonate rock; only a string near miss. |

The nearby generated GOLD siblings keep narrower alga-associated concepts as
their own item-reviewed records. `brown-alga-associated environment`,
`green-alga-associated environment`, `red-alga-associated environment`, and
`diatom-associated environment` all have `CONFIRM_UNGROUNDED` item decisions,
curated HabitatMech definitions, and the same immediate
`habitatmech:GOLD.02383c20a7` parent inferred from GOLD's `Host-associated >
Algae` hierarchy.

## Completeness

The generated record preserves the raw GOLD aggregate row and the class-level
curation decision. It correctly avoids unsupported characteristic taxa,
environmental parameters, evidence items, and causal graph claims, and it has
no generated place for the GOLD side-table study IDs or biosample count.

It is incomplete as an item-reviewed habitat. The only maintained decision is a
class-level no-match sweep that did not assess whether the source concept is a
habitat. Unlike its brown-, green-, red-, and diatom-alga siblings, it has no
committed term request that renames the organism-like source label to a
`microalga-associated environment`, defines the scope of a polyphyletic
microscopic-alga host class, or deliberately chooses `ENVO:01001000` versus
`ENVO:01001001` as the authored ontology genus.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| HM-MICROALGAE-001 | Major | `Microalgae` is still backed only by `review_depth` `CLASS`. The generated `UNGROUNDED` and `SEEDED` statuses are reproducible, but the maintained row explicitly says no one assessed whether the source concept is a habitat. The `Host-associated > Algae > Microalgae` path and four organism assertions make the target plausible as a host-associated habitat; the item review still needs to prove that the GOLD path denotes microalgae acting as hosts rather than an alga biomass, a laboratory culture, or another material use of microalgae. | `curation/decisions.tsv`; if the record is a real habitat, `curation/term_requests.tsv` |
| HM-MICROALGAE-002 | Major | The record still publishes the raw organism-group label `Microalgae` and lacks the curated definition and exact synonym pattern already used for sibling alga-associated records. Brown algae, green algae, red algae, and diatoms have item decisions, minted `*-associated environment` labels, and definitions under `ENVO:01001000` or `ENVO:01001001`; the Microalgae sibling remains an undefined minted leaf under `alga-associated environment`. | `curation/term_requests.tsv`, regenerated `data/habitats/host_associated/microalgae.yaml` |

## Recommended Edits

1. Item-review `habitatmech:GOLD.6759cf557e` in `curation/decisions.tsv`,
   replacing the class-level row with an item-level decision.
2. Inspect GOLD nodes `gold.ecosystem:4813`, `gold.ecosystem:4814`, and
   `gold.ecosystem:4815`, including the `4815` biosample side row and study IDs
   `Gs0144475`, `Gs0150293`, and `Gs0151601`, to confirm whether the GOLD leaf
   denotes microalgae acting as hosts.
3. If the GOLD source concept is a host-associated microalga habitat, retain a
   minted `CONFIRM_UNGROUNDED` item decision and add a
   `curation/term_requests.tsv` definition such as `microalga-associated
   environment` with source label `Microalgae` as an exact synonym.
4. Choose the term request genus deliberately: `ENVO:01001000` is safe for a
   polyphyletic microalga host class, while `ENVO:01001001` would overclaim
   Viridiplantae unless item review establishes the source leaf is restricted to
   green microalgae.
5. If the source path instead denotes an alga material or culture artifact,
   redirect the curation to that maintained input instead of copying the
   sibling host-associated term-request pattern.

Do not hand-edit `data/habitats/host_associated/microalgae.yaml`; regenerate it
from maintained TSVs after curation changes.

## Follow-up Checks

After adding item-level curation, rerun:

- `just seed`
- `just seed-canary habitatmech:GOLD.6759cf557e`
- inspect `data/habitats/host_associated/microalgae.yaml`
- `just validate-strict data/habitats/host_associated/microalgae.yaml`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just report`

If a causal overlay is added later, also run
`just validate-causal curation/causal_graphs/microalgae.yaml` and
`just validate-causal-all`.

## Additional Notes

None.
