# YAML Record Review

- Repository: HabitatMech
- Record: `data/habitats/engineered/percolator.yaml`
- Started UTC: 2026-09-25T21:26:48Z
- Finished UTC: 2026-09-25T21:26:58Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Identifier | `habitatmech:GOLD.7229e2cbd0` |
| Label | `Percolator` |
| Class | `HabitatRecord` |
| Category | `ENGINEERED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Source concept | GOLD `Engineered > Lab enrichment > Percolator` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/PATHS.tsv`, and `curation/decisions.tsv`; the YAML record is read-only |
| Locked slug | `data/habitats/PATHS.tsv:2129` maps `habitatmech:GOLD.7229e2cbd0` to `percolator` |

The generated record denotes the collapsed GOLD depth-3 path
`Engineered > Lab enrichment > Percolator`.
`data/raw/gold_ecosystem_paths.tsv:903` lists leaf label `Percolator`, depth
`3`, one organism assertion, no study or biosample assertions, and three GOLD
node ids: `gold.ecosystem:7128`, `gold.ecosystem:7129`, and
`gold.ecosystem:7130`.

`data/habitats/engineered/percolator.yaml:1-15` preserves that source identity
with the minted identifier, the source label, the exact source path, first
collapsed source id `gold.ecosystem:7128`, the `ORGANISM` assertion unit, and a
note that the other two node ids share this path.

## Validation

| Command | Result |
| --- | --- |
| `just validate data/habitats/engineered/percolator.yaml` | Passed; LinkML reported no issues. |
| `just validate-strict data/habitats/engineered/percolator.yaml` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; `curation/term_requests.tsv` is current for 109 generated terms. |
| `just validate-history` | Passed; 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected and found 3206 records, with 0 missing, 0 extra, and 0 differing files. |
| `just worklist --limit 0 --status all --out /tmp/habitatmech-percolator-worklist.tsv` | Passed; wrote 953 ungrounded rows and ranked `habitatmech:GOLD.7229e2cbd0` at row 341 with only `ENVO:01003008` `percolation` as a lexical candidate. |
| `just report` | Passed; the corpus report completed and kept the class-level sweep separate from individually examined term requests. |
| `git diff --check` | Passed before the report was written. |

No validator was skipped.

## Identity and Grounding

`data/raw/gold_ecosystem_paths.tsv:903` supports the generated GOLD identity
exactly. The raw row is for `Engineered > Lab enrichment > Percolator`, has
leaf label `Percolator`, reports one organism-level assertion, and collapses
source nodes `gold.ecosystem:7128`, `gold.ecosystem:7129`, and
`gold.ecosystem:7130`. The generated source attestation at
`data/habitats/engineered/percolator.yaml:8-15` repeats the first source node,
the exact path, the expected assertion count, the assertion unit, and the
collapsed-node note.

The only maintained grounding decision is still a lexical class sweep.
`curation/decisions.tsv:678` records `CONFIRM_UNGROUNDED` with no object term,
no object label, no relation, and `review_depth` `CLASS`. The note says no term
in the vendored ontology slice matched by the class-level search routes, and it
explicitly says the sweep did not assess whether the source concept is a
habitat.

`data/habitats/engineered/percolator.yaml:6-7` attaches parent
`habitatmech:GOLD.f69b5ed198`, matching the GOLD parent path
`Engineered > Lab enrichment`. That parent record is itself class-level
`UNGROUNDED`, so `percolator` is currently a minted child of a minted source
node rather than of an item-reviewed ontology genus.

`just worklist` offered `ENVO:01003008` `percolation` as the only lexical
candidate. The ontology slice defines that term as a material transport
process at `data/raw/ontology_terms.tsv:9398`, and the subclass table places it
under a process parent at `data/raw/ontology_subclass_edges.tsv:7777`. It is
therefore a near lexical miss, not a valid identity for a GOLD lab-enrichment
percolator.

## Evidence

| Claim | Evidence | Status |
| --- | --- | --- |
| The record denotes the GOLD path `Engineered > Lab enrichment > Percolator`. | `data/raw/gold_ecosystem_paths.tsv:903` lists that canonical path, leaf label `Percolator`, and node ids `gold.ecosystem:7128|gold.ecosystem:7129|gold.ecosystem:7130`; `data/habitats/engineered/percolator.yaml:8-15` repeats the first source node, the path, and the collapsed-node note. | Supported exactly. |
| The source attestation's assertion count is one GOLD organism assertion. | `data/raw/gold_ecosystem_paths.tsv:903` reports `organism_count` 1 and `total_assertions` 1 for the collapsed path, and `data/habitats/engineered/percolator.yaml:13-14` records `assertion_count: 1` with `assertion_unit: ORGANISM`. | Supported exactly. |
| The committed GOLD side inventories do not add direct study, biosample, or MIxS-triad context for this path. | Ignored/hidden-inclusive exact searches for `Engineered > Lab enrichment > Percolator`, `gold.ecosystem:7128`, `gold.ecosystem:7129`, and `gold.ecosystem:7130` found no rows in `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_studies.tsv`, or `data/raw/gold_path_triads.tsv`. | Supported for the current raw tables. |
| No item-level curation input or causal overlay is attached to this record. | Ignored/hidden-inclusive exact searches found `habitatmech:GOLD.7229e2cbd0` only in `curation/decisions.tsv`, `data/habitats/PATHS.tsv`, and the generated YAML, and `find curation/causal_graphs -maxdepth 1 -type f -name 'percolator.yaml' -print` returned no files. | Supported exactly. |

## Completeness

The record is complete for the currently generated source state but incomplete
as curation. It has the exact GOLD source path, a locked slug, one
source-derived parent, one class-level `CONFIRM_UNGROUNDED` decision, no
definition, no synonyms, no xrefs, no side-table study or biosample support,
no environmental parameters, no characteristic taxa, no causal graph, no
discussion, and no dataset.

Ignored/hidden-inclusive exact searches covered `curation/decisions.tsv`,
`curation/term_requests.tsv`, `curation/term_requests_excluded.tsv`,
`curation/term_requests/`, `curation/causal_graphs/`, `history/`,
`research/`, `reports/habitat_research_manifest.tsv`,
`conf/id_label_targets.yaml`, `data/habitats/PATHS.tsv`,
`data/habitats/engineered/percolator.yaml`,
`data/raw/gold_ecosystem_paths.tsv`, `data/raw/gold_path_biosamples.tsv`,
`data/raw/gold_studies.tsv`, `data/raw/gold_path_triads.tsv`,
`data/raw/ontology_terms.tsv`, and `data/raw/ontology_subclass_edges.tsv`.
They found the generated target, the locked slug, the source inventory row, the
`curation/decisions.tsv` class-level row, the process term `percolation`, and
no target-specific term request, excluded term-request row, history record,
committed research report, causal overlay, direct MIxS-triad row, label-drift
target entry, or pre-existing `*-percolator.md` review report.

The exact path has one direct GOLD organism assertion but no committed study or
biosample side-table rows. That is too little context to decide whether
`Percolator` names a real enrichment-culture vessel or matrix that should be
defined as a habitat, an apparatus that should only contextualize a sampled
medium, or an informal source bucket that should be marked non-habitat.

## Findings

| ID | Severity | Finding | Maintained owner |
| --- | --- | --- | --- |
| HM-PERCOLATOR-001 | Major | `Percolator` remains at `review_depth` `CLASS`. The only maintained decision at `curation/decisions.tsv:678` explicitly did not assess habitathood, while the exact GOLD path has one organism assertion and no side-table study, biosample, or MIxS-triad rows in the committed GOLD inventories. The only lexical worklist candidate, `ENVO:01003008` `percolation`, is a process and is not a defensible identity. No item-level curation has decided whether this GOLD concept should remain a minted lab-enrichment habitat, hang under a more specific enrichment-culturing genus, or be retired as a non-habitat apparatus/source bucket. | `curation/decisions.tsv`; if the concept stays minted, add a definition and authored hierarchy in `curation/term_requests.tsv`. |

## Recommended Edits

1. Reopen `habitatmech:GOLD.7229e2cbd0` in `curation/decisions.tsv` with
   `review_depth` `ITEM`; inspect GOLD nodes `7128`, `7129`, and `7130`, the
   one linked organism assertion, and any underlying sample metadata to decide
   whether `Engineered > Lab enrichment > Percolator` denotes a true microbial
   habitat.
2. If the source concept is a real lab-enrichment habitat with no exact
   ontology identity, keep it minted with `CONFIRM_UNGROUNDED`, add an
   evidence-backed `curation/term_requests.tsv` definition, and give it the
   tightest strict parent verified by the item review.
3. If the source concept is only a process, apparatus, or metadata bucket
   rather than a microbial habitat, mark it `NOT_APPLICABLE` and rerun seeding
   so the generated record is retired instead of promoted as a term-request
   candidate.

## Follow-up Checks

- `rg --no-ignore --hidden -n 'habitatmech:GOLD\.7229e2cbd0|Engineered > Lab enrichment > Percolator|gold\.ecosystem:(7128|7129|7130)' curation/decisions.tsv curation/term_requests.tsv history research data/habitats/engineered`
- `just seed`
- `just seed-canary habitatmech:GOLD.7229e2cbd0`
- `just seed-apply --force`
- `just validate data/habitats/engineered/percolator.yaml`
- `just validate-strict data/habitats/engineered/percolator.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just report`

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*percolator.md' -print` found no pre-existing Percolator review report before this file was written; `find` included ignored files under the searched directory.
- `find curation/causal_graphs -maxdepth 1 -type f -name 'percolator.yaml' -print` found no slug-matched causal overlay before this report was written; `find` included ignored files under the searched directory.
- `find . -name AGENTS.md -print` found no local `AGENTS.md` files before this report was written; `find` included ignored files under the searched tree.
