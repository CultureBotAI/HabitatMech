# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/abdomen__17b71cfa.yaml`
- Started UTC: 2026-09-22T20:09:00Z
- Finished UTC: 2026-09-22T20:11:02Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/host_associated/abdomen__17b71cfa.yaml` |
| Class | `HabitatRecord` |
| Identifier | `UBERON:0000916` |
| Label | `abdomen` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `REVIEWED` |
| Record status | Generated from one GOLD source concept with an item-level `GROUND` decision |

This is the ontology-grounded record currently fed by the GOLD path `Host-associated > Arthropoda: Crustaceans > Whole body > Abdomen/Pleon`. Its contributing source identifier is the deterministic minted id `habitatmech:GOLD.1e0a05a113`, computed from `sha1("GOLD:Host-associated > Arthropoda: Crustaceans > Whole body > Abdomen/Pleon")[:10]`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/abdomen__17b71cfa.yaml` | Passed; LinkML validation reported no issues. |
| `just validate-strict data/habitats/host_associated/abdomen__17b71cfa.yaml` | Passed; one file scanned with 0 error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 generated records, found 3206, with 0 missing, 0 extra, and 0 differing. |
| `git diff --check` | Passed; no whitespace errors. |

## Identity and Grounding

The direct GOLD source identity is supported:

| Raw field | Value |
|---|---|
| `canonical_path` | `Host-associated > Arthropoda: Crustaceans > Whole body > Abdomen/Pleon` |
| `leaf_label` | `Abdomen/Pleon` |
| `path_depth` | `4` |
| `gold_node_count` | `2` |
| `organism_count` | `0` |
| `metagenome_count` | `0` |
| `biosample_count` | `0` |
| `total_assertions` | `0` |
| `gold_node_ids` | `gold.ecosystem:7308\|gold.ecosystem:7309` |

`source_attestations` matches `data/raw/gold_ecosystem_paths.tsv:1786`: the generated record keeps `gold.ecosystem:7308` as the first source id, records the source label and path exactly, omits `assertion_count` because the raw organism count is zero, and includes the expected note that two GOLD node ids share this path.

The GOLD path-parent edge to `habitatmech:GOLD.bd9662fadb` is supported. `data/raw/gold_ecosystem_paths.tsv:1785` records the immediate parent path `Host-associated > Arthropoda: Crustaceans > Whole body`, whose deterministic minted id is `habitatmech:GOLD.bd9662fadb`; `data/habitats/host_associated/whole_body__e6584d8d.yaml` is the generated record for that parent path.

The exact ontology grounding is not supported. `curation/decisions.tsv:264` grounds `habitatmech:GOLD.1e0a05a113` to `UBERON:0000916` as an exact `abdomen` match because the GOLD slash label contains `Abdomen`, but `data/raw/ontology_terms.tsv:12962` defines `UBERON:0000916` as the subdivision of a vertebrate body between the thorax and pelvis. GOLD scopes the source path to `Arthropoda: Crustaceans`, so the `Abdomen/Pleon` source path denotes a crustacean body tagma rather than a vertebrate trunk subdivision.

`mapping_status: REVIEWED` is mechanically expected but semantically wrong: `curation/decisions.tsv:264` is an item-level `GROUND` row, and this record has no other contributing source concept, so the generated record becomes `REVIEWED` even though the row chooses the wrong ontology identifier.

## Evidence

| Claim | Nearest support | Review |
|---|---|---|
| The generated record represents GOLD node ids `gold.ecosystem:7308` and `gold.ecosystem:7309` at `Host-associated > Arthropoda: Crustaceans > Whole body > Abdomen/Pleon`. | `data/raw/gold_ecosystem_paths.tsv:1786` | Supported exactly. |
| The GOLD path has no upstream organism, metagenome, or biosample assertions. | `data/raw/gold_ecosystem_paths.tsv:1786` | Supported exactly. |
| The GOLD `Abdomen/Pleon` path is a child of the generated crustacean `Whole body` source path. | `data/raw/gold_ecosystem_paths.tsv:1785`; `src/habitatmech/seed.py` | Supported. `ingest_gold()` links each GOLD path to the concept generated from the next path level up. |
| `Host-associated > Arthropoda: Crustaceans > Whole body > Abdomen/Pleon` is exactly `UBERON:0000916 abdomen`. | `curation/decisions.tsv:264`; `data/raw/ontology_terms.tsv:12962`; `data/raw/gold_ecosystem_paths.tsv:1786` | Unsupported. The curation row makes this claim, but the UBERON target is vertebrate-scoped and the GOLD path is crustacean-scoped. |
| `mapping_status: REVIEWED` | `curation/decisions.tsv:264` | Structurally supported. The single source concept has an item-level decision, so the seeder emits `REVIEWED`. |

Unsupported or over-scoped claims: the `identifier: UBERON:0000916`, `grounding_status: EXACT`, `source_attestations.mapping_predicate: skos:exactMatch`, and ontology-derived label/definition/parent all rely on treating a crustacean `Abdomen/Pleon` source concept as the vertebrate abdomen class.

## Completeness

No direct source attestation is missing for the GOLD source row: the record carries the source name, first source id, source label, canonical source path, and the note explaining that two GOLD node ids share this path. Its omitted `assertion_count` is correct for a GOLD row with `organism_count: 0`.

The consequential completeness gap is the missing correct item-level decision for the crustacean `Abdomen/Pleon` concept. Exact ignored/hidden-inclusive searches found no `curation/term_requests.tsv`, `curation/external_xrefs.tsv`, `curation/causal_graphs`, `history`, or raw research artifact keyed by `habitatmech:GOLD.1e0a05a113`; the only exact curation row is the wrong `GROUND UBERON:0000916` row in `curation/decisions.tsv`.

No maintained causal overlay, history file, raw research report, or prior exact YAML review report was found by filename for `abdomen__17b71cfa` or `abdomen-17b71cfa` under `curation/causal_graphs`, `history`, `research`, or `reports/yaml_record_review`; the `find` search included ignored files beneath those checked directories.

The sibling crustacean whole-body part paths do not rescue this grounding. `Host-associated > Arthropoda: Crustaceans > Whole body > Cephalothorax` is seeded to a BTO `cephalothorax` term whose definition explicitly covers higher crustaceans, while `Host-associated > Arthropoda: Crustaceans > Whole body > Thorax/Pereon` is curated to BTO `thorax`, whose definition explicitly names the corresponding part of a crustacean; this abdomen record needs the same taxonomic fit check rather than a lexical match to a vertebrate anatomy term.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| blocker | `abdomen__17b71cfa.yaml` is exactly grounded to `UBERON:0000916`, making a crustacean `Abdomen/Pleon` source concept denote the vertebrate abdomen. The exact grounding is taxon-incompatible, and all ontology-derived fields on this record inherit the wrong identity. | `data/raw/gold_ecosystem_paths.tsv:1786` scopes the source path to `Arthropoda: Crustaceans`; `curation/decisions.tsv:264` grounds that source to `UBERON:0000916`; `data/raw/ontology_terms.tsv:12962` defines `UBERON:0000916` as the subdivision of a vertebrate body between the thorax and pelvis. | Replace the row for `habitatmech:GOLD.1e0a05a113` in `curation/decisions.tsv`; then regenerate the GOLD source concept away from `data/habitats/host_associated/abdomen__17b71cfa.yaml`. |

No major findings.

No minor findings.

## Recommended Edits

1. Replace the `curation/decisions.tsv` row for `habitatmech:GOLD.1e0a05a113`.
   Re-review `Host-associated > Arthropoda: Crustaceans > Whole body > Abdomen/Pleon` against a crustacean or arthropod abdomen/pleon term. If no exact or broader vendored ontology term fits, use an item-level `CONFIRM_UNGROUNDED` decision without `UBERON:0000916`; the GOLD source-path parent will still keep the regenerated source under `habitatmech:GOLD.bd9662fadb`.
2. Run `just seed-canary habitatmech:GOLD.1e0a05a113` and inspect the generated YAML to confirm the source concept no longer resolves to `UBERON:0000916`.
3. Regenerate the corpus with the normal retire/redirect workflow for an identifier move: the current `UBERON:0000916` record may disappear if no source concept remains grounded to it, and its old published page needs a redirect if it is pruned.
4. Screen the sibling `Abdomen` reports and GOLD crustacean whole-body part records to make sure all slash-label tagma paths are grounded only when the target ontology class covers that arthropod or crustacean anatomy.

## Follow-up Checks

- `just seed-canary habitatmech:GOLD.1e0a05a113`
- `just seed`
- `just verify-corpus --max-diffs 1`
- `just validate-strict`
- `just validate-history`
- Exact ignored/hidden-inclusive searches for `habitatmech:GOLD.1e0a05a113`, `Host-associated > Arthropoda: Crustaceans > Whole body > Abdomen/Pleon`, and `UBERON:0000916` across `curation`, `data/raw`, and `data/habitats`, excluding generated text maps and pages, to verify that the crustacean source concept no longer uses the vertebrate abdomen identity.

## Additional Notes

- The pre-report exact filename check `find reports/yaml_record_review -maxdepth 1 -type f -name '*abdomen__17b71cfa*' -print` returned no previous exact report; `find` includes ignored files.
- Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.1e0a05a113`, `Host-associated > Arthropoda: Crustaceans > Whole body > Abdomen/Pleon`, `gold.ecosystem:7308`, `gold.ecosystem:7309`, `UBERON:0000916`, and `habitatmech:GOLD.bd9662fadb` found the generated target, raw GOLD row, raw ontology row, the source-path parent, the exact curation decision row, and the sibling crustacean whole-body part paths cited above.
- An exact ignored/hidden-inclusive search of `curation/decisions.tsv` for `GROUND` rows targeting `UBERON:0000916` found only the `habitatmech:GOLD.1e0a05a113` row. The other current `UBERON:0000916` mentions under `data/habitats/host_associated/` are narrower minted GOLD records or discussions that already reject the term as vertebrate-scoped for insect abdomen descendants.
