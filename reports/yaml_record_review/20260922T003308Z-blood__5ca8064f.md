# YAML Record Review: Blood

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/host_associated/blood__5ca8064f.yaml`
- Started UTC: `2026-09-22T00:31:30Z`
- Finished UTC: `2026-09-22T00:33:08Z`
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.476e09babd` |
| Label | `Blood` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Generated path | `data/habitats/host_associated/blood__5ca8064f.yaml` |
| Source | `gold.ecosystem:3650`, first of two GOLD node IDs on `Host-associated > Fish > Circulatory system > Blood` |

The record is generated from the committed GOLD path inventory. `data/habitats/PATHS.tsv` locks `habitatmech:GOLD.476e09babd` to `blood__5ca8064f`, so the reviewed YAML is the unique generated file for the Fish Blood source concept.

The record has one generated curation event from `seed_from_sources` at `2026-08-16T05:58:02Z`. No authored definition, parameters, characteristic taxa, discussion entries, datasets, or causal edges are present on the record.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/host_associated/blood__5ca8064f.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/host_associated/blood__5ca8064f.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 records, found 3206, with 0 missing, 0 extra, and 0 differing records. |
| `git diff --check` | Passed. |

## Identity and Grounding

- The generated identifier, `habitatmech:GOLD.476e09babd`, is the minted GOLD identifier for the canonical path `Host-associated > Fish > Circulatory system > Blood`.
- The `HOST_ASSOCIATED` category matches the GOLD top-level path component, `Host-associated`.
- The exact GOLD inventory row records the leaf label `Blood`, depth 4, two collapsed GOLD node IDs, and seven organism assertions for `gold.ecosystem:3650|gold.ecosystem:3902`; the source attestation preserves the first node ID and the generated note points to the collapsed inventory.
- The immediate GOLD source-path parent is `Host-associated > Fish > Circulatory system`, which maps through `data/habitats/PATHS.tsv` to `habitatmech:GOLD.5b9148ecc0`; that identifier appears as a `parent_habitats` entry.
- The ontology parent `UBERON:0000178` is present in `data/raw/ontology_terms.tsv` with canonical label `blood`.
- The parent record `habitatmech:GOLD.5b9148ecc0` keeps the Fish `Circulatory system` path minted and attaches the broader UBERON `circulatory system` term, so Fish Blood's source-path parent points at the expected generated Fish-specific parent.
- Four same-depth GOLD paths end at `Circulatory system > Blood`: Mammals: Human, Mammals, Birds, and Fish. The anti-conflation rule in `docs/HARMONIZATION.md` therefore leaves each path minted and attaches `UBERON:0000178` as a broader parent instead of letting any one path claim exact identity with the ontology term.

## Evidence

| Claim | Trace |
|---|---|
| GOLD source path and organism count | `data/raw/gold_ecosystem_paths.tsv` contains `Host-associated > Fish > Circulatory system > Blood`, depth 4, two node IDs, and `7` organism assertions. |
| Path lock | `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.476e09babd` to `blood__5ca8064f`. |
| Source-path parent | `data/raw/gold_ecosystem_paths.tsv` contains `Host-associated > Fish > Circulatory system`; `data/habitats/PATHS.tsv` maps it to `habitatmech:GOLD.5b9148ecc0`; `data/habitats/host_associated/circulatory_system__f5b69d9c.yaml` is the generated parent record. |
| UBERON broader term | `data/raw/ontology_terms.tsv` labels `UBERON:0000178` as `blood` and `UBERON:0001009` as `circulatory system`. |
| Ambiguous Blood leaves | `data/raw/gold_ecosystem_paths.tsv` has four depth-4 host-associated `Circulatory system > Blood` paths under Mammals: Human, Mammals, Birds, and Fish, supporting the minted NARROW split. |

The exact ignored/hidden-inclusive searches for `habitatmech:GOLD.476e09babd`, `gold.ecosystem:3650`, `blood__5ca8064f`, and `Host-associated > Fish > Circulatory system > Blood` found no maintained decision row, term-request row, history record, causal-graph overlay, or prior YAML review report. Those searches did find the generated record, the path lock, the GOLD inventory row, and a `curation/samples/narrow-20260814.tsv` sample entry that marked this seeded NARROW mapping `correct`, but the sample row is not an item-level `curation/decisions.tsv` decision.

The exact ignored/hidden-inclusive GOLD triad search found the source path only in `data/raw/gold_ecosystem_paths.tsv` and found no `data/raw/gold_path_triads.tsv` rows for this Fish Blood path. The empty triad-derived slots in the generated record are therefore consistent with the committed GOLD triad inventory.

## Completeness

- No authored definition is expected on this seeded NARROW GOLD record because no `curation/term_requests.tsv` row exists for `habitatmech:GOLD.476e09babd`.
- No causal mechanism overlay is expected; `find curation/causal_graphs -name '*blood*' -print` found no blood-specific overlay.
- No characteristic taxa are expected; the record has no PREGO attestation, and the Madin `UBERON:0000178` record is represented separately as `data/habitats/host_associated/blood__d6f5ae05.yaml`.
- The generated `SEEDED` mapping status is expected because this Fish GOLD source concept has no item-level decision in `curation/decisions.tsv`.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

None required beyond the normal pull-request checks. If a future curator promotes this record, the maintained change should be an item-level row in `curation/decisions.tsv`, an optional definition row in `curation/term_requests.tsv`, or a causal overlay in `curation/causal_graphs/`, followed by `just seed`, a `just seed-canary habitatmech:GOLD.476e09babd`, `just validate-causal-all`, `just validate-strict data/habitats/host_associated/blood__5ca8064f.yaml`, `just verify-corpus`, and `just validate-history` if a history record is added.

## Additional Notes

- The file is one of several same-label `Blood` records. The reviewed file is the Fish-specific GOLD leaf; the UBERON-grounded `blood__d6f5ae05.yaml` record and the Mammals, Mammals: Human, and Birds minted GOLD leaves are separate HabitatRecords.
- The source attestation's `assertion_count: 7` is a GOLD organism count and should only be compared with other GOLD organism counts, not with BacDive strain counts or PREGO taxon counts.
