# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/other/spot_leaf_stem.yaml
- Started UTC: 2026-09-26T08:54:03Z
- Finished UTC: 2026-09-26T08:54:03Z
- Verdict: pass with minor issues

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:BACDIVE.31392c69b9` |
| Label | `leaf-or-stem spot lesion environment` |
| Class | `HabitatRecord` |
| Category | `OTHER` |
| Generated path | `data/habitats/other/spot_leaf_stem.yaml` |
| Maintained path lock | `data/habitats/PATHS.tsv:1180` maps `habitatmech:BACDIVE.31392c69b9` to `spot_leaf_stem` |
| Source concept | BACDIVE `bacdive.isolation_source:spot-leaf,stem` |
| Source label | `Spot-Leaf,Stem` |
| Source path | `#Infection > #Plant infections > #Spot (Leaf,Stem)` |
| Grounding | `UNGROUNDED` |
| Mapping | `REVIEWED` |

The reviewed file is generated from the BacDive raw inventory, item-level
curation decisions, and the ENVO term-request table. It should not be patched
directly.

## Validation

| Command | Result |
|---|---|
| `just worklist --limit 0 --status all --out /tmp/habitatmech-worklist.tsv` | Passed; wrote 953 rows and placed `habitatmech:BACDIVE.31392c69b9` after `Waste`. |
| `just validate data/habitats/other/spot_leaf_stem.yaml` | Passed with `No issues found`. |
| `just validate-strict data/habitats/other/spot_leaf_stem.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 expected records, 3206 found, 0 missing, 0 extra, 0 differing. |
| `just report` | Passed; no stale class-level sweep, contradictory path term, or non-habitat label contradiction was reported for this record. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

The generated identifier, label, source attestation, status, and characteristic
taxon rows agree with the BacDive source inventory:

| Claim | Evidence | Assessment |
|---|---|---|
| The target is the BacDive `Spot-Leaf,Stem` source concept. | `data/raw/bacdive_isolation_sources.tsv:138` records `bacdive.isolation_source:spot-leaf,stem`, source label `Spot-Leaf,Stem`, normalized value `spot-leaf,stem`, 5 strain assertions, and 4 source taxa. | Supported exactly. |
| The generated `source_attestations` block preserves the raw source ID, label, count, and unit. | The generated YAML carries `source: BACDIVE`, `source_id: bacdive.isolation_source:spot-leaf,stem`, `source_label: Spot-Leaf,Stem`, and `assertion_count: 5` with `assertion_unit: STRAIN`. | Supported exactly. |
| The four generated `characteristic_taxa` rows preserve the BacDive rank order and counts. | `data/raw/bacdive_source_taxa.tsv:2611-2614` lists `Xanthomonas campestris` with count 2, then `Pseudomonas floridensis`, `Pseudomonas syringae`, and `Klebsiella aerogenes` with count 1 each for the same BacDive source ID. | Supported exactly as reported BacDive source taxa. No strain count is upgraded to a causal or diagnostic claim. |
| The upstream lexical grounding to `PO:0009047` should not be adopted. | `data/raw/isolation_source_groundings.tsv:291` maps `Spot-Leaf,Stem` to `PO:0009047` `stem` as a medium-confidence `skos:closeMatch`; lines 266 and 295 map `Rot-Root,Stem` and `Stem-Branch` to the same `stem` term. | Supported. `stem` drops the leaf disjunct and would merge a localized plant lesion with both a root-or-stem rot source and the healthy BacDive stem/branch source. |
| `ENVO:01001057` is the current strict broader parent, not an exact identity. | `data/raw/ontology_terms.tsv:8552` names `ENVO:01001057` as `environment associated with a plant part or small plant`; `curation/term_requests.tsv:2` defines this record under that parent while noting that ENVO lacks a shared plant-infection-site environment parent. | Supported. The parent is broad enough for a foliar or stem lesion environment, and the record correctly remains `UNGROUNDED`. |
| The definition denotes an infected lesion rather than a healthy leaf or stem organ. | `research/habitats/other/spot-leaf-stem-habitatmech-bacdive-31392c69b9-deep-research-claude_code.md` resolves the BacDive source under `#Infection > #Plant infections > #Spot (Leaf,Stem)` and explicitly contrasts the separate healthy BacDive `#Leaf (Phyllosphere)` and `#Stem (Branch)` branch. | Supported. The generated definition captures a discrete necrotic or chlorotic lesion on a leaf or stem of a living plant, formed by localized non-systemic infection. |
| `REVIEWED` status is justified. | `curation/decisions.tsv:1545` has an item-level `CONFIRM_UNGROUNDED`; `curation/term_requests.tsv:2` adds the curated novel-term definition. | Supported. There is one contributing source concept and it has item-level review. |

A bounded hidden/ignored-inclusive search of `data/raw/`, `curation/`,
`history/`, `research/`, `reports/yaml_record_review/`, `data/habitats/`, and
`pages/habitats/` for `habitatmech:BACDIVE.31392c69b9`,
`bacdive.isolation_source:spot-leaf,stem`, `Spot-Leaf,Stem`, and
`leaf-or-stem spot lesion environment` found the expected raw BacDive rows,
upstream `PO:0009047` close match, item-level curation decision, term request,
generated robot-template row, generated YAML, path lock, live page, old page
redirect, committed deep-research report, and sibling research mentions. It
found no causal-graph overlay, append-only history entry, or prior exact YAML
review for this target.

## Evidence

| Record claim | Nearest source | Assessment |
|---|---|---|
| `source: BACDIVE`, `source_id: bacdive.isolation_source:spot-leaf,stem`, and `source_label: Spot-Leaf,Stem` | `data/raw/bacdive_isolation_sources.tsv:138` | Supported exactly. |
| `assertion_count: 5`, `assertion_unit: STRAIN`, and four source taxa | `data/raw/bacdive_isolation_sources.tsv:138`; `data/raw/bacdive_source_taxa.tsv:2611-2614` | Supported exactly. |
| Synonyms `Spot (Leaf,Stem)` and `Spot-Leaf,Stem` | The committed deep-research report records BacDive's hierarchical display form `#Spot (Leaf,Stem)`; the raw inventory carries `Spot-Leaf,Stem`. | Supported as source and display variants. |
| `grounding_status: UNGROUNDED` with no exact PO or ENVO target | `curation/decisions.tsv:1545` rejects the `PO:0009047` close match at item depth; `curation/term_requests.tsv:2` records `PO:0025034` `leaf`, `ENVO:01000170` `indeterminate root nodule infection zone`, and disease, trait, and clinical lesion terms as near misses or wrong entity types. | Supported. |
| Parent `ENVO:01001057` | `data/raw/ontology_terms.tsv:8552` plus `curation/term_requests.tsv:2` | Supported as the tightest vendored genus currently available. |
| Absence of environmental parameters, record-level evidence items, and causal graphs | Exact ignored/hidden-inclusive searches across `curation/`, `history/`, and the generated record corpus found no overlay or history for the target. | Supported. No curator-authored causal graph or habitat-specific environmental-parameter evidence exists for this source concept. |

The definition is narrow enough to avoid the two main scope traps called out in
the research report. It does not make the lesion bacterial-only just because
this BacDive record currently has bacterial strain assertions, and it does not
infer any pathovar or causality from the four reported taxa.

## Completeness

The generated record is complete for the maintained state: it preserves the
only raw BacDive source concept, retains the item-level `CONFIRM_UNGROUNDED`
decision, carries the curated `ADD` term request, uses the broad plant-part
environment parent while leaving the identity ungrounded, and omits unsupported
parameter, evidence-item, and causal-graph claims.

The one remaining curation gap is nonblocking and sits at the ENVO submission
layer. The committed deep-research report recommends first requesting a shared
parent such as `plant infection site environment` or `diseased plant tissue
environment` under `ENVO:01001057`, then placing `Spot-Leaf,Stem`,
`Rot-Root,Stem`, `Canker`, `Gall`, and `Wilt` as siblings under it. The current
term request instead asks for the leaf-or-stem spot lesion class directly under
`ENVO:01001057` while noting that the intermediate parent is missing.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| HM-SPOT-LEAF-STEM-001 | Minor | `curation/term_requests.tsv` requests `leaf-or-stem spot lesion environment` directly under `ENVO:01001057`. That is valid as an immediate fallback genus, but the record's own research report recommends adding a shared plant-infection-site parent first so multiple BacDive plant-infection children do not become flat, unrelated children of `environment associated with a plant part or small plant`. | `curation/term_requests.tsv`; generated `curation/term_requests/envo_robot_template.tsv` after `just term-requests` |

## Recommended Edits

1. Add a higher-priority ENVO term request for a shared plant-infection-site
   parent, using `ENVO:01001057` as its genus and explicitly citing the
   BacDive plant-infection cohort.
2. After that parent is accepted or locally modeled, update the
   `leaf-or-stem spot lesion environment` request so its requested parent is
   the shared infection-site term rather than the broad plant-part environment.

Do not hand-edit `data/habitats/other/spot_leaf_stem.yaml`; regenerate it from
the maintained TSVs after curation changes.

## Follow-up Checks

After changing the term-request hierarchy, rerun:

- `just term-requests`
- `just term-requests-check`
- `just seed`
- `just seed-canary habitatmech:BACDIVE.31392c69b9`
- inspect `data/habitats/other/spot_leaf_stem.yaml`
- `just validate-strict data/habitats/other/spot_leaf_stem.yaml`
- `just verify-corpus --max-diffs 1`
- `just report`

## Additional Notes

None.
