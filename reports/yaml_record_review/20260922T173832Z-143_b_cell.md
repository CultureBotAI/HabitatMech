# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/143_b_cell.yaml`
- Started UTC: 2026-09-22T17:38:32Z
- Finished UTC: 2026-09-22T17:38:47Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `BTO:0003167` |
| Label | `143-B cell` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the PREGO inventory plus the vendored ontology slice; generated YAML remains read-only |
| Locked slug | `data/habitats/PATHS.tsv:364` maps `BTO:0003167` to `143_b_cell` |

This is the generated BTO-grounded PREGO record for `143-B cell`, a human osteosarcoma fibroblast cell-line term. PREGO uses the BTO CURIE directly, so the seeder keeps `BTO:0003167` as the record identifier with `grounding_status: EXACT`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/143_b_cell.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/143_b_cell.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The exact identity, host-associated category, BTO label, BTO definition, ontology parent, path lock, PREGO attestation, and emitted taxon are supported by committed inputs:

- `data/habitats/PATHS.tsv:364` pins `BTO:0003167` to slug `143_b_cell`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:3167` gives `BTO:0003167` the ontology label `143-B cell`, the definition `Human fibroblast osteosarcoma cell line lacking the thymidine kinase (TK) gene.`, `is_directly_referenced: TRUE`, and `label_only: FALSE`.
- `data/raw/ontology_subclass_edges.tsv:2223` records `BTO:0003167 rdfs:subClassOf BTO:0000407`, which supports the generated `parent_habitats` entry.
- `data/raw/ontology_terms.tsv:409` labels parent `BTO:0000407` as `osteosarcoma cell line` and defines it as a cell line of a malignant neoplasm of bone-forming tissues.
- `data/raw/prego_habitats.tsv:636` records PREGO source id `BTO:0003167`, vocabulary `BTO`, Biolink type `GrossAnatomicalStructure`, one taxon assertion, one direct assertion, maximum PREGO score `3`, evidence channel `annotated_genomes_isolates`, and synonyms `143B cell|143BTK cell|143b|143b cell line|143b cell lines|143b cells`.
- `data/raw/prego_habitat_taxa.tsv:2770` is the only exact PREGO taxon row for `BTO:0003167`; it emits rank `1`, `NCBITaxon:1117958`, label `Pseudomonas extremaustralis 14-3 substr. 14-3b`, PREGO score `3`, `direct_flag: TRUE`, evidence channel `annotated_genomes_isolates`, and no corroborating source.

The deterministic PREGO source-concept key is `habitatmech:PREGO.72267be82f`, computed from `sha1("PREGO:BTO:0003167")[:10]`. An ignored/hidden-inclusive exact search found no item-level row for that key in `curation/decisions.tsv`, so `mapping_status: SEEDED` is expected. `habitat_category: HOST_ASSOCIATED` is also expected because BTO identifiers fall through `PREFIX_CATEGORY` to host-associated when no curator override is present.

## Evidence

Every generated scientific claim in this record is source-derived:

| Claim | Nearest source | Review |
|---|---|---|
| Record identifier, label, definition, and `definition_source: BTO` | `data/raw/ontology_terms.tsv:3167` | Supported exactly |
| BTO parent `BTO:0000407` | `data/raw/ontology_subclass_edges.tsv:2223` | Supported exactly |
| Locked filename | `data/habitats/PATHS.tsv:364` | Supported exactly |
| PREGO `source_id`, one-taxon assertion count, score `3.0`, and evidence channel | `data/raw/prego_habitats.tsv:636` | Supported exactly |
| PREGO `source_label` | `data/raw/ontology_terms.tsv:3167` | Supported exactly; PREGO rows carry ontology CURIEs, and `ingest_prego()` emits `source_label` from `store.ontology.label(prego_id)` |
| Related synonyms `143B cell`, `143BTK cell`, `143b`, `143b cell line`, `143b cell lines`, and `143b cells` | `data/raw/prego_habitats.tsv:636` | Supported exactly |
| The sole `characteristic_taxa` row for `NCBITaxon:1117958` | `data/raw/prego_habitat_taxa.tsv:2770` | Supported exactly |

The record has no xrefs, environmental parameters, record-level evidence, causal graphs, discussion links, datasets, or quality flags. Those absences match ignored/hidden-inclusive searches that found no maintained decision, term request, causal-graph overlay, history entry, target-specific research artifact, dataset row, or prior exact YAML review report for `BTO:0003167`, `143_b_cell`, `143-B cell`, `NCBITaxon:1117958`, or `habitatmech:PREGO.72267be82f`.

Unsupported or over-scoped claims: None found.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- The sole `source_attestations` entry captures the complete PREGO source id, source label, assertion count, `TAXON` unit, maximum score, and evidence channel.
- All six raw PREGO lexical variants are emitted as related synonyms.
- The one PREGO taxon row for `BTO:0003167` is emitted as the sole `characteristic_taxa` entry and retains its taxon id, taxon label, PREGO score, rank, source, and one-taxon candidate pool.
- The only broader BTO parent edge from the vendored ontology slice is emitted as `parent_habitats: [BTO:0000407]`.
- No authored definition or term request is expected because the record uses the existing exact BTO `143-B cell` term.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated target, excluding generated `build`, `data/text_map`, and `pages` trees where broad searches would otherwise hit generated artifacts. They found the expected PREGO habitat row, PREGO taxon row, ontology term rows, subclass edge, path lock, and generated YAML, and no target-specific maintained curation or research artifacts.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if future curation changes the PREGO rollup, BTO vendored terms, path lock, or characteristic-taxon emission:

- `just validate data/habitats/host_associated/143_b_cell.yaml`
- `just validate-strict data/habitats/host_associated/143_b_cell.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If future review redirects `BTO:0003167` away from this direct PREGO self-grounding, add an explicit `curation/decisions.tsv` row for `habitatmech:PREGO.72267be82f` first, regenerate with `just seed`, and verify that the PREGO attestation and rank-1 characteristic taxon remain attached to the surviving record.

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -name '*143_b_cell*' -print` found no pre-existing exact review report for this record before this file was written.
- `find curation/causal_graphs history research -name '*143_b_cell*' -print` found no target-specific causal overlay, history record, or research artifact before this file was written.
- Exact ignored/hidden-inclusive content searches for `BTO:0003167`, `143_b_cell`, `143-B cell`, `143B cell`, `143BTK`, `143b cell`, `NCBITaxon:1117958`, `BTO:0000407`, and `habitatmech:PREGO.72267be82f` found the path lock, generated YAML, raw PREGO rows, and vendored ontology rows cited above, and no target-specific maintained curation or research artifacts.
