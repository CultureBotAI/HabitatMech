# YAML Record Review

- Repository: HabitatMech
- Record: `data/habitats/aquatic/puddle.yaml`
- Started UTC: 2026-09-26T19:14:04Z
- Finished UTC: 2026-09-26T19:14:04Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Identifier | `habitatmech:GOLD.d250342fe1` |
| Label | `Puddle` |
| Class | `HabitatRecord` |
| Category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Source concept | GOLD `Environmental > Aquatic > Freshwater > Puddle` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/PATHS.tsv`, and `curation/decisions.tsv`; the YAML record is read-only |
| Locked slug | `data/habitats/PATHS.tsv:2841` maps `habitatmech:GOLD.d250342fe1` to `puddle` |

The target was selected from the current all-status worklist immediately after
the already-reviewed `Produced water/Flow back` row. The generated YAML
preserves the GOLD identity with the first collapsed source id
`gold.ecosystem:5746`, leaf label `Puddle`, source path
`Environmental > Aquatic > Freshwater > Puddle`, `assertion_count: 3`,
`assertion_unit: ORGANISM`, `mapping_status: SEEDED`, and one generated parent:
`ENVO:00002011` `fresh water`.

`data/raw/gold_ecosystem_paths.tsv:710` lists the same canonical path with two
collapsed GOLD nodes, `gold.ecosystem:5746|gold.ecosystem:5747`, and three GOLD
organism assertions. The side biosample table maps the exact path at node
`5747` to three biosamples.

## Validation

| Command | Result |
| --- | --- |
| `just worklist --limit 0 --status all --out /tmp/habitatmech-worklist.tsv` | Passed; wrote 953 ungrounded rows and listed `habitatmech:GOLD.d250342fe1` as the first worklist row after the already-reviewed `Produced water/Flow back` target. |
| `just report` | Passed; reported 3206 generated records, 953 `UNGROUNDED`, 830 class-level sweeps, and ranked `Puddle` among the remaining class-swept GOLD records. |
| `just validate data/habitats/aquatic/puddle.yaml` | Passed; LinkML reported no issues. |
| `just validate-strict data/habitats/aquatic/puddle.yaml` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| `just validate-causal-all` | Passed; validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Passed; the term-request table is current for 109 terms. |
| `just validate-history` | Passed; 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected and found 3206 generated records, with 0 missing, 0 extra, and 0 differing files. |
| `git diff --check` | Passed before this report was written. |

No validator was skipped.

## Identity and Grounding

The raw GOLD identity supports the generated source attestation exactly.
`data/raw/gold_ecosystem_paths.tsv:710` has the canonical path
`Environmental > Aquatic > Freshwater > Puddle`, leaf label `Puddle`, two
collapsed source nodes, and three organism assertions; the generated YAML
records the first source node, the exact path, the expected assertion count and
unit, and a collapsed-node note.

The current grounding row is only a class-level lexical sweep. The maintained
row at `curation/decisions.tsv:1160` is `CONFIRM_UNGROUNDED` with no object
term, no object label, no relation, `review_depth` `CLASS`, and the standard
sweep note saying no vendored term matched the label by the automated routes.
That row did not assess the source path or habitathood at item depth.

That class-level negative is stale for item review. The vendored ontology slice
contains `ENVO:01000871` `puddle of water`, defined as a small accumulation of
water on a surface, and places it under `ENVO:01000617` `lentic water body`.
The exact `Puddle` path has no direct MIxS triad row, but its child path
`Environmental > Aquatic > Freshwater > Puddle > Microbial mats` has a local
scale row selecting `ENVO:01000871` with share 1.00 in one study. That child
row is not automatic identity evidence for the parent, but it independently
confirms that submitter metadata used `puddle of water` for the GOLD Puddle
context.

The generated strict parent is also suspect. `ENVO:00002011` `fresh water` is
an environmental material defined by low dissolved-solute concentration. A
puddle is a small standing water body whose contents may be fresh water; ENVO
models `ENVO:01000871` under the water-body branch, not under `fresh water` or
`liquid water`. GOLD's `Freshwater` parent path therefore looks like contents
context rather than a strict is-a parent for this record.

## Evidence

| Claim | Evidence | Status |
| --- | --- | --- |
| The record denotes the GOLD path `Environmental > Aquatic > Freshwater > Puddle`. | `data/raw/gold_ecosystem_paths.tsv:710` lists the canonical path, leaf, two source nodes, and node ids `gold.ecosystem:5746|gold.ecosystem:5747`; the generated YAML repeats the first node and exact source path. | Supported exactly. |
| The source attestation has three GOLD organism assertions. | `data/raw/gold_ecosystem_paths.tsv:710` reports `organism_count` 3 and `total_assertions` 3; the generated YAML records `assertion_count: 3` with `assertion_unit: ORGANISM`. | Supported exactly. |
| `Puddle` has no nearby ontology term. | `data/raw/ontology_terms.tsv:8366` lists `ENVO:01000871` `puddle of water`; the GOLD child-path MIxS triad at `data/raw/gold_path_triads.tsv:372` uses that term for the `Puddle > Microbial mats` local scale. | Unsupported; item review must test that exact candidate. |
| `Puddle` is narrower than `ENVO:00002011` `fresh water`. | The source path places the GOLD node in a `Freshwater` bin, but the vendored ENVO rows define `fresh water` as a water material and `puddle of water` as a lentic water body. | Unsupported as a strict parent until item review decides the final identity and hierarchy. |
| The child `Puddle > Microbial mats` source path can remain narrower than this record. | `data/habitats/aquatic/microbial_mats__c3f0268a.yaml` lists `habitatmech:GOLD.d250342fe1` and `ENVO:01000008` `microbial mat` under `parent_habitats`, with an item-level `GROUND_AS_PARENT` decision for the microbial-mat part. | Supported as a generated source-path child link; if Puddle grounds to `ENVO:01000871`, the child should inherit the resolved ontology identifier. |

## Completeness

The generated YAML is complete for its current input state: it has the exact
GOLD source path, the collapsed-node note, one source-path parent, and generated
curation-history events for the class-level `CONFIRM_UNGROUNDED` row and the
source seed. Its empty definition, synonyms, xrefs, environmental parameters,
characteristic taxa, causal graphs, discussions, and datasets are expected for
a class-swept generated record that lacks an item-level term request or causal
overlay.

Ignored/hidden-inclusive exact searches for
`habitatmech:GOLD.d250342fe1`, `GOLD.d250342fe1`, `Puddle`, `puddle`,
`gold.ecosystem:5746`, `gold.ecosystem:5747`, and the exact GOLD path covered
`data/raw/`, `data/habitats/`, `curation/`, `history/`, `research/`, `conf/`,
`reports/habitat_research_manifest.tsv`, and `reports/yaml_record_review/`.
They found the generated target, its locked slug, its raw GOLD row, its
biosample side row, its study side row, the child `Puddle > Microbial mats`
path and triad rows, and no prior Puddle YAML review, no target-specific term
request or term-request exclusion, no history record, and no causal overlay.

The exact target has three direct GOLD organism assertions and one committed
biosample side-table row for node `5747`. The Puddle-context child path has one
biosample and a full MIxS triad backed by one study. That is enough local
evidence to reopen the target for item-level curation against
`ENVO:01000871`; it is not enough to decide whether the source should ground
exactly to that term, be minted as a freshwater-specific narrower concept, or
carry `puddle of water` as only a broader parent.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | `Puddle` remains at class-level `CONFIRM_UNGROUNDED` even though the vendored ontology contains `ENVO:01000871` `puddle of water`. | `curation/decisions.tsv:1160` has `review_depth` `CLASS`; `data/raw/ontology_terms.tsv:8366` defines `ENVO:01000871`; the child GOLD path's MIxS local-scale row also uses `ENVO:01000871` for the Puddle context. | `curation/decisions.tsv`; if the record stays minted, `curation/term_requests.tsv`. |
| Major | `Puddle` inherits `ENVO:00002011` `fresh water` as a strict parent even though the target denotes a water body rather than the fresh-water material itself. | `data/habitats/aquatic/puddle.yaml` lists `ENVO:00002011` under `parent_habitats`; the vendored ontology defines `ENVO:00002011` as water with low dissolved solute concentration and asserts `ENVO:01000871` `puddle of water` under `ENVO:01000617` `lentic water body`. | `curation/decisions.tsv`; if exact grounding keeps a false source-path parent, add maintained source-parent suppression or edge retyping in the seeder's authoritative inputs. |

No blockers or minor findings.

## Recommended Edits

1. Item-review `habitatmech:GOLD.d250342fe1` in
   `curation/decisions.tsv`. Inspect GOLD nodes `5746` and `5747`, the three
   organism assertions, the three node-5747 biosamples, and the exact-path
   study `Gs0114588`.
2. Test whether `ENVO:01000871` `puddle of water` exactly denotes GOLD
   `Environmental > Aquatic > Freshwater > Puddle`. If it does, replace the
   class-level row with an item-level `GROUND` decision to `ENVO:01000871`.
3. If `ENVO:01000871` is broader than the GOLD freshwater-scoped source
   concept, keep the source minted with a `GROUND_AS_PARENT` decision or a
   `CONFIRM_UNGROUNDED` xref as appropriate, then add an evidence-backed
   `curation/term_requests.tsv` definition.
4. Remove `ENVO:00002011` as an is-a parent for the final Puddle record unless
   item review can prove the GOLD source denotes the water material rather than
   the standing-water feature. For a minted record, use `parent_mode=REPLACE`
   in `curation/term_requests.tsv`; for exact grounding to `ENVO:01000871`,
   add a maintained way to suppress or retype the inherited `Freshwater`
   source-path edge.

## Follow-up Checks

After the item-level decision:

- Run an ignored/hidden-inclusive exact search for
  `habitatmech:GOLD.d250342fe1`, `ENVO:01000871`, `gold.ecosystem:5746`, and
  `gold.ecosystem:5747` across `curation`, `history`, `research`,
  `data/habitats`, `data/raw`, and `reports`.
- Run `just seed`.
- Run `just seed-canary habitatmech:GOLD.d250342fe1`; if the target grounds to
  `ENVO:01000871`, run `just seed-canary ENVO:01000871` after the grounding
  change.
- Inspect the regenerated target and
  `data/habitats/aquatic/microbial_mats__c3f0268a.yaml` to confirm the child
  source path points to the resolved Puddle parent.
- Run `just validate` on the regenerated target path.
- Run `just validate-strict` on the regenerated target path.
- Run `just validate-causal-all`.
- Run `just term-requests-check`.
- Run `just validate-history`.
- Run `just verify-corpus --max-diffs 1`.
- Run `git diff --check`.

## Additional Notes

None found.
