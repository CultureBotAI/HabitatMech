# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/host_associated/ear_discharge.yaml
- Started UTC: 2026-09-26T09:20:01Z
- Finished UTC: 2026-09-26T09:20:19Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.daebe0af3d` |
| Label | `Ear discharge` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Generated path | `data/habitats/host_associated/ear_discharge.yaml` |
| Maintained path lock | `data/habitats/PATHS.tsv:2914` maps `habitatmech:GOLD.daebe0af3d` to `ear_discharge` |
| Source concept | GOLD `gold.ecosystem:6305` |
| Source path | `Host-associated > Mammals: Human > Auditory/Hearing system > Outer ear > Ear discharge` |
| Grounding | `UNGROUNDED` |
| Mapping | `SEEDED` |

The reviewed file is generated from the GOLD source inventory and a
class-level curation decision. It should not be patched directly.

## Validation

| Command | Result |
|---|---|
| `just worklist --limit 0 --status all --out /tmp/habitatmech-worklist.tsv` | Passed; wrote 953 rows and placed `habitatmech:GOLD.daebe0af3d` immediately after the already reviewed `Crustaceans pond`. |
| `just validate data/habitats/host_associated/ear_discharge.yaml` | Passed with `No issues found`. |
| `just validate-strict data/habitats/host_associated/ear_discharge.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 expected records, 3206 found, 0 missing, 0 extra, 0 differing. |
| `just report` | Passed; no stale class-level sweep, contradictory path term, or non-habitat label contradiction was reported for this record. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

The generated identifier, label, category, source attestation, and assertion
count agree with the GOLD source inventory:

| Claim | Evidence | Assessment |
|---|---|---|
| The target is the GOLD human `Ear discharge` path. | `data/raw/gold_ecosystem_paths.tsv:680` records `Host-associated > Mammals: Human > Auditory/Hearing system > Outer ear > Ear discharge` with leaf label `Ear discharge`, depth 5, one GOLD node, four organism assertions, and source ID `gold.ecosystem:6305`. | Supported exactly. |
| The generated `source_attestations` block preserves the raw source ID, path, count, and unit. | The generated YAML carries `source: GOLD`, `source_id: gold.ecosystem:6305`, the same full source path, `assertion_count: 4`, and `assertion_unit: ORGANISM`. | Supported exactly. |
| The bare `Ear discharge` label is not unique in GOLD. | `data/raw/gold_ecosystem_paths.tsv:1988` records a second zero-assertion `Host-associated > Mammals > Auditory/Hearing system > Outer ear > Ear discharge` path with source ID `gold.ecosystem:6406`; `data/habitats/PATHS.tsv:3064` maps it to `ear_discharge__c59c71e2`. | Supported. The two generated records are separate and should be reviewed or merged by source concept, not by bare label alone. |
| The only generated parent is the human GOLD source-path parent `Outer ear`. | The target has `parent_habitats: habitatmech:GOLD.1821934761`; `data/habitats/PATHS.tsv:1454` maps that identifier to `outer_ear__1cde1a0e`, and `data/raw/gold_ecosystem_paths.tsv:851` records the parent path `Host-associated > Mammals: Human > Auditory/Hearing system > Outer ear`. | Supported as a GOLD path edge. Not supported as a strict `is_a` edge if this leaf denotes discharge fluid or exudate sampled from the outer ear. |
| The raw organism aggregate has no direct study, biosample, or MIxS triad rows for the exact human path. | Exact searches of `data/raw/gold_studies.tsv`, `data/raw/gold_path_biosamples.tsv`, and `data/raw/gold_path_triads.tsv` for the full human source path returned no rows; those side tables only mention the zero-assertion `Mammals` sibling. | Supported. The target's four assertions come only from the aggregate organism count on the collapsed GOLD ecosystem-path row. |
| `UNGROUNDED` is currently explained only by a class-level no-match sweep. | `curation/decisions.tsv:1210` has `CONFIRM_UNGROUNDED` with `review_depth` `CLASS`; the generated curation history repeats that no ontology term fit by any lexical route and that habitat status was not assessed. | Reproducible but incomplete. `CLASS` depth does not establish that the leaf denotes a habitat, that no exact term fits, or which broader material parent should replace the anatomical source-path parent. |

A bounded hidden/ignored-inclusive search of `data/raw/`, `curation/`,
`history/`, `research/`, `reports/yaml_record_review/`, `data/habitats/`, and
`pages/habitats/` for `habitatmech:GOLD.daebe0af3d`,
`gold.ecosystem:6305`, `ear_discharge`, `Ear discharge`, and the exact human
source path found the expected generated record, page, path lock, raw GOLD
aggregate row, class-level decision row, and same-label zero-assertion sibling.
It found no target-specific term request, generated term-request row, causal
overlay, append-only history entry, deep-research report, or prior exact YAML
review for this target.

## Evidence

| Record claim | Nearest source | Assessment |
|---|---|---|
| `source: GOLD`, `source_id: gold.ecosystem:6305`, `source_label: Ear discharge`, and the full source path | `data/raw/gold_ecosystem_paths.tsv:680` | Supported exactly. |
| `assertion_count: 4`, `assertion_unit: ORGANISM` | `data/raw/gold_ecosystem_paths.tsv:680` has `organism_count=4`, `study_count=0`, `biosample_count=0`, and `total_assertions=4`. | Supported; the count is a GOLD `ORGANISM` aggregate and is not study or biosample evidence. |
| Parent `habitatmech:GOLD.1821934761` | GOLD places the `Ear discharge` leaf below `Host-associated > Mammals: Human > Auditory/Hearing system > Outer ear`; `data/habitats/host_associated/outer_ear__1cde1a0e.yaml` is the generated record for that exact parent path. | Supported as source-path context. Potentially false as an `is_a` claim because a discharged fluid is contained by or derived from an outer ear rather than a subtype of that anatomy. |
| Absence of characteristic taxa, environmental parameters, record evidence, and causal graphs | Exact ignored/hidden-inclusive searches across `data/raw/`, `curation/causal_graphs/`, and the generated YAML found no target-specific taxa or curated overlay, and GOLD path aggregates do not emit inline characteristic-taxon claims. | Supported. |

The vendored ontology slice contains several nearby terms but no exact identity
for ear discharge or otorrhea. `UBERON:0036243` is `vaginal fluid`, matched only
through its `vaginal discharge` synonym, and is wrong for both anatomy and
material scope. The same-label `Mammals` sibling carries one raw MIxS medium
row to `ENVO:01000897` `electrostatic discharge process`, which is an upstream
homonym error and a process rather than the material this GOLD path names.
`BTO:0002100` `outer ear` and `UBERON:0001691` `external ear` denote the
anatomical site. `ENVO:02000028` `ear wax material` and `ENVO:02000038` `pus
material` are plausible components of ear discharge but over-specific as an
identity for the generic GOLD leaf. `ENVO:02000019` `bodily fluid material` and
`UBERON:0006314` `bodily fluid` are useful broader material candidates if item
review confirms the habitat reading.

## Completeness

The generated record preserves the existing raw GOLD aggregate row and
class-level curation decision and correctly avoids unsupported characteristic
taxa, environmental parameters, evidence items, and causal graph claims.

It is incomplete as an item-reviewed habitat. The GOLD path is the only
identity evidence for the nonzero human source concept; there are no exact-path
GOLD study, biosample, or triad rows, no committed research report, and no
definition that distinguishes ear discharge from the `Outer ear` anatomy,
cerumen, pus, vaginal discharge, and electrostatic discharge near misses. A
future curator needs to inspect the GOLD node and its four organism assertions
before deciding whether this leaf denotes discharge fluid from the outer ear,
the outer-ear sampling site, a clinical symptom name, or a bucket that should be
merged with the zero-assertion mammal sibling.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| HM-EAR-DISCHARGE-001 | Major | `Ear discharge` is still backed only by `review_depth` `CLASS`. The generated `UNGROUNDED` and `SEEDED` statuses are reproducible, but the maintained row explicitly says no one assessed whether the source concept is a habitat. The leaf label and GOLD path are consistent with a discharge-fluid material from the outer ear, but no item-level row confirms that identity, rejects the same-label zero-assertion sibling, or selects a true broader material parent. | `curation/decisions.tsv`; if the record is a real habitat, `curation/term_requests.tsv` |
| HM-EAR-DISCHARGE-002 | Major | The record inherits `Outer ear` as an `is_a` parent even though an ear discharge fluid is not a subtype of the external-ear anatomy. GOLD source-path inheritance preserves useful context, but `parent_habitats` requires a strictly broader habitat. | `curation/term_requests.tsv`, regenerated `data/habitats/host_associated/ear_discharge.yaml` |

## Recommended Edits

1. Item-review `habitatmech:GOLD.daebe0af3d` in
   `curation/decisions.tsv`, replacing the class-level row with an item-level
   decision.
2. Inspect GOLD node `gold.ecosystem:6305`, including the four organism
   assertions behind the aggregate row, to confirm whether the path denotes ear
   discharge material rather than the outer-ear site or a non-habitat clinical
   symptom.
3. Inspect the same-label zero-assertion sibling at `gold.ecosystem:6406` and
   decide whether it is the same habitat, an assertion-free parent-taxonomic
   duplicate, or a separate source concept that should remain split.
4. If the nonzero human path is a real discharge-material habitat, add an
   item-level `CONFIRM_UNGROUNDED` and define a novel `ear discharge material`
   term request under `ENVO:02000019` `bodily fluid material` or another
   inspected material genus. Use `parent_mode=REPLACE` only if the item review
   confirms that the inherited `Outer ear` source-path parent is contextual
   anatomy rather than a true `is_a` parent.
5. If the four assertions show this is merely the anatomical sampling site,
   ground the source concept to `BTO:0002100` `outer ear` or merge it into the
   generated outer-ear record instead of defining a new material class.
6. If the upstream node names a clinical symptom or otherwise non-habitat
   bucket rather than sampled material, replace the class-level row with an
   item-level `NOT_APPLICABLE`.

Do not hand-edit `data/habitats/host_associated/ear_discharge.yaml`;
regenerate it from maintained TSVs after curation changes.

## Follow-up Checks

After adding item-level curation, rerun:

- `just seed`
- `just seed-canary habitatmech:GOLD.daebe0af3d`
- inspect `data/habitats/host_associated/ear_discharge.yaml`
- `just validate-strict data/habitats/host_associated/ear_discharge.yaml`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just report`

If a causal overlay is added later, also run
`just validate-causal curation/causal_graphs/ear_discharge.yaml` and
`just validate-causal-all`.

## Additional Notes

None.
