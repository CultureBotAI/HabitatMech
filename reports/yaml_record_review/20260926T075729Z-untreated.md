# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/untreated.yaml
- Started UTC: 2026-09-26T07:53:58Z
- Finished UTC: 2026-09-26T07:57:29Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.3f6b823859` |
| Label | `Untreated` |
| Class | `HabitatRecord` |
| Category | `AQUATIC` |
| Generated path | `data/habitats/aquatic/untreated.yaml` |
| Maintained path lock | `data/habitats/PATHS.tsv:1765` maps `habitatmech:GOLD.3f6b823859` to `untreated` |
| Source concept | GOLD `gold.ecosystem:5857` |
| Source path | `Environmental > Aquatic > Freshwater > Drinking water > Untreated` |
| Grounding | `UNGROUNDED` |
| Mapping | `SEEDED` |

The reviewed file is a generated GOLD-only record. It is not a maintained
curation input and should not be patched directly.

## Validation

| Command | Result |
|---|---|
| `just worklist --limit 0 --status all --out /tmp/habitatmech-worklist.tsv` | Passed; wrote 953 rows and kept `habitatmech:GOLD.3f6b823859` as the next row after `Swine waste with corn`. |
| `just validate data/habitats/aquatic/untreated.yaml` | Passed with `No issues found`. |
| `just validate-strict data/habitats/aquatic/untreated.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 expected records, 3206 found, 0 missing, 0 extra, 0 differing. |
| `just report` | Passed; no stale class-level sweep, contradictory path term, or non-habitat label contradiction was reported for this record. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

The generated identifier, label, category, and source attestation agree with the
GOLD source inventory:

| Claim | Evidence | Assessment |
|---|---|---|
| The target is the GOLD `Untreated` leaf under drinking water. | `data/raw/gold_ecosystem_paths.tsv:635` gives the canonical path `Environmental > Aquatic > Freshwater > Drinking water > Untreated`, leaf label `Untreated`, depth `5`, and source node `gold.ecosystem:5857`. | Supported. |
| The GOLD row has five organism-level assertions. | `data/raw/gold_ecosystem_paths.tsv:635` has `organism_count=5`, `study_count=0`, `biosample_count=0`, and `total_assertions=5`. | Supported; the count is a GOLD `ORGANISM` aggregate and is not study or biosample evidence. |
| The only generated parent is the broader GOLD source-path parent `Drinking water`. | The target has `parent_habitats: habitatmech:GOLD.99a88ecb51`; `data/habitats/aquatic/drinking_water__6d6ef579.yaml` is the generated record for `Environmental > Aquatic > Freshwater > Drinking water`. | Supported. `Untreated` is a treatment-state child of drinking water, not a sibling duplicate of the broader parent. |
| `UNGROUNDED` is currently explained only by a class-level no-match sweep. | `curation/decisions.tsv:437` has `CONFIRM_UNGROUNDED` with `review_depth` `CLASS`; the generated curation history repeats that no ontology term fit by any lexical route and that habitat status was not assessed. | Reproducible but incomplete. `CLASS` depth does not establish that Untreated denotes a real untreated-drinking-water microbial habitat or that no manual broader parent or novel term should be attached. |

A bounded ignored/hidden-inclusive search of `data/raw/`, `curation/`,
`history/`, `research/`, `reports/yaml_record_review/`,
`data/habitats/PATHS.tsv`, `data/habitats/aquatic/`, and the generated target
page for `habitatmech:GOLD.3f6b823859`, `gold.ecosystem:5857`,
`Environmental > Aquatic > Freshwater > Drinking water > Untreated`,
`Untreated`, and `untreated` found the expected generated record, page, path
lock, raw GOLD aggregate row, and class-level decision row. It found no
target-specific term request, term-request exclusion, causal overlay,
append-only history entry, exact-path GOLD biosample, triad, or study row,
advisory sample row, or prior YAML review. Existing sibling-review mentions
only note that GOLD keeps `Untreated` as a separate child of the broader
drinking-water path beside `Chlorinated`, `Chloraminated`, `Unchlorinated`,
`Filtered water`, and `Delivery networks`.

## Evidence

| Record claim | Nearest source | Assessment |
|---|---|---|
| `source: GOLD`, `source_id: gold.ecosystem:5857`, `source_label: Untreated`, and the full GOLD path | `data/raw/gold_ecosystem_paths.tsv:635` | Supported exactly. |
| `assertion_count: 5`, `assertion_unit: ORGANISM` | `data/raw/gold_ecosystem_paths.tsv:635` records 5 organism assertions. | Supported exactly. |
| Parent `habitatmech:GOLD.99a88ecb51` | The GOLD path places `Untreated` below `Environmental > Aquatic > Freshwater > Drinking water`; `data/raw/gold_ecosystem_paths.tsv:124` records that parent path as `Drinking water`, with nodes `gold.ecosystem:3784|gold.ecosystem:4057`. | Supported as a source-path broader edge. |
| Absence of characteristic taxa, environmental parameters, record evidence, and causal graphs | Exact ignored/hidden-inclusive searches across `data/raw/`, `curation/causal_graphs/`, and the generated YAML found no `gold_path_biosamples.tsv`, `gold_path_triads.tsv`, or `gold_studies.tsv` rows for this path and no curated causal overlay. | Supported. GOLD organism aggregates are not characteristic-taxon claims, and no curated causal overlay exists. |

The generated `Drinking water` parent is itself still only seeded. It is
narrower than the vendored `ENVO:00002011` `fresh water` and `ENVO:00003064`
`drinking water` terms, both of which exist in `data/raw/ontology_terms.tsv`,
but exact ignored/hidden searches found no item-level decision or term request
for `habitatmech:GOLD.99a88ecb51`.

## Completeness

The generated record is structurally complete for the maintained inputs that
currently exist: it preserves the direct GOLD aggregate row, inherits a
supported broader GOLD source-path parent, and avoids unsupported optional
fields.

The consequential gap is curation depth. The only maintained decision is the
2026-08-12 class-level sweep; no committed row has inspected
`gold.ecosystem:5857` or the five organism assertions behind the aggregate to
decide whether the GOLD leaf denotes untreated drinking water as a real
microbial habitat and, if so, what novel term or parentage it should receive.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| HM-UNTREATED-001 | Major | `Untreated` is still backed only by `review_depth` `CLASS`. The maintained row says no vendored ontology term matched lexically and explicitly says no one assessed whether the concept is a habitat, so the generated `UNGROUNDED` and `SEEDED` statuses are reproducible but materially incomplete for a drinking-water treatment-state concept. | `curation/decisions.tsv`; if a novel term is requested, `curation/term_requests.tsv` |

## Recommended Edits

1. Item-review `habitatmech:GOLD.3f6b823859` in
   `curation/decisions.tsv`.
2. Inspect GOLD node `gold.ecosystem:5857`, including the five organism
   assertions behind the aggregate row, to confirm whether the leaf denotes
   untreated drinking water rather than the generic Drinking water parent.
3. If the source concept is true untreated drinking water with no exact ontology
   term, keep the minted identity, replace the class-level row with an
   item-level `CONFIRM_UNGROUNDED`, and consider a novel ENVO term under a
   strict drinking-water genus.
4. If the GOLD node duplicates the broader Drinking water source, consider a
   `SAME_AS` decision that merges it with `habitatmech:GOLD.99a88ecb51`. If it
   is instead a non-habitat bucket, use an item-level `NOT_APPLICABLE` decision
   that records that exact reading.

Do not hand-edit `data/habitats/aquatic/untreated.yaml`; regenerate it from the
maintained TSVs.

## Follow-up Checks

After adding item-level curation, rerun:

- `just seed`
- `just seed-canary habitatmech:GOLD.3f6b823859`
- inspect `data/habitats/aquatic/untreated.yaml`
- `just validate-strict data/habitats/aquatic/untreated.yaml`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just report`

If a causal overlay is added later, also run
`just validate-causal curation/causal_graphs/untreated.yaml` and
`just validate-causal-all`.

## Additional Notes

The adjacent `Chlorinated` and `Chloraminated` reviews found the same
class-depth curation gap on sibling GOLD leaves. Resolving this record should
keep those treatment-state siblings separate unless item review of the backing
GOLD nodes shows the upstream assertions overlap.
