# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/sludge__579ac3b2.yaml
- Started UTC: 2026-09-25T18:10:24Z
- Finished UTC: 2026-09-25T18:11:37Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.1032e10222` |
| Label | `Sludge` |
| Category | `ENGINEERED` |
| Source path | `Engineered > Bioreactor > DHS reactor > Anaerobic > Sludge` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv` and `data/habitats/PATHS.tsv`; future item-level review belongs in `curation/decisions.tsv`, and any retained minted definition or parent replacement belongs in `curation/term_requests.tsv`. |

The target is a GOLD-only, generated, `NARROW`, still-`SEEDED` record for the
zero-count sludge child under the anaerobic DHS reactor path. The seeder minted
a stable HabitatMech identifier, attached `ENVO:00002129` `anaerobic sludge`
through the ambiguous-leaf route, inherited `ENVO:00002124` `anaerobic
bioreactor` from the immediate GOLD parent path, and represented the one GOLD
ecosystem node with `source_id: gold.ecosystem:8431`.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/habitats/engineered/sludge__579ac3b2.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/sludge__579ac3b2.yaml` | Pass: scanned 1 file with 0 files containing `ERROR` and 0 total `ERROR` rows. |
| `just validate-causal curation/causal_graphs/sludge__579ac3b2.yaml` | Not applicable: the target has no `causal_graphs` block, and `find curation/causal_graphs -maxdepth 1 -type f -name 'sludge__579ac3b2.yaml' -print` found no target-specific overlay. |
| `just validate-causal-all` | Pass: validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Pass: the term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: expected 3,206 records, found 3,206 on disk, and the corpus reproduces exactly from `data/raw/`. |
| `just worklist --limit 2000 --status all --out /tmp/habitatmech-dhs-anaerobic-sludge-worklist.tsv` | Pass: reported 953 ungrounded records, 1,810 decisions on file, and wrote 953 rows to `/tmp/habitatmech-dhs-anaerobic-sludge-worklist.tsv`. |
| `just report` | Pass: completed the corpus report for 3,206 records. |
| Reference validator | Not applicable: this generated GOLD-only record has no record-level `evidence`, `characteristic_taxa`, `causal_graphs`, `discussions`, or `datasets` with citation-bearing `EvidenceItem` references. |
| `git diff --check` | Pass. |

## Identity and Grounding

The generated identifier, label, category, source attestation, and slug agree
with the raw GOLD inventory. `data/raw/gold_ecosystem_paths.tsv:1178` has the
exact path `Engineered > Bioreactor > DHS reactor > Anaerobic > Sludge` with
`gold_node_count=1`, no direct organism, study, biosample, or total assertions,
and `gold_node_ids=gold.ecosystem:8431`. `data/habitats/PATHS.tsv:1398` maps
`habitatmech:GOLD.1032e10222` to the stable `sludge__579ac3b2` slug.

The generated `ENVO:00002129` parent is supported and appropriately broader.
`data/raw/ontology_terms.tsv:7225` labels that term `anaerobic sludge`, and
`data/raw/ontology_subclass_edges.tsv:5329` places it under `ENVO:00002044`
`sludge`, the residual semi-solid material left from domestic, industrial, or
wastewater-treatment processes. The target is source-specific DHS anaerobic
reactor sludge, so generic anaerobic sludge is broader rather than exact.

The generated `ENVO:00002124` parent is not supported as a strict broader
habitat. The target leaf names sludge material in the
`Engineered > Bioreactor > DHS reactor > Anaerobic` context; that material can
be contained by an anaerobic DHS reactor, but it is not a subtype of the
bioreactor itself. The immediate GOLD parent currently resolves to generic
`ENVO:00002124` because `curation/decisions.tsv` exact-grounded
`habitatmech:GOLD.7b1df1e18d` to `anaerobic bioreactor`; the prior review of
`data/habitats/engineered/anaerobic_bioreactor.yaml` found that DHS-specific
source exact grounding is itself too broad.

No item-level curation row exists for `habitatmech:GOLD.1032e10222`; the target
has only its generated seed event. That means the `NARROW` state is explainable
from the seeder's ambiguous-leaf and source-parent routes, but it has not been
endorsed by a curator.

## Evidence

The sole `GOLD` source attestation mirrors
`data/raw/gold_ecosystem_paths.tsv:1178`: source ID `gold.ecosystem:8431`,
source label `Sludge`, source path
`Engineered > Bioreactor > DHS reactor > Anaerobic > Sludge`, and
`mapping_predicate: skos:narrowMatch`.

Exact gitignore-independent searches for the target source path in
`data/raw/gold_path_triads.tsv`, `data/raw/environment_parameters.tsv`,
`data/raw/gold_studies.tsv`, and `data/raw/gold_path_biosamples.tsv` found no
target-specific side-table rows.

Unsupported or over-scoped generated claims:

- `ENVO:00002124` as a `parent_habitats` entry is an over-scoped
  source-context edge. `ENVO:00002124` names an anaerobic bioreactor, not
  sludge material contained by an anaerobic DHS reactor.

## Completeness

The generated record carries the expected source-owned values for the current
raw inputs: a minted identifier, GOLD label, `ENGINEERED` category,
ambiguous-leaf `anaerobic sludge` parent, inherited `anaerobic bioreactor`
parent, a single GOLD source attestation, a seed event, and a stable slug lock.

The curation state is incomplete. Exact `rg --no-ignore --hidden` searches for
`habitatmech:GOLD.1032e10222`, `gold.ecosystem:8431`, the target source path,
and `sludge__579ac3b2` under the maintained source and review areas found the
generated YAML record, the raw GOLD row, and the slug-lock row, but no
item-level decision, `curation/term_requests.tsv` definition, target-specific
history, research report, maintained causal overlay, or prior slug-named YAML
review report. Exact `find` searches found no target-specific causal overlay
and no prior slug-named YAML review report. Ignored files were included in all
of those absence checks.

Optional characteristic-taxon, environmental-parameter, DOI/PMID/URL evidence,
dataset, discussion, and causal-graph slots are correctly absent for this GOLD
child in the generated record. The direct GOLD path contributes no organism,
study, biosample, triad, or environmental-parameter rows that could populate
those slots for `sludge__579ac3b2`.

## Findings

- **major**: `habitatmech:GOLD.1032e10222` has no item-level review, so its
  `NARROW` status and source-specific DHS anaerobic-sludge identity remain
  seeded rather than curated.

  **Evidence**: The target GOLD row is a distinct zero-count `Sludge` child
  under `Engineered > Bioreactor > DHS reactor > Anaerobic`, while the exact
  `ENVO:00002129` `anaerobic sludge` identity is already claimed by the generic
  `Engineered > Bioreactor > Anaerobic > Sludge` ontology record. Exact
  gitignore-independent searches found no target-specific row in
  `curation/decisions.tsv`, no `curation/term_requests.tsv` definition, and no
  target-specific history or research report.

  **Owner**: `curation/decisions.tsv` should hold the item-level decision; if
  the item-level review keeps the minted identifier,
  `curation/term_requests.tsv` should hold its definition and strict parent.

- **major**: `parent_habitats` asserts that this DHS anaerobic-reactor sludge
  is a kind of `ENVO:00002124` `anaerobic bioreactor`.

  **Evidence**: The inherited parent comes from GOLD's `Engineered >
  Bioreactor > DHS reactor > Anaerobic` path segment. `ENVO:00002124` names a
  bioreactor whose contained material is not oxygenated, while this child leaf
  names sludge material in the anaerobic-DHS-reactor context. The record has no
  item-level decision, term request, MIxS triad, causal overlay, or source
  side-table row saying the sludge is itself a reactor.

  **Owner**: `curation/term_requests.tsv` should replace the generated
  source-path parent with `ENVO:00002129` or a tighter inspected material genus
  if the item-level decision confirms the target is a real minted habitat.

No blockers found.

No minor findings.

## Recommended Edits

1. In `curation/decisions.tsv`, add an item-level review for
   `habitatmech:GOLD.1032e10222` scoped to
   `Engineered > Bioreactor > DHS reactor > Anaerobic > Sludge`, the broader
   `ENVO:00002129` `anaerobic sludge` parent, and the contextual
   `ENVO:00002124` `anaerobic bioreactor` edge.

2. If item review keeps this as source-specific DHS anaerobic-reactor sludge,
   define it in `curation/term_requests.tsv` under `ENVO:00002129` or a tighter
   inspected material genus, and use `parent_mode=REPLACE` if the review
   confirms that `ENVO:00002124` is contextual rather than strict.

3. Regenerate the corpus from those maintained inputs so the generated record
   records the curator decision and no longer carries a false
   anaerobic-bioreactor `is-a` edge.

## Follow-up Checks

After curation, rerun:

- `just term-requests-check`
- `just validate data/habitats/engineered/sludge__579ac3b2.yaml`
- `just validate-strict data/habitats/engineered/sludge__579ac3b2.yaml`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000 --status all --out /tmp/habitatmech-dhs-anaerobic-sludge-worklist.tsv`

## Additional Notes

- The false `ENVO:00002124` edge is downstream of the
  `habitatmech:GOLD.7b1df1e18d` `Engineered > Bioreactor > DHS reactor >
  Anaerobic` exact grounding. Correcting that parent concept from exact to
  source-specific narrow grounding would preserve `ENVO:00002124` as a true
  parent of an anaerobic DHS reactor record, but it would not by itself remove
  `ENVO:00002124` from this sludge material child.
- This target follows the same contextual-parent pattern already observed for
  zero-count anaerobic-bioreactor and DHS-aerobic-bioreactor material leaves:
  source-path reactor context becomes a false parent when the leaf is a
  material rather than the reactor device.
