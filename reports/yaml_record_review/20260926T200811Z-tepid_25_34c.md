# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/tepid_25_34c.yaml
- Started UTC: 2026-09-26T20:00:00Z
- Finished UTC: 2026-09-26T20:08:16Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/tepid_25_34c.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.12aa763db5` |
| Label | `Tepid (25-34C)` |
| Category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Generated or maintained | Generated from `data/raw/` plus `curation/decisions.tsv`; do not hand-edit |

The filename is pinned by `data/habitats/PATHS.tsv`, whose exact target row is:

```text
habitatmech:GOLD.12aa763db5	tepid_25_34c
```

The single source concept is maintained in `curation/decisions.tsv` as a
class-level sweep row:

| Field | Value |
|---|---|
| source ID | `habitatmech:GOLD.12aa763db5` |
| decision | `CONFIRM_UNGROUNDED` |
| curator/date | `claude-opus-5` / `2026-08-12` |
| review depth | `CLASS` |
| target ID, target label, predicate, relation | empty |
| note | Class-level sweep; no term in the vendored slice matched this label by any search route, but whether the concept is a habitat at all was not assessed. |

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/aquatic/tepid_25_34c.yaml` | PASS; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/tepid_25_34c.yaml` | PASS; one file scanned, zero files with errors, zero total error rows. |
| `just validate-causal-all` | PASS; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | PASS; the term-request table is current at 109 terms. |
| `just validate-history` | PASS; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | PASS; 3,206 expected records, zero missing, zero extra, zero differing. |
| `git diff --check` | PASS; no whitespace errors. |

No validator was skipped.

## Identity and Grounding

The generated record matches the GOLD source path mechanically:

- `data/raw/gold_ecosystem_paths.tsv` row 721 has exact canonical path
  `Environmental > Aquatic > Thermal springs > Tepid (25-34C)`, leaf label
  `Tepid (25-34C)`, two GOLD node IDs
  `gold.ecosystem:3760|gold.ecosystem:3989`, three organism assertions, zero
  study assertions, zero biosample assertions, and three total assertions.
- The generated `source_attestations[0]` keeps that full GOLD path, records
  `source_id: gold.ecosystem:3760`, reports `assertion_count: 3` with
  `assertion_unit: ORGANISM`, and explicitly notes that two GOLD ecosystem
  node IDs share the path.
- `parent_habitats` contains `ENVO:00000051`, whose vendored label is
  `hot spring` and whose definition is "A spring through which groundwater,
  heated by geothermal energy, flows."; this is the correct broader
  thermal-spring setting for the source path.

The record's source concept is not a habitat identity. In the GOLD path,
`Tepid (25-34C)` is an ecosystem-subtype slot beneath
`Environmental > Aquatic > Thermal springs`, and its own label is only a
temperature band qualifying the parent hot spring. The analogous sibling
records already have item-level `NOT_APPLICABLE` decisions:

| Identifier | Label | Path | Decision |
|---|---|---|---|
| `habitatmech:GOLD.c085f6141a` | `Hot (42-90C)` | `Environmental > Aquatic > Thermal springs > Hot (42-90C)` | `NOT_APPLICABLE`, `ITEM`; "a temperature band qualifying the thermal spring above it, not a habitat in its own right" |
| `habitatmech:GOLD.486b52b120` | `Warm (34-42C)` | `Environmental > Aquatic > Thermal springs > Warm (34-42C)` | `NOT_APPLICABLE`, `ITEM`; "A temperature band qualifying the parent node, not a habitat" |
| `habitatmech:GOLD.8b53067bc5` | `Near-boiling (>90C)` | `Environmental > Aquatic > Thermal springs > Near-boiling (>90C)` | `NOT_APPLICABLE`, `ITEM`; "A temperature band qualifying the parent node, not a habitat" |

The target's current `CONFIRM_UNGROUNDED` row is explicitly only a class-level
negative lexical sweep. It checked that no vendored ontology term matched the
label by the sweep routes; it did not establish that the source concept is a
microbial habitat.

## Evidence

Supported:

- GOLD provenance, path, and assertion count are consistent between
  `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/PATHS.tsv`, and the
  generated YAML.
- `ENVO:00000051` is a valid broader parent because the target is a thermal
  spring qualifier.
- The generated `mapping_status: SEEDED` is correct because the only decision
  row for `habitatmech:GOLD.12aa763db5` is `review_depth: CLASS`, not `ITEM`.

Unsupported or over-scoped:

- The class-level `CONFIRM_UNGROUNDED` curation event overstates the result by
  publishing `Tepid (25-34C)` as a real but ungrounded habitat. The full GOLD
  path and the locally curated handling of `Hot`, `Warm`, and `Near-boiling`
  show that this node is a temperature-band qualifier.

The GOLD environmental triad inventory confirms that submitters used
environmental terms such as `ENVO:00002030` aquatic biome, `ENVO:00000051` hot
spring, and `ENVO:03600065` spring water for samples on this path. Those triads
provide context for the spring setting, but none of them makes `Tepid
(25-34C)` an exact habitat identity.

## Completeness

This generated record correctly has no definition, exact xrefs, environmental
parameters, characteristic taxa, literature evidence, causal graphs,
discussions, or datasets. A temperature band should not receive a minted-term
definition or causal graph as though it were a habitat.

An ignored/hidden-inclusive search of `reports/yaml_record_review/`,
`history/`, `research/`, `curation/term_requests.tsv`, and
`curation/causal_graphs/` for `habitatmech:GOLD.12aa763db5`,
`tepid_25_34c`, and `Tepid (25-34C)` found no prior target-specific YAML
review, curation-history record, habitat research report, term request, or
causal overlay for this record.

## Findings

### Blocker

| ID | Finding | Maintained owner |
|---|---|---|
| B1 | `Tepid (25-34C)` is still published as a class-level `CONFIRM_UNGROUNDED` habitat even though its GOLD path denotes a temperature band qualifying `Thermal springs`. The sibling GOLD thermal-spring bands `Hot (42-90C)`, `Warm (34-42C)`, and `Near-boiling (>90C)` are already item-reviewed as `NOT_APPLICABLE` for the same reason. | `curation/decisions.tsv` |

### Major

None found.

### Minor

None found.

## Recommended Edits

1. In `curation/decisions.tsv`, replace the row for
   `habitatmech:GOLD.12aa763db5` with an item-level `NOT_APPLICABLE` decision.
   Leave the target ID, target label, mapping predicate, and relation empty.
   The note should state that `Tepid (25-34C)` is a temperature band qualifying
   `Environmental > Aquatic > Thermal springs`, consistent with the reviewed
   `Hot`, `Warm`, and `Near-boiling` sibling records.
2. Regenerate this record from maintained inputs:

```bash
just seed
just seed-canary habitatmech:GOLD.12aa763db5
just seed-apply --force
```

3. Add an append-only history record for the curation session under `history/`.

## Follow-up Checks

After the future curation edit:

1. Re-read `data/habitats/aquatic/tepid_25_34c.yaml` and confirm
   `grounding_status: NOT_APPLICABLE`, `mapping_status: REVIEWED`, and a
   generated `NOT_APPLICABLE` curation event for
   `habitatmech:GOLD.12aa763db5`.
2. Confirm `source_attestations[0]` still points at
   `Environmental > Aquatic > Thermal springs > Tepid (25-34C)` with three
   GOLD organism assertions.
3. Run the narrow validators:

```bash
just validate data/habitats/aquatic/tepid_25_34c.yaml
just validate-strict data/habitats/aquatic/tepid_25_34c.yaml
just validate-history
just verify-corpus --max-diffs 1
git diff --check
```

4. Run the full corpus gate before PR:

```bash
just qc
```

## Additional Notes

- The child source path
  `Environmental > Aquatic > Thermal springs > Tepid (25-34C) > Sediment`
  resolves to `habitatmech:GOLD.c43e79fda5` in
  `data/habitats/aquatic/sediment__6e0c4228.yaml`. That is a distinct sediment
  habitat whose `Tepid (25-34C)` context can remain part of its GOLD provenance;
  this review only covers the parent temperature-band record.
- This was a read-only review. It did not edit generated YAML, generated pages,
  source inventories, decision rows, term requests, causal overlays, or
  history records.
