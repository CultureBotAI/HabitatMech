# YAML Record Review: anoxic water

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/anoxic_water.yaml
- Started UTC: 2026-09-21T14:45:42Z
- Finished UTC: 2026-09-21T14:45:42Z
- Verdict: pass

## Target

The target is the generated `HabitatRecord` at `data/habitats/aquatic/anoxic_water.yaml`.

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `ENVO:01000173` |
| Label | `anoxic water` |
| Category | `AQUATIC` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| PREGO source concept | `habitatmech:PREGO.a03ff2e2c0` |
| PREGO source ID | `ENVO:01000173` |
| Locked stem | `anoxic_water` in `data/habitats/PATHS.tsv` |

This review covers the single PREGO source concept that self-grounds to vendored ENVO term `ENVO:01000173`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/anoxic_water.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/anoxic_water.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 records on disk, 0 missing, 0 extra, 0 differing. |
| Reference validator | Not checked: this record has no DOI/PMID/URL `EvidenceItem`, no causal-edge evidence, and no target-specific `curation/causal_graphs/` overlay. |

## Identity and Grounding

The record denotes anoxic water. PREGO attests `ENVO:01000173` with label `anoxic water`, and the vendored ENVO row for `ENVO:01000173` has label `anoxic water` and definition `Water which lacks dissolved oxygen.`, so the generated identifier, label, definition text, and `definition_source: ENVO` agree.

The synonym set is also source-supported: `anaerobic water` and `reducing water` come from the vendored ENVO synonym column, while PREGO contributes the alternate label `anoxic waters`.

The ontology parent is direct and strictly broader. `data/raw/ontology_subclass_edges.tsv` contains `ENVO:01000173 rdfs:subClassOf ENVO:00002006`, and `data/raw/ontology_terms.tsv` labels `ENVO:00002006` as `liquid water`.

`mapping_status: SEEDED` is correct because the source concept key `habitatmech:PREGO.a03ff2e2c0` has no item-level row in `curation/decisions.tsv`. Exact hidden/ignored-inclusive searches before this report was created covered the maintained `curation/`, `history/`, and `data/raw/` surfaces plus `data/habitats/PATHS.tsv`, `research/habitats/`, and prior `reports/yaml_record_review/` reports; they found the maintained PREGO, ontology, GOLD-triad, and PATHS rows cited here plus one contextual deep-research mention for a different engineered habitat, but no target-specific decision row, term request, causal overlay, history record, prior YAML review, or research report for `ENVO:01000173`, `anoxic_water`, `anoxic water`, `anoxic waters`, or `habitatmech:PREGO.a03ff2e2c0`.

## Evidence

| Claim | Evidence | Assessment |
|---|---|---|
| PREGO attests `ENVO:01000173` as `anoxic water`. | `data/raw/prego_habitats.tsv` has the row `ENVO:01000173	ENVO	biolink:OntologyClass	24	24	1.52414	environmental_samples	anoxic water\|anoxic waters`. | Supported. |
| The generated label and definition match the vendored ENVO identity. | `data/raw/ontology_terms.tsv` contains `ENVO:01000173` with label `anoxic water`, definition `Water which lacks dissolved oxygen.`, and ENVO synonyms `anaerobic water` and `reducing water`. | Supported. |
| PREGO contributes `anoxic waters` as a related synonym. | The PREGO label set for `ENVO:01000173` is `anoxic water\|anoxic waters`; the canonical label is already the record label, leaving `anoxic waters` as the source-specific synonym. | Supported. |
| The ontology parent `ENVO:00002006` is a direct superclass of anoxic water. | `data/raw/ontology_subclass_edges.tsv` contains `ENVO:01000173 rdfs:subClassOf ENVO:00002006`; `data/raw/ontology_terms.tsv` labels `ENVO:00002006` as `liquid water`. | Supported. |
| `data/habitats/aquatic/anoxic_water.yaml` is the stable file path for this record. | `data/habitats/PATHS.tsv` maps `ENVO:01000173` to `anoxic_water`. | Supported. |
| The source attestation count is 24 PREGO taxon associations. | `data/raw/prego_habitats.tsv` lists `taxon_count=24`, `association_count=24`, `max_score=1.52414`, and `evidence_channels=environmental_samples`; `data/raw/prego_habitat_taxa.tsv` has 24 rows for `ENVO:01000173`. | Supported. |
| The generated taxon set preserves the current PREGO associations. | The 24 `NCBITaxon` IDs, ranks, scores, labels, and `candidate_pool: 24` values in the target follow the 24 PREGO rows; `NCBITaxon:78178` correctly has no generated `taxon_label` because the PREGO label cell is blank. | Supported. |
| Existing target-specific causal evidence belongs on this record. | Exact hidden/ignored-inclusive searches of the maintained curation surfaces, `history`, raw inputs, and prior review reports found no target-specific overlay, term request, prior review, or maintained decision for `anoxic_water`, `ENVO:01000173`, or `habitatmech:PREGO.a03ff2e2c0` before this report was created. | Not supported by current maintained inputs. |

## Completeness

The generated record preserves every current PREGO fact for this target: the ENVO source ID, the source label, the 24-taxon assertion count, the `TAXON` unit, the maximum PREGO score, the `environmental_samples` channel summary, all 24 ranks and scores, and the blank taxon label where PREGO omits one.

The empty `environmental_parameters` list is appropriate for the current maintained inputs. Exact hidden/ignored-inclusive searches of `data/raw/environment_parameters.tsv` found no single-term or multi-term row that mentions `ENVO:01000173`, `anoxic_water`, `anoxic water`, or `anoxic waters`.

Empty `evidence`, `causal_graphs`, `discussions`, and `datasets` are also appropriate for this seeded PREGO-only record. No maintained input currently supplies those fields, and no target-specific causal overlay exists.

## Findings

None found.

## Recommended Edits

None found.

## Follow-up Checks

None found.

## Additional Notes

`data/raw/gold_path_triads.tsv` also mentions `ENVO:01000173` as the medium in `Environmental > Aquatic > Freshwater > Lake > Anoxic zone`. That row supports the presence of `anoxic water` in the generated `Anoxic zone` GOLD context; it is not a source attestation for this PREGO anoxic-water record.

A deep-research report for `dissolved organics anaerobic` mentions `ENVO:01000173` only as a false exact match for that engineered habitat. It does not supply target-specific curation evidence for this record.

If this PREGO source concept is ever promoted to item-reviewed, the maintained decision should key on `habitatmech:PREGO.a03ff2e2c0`.
