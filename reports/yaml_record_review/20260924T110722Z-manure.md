# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/manure.yaml
- Started UTC: 2026-09-24T11:07:22Z
- Finished UTC: 2026-09-24T11:07:22Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `HabitatRecord` |
| Identifier | `ENVO:03501300` |
| Label | `manure` |
| Category | `ENGINEERED` |
| Source concept | `habitatmech:GOLD.21f78f18dd` |
| Source path | `Engineered > Solid waste > Animal waste > Manure slurry > Liquid manure` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv`; future curation belongs in `curation/decisions.tsv` or in a maintained parent-override input, not in `data/habitats/engineered/manure.yaml`. |

The generated record grounds GOLD's `Liquid manure` leaf to ENVO's broader `manure` term with `grounding_status: CLOSE` and `mapping_predicate: skos:closeMatch`. The GOLD canonical path row is `gold.ecosystem:8076` and has no direct GOLD organism, study, or biosample assertions in `data/raw/gold_ecosystem_paths.tsv`.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/habitats/engineered/manure.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/manure.yaml` | Pass: scanned 1 file with 0 files containing `ERROR` and 0 total `ERROR` rows. |
| `just validate-causal curation/causal_graphs/<overlay>.yaml` | Not applicable: a gitignore-independent `rg --no-ignore --hidden` search for `ENVO:03501300`, `GOLD.21f78f18dd`, `gold.ecosystem:8076`, and `manure` under `curation/causal_graphs`, `curation/term_requests.tsv`, `history`, and `research` found no curated causal overlay for this record, and the target has no `causal_graphs` block. |
| `just validate-causal-all` | Pass: validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Pass: the term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: expected 3,206 records, found 3,206 on disk, and the corpus reproduces exactly from `data/raw/`. |
| `just worklist --limit 2000` | Pass: reported 0 undecided ungrounded records and 1,810 decisions on file. The first sandboxed run failed because `uv` could not open `/Users/marcin/.cache/uv/sdists-v9/.git`; the same command passed outside the sandbox. |
| `just report` | Pass: completed the corpus report for 3,206 records. The first sandboxed run failed because `uv` could not open `/Users/marcin/.cache/uv/sdists-v9/.git`; the same command passed outside the sandbox. |
| `git diff --check` | Pass before writing this report. |
| Reference validator | Not applicable: this record has no record-level `evidence`, `characteristic_taxa`, `causal_graphs`, `discussions`, or `datasets` with citation-bearing `EvidenceItem` references. |

## Identity and Grounding

`ENVO:03501300` is present in `data/raw/ontology_terms.tsv` as `manure` with definition `An environmental material consisting of organic matter from plants and/or animals that is used in agriculture as fertilizer.` and exact synonyms `animal manure`, `compost`, `farm slurry`, `green manure`, and `liquid manure`. Its ontology parent is supported: `data/raw/ontology_subclass_edges.tsv` asserts `ENVO:03501300 rdfs:subClassOf ENVO:00010483`, and `data/raw/ontology_terms.tsv` labels that parent `environmental material`.

The GOLD source path `Engineered > Solid waste > Animal waste > Manure slurry > Liquid manure` maps to `gold.ecosystem:8076` with no direct organism assertions on the canonical row. The item-level row for `habitatmech:GOLD.21f78f18dd` in `curation/decisions.tsv` explicitly endorses the seeder's `CLOSE` result rather than promoting the source concept to exact identity, so the generated `grounding_status: CLOSE`, `mapping_predicate: skos:closeMatch`, and `mapping_status: REVIEWED` agree with the maintained curation input.

The repeated `Manure` and `Fresh manure` GOLD siblings under `Engineered > Solid waste > Animal waste` remain separate minted records. That is consistent with the anti-conflation rule: source concepts narrower than the generic ENVO `manure` identity keep minted identifiers and can still preserve `ENVO:03501300` or `ENVO:00002276` as broader parents.

The source-derived parent `habitatmech:GOLD.18ee4196a9`, however, is GOLD's `Engineered > Solid waste > Animal waste > Manure slurry` path. `Manure slurry` is broader than the GOLD `Liquid manure` leaf, but it is not broader than the generated ENVO `manure` record as a whole. ENVO `manure` covers animal manure, compost, green manure, and liquid manure, while GOLD keeps `Manure`, `Fresh manure`, and `Manure slurry` as siblings under `Animal waste`. The current generated `parent_habitats` edge therefore turns a source-tree placement for a narrower source concept into an over-scoped `is-a` parent on a broader ENVO term.

## Evidence

The ENVO label, definition, exact synonyms, and `environmental material` parent trace to committed ontology inventories: `data/raw/ontology_terms.tsv` and `data/raw/ontology_subclass_edges.tsv`.

The GOLD attestation traces to `data/raw/gold_ecosystem_paths.tsv`, where `Engineered > Solid waste > Animal waste > Manure slurry > Liquid manure` has `gold_node_count=1`, all assertion counts are 0, and `gold_node_ids=gold.ecosystem:8076`. The generated source attestation agrees with that row by carrying `source: GOLD`, `source_id: gold.ecosystem:8076`, `source_label: Liquid manure`, and `source_path: Engineered > Solid waste > Animal waste > Manure slurry > Liquid manure`, with no `assertion_count`.

The item-level curation event traces to `curation/decisions.tsv`: source concept `habitatmech:GOLD.21f78f18dd` has decision `REVIEW`, curator `claude-opus-5`, date `2026-08-16`, `review_depth: ITEM`, and notes naming the same close-grounding review shown in the generated record's curation history.

The GOLD biosample side channel has three biosamples for path ID `8076`, all from one study, and the MIxS triad side channel annotates one sample from that study as `broad = ENVO:01000219 anthropogenic terrestrial biome`, `local = ENVO:01001872 manure pit`, and `medium = ENVO:03501300 manure`. Those rows support preserving `manure` as the material identity for the GOLD source path, but one study's medium annotation does not provide mechanism evidence or make `manure pit` the habitat identity.

## Completeness

- A gitignore-independent exact search for `ENVO:03501300`, `manure`, `GOLD.21f78f18dd`, `GOLD.18ee4196a9`, `gold.ecosystem:8076`, and `Engineered > Solid waste > Animal waste > Manure slurry > Liquid manure` across `data/raw`, `curation`, and `data/habitats/PATHS.tsv` found the expected path-lockfile rows, the GOLD source rows, the `REVIEW` decision, the class-level `Manure slurry` decision, the sibling GOLD `Manure` and `Fresh manure` records, and the ENVO ontology rows.
- A gitignore-independent exact search for `ENVO:03501300`, `GOLD.21f78f18dd`, `gold.ecosystem:8076`, and `manure` across `curation/causal_graphs`, `curation/term_requests.tsv`, `history`, and `research` found no term request, curated causal overlay, or dedicated history record for this target. The research hits were unrelated host-associated reports that mention manure only as an environmental-material near miss.
- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-manure.md' -print` found no prior top-level review report for the same record stem; `find` includes ignored files.
- Optional `environmental_parameters`, `characteristic_taxa`, `evidence`, `causal_graphs`, `discussions`, and `datasets` are correctly absent. The generated record contains only source-vocabulary provenance and ontology-owned definition data, and no maintained overlay adds claim-level mechanism evidence.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | `parent_habitats` contains `habitatmech:GOLD.18ee4196a9` `Manure slurry`, but `Manure slurry` is not a strictly broader habitat than `ENVO:03501300` `manure`. | The extra parent comes from GOLD placing the narrower `Liquid manure` leaf below `Manure slurry`. The generated record has resolved that leaf to ENVO's broader `manure` term, whose exact synonyms span `animal manure`, `compost`, `farm slurry`, `green manure`, and `liquid manure`; GOLD also keeps `Manure`, `Fresh manure`, and `Manure slurry` as sibling animal-waste paths. `Manure slurry` is at most a broader term for GOLD's `Liquid manure` source leaf, not for the whole ENVO `manure` class. | The fix needs a curator-maintained way to suppress or replace false source-path parents for ontology-grounded GOLD records. The current generated edge comes from `src/habitatmech/seed.py` applying GOLD parent paths; do not hand-edit `data/habitats/engineered/manure.yaml`. |

No blocker findings found.

No minor findings found.

## Recommended Edits

1. Add a maintained parent-suppression or parent-override input for ontology-grounded GOLD records and use it to drop `habitatmech:GOLD.18ee4196a9` from `ENVO:03501300`. The row should name source concept `habitatmech:GOLD.21f78f18dd`, rejected parent `habitatmech:GOLD.18ee4196a9`, and the fact that `Manure slurry` is a source parent of GOLD's narrower `Liquid manure` leaf, not a strict parent of all ENVO `manure`.
2. Update `src/habitatmech/seed.py` to apply that curated override before serializing `parent_habitats`.
3. Add a regression test that preserves `ENVO:00010483` on `manure` while rejecting only the source-derived `Manure slurry` parent.

## Follow-up Checks

| Edit | Proof |
| --- | --- |
| Drop only the false `Manure slurry` parent | `just seed-canary ENVO:03501300`, followed by an inspection of `data/habitats/engineered/manure.yaml` showing `ENVO:00010483` in `parent_habitats` and no `habitatmech:GOLD.18ee4196a9`. |
| Preserve corpus reproduction | `just seed-apply --force` and `just verify-corpus --max-diffs 1`. |
| Validate the focused record and new curated override | `just validate data/habitats/engineered/manure.yaml`, `just validate-strict data/habitats/engineered/manure.yaml`, and the new parent-override validator or regression test added with the maintained input. |
| Confirm no unrelated curation loss | `just term-requests-check`, `just validate-history`, `just worklist --limit 2000`, and `git diff --check`. |

## Additional Notes

- The existing `agricultural_waste_material` review reports the same systemic shape: a GOLD parent-path edge can be valid for a narrower source path but invalid once the source concept resolves to a broader ontology identity.
- The GOLD `Engineered > Solid waste > Animal waste > Manure` sibling has 124 direct organism assertions and stays minted with `grounding_status: NARROW`; it is deliberately not merged into `ENVO:03501300` because the shallower `Liquid manure` path already claimed the term.
