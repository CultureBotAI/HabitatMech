# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/bog_lake.yaml
- Started UTC: 2026-09-23T03:46:00Z
- Finished UTC: 2026-09-23T03:50:09Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/bog_lake.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.96e16482f3` |
| Label | `Bog lake` |
| Habitat category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Source | `GOLD` |
| GOLD source ids | `gold.ecosystem:7788`, `gold.ecosystem:7789` |
| GOLD source path | `Environmental > Aquatic > Freshwater > Bog lake` |
| Decision row | `curation/decisions.tsv:877` |
| Locked slug | `data/habitats/PATHS.tsv:2405` maps `habitatmech:GOLD.96e16482f3` to `bog_lake` |

This is the generated GOLD `Bog lake` record. Its identifier is the expected
`habitatmech:GOLD.` mint for
`sha1("GOLD:Environmental > Aquatic > Freshwater > Bog lake")[:10]`.

The complete generated record was read. It contains one GOLD source
attestation, the inherited `ENVO:00002011` `fresh water` parent, a class-level
`CONFIRM_UNGROUNDED` curation event, and the seed event. It has no definition,
synonyms, xrefs, characteristic taxa, environmental parameters, evidence,
causal graphs, discussions, or datasets.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/aquatic/bog_lake.yaml` | Passed: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/bog_lake.yaml` | Passed: 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Passed: 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed: the generated term-request table is current with 109 terms. |
| `just validate-history` | Passed: 77 history records validate against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed: 3,206 expected records, 3,206 found, 0 missing, 0 extra, 0 differing. |
| `just worklist --limit 2000` | Passed: 0 ungrounded records still undecided; 1,810 decisions on file. |
| `just report` | Passed: the corpus report completed and still reports 0 risky unreviewed groundings, 0 contradicted class-level sweeps, 0 swept path compounds, and 0 records claiming a non-habitat is a habitat. |

## Identity and Grounding

The record identity is stable and source-supported:

- `data/raw/gold_ecosystem_paths.tsv:562` records
  `Environmental > Aquatic > Freshwater > Bog lake` with leaf label
  `Bog lake`, two GOLD ecosystem node ids, 8 organism assertions, and
  8 total assertions.
- `data/raw/gold_path_biosamples.tsv:965` records one biosample on GOLD path
  id `7789`.
- `data/raw/gold_studies.tsv:587` records one study whose GOLD path list
  includes `Environmental > Aquatic > Freshwater > Bog lake`.
- `data/raw/gold_path_triads.tsv:239-241` records one MIxS-annotated sample
  for this path with `ENVO:01000252` `freshwater lake biome` as broad scale,
  `ENVO:00000021` `freshwater lake` as local scale, and `ENVO:00000546`
  `lake sediment` as medium.

The current `UNGROUNDED` status follows from the class-level
`CONFIRM_UNGROUNDED` row at `curation/decisions.tsv:877`. That row says only
that no vendored term matched the label by the class sweep's lexical routes;
it explicitly says item-level habitat status was not assessed.

The current parent `ENVO:00002011` is broader than the GOLD parent path
`Environmental > Aquatic > Freshwater`, but it is not strictly broader than
`Bog lake` itself. The vendored slice defines `ENVO:00002011` as `fresh water`,
water with a low concentration of dissolved solutes. A bog lake is a lake
context: the exact GOLD source path names a `Bog lake`, the only child GOLD path
is `Bog lake > Sediment`, and the target path's own MIxS local-scale term is
`ENVO:00000021` `freshwater lake`.

## Evidence

The source attestation is mostly supported by the raw GOLD aggregate row:

| Generated claim | Nearest source | Assessment |
|---|---|---|
| `source_id: gold.ecosystem:7788` with the duplicate-node note | `data/raw/gold_ecosystem_paths.tsv:562` lists `gold.ecosystem:7788\|gold.ecosystem:7789`. | Supported. |
| `source_path: Environmental > Aquatic > Freshwater > Bog lake` | `data/raw/gold_ecosystem_paths.tsv:562` has this exact canonical path. | Supported. |
| `assertion_count: 8`, `assertion_unit: ORGANISM` | `data/raw/gold_ecosystem_paths.tsv:562` has `organism_count: 8`. | Supported. |
| `parent_habitats: ENVO:00002011` | The source-path parent `Environmental > Aquatic > Freshwater` grounds to `ENVO:00002011`, but `ENVO:00002011` is the material `fresh water`, not a lake or lake environment. | Over-scoped; the parent comes from a source-path bin above the actual leaf. |

The MIxS triad rows are correctly absent from generated identity and parameter
fields. They are contextual evidence for one sample, not direct grounds for the
record identifier, and the generated record does not incorrectly promote the
local `freshwater lake` or medium `lake sediment` slot to exact identity.

The generated record has no external citation, quoted snippet,
characteristic-taxon claim, environmental-parameter claim, causal edge,
discussion, or dataset to verify.

## Completeness

The record is materially incomplete because it still reflects a class-level
screen rather than item-level curation. The GOLD path and the path's MIxS local
scale support treating `Bog lake` as a lake habitat narrower than
`ENVO:00000021` `freshwater lake`; no exact vendored `bog lake` term or
maintained term request currently captures that.

Hidden/ignored-inclusive exact searches covered `curation/term_requests.tsv`,
`curation/causal_graphs`, `history`, `research`, `reports`, `data/raw`, and
`data/habitats/PATHS.tsv`, excluding generated `reports/yaml_record_review`,
`research/text_map`, `data/text_map`, `pages`, and `build` outputs. They found
the expected raw GOLD rows and path pin, but no target-specific term request,
causal-graph overlay, history record, or research artifact for
`habitatmech:GOLD.96e16482f3`, `gold.ecosystem:7788`,
`gold.ecosystem:7789`, or the `Bog lake` GOLD path.

A gitignore-independent `find` over `reports/yaml_record_review` found no
prior exact `*-bog_lake.md` report.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| HM-BOG-LAKE-001 | Major | `Bog lake` inherits `ENVO:00002011` `fresh water` as a parent, but the source concept denotes a lake rather than the water material. | `data/habitats/aquatic/bog_lake.yaml` lists `ENVO:00002011`; `data/raw/ontology_terms.tsv:7173` defines `ENVO:00002011` as low-solute water; the target GOLD path is `Environmental > Aquatic > Freshwater > Bog lake`; `data/raw/gold_path_triads.tsv:240` uses `ENVO:00000021` `freshwater lake` for the local-scale slot of this path. | Replace the class-level row for `habitatmech:GOLD.96e16482f3` in `curation/decisions.tsv` and add a `curation/term_requests.tsv` row with `ENVO:00000021` as the maintained parent and `parent_mode=REPLACE`. |

## Recommended Edits

1. Replace the existing `curation/decisions.tsv` row for
   `habitatmech:GOLD.96e16482f3` with an item-level
   `CONFIRM_UNGROUNDED` decision that names `ENVO:00000021`
   `freshwater lake` as the nearest broader parent and notes that the GOLD
   source path denotes a lake context, not the generic `fresh water` material.
2. Add a `curation/term_requests.tsv` row for
   `habitatmech:GOLD.96e16482f3` with a source-derived `Bog lake` definition,
   `parent_class` `ENVO:00000021`, `parent_label` `freshwater lake`, and
   `parent_mode` `REPLACE` so reseeding drops the false inherited
   `ENVO:00002011` parent.

## Follow-up Checks

After those curation edits:

- Run `just seed`.
- Run `just seed-canary habitatmech:GOLD.96e16482f3`.
- Inspect `data/habitats/aquatic/bog_lake.yaml` and confirm it has a
  HabitatMech definition, `grounding_status: UNGROUNDED`,
  `mapping_status: REVIEWED`, and no `ENVO:00002011` parent.
- Run `just seed-apply --force`.
- Run `just term-requests-check`.
- Run `just validate data/habitats/aquatic/bog_lake.yaml`.
- Run `just validate-strict data/habitats/aquatic/bog_lake.yaml`.
- Run `just verify-corpus --max-diffs 1`.
- Run `just report` and confirm it still reports 0 risky unreviewed
  groundings and 0 swept path compounds.

## Additional Notes

`data/habitats/aquatic/sediment__d5068bb8.yaml` is the generated record for
the zero-assertion child path
`Environmental > Aquatic > Freshwater > Bog lake > Sediment`; it currently
uses `habitatmech:GOLD.96e16482f3` as its generated GOLD source-path parent.
Fixing `Bog lake` as a lake context would therefore also put that child
sediment record under a defensible generated parent.
