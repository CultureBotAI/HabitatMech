# YAML Record Review: Adjacent soil

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/adjacent_soil.yaml`
- Started UTC: 2026-09-21T11:05:00Z
- Finished UTC: 2026-09-21T11:25:14Z
- Verdict: needs curation

## Target

| Field | Observed value |
|---|---|
| Path | `data/habitats/aquatic/adjacent_soil.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.245555f695` |
| Label | `Adjacent soil` |
| Category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Maintained or generated | Generated from `data/raw/` plus curation inputs |

The record contains one GOLD source attestation:

| Source | Source ID | Source label | Source path |
|---|---|---|---|
| `GOLD` | `gold.ecosystem:6352` | `Adjacent soil` | `Environmental > Aquatic > Thermal springs > Runoff channel > Adjacent soil` |

Current hierarchy:

| Parent | Label in corpus | Basis |
|---|---|---|
| `habitatmech:GOLD.09350714a0` | `Runoff channel` | Inherited from the GOLD parent path |

The record has no definition, synonyms, xrefs, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussions, or datasets.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/adjacent_soil.yaml` | Passed; LinkML reported no issues. |
| `just validate-strict data/habitats/aquatic/adjacent_soil.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just term-requests-check` | Passed; the generated term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records were present, with 0 missing, 0 extra, and 0 differing files. |

## Identity and Grounding

The generated source identity is internally consistent:

| Claim | Review |
|---|---|
| GOLD source path | Supported. `data/raw/gold_ecosystem_paths.tsv` has the exact canonical path `Environmental > Aquatic > Thermal springs > Runoff channel > Adjacent soil` and records `gold.ecosystem:6352` as the only node ID for that collapsed path. |
| Source label | Supported. The raw GOLD path row sets the leaf label to `Adjacent soil`, matching the generated `source_label`. |
| Slug | Supported. `data/habitats/PATHS.tsv` pins `habitatmech:GOLD.245555f695` to `adjacent_soil`. |
| Category | Supported by the first two GOLD path levels, `Environmental > Aquatic`. |
| `mapping_status: SEEDED` | Supported. `curation/decisions.tsv` has only a `CLASS`-depth no-match decision for this source concept, and class-level decisions deliberately do not promote records to `REVIEWED`. |

The exact identifier has no item-level curation row. The only current decision row is:

```tsv
habitatmech:GOLD.245555f695	CONFIRM_UNGROUNDED				claude-opus-5	2026-08-12	Class-level sweep (see docs/HARMONIZATION.md#class-level-sweep): no term in the vendored slice matched this label by any search route. Whether the concept is a habitat at all was NOT assessed, so this is not yet a term-request candidate.	CLASS
```

That supports the current `SEEDED` lifecycle, but it is not an item-level judgement that `Adjacent soil` has been assessed as a habitat, checked against the thermal-spring runoff context, and found to lack an exact ENVO term.

## Evidence

The record has no curator-authored definition, literature evidence, causal graph, environmental parameter, characteristic taxon, discussion, or dataset claims to audit.

The GOLD occurrence itself is supported by local inventories:

| Inventory | Matching row |
|---|---|
| `data/raw/gold_ecosystem_paths.tsv` | The exact canonical path appears with depth `5`, node count `1`, zero source assertion totals, and `gold.ecosystem:6352`. |
| `data/raw/gold_path_biosamples.tsv` | The exact path appears with ecosystem path ID `6352` and biosample count `4`. |
| `data/raw/gold_studies.tsv` | GOLD study `Gs0154480` lists the exact adjacent-soil path along with `Environmental > Aquatic > Thermal springs > Runoff channel`, `Environmental > Terrestrial > Rock-dwelling (endoliths)`, and `Host-associated > Algae > Red algae`. |

## Completeness

Consequential gaps:

- There is no item-level `curation/decisions.tsv` row for `habitatmech:GOLD.245555f695`.
- There is no `curation/term_requests.tsv` definition that would model this as a minted habitat under `ENVO:00001998` `soil`.
- There is no maintained curation input that can replace the inherited source-path parent `habitatmech:GOLD.09350714a0` with a true soil genus.
- There is no causal graph overlay or record-level evidence; this is acceptable for a seeded, uncurated GOLD-only record.
- There are no seeded environmental parameters, characteristic taxa, discussions, or datasets; those optional slots are correctly absent because the searched maintained inputs provide no claim to emit.

Bounded absence checks:

| Query | Scope | Result |
|---|---|---|
| `habitatmech:GOLD.245555f695`, `gold.ecosystem:6352`, and the exact GOLD source path | Hidden/ignored-inclusive search over `data/raw`, `data/habitats/PATHS.tsv`, `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/causal_graphs`, `research/habitats`, and `reports/yaml_record_review`, excluding `data/text_map/**` | Found only the slug row, the class-level decision row, the generated target, and the raw GOLD inventory rows listed above. |
| `Adjacent soil` / `adjacent soil` | Hidden/ignored-inclusive search over `data/raw/ontology_terms.tsv`, `curation/term_requests.tsv`, `curation/term_requests_excluded.tsv`, `research/habitats`, and `reports/yaml_record_review` | No exact ontology term, term request, excluded term request, research report, or prior adjacent-soil review was found. |
| `ENVO:00001998` | Hidden/ignored-inclusive search in `data/raw/ontology_terms.tsv` | Confirmed the vendored exact broader term `soil`. |
| `Runoff channel` and `gold.ecosystem:6350` | Hidden/ignored-inclusive search over raw GOLD inventories, generated habitats, and curation inputs | Confirmed `Runoff channel` is the immediate source parent, is itself a class-level `UNGROUNDED` minted habitat, and is also the source parent for the sibling runoff-channel microbial-mat and sediment records. |

## Findings

### Major

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| M1 | `parent_habitats` asserts that `Adjacent soil` is a kind of `Runoff channel`, but the source leaf denotes soil adjacent to a runoff channel, not the channel itself. | `docs/CURATION.md` and `CLAUDE.md` both define `parent_habitats` as strictly broader is-a links. The GOLD row spells the final path as `... > Runoff channel > Adjacent soil`; the leaf is soil in an adjacent location. `ENVO:00001998` `soil` exists in the vendored ontology slice and is a true broader material class, while the adjacent runoff channel is contextual. HabitatMech already uses `curation/term_requests.tsv` with `parent_mode=REPLACE` when a GOLD path parent is contextual rather than broader, e.g. `habitatmech:GOLD.398aeb6c37` `indoor dust` replacing the inherited `Indoor Air` parent with `ENVO:00002008` `dust`. | `curation/decisions.tsv` for item-level review plus `curation/term_requests.tsv` for the minted definition and replacement parent. |

### Minor

None found.

### Blockers

None found.

## Recommended Edits

1. Add an item-level `CONFIRM_UNGROUNDED` decision for `habitatmech:GOLD.245555f695` in `curation/decisions.tsv` after reviewing the GOLD path and the `ENVO:00001998` near match. The exact identity should stay minted because `soil` is broader than soil adjacent to a thermal-spring runoff channel.
2. Add a `curation/term_requests.tsv` row for a minted adjacent-runoff-channel soil class under `ENVO:00001998` `soil` with `parent_mode` set to `REPLACE`. The replacement is needed because the inherited GOLD parent `habitatmech:GOLD.09350714a0` `Runoff channel` is contextual rather than strictly broader.
3. Regenerate only through the seeder: run `just seed`, canary `just seed-canary habitatmech:GOLD.245555f695`, inspect `data/habitats/aquatic/adjacent_soil.yaml`, then run `just seed-apply --force` and commit the regenerated record.
4. Scaffold an append-only history entry for the curation session with `just new-history`, filling in the evidence and checks used to justify the item-level decision and the authored term request.

## Follow-up Checks

- `just validate data/habitats/aquatic/adjacent_soil.yaml`
- `just validate-strict data/habitats/aquatic/adjacent_soil.yaml --quiet`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just qc`
- Manual post-regeneration check: confirm `mapping_status: REVIEWED`, `grounding_status: UNGROUNDED`, `definition_source: HabitatMech`, and `parent_habitats: [ENVO:00001998]`, with no retained `habitatmech:GOLD.09350714a0` parent.

## Additional Notes

- All absence searches described above included hidden and ignored files.
- The incidental phrase `adjacent soil` appears in `curation/causal_graphs/fungi_associated_environment.yaml` and in the generated fungi-associated-environment habitat, but those passages describe fungal hyphae releasing exudates into nearby soil and are unrelated to `gold.ecosystem:6352`.
- The sibling `data/habitats/aquatic/microbial_mats__42c901d2.yaml` shows the pattern for a source child that has been linked to a true ontology parent: it has an item-level `GROUND_AS_PARENT` decision for `ENVO:01000008` `microbial mat`. `Adjacent soil` needs the analogous item-level near-match review for `ENVO:00001998` `soil`, plus a replacement of the inherited contextual `Runoff channel` parent.
