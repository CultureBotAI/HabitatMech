# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/abdomen_nodes.yaml`
- Started UTC: 2026-09-22T20:59:00Z
- Finished UTC: 2026-09-22T21:01:24Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/host_associated/abdomen_nodes.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.2e3f6a8ccd` |
| Label | `Abdomen nodes` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Record status | Generated from one class-swept GOLD source concept |

This is the minted GOLD record for `Host-associated > Mammals: Human > Lymphatic system > Lymph nodes > Abdomen nodes`. Its identifier is the deterministic source-concept id `habitatmech:GOLD.2e3f6a8ccd`, computed from `sha1("GOLD:Host-associated > Mammals: Human > Lymphatic system > Lymph nodes > Abdomen nodes")[:10]`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/abdomen_nodes.yaml` | Passed; LinkML validation reported no issues. |
| `just validate-strict data/habitats/host_associated/abdomen_nodes.yaml` | Passed; one file scanned with 0 error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 generated records, found 3206, with 0 missing, 0 extra, and 0 differing. |
| `git diff --check` | Passed; no whitespace errors. |

## Identity and Grounding

The GOLD source identity is supported:

| Raw field | Value |
|---|---|
| `canonical_path` | `Host-associated > Mammals: Human > Lymphatic system > Lymph nodes > Abdomen nodes` |
| `ecosystem` | `Host-associated` |
| `ecosystem_category` | `Mammals: Human` |
| `ecosystem_type` | `Lymphatic system` |
| `ecosystem_subtype` | `Lymph nodes` |
| `specific_ecosystem` | `Abdomen nodes` |
| `leaf_label` | `Abdomen nodes` |
| `path_depth` | `5` |
| `gold_node_count` | `1` |
| `organism_count` | `0` |
| `metagenome_count` | `0` |
| `biosample_count` | `0` |
| `total_assertions` | `0` |
| `gold_node_ids` | `gold.ecosystem:6530` |

`source_attestations` matches `data/raw/gold_ecosystem_paths.tsv:2305`: the record keeps `gold.ecosystem:6530` as the source id, records the `Abdomen nodes` leaf label and canonical GOLD path exactly, omits `assertion_count` because the raw organism count is zero, and does not add a multi-node note because the path has only one GOLD node id.

The `UBERON:0000029` parent is supported. `data/raw/gold_ecosystem_paths.tsv:385` records the immediate GOLD parent path `Host-associated > Mammals: Human > Lymphatic system > Lymph nodes`; `curation/decisions.tsv:818` grounds that parent source concept to `UBERON:0000029`; and `data/habitats/host_associated/lymph_node__7af26c8b.yaml` is the generated reviewed record for the merged lymph-node parent.

`grounding_status: UNGROUNDED` and `mapping_status: SEEDED` are mechanically expected. `curation/decisions.tsv:349` confirms `habitatmech:GOLD.2e3f6a8ccd` as ungrounded at `review_depth: CLASS`: that records that no vendored ENVO, UBERON, FOODON, or BTO term matched the label during the mechanical class-level sweep, but it deliberately does not make an item-level habitat judgment. `docs/HARMONIZATION.md` states that class-level decisions do not promote a record to `REVIEWED`, so the generated status remains `SEEDED`.

No exact ontology grounding appears to be missing. An ignored/hidden-inclusive search for `abdomen nodes`, `abdominal lymph`, and overlapping `lymph`/`abdomen` strings across `data/raw/ontology_terms.tsv`, `curation`, and `data/habitats/host_associated` found no vendored abdominal-lymph-node term.

## Evidence

| Claim | Nearest support | Review |
|---|---|---|
| The generated record represents GOLD node `gold.ecosystem:6530` at `Host-associated > Mammals: Human > Lymphatic system > Lymph nodes > Abdomen nodes`. | `data/raw/gold_ecosystem_paths.tsv:2305` | Supported exactly. |
| The GOLD source row has no upstream organism, metagenome, or biosample assertions. | `data/raw/gold_ecosystem_paths.tsv:2305` | Supported exactly. |
| The `Abdomen nodes` row is a child of the reviewed `lymph node` record. | `data/raw/gold_ecosystem_paths.tsv:385`; `curation/decisions.tsv:818`; `src/habitatmech/seed.py` | Supported. `ingest_gold()` links each GOLD row to the generated record for the next path level up, and the parent path resolves to `UBERON:0000029`. |
| `mapping_status: SEEDED` | `curation/decisions.tsv:349`; `docs/HARMONIZATION.md` | Supported. The only source-concept decision is class-level, so it confirms the lexical screen without making this record item-reviewed. |

Unsupported or over-scoped claims: None found. The record avoids the known sibling error from `Host-associated > Mammals > Lymphatic system > Lymph nodes > Abdomen`: it keeps the supported `lymph node` parent and does not add the abdomen-location term `UBERON:0000916` as a false broader parent.

## Completeness

No direct source attestation is missing for the GOLD source row: the record carries the source name, source id, source label, and canonical source path. Its omitted `assertion_count` and omitted node-count note are both expected for a raw row with `organism_count: 0` and `gold_node_count: 1`.

The target has no definition, causal graph, environmental parameters, characteristic taxa, discussions, or datasets. Those optional slots are correctly absent for a class-level ungrounded GOLD path with no maintained `curation/term_requests.tsv`, `curation/causal_graphs`, or external evidence keyed by `habitatmech:GOLD.2e3f6a8ccd`.

No maintained term request, external xref, causal overlay, history file, or raw research report was found for `habitatmech:GOLD.2e3f6a8ccd`; the exact search covered `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/term_requests/envo_robot_template.tsv`, `curation/external_xrefs.tsv`, `curation/causal_graphs`, `history`, and `research` with ignored and hidden files included. The only maintained curation input for the source is the class-level `curation/decisions.tsv:349` row cited above.

No maintained causal overlay, history file, raw research report, or prior exact YAML review report was found by filename for `abdomen_nodes` or `abdomen-nodes` under `curation/causal_graphs`, `history`, `research`, or `reports/yaml_record_review`; the `find` search included ignored files beneath those checked directories.

Sibling regional human lymph-node records generated from `gold.ecosystem:6531` through `gold.ecosystem:6534` and `gold.ecosystem:6529` use the same source-derived `UBERON:0000029` parent-only shape. `Host-associated > Mammals: Human > Lymphatic system > Lymph nodes > Cervical/Neck nodes` and `Groin nodes` have direct GOLD organism assertions and will need their own item-specific review, but that does not affect this zero-assertion `Abdomen nodes` record.

## Findings

None found.

No blocker findings.

No major findings.

No minor findings.

## Recommended Edits

None required.

If the source concept later receives an item-level review, add a replacement `curation/decisions.tsv` row for `habitatmech:GOLD.2e3f6a8ccd` and rerun `just seed-canary habitatmech:GOLD.2e3f6a8ccd`; a status-promotion-only edit should keep `parent_habitats` limited to `UBERON:0000029`.

## Follow-up Checks

None required beyond the validators already run for this report.

For any future status-promotion-only edit, run:

- `just seed-canary habitatmech:GOLD.2e3f6a8ccd`
- `just validate data/habitats/host_associated/abdomen_nodes.yaml`
- `just validate-strict data/habitats/host_associated/abdomen_nodes.yaml`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

## Additional Notes

- The pre-report exact filename check `find reports/yaml_record_review -maxdepth 1 -type f -name '*abdomen_nodes*' -print` returned no previous exact report; `find` includes ignored files.
- Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.2e3f6a8ccd`, `Host-associated > Mammals: Human > Lymphatic system > Lymph nodes > Abdomen nodes`, `gold.ecosystem:6530`, `UBERON:0000029`, and candidate abdominal-lymph-node labels found the generated target, the raw GOLD row, the class-level decision row, the reviewed lymph-node parent, and the human lymph-node sibling rows cited above.
- The prior `abdomen__0eb11b74` review mentions this record only as a safer human-specific lymph-node sibling; it is not a prior review of `data/habitats/host_associated/abdomen_nodes.yaml`.
