# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/biofilter__c190958d.yaml
- Started UTC: 2026-09-24T19:26:25Z
- Finished UTC: 2026-09-24T19:29:15Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.c023583133` |
| Label | `Biofilter` |
| Category | `ENGINEERED` |
| Grounding | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained source | generated from `data/raw/gold_ecosystem_paths.tsv` and `data/habitats/PATHS.tsv` |

Reviewed the complete generated record at `data/habitats/engineered/biofilter__c190958d.yaml`. The record is a GOLD-only habitat for `Engineered > Artificial ecosystem > Aquaculture > Freshwater RAS > Biofilter`, with the displayed `gold.ecosystem:8322` source attestation and zero GOLD organism, study, or biosample assertions. The file itself is generated output; any future item-review belongs in `curation/decisions.tsv`, and any future definition or inherited-parent replacement belongs in `curation/term_requests.tsv`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/biofilter__c190958d.yaml` | Passed |
| `just validate-strict data/habitats/engineered/biofilter__c190958d.yaml` | Passed |
| Focused causal overlay validator | Not applicable; exact ignored-inclusive searches in `curation/causal_graphs/` found no `habitatmech:GOLD.c023583133`, `gold.ecosystem:8322`, or exact Freshwater RAS Biofilter source-path overlay |
| `just validate-causal-all` | Passed; 32 files / 32 graphs |
| Reference validator | Not applicable; the generated GOLD-only record has no record-level `evidence` items, no causal graph, and no environmental-parameter references |
| `just term-requests-check` | Passed; 109 terms |
| `just validate-history` | Passed; 77 history records |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected / 3,206 found / 0 missing / 0 extra / 0 differing |
| `just worklist --limit 2000` | Passed; 0 ungrounded concepts remain undecided and 1,810 decisions are present |
| `just report` | Passed; 3,206 habitat records summarized |
| `git diff --check` | Passed |

## Identity and Grounding

The generated identifier, label, category, NARROW grounding, and source attestation agree with the committed GOLD inventory. `data/raw/gold_ecosystem_paths.tsv` has the path `Engineered > Artificial ecosystem > Aquaculture > Freshwater RAS > Biofilter`, with `depth` 5, `gold_node_count` 1, `organism_count` 0, `study_count` 0, `biosample_count` 0, `total_assertions` 0, and `gold_node_ids` `gold.ecosystem:8322`.

The `ENVO:00002152` `biofilter` parent is a sound broader term for a biofilter in a freshwater recirculating aquaculture system. The vendored ontology slice defines `ENVO:00002152` as "A bioreactor which captures and, through the biological processes maintained in the medium it contains, degrades pollutants.", and lists it as a subclass of `ENVO:00002123` `bioreactor`.

The other generated parent, `habitatmech:GOLD.2b5caaea9b`, is the immediate GOLD source-path parent for `Engineered > Artificial ecosystem > Aquaculture > Freshwater RAS`. Its only maintained decision is a class-level `CONFIRM_UNGROUNDED` row, not item review, and that is a weaker problem than the edge direction itself: the target denotes a biofilter inside a freshwater recirculating aquaculture system, not a subtype of the whole system. `parent_habitats` is an `is_a` claim, so the container-system parent needs to be dropped or replaced by a true authored genus.

## Evidence

- Supported: the source path, source leaf, `gold.ecosystem:8322` source ID, and zero-assertion count match `data/raw/gold_ecosystem_paths.tsv`.
- Supported: exact ignored-inclusive searches for `gold.ecosystem:8322` and the full Freshwater RAS Biofilter source path under `data`, `curation`, `history`, `research`, `conf`, and `reports` found the target YAML and the canonical `gold_ecosystem_paths.tsv` row, with no direct GOLD biosample, MIxS triad, or study side-table row to audit.
- Supported: exact `Biofilter` searches over `curation/decisions.tsv`, `curation/term_requests.tsv`, `reports/yaml_record_review`, `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/PATHS.tsv`, and `curation/causal_graphs` show the raw Freshwater RAS Biofilter leaf plus two GOLD Biofilter siblings, but no maintained curation row for `habitatmech:GOLD.c023583133`.
- Under-reviewed: the source-path parent `habitatmech:GOLD.2b5caaea9b` has only a class-level sweep decision whose note explicitly says habitathood was not assessed.
- Unsupported: the generated hierarchy treats the target as a kind of Freshwater RAS. The source path establishes a contained biofilter component in that RAS context; it does not establish that the component is itself a recirculating aquaculture system.
- No record-level citations, characteristic taxa, environmental parameters, or causal edges are present, so there are no attached snippets or literature references to validate.

## Completeness

The record is structurally sparse but not missing any required generated field. It has the expected GOLD source attestation and the expected `skos:narrowMatch` to `ENVO:00002152` for a zero-assertion Biofilter source concept.

Bounded ignored-inclusive searches were run over `data/habitats/PATHS.tsv`, `curation/decisions.tsv`, `curation/term_requests.tsv`, `history`, `research`, `conf/id_label_targets.yaml`, `curation/causal_graphs`, `reports/yaml_record_review`, and the focused `data/raw` GOLD side tables for `habitatmech:GOLD.c023583133`, `gold.ecosystem:8322`, and `Engineered > Artificial ecosystem > Aquaculture > Freshwater RAS > Biofilter`. They found the generated target, the `data/habitats/PATHS.tsv` row, and the canonical GOLD ecosystem-path row; they found no existing review report for this exact stem before this report was written and no item-level decision, term request, history entry, research report, label-correspondence exception, auxiliary GOLD biosample/triad/study row, or target-specific causal overlay.

The immediate generated parent at `data/habitats/engineered/freshwater_ras.yaml` is also sparse: it is `SEEDED`, `UNGROUNDED`, and backed only by a class-level decision. Item-review of that parent is still useful before signing off on the full source path, but it would not by itself make the Biofilter component a kind of Freshwater RAS.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `biofilter__c190958d.yaml` inherits `habitatmech:GOLD.2b5caaea9b` `Freshwater RAS` as a parent, turning GOLD's component-in-system path into the claim that a biofilter is a kind of freshwater recirculating aquaculture system. | The target GOLD path is `Engineered > Artificial ecosystem > Aquaculture > Freshwater RAS > Biofilter`, the target already keeps the true broader `ENVO:00002152` `biofilter` parent, and the parent record denotes the surrounding Freshwater RAS system. `CLAUDE.md` and `docs/CURATION.md` both reserve `parent_habitats` for strictly broader habitats; a contained biofilter is not a subtype of the system that contains it. | `curation/decisions.tsv` and `curation/term_requests.tsv` |

## Recommended Edits

1. Item-review `habitatmech:GOLD.c023583133` in `curation/decisions.tsv` against the exact `Engineered > Artificial ecosystem > Aquaculture > Freshwater RAS > Biofilter` source path, `ENVO:00002152` `biofilter`, and the generated `habitatmech:GOLD.2b5caaea9b` Freshwater RAS parent.
2. If the record remains distinct from generic `ENVO:00002152`, add a `curation/term_requests.tsv` definition for a freshwater recirculating aquaculture system biofilter under `ENVO:00002152` `biofilter` with `parent_mode=REPLACE`, so regeneration keeps the true Biofilter genus and drops the false Freshwater RAS source-path parent.
3. Canary-regenerate `habitatmech:GOLD.c023583133` and inspect `data/habitats/engineered/biofilter__c190958d.yaml` to confirm the inherited Freshwater RAS parent is gone and the generated `ENVO:00002152` parent remains.

## Follow-up Checks

- `just seed`
- `uv run python scripts/seed_from_sources.py --apply --force --only habitatmech:GOLD.c023583133`
- Inspect `data/habitats/engineered/biofilter__c190958d.yaml`
- `just validate data/habitats/engineered/biofilter__c190958d.yaml`
- `just validate-strict data/habitats/engineered/biofilter__c190958d.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

## Additional Notes

- No blocker finding was found: the current generated YAML validates, its GOLD source attestation is internally consistent, and the record denotes an engineered biofilter rather than a non-habitat.
- No minor findings were found.
