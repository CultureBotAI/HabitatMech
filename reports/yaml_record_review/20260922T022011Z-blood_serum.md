# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/blood_serum.yaml`
- Started UTC: 2026-09-22T02:18:20Z
- Finished UTC: 2026-09-22T02:20:11Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `UBERON:0001977` |
| Label | `blood serum` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the vendored UBERON ontology slice and GOLD raw tables; future item-level review belongs in `curation/decisions.tsv`, and future mechanism evidence belongs in `curation/causal_graphs/*.yaml`, not in this generated YAML |
| Locked slug | `data/habitats/PATHS.tsv:1052` maps `UBERON:0001977` to `blood_serum` |

This is the UBERON-grounded GOLD `Serum` record merged from three same-meaning host-clade paths:

- `Host-associated > Mammals: Human > Circulatory system > Blood > Serum`, deterministically keyed as `habitatmech:GOLD.9bed5e7377`
- `Host-associated > Mammals > Circulatory system > Blood > Serum`, deterministically keyed as `habitatmech:GOLD.1a5b0996a6`
- `Host-associated > Birds > Circulatory system > Blood > Serum`, deterministically keyed as `habitatmech:GOLD.643da6e69e`

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/blood_serum.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/blood_serum.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors |

## Identity and Grounding

The exact UBERON identity is supported by the vendored ontology slice and by HabitatMech's documented composed-label GOLD route:

- `data/raw/ontology_terms.tsv:13096` defines `UBERON:0001977` with canonical label `blood serum`, definition `The portion of blood plasma that excludes clotting factors.`, exact synonym `serum`, a blank `deprecated` flag, and `label_only: FALSE`.
- `data/raw/ontology_subclass_edges.tsv:11284` places `UBERON:0001977` under `UBERON:0000179` haemolymphatic fluid, matching the generated ontology parent.
- `docs/HARMONIZATION.md:115` lists `UBERON:0001977` blood serum as a deliberate composed-label merge for the Mammals: Human, Mammals, and Birds GOLD `> Circulatory system > Blood > Serum` paths.
- `src/habitatmech/seed.py` lets every matching `Host-associated` path claim a composed term when the paths differ only by host clade, and `tests/test_seed_harmonization.py` explicitly tests that a bird `Blood > Serum` path resolves to `UBERON:0001977` instead of a minted child.
- `data/raw/gold_ecosystem_paths.tsv:852`, `:743`, and `:1866` are the human, mammal, and bird Serum rows that generate the record's three `source_attestations`.

Each GOLD source path has a strictly broader generated parent for its `Blood` prefix:

| Parent | Locked slug | Scope |
|---|---|---|
| `habitatmech:GOLD.d25ff4a586` | `data/habitats/PATHS.tsv:2842` -> `blood__5ef3f2f4` | Mammals: Human `Blood` |
| `habitatmech:GOLD.6c6aab19d3` | `data/habitats/PATHS.tsv:2082` -> `blood__c1b484fe` | Mammals `Blood` |
| `habitatmech:GOLD.f46a5017f9` | `data/habitats/PATHS.tsv:3110` -> `blood__070ce7aa` | Birds `Blood` |

The generated `mapping_status: SEEDED` is expected. Exact ignored/hidden-inclusive searches found no `curation/decisions.tsv` item-level row for `habitatmech:GOLD.9bed5e7377`, `habitatmech:GOLD.1a5b0996a6`, or `habitatmech:GOLD.643da6e69e`.

## Evidence

Every generated scientific claim in this record is source-derived:

| Claim | Nearest source | Review |
|---|---|---|
| UBERON identifier, canonical label, definition, exact `serum` synonym, blank `deprecated` flag, and `label_only: FALSE` status | `data/raw/ontology_terms.tsv:13096` | Supported exactly |
| GOLD exact synonym `Serum` | `data/raw/gold_ecosystem_paths.tsv:743`, `:852`, `:1866` | Supported by all three Serum rows |
| GOLD `source_id: gold.ecosystem:4430`, source path, `assertion_count: 2`, and `assertion_unit: ORGANISM` | `data/raw/gold_ecosystem_paths.tsv:852` | Supported; `assertion_count` comes from the row's `organism_count` |
| GOLD `source_id: gold.ecosystem:6818`, source path, `assertion_count: 3`, and `assertion_unit: ORGANISM` | `data/raw/gold_ecosystem_paths.tsv:743` | Supported; `assertion_count` comes from the row's `organism_count` |
| GOLD `source_id: gold.ecosystem:6898` and Birds Serum source path with no assertion count | `data/raw/gold_ecosystem_paths.tsv:1866` | Supported; `organism_count` is `0`, and the seeder only emits nonzero assertion counts |
| Source-path Blood parents | `data/raw/gold_ecosystem_paths.tsv` plus `data/habitats/PATHS.tsv` | Supported for human, mammal, and bird parent paths |

The human Serum path appears in `data/raw/gold_path_biosamples.tsv:46`, and the bird Serum path appears in `data/raw/gold_path_biosamples.tsv:853` and `data/raw/gold_path_triads.tsv:1298-1300`. Those side tables do not feed any current generated field in this record; GOLD source assertion counts come from `gold_ecosystem_paths.tsv` `organism_count`, not biosample or study counts.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- The three `source_attestations` entries capture every GOLD canonical path that intentionally merges into `UBERON:0001977`.
- The ontology and GOLD synonyms are both present and have the correct synonym sources.
- `parent_habitats` has the direct UBERON parent plus all three source-path Blood parents.
- No record-level evidence, environmental parameter, characteristic taxon, causal graph, discussion, or dataset is expected because no maintained input supplies one for this generated GOLD/UBERON record.
- `mapping_status` stays `SEEDED` because none of the three source concepts has an item-level decision row.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research/habitats`, `reports/yaml_record_review`, `docs`, `.claude`, `src`, `tests`, `build`, `data/habitats/PATHS.tsv`, and the exact raw GOLD and ontology TSVs that can feed this target, while excluding generated `data/text_map/**` and `pages/**`. They found only the target YAML, raw rows, path locks, generated text-map projection, seed-code/test references for the composed merge, the `docs/HARMONIZATION.md` design note, and contextual prior-report or decision-note mentions; they found no maintained decision row, term request, history record, causal-graph overlay, target-specific research report, or prior exact YAML review report for `blood_serum`.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if a future curation change adds item-level decisions or causal-graph overlays for the three GOLD Serum source paths:

- `just validate data/habitats/host_associated/blood_serum.yaml`
- `just validate-strict data/habitats/host_associated/blood_serum.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If future curation signs off the three composed-label source concepts, add one `curation/decisions.tsv` `GROUND` row for each of `habitatmech:GOLD.9bed5e7377`, `habitatmech:GOLD.1a5b0996a6`, and `habitatmech:GOLD.643da6e69e`, run `just seed`, preview `just seed-canary UBERON:0001977`, and then rerun the corpus-level checks after regeneration.

## Additional Notes

- `find reports/yaml_record_review -name '*blood_serum*' -print` found no pre-existing exact review report for this record before this file was written.
- The vendored ontology slice also contains BTO `blood serum` and broader `serum` classes, but an ignored/hidden-inclusive search found no `BTO:0000133` or `BTO:0001239` entry in `data/habitats/PATHS.tsv` or generated `data/habitats/**/*.yaml`.
- Related exact-search hits in prior Blood reports, decision rows for other host-clade merge records, and the `blood_serum` text-map projection are context only and are not maintained curation inputs to the target reviewed here.
