# YAML Record Review: Biofouling

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/biofouling.yaml`
- Started UTC: 2026-09-21T21:31:11Z
- Finished UTC: 2026-09-21T21:47:00Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.15bcd61a84` |
| Label | `Biofouling` |
| Category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `REVIEWED` |
| Maintained inputs | `data/raw/gold_ecosystem_paths.tsv`, `curation/decisions.tsv`, `curation/term_requests_excluded.tsv`, `data/habitats/PATHS.tsv` |

This is a generated GOLD record. The path lock maps `habitatmech:GOLD.15bcd61a84` to the `biofouling` slug, and the raw GOLD row maps `gold.ecosystem:8363` to exactly `Environmental > Aquatic > Deep subsurface > Groundwater > Biofouling` with `Biofouling` as the leaf label, depth 5, one GOLD ecosystem node, and zero organisms, studies, biosamples, or total assertions.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/aquatic/biofouling.yaml` | Pass; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/biofouling.yaml --quiet` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Pass; the committed term-request table is current with 109 terms. |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass; 3,206 expected records, 3,206 records on disk, and 0 missing, extra, or differing records. |

`just validate-products` was not run. This review did not add or change a grounding row, and the record's only ontology xref, `ENVO:06105023`, was checked directly in the vendored `data/raw/ontology_terms.tsv` slice.

## Identity and Grounding

The generated identity is internally consistent:

- `data/habitats/PATHS.tsv` pins `habitatmech:GOLD.15bcd61a84` to `biofouling`, matching the reviewed YAML path.
- `data/raw/gold_ecosystem_paths.tsv` carries the exact source path and source node `gold.ecosystem:8363` copied into `source_attestations`.
- The immediate path parent is the generated `habitatmech:GOLD.22a80cbd14` `Groundwater` record from GOLD's `Environmental > Aquatic > Deep subsurface > Groundwater` path; this is a source-path parent, not a claim that the ENVO biofouling process is a habitat parent.
- `curation/decisions.tsv` has an item-level `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.15bcd61a84` with `ENVO:06105023` retained through `relation: xref`.

`ENVO:06105023` is present in `data/raw/ontology_terms.tsv` as `biofouling` with the definition "A fouling process during which biological matter accumulates on a solid surface." That supports keeping it as an `xrefs` value rather than putting it in `parent_habitats`, because `parent_habitats` is restricted to strictly broader habitat concepts.

The record is also correctly left ungrounded to `ENVO:00002034` `biofilm`. The three GOLD `Microfouling/Biofilm` leaves under Freshwater, Marine, and Non-marine Saline and Alkaline already ground exactly to `ENVO:00002034`; an ignored-file-inclusive search of the raw GOLD ecosystem table found no `Microfouling/Biofilm` child under `Environmental > Aquatic > Deep subsurface > Groundwater > Biofouling`.

## Evidence

Supported claims:

- The `Biofouling` label, `GOLD` source, `gold.ecosystem:8363` source id, and full source path all come verbatim from `data/raw/gold_ecosystem_paths.tsv`.
- `habitat_category: AQUATIC` follows from the GOLD `Environmental > Aquatic` source path.
- `parent_habitats: habitatmech:GOLD.22a80cbd14` follows from the next GOLD path level, `Environmental > Aquatic > Deep subsurface > Groundwater`.
- `grounding_status: UNGROUNDED`, `mapping_status: REVIEWED`, and `xrefs: ENVO:06105023` follow from the `CONFIRM_UNGROUNDED`, `ITEM`, `xref` decision row for `habitatmech:GOLD.15bcd61a84`.
- `curation/term_requests_excluded.tsv` explicitly excludes the record from HabitatMech term requests because this GOLD leaf has zero organisms, studies, and biosamples, has no `Microfouling/Biofilm` child under Groundwater, and only matches a process term.

Unsupported or over-scoped claims: None found.

The record has no definition, record-level evidence, environmental parameters, characteristic taxa, causal graphs, discussion links, or datasets. That absence is supported: ignored-file-inclusive searches found no exact `Environmental > Aquatic > Deep subsurface > Groundwater > Biofouling` or `gold.ecosystem:8363` entries in `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, or `data/raw/gold_studies.tsv`, and no exact `habitatmech:GOLD.15bcd61a84` entry in `curation/term_requests.tsv` or `curation/causal_graphs/`.

## Completeness

The record is intentionally sparse and complete enough for the unused GOLD parent leaf it preserves:

- No Biofouling definition is expected because no HabitatMech term should be requested for this zero-assertion grouping bin.
- No GOLD assertion count is expected because the raw `organism_count`, `study_count`, `biosample_count`, and `total_assertions` are all zero.
- No GOLD MIxS triads or study rows are expected; the exact source path and source id are absent from the committed GOLD API TSVs named above.
- No causal graph is expected; the record has no sample-derived or literature-derived mechanism claims to support.

All negative checks above used `rg --no-ignore --hidden` or `find`, so ignored files were included where they were in the searched path.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

If a future curation change touches this record or its Biofouling cohort, rerun:

- `just validate data/habitats/aquatic/biofouling.yaml`
- `just validate-strict data/habitats/aquatic/biofouling.yaml --quiet`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`

## Additional Notes

An ignored-file-inclusive search of `reports/yaml_record_review/` found no prior report for this `biofouling` record before this file was written.
