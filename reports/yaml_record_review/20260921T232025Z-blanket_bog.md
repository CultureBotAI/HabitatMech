# YAML Record Review: blanket bog

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/other/blanket_bog.yaml`
- Started UTC: 2026-09-21T23:16:00Z
- Finished UTC: 2026-09-21T23:20:37Z
- Verdict: pass with minor issues

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `ENVO:00000511` |
| Label | `blanket bog` |
| Category | `OTHER` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Maintained inputs | `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, `data/raw/prego_habitats.tsv`, `data/raw/prego_habitat_taxa.tsv`, `data/habitats/PATHS.tsv` |

This is a generated ENVO-grounded record produced from one PREGO source concept. The path lock maps `ENVO:00000511` to `blanket_bog`, the vendored ontology slice supplies the `blanket bog` label, definition, exact synonym, and `ENVO:00000044` parent, and PREGO directly asserts `ENVO:00000511`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/other/blanket_bog.yaml` | Pass; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/other/blanket_bog.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Pass; the committed term-request table is current with 109 terms. |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass; 3,206 expected records, 3,206 records on disk, and 0 missing, extra, or differing records. |
| `git diff --check` | Pass. |

`just validate-products` was not run. This read-only review did not add or change a grounding row, and every ontology identifier on the record was checked directly in `data/raw/ontology_terms.tsv` and `data/raw/ontology_subclass_edges.tsv`.

## Identity and Grounding

The generated identity and exact grounding are supported:

- `data/habitats/PATHS.tsv` pins `ENVO:00000511` to `blanket_bog`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv` labels `ENVO:00000511` as `blanket bog`, carries the copied definition, lists `wetland` as an exact synonym, and keeps the class active.
- PREGO directly asserts the ontology class `ENVO:00000511` with synonyms `blanket bog|blanket bogs`, so `ingest_prego()` resolves the source concept to `ENVO:00000511` with `grounding_status: EXACT`.
- The stable PREGO decision key is `habitatmech:PREGO.4a3f6a9887`, computed by the seeder from `sha1("PREGO:ENVO:00000511")[:10]`.
- There is no `curation/decisions.tsv` row for `habitatmech:PREGO.4a3f6a9887`, so the generated `mapping_status: SEEDED` is expected.
- `data/raw/ontology_subclass_edges.tsv` asserts `ENVO:00000511 rdfs:subClassOf ENVO:00000044`; `data/raw/ontology_terms.tsv` labels `ENVO:00000044` as `peatland`.

The only identity-adjacent mismatch is the coarse category. `infer_category()` categorizes ENVO concepts by a fixed ancestor-anchor list, but that list contains neither `ENVO:00000044` `peatland` nor `ENVO:01001209` `wetland ecosystem`, so this direct PREGO wetland currently falls through to `habitat_category: OTHER`.

## Evidence

Supported claims:

- The definition and `definition_source: ENVO` come verbatim from the vendored ENVO row for `ENVO:00000511`.
- The `wetland` synonym comes from the same ENVO row, and the PREGO plural `blanket bogs` becomes a PREGO `RELATED_SYNONYM`.
- `parent_habitats: ENVO:00000044` comes from the vendored ENVO subclass edge from `blanket bog` to `peatland`.
- The PREGO source id, source label, `assertion_count: 1316`, `assertion_unit: TAXON`, `score: 1.93268`, and `evidence_channels: environmental_samples` come from `data/raw/prego_habitats.tsv`.
- The 25 `characteristic_taxa` entries are copied in rank order from `data/raw/prego_habitat_taxa.tsv`; none is promoted with `is_characteristic: true`.

Unsupported or over-scoped claims:

- The category is generated by a known heuristic fallthrough rather than by ontology evidence or item-level review.

The record has no record-level evidence, environmental parameters, causal graphs, discussion links, or datasets. That absence is supported: ignored-file-inclusive searches found no exact `ENVO:00000511`, `blanket_bog`, `blanket bog`, `blanket bogs`, or `habitatmech:PREGO.4a3f6a9887` entry in `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/causal_graphs/`, `history/`, or prior `reports/yaml_record_review/` reports before this report was written.

## Completeness

The record is complete enough for its current source-backed scope:

- No authored definition or term request is expected because the record uses the existing exact ENVO `blanket bog` term.
- No item-level curation decision exists yet; with one PREGO-only source concept, `mapping_status: SEEDED` is therefore correct.
- No HabitatMech-authored causal graph is expected because no maintained `curation/causal_graphs/` overlay names this habitat.
- No additional characteristic-taxon evidence is expected because the PREGO taxa are presented only as reported associations from PREGO, not as curator-asserted typifying taxa.

All negative checks above used `rg --no-ignore --hidden`, so ignored files were included in the searched paths.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Minor | `habitat_category` falls through to `OTHER` even though `blanket bog` is a peatland/wetland ecosystem that should be reviewable as `TERRESTRIAL`. | `ENVO:00000511` is a subclass of `ENVO:00000044` `peatland`, the vendored `wetland ecosystem` definition explicitly describes a terrestrial ecosystem, and `ENVO_CATEGORY_ANCHORS` has no peatland or wetland anchor for PREGO's direct ENVO rows. | Add an item-level `REVIEW` row for `habitatmech:PREGO.4a3f6a9887` in `curation/decisions.tsv` with `category: TERRESTRIAL`, or review the affected wetland cohort and add a systemic ENVO category anchor in `src/habitatmech/seed.py`. |

## Recommended Edits

1. Add a maintained category override for `habitatmech:PREGO.4a3f6a9887`.

   A record-local fix belongs in `curation/decisions.tsv` as an item-level `REVIEW` row for the PREGO source concept. The row can endorse the seeder's exact `ENVO:00000511` identity, leave `object_id`, `object_label`, `grounding_status`, and `relation` empty, and set `category` to `TERRESTRIAL`.

2. Before adding a systemic seed anchor, review the neighboring PREGO/ENVO wetland terms.

   This direct record shows the heuristic gap, but an anchor on `ENVO:00000044` `peatland` or `ENVO:01001209` `wetland ecosystem` would recategorize every direct ENVO descendant whose category currently comes only from `infer_category()`. That broader choice belongs in `src/habitatmech/seed.py` only after a cohort check.

## Follow-up Checks

After a record-local category override, rerun:

- `just validate data/habitats/terrestrial/blanket_bog.yaml`
- `just validate-strict data/habitats/terrestrial/blanket_bog.yaml`
- `just seed`
- `just seed-canary ENVO:00000511`
- `just seed-apply --force --prune`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If the fix adds a systemic ENVO category anchor, also rerun `just validate-all` and inspect every generated record that moves out of `data/habitats/other/`.

## Additional Notes

An ignored-file-inclusive search of `reports/yaml_record_review/` found no prior report for this `blanket_bog` record before this file was written.
