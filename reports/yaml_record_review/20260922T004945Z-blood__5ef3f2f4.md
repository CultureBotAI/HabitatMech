# YAML Record Review: Blood

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/host_associated/blood__5ef3f2f4.yaml`
- Started UTC: `2026-09-22T00:48:45Z`
- Finished UTC: `2026-09-22T00:49:45Z`
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.d25ff4a586` |
| Label | `Blood` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Generated path | `data/habitats/host_associated/blood__5ef3f2f4.yaml` |
| Source | `gold.ecosystem:3693`, first of two GOLD node IDs on `Host-associated > Mammals: Human > Circulatory system > Blood` |

This is a generated GOLD-derived `HabitatRecord` for the Mammals: Human `Blood` source path. `data/habitats/PATHS.tsv` locks `habitatmech:GOLD.d25ff4a586` to `blood__5ef3f2f4`, so the reviewed YAML is the unique generated file for this human Blood source concept.

The record has one generated `seed_from_sources` curation event at `2026-08-16T05:58:02Z`. No authored definition, environmental parameters, characteristic taxa, discussion entries, datasets, or causal edges are present.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/host_associated/blood__5ef3f2f4.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/host_associated/blood__5ef3f2f4.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 records, found 3206, with 0 missing, 0 extra, and 0 differing records. |
| `git diff --check` | Passed. |

## Identity and Grounding

- The generated identifier, `habitatmech:GOLD.d25ff4a586`, is the minted GOLD identifier for `Host-associated > Mammals: Human > Circulatory system > Blood`.
- `HOST_ASSOCIATED` agrees with the source path's top-level GOLD component.
- The exact GOLD row records leaf label `Blood`, depth 4, two collapsed GOLD node IDs, and 7375 organism assertions for `gold.ecosystem:3693|gold.ecosystem:3932`; the source attestation preserves the first GOLD node ID and the generated note points to `data/raw/gold_ecosystem_paths.tsv` for the collapsed inventory.
- The immediate GOLD source-path parent is `Host-associated > Mammals: Human > Circulatory system`, which `data/habitats/PATHS.tsv` maps to `habitatmech:GOLD.0635cc88f0`.
- The `habitatmech:GOLD.0635cc88f0` parent record represents the same host branch's `Circulatory system` path and attaches `UBERON:0001009` as its broader ontology parent.
- `UBERON:0000178` is present in `data/raw/ontology_terms.tsv` with canonical label `blood`.
- Four same-depth GOLD paths end at `Circulatory system > Blood`: Mammals: Human, Mammals, Birds, and Fish. Under the ambiguous-leaf rule in `docs/HARMONIZATION.md`, each remains minted and keeps `UBERON:0000178` as a broader parent rather than merging into the ontology-grounded `blood` record.

## Evidence

| Claim | Trace |
|---|---|
| GOLD source path and organism count | `data/raw/gold_ecosystem_paths.tsv` contains `Host-associated > Mammals: Human > Circulatory system > Blood`, depth 4, two node IDs, and `7375` organism assertions. |
| Path lock | `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.d25ff4a586` to `blood__5ef3f2f4`. |
| Source-path parent | `data/raw/gold_ecosystem_paths.tsv` contains `Host-associated > Mammals: Human > Circulatory system`; `data/habitats/PATHS.tsv` maps it to `habitatmech:GOLD.0635cc88f0`; `data/habitats/host_associated/circulatory_system__5b3debaf.yaml` is the generated parent record. |
| UBERON broader terms | `data/raw/ontology_terms.tsv` labels `UBERON:0000178` as `blood` and `UBERON:0001009` as `circulatory system`. |
| Ambiguous Blood leaves | `data/raw/gold_ecosystem_paths.tsv` has four depth-4 host-associated `Circulatory system > Blood` paths under Mammals: Human, Mammals, Birds, and Fish. |

Exact ignored/hidden-inclusive searches for `habitatmech:GOLD.d25ff4a586`, `gold.ecosystem:3693`, `blood__5ef3f2f4`, and `Host-associated > Mammals: Human > Circulatory system > Blood` found no maintained `curation/decisions.tsv` decision for the target, no term-request row, no history record, no causal-graph overlay, and no prior YAML review report for this record. The same searches found the expected generated record, `PATHS.tsv` row, raw GOLD inventory row, and generated child records whose source paths extend below this Blood node.

A tab-anchored ignored/hidden-inclusive GOLD triad search found no exact `data/raw/gold_path_triads.tsv` row for this Mammals: Human Blood path. The exact path does appear in `data/raw/gold_path_biosamples.tsv` and several `data/raw/gold_studies.tsv` rows, but those biosample and study inventories do not seed triad-derived slots; GOLD triads exist for the child `Whole blood` path, not for this parent Blood path.

## Completeness

- No authored definition is expected because no `curation/term_requests.tsv` row exists for `habitatmech:GOLD.d25ff4a586`.
- No causal mechanism overlay is expected; `find curation/causal_graphs -name '*blood*' -print` found no blood-specific overlay.
- No characteristic taxa are expected; the record has only one GOLD source attestation and no PREGO or Madin source attestation.
- The generated `SEEDED` mapping status is expected because no item-level `curation/decisions.tsv` row exists for the Mammals: Human Blood source concept.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

None required beyond the normal pull-request checks. If a future curation session promotes this record, the maintained change should be an item-level row in `curation/decisions.tsv`, an optional definition row in `curation/term_requests.tsv`, or a causal overlay in `curation/causal_graphs/`, followed by `just seed`, `just seed-canary habitatmech:GOLD.d25ff4a586`, `just validate-causal-all`, `just validate-strict data/habitats/host_associated/blood__5ef3f2f4.yaml`, `just verify-corpus`, and `just validate-history` if a history record is added.

## Additional Notes

- The reviewed file is the Mammals: Human minted GOLD `Blood` leaf, distinct from the Mammals, Birds, Fish, and UBERON-grounded `blood` records with the same or similar label.
- The source attestation's `assertion_count: 7375` is a GOLD organism count and is comparable only to other GOLD organism counts.
