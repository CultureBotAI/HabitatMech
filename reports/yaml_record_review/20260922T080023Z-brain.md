# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/brain.yaml`
- Started UTC: 2026-09-22T08:00:23Z
- Finished UTC: 2026-09-22T08:00:23Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `BTO:0000142` |
| Label | `brain` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the PREGO inventory plus the vendored ontology slice; generated YAML remains read-only |
| Locked slug | `data/habitats/PATHS.tsv:29` maps `BTO:0000142` to `brain` |

This is the seeded PREGO record for the BRENDA Tissue Ontology `brain` class. The record is exactly grounded because PREGO uses `BTO:0000142` directly as its habitat identifier, and the seeder carries that CURIE through instead of minting a HabitatMech source identifier.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/brain.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/brain.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was staged |

## Identity and Grounding

The exact identity, label, definition, slug, and PREGO attestation are supported by the vendored ontology and PREGO source tables:

- `data/habitats/PATHS.tsv:29` pins `BTO:0000142` to slug `brain`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:144` gives the `BTO:0000142` ontology label `brain`, the generated definition, `directly_referenced: TRUE`, and `label_only: FALSE`.
- `data/raw/prego_habitats.tsv:239` records PREGO source id `BTO:0000142`, vocabulary `BTO`, Biolink type `GrossAnatomicalStructure`, 40 taxon assertions, maximum PREGO score `4`, evidence channels `annotated_genomes_isolates|environmental_samples`, and synonyms `brain|brains|encephalon|encephalonal|encephalons`.
- `src/habitatmech/seed.py` grounds PREGO rows to themselves as `EXACT`, uses the ontology label for `source_label`, copies the raw `taxon_count`, `max_prego_score`, and `channels` into the PREGO attestation, and records raw PREGO taxon rows with their score, rank, and the 40-taxon candidate pool.

`grounding_status: EXACT` is correct because no lexical or curated redirect is involved for PREGO. `mapping_status: SEEDED` is likewise correct because ignored/hidden-inclusive exact searches found no maintained `BTO:0000142` decision row that would upgrade this record to `REVIEWED`.

## Evidence

Every generated scientific claim in this record is source-derived:

| Claim | Nearest source | Review |
|---|---|---|
| Ontology identity, label, definition, and host-associated category inference | `data/raw/ontology_terms.tsv:144` | Supported exactly |
| Locked filename | `data/habitats/PATHS.tsv:29` | Supported exactly |
| PREGO `source_id`, `source_label`, 40-taxon assertion count, score `4.0`, and evidence channels | `data/raw/prego_habitats.tsv:239` | Supported exactly |
| Related synonyms `brains`, `encephalon`, `encephalonal`, and `encephalons` | `data/raw/prego_habitats.tsv:239` | Supported; the raw canonical synonym `brain` collapses against the label |
| Top 25 `characteristic_taxa` rows, scores, ranks, labels, and 40-taxon candidate pools | `data/raw/prego_habitat_taxa.tsv:163-187` | Supported exactly |

The record has no `parent_habitats`, xrefs, environmental parameters, record-level evidence, causal graphs, discussion links, or datasets. Those absences are consistent with exact ignored/hidden-inclusive searches that found no maintained decision, term-request, causal-graph, research, or dataset row for `BTO:0000142`; exact source searches found only two vendored subclass edges where `embryonic brain` and `larval brain` are children of `BTO:0000142`, not parents emitted into this record.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- The sole `source_attestations` entry captures the complete PREGO source id, source label, assertion count, `TAXON` unit, maximum score, and evidence-channel set.
- The synonym list carries the PREGO alternate forms after de-duplicating the canonical `brain` label.
- The 25 emitted `characteristic_taxa` entries are complete for the generated cutoff and retain the raw PREGO ranks from `data/raw/prego_habitat_taxa.tsv:163-187`.
- Every ranked taxon keeps `candidate_pool: 40`, which is the full PREGO taxon count from `data/raw/prego_habitats.tsv:239`.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated target. They found the expected PREGO row, PREGO taxon rows, ontology definition row, subclass edges, path lock, and generated YAML, and no maintained decision, term request, history record, causal-graph overlay, target-specific research report, or prior exact YAML review report for `BTO:0000142`.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if future curation changes the PREGO rollup, BTO vendored terms, path lock, or characteristic-taxon emission:

- `just validate data/habitats/host_associated/brain.yaml`
- `just validate-strict data/habitats/host_associated/brain.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If future review redirects `BTO:0000142` to the parallel UBERON `brain` class, add an explicit `curation/decisions.tsv` row first, regenerate with `just seed`, and verify that the PREGO attestation and all 25 characteristic taxa remain attached to the surviving anatomical record.

## Additional Notes

- `find reports/yaml_record_review -name '*brain*' -print` found no pre-existing exact review report for this record before this file was written.
- `find curation history research reports -name '*brain*' -print` found no target-specific maintained history, research, report, or causal overlay artifacts before this file was written.
- Exact ignored/hidden-inclusive content searches for `BTO:0000142` found only the path lock, generated YAML, raw PREGO habitat row, raw PREGO top-taxa rows, and vendored ontology rows cited above.
