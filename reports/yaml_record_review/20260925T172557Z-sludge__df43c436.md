# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/sludge__df43c436.yaml
- Started UTC: 2026-09-25T17:25:57Z
- Finished UTC: 2026-09-25T17:27:29Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.203ab14b02` |
| Label | `Sludge` |
| Category | `ENGINEERED` |
| Source path | `Engineered > Bioreactor > DHS reactor > Aerobic > Sludge` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv` and `data/habitats/PATHS.tsv`; future item-level review belongs in `curation/decisions.tsv`, and any retained minted definition or parent replacement belongs in `curation/term_requests.tsv`. |

The target is a GOLD-only, generated, `NARROW`, still-`SEEDED` record for the
zero-count sludge child under the aerobic DHS reactor path. The seeder minted a
stable HabitatMech identifier, attached `ENVO:00002044` `sludge` through the
ambiguous-leaf route, inherited `ENVO:00002126` `aerobic bioreactor` from the
immediate GOLD parent path, and represented the one GOLD ecosystem node with
`source_id: gold.ecosystem:8430`.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/habitats/engineered/sludge__df43c436.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/sludge__df43c436.yaml` | Pass: scanned 1 file with 0 files containing `ERROR` and 0 total `ERROR` rows. |
| `just validate-causal curation/causal_graphs/sludge__df43c436.yaml` | Not applicable: the target has no `causal_graphs` block, and `find curation/causal_graphs -maxdepth 1 -type f -name 'sludge__df43c436.yaml' -print` found no target-specific overlay. |
| `just validate-causal-all` | Pass: validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Pass: the term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: expected 3,206 records, found 3,206 on disk, and the corpus reproduces exactly from `data/raw/`. |
| `just worklist --limit 2000 --status all --out /tmp/habitatmech-dhs-aerobic-sludge-worklist.tsv` | Pass: reported 953 ungrounded records, 1,810 decisions on file, and wrote 953 rows to `/tmp/habitatmech-dhs-aerobic-sludge-worklist.tsv`. |
| `just report` | Pass: completed the corpus report for 3,206 records. |
| `git diff --check` | Pass. |
| Reference validator | Not applicable: this generated GOLD-only record has no record-level `evidence`, `characteristic_taxa`, `causal_graphs`, `discussions`, or `datasets` with citation-bearing `EvidenceItem` references. |

## Identity and Grounding

The generated identifier, label, category, source attestation, and slug agree
with the raw GOLD inventory. `data/raw/gold_ecosystem_paths.tsv` has the exact
path `Engineered > Bioreactor > DHS reactor > Aerobic > Sludge` with
`gold_node_count=1`, no direct organism, study, biosample, or total assertions,
and `gold_node_ids=gold.ecosystem:8430`. `data/habitats/PATHS.tsv` maps
`habitatmech:GOLD.203ab14b02` to the stable `sludge__df43c436` slug.

The generated `ENVO:00002044` parent is supported and appropriately broader.
`data/raw/ontology_terms.tsv` labels that term `sludge` and defines it as
residual semi-solid material left from domestic or industrial processes, or
wastewater treatment processes. The target is source-specific DHS aerobic
reactor sludge, so generic sludge is broader rather than exact.

The generated `ENVO:00002126` parent is not supported as a strict broader
habitat. The target leaf names sludge material in the
`Engineered > Bioreactor > DHS reactor > Aerobic` context; that material can be
contained by an aerobic DHS reactor, but it is not a subtype of the bioreactor
itself. The immediate GOLD parent currently resolves to generic
`ENVO:00002126` because `curation/decisions.tsv` exact-grounded
`habitatmech:GOLD.0b917b1e6b` to `aerobic bioreactor`; the prior review of
`data/habitats/engineered/aerobic_bioreactor.yaml` found that DHS-specific
source exact grounding is itself too broad.

No item-level curation row exists for `habitatmech:GOLD.203ab14b02`; the target
has only its generated seed event. That means the `NARROW` state is
explainable from the seeder's ambiguous-leaf and source-parent routes, but it
has not been endorsed by a curator.

## Evidence

The sole `GOLD` source attestation mirrors
`data/raw/gold_ecosystem_paths.tsv`: source ID `gold.ecosystem:8430`, source
label `Sludge`, source path
`Engineered > Bioreactor > DHS reactor > Aerobic > Sludge`, and
`mapping_predicate: skos:narrowMatch`.

Exact gitignore-independent searches for the target source path in
`data/raw/gold_path_triads.tsv`, `data/raw/environment_parameters.tsv`,
`data/raw/gold_studies.tsv`, and `data/raw/gold_path_biosamples.tsv` found no
target-specific side-table rows.

Unsupported or over-scoped generated claims:

- `ENVO:00002126` as a `parent_habitats` entry is an over-scoped
  source-context edge. `ENVO:00002126` names an aerobic bioreactor, not sludge
  material contained by an aerobic DHS reactor.

## Completeness

The generated record carries the expected source-owned values for the current
raw inputs: a minted identifier, GOLD label, `ENGINEERED` category, inherited
`sludge` and `aerobic bioreactor` parents, a single GOLD source attestation, a
seed event, and a stable slug lock.

The curation state is incomplete. Exact `rg --no-ignore --hidden` searches for
`habitatmech:GOLD.203ab14b02`, `gold.ecosystem:8430`, the target source path,
and `sludge__df43c436.yaml` under the maintained source and review areas found
the generated YAML record, the raw GOLD row, the slug-lock row, and one
pre-existing parent-focused report mention, but no item-level decision,
`curation/term_requests.tsv` definition, target-specific history, research
report, or maintained causal overlay. Exact `find` searches found no
target-specific causal overlay and no prior slug-named YAML review report.
Ignored files were included in all of those absence checks.

Optional characteristic-taxon, environmental-parameter, DOI/PMID/URL evidence,
dataset, discussion, and causal-graph slots are correctly absent for this GOLD
child in the generated record. The direct GOLD path contributes no organism,
study, biosample, triad, or environmental-parameter rows that could populate
those slots for `sludge__df43c436`.

## Findings

- **major**: `habitatmech:GOLD.203ab14b02` has no item-level review, so its
  NARROW status and source-specific sludge identity remain seeded rather than
  curated.

  **Evidence**: The target GOLD row is a distinct zero-count `Sludge` child
  under `Engineered > Bioreactor > DHS reactor > Aerobic`, while the exact
  `ENVO:00002044` `sludge` identity is already claimed by the generic ontology
  record. Exact gitignore-independent searches found no target-specific row in
  `curation/decisions.tsv`, no `curation/term_requests.tsv` definition, and no
  target-specific history or research report.

  **Owner**: `curation/decisions.tsv` should hold the item-level decision; if
  the item-level review keeps the minted identifier, `curation/term_requests.tsv`
  should hold its definition and strict parent.

- **major**: `parent_habitats` asserts that this DHS aerobic-reactor sludge is
  a kind of `ENVO:00002126` `aerobic bioreactor`.

  **Evidence**: The inherited parent comes from GOLD's `Engineered >
  Bioreactor > DHS reactor > Aerobic` path segment. `ENVO:00002126` names a
  bioreactor in which the contained material is well-oxygenated, while this
  child leaf names sludge material in the aerobic-DHS-reactor context. The
  record has no item-level decision, term request, MIxS triad, causal overlay,
  or source side-table row saying the sludge is itself a reactor.

  **Owner**: `curation/term_requests.tsv` should replace the generated
  source-path parent with `ENVO:00002044` or a tighter inspected material genus
  if the item-level decision confirms the target is a real minted habitat.

No blockers found.

No minor findings.

## Recommended Edits

1. In `curation/decisions.tsv`, add an item-level review for
   `habitatmech:GOLD.203ab14b02` scoped to
   `Engineered > Bioreactor > DHS reactor > Aerobic > Sludge`, the broader
   `ENVO:00002044` `sludge` parent, and the contextual `ENVO:00002126`
   `aerobic bioreactor` edge.

2. If item review keeps this as source-specific DHS aerobic-reactor sludge,
   define it in `curation/term_requests.tsv` under `ENVO:00002044` or a tighter
   inspected material genus, and use `parent_mode=REPLACE` if the review
   confirms that `ENVO:00002126` is contextual rather than strict.

3. Regenerate the corpus from those maintained inputs so the generated record
   records the curator decision and no longer carries a false
   aerobic-bioreactor `is-a` edge.

## Follow-up Checks

After curation, rerun:

- `just term-requests-check`
- `just validate data/habitats/engineered/sludge__df43c436.yaml`
- `just validate-strict data/habitats/engineered/sludge__df43c436.yaml`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000 --status all --out /tmp/habitatmech-dhs-aerobic-sludge-worklist.tsv`

## Additional Notes

- The false `ENVO:00002126` edge is downstream of the
  `habitatmech:GOLD.0b917b1e6b` `Engineered > Bioreactor > DHS reactor >
  Aerobic` exact grounding. Correcting that parent concept from exact to
  source-specific narrow grounding would preserve `ENVO:00002126` as a true
  parent of an aerobic DHS reactor record, but it would not by itself remove
  `ENVO:00002126` from this sludge material child.
- This target follows the same contextual-parent pattern already observed for
  zero-count anaerobic-bioreactor material leaves: source-path reactor context
  becomes a false parent when the leaf is a material rather than the reactor
  device.
