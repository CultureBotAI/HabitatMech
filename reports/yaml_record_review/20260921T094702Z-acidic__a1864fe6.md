# YAML Record Review: Acidic

- Repository: HabitatMech
- Record: data/habitats/aquatic/acidic__a1864fe6.yaml
- Started UTC: 2026-09-21T09:47:02Z
- Finished UTC: 2026-09-21T09:47:38Z
- Verdict: Needs follow-up

## Target

The target record is `habitatmech:GOLD.cfe5d67257`, labeled `Acidic`, in `data/habitats/aquatic/acidic__a1864fe6.yaml`.

It represents the GOLD source concept `gold.ecosystem:4546` at:

`Environmental > Aquatic > Thermal springs > Warm (34-42C) > Acidic`

The record is currently:

- `habitat_category: AQUATIC`
- `grounding_status: NOT_APPLICABLE`
- `mapping_status: REVIEWED`
- `parent_habitats: habitatmech:GOLD.486b52b120`

## Validation

The record and generated corpus validate cleanly:

- `just validate data/habitats/aquatic/acidic__a1864fe6.yaml`: no LinkML issues.
- `just validate-strict data/habitats/aquatic/acidic__a1864fe6.yaml --quiet`: 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows.
- `just term-requests-check`: term-request table is current with 109 terms.
- `just validate-history`: 77 history records valid against `src/habitatmech/schema/history.yaml`.
- `just verify-corpus --max-diffs 1`: expected and found 3,206 records, with 0 missing, 0 extra, and 0 differing.

## Identity and Grounding

The reviewed `NOT_APPLICABLE` disposition is correct. In GOLD, this `Acidic` node is a terminal pH-band qualifier beneath `Warm (34-42C)`, not an independently grounded aquatic habitat.

The exact source concept is reviewed in `curation/decisions.tsv` with:

- `decision: NOT_APPLICABLE`
- `scope: ITEM`
- note: `A pH band qualifying the parent node, not a habitat in its own right. Belongs in environmental_parameters. Path: Environmental > Aquatic > Thermal springs > Warm (34-42C) > Acidic`

That reviewed decision is also reflected in the YAML `curation_history`. The independent raw BacDive grounding for the standalone string `Acidic` maps to `PATO:0001429`, which corroborates that the term denotes an acidity quality rather than a habitat class when it appears as a leaf qualifier.

The sole identity defect is hierarchical: the reviewed pH qualifier still has `parent_habitats: habitatmech:GOLD.486b52b120`, and that parent is itself reviewed `NOT_APPLICABLE` because `Warm (34-42C)` is a temperature band.

## Evidence

The raw GOLD inventory contains a direct row for this exact source path at depth 5 with leaf `Acidic`, `gold_node_count=1`, `organism_count=0`, `total_assertions=0`, and node `gold.ecosystem:4546`.

The same exact source path also appears in raw GOLD biosample and MIxS triad tables. Those rows attach two biosamples to the aquatic biome, hot spring, and spring-water context, which supports the path interpretation as a warm thermal spring with an added pH qualifier. They do not make `Acidic` a habitat in its own right.

A raw GOLD studies row includes the exact path among a multi-path study. It is useful source context, but the row is not stronger than the reviewed item-level curation decision.

Ignored and hidden files were included when searching `curation`, `data`, `research`, and `reports` for the target identifier, `gold.ecosystem:4546`, the exact source path, and `acidic__a1864fe6`. Those searches found the expected maintained decision, raw GOLD source rows, generated YAML, generated path map, rendered page, and sibling review mentions; they did not find target-specific causal-graph or literature-research files.

## Completeness

The sparse YAML is complete for a reviewed source concept that has been rejected as a non-habitat qualifier:

- No `definition` is needed because the source concept is not a HabitatMech habitat.
- No term request is needed because the source concept should not be grounded as an ontology class.
- No `environmental_parameters`, characteristic taxa, evidence items, datasets, or causal graph are needed on this rejected record.
- Omitting `assertion_count` is consistent with the exact raw GOLD path having zero organism assertions.
- The `source_attestations` block still preserves the original GOLD identifier, label, and full path needed to audit the rejection.

`parent_habitats` is the only field that is not complete in the current model: it encodes the source-path parent as an is-a relationship even after the child and parent were both reviewed as measurement qualifiers rather than habitats.

## Findings

- **Major - `parent_habitats` keeps a non-habitat source-path edge.** `acidic__a1864fe6.yaml` declares `habitatmech:GOLD.486b52b120` as a parent even though `docs/CURATION.md` defines `parent_habitats` as is-a ancestry and the parent `Warm (34-42C)` record is reviewed `NOT_APPLICABLE`. The emitted edge says a pH quality is a kind of temperature quality, and also lets two rejected qualifier concepts appear as habitat hierarchy nodes. The edge appears to be generated from `src/habitatmech/seed.py`, which unconditionally copies a GOLD path parent into `parent_habitats` before writing the collected parents back to YAML.

## Recommended Edits

Update the maintained seeding layer to suppress this generated parent edge when a source path item has been reviewed `NOT_APPLICABLE` because it denotes an environmental parameter instead of a habitat. A narrower alternative is to add a maintained parent-edge suppression table consumed by `src/habitatmech/seed.py`, then suppress:

- `Environmental > Aquatic > Thermal springs > Warm (34-42C) > Acidic`
- the sibling warm thermal-spring `Neutral` pH-band edge
- any other reviewed pH-band or temperature-band source concepts that currently inherit source path ancestry as `parent_habitats`

After regenerating, this YAML should preserve the GOLD source attestation and reviewed rejection while dropping the `parent_habitats` relationship to `Warm (34-42C)`.

Add a regression that fails if an item-reviewed `NOT_APPLICABLE` pH or temperature qualifier is emitted as a habitat child of another reviewed qualifier.

No hand edit is recommended for `grounding_status`, `mapping_status`, source attestation, or curation history.

## Follow-up Checks

- Regenerate the corpus after suppressing the edge and confirm `data/habitats/aquatic/acidic__a1864fe6.yaml` no longer lists `habitatmech:GOLD.486b52b120` in `parent_habitats`.
- Recheck `neutral__819b9d53.yaml` and the other pH-band leaves because they share the same reviewed non-habitat pattern.
- Confirm legitimate habitat children under `Warm (34-42C)`, such as the `Sediment` source branch, keep appropriate habitat ancestry.
- Rebuild downstream KGX and SSSOM outputs and confirm the suppressed qualifier edge is absent from exported products.

## Additional Notes

This review intentionally makes no curation-history or generated-data edits. The YAML accurately records the reviewed rejection of GOLD's `Acidic` leaf; the follow-up belongs in the maintained hierarchy-generation logic that is still converting reviewed qualifier path structure into `parent_habitats` assertions.
