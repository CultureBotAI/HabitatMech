# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/sediment__cb692bfb.yaml
- Started UTC: 2026-09-24T20:08:00Z
- Finished UTC: 2026-09-24T20:10:55Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.6d45562fe0` |
| Label | `Sediment` |
| Category | `ENGINEERED` |
| Grounding | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained source | generated from `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/PATHS.tsv`, and GOLD source-path hierarchy |

Reviewed the complete generated record at `data/habitats/engineered/sediment__cb692bfb.yaml`. The record is a GOLD-only habitat for `Engineered > Artificial ecosystem > Aquaculture > Fish tank > Sediment`, with displayed source ID `gold.ecosystem:8005`, zero GOLD organism, study, or biosample assertions, a broader `ENVO:00002007` sediment parent, and a generated source-path parent to the `Engineered > Artificial ecosystem > Aquaculture > Fish tank` record. The YAML is generated output; future item-review belongs in `curation/decisions.tsv`, and any future coined definition or inherited-parent replacement belongs in `curation/term_requests.tsv`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/sediment__cb692bfb.yaml` | Passed |
| `just validate-strict data/habitats/engineered/sediment__cb692bfb.yaml` | Passed |
| Focused causal overlay validator | Not applicable; exact ignored-inclusive searches in `curation/causal_graphs/` found no `habitatmech:GOLD.6d45562fe0`, `gold.ecosystem:8005`, or exact Fish tank sediment source-path overlay |
| `just validate-causal-all` | Passed; 32 files / 32 graphs |
| Reference validator | Not applicable; the generated GOLD-only record has no record-level `evidence` items, no causal graph, and no environmental-parameter references |
| `just term-requests-check` | Passed; 109 terms |
| `just validate-history` | Passed; 77 history records |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected / 3,206 found / 0 missing / 0 extra / 0 differing |
| `just worklist --limit 2000` | Passed; 0 ungrounded concepts remain undecided and 1,810 decisions are present |
| `just report` | Passed; 3,206 habitat records summarized |
| `git diff --check` | Passed |

## Identity and Grounding

The generated identifier, label, category, source path, zero-assertion shape, and source ID agree with the committed GOLD inventory. `data/raw/gold_ecosystem_paths.tsv` has the canonical path `Engineered > Artificial ecosystem > Aquaculture > Fish tank > Sediment`, `depth` 5, `gold_node_count` 1, `organism_count` 0, `study_count` 0, `biosample_count` 0, `total_assertions` 0, and `gold_node_ids` `gold.ecosystem:8005`.

The record's `NARROW` relationship to `ENVO:00002007` `sediment` is sound. GOLD has many distinct deeper paths with the `Sediment` leaf; `docs/HARMONIZATION.md` explains that these ambiguous leaves keep minted identifiers and carry generic `sediment` as a broader parent instead of all collapsing into the ontology record `ENVO:00002007`.

The generated Fish tank parent is not a sound broader habitat. It records the direct GOLD source path, but `parent_habitats` is an `is_a` relationship in this corpus: sediment within a fish tank is a sediment material in a fish-tank context, not a kind of the fish tank container itself. That context should be handled by an item-level review and a coined definition that keeps the true `ENVO:00002007` genus while replacing the inherited source-path parent.

## Evidence

- Supported: the source leaf `Sediment`, full `Engineered > Artificial ecosystem > Aquaculture > Fish tank > Sediment` path, displayed `gold.ecosystem:8005` source ID, and zero upstream assertion counts match `data/raw/gold_ecosystem_paths.tsv`.
- Supported: `ENVO:00002007` `sediment` is present in `data/raw/ontology_terms.tsv` with label `sediment`, and `data/habitats/PATHS.tsv` maps the ontology record to the stable slug `sediment`.
- Supported: exact ignored-inclusive searches across `data/raw` for `gold.ecosystem:8005` and the full Fish tank sediment source path found only the canonical `gold_ecosystem_paths.tsv` row; no direct GOLD biosample, MIxS triad, or study side-table row needs a narrower audit for the target.
- Supported: exact ignored-inclusive searches over `data/habitats/PATHS.tsv`, `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/term_requests/`, `history`, `research`, `conf/id_label_targets.yaml`, `curation/causal_graphs`, and `reports/yaml_record_review` found no target-specific decision row, term request, history entry, research report, label-correspondence exception, or causal overlay for `habitatmech:GOLD.6d45562fe0`.
- Unsupported: the GOLD source path establishes that this sediment node sits under Fish tank in the source tree; it does not establish that a sediment material is a subtype of the tank that contains it.
- No record-level citations, characteristic taxa, environmental parameters, or causal edges are present, so there are no attached snippets or literature references to validate.

## Completeness

The record is structurally sparse but not missing any required generated field. It has the expected GOLD source attestation and the expected `skos:narrowMatch` relationship to `ENVO:00002007` for a path-specific sediment source concept with zero committed GOLD organism, study, or biosample assertions.

The exact ignored-inclusive target searches covered `data/habitats/PATHS.tsv`, `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/term_requests/`, `history`, `research`, `conf/id_label_targets.yaml`, `curation/causal_graphs`, `reports/yaml_record_review`, and focused GOLD source inventories under `data/raw`. They found the generated target, the `data/habitats/PATHS.tsv` row, the canonical GOLD ecosystem-path row, and the prior Fish tank review's follow-up references to this child; they found no existing review report for this exact stem before this report was written and no item-level decision, term request, history entry, research report, label-correspondence exception, direct GOLD biosample/triad/study side-table row, or target-specific causal overlay.

The immediate source-path parent at `data/habitats/engineered/fish_tank.yaml` has also been reviewed. That review found its generated GOLD source attestation internally consistent and flagged the class-level Fish tank `UNGROUNDED` decision for item-level comparison against `ENVO:00002196` `aquarium`, but no possible outcome of Fish tank grounding makes a sediment material into a subtype of the tank.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `sediment__cb692bfb.yaml` inherits `habitatmech:GOLD.1a33387187` `Fish tank` as a parent, turning GOLD's sediment-in-tank path into the claim that the sediment material is a kind of fish tank. | The target GOLD path is `Engineered > Artificial ecosystem > Aquaculture > Fish tank > Sediment`, and the generated target already keeps the true broader `ENVO:00002007` `sediment` parent. `CLAUDE.md` and `docs/CURATION.md` both reserve `parent_habitats` for strictly broader habitats; a contained sediment material is not a subtype of the engineered tank that contains it. | `curation/decisions.tsv` and `curation/term_requests.tsv` |

## Recommended Edits

1. Item-review `habitatmech:GOLD.6d45562fe0` in `curation/decisions.tsv` against the exact `Engineered > Artificial ecosystem > Aquaculture > Fish tank > Sediment` source path, the existing `ENVO:00002007` sediment parent, and the generated `habitatmech:GOLD.1a33387187` Fish tank parent.
2. Add a `curation/term_requests.tsv` definition for fish-tank sediment under `ENVO:00002007` `sediment` with `parent_mode=REPLACE`, so regeneration keeps the true sediment genus and drops the false Fish tank source-path parent.
3. Canary-regenerate `habitatmech:GOLD.6d45562fe0` and inspect `data/habitats/engineered/sediment__cb692bfb.yaml` to confirm the inherited Fish tank parent is gone and the `ENVO:00002007` parent remains.

## Follow-up Checks

- `just seed`
- `uv run python scripts/seed_from_sources.py --apply --force --only habitatmech:GOLD.6d45562fe0`
- Inspect `data/habitats/engineered/sediment__cb692bfb.yaml`
- `just validate data/habitats/engineered/sediment__cb692bfb.yaml`
- `just validate-strict data/habitats/engineered/sediment__cb692bfb.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

## Additional Notes

- No blocker finding was found: the current generated YAML validates, its GOLD source attestation is internally consistent, and the record denotes engineered fish-tank sediment rather than a non-habitat.
- No minor findings were found.
