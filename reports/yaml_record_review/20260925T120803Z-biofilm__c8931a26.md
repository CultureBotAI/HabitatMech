# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/biofilm__c8931a26.yaml
- Started UTC: 2026-09-25T12:08:03Z
- Finished UTC: 2026-09-25T12:08:03Z
- Verdict: needs curation

## Target

Reviewed the complete generated record at `data/habitats/engineered/biofilm__c8931a26.yaml`. The record is the GOLD-only engineered child for `Engineered > Artificial ecosystem > Vivarium > Seawater aquarium > Biofilm`, currently carried as the minted identifier `habitatmech:GOLD.667125d07b`.

The generated file has `label: Biofilm`, `habitat_category: ENGINEERED`, `grounding_status: NARROW`, and `mapping_status: SEEDED`. Its generated parents are `ENVO:00002034` `biofilm`, from the ambiguous GOLD leaf match, and `habitatmech:GOLD.c483f43031` `Seawater aquarium`, from the immediate GOLD source-path parent. Its only source attestation is `gold.ecosystem:8042` with `mapping_predicate: skos:narrowMatch`.

The file is generated from `data/raw/` and seeded path logic. No `curation/decisions.tsv` row, authored term request, source extractor override, or causal-graph overlay currently contributes to this record. Future item review belongs in `curation/decisions.tsv`; a future path-qualified label, definition, or parent replacement belongs in `curation/term_requests.tsv`; future causal overlays belong under `curation/causal_graphs/`.

## Validation

All required local checks passed:

- `just validate data/habitats/engineered/biofilm__c8931a26.yaml`
- `just validate-strict data/habitats/engineered/biofilm__c8931a26.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000 --status all --out /tmp/habitatmech-seawater-aquarium-biofilm-worklist.tsv`
- `just report`

No validator was skipped.

## Identity and Grounding

The generated identifier, label, category, source attestation, and path slug agree with the maintained inputs. `data/raw/gold_ecosystem_paths.tsv` has the exact `Engineered > Artificial ecosystem > Vivarium > Seawater aquarium > Biofilm` row at depth 5, with one GOLD node ID, zero `organism_count`, `study_count`, `biosample_count`, and `total_assertions` values in this bulk-export summary, and `gold.ecosystem:8042` as the node list. `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.667125d07b` to the stable `biofilm__c8931a26` slug.

The `ENVO:00002034` parent is a plausible broader term. The vendored ontology slice defines `ENVO:00002034` with canonical label `biofilm`, and the GOLD source path adds a Seawater aquarium context under that generic leaf. `docs/HARMONIZATION.md` documents that deeper repeated GOLD leaves keep a minted identifier and use the matched leaf term as a broader parent instead of claiming the ontology term as an exact identity for every context-specific child.

The inherited `habitatmech:GOLD.c483f43031` Seawater aquarium parent is not yet defensible as a strict `is-a` parent. The raw row places the Biofilm leaf under Seawater aquarium, but a seawater-aquarium biofilm is attached biomass within that engineered aquarium context, not a kind of aquarium. This is the same path-qualified Biofilm pattern that already led the curated air-scrubber Biofilm term request to use `ENVO:00002034` as the genus and `parent_mode=REPLACE` to drop its inherited equipment parent.

This target has no item-level decision. Exact ignored-inclusive searches found no `habitatmech:GOLD.667125d07b`, `gold.ecosystem:8042`, `source_id: gold.ecosystem:8042`, or exact path-specific term request in maintained curation inputs.

## Evidence

- Supported as generated source provenance: exact ignored-inclusive searches found `gold.ecosystem:8042` and the exact tab-delimited `Engineered > Artificial ecosystem > Vivarium > Seawater aquarium > Biofilm` row in `data/raw/gold_ecosystem_paths.tsv`.
- Supported as broader ontology context: `ENVO:00002034` is present in the vendored ontology slice as `biofilm`, and a path-qualified GOLD Biofilm child is narrower than the generic ontology class.
- Supported as generated context but not as a strict parent: `habitatmech:GOLD.c483f43031` is the immediate parent source path, which explains why it is generated, but the GOLD containment/context path has not been item-reviewed into a true `parent_habitats` claim.
- Not yet supported as individually reviewed habitat identity: no item-level `curation/decisions.tsv` row decides whether this source concept should stay as a specific seawater-aquarium biofilm, become a different exact ontology grounding, or be handled some other way.

The record has no definition, environmental parameters, characteristic taxa, claim-level `EvidenceItem` objects, causal graphs, discussions, or datasets to inspect.

## Completeness

The generated source attestation is complete for the committed GOLD ecosystem inventory. `data/raw/gold_ecosystem_paths.tsv` lists a single source node ID for this exact path, so no generated note is needed to account for collapsed sibling nodes.

Exact ignored-inclusive searches of the GOLD API side tables found no `gold.ecosystem:8042` and no exact tab-delimited `Engineered > Artificial ecosystem > Vivarium > Seawater aquarium > Biofilm` row in `data/raw/gold_studies.tsv`, `data/raw/gold_path_biosamples.tsv`, or `data/raw/gold_path_triads.tsv`; no GOLD study, biosample, or MIxS triad context is committed for this exact path.

Before this report was written, exact ignored-inclusive searches over `history/`, `research/habitats/`, `curation/causal_graphs/`, `curation/term_requests.tsv`, `curation/term_requests/`, `curation/term_requests_excluded.tsv`, `conf/id_label_targets.yaml`, and `reports/yaml_record_review/` found no target-specific append-only history record, research report, causal overlay, authored term request, generated ENVO term-request row, term-request exclusion, or label-correspondence residual for `habitatmech:GOLD.667125d07b` or `gold.ecosystem:8042`. The exact `biofilm__c8931a26` and `Engineered > Artificial ecosystem > Vivarium > Seawater aquarium > Biofilm` searches found a prior air-scrubber Biofilm research report that only mentions this YAML file as a comparison point; it is not target-specific evidence. Before this report was written, `find reports/yaml_record_review -maxdepth 1 -type f -name '*biofilm__c8931a26*.md' -print` found no prior review report for this exact generated stem.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `Engineered > Artificial ecosystem > Vivarium > Seawater aquarium > Biofilm` is still only a seeded ambiguous-leaf child, and its generated `habitatmech:GOLD.c483f43031` Seawater aquarium parent is likely a contextual location rather than a true `is-a` parent. | `data/raw/gold_ecosystem_paths.tsv` has this source concept as the `Biofilm` child of `Engineered > Artificial ecosystem > Vivarium > Seawater aquarium`; `curation/decisions.tsv` has no item-level row for `habitatmech:GOLD.667125d07b`; `curation/term_requests.tsv` already defines the reviewed air-scrubber Biofilm under `ENVO:00002034` with `parent_mode=REPLACE` because the biofilm is inside the scrubber but not a kind of scrubber. | `curation/decisions.tsv` and `curation/term_requests.tsv`. |

## Recommended Edits

1. Item-review `habitatmech:GOLD.667125d07b` against the exact `Engineered > Artificial ecosystem > Vivarium > Seawater aquarium > Biofilm` GOLD path, `ENVO:00002034` `biofilm`, and the immediate `habitatmech:GOLD.c483f43031` Seawater aquarium context.
2. If the source concept is a seawater-aquarium biofilm, add an `ITEM` `CONFIRM_UNGROUNDED` decision in `curation/decisions.tsv` for `habitatmech:GOLD.667125d07b`, because no exact term in the vendored slice currently names the path-qualified child.
3. In that same future curation session, add a `curation/term_requests.tsv` row that defines the record under `ENVO:00002034` `biofilm`; use `parent_mode=REPLACE` if item review confirms the generated Seawater aquarium parent is contextual.
4. Add an append-only `history/` record for that future curation session.

## Follow-up Checks

- After updating `curation/decisions.tsv` and `curation/term_requests.tsv`, run `just seed`, `just seed-canary habitatmech:GOLD.667125d07b`, and inspect the regenerated `data/habitats/engineered/biofilm__c8931a26.yaml`.
- If the canary is correct, run `just seed-apply --force`, then `just validate-strict data/habitats/engineered/biofilm__c8931a26.yaml`, `just term-requests-check`, `just validate-history`, `just validate-causal-all`, `just verify-corpus --max-diffs 1`, and `just report`.
- If the future item review adds a causal overlay, run the focused `just validate-causal curation/causal_graphs/<slug>.yaml` before `just validate-causal-all`.

## Additional Notes

All absence checks in this review used ignored-inclusive `rg --no-ignore --hidden`, exact scans of the committed raw GOLD side tables, or `find`, bounded to maintained curation, history, research, raw inventory, generated habitat, label-residual, and review-report paths. No search depended on rendered `pages/` or text-map JSON.

The generic `ENVO:00002034` Biofilm record and its `curation/causal_graphs/biofilm.yaml` overlay remain out of scope for this single-record review. The generic mechanism overlay can inform future curation, but it does not prove a mechanism for the Seawater aquarium-specific child and does not decide whether the Seawater aquarium source-path parent is a true superclass.

The immediate Seawater aquarium parent has already been reviewed with a `needs curation` verdict in `reports/yaml_record_review/20260925T114550Z-seawater_aquarium.md`. Future curation of that parent should not block item review of this child: whether Seawater aquarium later grounds to `ENVO:00002197` `saline water aquarium` or remains minted, the generated child still needs a strict-parent check of the Biofilm-to-aquarium edge.
