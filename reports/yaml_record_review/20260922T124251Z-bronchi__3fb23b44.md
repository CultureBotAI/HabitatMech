# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/bronchi__3fb23b44.yaml`
- Started UTC: 2026-09-22T12:42:51Z
- Finished UTC: 2026-09-22T12:42:51Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.67ad6b3f36` |
| Label | `Bronchi` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv` through the automatic GOLD ambiguous-leaf route |
| Locked slug | `data/habitats/PATHS.tsv:2040` maps `habitatmech:GOLD.67ad6b3f36` to `bronchi__3fb23b44` |

This is the GOLD path record for `Host-associated > Mammals: Human > Respiratory system > Bronchi`. The generated YAML keeps it as a path-specific minted record with `skos:narrowMatch` to `UBERON:0002185` because `Bronchi` is an ambiguous GOLD leaf shared by Mammals, Mammals: Human, and Birds rows. The leaf now needs item-level curation: the source concept denotes the human bronchial anatomy, `bronchi` is an exact UBERON synonym of `bronchus`, and the exact `UBERON:0002185` record already carries a reviewed BacDive bronchial-wash attestation.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/bronchi__3fb23b44.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/bronchi__3fb23b44.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The generated identifier, label, host-associated category, UBERON and GOLD parents, path lock, and GOLD source attestation are supported by maintained inputs:

- `data/raw/gold_ecosystem_paths.tsv:570` is the raw GOLD row for this record. Its canonical path is `Host-associated > Mammals: Human > Respiratory system > Bronchi`; its leaf label is `Bronchi`; it is a depth-4 path with 2 GOLD node ids, `gold.ecosystem:6185|gold.ecosystem:6186`, 8 organism assertions, and no aggregate study or biosample assertions.
- `data/raw/gold_path_biosamples.tsv:189` records 130 biosamples for the target path's `6186` node, and `data/raw/gold_studies.tsv:2089` records the single-path GOLD study `Gs0135095`.
- No `curation/decisions.tsv` row addresses `habitatmech:GOLD.67ad6b3f36`; the generated `NARROW` status comes from the ambiguous `Bronchi` leaf rather than from a curator-authored decision.
- `data/habitats/PATHS.tsv:2040` pins `habitatmech:GOLD.67ad6b3f36` to slug `bronchi__3fb23b44`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:13131` provides `UBERON:0002185` `bronchus`, whose exact synonym list includes `bronchi`; `data/habitats/PATHS.tsv:1067` pins that ontology record to `bronchus`.
- `data/raw/isolation_source_groundings.tsv:52` maps BacDive `Bronchial-wash` to `UBERON:0002185` with `skos:closeMatch`, and `curation/decisions.tsv:1544` item-reviews that BacDive source concept because a bronchial wash samples the bronchus.
- The source path parent `Host-associated > Mammals: Human > Respiratory system` mints to `habitatmech:GOLD.e36ba981d1`; `data/habitats/PATHS.tsv:2979` pins that parent to `respiratory_system__c59e08b7`, and `data/raw/gold_ecosystem_paths.tsv:21` is the raw parent row.

`grounding_status: NARROW` and `mapping_status: SEEDED` are mechanically correct for an undecided ambiguous GOLD leaf, but they are not complete curation. This source path has four raw child rows under `Host-associated > Mammals: Human > Respiratory system > Bronchi`: `Bronchial epithelial cells`, `Primary`, `Secondary`, and `Tertiary`. Those child paths depend on this exact minted parent today, so a future grounding decision must preserve their hierarchy through the exact `UBERON:0002185` parent.

## Evidence

Every generated claim in this record is source-derived or inferred by the seeder:

| Claim | Nearest source | Review |
|---|---|---|
| Minted record identifier `habitatmech:GOLD.67ad6b3f36` | `src/habitatmech/seed.py`, `data/raw/gold_ecosystem_paths.tsv:570` | Supported exactly by `mint("GOLD", canonical_path)` |
| Label `Bronchi`, host-associated category, GOLD source id, source path, 2-node note, 8-organism count, and `ORGANISM` unit | `data/raw/gold_ecosystem_paths.tsv:570` | Supported exactly |
| `NARROW` status, `skos:narrowMatch`, and parent `UBERON:0002185` | GOLD leaf-label matching in `src/habitatmech/seed.py`, backed by `data/raw/ontology_terms.tsv:13131` | Supported as an automatic ambiguity fallback; not item-reviewed |
| Parent `habitatmech:GOLD.e36ba981d1` | GOLD parent-path emission in `src/habitatmech/seed.py`, backed by `data/raw/gold_ecosystem_paths.tsv:21` | Supported exactly |
| `SEEDED` mapping status and only a seed-from-sources history event | Absence of a `curation/decisions.tsv` row for `habitatmech:GOLD.67ad6b3f36` | Supported exactly |
| Locked filename | `data/habitats/PATHS.tsv:2040` | Supported exactly |

The record has no definition, synonyms, xrefs, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussion links, or datasets. Those absences are consistent with this unreviewed single-source GOLD record. Exact side-table lookups found one biosample row and one study row for this exact path, and found no exact `Host-associated > Mammals: Human > Respiratory system > Bronchi` row in `data/raw/gold_path_triads.tsv`.

## Completeness

The generated slots are faithful to the maintained inputs that feed this record, but one curation decision is missing:

- The sole `source_attestations` entry captures the complete target GOLD path, both upstream node ids through the multi-node note, and all 8 GOLD organism assertions.
- The UBERON parent preserves the automatic `Bronchi` synonym match without letting the seeder conflate the Mammals, Mammals: Human, and Birds `Bronchi` rows.
- The GOLD parent list preserves the exact source parent `Host-associated > Mammals: Human > Respiratory system`, so this node can still parent its GOLD child rows while unreviewed.
- `data/raw/gold_ecosystem_paths.tsv:2418-2421` are the four exact child rows below this target; the generated `bronchial_epithelial_cell.yaml`, `lobar_bronchus.yaml`, `primary.yaml`, and `tertiary.yaml` records all currently name `habitatmech:GOLD.67ad6b3f36` as a parent.
- The same-depth Mammals and Birds `Bronchi` sibling rows explain why the seeder did not claim `UBERON:0002185` automatically, but they do not feed this target.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated target, excluding generated `build`, `data/text_map`, and `pages` trees. They found the target raw GOLD row, biosample side-table row, study side-table row, path lock, generated YAML, exact `UBERON:0002185` ontology row, BacDive bronchial-wash grounding and source rows, the reviewed `bronchus.yaml` record, four generated child records, and the previously added Mammals `Bronchi` sibling review; they found no target-specific maintained decision, term request, causal overlay, history record, research report, GOLD triad row, or prior exact YAML review report.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Major | `Host-associated > Mammals: Human > Respiratory system > Bronchi` remains a minted `NARROW` child of `UBERON:0002185` even though the exact matched ontology class is `bronchus`, its exact synonyms include `bronchi`, and the corpus already attaches a reviewed BacDive bronchial-wash source to `UBERON:0002185`. The generated state is a faithful ambiguity fallback, but without an item-level decision it leaves an exact human bronchus attestation outside the reviewed bronchus record and forces child GOLD paths to parent through an unreviewed duplicate. | Add an `ITEM`-depth `curation/decisions.tsv` row for `habitatmech:GOLD.67ad6b3f36`, most likely `GROUND UBERON:0002185 bronchus EXACT`, after confirming the human GOLD path is the bronchial anatomy and not a bronchial material bucket. |

## Recommended Edits

- Add an item-level `curation/decisions.tsv` row for `habitatmech:GOLD.67ad6b3f36`. The expected disposition is `GROUND` to `UBERON:0002185` with exact relation because `Bronchi` is an exact synonym of `bronchus` and the GOLD path adds only human host context.
- Regenerate from maintained inputs with `just seed` and preview `UBERON:0002185` with `just seed-canary UBERON:0002185` before applying the full generated corpus.
- Confirm `data/habitats/host_associated/bronchi__3fb23b44.yaml` is retired, the GOLD attestation from `gold.ecosystem:6185` lands on `data/habitats/host_associated/bronchus.yaml`, and the four GOLD child rows retain a valid parent through `UBERON:0002185`.

## Follow-up Checks

After adding the item-level human Bronchi decision, re-run the same focused validation set and confirm the regenerated corpus merges this source into `UBERON:0002185`:

- `just validate data/habitats/host_associated/bronchus.yaml`
- `just validate-strict data/habitats/host_associated/bronchus.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

Also verify the retired `habitatmech:GOLD.67ad6b3f36` source concept appears in `bronchus.yaml` curation history and that `bronchial_epithelial_cell.yaml`, `lobar_bronchus.yaml`, `primary.yaml`, and `tertiary.yaml` keep a strictly broader parent after the minted parent disappears.

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -name '*bronchi__3fb23b44*' -print` found no pre-existing exact review report for this record before this file was written.
- Exact ignored/hidden-inclusive content searches for `Host-associated > Mammals: Human > Respiratory system > Bronchi`, `habitatmech:GOLD.67ad6b3f36`, `gold.ecosystem:6185`, and `gold.ecosystem:6186` found the maintained source, side-table, child, and generated rows cited above. The broader exact `Bronchi` sibling search found the already reviewed Mammals report and the Birds GOLD row; those rows share the same leaf label but do not feed this target.
