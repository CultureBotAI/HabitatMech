# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/brine__2299825d.yaml
- Started UTC: 2026-09-23T15:03:15Z
- Finished UTC: 2026-09-23T15:07:55Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | habitatmech:GOLD.cc5b244380 |
| Label | Brine |
| Class | HabitatRecord |
| Category | AQUATIC |
| Grounding status | NARROW |
| Mapping status | SEEDED |
| Maintained owner | generated from `data/raw/gold_ecosystem_paths.tsv` and `data/habitats/PATHS.tsv`, with the source-path parent added by `src/habitatmech/seed.py` |

[data/habitats/aquatic/brine__2299825d.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/aquatic/brine__2299825d.yaml) is the generated GOLD record for `Environmental > Aquatic > Non-marine Saline and Alkaline > Lake > Brine`.

The target was resolved with ignored-file-inclusive searches:

- `find data/habitats -type f -name 'brine__2299825d.yaml' -print` found exactly one generated YAML record.
- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-brine__2299825d.md' -print` found no prior exact review report.
- `rg --no-ignore --hidden -n 'gold\.ecosystem:5372|Environmental > Aquatic > Non-marine Saline and Alkaline > Lake > Brine|habitatmech:GOLD\.cc5b244380' data/raw curation data/habitats/PATHS.tsv history research reports -g '!reports/yaml_record_review/**' -g '!data/text_map/**' -g '!pages/**' -g '!build/**'` found the authoritative GOLD source rows and no maintained curation overlay, history record, research report, or auxiliary report for this path outside raw generated inputs.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/brine__2299825d.yaml` | pass; no LinkML issues found |
| `just validate-strict data/habitats/aquatic/brine__2299825d.yaml` | pass; 1 file scanned, 0 files with ERROR, 0 total ERROR rows |
| `just validate-causal curation/causal_graphs/brine__2299825d.yaml` | not applicable; exact `find curation/causal_graphs -maxdepth 1 -type f -name 'brine__2299825d.yaml' -print` found no overlay |
| `just validate-causal-all` | pass; 32 causal-graph curation files with 32 graphs validated |
| Reference validator | not applicable; this generated GOLD record has no causal graph and no DOI, PMID, or URL evidence slots to inspect |
| `just term-requests-check` | pass; term-request table is current with 109 terms |
| `just validate-history` | pass; no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | pass; 3206 expected, 3206 found, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | pass; 0 ungrounded records still undecided; 1810 decisions on file |
| `just report` | pass; 3206 records; no global issue specific to `habitatmech:GOLD.cc5b244380` surfaced |
| `git diff --check` | pass |

## Identity and Grounding

The minted record identity matches its GOLD source path:

- `data/raw/gold_ecosystem_paths.tsv` contains canonical path `Environmental > Aquatic > Non-marine Saline and Alkaline > Lake > Brine`, leaf label `Brine`, depth `5`, one GOLD node, `100` organism assertions, and source node `gold.ecosystem:5372`.
- `data/raw/gold_path_biosamples.tsv` maps the same path to ecosystem path id `5372` with `3` biosamples.
- `data/habitats/PATHS.tsv` pins the deterministic identifier `habitatmech:GOLD.cc5b244380` to the collision-resolved slug `brine__2299825d`.

The `NARROW` grounding to `ENVO:00003044` is well supported. The GOLD leaf `Brine` matches ENVO's `brine` label, and this depth-5 GOLD path is narrower than the generic ENVO material because it denotes brine in the context of a non-marine saline or alkaline lake. The same path's GOLD triads also put `ENVO:00003044` in the `medium` slot and `ENVO:00000020` lake in the `local` slot, agreeing that brine is the sampled material rather than the lake feature itself.

The generated `parent_habitats` list is only partly coherent:

- `ENVO:00003044` is a correct broader parent for a lake brine material.
- `habitatmech:GOLD.713c7f352a` is generated from the immediate GOLD parent path `Environmental > Aquatic > Non-marine Saline and Alkaline > Lake`, but the target is brine from a lake, not a kind of lake.

`mapping_status: SEEDED` is expected. Exact ignored-file-inclusive search found no `curation/decisions.tsv` row for `habitatmech:GOLD.cc5b244380`, and exact search for the minted identifier, `gold.ecosystem:5372`, `brine__2299825d`, and the full source path across `curation`, `history`, `research`, and non-review `reports` found no maintained review artifact.

## Evidence

This record has one source attestation and no causal mechanism evidence:

- The `GOLD` source attestation mirrors `data/raw/gold_ecosystem_paths.tsv`: first and only source node `gold.ecosystem:5372`, leaf `Brine`, the full canonical path, `100` source organisms, and assertion unit `ORGANISM`.
- The attestation correctly uses `skos:narrowMatch`: the record keeps its minted GOLD identifier and records the matching generic brine term as a broader parent instead of merging the lake-specific GOLD path into the generic ENVO material.
- No characteristic taxa, environmental parameters, causal graphs, discussion items, or external datasets are generated for this source path.

## Completeness

The record carries all currently generated source-owned GOLD content: the source path, source node, organism assertion count, mapping predicate, generated parent links, category, and generated curation history.

It is missing a maintained way to suppress or qualify a false source-path parent. The second GOLD seeding pass in `src/habitatmech/seed.py` unconditionally adds the parent path's resolved identifier when it differs from the child identifier. For `Lake > Brine`, that turns a source hierarchy context into an `is-a` claim and cannot currently be repaired in `curation/decisions.tsv`, which decides source concepts rather than individual generated parent edges.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | `parent_habitats` incorrectly says the `Brine` medium is a narrower kind of the GOLD `Lake` path. | The target source path is `Environmental > Aquatic > Non-marine Saline and Alkaline > Lake > Brine`, and GOLD triads for that exact path put `ENVO:00000020` lake in the `local` slot and `ENVO:00003044` brine in the `medium` slot. `src/habitatmech/seed.py` nevertheless adds the immediate source-path parent `habitatmech:GOLD.713c7f352a`, whose source path is `Environmental > Aquatic > Non-marine Saline and Alkaline > Lake`. | Add a maintained parent-edge override or equivalent source-path relation table, then make `src/habitatmech/seed.py` consult it before adding the GOLD parent path as a `parent_habitats` edge. |

## Recommended Edits

1. Add a maintained override for the false `habitatmech:GOLD.cc5b244380 -> habitatmech:GOLD.713c7f352a` source-path parent edge instead of hand-editing [data/habitats/aquatic/brine__2299825d.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/aquatic/brine__2299825d.yaml). The current generated-input model has no edge-level table for this, so the fix needs either a new curation input or a seeder rule that can distinguish source-path containment from strict `parent_habitats`.
2. Re-seed `habitatmech:GOLD.cc5b244380` after that maintained input exists and confirm the generated record keeps `ENVO:00003044` as its broader material parent while no longer claiming the non-marine saline-and-alkaline `Lake` path as an `is-a` parent.

## Follow-up Checks

The narrowest checks for the recommended fix are:

- `just seed`
- `just seed-canary habitatmech:GOLD.cc5b244380`
- `just validate data/habitats/aquatic/brine__2299825d.yaml`
- `just validate-strict data/habitats/aquatic/brine__2299825d.yaml`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- an ignored-file-inclusive exact search for `habitatmech:GOLD.cc5b244380`, `habitatmech:GOLD.713c7f352a`, and the new override row to confirm the false parent edge has a maintained explanation

## Additional Notes

There are two exact GOLD `Brine` records plus the generic PREGO/ENVO lowercase `brine` record in the corpus, and they are not conflated:

- [data/habitats/aquatic/brine.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/aquatic/brine.yaml) is the generic PREGO/ENVO `ENVO:00003044` brine record.
- [data/habitats/aquatic/brine__2299825d.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/aquatic/brine__2299825d.yaml) is this minted GOLD record for non-marine saline-and-alkaline lake brine.
- [data/habitats/aquatic/brine__791bb429.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/aquatic/brine__791bb429.yaml) is the minted GOLD record for marine coastal brine.

The nearby `brine_pool.yaml` path is unrelated to the same-label GOLD `Brine` split.
