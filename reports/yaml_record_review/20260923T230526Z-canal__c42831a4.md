# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/aquatic/canal__c42831a4.yaml`
- Started UTC: `2026-09-23T23:02:01Z`
- Finished UTC: `2026-09-23T23:05:26Z`
- Verdict: `needs curation`

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/canal__c42831a4.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.656eda3dea` |
| Label | `Canal` |
| Definition source | None |
| Category | `AQUATIC` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |

This is a generated GOLD record owned by upstream inventories, curation inputs,
and the seeder, not by hand edits under `data/habitats/`. Its path is pinned by
`data/habitats/PATHS.tsv`, which maps `habitatmech:GOLD.656eda3dea` to
`canal__c42831a4`.

## Validation

| Check | Result |
|---|---|
| `find reports/yaml_record_review -maxdepth 1 -type f -name '*-canal__c42831a4.md' -print` | Passed; no prior exact report was present. |
| `find curation/causal_graphs -maxdepth 1 -type f -name '*canal*' -print` | Passed; no candidate causal overlay was present. |
| `rg --no-ignore --hidden -n --fixed-strings ... -g '!data/text_map/**' -g '!pages/**' -g '!build/**'` | Passed; bounded exact searches found the target generated record, `PATHS.tsv` slug row, GOLD source-inventory rows, the ENVO parent term rows, the generated child sediment record, separate engineered canal records, and no maintained curation decision, term request, causal overlay, history record, research report, or prior exact YAML review for `habitatmech:GOLD.656eda3dea`. Ignored and hidden files were included. |
| `just validate data/habitats/aquatic/canal__c42831a4.yaml` | Passed; `linkml-validate` reported no issues. |
| `just validate-strict data/habitats/aquatic/canal__c42831a4.yaml` | Passed; 1 file scanned, 0 files with errors, and 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| Focused causal-graph validation | Not applicable; the record has no maintained overlay under `curation/causal_graphs/`. |
| Reference validator | Not applicable; the record has no DOI, PMID, URL, causal-edge evidence, dataset, or discussion references to check. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; no issues found and 77 history records validated. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 on disk, 0 missing, 0 extra, and 0 differing. |
| `just worklist --limit 2000` | Passed; the command reported 0 ungrounded records still undecided and 1,810 decisions on file. |
| `just report` | Passed; the corpus report completed for 3,206 records. |
| `git diff --check` | Passed before report creation. |

## Identity and Grounding

The minted identifier is stable and matches
`mint("GOLD", "Environmental > Aquatic > Freshwater > Canal")`. The source
attestation is also faithful to the collapsed GOLD inventory:
`data/raw/gold_ecosystem_paths.tsv` has the canonical path
`Environmental > Aquatic > Freshwater > Canal` at depth 4, label `Canal`,
`gold_node_count` 2, zero organism/study/biosample assertions, and
`gold.ecosystem:8473|gold.ecosystem:8474` as the node IDs. The generated
attestation emits the first node ID and keeps the collapsed-path note.

`NARROW` is the correct seeded grounding relation for this source path. The
GOLD leaf label `Canal` lexically matches `ENVO:00000014` `canal`, but
`src/habitatmech/seed.py` lets the shallower
`Engineered > Built environment > Canal` path claim exact identity with that
generic ENVO term. The freshwater path remains minted and narrower than
`ENVO:00000014`, which fits the vendored ENVO definition of a canal as an
artificial watercourse with no flow or controlled flow.

`ENVO:00002011` `fresh water` is not a sound `parent_habitats` value for this
record. It is inherited by the second GOLD pass because the immediate GOLD
parent path `Environmental > Aquatic > Freshwater` resolves to `ENVO:00002011`;
the vendored definition says `fresh water` is water with a low dissolved-solute
concentration, not an artificial watercourse.

## Evidence

The record has exactly one source attestation, from GOLD. The raw row for
`Environmental > Aquatic > Freshwater > Canal` carries no organism, study, or
biosample assertions, so the generated attestation correctly omits
`assertion_count` and `assertion_unit`.

The true generated parent is `ENVO:00000014` `canal`, contributed by the
GOLD ambiguous-leaf rule. `ENVO:00000014` is broader than a freshwater canal.

The generated `ENVO:00002011` parent is over-scoped. The source path places
this node under GOLD's `Freshwater` bin, but that source-path context does not
make the child a kind of fresh water material. This is the same contextual
source-parent pattern the seeder uses for every GOLD child path; it needs
curation when a path parent is a location or context rather than a strict
genus.

The record has no `environmental_parameters`, `characteristic_taxa`, causal
graph, claim-level evidence block, discussion item, or dataset usage. No
maintained input currently asserts those fields for this exact source concept.

## Completeness

The generated record preserves the GOLD source path, the two collapsed GOLD
node IDs, the generated `NARROW` link to ENVO `canal`, the inherited GOLD
source-path parent, and the generated `SEEDED_FROM_SOURCES` event. It is not
complete enough for item-level signoff because the current hierarchy still
asserts one false `is-a` edge and the record has no maintained HabitatMech
definition naming what distinguishes this freshwater canal concept from the
generic engineered canal record.

The generated empty fields are expected for the current maintained inputs:

- no item-level curation decision is keyed by `habitatmech:GOLD.656eda3dea`;
- no term request is keyed by `habitatmech:GOLD.656eda3dea`;
- no causal overlay file matches `canal`;
- no record-specific history record, research report, or auxiliary YAML review
  report exists for this exact source concept.

Bounded, ignored-inclusive exact searches for
`habitatmech:GOLD.656eda3dea`, `canal__c42831a4`,
`Environmental > Aquatic > Freshwater > Canal`, `gold.ecosystem:8473`,
`ENVO:00000014`, `ENVO:00002011`, and `Canal` across curation inputs, raw
inputs, generated YAML, history, reports, research, source, tests, and
configuration found the expected generated-record, raw GOLD, ENVO, and
path-lock rows. Generated `data/text_map/`, `pages/`, and `build/` files were
excluded; ignored and hidden files were included.

## Findings

### Major

| Finding | Evidence | Maintained owner |
|---|---|---|
| `parent_habitats` asserts the freshwater canal source concept is a kind of `ENVO:00002011` `fresh water`, but the source concept denotes a canal watercourse rather than the water material. | `data/habitats/aquatic/canal__c42831a4.yaml` lists both `ENVO:00000014` and `ENVO:00002011`; `data/raw/gold_ecosystem_paths.tsv` places the source at `Environmental > Aquatic > Freshwater > Canal`; `data/raw/ontology_terms.tsv` defines `ENVO:00000014` as an artificial watercourse and `ENVO:00002011` as water with a low dissolved-solute concentration. | Add item-level review in `curation/decisions.tsv`; add a `curation/term_requests.tsv` row with `parent_class` `ENVO:00000014`, `parent_label` `canal`, and `parent_mode=REPLACE` to drop the inherited `ENVO:00002011` edge. |

No blocker or minor findings found.

## Recommended Edits

1. Add an item-level `curation/decisions.tsv` row for
   `habitatmech:GOLD.656eda3dea`, likely `GROUND_AS_PARENT` to
   `ENVO:00000014` `canal`, after confirming the freshwater GOLD path is
   intentionally distinct from the exact engineered `ENVO:00000014` canal
   record.
2. Add a `curation/term_requests.tsv` row defining a freshwater-canal habitat
   under `ENVO:00000014` `canal`, with `parent_mode=REPLACE` so regeneration
   removes `ENVO:00002011` while preserving the true canal genus.
3. After regeneration, inspect
   `data/habitats/aquatic/sediment__155ecb33.yaml` and confirm the
   freshwater-canal sediment child still has `habitatmech:GOLD.656eda3dea` as
   its GOLD source-path parent.

## Follow-up Checks

- Run `just seed`.
- Run `just seed-canary habitatmech:GOLD.656eda3dea` and inspect
  `data/habitats/aquatic/canal__c42831a4.yaml`.
- Run `just validate data/habitats/aquatic/canal__c42831a4.yaml`.
- Run `just validate-strict data/habitats/aquatic/canal__c42831a4.yaml`.
- Run `just term-requests-check`.
- Run `just validate-history`.
- Run `just verify-corpus --max-diffs 1`.

## Additional Notes

The ignored-inclusive exact search found several separate engineered canal
records:

- `ENVO:00000014` itself is generated at `data/habitats/engineered/canal.yaml`
  from `Engineered > Built environment > Canal`;
- `data/habitats/engineered/irrigation_canal.yaml` and
  `data/habitats/engineered/town_canal.yaml` are narrower engineered canal
  descendants;
- `data/habitats/engineered/water.yaml` is a canal-water material record with
  an item-level `GROUND_AS_PARENT` decision to `ENVO:00002006` `liquid water`.

Those engineered records reinforce why the freshwater GOLD path should stay
minted as a path-qualified canal rather than merging into the exact generic
ENVO canal record.
