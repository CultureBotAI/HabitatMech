# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/biofilm__b4a8654c.yaml
- Started UTC: 2026-09-25T14:28:16Z
- Finished UTC: 2026-09-25T14:28:22Z
- Verdict: needs curation

## Target

Reviewed the complete generated record at `data/habitats/engineered/biofilm__b4a8654c.yaml`. The record is the GOLD-only engineered child for `Engineered > Bioreactor > Anaerobic > Biofilm`, carried as the minted identifier `habitatmech:GOLD.6c12d05420`.

The generated file has `label: Biofilm`, `habitat_category: ENGINEERED`, `grounding_status: NARROW`, and `mapping_status: SEEDED`. Its generated parents are `ENVO:00002034` `biofilm`, from the ambiguous GOLD leaf match, and `ENVO:00002124` `anaerobic bioreactor`, from the already reviewed immediate source-path parent. Its only source attestation is `gold.ecosystem:8156` with `mapping_predicate: skos:narrowMatch`.

The file is generated from `data/raw/` plus seeded path logic. No `curation/decisions.tsv` row, authored term request, source extractor override, or target causal-graph overlay currently contributes to this record. Future item review belongs in `curation/decisions.tsv`; a future path-qualified label, definition, or parent replacement belongs in `curation/term_requests.tsv`; future target-specific overlays belong under `curation/causal_graphs/`.

## Validation

All required local checks passed:

- `just validate data/habitats/engineered/biofilm__b4a8654c.yaml`
- `just validate-strict data/habitats/engineered/biofilm__b4a8654c.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000 --status all --out /tmp/habitatmech-anaerobic-biofilm-worklist.tsv`
- `just report`

No validator was skipped.

## Identity and Grounding

The generated identifier, label, category, source attestation, and path slug agree with the maintained inputs. `data/raw/gold_ecosystem_paths.tsv` has the exact `Engineered > Bioreactor > Anaerobic > Biofilm` row at depth 4, with two collapsed GOLD node IDs, zero direct `organism_count`, `study_count`, `biosample_count`, and `total_assertions` values in the bulk ecosystem-path inventory, and `gold.ecosystem:8156|gold.ecosystem:8157` as the source node list. `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.6c12d05420` to the stable `biofilm__b4a8654c` slug.

The `ENVO:00002034` parent is a plausible broader term. The vendored ontology slice defines `ENVO:00002034` with canonical label `biofilm`, and the GOLD source path adds the anaerobic-bioreactor context under that generic leaf. `docs/HARMONIZATION.md` documents that deeper repeated GOLD leaves keep a minted identifier and use the matched leaf term as a broader parent instead of claiming the ontology term as an exact identity for every context-specific child.

The generated `ENVO:00002124` parent is the already reviewed exact grounding for GOLD `Engineered > Bioreactor > Anaerobic`. This parent has an item-level `curation/decisions.tsv` row and a generated exact record at `data/habitats/engineered/anaerobic_bioreactor.yaml`, so the context term itself is settled. The remaining question is whether a biofilm in an anaerobic bioreactor should keep `anaerobic bioreactor` as a strict parent. By analogy to the curated `air scrubber biofilm` term request, the GOLD source-path edge is likely contextual: a bioreactor biofilm grows in a bioreactor but is not itself a kind of bioreactor.

This target has no item-level decision. Exact ignored-inclusive searches found no `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/term_requests/`, `curation/term_requests_excluded.tsv`, `history/`, `research/habitats/`, `curation/causal_graphs/`, `conf/id_label_targets.yaml`, or prior review-report entry for `habitatmech:GOLD.6c12d05420`, `gold.ecosystem:8156`, `gold.ecosystem:8157`, `biofilm__b4a8654c`, or the exact source path.

## Evidence

- Supported as generated source provenance: exact ignored-inclusive searches found `gold.ecosystem:8156`, `gold.ecosystem:8157`, and the exact tab-delimited `Engineered > Bioreactor > Anaerobic > Biofilm` row in `data/raw/gold_ecosystem_paths.tsv`.
- Supported as broader ontology context: `ENVO:00002034` is present as `biofilm`, and a path-qualified GOLD `Biofilm` under an anaerobic bioreactor is narrower than generic `biofilm`.
- Supported as generated context but not yet as a strict parent: `ENVO:00002124` is the reviewed exact grounding for the immediate `Engineered > Bioreactor > Anaerobic` source path, but the generated source path says the leaf is a `Biofilm` inside that branch rather than demonstrating that the biofilm is a subtype of the bioreactor.
- Supported as direct GOLD API side-table context: `data/raw/gold_studies.tsv` has one study, `Gs0133417`, whose only path is `Engineered > Bioreactor > Anaerobic > Biofilm`; `data/raw/gold_path_biosamples.tsv` has three biosamples for numeric GOLD path ID `8157`.
- Not yet supported as individually reviewed habitat identity: no item-level `curation/decisions.tsv` row decides whether this source concept should stay as a specific anaerobic-bioreactor biofilm, become a different exact ontology grounding, or be handled some other way.

The record has no definition, environmental parameters, characteristic taxa, claim-level `EvidenceItem` objects, target-specific causal graphs, discussions, or datasets to inspect.

## Completeness

The generated source attestation is complete for the committed GOLD ecosystem inventory: the raw row lists exactly two collapsed source node IDs, and the generated note says two GOLD ecosystem node IDs share this path.

The GOLD API side tables add only limited target-specific context. Exact ignored-inclusive searches over `data/raw` found the exact source path in `data/raw/gold_studies.tsv` and `data/raw/gold_path_biosamples.tsv`, but not in `data/raw/gold_path_triads.tsv` or `data/raw/environment_parameters.tsv`. The one committed study and three committed biosamples therefore do not carry MIxS triad terms or environmental-parameter rows for this exact child.

Before this report was written, exact ignored-inclusive searches over maintained curation, append-only history, research, generated habitat YAML, label residuals, and existing YAML-record reviews found no target-specific decision, authored term request, generated term-request row, term-request exclusion, history record, deep-research report, causal overlay, label-correspondence residual, or prior exact review report for `habitatmech:GOLD.6c12d05420`, `gold.ecosystem:8156`, `gold.ecosystem:8157`, `biofilm__b4a8654c`, or `Engineered > Bioreactor > Anaerobic > Biofilm`. `find reports/yaml_record_review -maxdepth 1 -type f -name '*biofilm__b4a8654c*' -print` also found no prior review report for this exact generated stem.

The generic `ENVO:00002034` record already has `curation/causal_graphs/biofilm.yaml`, but that overlay belongs to generic biofilm and does not item-review this anaerobic-bioreactor-specific child.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `Engineered > Bioreactor > Anaerobic > Biofilm` is still only a seeded ambiguous-leaf child, and its generated `ENVO:00002124` anaerobic-bioreactor parent is likely contextual rather than a true `is-a` parent. | `data/raw/gold_ecosystem_paths.tsv` has this source concept as a `Biofilm` leaf nested under the already reviewed `Engineered > Bioreactor > Anaerobic` path; no item-level decision exists for `habitatmech:GOLD.6c12d05420`; `curation/term_requests.tsv` already defines the reviewed `air scrubber biofilm` child under `ENVO:00002034` with `parent_mode=REPLACE` because its inherited source-path parent is contextual. | `curation/decisions.tsv` and `curation/term_requests.tsv`. |

No blockers or minor findings were found.

## Recommended Edits

1. Item-review `habitatmech:GOLD.6c12d05420` against the exact `Engineered > Bioreactor > Anaerobic > Biofilm` GOLD path, `ENVO:00002034` `biofilm`, and the immediate `ENVO:00002124` anaerobic-bioreactor context.
2. If the source concept is an anaerobic-bioreactor biofilm, add an `ITEM` `CONFIRM_UNGROUNDED` decision in `curation/decisions.tsv`, because no exact term in the vendored slice currently names the path-qualified child.
3. In that same future curation session, add a `curation/term_requests.tsv` row that defines the record under `ENVO:00002034` `biofilm`; use `parent_mode=REPLACE` if item review confirms the generated `anaerobic bioreactor` parent is contextual.
4. Add an append-only `history/` record for that future curation session.

## Follow-up Checks

- After updating `curation/decisions.tsv` and `curation/term_requests.tsv`, run `just seed`, `just seed-canary habitatmech:GOLD.6c12d05420`, and inspect the regenerated `data/habitats/engineered/biofilm__b4a8654c.yaml`.
- If the canary is correct, run `just seed-apply --force`, then `just validate-strict data/habitats/engineered/biofilm__b4a8654c.yaml`, `just term-requests-check`, `just validate-history`, `just validate-causal-all`, `just verify-corpus --max-diffs 1`, and `just report`.
- If the future item review adds a causal overlay, run the focused `just validate-causal curation/causal_graphs/<slug>.yaml` before `just validate-causal-all`.

## Additional Notes

All absence checks in this review used ignored-inclusive `rg --no-ignore --hidden`, exact scans of the committed raw GOLD side tables, or `find`, bounded to maintained curation, history, research, raw inventory, generated habitat, label-residual, and review-report paths. The review did not depend on rendered `pages/` or text-map JSON.

The generic `ENVO:00002034` `biofilm` record and its `curation/causal_graphs/biofilm.yaml` overlay remain out of scope for this single-record review. The generic mechanism overlay can inform future curation, but it does not prove a mechanism for the anaerobic-bioreactor-specific child and does not decide whether the anaerobic-bioreactor source-path parent is a true superclass.
