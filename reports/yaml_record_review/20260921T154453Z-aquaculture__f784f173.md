# YAML Record Review: Aquaculture

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/aquaculture__f784f173.yaml
- Started UTC: 2026-09-21T15:44:53Z
- Finished UTC: 2026-09-21T15:44:53Z
- Verdict: needs curation

## Target

The target is the generated `HabitatRecord` at `data/habitats/aquatic/aquaculture__f784f173.yaml`.

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.8cd1d6fe85` |
| Label | `Aquaculture` |
| Category | `AQUATIC` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| GOLD source node | `gold.ecosystem:3481` |
| Source path | `Environmental > Aquatic > Aquaculture` |
| Locked stem | `aquaculture__f784f173` in `data/habitats/PATHS.tsv` |

This review covers the single GOLD source concept minted from `Environmental > Aquatic > Aquaculture`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/aquaculture__f784f173.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/aquaculture__f784f173.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 records on disk, 0 missing, 0 extra, 0 differing. |
| Reference validator | Not checked: this record has no DOI/PMID/URL `EvidenceItem`, no causal-edge evidence, and no target-specific `curation/causal_graphs/` overlay. |

## Identity and Grounding

The record denotes GOLD path `Environmental > Aquatic > Aquaculture`. `data/raw/gold_ecosystem_paths.tsv` preserves that path with leaf label `Aquaculture`, depth 3, three GOLD nodes, 77 GOLD organism assertions, and source nodes `gold.ecosystem:3481|gold.ecosystem:3750|gold.ecosystem:3965`.

The minted identity is defensible for the current ontology slice and curation state. `ENVO:03600074` `aquaculture farm` has `aquaculture` as an exact synonym and is an appropriate broader candidate, but the seeder does not exact-ground this GOLD source automatically because `Environmental > Aquatic > Aquaculture` and `Engineered > Artificial ecosystem > Aquaculture` tie at the shallowest depth for the same `Aquaculture` leaf. The automatic ambiguous-leaf rule therefore keeps both paths minted and attaches the synonym-matched `ENVO:03600074` term as a `skos:narrowMatch` parent.

`mapping_status: SEEDED` is correct because exact hidden/ignored-inclusive searches found no row for `habitatmech:GOLD.8cd1d6fe85` in `curation/decisions.tsv` and no authored definition for the record in `curation/term_requests.tsv`.

The generated `ENVO:00002030` `aquatic biome` parent is false. It is inherited from the GOLD parent path `Environmental > Aquatic`, but this source concept denotes an aquaculture farming environment, not a biome whose communities undergo ecological climax succession. The sibling `ENVO:03600074` term is categorized as `ENGINEERED`, and ENVO places `ENVO:03600074` under `ENVO:00000077` `artificial ecosystem`, not under `ENVO:00002030`.

Exact hidden/ignored-inclusive searches before this report was created covered `curation/`, `history/`, `data/raw/`, `data/habitats/PATHS.tsv`, `research/habitats/`, and prior `reports/yaml_record_review/` reports for `habitatmech:GOLD.8cd1d6fe85`, `gold.ecosystem:3481`, `gold.ecosystem:3750`, `gold.ecosystem:3965`, `aquaculture__f784f173`, and the exact GOLD path. They found the raw GOLD path, biosample, triad, study, and PATHS rows cited here, but no target-specific decision row, term request, causal overlay, history entry, research report, or prior YAML review.

## Evidence

| Claim | Evidence | Assessment |
|---|---|---|
| GOLD attests one `Aquaculture` source concept at the canonical path `Environmental > Aquatic > Aquaculture`. | `data/raw/gold_ecosystem_paths.tsv` has the exact canonical path, leaf label `Aquaculture`, `gold_node_count=3`, 77 organism assertions, and node IDs `gold.ecosystem:3481`, `gold.ecosystem:3750`, and `gold.ecosystem:3965`. | Supported. |
| The generated source attestation correctly emits `gold.ecosystem:3481` as the displayed source ID and a 3-node note. | The raw GOLD path row lists three node IDs and orders `gold.ecosystem:3481` first. `src/habitatmech/seed.py` emits the first GOLD node and adds a note when more than one node shares the path. | Supported. |
| The generated source attestation correctly reports `assertion_count: 77` with `assertion_unit: ORGANISM`. | The raw GOLD path row has `organism_count=77`, `genome_count=0`, `biosample_count=0`, and `total_assertions=77`; the separate aggregate `data/raw/gold_path_biosamples.tsv` row reports 118 biosamples for node `3965`, but GOLD `SourceAttestation` consumes the organism count from `data/raw/gold_ecosystem_paths.tsv`. | Supported. |
| `ENVO:03600074` is an appropriate broader candidate for this record. | `data/raw/ontology_terms.tsv` defines `ENVO:03600074` as `aquaculture farm`, includes `aquaculture` as an exact synonym, and scopes the class to cultivated fish, crustaceans, mollusks, aquatic plants, and algae. | Supported. |
| `ENVO:00002030` is a valid strict parent for this source concept. | `ENVO:00002030` is `aquatic biome`, a biome determined by a water body with ecological climax communities; the GOLD source path and its synonym-matched `ENVO:03600074` parent denote an aquaculture farming environment. | Not supported. |
| Existing target-specific causal evidence belongs on this record. | Exact hidden/ignored-inclusive searches of `curation/causal_graphs/`, `history/`, `research/habitats/`, and prior review reports found no target-specific overlay, history entry, research report, or prior review for this source concept before this report was created. | Not supported by current maintained inputs. |

## Completeness

The record preserves the current GOLD source identity: the source path, leaf label, node ID count, first displayed GOLD node, organism assertion count, aquatic category, seeded narrow-match grounding, and generated seed history are present.

The record is incomplete at the item level. No `curation/decisions.tsv` row records a curator judgment for `habitatmech:GOLD.8cd1d6fe85`, and no `curation/term_requests.tsv` row defines the minted aquaculture concept or repairs the false inherited `aquatic biome` parent.

The empty `environmental_parameters`, `characteristic_taxa`, `evidence`, `causal_graphs`, `discussions`, and `datasets` lists are appropriate for the current maintained inputs. The GOLD MIxS triad rows are contextual candidates for future curation, but they are not causal-edge evidence and are not generated directly into those optional fields.

## Findings

### Major

- `data/habitats/aquatic/aquaculture__f784f173.yaml` currently asserts `ENVO:00002030` `aquatic biome` as a `parent_habitats` value even though `Environmental > Aquatic > Aquaculture` denotes an aquaculture farming environment rather than a biome whose communities undergo ecological climax succession. `parent_habitats` is an is-a claim per `docs/CURATION.md`, and this false edge is inherited from the GOLD parent path and needs item-level replacement in `curation/term_requests.tsv`.

## Recommended Edits

- Add item-level curation for `habitatmech:GOLD.8cd1d6fe85` in `curation/decisions.tsv`, preserving `ENVO:03600074` `aquaculture farm` as a broader parent only if item review confirms the GOLD path denotes a narrower aquaculture-farm environment rather than exactly the ENVO class.
- Add a `curation/term_requests.tsv` row that defines the minted `Environmental > Aquatic > Aquaculture` concept and uses `parent_mode: REPLACE` to remove the inherited `ENVO:00002030` parent after selecting the tightest supported vendored genus.
- Regenerate with `just seed`, inspect `just seed-canary habitatmech:GOLD.8cd1d6fe85`, and apply with `just seed-apply --force` only after the canary removes `ENVO:00002030` while retaining the GOLD source attestation.

## Follow-up Checks

- `just validate data/habitats/aquatic/aquaculture__f784f173.yaml`
- `just validate-strict data/habitats/aquatic/aquaculture__f784f173.yaml --quiet`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- Manual check: confirm `data/habitats/aquatic/aquaculture__f784f173.yaml` no longer lists `ENVO:00002030` as a parent after the item-level decision and `REPLACE` term request.

## Additional Notes

The GOLD path has separate MIxS triad candidates from 14 biosamples across 3 studies: `ENVO:00000873` `freshwater biome` in `env_broad_scale`, `ENVO:00000294` `fish farm` in `env_local_scale`, and `ENVO:00002011` `fresh water` in `env_medium`. These are useful clues for a future definition, but none is direct evidence that this record is a kind of `aquatic biome`.
