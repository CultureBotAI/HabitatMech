# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/host_associated/aerial_part.yaml
- Started UTC: 2026-09-23T17:05:10Z
- Finished UTC: 2026-09-23T17:07:59Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | BTO:0001658 |
| Label | aerial part |
| Class | HabitatRecord |
| Category | HOST_ASSOCIATED |
| Grounding status | EXACT |
| Mapping status | SEEDED |
| Maintained owner | generated from the BTO ontology slice, PREGO raw tables, and `data/habitats/PATHS.tsv`; PREGO self-grounding is added by `src/habitatmech/seed.py` |

[data/habitats/host_associated/aerial_part.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/host_associated/aerial_part.yaml) is the generated PREGO/BTO record for `BTO:0001658` `aerial part`.

The target was resolved with ignored-file-inclusive searches:

- `find data/habitats -type f -name 'aerial_part.yaml' -print` found exactly one generated YAML record.
- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-aerial_part.md' -print` found no prior exact review report.
- `find curation/causal_graphs -maxdepth 1 -type f -name 'aerial_part.yaml' -print` found no causal-graph overlay for this record.
- `rg --no-ignore --hidden -n 'BTO:0001658|habitatmech:PREGO\.1b117510f3|aerial part|aerial bodies|aerial body|aerial partic|aerial parts|aerial tissue|aerial tissues' curation history research reports data/habitats/PATHS.tsv -g '!reports/yaml_record_review/**' -g '!data/text_map/**' -g '!pages/**' -g '!build/**'` found the `PATHS.tsv` slug lock and an advisory `curation/samples/exact-20260814.tsv` row that sampled the exact grounding as `ok`; it found no item-level `curation/decisions.tsv` row, maintained causal overlay, history record, or aerial-part research report for this PREGO source concept.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/host_associated/aerial_part.yaml` | pass; no LinkML issues found |
| `just validate-strict data/habitats/host_associated/aerial_part.yaml` | pass; 1 file scanned, 0 files with ERROR, 0 total ERROR rows |
| `just validate-causal curation/causal_graphs/aerial_part.yaml` | not applicable; exact `find curation/causal_graphs -maxdepth 1 -type f -name 'aerial_part.yaml' -print` found no overlay |
| `just validate-causal-all` | pass; 32 causal-graph curation files with 32 graphs validated |
| Reference validator | not applicable; this generated PREGO/BTO record has no causal graph and no DOI, PMID, or URL evidence slots to inspect |
| `just term-requests-check` | pass; term-request table is current with 109 terms |
| `just validate-history` | pass; no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | pass; 3206 expected, 3206 found, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | pass; 0 ungrounded records still undecided; 1810 decisions on file |
| `just report` | pass; 3206 records; no global issue specific to `BTO:0001658` surfaced |
| `git diff --check` | pass |

## Identity and Grounding

The exact identity is supported for a broad plant aerial-part habitat. `data/raw/ontology_terms.tsv` gives `BTO:0001658` primary label `aerial part` and definition `Existing or growing in the air rather than in the ground or in water.`, matching the generated identifier, label, definition, and `definition_source: BTO`.

The PREGO source concept is real and addresses the same BTO term. `data/raw/prego_habitats.tsv` has a direct PREGO row for `BTO:0001658` with source synonyms `aerial bodies|aerial body|aerial part|aerial partic|aerial parts|aerial tissue|aerial tissues`, `taxon_count` `1`, `direct_assertion_count` `1`, max PREGO score `3`, and channel `annotated_genomes_isolates`. The deterministic source-concept key is `habitatmech:PREGO.1b117510f3`.

`mapping_status: SEEDED` is expected. `src/habitatmech/seed.py` self-grounds PREGO ontology CURIEs until an item-level `curation/decisions.tsv` row reviews the minted PREGO source concept. The advisory sample row in `curation/samples/exact-20260814.tsv` records this exact grounding as `ok`, but it is not an item-level decision row and should not promote the record to `REVIEWED`.

The category is also coherent. `aerial part` denotes part of a plant exposed above ground or water, and a plant aerial structure can be a host-associated microbial habitat.

## Evidence

This record has one PREGO source attestation and one emitted PREGO taxon association:

- The `PREGO` attestation mirrors `data/raw/prego_habitats.tsv`: source id `BTO:0001658`, label `aerial part`, 1 distinct taxon, max score `3`, and the `annotated_genomes_isolates` channel. The `direct_assertion_count` value is also `1`, and the seeder intentionally emits PREGO `taxon_count` as `assertion_count` with unit `TAXON`.
- `data/raw/prego_habitat_taxa.tsv` row `2414` is the only PREGO taxon row for `BTO:0001658`; the generated row preserves `NCBITaxon:693986`, `Methylobacterium oryzae CBMB20`, rank `1`, PREGO score `3`, and candidate pool `1`.
- The PREGO lexical variants are source-owned synonyms. The odd-looking `aerial partic` string is present in `data/raw/prego_habitats.tsv`, so the generated record is faithfully preserving the raw PREGO synonym list rather than inventing it.

Exact ignored-file-inclusive search found no matching GOLD, BacDive, Madin, or environments-table raw rows for `BTO:0001658`, so the single PREGO attestation is the whole source inventory for this record.

No evidence-backed causal mechanism evidence is present.

Unsupported or over-scoped claims:

None found.

## Completeness

The record carries all generated source-owned PREGO content for this source concept: the source id, source label, PREGO taxon count, assertion unit, max score, evidence channel, PREGO synonyms, the single PREGO taxon row, BTO definition, generated slug lock, and generated seeding history.

`parent_habitats` is correctly absent from the generated record. Exact ignored-file-inclusive search found no `BTO:0001658` subclass row in `data/raw/ontology_subclass_edges.tsv`, no curated decision that adds a parent, and no term request or causal overlay that would add a maintained broader habitat.

Optional evidence, environmental-parameter, discussion, dataset, and causal-graph slots are also correctly absent; there is no maintained input for any of those slots for this PREGO-only record.

## Findings

None found.

## Recommended Edits

None required.

## Follow-up Checks

No corrective follow-up is required. If curators later choose to replace the sampled `ok` row with a real item-level `REVIEW` decision for `habitatmech:PREGO.1b117510f3`, the narrow proof path would be:

- `just seed`
- `just seed-canary BTO:0001658`
- inspect `data/habitats/host_associated/aerial_part.yaml` and confirm `mapping_status` changes from `SEEDED` to `REVIEWED` without changing the generated BTO or PREGO content
- `just validate-strict data/habitats/host_associated/aerial_part.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- ignored-file-inclusive exact searches for `BTO:0001658` and `habitatmech:PREGO.1b117510f3` to confirm the new decision row is the maintained owner of the reviewed status
- `git diff --check`

## Additional Notes

No additional notes.
