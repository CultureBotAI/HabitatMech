# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/abdomen__0eb11b74.yaml`
- Started UTC: 2026-09-22T19:49:00Z
- Finished UTC: 2026-09-22T19:52:02Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/host_associated/abdomen__0eb11b74.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.1743ebf24f` |
| Label | `Abdomen` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Record status | Generated from the GOLD inventory without an item-level curation decision |

This is the generated GOLD record for the path `Host-associated > Mammals > Lymphatic system > Lymph nodes > Abdomen`. Its identifier is the deterministic minted id `habitatmech:GOLD.1743ebf24f`, computed from `sha1("GOLD:Host-associated > Mammals > Lymphatic system > Lymph nodes > Abdomen")[:10]`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/abdomen__0eb11b74.yaml` | Passed; LinkML validation reported no issues. |
| `just validate-strict data/habitats/host_associated/abdomen__0eb11b74.yaml` | Passed; one file scanned with 0 error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 generated records, found 3206, with 0 missing, 0 extra, and 0 differing. |
| `git diff --check` | Passed; no whitespace errors. |

## Identity and Grounding

The direct GOLD identity is supported:

| Raw field | Value |
|---|---|
| `canonical_path` | `Host-associated > Mammals > Lymphatic system > Lymph nodes > Abdomen` |
| `leaf_label` | `Abdomen` |
| `path_depth` | `5` |
| `gold_node_count` | `1` |
| `organism_count` | `2` |
| `metagenome_count` | `0` |
| `biosample_count` | `0` |
| `total_assertions` | `2` |
| `gold_node_ids` | `gold.ecosystem:6763` |

`source_attestations` matches `data/raw/gold_ecosystem_paths.tsv:849`: the generated record keeps `gold.ecosystem:6763`, records the source label and path exactly, and emits `assertion_count: 2` with `assertion_unit: ORGANISM`.

The GOLD path-parent edge to `UBERON:0000029` is supported. `data/raw/gold_ecosystem_paths.tsv:168` records the immediate parent path `Host-associated > Mammals > Lymphatic system > Lymph nodes`; `curation/decisions.tsv:1055` grounds that parent source to `UBERON:0000029` `lymph node`; and `data/habitats/host_associated/lymph_node__7af26c8b.yaml` is the generated reviewed record for that lymph-node parent.

The ontology-derived parent to `UBERON:0000916` is not supported. The target's GOLD path is a child of `Lymph nodes`, so it denotes abdominal lymph nodes rather than the abdomen as a body subdivision. `data/raw/ontology_terms.tsv:12962` defines `UBERON:0000916 abdomen` as the subdivision of the vertebrate body between the thorax and pelvis, which is a location for abdominal lymph nodes and not a strictly broader lymph-node habitat.

This false parent is the expected output of the automatic ambiguous-leaf rule rather than a maintained curation row. GOLD has four distinct leaf paths labeled `Abdomen`; two shallowest paths, `Host-associated > Arthropoda: Insects > Abdomen` and `Host-associated > Fish > Abdomen`, tie at depth 3, so `leaf_claimants()` leaves the normalized `abdomen` label unclaimed. `resolve_gold()` therefore keeps this depth-5 source path minted and attaches the lexical `abdomen` match as a `skos:narrowMatch` parent. An ignored/hidden-inclusive exact search found no row for `habitatmech:GOLD.1743ebf24f` in `curation/decisions.tsv`, so `mapping_status: SEEDED` is mechanically expected.

## Evidence

| Claim | Nearest support | Review |
|---|---|---|
| The generated record represents GOLD node id `gold.ecosystem:6763` at `Host-associated > Mammals > Lymphatic system > Lymph nodes > Abdomen`. | `data/raw/gold_ecosystem_paths.tsv:849` | Supported exactly. |
| The GOLD path has two organism assertions and no metagenome or biosample assertions. | `data/raw/gold_ecosystem_paths.tsv:849` | Supported exactly. |
| The GOLD `Abdomen` path is a child of the generated `lymph node` record. | `data/raw/gold_ecosystem_paths.tsv:168`; `curation/decisions.tsv:1055`; `src/habitatmech/seed.py` | Supported. `ingest_gold()` links each GOLD path to the concept generated from the next path level up, and the parent path resolves to `UBERON:0000029`. |
| `UBERON:0000916` is a broader habitat for this abdominal-lymph-node source path. | `data/raw/ontology_terms.tsv:12962`; `data/raw/gold_ecosystem_paths.tsv:849` | Unsupported. The ontology term names the abdomen body subdivision; the GOLD path is a lymph-node child scoped by an abdomen location. |
| `mapping_status: SEEDED` | Exact ignored/hidden-inclusive search for `habitatmech:GOLD.1743ebf24f` in `curation/decisions.tsv` | Supported. No item-level decision exists for this source concept. |

Unsupported or over-scoped claims: the `parent_habitats` edge to `UBERON:0000916` and the `source_attestations.mapping_predicate` value `skos:narrowMatch` overstate a lexical location match as a broader-parent relationship for an abdominal lymph-node habitat.

## Completeness

No direct source attestation is missing for the GOLD source row: the record carries the source name, source id, source label, canonical source path, organism assertion count, and assertion unit.

The consequential completeness gap is the missing item-level curation row for this ambiguous GOLD label. The target should keep its source-derived `lymph node` parent, but a maintained decision needs to reject the automatic abdomen parent so the record stops asserting that abdominal lymph nodes are a kind of abdomen.

No maintained causal overlay, history file, raw research report, or prior exact YAML review report was found by filename for `abdomen__0eb11b74` or `abdomen-0eb11b74` under `curation/causal_graphs`, `history`, `research`, or `reports/yaml_record_review`; the `find` search included ignored files beneath those checked directories.

The closest human-specific sibling already follows the safer hierarchy. `data/raw/gold_ecosystem_paths.tsv:2305` records `Host-associated > Mammals: Human > Lymphatic system > Lymph nodes > Abdomen nodes`, `curation/decisions.tsv:349` confirms the source as ungrounded at class depth, and `data/habitats/host_associated/abdomen_nodes.yaml` keeps only `UBERON:0000029` as a parent without also adding `UBERON:0000916`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| major | `abdomen__0eb11b74.yaml` asserts `UBERON:0000916` as a broader habitat for a lymph-node source path whose leaf marks the abdomen as a node location. This is a wrong ontology parent and an over-scoped `skos:narrowMatch` attestation on an unreviewed source path. | The reviewed record contains `parent_habitats: [UBERON:0000029, UBERON:0000916]` and `mapping_predicate: skos:narrowMatch`; `data/raw/gold_ecosystem_paths.tsv:849` scopes the GOLD source path to lymph nodes; `data/raw/ontology_terms.tsv:12962` defines `UBERON:0000916` as the abdomen body subdivision. | Add an item-level row for `habitatmech:GOLD.1743ebf24f` in `curation/decisions.tsv`; then regenerate `data/habitats/host_associated/abdomen__0eb11b74.yaml`. |

No blocker findings.

No minor findings.

## Recommended Edits

1. Add an item-level `curation/decisions.tsv` row for `habitatmech:GOLD.1743ebf24f`.
   Confirm `Host-associated > Mammals > Lymphatic system > Lymph nodes > Abdomen` as an abdominal lymph-node habitat and explicitly reject `UBERON:0000916` as a parent for this source path. If no exact or broader vendored ontology term fits, use `CONFIRM_UNGROUNDED` without `UBERON:0000916`; the GOLD source-path parent will still keep it under `UBERON:0000029`.
2. Regenerate the corpus and verify that `data/habitats/host_associated/abdomen__0eb11b74.yaml` still lists `UBERON:0000029` in `parent_habitats` but no longer lists `UBERON:0000916` or reports `mapping_predicate: skos:narrowMatch`.
3. Screen the sibling GOLD lymph-node location records under `Host-associated > Mammals > Lymphatic system > Lymph nodes` for the same location-as-identity pattern before item-reviewing them.

## Follow-up Checks

- `just seed-canary habitatmech:GOLD.1743ebf24f`
- `just verify-corpus --max-diffs 1`
- `just validate data/habitats/host_associated/abdomen__0eb11b74.yaml`
- `just validate-strict data/habitats/host_associated/abdomen__0eb11b74.yaml`
- Exact ignored/hidden-inclusive searches for `habitatmech:GOLD.1743ebf24f`, `Host-associated > Mammals > Lymphatic system > Lymph nodes > Abdomen`, and `UBERON:0000916` across `curation`, `data/raw`, and `data/habitats`, excluding generated text maps and pages, to verify that the target source concept is item-reviewed and no longer has the false UBERON parent.

## Additional Notes

- The pre-report exact filename check `find reports/yaml_record_review -maxdepth 1 -type f -name '*abdomen__0eb11b74*' -print` returned no previous exact report; `find` includes ignored files.
- Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.1743ebf24f`, `Host-associated > Mammals > Lymphatic system > Lymph nodes > Abdomen`, `gold.ecosystem:6763`, `UBERON:0000029`, and `UBERON:0000916` found the generated target, raw GOLD row, raw ontology rows, reviewed lymph-node parent, and relevant sibling rows cited above.
- The exact ignored/hidden-inclusive search for the other GOLD abdomen paths found `Host-associated > Arthropoda: Insects > Abdomen`, `Host-associated > Arthropoda: Insects > Larva > Abdomen`, and `Host-associated > Fish > Abdomen`; those rows explain why no shallowest GOLD `Abdomen` path can claim the lexical ontology match outright.
