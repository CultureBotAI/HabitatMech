# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/host_associated/fecal__ceae17b6.yaml
- Started UTC: 2026-09-26T14:25:18Z
- Finished UTC: 2026-09-26T14:25:18Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.48d5963d27` |
| Label | `Fecal` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Generated path | `data/habitats/host_associated/fecal__ceae17b6.yaml` |
| Maintained path lock | `data/habitats/PATHS.tsv:1832` maps `habitatmech:GOLD.48d5963d27` to `fecal__ceae17b6` |
| Source concept | GOLD `gold.ecosystem:7381` |
| Source label | `Fecal` |
| Source path | `Host-associated > Arthropoda: Insects > Nymph/Instar > Gut > Fecal` |
| Grounding | `UNGROUNDED` |
| Mapping | `SEEDED` |

The reviewed file is generated from the GOLD source inventory and a class-level
curation decision. It should not be patched directly.

## Validation

| Command | Result |
|---|---|
| `just worklist --limit 0 --status all --out /tmp/habitatmech-worklist.tsv` | Passed; wrote 953 rows. The target appeared as row 239 with three GOLD organism assertions and lexical candidates `UBERON:0001970` `fel` and `PATO:0000383` `female`, neither of which is relevant to fecal material. |
| `just validate data/habitats/host_associated/fecal__ceae17b6.yaml` | Passed with `No issues found`. |
| `just validate-strict data/habitats/host_associated/fecal__ceae17b6.yaml` | Passed; one file scanned, zero files with `ERROR`, zero total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 expected records, 3206 found, 0 missing, 0 extra, 0 differing. |
| `just report` | Passed. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

| Claim | Source | Review |
|---|---|---|
| The target is the nymph/instar insect gut `Fecal` GOLD leaf. | `data/raw/gold_ecosystem_paths.tsv:737` records `Host-associated > Arthropoda: Insects > Nymph/Instar > Gut > Fecal`, leaf label `Fecal`, depth 5, one GOLD node, three organism assertions, and zero study or biosample assertions. | Supported exactly. |
| The stable generated filename is pinned for this identifier. | `data/habitats/PATHS.tsv:1832` maps `habitatmech:GOLD.48d5963d27` to `fecal__ceae17b6`. | Supported exactly. |
| The only current broader parent is the GOLD source-path parent. | The YAML lists `habitatmech:GOLD.c652e27320`; `data/raw/gold_ecosystem_paths.tsv:1020` shows that parent is `Host-associated > Arthropoda: Insects > Nymph/Instar > Gut`, and `data/habitats/PATHS.tsv:2743` maps it to `gut__315ebfad`. | Reproducible, but the is-a claim is biologically suspect. Fecal material sampled from a gut is not a kind of gut. |
| The grandparent nymph/instar branch has already been reviewed and defined. | `curation/decisions.tsv:677` records an item-level `CONFIRM_UNGROUNDED` decision for `habitatmech:GOLD.71e7bb35e2`, and `curation/term_requests.tsv:101` defines that GOLD concept as `insect nymph-associated environment`. | Supported exactly; the parent branch explicitly excludes gut and fecal material from the whole-host nymph concept. |
| The source concept is still owned only by a class-level sweep decision. | `curation/decisions.tsv:482` has `CONFIRM_UNGROUNDED` for `habitatmech:GOLD.48d5963d27` with `review_depth` `CLASS`. Its note says habitat status was not assessed. | Reproducible but incomplete. |

The current identity is faithful to the source path: this record is not the
Fish `Fecal` path in `data/habitats/host_associated/fecal.yaml`, the
Arthropoda: Insects `Digestive system > Hindgut > Fecal` sibling in
`data/habitats/host_associated/fecal__e9f04eec.yaml`, or the exact feces record
that already merges mammalian, human, avian, BacDive, guano, and annelid-cast
attestations at `data/habitats/host_associated/feces.yaml`.

Grounding is where the target needs curation. Vendored `ENVO:00002003` is
`fecal material` and has `frass` as a synonym; vendored `UBERON:0001988` is
`feces` and has `fecal material`, `fecal matter`, and `frass` as synonyms.
Both are better candidates than the worklist's lexical false positives. The two
GOLD insect `Frass` leaves are already `NARROW` to `ENVO:00002003` through the
seeder, and several non-insect fecal leaves have item-level decisions that
merge exact fecal material onto the shared `UBERON:0001988` record. No
item-level decision says whether the nymph gut `Fecal` leaf should also merge
with `feces`, stay minted with `fecal material` as a broader parent, or use a
different fecal-material modelling pattern.

## Evidence

| Claim | Nearest evidence | Review |
|---|---|---|
| `source: GOLD`, `source_id: gold.ecosystem:7381`, `source_label: Fecal`, and the exact `source_path` | `data/raw/gold_ecosystem_paths.tsv:737` | Supported exactly. |
| `assertion_count: 3`, `assertion_unit: ORGANISM` | The same raw GOLD aggregate row has `organism_count` 3 and zero study or biosample assertions. | Supported exactly. |
| No duplicate-GOLD-node note is emitted. | The same row has `gold_node_count` 1 and only `gold.ecosystem:7381` in `gold_node_ids`. | Supported exactly. |
| Source-derived parent `habitatmech:GOLD.c652e27320` | The raw target path is a child of `Host-associated > Arthropoda: Insects > Nymph/Instar > Gut`; the generated gut record uses the first node in the exact parent row, `gold.ecosystem:7379`, and carries a duplicate-node note for `gold.ecosystem:7379` plus `gold.ecosystem:7380`. | Supported as a source hierarchy edge, not as a strict is-a hierarchy claim. |
| Absence of a curated definition, environmental parameters, characteristic taxa, evidence objects, causal graphs, discussions, datasets, item-level curation history, target-specific research report, and prior exact YAML review | Exact hidden/ignored-inclusive searches for `habitatmech:GOLD.48d5963d27`, `fecal__ceae17b6`, `gold.ecosystem:7381`, and `Host-associated > Arthropoda: Insects > Nymph/Instar > Gut > Fecal` across `data/raw/`, `curation/`, `history/`, `research/habitats/`, `reports/yaml_record_review/`, `data/habitats/`, `pages/habitats/`, and `conf/` found the expected raw row, path lock, class-level decision, generated record, and generated page. Before this report was written, a hidden/ignored-inclusive `find` for `*fecal*` under `reports/yaml_record_review/` and `research/habitats/` found no exact prior YAML review and no target-specific research report. | Supported. The current record is a generated GOLD leaf with no committed item-level curation of its own. |

## Completeness

The record is schema-valid, reproducible, and has no generated drift. It is also
materially incomplete because its only judgement is the 2026-08-12 class sweep.
That sweep recorded only the negative result of a lexical ontology search and
explicitly did not assess whether the GOLD concept is a habitat.

The full source path, not the bare `Fecal` label, is the identity that must be
reviewed. The reviewed nymph/instar term request treats `Nymph/Instar > Gut`
and `Nymph/Instar > Gut > Fecal` as child or refinement concepts beneath the
whole-host nymph habitat. The exact hidden/ignored-inclusive target searches
found no GOLD biosample, MIxS triad, or study side-table row for this nymph-gut
fecal path; future curation therefore needs to rely on the source path plus
fecal-material ontology modelling unless a curator inspects external evidence.

The optional causal, taxon, dataset, discussion, and record-level evidence
blocks are correctly absent for the current seeded record. No curator-authored
mechanism claim is present that would need edge-level evidence.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| HM-FECAL-NYMPH-001 | Major | The GOLD `Nymph/Instar > Gut > Fecal` leaf is still `SEEDED` and `UNGROUNDED` from a class-level decision even though vendored ENVO and UBERON both contain fecal-material terms and related fecal paths have already been grounded, parented, or minted by item-level curation. Because `CLASS` says the source concept was not assessed, the current record has no maintained decision explaining whether this is exact feces/fecal material, a nymph-specific fecal material narrower than `ENVO:00002003`, or a distinct insect `frass` modelling case. | `curation/decisions.tsv`; possibly `curation/term_requests.tsv` if the item remains minted |
| HM-FECAL-NYMPH-002 | Major | The generated record's only parent is `habitatmech:GOLD.c652e27320` `Gut`, inherited solely from the GOLD source path. For a fecal-material reading, `Gut` is a sampling context or container rather than a strictly broader material class, and local curation rules state that `parent_habitats` is an is-a claim. Leaving the target minted under `Gut` would therefore encode a false is-a edge. | `curation/term_requests.tsv` with `parent_mode=REPLACE` if the source concept remains minted; if it exact-merges, the existing source-parent inheritance needs item-level verification or a maintained suppression path |

## Recommended Edits

1. In `curation/decisions.tsv`, replace the class-level
   `habitatmech:GOLD.48d5963d27` row with an item-level decision that inspects
   the full `Host-associated > Arthropoda: Insects > Nymph/Instar > Gut >
   Fecal` path. The decision should choose among exact grounding to an existing
   feces/fecal-material term, `GROUND_AS_PARENT` to `ENVO:00002003` if the
   record must retain nymph-gut specificity, or another reviewed disposition
   with the entity-type reasoning recorded in the note.
2. Do not leave the fecal-material result with `Gut` as an is-a parent. If the
   item remains minted rather than merging into an ontology record, add a
   definition to `curation/term_requests.tsv` with `ENVO:00002003` `fecal
   material` as the ontology parent and `parent_mode=REPLACE` so the inherited
   `Gut` source-path parent is removed. If item review instead chooses an exact
   merge, explicitly verify whether that merge would retain the same false GOLD
   source-path parent and either use an existing maintained suppression path or
   add one before regenerating.
3. Review `habitatmech:GOLD.e381535c08` separately after this target. It is the
   higher-assertion adult-insect sibling `Host-associated > Arthropoda: Insects
   > Digestive system > Hindgut > Fecal` and still has the same class-level
   `CONFIRM_UNGROUNDED` shape.

## Follow-up Checks

1. Run `just seed` to confirm the proposed decision and optional term request
   are accepted without writing generated YAML.
2. Run `just seed-canary habitatmech:GOLD.48d5963d27` and inspect the
   regenerated target or its exact-merge successor to confirm the grounding,
   parent set, source attestation, and curation history all follow from the
   maintained inputs.
3. Run `just seed-apply --force` after the canary, without `--prune`, if the
   record path changes because an exact grounding merges the source concept.
4. Run `just validate data/habitats/host_associated/fecal__ceae17b6.yaml` if
   the file remains minted, or focused validation on the exact-merge successor
   if it does not.
5. Run `just validate-strict`, `just term-requests-check`,
   `just validate-history`, `just verify-corpus`, `just render`, and `just qc`.
6. Run an exact hidden/ignored-inclusive search for
   `habitatmech:GOLD.48d5963d27`, `gold.ecosystem:7381`,
   `fecal__ceae17b6`, and the full source path before declaring the
   item-level curation complete.

## Additional Notes

The target's bare label is highly ambiguous in this corpus.
`data/raw/gold_ecosystem_paths.tsv` contains GOLD `Fecal` leaves for human,
mammalian, avian, fish, amphibian, reptile, cephalochordate, mollusc,
myriapod, crustacean, adult-insect, and nymph-insect host paths.
`data/habitats/PATHS.tsv` pins `habitatmech:GOLD.227b8ef4a5` to the
unsuffixed `fecal` slug, so the stable `fecal__ceae17b6` suffix is expected and
is the correct target for `habitatmech:GOLD.48d5963d27`.

The exact search over `data/raw/` found no MIxS triad row for the
nymph-gut-fecal path. That absence should not be generalized to fecal material
as a category: other GOLD fecal leaves, including the human, mammalian, avian,
amphibian, and reptile leaves, do have path-specific `ENVO:00002003` medium
triads.
