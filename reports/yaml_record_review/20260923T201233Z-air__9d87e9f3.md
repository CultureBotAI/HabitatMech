# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/air__9d87e9f3.yaml
- Started UTC: 2026-09-23T20:09:58Z
- Finished UTC: 2026-09-23T20:12:33Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | habitatmech:GOLD.94928acca0 |
| Label | Air |
| Class | HabitatRecord |
| Category | ENGINEERED |
| Grounding status | NARROW |
| Mapping status | SEEDED |
| Maintained owner | generated from `data/raw/gold_ecosystem_paths.tsv` and `data/habitats/PATHS.tsv`; item-level follow-up belongs in `curation/decisions.tsv` and, if the minted cleanroom-air record is retained, `curation/term_requests.tsv` |

[data/habitats/engineered/air__9d87e9f3.yaml](../../data/habitats/engineered/air__9d87e9f3.yaml) is the generated record for GOLD `Engineered > Built environment > Spacecraft Assembly Cleanrooms > Air`.

The target was resolved with ignored-file-inclusive searches:

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-air__9d87e9f3.md' -print` found no prior exact review report.
- `find curation/causal_graphs -maxdepth 1 -type f -name 'air__9d87e9f3.yaml' -print` found no causal-graph overlay for this record.
- Exact `rg --no-ignore --hidden` searches for `habitatmech:GOLD.94928acca0`, `air__9d87e9f3`, `gold.ecosystem:5307`, `gold.ecosystem:5308`, and `Engineered > Built environment > Spacecraft Assembly Cleanrooms > Air` across `curation`, `data/habitats`, `data/raw`, `history`, `research`, and `reports`, excluding generated `data/text_map`, `pages`, `build`, prior YAML-review reports, and `.venv`, found the GOLD path inventory, the slug lock, and this generated record; they found no maintained `curation/decisions.tsv` row, term request, causal overlay, history record, or target-specific research report.
- Exact ignored-file-inclusive searches for `habitatmech:GOLD.013b99db03` and `Spacecraft Assembly Cleanrooms` found the generated cleanroom parent record, its `CLASS`-depth decision row, the class-swept-unscreened sample row, and sibling GOLD cleanroom paths, but no item-level decision for this target path.
- Exact searches of `data/raw/gold_path_triads.tsv`, `data/raw/gold_studies.tsv`, and `data/raw/gold_path_biosamples.tsv` found no triad, study, or biosample rows for `Engineered > Built environment > Spacecraft Assembly Cleanrooms > Air`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/air__9d87e9f3.yaml` | pass; no LinkML issues found |
| `just validate-strict data/habitats/engineered/air__9d87e9f3.yaml` | pass; 1 file scanned, 0 files with ERROR, 0 total ERROR rows |
| `just validate-causal curation/causal_graphs/air__9d87e9f3.yaml` | not applicable; exact `find curation/causal_graphs -maxdepth 1 -type f -name 'air__9d87e9f3.yaml' -print` found no overlay |
| `just validate-causal-all` | pass; 32 causal-graph curation files with 32 graphs validated |
| Reference validator | not applicable; this generated GOLD-only record has no causal graph and no DOI, PMID, or URL evidence slots to inspect |
| `just term-requests-check` | pass; term-request table is current with 109 terms |
| `just validate-history` | pass; no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | pass; 3206 expected, 3206 found, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | pass; 0 ungrounded records still undecided; 1810 decisions on file |
| `just report` | pass; 3206 records; 0 stale class-level sweeps, 0 swept paths whose parent-plus-leaf compound names a habitat term, and 0 records claiming a non-habitat is a habitat |
| `git diff --check` | pass |

## Identity and Grounding

The minted identifier, label, category, and source path agree with the GOLD inventory. `data/raw/gold_ecosystem_paths.tsv` has `Engineered > Built environment > Spacecraft Assembly Cleanrooms > Air` with leaf label `Air`, collapsed GOLD node IDs `gold.ecosystem:5307|gold.ecosystem:5308`, and `5` organism assertions. `data/habitats/PATHS.tsv` locks `habitatmech:GOLD.94928acca0` to the `air__9d87e9f3` slug.

The `NARROW` status is the seeder's unreviewed lexical answer, not an item-level curation decision. Exact ignored-file-inclusive searches found no `curation/decisions.tsv` row for `habitatmech:GOLD.94928acca0`, and the generated record has only the seed curation event.

`ENVO:00002005` `air` is a true broader parent for this GOLD path. `Engineered > Built environment > Spacecraft Assembly Cleanrooms > Air` narrows the ordinary air material to air in a spacecraft assembly cleanroom.

`habitatmech:GOLD.013b99db03` `Spacecraft Assembly Cleanrooms` is not a true broader parent for this source concept. A spacecraft assembly cleanroom is the engineered place that contains the air; the air in that cleanroom is not a kind of cleanroom.

## Evidence

This record has one GOLD source attestation:

- The `GOLD` attestation mirrors `data/raw/gold_ecosystem_paths.tsv`: source id `gold.ecosystem:5307`, source label `Air`, source path `Engineered > Built environment > Spacecraft Assembly Cleanrooms > Air`, two collapsed GOLD node IDs, and `5` direct organism assertions.

No PREGO or BacDive source concept is merged into this record, and no PREGO or BacDive taxon list is present. Exact source-path searches found no MIxS triad row, source study row, biosample row, evidence-backed causal mechanism, or dataset entry for this specific GOLD path.

Unsupported or over-scoped claims:

- `parent_habitats` over-scopes the GOLD source hierarchy by using `habitatmech:GOLD.013b99db03` as an is-a parent. The GOLD path shows a context relation between cleanrooms and the sampled air, not that cleanroom air is a subtype of the cleanroom itself.

## Completeness

The record carries the generated source-owned content for this single GOLD path: the minted source identifier, GOLD label, engineered category, the broader `ENVO:00002005` air parent, source attestation, seed event, and slug lock.

The curation state is materially incomplete. This path has five GOLD organism assertions and a true `ENVO:00002005` parent, but exact ignored-file-inclusive searches found no item-level decision, no `curation/term_requests.tsv` row that would define a minted cleanroom-air habitat, and no target-specific research report. The inherited cleanroom parent also needs item-level correction because it is contextual rather than broader.

Optional characteristic-taxon, environmental-parameter, DOI/PMID/URL evidence, dataset, discussion, and causal-graph slots are correctly absent; the GOLD inventory provides source occurrence evidence, not characteristic taxa, mechanism edges, or environmental parameter rows.

## Findings

- **major**: `habitatmech:GOLD.013b99db03` is asserted as a `parent_habitats` edge even though spacecraft assembly cleanrooms are the setting containing the target air, not a broader class of that air.

  **Evidence**: The target path is `Engineered > Built environment > Spacecraft Assembly Cleanrooms > Air`, and `parent_habitats` contains both `ENVO:00002005` `air` and `habitatmech:GOLD.013b99db03` `Spacecraft Assembly Cleanrooms`. `ENVO:00002005` is strictly broader, but `Spacecraft Assembly Cleanrooms` denotes the engineered room type in which the air occurs. `CLAUDE.md` and `docs/CURATION.md` both state that `parent_habitats` means strictly broader and that related non-broader terms belong in xrefs or other relations.

  **Maintained owner**: item-level review of the NARROW answer belongs in `curation/decisions.tsv`; if this stays a minted narrow concept, the cleanroom-air definition and `REPLACE` parent behavior belong in `curation/term_requests.tsv` so the false source-path parent is dropped during generation.

- **major**: `habitatmech:GOLD.94928acca0` has no item-level curation decision for a specific cleanroom-air source path with five GOLD organism assertions.

  **Evidence**: Exact ignored-file-inclusive searches found no `curation/decisions.tsv` row for `habitatmech:GOLD.94928acca0`, and the generated record's history has only `SEEDED_FROM_SOURCES`. `mapping_status` is therefore `SEEDED`, not `REVIEWED`, and no target-specific term request or research report exists to define the retained minted concept.

  **Maintained owner**: the source-concept review belongs in `curation/decisions.tsv`; any authored definition for the minted term belongs in `curation/term_requests.tsv`.

## Recommended Edits

- Reopen `habitatmech:GOLD.94928acca0` as an item-level curation task in `curation/decisions.tsv` and verify whether the intended identity is cleanroom air, spacecraft assembly cleanroom air, or a broader existing air term.
- If no exact external term fits, keep the record minted under `ENVO:00002005` and add a `curation/term_requests.tsv` definition for air from spacecraft assembly cleanrooms. Use `parent_mode=REPLACE` unless item-level review finds a true source-path parent in addition to `air`; `Spacecraft Assembly Cleanrooms` should not remain as an is-a parent.
- Regenerate `habitatmech:GOLD.94928acca0` and inspect [data/habitats/engineered/air__9d87e9f3.yaml](../../data/habitats/engineered/air__9d87e9f3.yaml) to confirm `ENVO:00002005` remains as a true broader parent, `habitatmech:GOLD.013b99db03` is removed from `parent_habitats`, and the record moves from the seeded state to the expected reviewed or term-requested state.

## Follow-up Checks

The narrow proof path for a future fix is:

- exact ignored-file-inclusive searches for `habitatmech:GOLD.94928acca0`, `gold.ecosystem:5307`, `gold.ecosystem:5308`, `Engineered > Built environment > Spacecraft Assembly Cleanrooms > Air`, and any selected external ontology candidate
- `just seed`
- `just seed-canary habitatmech:GOLD.94928acca0`
- inspect [data/habitats/engineered/air__9d87e9f3.yaml](../../data/habitats/engineered/air__9d87e9f3.yaml) for the expected `grounding_status`, `mapping_status`, `parent_habitats`, and curation history
- `just validate-strict data/habitats/engineered/air__9d87e9f3.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

## Additional Notes

The generated `Spacecraft Assembly Cleanrooms` parent is itself still a class-swept seed. `curation/decisions.tsv` only has a `CLASS`-depth `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.013b99db03`, although `curation/samples/class_swept_unscreened-20260814.tsv` did sample that path as a habitat and `just report` ranks it among the highest-volume class-swept concepts.

This specific cleanroom-air path has no `gold_path_triads`, `gold_studies`, or `gold_path_biosamples` entry in the committed GOLD auxiliary inventories; the five organism assertions in `data/raw/gold_ecosystem_paths.tsv` are therefore the only direct source counts available to this generated record.
