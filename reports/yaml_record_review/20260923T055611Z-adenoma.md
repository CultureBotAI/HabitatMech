# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/adenoma.yaml`
- Started UTC: 2026-09-23T05:36:00Z
- Finished UTC: 2026-09-23T05:56:11Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.5dd1030108` |
| Label | `Adenoma` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the GOLD inventory and `curation/decisions.tsv`; generated YAML remains read-only |
| Locked slug | `data/habitats/PATHS.tsv:1977` maps `habitatmech:GOLD.5dd1030108` to `adenoma` |

This is the generated GOLD record for the path `Host-associated > Mammals: Human > Benign tumor > Adenoma`. The record's identifier is the expected content hash for that path: `sha1("GOLD:Host-associated > Mammals: Human > Benign tumor > Adenoma")[:10]` is `5dd1030108`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/adenoma.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/adenoma.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `just worklist --status all --limit 2000` | Pass; 953 ungrounded records and 1810 decisions are on file |
| `just report` | Pass; the report completed and kept the corpus at 3206 records |
| `git diff --check` | Pass; no whitespace errors |

## Identity and Grounding

The generated identifier, label, category, path lock, source path, source id, duplicate-node note, and absence of organism/sample assertions are internally reproducible:

- `data/raw/gold_ecosystem_paths.tsv:2153` records the exact source path, leaf label `Adenoma`, `depth: 4`, two GOLD ecosystem node ids, zero organism/study/biosample/total assertions, and `gold.ecosystem:6647|gold.ecosystem:6648`.
- The generated `source_attestations` entry correctly uses `gold.ecosystem:6647`, the first collapsed GOLD node id, and preserves the multiplicity note for the two node ids.
- `data/habitats/PATHS.tsv:1977` pins `habitatmech:GOLD.5dd1030108` to the `adenoma` slug.
- `data/raw/gold_ecosystem_paths.tsv:2152` records the immediate GOLD parent path `Host-associated > Mammals: Human > Benign tumor`, which hashes to `habitatmech:GOLD.333c18201f`.

The grounding decision is not item-reviewed. `curation/decisions.tsv:578` carries only a `CLASS`-depth `CONFIRM_UNGROUNDED` decision for `habitatmech:GOLD.5dd1030108`; by design, that class-level sweep proves only that no vendored ENVO, UBERON, FOODON, or BTO term matched the label through the configured lexical routes. It explicitly did not assess whether the concept is a habitat.

That matters for this record because GOLD places `Adenoma` directly below `Benign tumor`, and `curation/decisions.tsv:379` has already item-reviewed the parent `habitatmech:GOLD.333c18201f` as `NOT_APPLICABLE`: a non-habitat source concept. The target therefore still needs item-level curation before it can remain a real `UNGROUNDED` habitat, request an ENVO term, or be reclassified as `NOT_APPLICABLE`.

## Evidence

| Claim | Nearest source | Review |
|---|---|---|
| The generated record represents the collapsed GOLD path `Host-associated > Mammals: Human > Benign tumor > Adenoma`. | `data/raw/gold_ecosystem_paths.tsv:2153` | Supported exactly |
| The minted identifier is `habitatmech:GOLD.5dd1030108`. | `sha1("GOLD:Host-associated > Mammals: Human > Benign tumor > Adenoma")[:10]` | Supported exactly |
| The source id shown in the record is `gold.ecosystem:6647`, with another node collapsed into the same path. | `data/raw/gold_ecosystem_paths.tsv:2153`; `src/habitatmech/seed.py` emits the first node and records a multiplicity note | Supported exactly |
| The record omits `assertion_count` and `assertion_unit`. | `data/raw/gold_ecosystem_paths.tsv:2153` | Supported exactly; the raw row has zero organism, study, biosample, and total assertions |
| `grounding_status: UNGROUNDED` with `mapping_status: SEEDED` | `curation/decisions.tsv:578`; `docs/HARMONIZATION.md` class-level sweep rules | Supported mechanically; not sufficient item-level review |
| Parent `habitatmech:GOLD.333c18201f` | GOLD parent path at `data/raw/gold_ecosystem_paths.tsv:2152`; parent-link code in `src/habitatmech/seed.py` | Generated reproducibly but semantically unsupported, because the parent source concept is item-reviewed as `NOT_APPLICABLE` |

The record has no xrefs, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussion links, datasets, or quality flags. Target-id and exact-source searches with ignored and hidden files included found no target-specific term request, causal-graph overlay, history entry, or research artifact for `habitatmech:GOLD.5dd1030108`, `gold.ecosystem:6647`, `gold.ecosystem:6648`, or the exact GOLD path. The source row itself also has no GOLD study, biosample, or triad evidence: exact ignored/hidden-inclusive searches for `Host-associated > Mammals: Human > Benign tumor > Adenoma` found no rows in `data/raw/gold_studies.tsv`, `data/raw/gold_path_biosamples.tsv`, or `data/raw/gold_path_triads.tsv`.

Unsupported or over-scoped claims:

- The class-level `CONFIRM_UNGROUNDED` decision overstates the state of curation if read as a finding that `Adenoma` is a real habitat with no ontology term; the decision note itself says habitat-ness was not assessed.
- The generated `parent_habitats` edge treats item-reviewed non-habitat `Benign tumor` as a strictly broader habitat.

## Completeness

The GOLD source attestation is complete for a collapsed two-node, zero-assertion row: the record carries the first node id, the source label, the complete source path, and the duplicate-node note, and it correctly omits count, unit, score, and evidence-channel fields.

The missing item-level decision is consequential. Adenoma is still in the mechanically swept `CLASS` bucket, and the source path puts it inside an already curated non-habitat branch. A future curator needs to decide the individual source concept, most likely by changing `curation/decisions.tsv` row `habitatmech:GOLD.5dd1030108` to `NOT_APPLICABLE` unless inspected evidence establishes that GOLD meant an adenoma tissue or adenoma-associated environment rather than the tumor class itself.

The generated hierarchy is incomplete as semantic curation because the only emitted parent is `habitatmech:GOLD.333c18201f`, the `Benign tumor` source concept that `curation/decisions.tsv` has already marked `NOT_APPLICABLE`. This is not specific to a missing row on `Adenoma`; the source-path parent construction in `src/habitatmech/seed.py` links a GOLD child to its immediate path parent without screening the parent grounding status.

Ignored/hidden-inclusive exact searches covered the target id, exact GOLD path, `gold.ecosystem:6647`, `gold.ecosystem:6648`, `gold.ecosystem:6649`, `Adenoma`, `Benign tumor`, and `habitatmech:GOLD.333c18201f`, while excluding generated `build`, `data/text_map`, and `pages` trees for broad content searches. They found the expected GOLD inventory, generated-record, path-lock, and decision rows, the child `Colorectal polyps` source row, BTO cell terms that are not exact habitat identities for the target path, and no target-specific maintained term request or causal overlay.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| HM-ADENOMA-001 | major | `Adenoma` still has only a class-level `CONFIRM_UNGROUNDED` decision even though its GOLD path places it under `Benign tumor`, an item-reviewed `NOT_APPLICABLE` non-habitat. A class sweep only found no lexical ontology match; it did not decide that the concept is a habitat. The source concept needs item-level curation and should probably be reclassified as `NOT_APPLICABLE` unless a curator verifies that this GOLD node means adenoma tissue or an adenoma-associated environment rather than the tumor class. | `curation/decisions.tsv` row for `habitatmech:GOLD.5dd1030108` |
| HM-ADENOMA-002 | major | The record's only `parent_habitats` entry is `habitatmech:GOLD.333c18201f`, but that parent record is already item-reviewed as `NOT_APPLICABLE`. The generated YAML is byte-reproducible, but the source-path parent edge is not a valid strictly-broader habitat claim. | `src/habitatmech/seed.py` GOLD source-path parent construction |

No blocker findings.

No minor findings.

## Recommended Edits

1. Replace the `CLASS`-depth `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.5dd1030108` with an item-level decision in `curation/decisions.tsv`. If curator review confirms that `Adenoma` names the benign tumor class rather than a habitat, use `NOT_APPLICABLE` and explain the source path in the note.
2. Add the required append-only history record for that decision change under `history/`.
3. Update `src/habitatmech/seed.py` so GOLD source-path parent emission cannot attach a generated record to a parent source concept whose reviewed status is `NOT_APPLICABLE`, and add a regression test proving that `parent_habitats` does not contain item-reviewed non-habitats.
4. Reseed `habitatmech:GOLD.5dd1030108` and inspect `data/habitats/host_associated/adenoma.yaml` to confirm the item-level decision is reflected in `grounding_status`, `mapping_status`, and `curation_history`, and that `Benign tumor` no longer appears as a broader habitat.

## Follow-up Checks

After the future curation and seeder fixes, run:

- `just seed`
- `just seed-canary habitatmech:GOLD.5dd1030108`
- `just validate data/habitats/host_associated/adenoma.yaml`
- `just validate-strict data/habitats/host_associated/adenoma.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --status all --limit 2000`
- `just report`
- `just test tests/test_decisions.py tests/test_corpus_integrity.py`
- `git diff --check`

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-adenoma.md' -print` found no pre-existing exact review report for this record before this file was written, and `find` included ignored files under the searched directory.
- Exact ignored/hidden-inclusive content searches for the target id, source ids, exact GOLD source path, exact GOLD parent path, and `Adenoma` found the cited GOLD inventory, decision, path-lock, and generated-record rows. Those searches excluded `build`, `data/text_map`, and `pages` so generated projections could not hide the maintained inputs.
