# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/aquatic/carbonate.yaml`
- Started UTC: `2026-09-23T23:23:30Z`
- Finished UTC: `2026-09-23T23:26:57Z`
- Verdict: `pass with minor issues`

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/carbonate.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.1208484062` |
| Label | `serpentinization-hosted carbonate hydrothermal vent chimney` |
| Generated or maintained | Generated from `data/raw/gold_ecosystem_paths.tsv`, `curation/decisions.tsv`, and `curation/term_requests.tsv` |
| Habitat category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `REVIEWED` |
| Source concept | `Environmental > Aquatic > Non-marine Saline and Alkaline > Alkaline > Carbonate` |

This is the generated GOLD Carbonate leaf beneath Non-marine Saline and
Alkaline / Alkaline. Its identifier reproduces from
`mint("GOLD", "Environmental > Aquatic > Non-marine Saline and Alkaline > Alkaline > Carbonate")`
as `habitatmech:GOLD.1208484062`, and `data/habitats/PATHS.tsv` pins the
identifier to the corpus-wide `carbonate` slug.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/carbonate.yaml` | Passed; LinkML accepted the record as `HabitatRecord`. |
| `just validate-strict data/habitats/aquatic/carbonate.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Focused causal validator | Not applicable: ignored-inclusive `find`/`rg` checks found no `curation/causal_graphs/*carbonate*` overlay and no causal overlay mentioning `habitatmech:GOLD.1208484062`. |
| `just validate-causal-all` | Passed; 32 curated causal-graph files with 32 graphs validated. |
| Reference validator | Not applicable: `CLAUDE.md` and `justfile` do not document a standalone reference validator for records without causal overlays. |
| `just term-requests-check` | Passed; `curation/term_requests/envo_robot_template.tsv` is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 records expected, 3,206 found, 0 missing, 0 extra, 0 differing. |
| `just worklist --limit 2000` | Passed; 0 ungrounded records still undecided, 1,810 decisions on file. |
| `just report` | Passed; corpus report completed after counting 3,206 records and 686 `REVIEWED` records. |
| `git diff --check` | Passed. |

## Identity and Grounding

The source identity is internally consistent. `data/raw/gold_ecosystem_paths.tsv`
has one collapsed GOLD ecosystem concept for the exact canonical path, with
leaf label `Carbonate`, `gold_node_count` 1, `organism_count` 12,
`study_count` 0, `biosample_count` 0, `total_assertions` 12, and
`gold_node_ids` `gold.ecosystem:3969`. The generated `source_attestations`
entry carries the same source id, label, path, count, and `ORGANISM` unit, with
no count aggregation across unlike source units.

The curated grounding is item-level and uses the right relation shape for a
novel habitat. `curation/decisions.tsv` has an `ITEM`-depth
`CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.1208484062`; the row keeps
`ENVO:01000053` `marine ultramafic hydrothermal vent biome` as an `xref`
rather than as the record identity or a broader parent. `ENVO:01000053` is
present in `data/raw/ontology_terms.tsv` with that canonical label.

The requested genus is also valid. `curation/term_requests.tsv` defines the
record under `ENVO:01000129` `marine hydrothermal vent chimney`; that CURIE is
vendored in `data/raw/ontology_terms.tsv`, and the generated
`parent_habitats` entry matches the term-request parent. The term request uses
`parent_mode=REPLACE`, which is the maintained input that deliberately removes
the inherited GOLD `Alkaline` parent after item-level review found the
intermediate GOLD bucket to be a pH qualifier and a source-classification
artifact for this leaf rather than a broader habitat.

The `REVIEWED` mapping status follows from the single contributing GOLD source
concept having an `ITEM` decision. There is no multi-source merge whose second
source concept could leave the record only partially reviewed.

## Evidence

The generated record is traceable to maintained inputs:

| Claim | Maintained input or inventory |
|---|---|
| GOLD source path and 12 `ORGANISM` assertions | `data/raw/gold_ecosystem_paths.tsv` row for `gold.ecosystem:3969` |
| `CONFIRM_UNGROUNDED` status and `ENVO:01000053` xref | `curation/decisions.tsv` row for `habitatmech:GOLD.1208484062` |
| Label, definition, exact synonyms, `ENVO:01000129` parent, and `REPLACE` parent mode | `curation/term_requests.tsv` row for `habitatmech:GOLD.1208484062` |
| Published ENVO term-request export | `curation/term_requests/envo_robot_template.tsv` |
| Two curator-history events for the September 2026 decision and definition edits | `history/mappings/carbonate/` |

The maintained notes say the inspected GOLD organism rows resolved to Prony
Hydrothermal Field carbonate-chimney isolates, not to soda-lake water or to
carbonate ion. A spot-check of open PubMed/PMC/Frontiers records corroborates
the biological interpretation at the concept level: Postec et al. 2015,
DOI `10.3389/fmicb.2015.00857`, sampled active carbonate chimneys from the
shallow marine serpentinizing Prony Hydrothermal Field, and Pisapia et al.
2017, DOI `10.3389/fmicb.2017.00057`, describes the same field as discharging
high-pH hydrogen- and methane-rich fluids from carbonate chimneys at the
seafloor in a shallow lagoonal setting. The external spot-check supports the
current definition's parent and differentia; it is not itself a maintained
HabitatMech input.

No causal graph is attached to this record, so there are no mechanism edges
needing claim-level `EvidenceItem` support.

## Completeness

The required identity, source attestation, grounding status, reviewed mapping
status, parent, xref, curation history, and source-derived `Carbonate` synonym
are present.

The empty optional slots are acceptable:

| Slot | Review |
|---|---|
| `environmental_parameters` | Empty. The GOLD path has no committed biosample triad or environment-parameter row in `data/raw/`, and no curator-authored parameter row is expected for a review-only change. |
| `characteristic_taxa` | Empty. The GOLD aggregate commits counts but not organism names or taxon identities for `gold.ecosystem:3969`. |
| `evidence` | Empty. Optional for record-level literature; the current evidence lives in the maintained decision and term-request rows. |
| `causal_graphs` | Empty. Exact ignored-inclusive searches found no `Carbonate` overlay or overlay keyed to `habitatmech:GOLD.1208484062`. |
| `discussions` and `datasets` | Empty. No maintained discussion or dataset input exists for this record. |

Exact ignored-inclusive searches for `habitatmech:GOLD.1208484062`,
`gold.ecosystem:3969`, the GOLD source path, the curated label, `Prony`, and
the report-backed synonyms found the generated record, the generated path and
term-request outputs, the two maintained curation rows, two append-only history
records, and the expected retired page slug. They did not find a dedicated
checked-in deep-research report for this identifier or the exact GOLD path.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | None found | The record validates, reproduces from source inventories, and denotes the intended GOLD source concept. | Not applicable |
| Major | None found | The minted identity, `UNGROUNDED` decision, `REVIEWED` status, parent, xref, and source attestation all trace to maintained rows or committed raw inventory. | Not applicable |
| Minor | The primary Carbonate evidence review is summarized but not durable as a checked-in evidence artifact. The term-request note says ten of the twelve current organism assertions were inspected, found only Prony Hydrothermal Field carbonate-chimney isolates, and left two rows unchecked because live GOLD returned 403; however, exact ignored-inclusive searches found no `research/habitats/` report, citation list, or local raw organism-row artifact for `habitatmech:GOLD.1208484062`, `gold.ecosystem:3969`, or the exact source path. A future curator cannot re-open the inspected organism-row/source mapping from the repository alone. | `curation/term_requests.tsv`, `curation/decisions.tsv`, and `history/mappings/carbonate/` all reference the prior evidence review, while `find research reports -type f -name '*1208484062*' -print`, `find research reports -type f -name '*carbonate*' -print`, and exact ignored-inclusive `rg` searches found no target-specific report. | `research/habitats/` if a prior report can be recovered; otherwise `curation/term_requests.tsv` or a new maintained evidence-support surface for the inspected source list |

## Recommended Edits

| Priority | Edit | Maintained path |
|---|---|---|
| Minor | Recover the original Carbonate evidence report, or add an equivalent durable citation and organism-row support note that names the inspected GOLD rows and the papers proving they came from Prony carbonate chimneys. Keep the existing unresolved note for the two post-2021 rows unless those rows can be inspected. | Prefer `research/habitats/aquatic/`; otherwise update `curation/term_requests.tsv` through the normal curation workflow |

Do not hand-edit `data/habitats/aquatic/carbonate.yaml`; regenerate it from the
maintained input if the curation notes change.

## Follow-up Checks

| Edit | Narrowest proving checks |
|---|---|
| Durable Carbonate evidence support | Exact ignored-inclusive `rg` for `habitatmech:GOLD.1208484062`, `gold.ecosystem:3969`, `Prony`, and the GOLD source path should find the restored report or expanded maintained note; `just seed-canary habitatmech:GOLD.1208484062`, `just term-requests-check`, `just validate data/habitats/aquatic/carbonate.yaml`, `just validate-strict data/habitats/aquatic/carbonate.yaml`, `just validate-history`, and `just verify-corpus --max-diffs 1` should still pass. |

## Additional Notes

- Searches used to establish absence included ignored and hidden files. Broad
  exact content searches excluded `data/text_map/**`, `pages/**`, and
  `build/**` so generated text-map, rendered-page, and build products did not
  dominate the results.
- The source label `Carbonate` is ambiguous in isolation, but its GOLD source
  path plus the item-level curation row bind this record to the Prony
  serpentinization chimney concept.
- `data/habitats/terrestrial/marine_hydrothermal_vent_chimney.yaml` is the
  generated PREGO record for the broader parent term `ENVO:01000129`; its
  `TERRESTRIAL` category is a separate heuristic-classification artifact and
  does not change the reviewed Carbonate record's GOLD-derived `AQUATIC`
  category.
