# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/engineered/sieved.yaml`
- Started UTC: `2026-09-25T02:52:15Z`
- Finished UTC: `2026-09-25T02:53:30Z`
- Verdict: needs curation

## Target

Reviewed the complete generated record at `data/habitats/engineered/sieved.yaml`. The record is a GOLD-only engineered habitat for `Engineered > Artificial ecosystem > Mesocosm > Lake water > Sieved`, minted as `habitatmech:GOLD.0bab26d396`.

The generated file has `grounding_status: UNGROUNDED` and `mapping_status: SEEDED`. Its only parent is `ENVO:04000007` `lake water`, inherited from the generated GOLD parent path `Engineered > Artificial ecosystem > Mesocosm > Lake water`. Its only source attestation shows `gold.ecosystem:7860`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/sieved.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/sieved.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Focused causal overlay validator | Not applicable; exact ignored-inclusive searches in `curation/causal_graphs/` found no `habitatmech:GOLD.0bab26d396`, `gold.ecosystem:7860`, `sieved`, or exact Sieved source-path overlay. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| Reference validator | Not applicable; the target record has no record-level `evidence` entries and no causal graph edges with citation references. |
| `just term-requests-check` | Passed; the generated ENVO term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records all exist on disk, with 0 missing, 0 extra, and 0 differing records. |
| `just worklist --limit 2000` | Passed; it reported 0 still-undecided ungrounded records and 1,810 decisions on file. |
| `just report` | Passed; the corpus report completed for 3,206 records. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

The generated identifier, label, category, and source attestation agree with the committed GOLD inventory. `data/raw/gold_ecosystem_paths.tsv` has the exact `Engineered > Artificial ecosystem > Mesocosm > Lake water > Sieved` row at depth 5 with one GOLD node, no aggregate organism, study, or biosample assertions in that table, and node ID `gold.ecosystem:7860`. `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.0bab26d396` to the stable `sieved` slug.

The generated `UNGROUNDED` status comes from `curation/decisions.tsv`, which has only a class-level `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.0bab26d396`. That row records a lexical miss for the bare `Sieved` leaf, but its note explicitly says the source concept's habitathood was not assessed. The exact GOLD path and GOLD API side tables do have more context than the bare leaf: `data/raw/gold_path_biosamples.tsv` has 88 biosamples for this path, `data/raw/gold_studies.tsv` has one study, and `data/raw/gold_path_triads.tsv` records `ENVO:01000620` `mesocosm` as the local slot plus `ENVO:00002011` `fresh water` as the medium slot.

The sole generated parent, `ENVO:04000007` `lake water`, also needs item review in this engineered source context. The inherited parent path `Engineered > Artificial ecosystem > Mesocosm > Lake water` is currently exact-grounded to `ENVO:04000007`, whose definition is `Water which is part of a lake.` The child source path and its GOLD triad instead place the sample in a `mesocosm` local feature with `fresh water` as the medium, so the current `lake water` parent may be an over-literal grounding of the source-path label rather than a strict broader class for sieved mesocosm water.

## Evidence

- Supported: the `GOLD` source attestation is traceable to `data/raw/gold_ecosystem_paths.tsv`, including `gold.ecosystem:7860` as the sole collapsed GOLD node ID.
- Supported: GOLD API side tables have direct evidence for the exact source path: 88 biosamples in `data/raw/gold_path_biosamples.tsv`, one study in `data/raw/gold_studies.tsv`, and MIxS broad, local, and medium triad rows in `data/raw/gold_path_triads.tsv`.
- Supported: the `CONFIRM_UNGROUNDED` history event in the generated record mirrors the class-level decision row from `curation/decisions.tsv`.
- Under-reviewed: the record has no item-level decision, authored definition, term request, record-level evidence, discussion, dataset, characteristic taxon, environmental parameter, or causal graph.
- Unsupported pending item review: the generated hierarchy treats the target as a kind of in-lake `ENVO:04000007` `lake water`; the available path and triad evidence establish an engineered mesocosm context and should be reviewed before that parent is kept.

## Completeness

The absence of `assertion_count` and `assertion_unit` in the generated `source_attestations` block agrees with the zero organism count in `data/raw/gold_ecosystem_paths.tsv`; the later GOLD API biosample and study side tables are preserved as separate source context but do not emit those two fields. The absence of characteristic taxa and environmental parameters is also expected: source occurrence and the MIxS broad/local/medium rows do not automatically produce characteristic taxa or physicochemical parameter values.

Exact ignored-inclusive searches in `history/`, `research/`, `curation/causal_graphs/`, `curation/term_requests.tsv`, `curation/term_requests/`, and `conf/id_label_targets.yaml` found no target-specific history entry, research report, causal overlay, minted definition, generated term-request row, or label-correspondence residual for `habitatmech:GOLD.0bab26d396`, `gold.ecosystem:7860`, `sieved`, or the exact source path. The broader lowercase `sieved` search found only an unrelated prose mention of a sieved fraction in a Dust-ash research report.

`find reports/yaml_record_review -maxdepth 1 -type f -name '*-sieved.md' -print` found no existing `sieved` review before this report was written. `find` is not `.gitignore`-filtered, so ignored files were included in that absence check.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| Major | `Sieved` remains only class-reviewed even though the full GOLD path and GOLD triad show this is not just the bare quality or process label `Sieved`. | The only maintained row for `habitatmech:GOLD.0bab26d396` is a `CLASS` `CONFIRM_UNGROUNDED` row whose note says habitathood was not assessed. The exact source path is `Engineered > Artificial ecosystem > Mesocosm > Lake water > Sieved`, and the GOLD API triad records `ENVO:01000620` `mesocosm` as local context plus `ENVO:00002011` `fresh water` as the medium. | `curation/decisions.tsv`; if no exact term fits, `curation/term_requests.tsv` |
| Major | The generated `ENVO:04000007` `lake water` parent needs item-level review because the source context is engineered mesocosm water rather than a portion of water still part of a lake. | `data/habitats/engineered/sieved.yaml` lists `ENVO:04000007`; the immediate generated parent `data/habitats/engineered/lake_water.yaml` exact-grounds the GOLD `Mesocosm > Lake water` path to ENVO's `Water which is part of a lake.` term; and this child path's MIxS local slot is `ENVO:01000620` `mesocosm`, not a lake. | `curation/decisions.tsv`; if an authored Sieved term is needed, `curation/term_requests.tsv` with the right parent mode |

## Recommended Edits

1. Item-review `habitatmech:GOLD.0bab26d396` in `curation/decisions.tsv` against the exact source path, the API-side Mesocosm/Fresh water GOLD triad, `ENVO:04000007` `lake water`, `ENVO:01000620` `mesocosm`, and `ENVO:00002011` `fresh water`.
2. If no exact ontology term fits, keep the minted identity and either record a `GROUND_AS_PARENT` decision to a strict broader term or leave a reviewed `CONFIRM_UNGROUNDED` decision with the nearby water and mesocosm candidates explicitly rejected or scoped.
3. If future review confirms that the inherited `ENVO:04000007` parent is false for the engineered mesocosm context, add a `curation/term_requests.tsv` definition for a path-qualified sieved lake-water mesocosm with `parent_mode=REPLACE` so regeneration can drop the in-situ lake-water parent and retain only true authored parents.

## Follow-up Checks

- Run `just seed`, then `just seed-canary habitatmech:GOLD.0bab26d396`, and inspect `data/habitats/engineered/sieved.yaml` before any wider `just seed-apply --force`.
- Run `just validate data/habitats/engineered/sieved.yaml`, `just validate-strict data/habitats/engineered/sieved.yaml`, `just term-requests-check`, `just validate-history`, `just verify-corpus --max-diffs 1`, `just worklist --limit 2000`, `just report`, and `git diff --check` after the maintained input changes.

## Additional Notes

The review did not find a target causal overlay, so there was no focused `just validate-causal curation/causal_graphs/<slug>.yaml` invocation to run. The whole-overlay validator still passed.

The immediate parent record `data/habitats/engineered/lake_water.yaml` has not yet been reviewed in `reports/yaml_record_review/`. Item-reviewing that parent before or with this child would make the Sieved hierarchy easier to regenerate coherently.
