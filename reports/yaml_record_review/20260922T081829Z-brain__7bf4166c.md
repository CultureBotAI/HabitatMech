# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/brain__7bf4166c.yaml`
- Started UTC: 2026-09-22T08:18:29Z
- Finished UTC: 2026-09-22T08:18:29Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.752ac1c5aa` |
| Label | `Brain` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the GOLD ecosystem-path inventory plus the vendored ontology slice; generated YAML remains read-only |
| Locked slug | `data/habitats/PATHS.tsv:2153` maps `habitatmech:GOLD.752ac1c5aa` to `brain__7bf4166c` |

This is the seeded GOLD record for `Host-associated > Mammals: Human > Nervous system > Brain`. It intentionally remains a minted, `NARROW` child of `UBERON:0000955` because several GOLD host-associated branches end in the same `Brain` leaf and none is identical to the generic ontology class.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/brain__7bf4166c.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/brain__7bf4166c.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was staged |

## Identity and Grounding

The minted identity, source path, ontology parent, and generated GOLD parent are supported by the raw GOLD inventory and path lock:

- `data/habitats/PATHS.tsv:2153` pins `habitatmech:GOLD.752ac1c5aa` to slug `brain__7bf4166c`, matching the reviewed YAML path.
- `GOLD:Host-associated > Mammals: Human > Nervous system > Brain` hashes to `752ac1c5aa`, matching the generated record identifier.
- `data/raw/gold_ecosystem_paths.tsv:137` is the exact raw GOLD path, gives leaf label `Brain`, records depth `4`, aggregates two GOLD ecosystem node ids, names `gold.ecosystem:4495|gold.ecosystem:4496`, and carries 167 organism assertions.
- `data/raw/gold_ecosystem_paths.tsv:137`, `:209`, `:331`, `:525`, and `:1110` show five sibling host-associated GOLD source concepts with the leaf label `Brain`, so the human nervous-system child keeps a minted identity and uses `UBERON:0000955` as a broader parent.
- `data/raw/ontology_terms.tsv:12968` defines `UBERON:0000955` as `brain`, marks it directly referenced, and supplies synonyms including `encephalon` and `the brain`.
- `data/raw/gold_ecosystem_paths.tsv:527` is the immediate GOLD parent path `Host-associated > Mammals: Human > Nervous system`, and `data/habitats/PATHS.tsv:1406` pins its generated identifier `habitatmech:GOLD.11bece2c6f` to `nervous_system__b6871e1f`.

`grounding_status: NARROW` and `mapping_predicate: skos:narrowMatch` are expected for an ambiguous GOLD leaf that is more specific than the ontology `brain` term. `mapping_status: SEEDED` is also expected because ignored/hidden-inclusive exact searches found no item-level `curation/decisions.tsv` row for `habitatmech:GOLD.752ac1c5aa`.

## Evidence

Every generated scientific claim in this record is source-derived:

| Claim | Nearest source | Review |
|---|---|---|
| Minted identifier and locked filename | `data/habitats/PATHS.tsv:2153` plus the GOLD source-path hash | Supported exactly |
| GOLD `source_id: gold.ecosystem:4495`, label, full source path, 2-node note, 167-organism assertion count, and `ORGANISM` unit | `data/raw/gold_ecosystem_paths.tsv:137` | Supported exactly |
| `parent_habitats: UBERON:0000955` and `skos:narrowMatch` | `data/raw/gold_ecosystem_paths.tsv` rows 137, 209, 331, 525, and 1110 plus `data/raw/ontology_terms.tsv:12968` | Supported; the repeated `Brain` leaf makes this GOLD path narrower than `UBERON:0000955` |
| Immediate generated GOLD parent `habitatmech:GOLD.11bece2c6f` | `data/raw/gold_ecosystem_paths.tsv:527`, `data/habitats/PATHS.tsv:1406`, and `data/habitats/host_associated/nervous_system__b6871e1f.yaml` | Supported exactly |

The record has no definition, synonyms, xrefs, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussion links, or datasets. Those absences are consistent with the exact ignored/hidden-inclusive target searches that found no maintained decision, term request, causal overlay, or research artifact for `habitatmech:GOLD.752ac1c5aa`. Exact GOLD side-table searches found no row for the target path in `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_studies.tsv`, or `data/raw/gold_path_triads.tsv`.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- The sole `source_attestations` entry captures the first GOLD ecosystem node id, source label, complete source path, `skos:narrowMatch` predicate, organism assertion count, and two-node aggregation note.
- `parent_habitats` preserves both broader meanings needed here: `UBERON:0000955` for the ontology brain parent and `habitatmech:GOLD.11bece2c6f` for the immediate GOLD path parent.
- No `REVIEWED` status is emitted because no item-level curator decision overrides the seeded GOLD ambiguous-leaf result.
- The record correctly has no characteristic taxa because the GOLD ecosystem-path inventory contributes only organism-level assertion counts, not ranked taxa.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated target. They found the expected raw GOLD source row, raw sibling `Brain` rows, raw GOLD parent row, UBERON ontology row, path locks, and generated YAML, and no maintained decision, term request, history record, causal-graph overlay, target-specific research report, or prior exact YAML review report for `habitatmech:GOLD.752ac1c5aa`.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if future curation changes the GOLD Brain ambiguous-leaf resolution, UBERON parent, GOLD source-path hierarchy, or assertion rollup:

- `just validate data/habitats/host_associated/brain__7bf4166c.yaml`
- `just validate-strict data/habitats/host_associated/brain__7bf4166c.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If future curation reviews this source path, keep its immediate GOLD parent separate from the ontology parent and rerun `just seed-canary habitatmech:GOLD.752ac1c5aa` to confirm the generated `source_attestations` note still reflects the two GOLD node ids.

## Additional Notes

- `find reports/yaml_record_review -name '*brain__7bf4166c*' -print` found no pre-existing exact review report for this record before this file was written.
- `find curation history research reports -name '*brain__7bf4166c*' -print` found no target-specific maintained history, research, report, or causal overlay artifacts before this file was written.
- Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.752ac1c5aa`, `gold.ecosystem:4495`, and `Host-associated > Mammals: Human > Nervous system > Brain` found the path lock, generated YAML, and raw GOLD rows cited above.
