# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/abscess__0f7f7357.yaml`
- Started UTC: 2026-09-22T23:00:00Z
- Finished UTC: 2026-09-22T23:05:18Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/host_associated/abscess__0f7f7357.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.d1d916a248` |
| Label | `Abscess` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Record status | Generated from one GOLD source concept with no item-level curation decision |

This is the generated record for the GOLD path `Host-associated > Mammals: Human > Multisystem conditions > Soft tissues > Abscess`. The GOLD source-concept key is `habitatmech:GOLD.d1d916a248`, computed from `sha1("GOLD:Host-associated > Mammals: Human > Multisystem conditions > Soft tissues > Abscess")[:10]`. The seeder keeps it minted because the generic `mesh:D000038` `Abscess` leaf match is broader than this soft-tissue-specific GOLD path and is also shared by several other GOLD abscess paths.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/abscess__0f7f7357.yaml` | Passed; LinkML validation reported no issues. |
| `just validate-strict data/habitats/host_associated/abscess__0f7f7357.yaml` | Passed; one file scanned with 0 error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 generated records, found 3206, with 0 missing, 0 extra, and 0 differing. |
| `just worklist` filtered for `Abscess`, `abscess`, `GOLD\.d1d916a248`, and `mesh:D000038` | Completed with no matching rows, as expected for a seeded `NARROW` GOLD record that is not in the class-level ungrounded backlog. |
| `just report` | Passed; the report regenerated and did not place the reviewed concept in the risky-grounding, stale-sweep, current non-habitat, same-label, GOLD-triad, unsupported-prefix, or organism-identity sections. |
| `git diff --check` | Passed; no whitespace errors. |

## Identity and Grounding

The GOLD source identity is supported:

| Raw field | Value |
|---|---|
| `canonical_path` | `Host-associated > Mammals: Human > Multisystem conditions > Soft tissues > Abscess` |
| `ecosystem` | `Host-associated` |
| `ecosystem_category` | `Mammals: Human` |
| `ecosystem_type` | `Multisystem conditions` |
| `ecosystem_subtype` | `Soft tissues` |
| `specific_ecosystem` | `Abscess` |
| `leaf_label` | `Abscess` |
| `depth` | `5` |
| `gold_node_count` | `1` |
| `organism_count` | `0` |
| `study_count` | `0` |
| `biosample_count` | `0` |
| `total_assertions` | `0` |
| `gold_node_ids` | `gold.ecosystem:7038` |

`source_attestations` matches `data/raw/gold_ecosystem_paths.tsv:2359`: the record keeps `gold.ecosystem:7038` as the source id and records the `Abscess` leaf label and canonical GOLD path exactly. It correctly omits `assertion_count` and `assertion_unit` because the raw row has zero organism, study, biosample, and total assertions.

The `habitatmech:GOLD.523243d1da` parent is supported. `data/raw/gold_ecosystem_paths.tsv:2358` records the immediate parent path `Host-associated > Mammals: Human > Multisystem conditions > Soft tissues`, and `data/habitats/host_associated/soft_tissues.yaml` is the generated record for that path.

The `mesh:D000038` parent is supported. `data/raw/ontology_terms.tsv:13554` defines `mesh:D000038` with label `Abscess`, and this soft-tissue abscess source concept is narrower than a generic abscess. `data/raw/gold_ecosystem_paths.tsv` contains other `Abscess` leaves at abdominal/peritoneal, nasal-cavity, brain, spinal-cord, joint-capsule, skin, and soft-tissue paths, so the seeder's ambiguous-leaf rule correctly prevents this one path from adopting `mesh:D000038` as an exact identity.

`grounding_status: NARROW` is mechanically expected for an uncurated GOLD source path whose leaf label matches a broader ontology term but is not the unique defensible claimant of that term. `mapping_status: SEEDED` is also expected: ignored/hidden-inclusive exact searches found no maintained `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/term_requests_excluded.tsv`, `curation/causal_graphs`, or `history` row for `habitatmech:GOLD.d1d916a248`.

## Evidence

| Claim | Nearest support | Review |
|---|---|---|
| The generated record represents the GOLD path `Host-associated > Mammals: Human > Multisystem conditions > Soft tissues > Abscess`. | `data/raw/gold_ecosystem_paths.tsv:2359` | Supported exactly. |
| The GOLD source row has one node and zero organism, study, biosample, and total assertions. | `data/raw/gold_ecosystem_paths.tsv:2359` | Supported exactly. |
| `habitatmech:GOLD.523243d1da` is the source-path parent. | `data/raw/gold_ecosystem_paths.tsv:2358`; `data/habitats/host_associated/soft_tissues.yaml` | Supported. A soft-tissue abscess record is narrower than the parent soft-tissue GOLD path. |
| `mesh:D000038` `Abscess` is broader than the soft-tissue-specific GOLD source concept. | `data/raw/ontology_terms.tsv:13554`; `data/raw/gold_ecosystem_paths.tsv:448,655,679,687,761,1093,2359` | Supported. The source path supplies the soft-tissue context that makes this a narrower abscess record rather than the generic abscess identity. |

Unsupported or over-scoped claims: None found. The record carries only GOLD source provenance, the generic abscess parent, the GOLD source-path parent, and seeded audit metadata. It does not claim characteristic taxa, environmental parameters, causal mechanisms, or literature-backed definition text.

## Completeness

The direct GOLD source attestation is complete for a zero-assertion, one-node raw row: the record carries the source name, source id, source label, source path, and mapping predicate, while correctly omitting counts, an assertion unit, and the multi-node note used for collapsed paths.

Separate committed context does not imply a missing maintained field. An ignored/hidden-inclusive exact search for `gold.ecosystem:7038` across the raw, curation, history, and generated record inputs found only the GOLD path row and this generated record. An ignored/hidden-inclusive exact search for the full canonical path found only `data/raw/gold_ecosystem_paths.tsv`; the generated record stores the same scalar folded across its `source_path` lines.

Ignored/hidden-inclusive exact searches found no maintained `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/term_requests_excluded.tsv`, `curation/causal_graphs`, or `history` row for `habitatmech:GOLD.d1d916a248`; generated `data/text_map`, `pages`, and `build` outputs were excluded only from broad exact content searches.

No maintained causal overlay, history file, or prior exact YAML review report was found by filename for `abscess__0f7f7357` under `curation/causal_graphs`, `history`, or `reports/yaml_record_review`; the `find` search included ignored files beneath those checked directories. `find research/habitats -name '*abscess*' -print` also found no abscess-named raw research report.

## Findings

None found.

No blocker findings.

No major findings.

No minor findings.

## Recommended Edits

None required.

If a future curator wants to mark the GOLD narrow leaf review-complete, add a `REVIEW` row for `habitatmech:GOLD.d1d916a248` in `curation/decisions.tsv` and rerun `just seed-canary habitatmech:GOLD.d1d916a248`. That should be a status-only change: keep the minted identifier, keep `grounding_status: NARROW`, and keep both existing parents.

## Follow-up Checks

None required beyond the validators already run for this report.

For any future status-promotion-only edit, run:

- `just seed-canary habitatmech:GOLD.d1d916a248`
- `just validate data/habitats/host_associated/abscess__0f7f7357.yaml`
- `just validate-strict data/habitats/host_associated/abscess__0f7f7357.yaml`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

## Additional Notes

- The pre-report exact filename check `find reports/yaml_record_review -maxdepth 1 -type f -name '*-abscess__0f7f7357.md' -print` returned no previous exact report; `find` includes ignored files.
- `just worklist` did not list `habitatmech:GOLD.d1d916a248`, `mesh:D000038`, or `Abscess`, consistent with a seeded `NARROW` GOLD leaf rather than an undecided ungrounded source concept.
- `just report` completed successfully and did not flag `mesh:D000038` or `habitatmech:GOLD.d1d916a248` in any risky-grounding, stale-sweep, current non-habitat, same-label, GOLD-triad-compatibility, minted-GOLD-triad, unsupported-prefix, or organism-identity section.
- Existing item-level `NOT_APPLICABLE` rows for `Abscess: Furuncle/Boil` and `Abscess: Pilonidal sinus` are decisions about those colon-expanded skin leaves only; they do not apply to this plain soft-tissue `Abscess` source concept.
