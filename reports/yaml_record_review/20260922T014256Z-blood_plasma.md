# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/blood_plasma.yaml`
- Started UTC: 2026-09-22T01:42:56Z
- Finished UTC: 2026-09-22T01:43:07Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `BTO:0000131` |
| Label | `blood plasma` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the vendored BTO ontology slice and PREGO raw tables; future curation belongs in `curation/decisions.tsv` or `curation/causal_graphs/*.yaml`, not in this generated YAML |
| Locked slug | `data/habitats/PATHS.tsv:26` maps `BTO:0000131` to `blood_plasma` |

This is the BTO-grounded PREGO blood-plasma record. It is intentionally reviewed separately from `data/habitats/host_associated/blood_plasma__00b70c6a.yaml`, the UBERON `blood plasma` record seeded from GOLD Mammals and Mammals: Human `Plasma` leaves.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/blood_plasma.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/blood_plasma.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors |

## Identity and Grounding

The exact BTO identity is supported by the vendored ontology slice and the PREGO direct assertion:

- `data/habitats/PATHS.tsv:26` pins `BTO:0000131` to `blood_plasma`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:133` defines `BTO:0000131` with canonical label `blood plasma`, definition `The fluid portion of the blood in which the particulate components are suspended.`, `active: TRUE`, and `obsolete: FALSE`.
- `data/raw/prego_habitats.tsv:306` directly asserts `BTO:0000131` with ontology `BTO`, category `biolink:GrossAnatomicalStructure`, label/synonym text `blood plasma|blood plasmal|blood plasmas|plasma|plasmal|plasmas`, 15 associated taxa, 16 direct assertions, score `3`, and channel `annotated_genomes_isolates`.
- BTO records fall through `PREFIX_CATEGORY` as `HOST_ASSOCIATED`, so the generated `habitat_category: HOST_ASSOCIATED` is consistent with the record identifier.
- The stable PREGO curation key is `habitatmech:PREGO.cedc68741c`, computed as `sha1("PREGO:BTO:0000131")[:10]`. There is no item-level row for that key in `curation/decisions.tsv`, so the generated `mapping_status: SEEDED` is expected.

`parent_habitats` is correctly absent. An exact ignored/hidden-inclusive search for `BTO:0000131` in `data/raw/ontology_subclass_edges.tsv` found no vendored subclass edge with this class as child or parent.

## Evidence

Every generated scientific claim in this record is source-derived:

| Claim | Nearest source | Review |
|---|---|---|
| BTO identifier, canonical label, definition, and active non-obsolete status | `data/raw/ontology_terms.tsv:133` | Supported exactly |
| PREGO source ID, source label, `assertion_count: 15`, `assertion_unit: TAXON`, `score: 3.0`, and `evidence_channels: annotated_genomes_isolates` | `data/raw/prego_habitats.tsv:306` | Supported; `assertion_count` comes from PREGO `taxon_count`, not the separate `direct_assertion_count` value of 16 |
| PREGO synonyms `blood plasmal`, `blood plasmas`, `plasma`, `plasmal`, and `plasmas` | `data/raw/prego_habitats.tsv:306` | Supported; `blood plasma` duplicates the record label and is correctly omitted from the YAML synonym list |
| The 15 inline PREGO taxon associations | `data/raw/prego_habitat_taxa.tsv:143-157` | Supported exactly in rank order, with `score: 3.0`, source `PREGO`, and `candidate_pool: 15` |

The record has no record-level evidence, environmental parameters, causal graphs, discussion links, or datasets. That absence is consistent with the maintained inputs: no row in `curation/term_requests.tsv`, no causal overlay in `curation/causal_graphs/`, and no source inventory row supplies physicochemical parameters, discussions, or datasets for `BTO:0000131`.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- The sole `source_attestations` entry captures the direct PREGO `BTO:0000131` source concept.
- All five non-label PREGO lexical variants are represented as `RELATED_SYNONYM` entries.
- All 15 PREGO habitat-taxon rows for `BTO:0000131` are represented once in `characteristic_taxa`.
- `parent_habitats` is empty because the vendored BTO slice has no subclass edge for `BTO:0000131`.
- `characteristic_taxa.corroborated_by` is absent because the raw `corroborated_by` column is blank for all 15 rows.
- `mapping_status` stays `SEEDED`, as expected; there is no item-level curation decision for `habitatmech:PREGO.cedc68741c`.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research/habitats`, `reports/yaml_record_review`, `docs`, `.claude`, `src`, `data/habitats/PATHS.tsv`, and the exact raw TSVs that can feed BTO/PREGO records, while excluding generated `data/text_map/**` and `pages/**`. They found the raw rows and path lock cited above, related mentions of the UBERON `blood_plasma__00b70c6a` sibling, and no maintained decision row, term request, history record, causal-graph overlay, target-specific research report, or prior exact YAML review report for `blood_plasma`.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if a future curation change adds a reviewed decision or causal graph overlay for this BTO-grounded blood-plasma record:

- `just validate data/habitats/host_associated/blood_plasma.yaml`
- `just validate-strict data/habitats/host_associated/blood_plasma.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If future cross-ontology curation decides to merge this BTO/PREGO source concept into the same identity as UBERON `blood plasma`, add the maintained decision to `curation/decisions.tsv`, run `just seed`, preview `just seed-canary BTO:0000131`, and then rerun the corpus-level checks after regeneration.

## Additional Notes

- `find reports/yaml_record_review -name '*blood_plasma*' -print` found no pre-existing exact review report for this record before this file was written.
- The same-label `UBERON:0001969` record at `data/habitats/host_associated/blood_plasma__00b70c6a.yaml` is generated from two GOLD source paths and has a separate locked slug at `data/habitats/PATHS.tsv:1050`; it does not feed this PREGO-only `BTO:0000131` generated record.
- Related exact-search hits in prior reports and `docs/HARMONIZATION.md` describe neighboring blood or UBERON blood-plasma records; they are context only and are not maintained inputs to the target reviewed here.
