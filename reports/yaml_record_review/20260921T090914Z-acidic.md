# YAML Record Review: Acidic

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/acidic.yaml
- Started UTC: 2026-09-21T09:07:10Z
- Finished UTC: 2026-09-21T09:09:14Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/acidic.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.8ee6ff626d` |
| Label | `Acidic` |
| Category | `AQUATIC` |
| Grounding status | `NOT_APPLICABLE` |
| Mapping status | `REVIEWED` |
| Maintained/generated status | Generated from `data/raw/` inventories and curator inputs; `just verify-corpus --max-diffs 1` reproduces it byte-for-byte. |

The generated record is an item-reviewed GOLD qualifier record:

- GOLD supplies the source concept `Environmental > Aquatic > Artesian spring >
  Acidic`, collapsed from two GOLD ecosystem node IDs,
  `gold.ecosystem:7715|gold.ecosystem:7716`.
- `curation/decisions.tsv` marks the source concept `NOT_APPLICABLE` at
  `ITEM` depth because `Acidic` is a pH band qualifying the artesian-spring
  parent, not a habitat in its own right.
- The stable filename comes from the lockfile row for
  `habitatmech:GOLD.8ee6ff626d` in `data/habitats/PATHS.tsv`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/acidic.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/acidic.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 records on disk, 0 missing, 0 extra, 0 differing. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| Reference validator | Not checked: this record has no DOI/PMID/URL `EvidenceItem`, no causal-edge evidence, and no dedicated `research/habitats/` report for `habitatmech:GOLD.8ee6ff626d`. |

## Identity and Grounding

| Claim | Evidence | Assessment |
|---|---|---|
| The record denotes the GOLD `Environmental > Aquatic > Artesian spring > Acidic` source concept. | `data/raw/gold_ecosystem_paths.tsv` has exactly that canonical path, leaf `Acidic`, depth `4`, `gold_node_count=2`, `organism_count=0`, `total_assertions=0`, and node IDs `gold.ecosystem:7715\|gold.ecosystem:7716`. | Supported exactly. |
| The stable file stem is `acidic`. | `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.8ee6ff626d` to `acidic`. | Supported exactly. |
| `grounding_status: NOT_APPLICABLE` follows from item-level curator input. | `curation/decisions.tsv` has a `NOT_APPLICABLE` row for `habitatmech:GOLD.8ee6ff626d` with `review_depth=ITEM` and the exact GOLD path in its note. | Supported exactly. |
| `mapping_status: REVIEWED` follows from curator input. | The target record has one contributing source concept and its `curation/decisions.tsv` row has `review_depth=ITEM`; README and `docs/CURATION.md` state that item-level decisions promote records to `REVIEWED`. | Supported exactly. |
| `Acidic` should not be adopted as a habitat identity. | The decision row states that this is a pH band qualifying `Environmental > Aquatic > Artesian spring`, and `data/raw/ontology_terms.tsv` has the same label on `PATO:0001429`, a quality term rather than a habitat term. | Supported exactly. |
| The `habitatmech:GOLD.19b5876c23` `Artesian spring` parent follows from the GOLD source path. | The raw path sits immediately under `Environmental > Aquatic > Artesian spring`, and `data/habitats/PATHS.tsv` maps that parent source concept to `artesian_spring`. | Mechanically supported but semantically problematic; see `HM-ACIDIC-001`. |

## Evidence

The generated record has no record-level `evidence` entries and no
`causal_graphs`.

| Claim | Evidence | Assessment |
|---|---|---|
| GOLD is the only upstream attestation feeding this record. | Ignored/hidden-inclusive exact searches for `habitatmech:GOLD.8ee6ff626d`, `gold.ecosystem:7715`, `gold.ecosystem:7716`, and `Environmental > Aquatic > Artesian spring > Acidic` found the target generated record, its `PATHS.tsv` row, its `curation/decisions.tsv` row, and the `data/raw/gold_ecosystem_paths.tsv` row. No BacDive, PREGO, Madin, term-request, or causal-graph curation row matched those exact keys. | Supported exactly. |
| The zero assertion count belongs to the qualifier node only. | Exact ignored/hidden-inclusive GOLD-path searches found no biosample, MIxS triad, or study row for `Environmental > Aquatic > Artesian spring > Acidic` itself; the only biosample, triad, and study rows under this branch use the child path `Environmental > Aquatic > Artesian spring > Acidic > Sediment`. | Supported exactly. |
| The pH-quality disposition is consistent with nearby maintained decisions. | Exact ignored/hidden-inclusive searches for the target decision text found three other GOLD `Acidic` source concepts item-marked `NOT_APPLICABLE` as pH bands rather than habitats. `data/raw/isolation_source_groundings.tsv` also maps BacDive `Acidic` to `PATO:0001429` `acidic`. | Supported exactly. |

## Completeness

- The target is complete enough as an item-reviewed `NOT_APPLICABLE` source
  concept: it has one source attestation, the matching item-level decision, a
  generated curation-history event for that decision, and a seeding event.
- The target correctly lacks a definition, term-request row, characteristic
  taxa, environmental-parameter rows, datasets, and causal graphs because the
  reviewed source concept is a pH quality rather than a habitat.
- Ignored/hidden-inclusive exact searches covered `data/habitats`, `data/raw`,
  `curation`, `research/habitats`, `reports/habitat_research_manifest.tsv`, and
  `reports/yaml_record_review` for `habitatmech:GOLD.8ee6ff626d`,
  `gold.ecosystem:7715`, `gold.ecosystem:7716`, and the full GOLD path. These
  found the expected generated record, `PATHS.tsv` row,
  `curation/decisions.tsv` row, and `data/raw/gold_ecosystem_paths.tsv` row.
- Gitignore-independent `find` checks for `*acidic*` and `*artesian*` under
  `curation/causal_graphs`, `research/habitats`, and
  `reports/yaml_record_review` found no target-specific causal overlay,
  research report, or prior YAML review report.
- The branch under the GOLD qualifier is not empty: the child
  `Environmental > Aquatic > Artesian spring > Acidic > Sediment` path has 28
  biosamples, three MIxS triad rows, one GOLD study row, and a generated record
  at `data/habitats/aquatic/sediment__4ec76487.yaml`.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| `HM-ACIDIC-001` | Major | The reviewed non-habitat pH band is still emitted as a habitat-hierarchy node. `Acidic` is correctly `NOT_APPLICABLE`, but the record still lists `habitatmech:GOLD.19b5876c23` `Artesian spring` in `parent_habitats`, and `data/habitats/aquatic/sediment__4ec76487.yaml` lists this non-habitat `Acidic` record as a parent. That makes a pH quality participate in the is-a habitat graph even though the item-level decision says it is not a habitat in its own right. | `docs/CURATION.md` states that `parent_habitats` is an is-a claim. `curation/decisions.tsv` row `habitatmech:GOLD.8ee6ff626d` says `Acidic` is a pH band qualifying the parent node, not a habitat. `data/habitats/aquatic/acidic.yaml` lists `habitatmech:GOLD.19b5876c23` in `parent_habitats`; `data/habitats/aquatic/sediment__4ec76487.yaml` lists `habitatmech:GOLD.8ee6ff626d` in `parent_habitats`. | `src/habitatmech/seed.py`, or an explicit maintained parent-edge suppression consumed by the seeder. |

No blocker or minor findings found.

## Recommended Edits

1. Update the seeder's GOLD path-parent handling so `NOT_APPLICABLE` source
   concepts do not become `parent_habitats` bridge nodes. A pH, salinity,
   temperature, disease, procedure, or sample-artifact record can stay citable
   without being advertised as a broader habitat.
2. Preserve source context when a non-habitat path segment is skipped. For the
   child `Environmental > Aquatic > Artesian spring > Acidic > Sediment`
   record, either connect to the nearest real habitat ancestor
   `Environmental > Aquatic > Artesian spring`, record acidity as a qualifier
   through a maintained environmental-parameter path, or add a curator-owned
   suppression with an explicit note that the pH band is not a habitat parent.
3. Add a regression that covers both directions of this target bug:
   `data/habitats/aquatic/acidic.yaml` should not claim `Artesian spring` as a
   broader habitat while it is `NOT_APPLICABLE`, and
   `data/habitats/aquatic/sediment__4ec76487.yaml` should not list the
   `NOT_APPLICABLE` `Acidic` concept in `parent_habitats`.
4. Re-seed the target and the artesian acidic sediment child, inspect both
   generated records, then run the corpus integrity and render checks that
   cover parent links and pages.

## Follow-up Checks

| Edit | Narrowest proving check |
|---|---|
| Suppress or skip `NOT_APPLICABLE` nodes in GOLD path parentage. | `just seed`, followed by inspection of the dry-run resolution for `habitatmech:GOLD.8ee6ff626d` and `habitatmech:GOLD.2d7dfa9dc4`. |
| Regenerate the affected canaries. | `just seed-canary habitatmech:GOLD.8ee6ff626d habitatmech:GOLD.2d7dfa9dc4`, then inspect `data/habitats/aquatic/acidic.yaml` and `data/habitats/aquatic/sediment__4ec76487.yaml`. |
| Add the parent-hierarchy regression. | `just test tests/test_decisions.py tests/test_corpus_integrity.py`, or the narrower new test path if the regression belongs elsewhere. |
| Validate the regenerated corpus. | `just validate data/habitats/aquatic/acidic.yaml`, `just validate data/habitats/aquatic/sediment__4ec76487.yaml`, `just validate-strict data/habitats/aquatic/acidic.yaml data/habitats/aquatic/sediment__4ec76487.yaml --quiet`, `just verify-corpus --max-diffs 1`, `just render-check`, and `just validate-history`. |

## Additional Notes

- The same pH-band `NOT_APPLICABLE` reasoning is applied to
  `Environmental > Aquatic > Acidic`,
  `Environmental > Aquatic > Thermal springs > Warm (34-42C) > Acidic`, and
  `Environmental > Aquatic > Thermal springs > Hot (42-90C) > Acidic`.
  A hierarchy fix for this target should check all three sibling `Acidic`
  records.
- `data/raw/isolation_source_groundings.tsv` maps BacDive `Acidic` to
  `PATO:0001429` `acidic`, and `data/raw/ontology_terms.tsv` vendors that term.
  The GOLD decision for `habitatmech:GOLD.8ee6ff626d` does not keep the PATO
  quality as an xref; that is not a correctness issue, because the maintained
  note is already enough to justify `NOT_APPLICABLE`.
