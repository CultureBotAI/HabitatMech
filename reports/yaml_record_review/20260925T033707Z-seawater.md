# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/seawater.yaml
- Started UTC: 2026-09-25T03:37:07Z
- Finished UTC: 2026-09-25T03:37:07Z
- Verdict: pass

## Target

Reviewed the complete generated record at `data/habitats/engineered/seawater.yaml`. The record is a GOLD-only engineered habitat for `Engineered > Artificial ecosystem > Mesocosm > Seawater`, minted as `habitatmech:GOLD.0cadaca0bf`.

The generated file has `label: Seawater`, `habitat_category: ENGINEERED`, `grounding_status: NARROW`, and `mapping_status: REVIEWED`. Its parents are `ENVO:00002149` `sea water`, attached by the item-level `GROUND_AS_PARENT` decision, and `ENVO:01000620` `mesocosm`, inherited from the immediate GOLD source-path parent. Its only source attestation shows `gold.ecosystem:6459` and correctly notes that two GOLD ecosystem node IDs share this canonical path.

The file is generated from `data/raw/` plus `curation/decisions.tsv`. Future changes to the grounding decision belong in `curation/decisions.tsv`; future authored definition or parent-mode changes would belong in `curation/term_requests.tsv`.

## Validation

All required local checks passed:

- `just validate data/habitats/engineered/seawater.yaml`
- `just validate-strict data/habitats/engineered/seawater.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

No validator was skipped.

## Identity and Grounding

The generated identifier, label, category, source attestation, and path slug agree with the maintained inputs. `data/raw/gold_ecosystem_paths.tsv` has the exact `Engineered > Artificial ecosystem > Mesocosm > Seawater` row at depth 4, with two GOLD node IDs, zero aggregate organism, study, biosample, and total assertions, and `gold.ecosystem:6459|gold.ecosystem:6460` as the node list. `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.0cadaca0bf` to the stable `seawater` slug.

The `NARROW` grounding and `REVIEWED` mapping are curator-reviewed. `curation/decisions.tsv` has exactly one row for `habitatmech:GOLD.0cadaca0bf`: an `ITEM` `GROUND_AS_PARENT` decision attaching `ENVO:00002149` `sea water` with inferred status `NARROW`. Its note explains that mesocosm seawater is experimental and held in an artificial ecosystem, so merging it with in-situ sea water would conflate the two concepts.

The broader ENVO terms are present in the vendored ontology slice. `data/raw/ontology_terms.tsv` defines `ENVO:00002149` as water with physicochemical properties determined by processes occurring in a sea or ocean, and `ENVO:01000620` as a vivarium embedded within a natural environment for placing a small part of that environment under experimental control. The generated record keeps the source concept minted and uses both terms only as parents, which matches the item decision and GOLD path.

## Evidence

- Supported as generated source provenance: exact ignored-inclusive searches found `gold.ecosystem:6459`, `gold.ecosystem:6460`, and `Engineered > Artificial ecosystem > Mesocosm > Seawater` in `data/raw/gold_ecosystem_paths.tsv`; `gold.ecosystem:6459` is the first node shown in the generated source attestation.
- Supported as a reviewed parent: `ENVO:00002149` `sea water` is the target of the `GROUND_AS_PARENT` decision in `curation/decisions.tsv`.
- Supported as generated source hierarchy: `ENVO:01000620` `mesocosm` is the identifier for the immediate GOLD parent path, `Engineered > Artificial ecosystem > Mesocosm`.
- Supported as generated status: the only contributing source concept has an item-level decision, so `mapping_status: REVIEWED` follows from the current decision table.

The record has no causal graphs, environmental parameters, characteristic taxa, datasets, discussions, or claim-level `EvidenceItem` objects to inspect.

## Completeness

The generated attestation is complete for the committed GOLD ecosystem inventory. `data/raw/gold_ecosystem_paths.tsv` lists both source node IDs, and the generated record's first-node note accounts for the second node ID.

The GOLD API side tables add context but no generated-data gap for this path. Exact ignored-inclusive searches found one `data/raw/gold_path_biosamples.tsv` row with 15 biosamples for node `6460` and one `data/raw/gold_studies.tsv` row, `Gs0159261`, that also lists `Environmental > Aquatic > Marine > Coastal` and `Environmental > Aquatic > Marine > Oceanic`. The same exact source-path search found no row in `data/raw/gold_path_triads.tsv`.

Exact ignored-inclusive searches over `history/`, `research/`, `curation/causal_graphs/`, `curation/term_requests.tsv`, `curation/term_requests/`, and `conf/id_label_targets.yaml` found no target-specific append-only history record, research report, causal overlay, authored term request, generated ENVO term-request row, or label-correspondence residual for `habitatmech:GOLD.0cadaca0bf`. Before this report was written, `find reports/yaml_record_review -maxdepth 1 -type f -name '*-seawater.md' -print` found no prior exact review report.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

None required. If future curation changes this grounding or adds an authored definition, rerun `just seed`, a focused `just seed-canary` for the affected generated identifier, `just validate-strict` on the regenerated record path, `just term-requests-check`, `just validate-history`, `just validate-causal-all`, `just verify-corpus --max-diffs 1`, and `just report`.

## Additional Notes

All absence checks in this review used ignored-inclusive `rg --no-ignore --hidden` or `find`, bounded to maintained curation, history, research, raw inventory, generated habitat, and review-report paths. No search depended on rendered `pages/` or text-map JSON.

The exact `Seawater` label also appears in prior reviews and research prose for different records, most notably Seawater RAS and related Biofilter reports. Those references are sibling context, not prior review of `data/habitats/engineered/seawater.yaml`.
