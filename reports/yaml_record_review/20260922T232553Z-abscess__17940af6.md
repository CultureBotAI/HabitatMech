# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/abscess__17940af6.yaml`
- Started UTC: 2026-09-22T23:20:00Z
- Finished UTC: 2026-09-22T23:25:53Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/host_associated/abscess__17940af6.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.8423e76015` |
| Label | `Abscess` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Record status | Generated from one GOLD source concept with no item-level curation decision |

This is the generated record for the GOLD path `Host-associated > Mammals: Human > Nervous system > Brain > Abscess`. The GOLD source-concept key is `habitatmech:GOLD.8423e76015`, computed from `sha1("GOLD:Host-associated > Mammals: Human > Nervous system > Brain > Abscess")[:10]`. The seeder keeps it minted because the generic `mesh:D000038` `Abscess` leaf match is broader than this brain-specific GOLD path and is also shared by several other GOLD abscess paths.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/abscess__17940af6.yaml` | Passed; LinkML validation reported no issues. |
| `just validate-strict data/habitats/host_associated/abscess__17940af6.yaml` | Passed; one file scanned with 0 error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 generated records, found 3206, with 0 missing, 0 extra, and 0 differing. |
| `just worklist` filtered for `Abscess`, `abscess`, `GOLD\.8423e76015`, and `mesh:D000038` | Completed with no matching rows, as expected for a seeded `NARROW` GOLD record that is not in the class-level ungrounded backlog. |
| `just report` | Passed; the report regenerated and did not place the reviewed concept in the risky-grounding, stale-sweep, current non-habitat, same-label, GOLD-triad, unsupported-prefix, or organism-identity sections. |
| `git diff --check` | Passed; no whitespace errors. |

## Identity and Grounding

The GOLD source identity is supported:

| Raw field | Value |
|---|---|
| `canonical_path` | `Host-associated > Mammals: Human > Nervous system > Brain > Abscess` |
| `ecosystem` | `Host-associated` |
| `ecosystem_category` | `Mammals: Human` |
| `ecosystem_type` | `Nervous system` |
| `ecosystem_subtype` | `Brain` |
| `specific_ecosystem` | `Abscess` |
| `leaf_label` | `Abscess` |
| `depth` | `5` |
| `gold_node_count` | `1` |
| `organism_count` | `3` |
| `study_count` | `0` |
| `biosample_count` | `0` |
| `total_assertions` | `3` |
| `gold_node_ids` | `gold.ecosystem:6343` |

`source_attestations` matches `data/raw/gold_ecosystem_paths.tsv:761`: the record keeps `gold.ecosystem:6343` as the source id, records the `Abscess` leaf label and canonical GOLD path exactly, records `assertion_count: 3` with `assertion_unit: ORGANISM`, and omits a multi-node note because the raw row has a single GOLD node id.

The `habitatmech:GOLD.752ac1c5aa` parent is supported. `data/raw/gold_ecosystem_paths.tsv:137` records the immediate parent path `Host-associated > Mammals: Human > Nervous system > Brain`, and `data/habitats/host_associated/brain__7bf4166c.yaml` is the generated record for that path.

The `mesh:D000038` parent is supported. `data/raw/ontology_terms.tsv:13554` defines `mesh:D000038` with label `Abscess`, and this brain abscess source concept is narrower than a generic abscess. `data/raw/gold_ecosystem_paths.tsv` contains other `Abscess` leaves at skin, nasal-cavity, abdominal/peritoneal, spinal-cord, joint-capsule, and soft-tissue paths, so the seeder's ambiguous-leaf rule correctly prevents this one path from adopting `mesh:D000038` as an exact identity.

`grounding_status: NARROW` is mechanically expected for an uncurated GOLD source path whose leaf label matches a broader ontology term but is not the unique defensible claimant of that term. `mapping_status: SEEDED` is also expected: ignored/hidden-inclusive exact searches found no maintained `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/term_requests_excluded.tsv`, `curation/causal_graphs`, or `history` row for `habitatmech:GOLD.8423e76015`.

## Evidence

| Claim | Nearest support | Review |
|---|---|---|
| The generated record represents the GOLD path `Host-associated > Mammals: Human > Nervous system > Brain > Abscess`. | `data/raw/gold_ecosystem_paths.tsv:761` | Supported exactly. |
| The GOLD source row contributes 3 organism assertions from one GOLD ecosystem node id. | `data/raw/gold_ecosystem_paths.tsv:761` | Supported exactly. |
| `habitatmech:GOLD.752ac1c5aa` is the source-path parent. | `data/raw/gold_ecosystem_paths.tsv:137`; `data/habitats/host_associated/brain__7bf4166c.yaml` | Supported. A brain abscess record is narrower than the parent brain GOLD path. |
| `mesh:D000038` `Abscess` is broader than the brain-specific GOLD source concept. | `data/raw/ontology_terms.tsv:13554`; `data/raw/gold_ecosystem_paths.tsv:448,655,679,687,761,1093,2359` | Supported. The source path supplies the brain context that makes this a narrower abscess record rather than the generic abscess identity. |

Unsupported or over-scoped claims: None found. The record carries only GOLD source provenance, the generic abscess parent, the GOLD source-path parent, and seeded audit metadata. It does not claim characteristic taxa, environmental parameters, causal mechanisms, or literature-backed definition text.

## Completeness

The direct GOLD source attestation is complete for a three-organism, one-node raw row: the record carries the source name, source id, source label, source path, mapping predicate, assertion count, and assertion unit, while correctly omitting the multi-node note used for collapsed paths.

Separate committed context does not imply a missing maintained field. An ignored/hidden-inclusive exact search for `gold.ecosystem:6343` across the raw, curation, history, and generated record inputs found only the GOLD path row and this generated record. An ignored/hidden-inclusive exact search for the full canonical path found only `data/raw/gold_ecosystem_paths.tsv` and this generated record.

Ignored/hidden-inclusive exact searches found no maintained `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/term_requests_excluded.tsv`, `curation/causal_graphs`, or `history` row for `habitatmech:GOLD.8423e76015`; generated `data/text_map`, `pages`, and `build` outputs were excluded only from broad exact content searches.

No maintained causal overlay, history file, raw research report, or prior exact YAML review report was found by filename for `abscess__17940af6` under `curation/causal_graphs`, `history`, `research/habitats`, or `reports/yaml_record_review`; the `find` search included ignored files beneath those checked directories.

## Findings

None found.

No blocker findings.

No major findings.

No minor findings.

## Recommended Edits

None required.

If a future curator wants to mark the GOLD narrow leaf review-complete, add a `REVIEW` row for `habitatmech:GOLD.8423e76015` in `curation/decisions.tsv` and rerun `just seed-canary habitatmech:GOLD.8423e76015`. That should be a status-only change: keep the minted identifier, keep `grounding_status: NARROW`, and keep both existing parents.

## Follow-up Checks

None required beyond the validators already run for this report.

For any future status-promotion-only edit, run:

- `just seed-canary habitatmech:GOLD.8423e76015`
- `just validate data/habitats/host_associated/abscess__17940af6.yaml`
- `just validate-strict data/habitats/host_associated/abscess__17940af6.yaml`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

## Additional Notes

- The pre-report exact filename check `find reports/yaml_record_review -maxdepth 1 -type f -name '*-abscess__17940af6.md' -print` returned no previous exact report; `find` includes ignored files.
- `just worklist` did not list `habitatmech:GOLD.8423e76015`, `mesh:D000038`, or `Abscess`, consistent with a seeded `NARROW` GOLD leaf rather than an undecided ungrounded source concept.
- `just report` completed successfully and did not flag `mesh:D000038` or `habitatmech:GOLD.8423e76015` in any risky-grounding, stale-sweep, current non-habitat, same-label, GOLD-triad-compatibility, minted-GOLD-triad, unsupported-prefix, or organism-identity section.
- Existing item-level `NOT_APPLICABLE` rows for `Abscess: Furuncle/Boil` and `Abscess: Pilonidal sinus` are decisions about those colon-expanded skin leaves only; they do not apply to this plain brain `Abscess` source concept.
