# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/aerobic_zone.yaml
- Started UTC: 2026-09-23T18:36:00Z
- Finished UTC: 2026-09-23T18:39:29Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | habitatmech:GOLD.b0f6a1ef74 |
| Label | Aerobic zone |
| Class | HabitatRecord |
| Category | ENGINEERED |
| Grounding status | UNGROUNDED |
| Mapping status | SEEDED |
| Maintained owner | generated from `data/raw/gold_ecosystem_paths.tsv`, `curation/decisions.tsv`, and `data/habitats/PATHS.tsv`; the false parent is generated by the GOLD parent-path pass in `src/habitatmech/seed.py` |

[data/habitats/engineered/aerobic_zone.yaml](../../data/habitats/engineered/aerobic_zone.yaml) is the generated GOLD record for `Engineered > Bioreactor > Partial-Nitrification/Anammox (PNA) > Aerobic zone`, whose deterministic source-concept key is `habitatmech:GOLD.b0f6a1ef74`.

The target was resolved with ignored-file-inclusive searches:

- `find data/habitats -type f -name 'aerobic_zone.yaml' -print` found exactly one generated YAML record.
- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-aerobic_zone.md' -print` found no prior exact review report.
- `find curation/causal_graphs -maxdepth 1 -type f -name 'aerobic_zone.yaml' -print` found no causal-graph overlay for this record.
- `rg --no-ignore --hidden -n --fixed-strings 'habitatmech:GOLD.b0f6a1ef74' . --glob '!data/text_map/**' --glob '!pages/**' --glob '!build/**' --glob '!.venv/**'` found only the target generated YAML, the slug lock, the class-level `curation/decisions.tsv` row for this GOLD source concept, and the child `Biomass` record that inherits the target as its GOLD source-path parent.
- `rg --no-ignore --hidden -n -i 'aerobic zone|oxic zone|anammox|partial.nitrification|pna' curation history research data/raw data/habitats/PATHS.tsv reports --glob '!data/text_map/**' --glob '!pages/**' --glob '!build/**'` found the GOLD PNA raw rows and unrelated `anammox` or `zone` mentions, but no target-specific maintained definition, history record, causal overlay, or research report.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/aerobic_zone.yaml` | pass; no LinkML issues found |
| `just validate-strict data/habitats/engineered/aerobic_zone.yaml` | pass; 1 file scanned, 0 files with ERROR, 0 total ERROR rows |
| `just validate-causal curation/causal_graphs/aerobic_zone.yaml` | not applicable; exact `find curation/causal_graphs -maxdepth 1 -type f -name 'aerobic_zone.yaml' -print` found no overlay |
| `just validate-causal-all` | pass; 32 causal-graph curation files with 32 graphs validated |
| Reference validator | not applicable; this generated GOLD record has no causal graph and no DOI, PMID, or URL evidence slots to inspect |
| `just term-requests-check` | pass; term-request table is current with 109 terms |
| `just validate-history` | pass; no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | pass; 3206 expected, 3206 found, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | pass; 0 ungrounded records still undecided; 1810 decisions on file |
| `just report` | pass; 3206 records; no global issue specific to `habitatmech:GOLD.b0f6a1ef74` surfaced |
| `git diff --check` | pass |

## Identity and Grounding

The minted identity is faithful to the GOLD source concept. `data/raw/gold_ecosystem_paths.tsv` has the canonical path `Engineered > Bioreactor > Partial-Nitrification/Anammox (PNA) > Aerobic zone` at depth 4 with `leaf_label` `Aerobic zone`, 2 collapsed GOLD node ids, first node id `gold.ecosystem:8546`, and zero direct organism, study, biosample, or total assertions. `src/habitatmech/seed.py` mints the source concept as the first ten SHA1 hex digits of `GOLD:Engineered > Bioreactor > Partial-Nitrification/Anammox (PNA) > Aerobic zone`, yielding `habitatmech:GOLD.b0f6a1ef74`.

`mapping_status: SEEDED` is correct for the current curation input. `curation/decisions.tsv` has a `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.b0f6a1ef74`, but its `review_depth` is `CLASS`, not `ITEM`; the row records that no vendored ontology term matched by the class-level lexical sweep and explicitly says that whether the concept is a habitat was not assessed.

The generated `parent_habitats` value is not a true broader habitat. `habitatmech:GOLD.5f3cbd7563` is `Partial-Nitrification/Anammox (PNA)`, generated from `Engineered > Bioreactor > Partial-Nitrification/Anammox (PNA)` with broader parent `ENVO:00002123` `bioreactor`. `Engineered > Bioreactor > Partial-Nitrification/Anammox (PNA) > Aerobic zone` means an aerobic zone is a compartment in a PNA bioreactor, not a kind of partial-nitrification/anammox bioreactor.

No exact ontology identity is maintained for the aerobic-zone concept. Exact search of `data/raw/ontology_terms.tsv` found `ENVO:00002126` `aerobic bioreactor`, but that term names a whole bioreactor in which the contained material is well-oxygenated. Existing item-level decisions ground bare `Aerobic` leaves below a direct `Bioreactor` parent to `ENVO:00002126`; they do not support grounding or parenting an internal PNA aerobic-zone compartment to the whole-reactor term.

## Evidence

This record has one GOLD source attestation:

- The `GOLD` attestation mirrors `data/raw/gold_ecosystem_paths.tsv`: source id `gold.ecosystem:8546`, source label `Aerobic zone`, source path `Engineered > Bioreactor > Partial-Nitrification/Anammox (PNA) > Aerobic zone`, and no `assertion_count`.
- The raw row's `gold_node_count` is `2` and its `gold_node_ids` field is `gold.ecosystem:8546|gold.ecosystem:8547`, matching the generated de-duplication note.
- The source path has `organism_count`, `study_count`, `biosample_count`, and `total_assertions` all equal to `0`, so the generated attestation correctly omits `assertion_count` and `assertion_unit`.

No evidence-backed causal mechanism evidence is present.

Unsupported or over-scoped claims:

- `parent_habitats: habitatmech:GOLD.5f3cbd7563` overstates a compartment relationship as a broader-habitat edge.

## Completeness

The record carries the generated GOLD source-owned content for this source concept: the first GOLD ecosystem id, source label, source path, duplicate-node note, class-level sweep history, generated seeding history, and generated slug lock.

The hierarchy is materially incomplete because the only generated parent is false. Exact ignored-file-inclusive search found no item-level decision or authored definition for this source concept.

Optional characteristic-taxon, environmental-parameter, discussion, dataset, and causal-graph slots are correctly absent; no maintained causal overlay, history entry, or item-level curated review row adds any of those fields.

## Findings

- **major**: `parent_habitats` asserts `Aerobic zone` `is-a` `habitatmech:GOLD.5f3cbd7563` `Partial-Nitrification/Anammox (PNA)`.

  **Evidence**: GOLD places `Engineered > Bioreactor > Partial-Nitrification/Anammox (PNA) > Aerobic zone` below the PNA bioreactor source path, and `src/habitatmech/seed.py` copies that source-tree edge into `parent_habitats`. The source path says the aerobic zone is a compartment in the PNA reactor; it is not a subclass of the whole PNA bioreactor. `CLAUDE.md` requires `parent_habitats` edges to be strictly broader.

  **Maintained owner**: `src/habitatmech/seed.py` currently generates this edge because this record has no `curation/term_requests.tsv` definition with `parent_mode=REPLACE` or other maintained row that replaces the inherited parent.

## Recommended Edits

- Item-review `habitatmech:GOLD.b0f6a1ef74`. If no vendored exact term exists and the concept is retained as a real engineered habitat, keep it minted with an item-level `CONFIRM_UNGROUNDED` row rather than promoting the class-level sweep to reviewed status.
- Add a `curation/term_requests.tsv` definition with a true authored genus and `parent_mode=REPLACE`, then confirm the generated edge from `habitatmech:GOLD.b0f6a1ef74` to the resolved parent-path record `habitatmech:GOLD.5f3cbd7563` is removed.
- Regenerate only the affected source concept and inspect [data/habitats/engineered/aerobic_zone.yaml](../../data/habitats/engineered/aerobic_zone.yaml) to confirm `parent_habitats: habitatmech:GOLD.5f3cbd7563` is gone and no false replacement parent was introduced.

## Follow-up Checks

The narrow proof path for a future fix is:

- add or update focused tests that prove the `parent_mode=REPLACE` definition removes the inherited PNA parent from the aerobic-zone record
- `just seed`
- `just seed-canary habitatmech:GOLD.b0f6a1ef74`
- inspect [data/habitats/engineered/aerobic_zone.yaml](../../data/habitats/engineered/aerobic_zone.yaml) and confirm `habitatmech:GOLD.5f3cbd7563` is no longer in `parent_habitats`
- inspect the generated `Biomass` child record and confirm needed hierarchy was intentionally retained or removed
- `just validate-strict data/habitats/engineered/aerobic_zone.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- ignored-file-inclusive exact searches for `habitatmech:GOLD.b0f6a1ef74`, `Engineered > Bioreactor > Partial-Nitrification/Anammox (PNA) > Aerobic zone`, and the authored definition row
- `git diff --check`

## Additional Notes

The raw GOLD child path `Engineered > Bioreactor > Partial-Nitrification/Anammox (PNA) > Aerobic zone > Biomass` is present and generates `data/habitats/engineered/biomass__e0c132c1.yaml`, which currently inherits this aerobic-zone record as its GOLD source-path parent. That child relationship does not support the false broader edge from `Aerobic zone` to `Partial-Nitrification/Anammox (PNA)`.

The exact ignored-file-inclusive search also found the parallel `Anaerobic zone` sibling in the same raw GOLD PNA subtree. That sibling was not reviewed here.
