# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/wastewater__b5b1cb15.yaml
- Started UTC: 2026-09-25T18:52:26Z
- Finished UTC: 2026-09-25T18:52:26Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.a2b1243a87` |
| Label | `Wastewater` |
| Category | `ENGINEERED` |
| Source path | `Engineered > Bioreactor > DHS reactor > Wastewater` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv` and `data/habitats/PATHS.tsv`; future item-level review belongs in `curation/decisions.tsv`, and any retained minted definition or parent replacement belongs in `curation/term_requests.tsv`. |

The target is a GOLD-only, generated, `NARROW`, still-`SEEDED` record for the
two-node `Wastewater` child under the DHS reactor path. The seeder minted a
stable HabitatMech identifier, attached `ENVO:00002001` `waste water` through
the ambiguous-leaf route, inherited `habitatmech:GOLD.24cf427e7d`
`DHS reactor` from the immediate GOLD parent path, and represented the two
collapsed GOLD ecosystem node IDs with `source_id: gold.ecosystem:5837`.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/habitats/engineered/wastewater__b5b1cb15.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/wastewater__b5b1cb15.yaml` | Pass: scanned 1 file with 0 files containing `ERROR` and 0 total `ERROR` rows. |
| `just validate-causal curation/causal_graphs/wastewater__b5b1cb15.yaml` | Not applicable: the target has no `causal_graphs` block, and `find curation/causal_graphs -maxdepth 1 -type f -name 'wastewater__b5b1cb15.yaml' -print` found no target-specific overlay. |
| `just validate-causal-all` | Pass: validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Pass: the term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: expected 3,206 records, found 3,206 on disk, and the corpus reproduces exactly from `data/raw/`. |
| `just worklist --limit 2000 --status all --out /tmp/habitatmech-dhs-wastewater-worklist.tsv` | Pass: reported 953 ungrounded records, 1,810 decisions on file, and wrote 953 rows to `/tmp/habitatmech-dhs-wastewater-worklist.tsv`. |
| `just report` | Pass: completed the corpus report for 3,206 records. |
| `git diff HEAD --check` | Pass. |
| Reference validator | Not applicable: this generated GOLD-only record has no record-level `evidence`, `characteristic_taxa`, `causal_graphs`, `discussions`, or `datasets` with citation-bearing `EvidenceItem` references. |

## Identity and Grounding

The generated identifier, label, category, source attestation, and slug agree
with the raw GOLD inventory. `data/raw/gold_ecosystem_paths.tsv:1180` has the
exact path `Engineered > Bioreactor > DHS reactor > Wastewater`,
`gold_node_count=2`, no direct organism, study, biosample, or total assertions
in the collapsed summary row, and
`gold_node_ids=gold.ecosystem:5837|gold.ecosystem:5838`.
`data/habitats/PATHS.tsv:2500` maps `habitatmech:GOLD.a2b1243a87` to the stable
`wastewater__b5b1cb15` slug.

The generated `ENVO:00002001` parent is supported and appropriately broader.
`data/raw/ontology_terms.tsv:7166` labels that term `waste water` and defines
it as water whose quality has been adversely affected by anthropogenic
influence. The target is source-specific DHS reactor wastewater, so generic
waste water is broader rather than exact.

The generated `habitatmech:GOLD.24cf427e7d` parent is not supported as a strict
broader habitat. The target leaf names wastewater material in the
`Engineered > Bioreactor > DHS reactor` context; that material can be contained
by a DHS reactor, but it is not a subtype of the reactor itself. The immediate
GOLD parent is itself only class-reviewed: `curation/decisions.tsv:307` records
a `CLASS`-depth `CONFIRM_UNGROUNDED` decision whose rationale explicitly says
whether the DHS reactor concept is a habitat was not assessed.

No item-level curation row exists for `habitatmech:GOLD.a2b1243a87`; the target
has only its generated seed event. The `NARROW` state is explainable from the
seeder's ambiguous-leaf and source-parent routes, but it has not been endorsed
by a curator.

## Evidence

The sole `GOLD` source attestation mirrors
`data/raw/gold_ecosystem_paths.tsv:1180`: source ID `gold.ecosystem:5837`,
source label `Wastewater`, source path
`Engineered > Bioreactor > DHS reactor > Wastewater`, and
`mapping_predicate: skos:narrowMatch`. The attestation note correctly records
that two GOLD ecosystem node IDs share this collapsed path and that the first
one is shown.

Two GOLD side tables also name the same path:
`data/raw/gold_path_biosamples.tsv:813` records three biosamples for path ID
`5838`, and `data/raw/gold_studies.tsv:683` records study `Gs0111456` across
this wastewater path and `Engineered > Bioreactor > DHS reactor > Sludge`.
Those tables are supporting inventory for GOLD API triad enumeration;
`src/habitatmech/seed.py` currently builds GOLD source attestations from
`gold_ecosystem_paths.tsv`, and this target has no exact path row in
`data/raw/gold_path_triads.tsv` or `data/raw/environment_parameters.tsv`.

Unsupported or over-scoped generated claims:

- `habitatmech:GOLD.24cf427e7d` as a `parent_habitats` entry is an over-scoped
  source-context edge. `habitatmech:GOLD.24cf427e7d` names a DHS reactor, not
  wastewater contained by a DHS reactor.

## Completeness

The generated record carries the expected source-owned values for the current
raw inputs: a minted identifier, GOLD label, `ENGINEERED` category,
ambiguous-leaf `waste water` parent, inherited `DHS reactor` parent, a single
GOLD source attestation covering the collapsed GOLD path, a seed event, and a
stable slug lock.

The curation state is incomplete. Exact `rg --no-ignore --hidden` searches for
`habitatmech:GOLD.a2b1243a87`, `gold.ecosystem:5837`,
`gold.ecosystem:5838`, the target source path, and `wastewater__b5b1cb15` under
the maintained source and review areas found the generated YAML record, the raw
GOLD rows, the prior sludge report that mentions the paired wastewater path,
and the slug-lock row, but no item-level decision,
`curation/term_requests.tsv` definition, target-specific history, research
report, maintained causal overlay, or prior slug-named YAML review report.
Exact `find` searches found no target-specific causal overlay and no prior
slug-named YAML review report. Ignored files were included in all of those
absence checks.

Optional characteristic-taxon, environmental-parameter, DOI/PMID/URL evidence,
dataset, discussion, and causal-graph slots are correctly absent for this GOLD
child in the generated record. The source-side biosample and study rows name
the path, but they do not currently provide a generated record field beyond the
GOLD path support already captured by the source attestation.

## Findings

- **major**: `habitatmech:GOLD.a2b1243a87` has no item-level review, so its
  `NARROW` status and source-specific DHS-wastewater identity remain seeded
  rather than curated.

  **Evidence**: The target GOLD row is a distinct two-node `Wastewater` child
  under `Engineered > Bioreactor > DHS reactor`, while the exact
  `ENVO:00002001` `waste water` identity is already claimed by the generic
  `data/habitats/engineered/waste_water.yaml` ontology record. Exact
  gitignore-independent searches found no target-specific row in
  `curation/decisions.tsv`, no `curation/term_requests.tsv` definition, and no
  target-specific history or research report.

  **Owner**: `curation/decisions.tsv` should hold the item-level decision; if
  the item-level review keeps the minted identifier,
  `curation/term_requests.tsv` should hold its definition and strict parent.

- **major**: `parent_habitats` asserts that this DHS-reactor wastewater is a
  kind of `habitatmech:GOLD.24cf427e7d` `DHS reactor`.

  **Evidence**: The inherited parent comes from GOLD's `Engineered >
  Bioreactor > DHS reactor` path segment. `habitatmech:GOLD.24cf427e7d` names
  a bioreactor context, while this child leaf names wastewater in that context.
  The record has no item-level decision, term request, MIxS triad, causal
  overlay, or source side-table row saying the wastewater is itself a reactor.

  **Owner**: `curation/term_requests.tsv` should replace the generated
  source-path parent with `ENVO:00002001` or a tighter inspected material genus
  if the item-level decision confirms the target is a real minted habitat.

No blockers found.

No minor findings.

## Recommended Edits

1. In `curation/decisions.tsv`, add an item-level review for
   `habitatmech:GOLD.a2b1243a87` scoped to
   `Engineered > Bioreactor > DHS reactor > Wastewater`, the broader
   `ENVO:00002001` `waste water` parent, and the contextual
   `habitatmech:GOLD.24cf427e7d` `DHS reactor` edge.

2. If item review keeps this as source-specific DHS-reactor wastewater, define
   it in `curation/term_requests.tsv` under `ENVO:00002001` or a tighter
   inspected material genus, and use `parent_mode=REPLACE` if the review
   confirms that `habitatmech:GOLD.24cf427e7d` is contextual rather than
   strict.

3. Regenerate the corpus from those maintained inputs so the generated record
   records the curator decision and no longer carries a false DHS-reactor
   `is-a` edge.

## Follow-up Checks

After curation, rerun:

- `just term-requests-check`
- `just validate data/habitats/engineered/wastewater__b5b1cb15.yaml`
- `just validate-strict data/habitats/engineered/wastewater__b5b1cb15.yaml`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000 --status all --out /tmp/habitatmech-dhs-wastewater-worklist.tsv`

## Additional Notes

- `data/raw/gold_path_biosamples.tsv` shows that the second collapsed GOLD node
  ID, `5838`, has three biosamples behind it. That support can help a future
  item-level review find the submitter context, but the current generator
  intentionally emits the first collapsed node ID and does not use the
  biosample side table as the source-attestation assertion count.
- The false `DHS reactor` edge is the wastewater variant of the
  contextual-parent pattern already observed for the DHS sludge children:
  source-path reactor context becomes a false parent when the leaf is material
  contained by the reactor rather than the reactor device.
