# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/microcosm.yaml
- Started UTC: 2026-09-25T04:41:03Z
- Finished UTC: 2026-09-25T04:41:03Z
- Verdict: pass

## Target

Reviewed the complete generated record at `data/habitats/engineered/microcosm.yaml`. The record is a GOLD-only engineered habitat for `Engineered > Artificial ecosystem > Microcosm`, grounded exactly to `ENVO:01000621` `microcosm`.

The generated file has `definition_source: ENVO`, `habitat_category: ENGINEERED`, `grounding_status: EXACT`, and `mapping_status: SEEDED`. Its parents are the ENVO ontology parent `ENVO:00010622` `vivarium` and the immediate GOLD source-path parent `habitatmech:GOLD.0acae9a1a4` `Artificial ecosystem`. Its only source attestation shows `gold.ecosystem:8179` and correctly notes that three GOLD ecosystem node IDs share this canonical path.

The file is generated from `data/raw/`; no item-level decision, authored term request, source extractor override, or causal-graph overlay currently contributes to this record. Future grounding changes belong in `curation/decisions.tsv`; future authored definition or parent-mode changes would belong in `curation/term_requests.tsv`; future causal overlays would belong under `curation/causal_graphs/`.

## Validation

All required local checks passed:

- `just validate data/habitats/engineered/microcosm.yaml`
- `just validate-strict data/habitats/engineered/microcosm.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

No validator was skipped.

## Identity and Grounding

The generated identifier, label, category, source attestation, and path slug agree with the maintained inputs. `data/raw/gold_ecosystem_paths.tsv` has the exact `Engineered > Artificial ecosystem > Microcosm` row at depth 3, with three GOLD node IDs, zero `organism_count`, `study_count`, `biosample_count`, and `total_assertions` values in this bulk-export summary, and `gold.ecosystem:8179|gold.ecosystem:8180|gold.ecosystem:8181` as the node list. `data/habitats/PATHS.tsv` maps `ENVO:01000621` to the stable `microcosm` slug.

The `EXACT` grounding and `SEEDED` mapping follow from the GOLD leaf-label route. `data/raw/ontology_terms.tsv` defines `ENVO:01000621` with canonical label `microcosm`; the generated definition is copied from that vendored ENVO row. There is no target row in `curation/decisions.tsv`, so the record remains `SEEDED` rather than `REVIEWED`.

Both broader terms are traceable. `data/raw/ontology_subclass_edges.tsv` has `ENVO:01000621 rdfs:subClassOf ENVO:00010622`, and `data/raw/ontology_terms.tsv` defines `ENVO:00010622` as `vivarium`. The `habitatmech:GOLD.0acae9a1a4` parent comes from the immediate GOLD path prefix `Engineered > Artificial ecosystem`, which currently resolves to `data/habitats/engineered/artificial_ecosystem.yaml`.

`curation/samples/exact-20260814.tsv` includes `ENVO:01000621`, ENVO label `microcosm`, GOLD label `Microcosm`, and the exact `Engineered > Artificial ecosystem > Microcosm` path with verdict `ok`. That sampled exact-match row supports the lexical grounding, but it is not an item-level `curation/decisions.tsv` row and correctly does not promote this record to `REVIEWED`.

## Evidence

- Supported as generated source provenance: exact ignored-inclusive searches found `gold.ecosystem:8179`, `gold.ecosystem:8180`, `gold.ecosystem:8181`, and `Engineered > Artificial ecosystem > Microcosm` in `data/raw/gold_ecosystem_paths.tsv`; `gold.ecosystem:8179` is the first node shown in the generated source attestation.
- Supported as generated ontology grounding: `ENVO:01000621` `microcosm` is present in `data/raw/ontology_terms.tsv`, and the record's definition and `ENVO:00010622` parent are vendored ENVO content.
- Supported as generated source hierarchy: `habitatmech:GOLD.0acae9a1a4` is the generated identifier for the immediate GOLD parent path, `Engineered > Artificial ecosystem`.
- Supported as generated status: no contributing source concept has an item-level decision, so `mapping_status: SEEDED` follows from the current decision table.

The record has no causal graphs, environmental parameters, characteristic taxa, datasets, discussions, or claim-level `EvidenceItem` objects to inspect.

## Completeness

The generated attestation is complete for the committed GOLD ecosystem inventory. `data/raw/gold_ecosystem_paths.tsv` lists all three source node IDs, and the generated record's first-node note accounts for the second and third node IDs.

Exact full-file scans of the GOLD API side tables found no `Engineered > Artificial ecosystem > Microcosm` rows in `data/raw/gold_studies.tsv`, `data/raw/gold_path_biosamples.tsv`, or `data/raw/gold_path_triads.tsv`; no GOLD study, biosample, or MIxS triad context is committed for this exact path.

Exact ignored-inclusive searches over `history/`, `research/`, `curation/causal_graphs/`, `curation/term_requests.tsv`, `curation/term_requests/`, `conf/id_label_targets.yaml`, and `reports/yaml_record_review/` found no target-specific append-only history record, research report, causal overlay, authored term request, generated ENVO term-request row, label-correspondence residual, or prior review for `ENVO:01000621`, `gold.ecosystem:8179`, `gold.ecosystem:8180`, `gold.ecosystem:8181`, or `Engineered > Artificial ecosystem > Microcosm`. Before this report was written, `find reports/yaml_record_review -maxdepth 1 -type f -name '*-microcosm.md' -print` found no prior exact review report.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

None required. If future curation changes this grounding or adds an authored definition, rerun `just seed`, a focused `just seed-canary` for the affected generated identifier, `just validate-strict` on the regenerated record path, `just term-requests-check`, `just validate-history`, `just validate-causal-all`, `just verify-corpus --max-diffs 1`, and `just report`.

## Additional Notes

All absence checks in this review used ignored-inclusive `rg --no-ignore --hidden`, exact full-file scans of the committed raw side tables, or `find`, bounded to maintained curation, history, research, raw inventory, generated habitat, and review-report paths. No search depended on rendered `pages/` or text-map JSON.

The `habitatmech:GOLD.0acae9a1a4` `Artificial ecosystem` parent has a prior `needs curation` review because the parent record itself only has a class-level sweep decision. That is a parent-record curation gap, not a `microcosm` identity defect: this record is exactly grounded to ENVO, and the `Artificial ecosystem` edge is reproducibly inherited from the immediate GOLD source path.

The exact lower-case label `microcosm` also appears in the committed Indoor water-channel deep-research report as a rejected fallback parent for a different record. That research report does not curate or support `data/habitats/engineered/microcosm.yaml`.
