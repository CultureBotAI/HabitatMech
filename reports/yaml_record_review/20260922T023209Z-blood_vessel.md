# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/blood_vessel.yaml`
- Started UTC: 2026-09-22T02:31:00Z
- Finished UTC: 2026-09-22T02:32:09Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `BTO:0001102` |
| Label | `blood vessel` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the vendored BTO ontology slice and PREGO raw tables; future item-level review belongs in `curation/decisions.tsv`, and future mechanism evidence belongs in `curation/causal_graphs/*.yaml`, not in this generated YAML |
| Locked slug | `data/habitats/PATHS.tsv:204` maps `BTO:0001102` to `blood_vessel` |

This is the BTO-grounded PREGO `blood vessel` record. Its direct PREGO source concept is deterministically keyed as `habitatmech:PREGO.dfd77f24c3`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/blood_vessel.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/blood_vessel.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors |

## Identity and Grounding

The exact BTO identity is supported by the vendored ontology slice and the direct PREGO assertion:

- `data/habitats/PATHS.tsv:204` pins `BTO:0001102` to `blood_vessel`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:1103` defines `BTO:0001102` with canonical label `blood vessel`, definition `Any of the vessels through which blood circulates in the body.`, `directly_referenced: TRUE`, and `label_only: FALSE`.
- `data/raw/prego_habitats.tsv:585` directly asserts `BTO:0001102` as a BTO `biolink:GrossAnatomicalStructure` with one associated taxon, one direct assertion, maximum PREGO score `3`, channel `annotated_genomes_isolates`, and lexical forms `blood vessel|blood vessels`.
- BTO records fall through `PREFIX_CATEGORY` as `HOST_ASSOCIATED`, so the generated `habitat_category: HOST_ASSOCIATED` is consistent with the record identifier.
- The stable PREGO curation key is `habitatmech:PREGO.dfd77f24c3`, computed as `sha1("PREGO:BTO:0001102")[:10]`. There is no item-level row for that key in `curation/decisions.tsv`, so `mapping_status: SEEDED` is expected.

`parent_habitats` is correctly absent. An exact ignored/hidden-inclusive search of `data/raw/ontology_subclass_edges.tsv` found rows where `BTO:0001102` is the parent of more specific vessel terms, such as `BTO:0002045` capillary, but no row where `BTO:0001102` itself has a vendored parent.

## Evidence

Every generated scientific claim in this record is source-derived:

| Claim | Nearest source | Review |
|---|---|---|
| BTO identifier, canonical label, definition, directly referenced flag, and non-`label_only` status | `data/raw/ontology_terms.tsv:1103` | Supported exactly |
| PREGO source ID, source label, `assertion_count: 1`, `assertion_unit: TAXON`, `score: 3.0`, and `evidence_channels: annotated_genomes_isolates` | `data/raw/prego_habitats.tsv:585` | Supported; `assertion_count` comes from PREGO `taxon_count`, which equals the separate `direct_assertion_count` here |
| PREGO synonym `blood vessels` | `data/raw/prego_habitats.tsv:585` | Supported; `blood vessel` duplicates the record label and is correctly omitted from the YAML synonym list |
| Inline PREGO taxon association for `NCBITaxon:1329902` `Mannheimia haemolytica MhBrain2012` | `data/raw/prego_habitat_taxa.tsv:1642` | Supported exactly with `rank: 1`, `score: 3.0`, channel `annotated_genomes_isolates`, `direct_flag: TRUE`, and `candidate_pool: 1` |

The record has no record-level evidence, environmental parameters, causal graphs, discussion links, or datasets. It also correctly omits `characteristic_taxa.corroborated_by` and `is_characteristic`; the PREGO `corroborated_by` column is blank, and PREGO's association alone is reported-from evidence, not curated evidence that the strain typifies the habitat.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- The sole `source_attestations` entry captures the direct PREGO `BTO:0001102` source concept.
- The one non-label PREGO lexical variant is present as a `RELATED_SYNONYM`.
- The single PREGO habitat-taxon row for `BTO:0001102` is represented once in `characteristic_taxa`.
- `parent_habitats` is empty because the vendored BTO slice has no subclass edge with `BTO:0001102` as the child.
- `mapping_status` stays `SEEDED`, as expected; no item-level curation decision exists for `habitatmech:PREGO.dfd77f24c3`.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research/habitats`, `reports/yaml_record_review`, `docs`, `.claude`, `src`, `tests`, `build`, `data/habitats/PATHS.tsv`, and the exact raw TSVs that can feed BTO/PREGO records, while excluding generated `data/text_map/**` and `pages/**`. They found the raw rows and path lock cited above, a generated text-map projection, one historical `curation/samples/exact-20260814.tsv` sample row, and no maintained decision row, term request, history record, causal-graph overlay, target-specific research report, or prior exact YAML review report for `blood_vessel`.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if a future curation change adds a reviewed decision or causal graph overlay for this BTO-grounded blood-vessel record:

- `just validate data/habitats/host_associated/blood_vessel.yaml`
- `just validate-strict data/habitats/host_associated/blood_vessel.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If future curation signs off the PREGO source concept, add the maintained `GROUND` decision to `curation/decisions.tsv`, run `just seed`, preview `just seed-canary BTO:0001102`, and then rerun the corpus-level checks after regeneration.

## Additional Notes

- `find reports/yaml_record_review -name '*blood_vessel*' -print` found no pre-existing exact review report for this record before this file was written.
- The repeated `NCBITaxon:1329902` hits in `data/raw/prego_habitat_taxa.tsv` are PREGO associations to neighboring BTO anatomy records; only the `BTO:0001102` row at `data/raw/prego_habitat_taxa.tsv:1642` feeds the target reviewed here.
- The generated text-map hits for records such as `capillary`, `cardiovascular system`, and `endothelial cell` contain the words "blood vessel" in neighboring record definitions, but they are not maintained inputs to this target.
