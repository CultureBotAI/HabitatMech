# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/bone.yaml`
- Started UTC: 2026-09-22T02:48:52Z
- Finished UTC: 2026-09-22T02:49:00Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.1e2686ea7e` |
| Label | `Bone` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the GOLD ecosystem-path inventory plus the vendored ontology slice; future item-level review belongs in `curation/decisions.tsv`, future authored definitions belong in `curation/term_requests.tsv`, and future mechanism evidence belongs in `curation/causal_graphs/*.yaml`, not in this generated YAML |
| Locked slug | `data/habitats/PATHS.tsv:1504` maps `habitatmech:GOLD.1e2686ea7e` to `bone` |

This is the minted GOLD record for `Host-associated > Mammals: Human > Remains > Fossilized remains > Bone`. The leaf label alone is generic, but the source path scopes this record to a fossilized-remains child that is narrower than the vendored BTO `bone` term.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/bone.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/bone.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors |

## Identity and Grounding

The minted identity and its broader parents are supported by the GOLD inventory, the path lock, and the vendored ontology slice:

- `data/raw/gold_ecosystem_paths.tsv:2374` is the exact raw GOLD path `Host-associated > Mammals: Human > Remains > Fossilized remains > Bone`, gives leaf label `Bone`, records depth `5`, records one GOLD ecosystem node, and names `gold.ecosystem:6676` as the upstream node.
- `data/habitats/PATHS.tsv:1504` pins the minted identifier `habitatmech:GOLD.1e2686ea7e` to slug `bone`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:142` defines `BTO:0000140` with canonical label `bone`, a connective-tissue definition, `directly_referenced: FALSE`, and `label_only: FALSE`.
- `data/raw/gold_ecosystem_paths.tsv:2373` is the immediate GOLD parent path `Host-associated > Mammals: Human > Remains > Fossilized remains`, and `data/habitats/PATHS.tsv:3084` pins its generated identifier `habitatmech:GOLD.f1a2eca320` to `fossilized_remains__8b01c49f`.
- `curation/decisions.tsv:1335` has only a class-level `CONFIRM_UNGROUNDED` decision for the parent fossilized-remains source concept. It does not item-review the target `Bone` child, so the target record correctly remains `mapping_status: SEEDED`.

`grounding_status: NARROW` is consistent with the path context: the source concept is fossilized human-remains bone, which should not be merged into the generic BTO `bone` identity. Keeping the GOLD path as a minted record and attaching `BTO:0000140` as a broader term preserves the fossilized-remains distinction while retaining the anatomical/material parent. The other parent, `habitatmech:GOLD.f1a2eca320`, follows directly from the next broader GOLD path step.

## Evidence

Every generated scientific claim in this record is source-derived:

| Claim | Nearest source | Review |
|---|---|---|
| Minted identifier and locked filename | `data/habitats/PATHS.tsv:1504` | Supported exactly |
| GOLD `source_id: gold.ecosystem:6676`, `source_label: Bone`, `source_path`, zero assertion counts, and absence of inline source count fields | `data/raw/gold_ecosystem_paths.tsv:2374` | Supported exactly |
| Broader BTO `bone` parent | `data/raw/ontology_terms.tsv:142` | Supported as a broader term for the fossilized-bone source concept, not as an exact identity |
| Broader GOLD `Fossilized remains` parent | `data/raw/gold_ecosystem_paths.tsv:2373`, `data/habitats/PATHS.tsv:3084` | Supported exactly |

The record has no definition, synonyms, xrefs, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussion links, or datasets. Exact ignored/hidden-inclusive searches of the committed raw GOLD tables for `gold.ecosystem:6676` and the full target source path found only the `gold_ecosystem_paths.tsv` row cited above; there are no committed GOLD biosample, triad, or study rows that would feed omitted optional fields.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- The sole `source_attestations` entry captures the complete target GOLD path and upstream node id.
- Both `parent_habitats` values are expected: the BTO parent records the matched broader ontology term, and the minted `Fossilized remains` parent records the next broader GOLD path.
- The record has no `assertion_count` or `assertion_unit` because the raw target path has zero nonzero GOLD assertion counts.
- `mapping_status` stays `SEEDED` because no item-level curation decision exists for `habitatmech:GOLD.1e2686ea7e`.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research/habitats`, `reports/yaml_record_review`, `docs`, `.claude`, `src`, `tests`, `build`, `data/raw`, and `data/habitats/PATHS.tsv`, while excluding generated `data/text_map/**` and `pages/**`. They found the GOLD and BTO rows, pinned slugs, parent class-level decision, and generated text-map projections cited above, but no maintained decision row, term request, history record, causal-graph overlay, target-specific research report, or prior exact YAML review report for `habitatmech:GOLD.1e2686ea7e`.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if a future curation change adds an item-level decision, authored definition, or causal graph overlay for this fossilized-bone record:

- `just validate data/habitats/host_associated/bone.yaml`
- `just validate-strict data/habitats/host_associated/bone.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If future curation signs off the exact GOLD path, add the maintained item-level row to `curation/decisions.tsv`, run `just seed`, preview `just seed-canary habitatmech:GOLD.1e2686ea7e`, and rerun the corpus-level checks after regeneration.

## Additional Notes

- `find reports/yaml_record_review -name '*-bone.md' -print` found no pre-existing exact review report for this record before this file was written.
- `find research/habitats reports/yaml_record_review curation/causal_graphs -iname '*bone*' -print` found no target-specific research report, prior review report, or causal overlay before this file was written.
- The generated text-map hits for `bone` and `fossilized_remains__8b01c49f` are search projections, not maintained inputs.
