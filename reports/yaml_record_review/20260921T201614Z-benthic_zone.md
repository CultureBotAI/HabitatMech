# YAML Record Review: Benthic zone

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/benthic_zone.yaml`
- Started UTC: 2026-09-21T20:16:14Z
- Finished UTC: 2026-09-21T20:16:58Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/benthic_zone.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.c08255b2d2` |
| Label | `Benthic zone` |
| Category | `AQUATIC` |
| Grounding | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Maintained owner | generated from `data/raw/gold_ecosystem_paths.tsv`, `curation/decisions.tsv`, and `data/habitats/PATHS.tsv` |

The record is a generated GOLD-only habitat record. It has no authored definition, synonyms, xrefs, environmental parameters, taxa, evidence items, causal graphs, discussions, external datasets, or curator-authored term request.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/aquatic/benthic_zone.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/benthic_zone.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| Reference validator | Not available: `justfile` exposes no reference-specific validator, and this record has no `EvidenceItem`, `EnvironmentalParameter.reference`, `CharacteristicTaxon.reference`, or causal-edge evidence to target. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the committed ENVO term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 found, 0 missing, 0 extra, 0 differing. |

## Identity and Grounding

| Claim | Trace | Assessment |
|---|---|---|
| `habitatmech:GOLD.c08255b2d2` is the pinned record identifier for this YAML file. | `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.c08255b2d2` to `benthic_zone`. | Supported. |
| The GOLD source concept is `Environmental > Aquatic > Marine > Benthic zone`. | `data/raw/gold_ecosystem_paths.tsv` has one collapsed row for that canonical path, leaf label `Benthic zone`, depth 4, `gold_node_count=2`, and `gold_node_ids=gold.ecosystem:7904\|gold.ecosystem:7905`. The YAML uses `gold.ecosystem:7904`, the first listed node id, and carries the expected note that two GOLD node ids share the path. | Supported. |
| The source has no GOLD organism, study, biosample, or aggregate assertions on the collapsed `Benthic zone` path. | The same collapsed path row reports `organism_count=0`, `study_count=0`, `biosample_count=0`, and `total_assertions=0`. | Supported. The lack of an `assertion_count` field is therefore not hiding a positive count. |
| The `ENVO:00001999` parent comes from the GOLD parent path `Environmental > Aquatic > Marine`. | `data/raw/gold_ecosystem_paths.tsv` has `Environmental > Aquatic > Marine`; `data/habitats/PATHS.tsv` maps that parent record to `ENVO:00001999`; `data/raw/ontology_terms.tsv` labels `ENVO:00001999` as `marine water body`. | Mechanically supported as the generated GOLD source-path parent. The biological `is-a` relation remains unresolved because a benthic zone may denote the sea floor / bottom biome rather than a marine water body. |
| The class-level `CONFIRM_UNGROUNDED` decision explains `grounding_status: UNGROUNDED` and `mapping_status: SEEDED`. | `curation/decisions.tsv` has `habitatmech:GOLD.c08255b2d2` with `decision=CONFIRM_UNGROUNDED` and `review_depth=CLASS`; `docs/HARMONIZATION.md` states that `CLASS` decisions do not promote a record to `REVIEWED` and do not establish whether the concept is a habitat. | Supported. The row intentionally records only the lexical no-match sweep, not an item-level endorsement that no ENVO term can fit. |

The generated curation history mirrors the two maintained events that can apply to this source concept: the 2026-08-12 class-level no-match sweep in `curation/decisions.tsv`, followed by the 2026-08-16 `seed_from_sources` event.

## Evidence

| Area | Status |
|---|---|
| Source attestation | Supported by the maintained GOLD collapsed-path row. The record correctly has a GOLD `source_attestations` entry but no organism count because the path has zero upstream assertions. |
| Definition | Empty. This is expected for a minted, ungrounded, class-level record with no term request. |
| Synonyms and xrefs | Empty. No ontology-grounded identity, BacDive/PREGO merge, or allow-listed external xref supports adding any. |
| Environmental parameters | Empty. Ignored-file-inclusive searches of the raw GOLD triad, GOLD biosample, GOLD study, and environment-parameter inventories found no rows for the exact parent path `Environmental > Aquatic > Marine > Benthic zone`; matches were for the child path `Environmental > Aquatic > Marine > Benthic zone > Sediment`. |
| Characteristic taxa | Empty. The record is not PREGO- or BacDive-attested, and the GOLD path has zero organism assertions. |
| Causal graphs | Empty. There is no `curation/causal_graphs/benthic_zone.yaml`, and upstream GOLD attestation would not be mechanism evidence. |
| Discussions and datasets | Empty. No maintained input currently exists for a record-level note or external dataset on this generated record. |

No evidence item is over-scoped because the record has no curator-authored evidence-bearing definition or mechanism claim.

## Completeness

- An ignored-file-inclusive `find` found no pre-existing exact `*-benthic_zone.md` review report before this report was created.
- Ignored-file-inclusive source searches for `habitatmech:GOLD.c08255b2d2`, `gold.ecosystem:7904`, `Benthic zone`, `Environmental > Aquatic > Marine > Benthic zone`, and `ENVO:00001999` covered maintained raw inputs, `curation/`, `data/habitats/PATHS.tsv`, and the generated target YAML.
- Ignored-file-inclusive searches under `curation`, `history`, `research/habitats`, and `reports/yaml_record_review` found no exact Benthic-zone-owned research report, history record, causal overlay, term request, or prior exact YAML review.
- A broader ignored-file-inclusive search for `benthic` found the vendored `ENVO:01000024` `marine benthic biome` term and an existing item-level curation decision that grounded GOLD `Environmental > Aquatic > Marine > Benthic` to that ENVO class with `relation=CLOSE`. That makes the unreviewed `Benthic zone` split consequential rather than only a missing definition.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| HM-BENTHIC-ZONE-001 | Major | The GOLD `Benthic zone` concept needs item-level curation before the generated `UNGROUNDED` identity and `ENVO:00001999` parent can be trusted. The class-level sweep only proved that the literal label did not match the vendored slice; it did not assess the existing `ENVO:01000024` `marine benthic biome` candidate or the near-duplicate, item-grounded GOLD path `Environmental > Aquatic > Marine > Benthic`. If `Benthic zone` denotes the same bottom habitat/biome as that sibling path, it should probably merge into `ENVO:01000024`; if it denotes a distinct zone, the inherited `marine water body` parent needs explicit review because it may be related context rather than a strictly broader habitat. | `curation/decisions.tsv` row `habitatmech:GOLD.c08255b2d2` has `review_depth=CLASS`. The adjacent GOLD row `Environmental > Aquatic > Marine > Benthic` was item-grounded to `ENVO:01000024` `marine benthic biome`; ENVO defines that term around the seafloor and includes shores, littoral/intertidal areas, coral reefs, and the deep seabed. `docs/HARMONIZATION.md` explicitly says `CLASS` decisions do not establish whether a concept is a habitat. | `curation/decisions.tsv`; possibly `curation/term_requests.tsv` only if item review establishes that neither `ENVO:01000024` nor another existing ontology term fits. |

No blocker findings.

## Recommended Edits

| Priority | Edit | Owner | Follow-up validator |
|---:|---|---|---|
| 1 | Compare `Environmental > Aquatic > Marine > Benthic zone` against the already item-reviewed `Environmental > Aquatic > Marine > Benthic` source and `ENVO:01000024` `marine benthic biome`, then replace the `CLASS` row for `habitatmech:GOLD.c08255b2d2` with an `ITEM`-depth decision. | `curation/decisions.tsv` | `just seed`, a `just seed-canary` for the affected source concept or resolved ENVO target, and `just verify-corpus --max-diffs 1` after applying the regenerated output. |
| 2 | If item review keeps `Benthic zone` minted instead of merging it, add a supported definition and strict broader parent; use `ADD` versus `REPLACE` to retain only true source-derived parents. | `curation/term_requests.tsv` | `just term-requests-check`, `just validate data/habitats/aquatic/benthic_zone.yaml`, and `just validate-strict data/habitats/aquatic/benthic_zone.yaml --quiet`. |

## Follow-up Checks

- Re-run `just validate data/habitats/aquatic/benthic_zone.yaml` on any regenerated minted record, or the resolved `marine_benthic_biome.yaml` if item curation merges the GOLD source into `ENVO:01000024`.
- Re-run `just validate-strict <affected-record> --quiet` to enforce closed-schema output.
- Re-run `just verify-corpus --max-diffs 1` to prove generated YAML still exactly reproduces from `data/raw/` and `curation/`.
- Re-run `just term-requests-check` if a minted definition or ENVO term request is added.
- Re-run `just validate-history` after adding the required append-only history record for an item-level curation change.

## Additional Notes

- The generated YAML itself should remain read-only. The review identified curation ownership in `curation/decisions.tsv` and, conditionally, `curation/term_requests.tsv`.
- The source row's zero counts are not a validator problem: the collapsed GOLD inventory intentionally retains ecosystem categories even when no current organism, study, or biosample assertion is attached directly to that path.
- The raw GOLD child path `Environmental > Aquatic > Marine > Benthic zone > Sediment` has one study, six biosamples, and MIxS triads, but those rows belong to `gold.ecosystem:7906` and do not directly support adding parameters to this parent `Benthic zone` record.
