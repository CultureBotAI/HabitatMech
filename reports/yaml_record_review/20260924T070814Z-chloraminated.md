# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/aquatic/chloraminated.yaml`
- Started UTC: 2026-09-24T07:05:52Z
- Finished UTC: 2026-09-24T07:08:14Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/chloraminated.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.1aca20ff55` |
| Label | `Chloraminated` |
| Category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| GOLD source-concept key | `habitatmech:GOLD.1aca20ff55` |
| GOLD parent source-concept key | `habitatmech:GOLD.99a88ecb51` |
| Maintained owners | Generated from `data/raw/gold_ecosystem_paths.tsv`, `curation/decisions.tsv`, `data/habitats/PATHS.tsv`, and `src/habitatmech/seed.py`; `data/habitats/` is generated and remains read-only |

This generated GOLD-only record represents `gold.ecosystem:5856`:

```text
Environmental > Aquatic > Freshwater > Drinking water > Chloraminated
```

The record is one of the disinfection-state or treatment-state leaves under
GOLD's `Drinking water` path. This review covers only the `Chloraminated` child
source with stable minted identifier `habitatmech:GOLD.1aca20ff55`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/chloraminated.yaml` | Passed: LinkML reported no issues |
| `just validate-strict data/habitats/aquatic/chloraminated.yaml` | Passed: 1 file scanned, 0 files with errors, 0 total error rows |
| `just validate-causal <overlay>` | Not applicable: the record has no `causal_graphs` slot, and ignored/hidden-inclusive searches of `curation/causal_graphs/` found no overlay for `habitatmech:GOLD.1aca20ff55`, `gold.ecosystem:5856`, or `chloraminated` |
| `just validate-causal-all` | Passed: 32 causal-graph curation files with 32 graphs |
| `just term-requests-check` | Passed: term-request table is current with 109 terms |
| `just validate-history` | Passed: no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Passed: expected 3,206 records, found 3,206, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | Passed: 0 ungrounded records still undecided, 1,810 decisions on file |
| `just report` | Passed: corpus report completed for 3,206 records |
| Reference validator | Not applicable: this record has no DOI, PMID, URL, `EvidenceItem`, or causal-edge evidence that would need citation checking |
| `git diff --check` | Passed before this report was written |

## Identity and Grounding

| Claim | Maintained evidence | Assessment |
|---|---|---|
| The output identifier is `habitatmech:GOLD.1aca20ff55`. | `data/habitats/PATHS.tsv` pins that identifier to `chloraminated`. | Supported. |
| The GOLD source ID is `gold.ecosystem:5856`. | `data/raw/gold_ecosystem_paths.tsv` has one row for `Environmental > Aquatic > Freshwater > Drinking water > Chloraminated` with one GOLD node ID, depth 5, and zero organism, study, biosample, or total assertions. | Supported. |
| The source belongs in the aquatic category. | `src/habitatmech/seed.py` infers the category from GOLD's `Environmental > Aquatic` prefix for this raw row. | Supported. |
| The source is broader than neither `Chlorinated` nor `Unchlorinated`. | The committed GOLD ecosystem inventory keeps `Chloraminated`, `Chlorinated`, `Unchlorinated`, `Untreated`, `Filtered water`, and `Delivery networks` as separate children of `Environmental > Aquatic > Freshwater > Drinking water`. | Supported. |
| `habitatmech:GOLD.99a88ecb51` `Drinking water` is a broader source-path parent for this source concept. | The GOLD parent-path pass in `src/habitatmech/seed.py` links each GOLD concept to the resolved concept of its parent path; the parent path `Environmental > Aquatic > Freshwater > Drinking water` resolves to `habitatmech:GOLD.99a88ecb51`. | Supported. |
| `grounding_status: UNGROUNDED` is generated by a class-level no-match sweep. | `curation/decisions.tsv` has `habitatmech:GOLD.1aca20ff55` as `CONFIRM_UNGROUNDED` with no `object_id` and `review_depth: CLASS`. The row records that no vendored-slice term matched the label by any search route, and that whether the concept is a habitat was not assessed. | Mechanically supported, but incomplete for item-level curation. |
| `mapping_status: SEEDED` is correct for the current curation input. | `docs/CURATION.md` says `CLASS` records membership in a mechanically defined screen and does not promote a record to `REVIEWED`; `Decision.counts_as_reviewed` only returns true when `review_depth` is `ITEM`. | Supported. |

The generated record is reproducible, but its only curator decision is too weak
now that the full GOLD path has been read. GOLD's leaf label is an adjective,
yet the five-level source path scopes it to chloraminated drinking water: a
real microbial habitat that is narrower than drinking water and distinct from
the sibling chlorine-treated and untreated drinking-water leaves.

## Evidence

The source attestation is an accurate transcription of the committed GOLD row:

- `source: GOLD`
- `source_id: gold.ecosystem:5856`
- `source_label: Chloraminated`
- `source_path: Environmental > Aquatic > Freshwater > Drinking water > Chloraminated`

The attestation correctly omits `assertion_count` and `assertion_unit`: the
maintained GOLD ecosystem-path row for `gold.ecosystem:5856` has
`organism_count`, `study_count`, `biosample_count`, and `total_assertions` all
set to `0`.

Additional GOLD-derived evidence supports the existence and boundary of the
source path:

| Source | Maintained row | Assessment |
|---|---|---|
| Biosamples | `data/raw/gold_path_biosamples.tsv` maps the exact path and node `5856` to 32 biosamples. | Supported; biosample counts are independent of the `ORGANISM` count in the generated source attestation and are not emitted on this record. |
| GOLD studies | `data/raw/gold_studies.tsv` has one study row, `Gs0154279`, that includes `Environmental > Aquatic > Freshwater > Drinking water > Chloraminated` beside its Drinking water parent and its `Chlorinated` sibling. | Supports source use of the exact path and confirms that GOLD distinguishes chloramine-treated drinking water from chlorine-treated drinking water. |
| MIxS triads | Ignored/hidden-inclusive exact searches found no row for `Environmental > Aquatic > Freshwater > Drinking water > Chloraminated` or node `5856` in `data/raw/gold_path_triads.tsv`. | No MIxS triad should be emitted as `environmental_parameters`. |

There is no claim-level evidence, causal graph, or reference-bearing assertion
on this seeded GOLD record.

## Completeness

- The generated target contains every field currently emitted from the exact
  maintained GOLD row and the class-level curation row: minted identity,
  aquatic category, ungrounded seeded status, the GOLD source attestation, the
  generated parent-path parent, the class-sweep curation event, and one seed
  curation event.
- Empty `synonyms`, `environmental_parameters`, `characteristic_taxa`,
  `evidence`, `causal_graphs`, `discussion`, and `datasets` are technically
  acceptable for the current maintained inputs. No maintained input supplies an
  output synonym, MIxS triad, taxon association, claim-level evidence item,
  causal overlay, discussion item, or dataset reference for this source
  concept.
- Ignored/hidden-inclusive exact searches covered `curation/decisions.tsv`,
  `curation/term_requests.tsv`, `curation/external_xrefs.tsv`,
  `curation/causal_graphs/`, `history/`, `research/`, and
  `reports/yaml_record_review/` for `habitatmech:GOLD.1aca20ff55`,
  `gold.ecosystem:5856`,
  `Environmental > Aquatic > Freshwater > Drinking water > Chloraminated`,
  `chloraminated`, and `Chloraminated`. They found the class-depth decision row
  and one contextual oil/gas-pipeline research mention of chloraminated potable
  water, but no item-level decision, term-request row, external-xref row,
  causal overlay, history entry, dedicated research item, or prior exact YAML
  review report.
- A gitignore-independent `find` search covered `reports/yaml_record_review/`
  before report creation; no exact `*-chloraminated.md` report was found.

## Findings

### Major

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| `HM-CHLORAMINATED-001` | The source concept still needs item-level curation. It denotes chloraminated drinking water, but the only current decision is a class-depth lexical no-match row that did not assess the full GOLD path, did not confirm habitathood, and did not decide whether the concept needs a novel term request. | `curation/decisions.tsv` records `habitatmech:GOLD.1aca20ff55` as `CONFIRM_UNGROUNDED` with `review_depth: CLASS`. The raw GOLD path places `Chloraminated` directly under `Environmental > Aquatic > Freshwater > Drinking water`, beside other treatment-state leaves such as `Chlorinated`, `Unchlorinated`, `Untreated`, and `Filtered water`. Exact ignored/hidden-inclusive searches of the vendored ontology slice and curation inputs found no chloraminated-drinking-water term, no `curation/term_requests.tsv` row, and no target-specific item-level decision. | Replace the class-depth row for `habitatmech:GOLD.1aca20ff55` in `curation/decisions.tsv` with an item-level `CONFIRM_UNGROUNDED` row if no exact ontology identity exists after final review, then add a `curation/term_requests.tsv` definition for the chloraminated-drinking-water habitat. |

No blocker or minor findings found.

## Recommended Edits

1. Replace the existing `CLASS`-depth `curation/decisions.tsv` row for
   `habitatmech:GOLD.1aca20ff55` with an `ITEM`-depth decision. If final item
   review confirms no exact ontology term exists, keep the minted identifier
   with `CONFIRM_UNGROUNDED`.

2. Add a `curation/term_requests.tsv` row defining the novel
   chloraminated-drinking-water concept under `ENVO:00003064` `drinking water`.
   Preserve the generated GOLD parent-path edge to
   `habitatmech:GOLD.99a88ecb51` unless item review finds that the source
   hierarchy itself is too broad.

3. Regenerate the corpus from the maintained inputs and confirm
   `data/habitats/aquatic/chloraminated.yaml` is still minted, has the same GOLD
   source attestation for `gold.ecosystem:5856`, receives the authored
   definition, and becomes `mapping_status: REVIEWED`.

## Follow-up Checks

| Scope | Command or check |
|---|---|
| Target schema | `just validate data/habitats/aquatic/chloraminated.yaml` |
| Closed-schema validation | `just validate-strict data/habitats/aquatic/chloraminated.yaml` |
| Corpus reproduction | `just verify-corpus --max-diffs 1` |
| Worklist/report sanity | `just worklist --limit 2000` and `just report` |
| Causal overlays | `just validate-causal-all` |
| Term-request exports | `just term-requests-check` |
| History | `just validate-history` |
| Manual decision check | Confirm the replacement decision is keyed to `habitatmech:GOLD.1aca20ff55`, uses `review_depth: ITEM`, and does not merge this source with the sibling `Chlorinated`, `Unchlorinated`, `Untreated`, or `Filtered water` leaves |
| Diff hygiene | `git diff --check` |

## Additional Notes

- The exact `Chloraminated` GOLD path has no committed MIxS triad row, unlike
  its `Chlorinated` and `Delivery networks` siblings. That absence does not
  make the source path invalid; it only means there is no triad-derived term to
  use as independent grounding evidence.
- GOLD records 32 biosamples for `gold.ecosystem:5856` even though the KGX
  ecosystem-path aggregate has zero organism assertions. Those are different
  source tables and should stay separated: the generated GOLD attestation
  should keep omitting `assertion_count` until the exact ecosystem path has a
  nonzero `organism_count`.
