# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/aquatic/cave_lake.yaml`
- Started UTC: `2026-09-23T23:45:04Z`
- Finished UTC: `2026-09-23T23:48:30Z`
- Verdict: `needs curation`

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/cave_lake.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.d9fb6dd542` |
| Label | `Cave lake` |
| Generated or maintained | Generated from `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/PATHS.tsv`, and `curation/decisions.tsv` |
| Habitat category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Source concept | `Environmental > Aquatic > Deep subsurface > Groundwater > Cave lake` |

This is the generated GOLD Cave lake leaf beneath Groundwater. Its identifier
reproduces from
`mint("GOLD", "Environmental > Aquatic > Deep subsurface > Groundwater > Cave lake")`
as `habitatmech:GOLD.d9fb6dd542`, and `data/habitats/PATHS.tsv` pins the
identifier to the corpus-wide `cave_lake` slug.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/cave_lake.yaml` | Passed; LinkML accepted the record as `HabitatRecord`. |
| `just validate-strict data/habitats/aquatic/cave_lake.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Focused causal validator | Not applicable: ignored-inclusive `find`/`rg` checks found no `curation/causal_graphs/*cave_lake*` overlay and no causal overlay mentioning `habitatmech:GOLD.d9fb6dd542`. |
| `just validate-causal-all` | Passed; 32 curated causal-graph files with 32 graphs validated. |
| Reference validator | Not applicable: `CLAUDE.md` and `justfile` do not document a standalone reference validator for records without causal overlays. |
| `just term-requests-check` | Passed; `curation/term_requests/envo_robot_template.tsv` is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 records expected, 3,206 found, 0 missing, 0 extra, 0 differing. |
| `just worklist --limit 2000` | Passed; 0 ungrounded records still undecided, 1,810 decisions on file. |
| `just report` | Passed; corpus report completed after counting 3,206 records and 686 `REVIEWED` records. |
| `git diff --check` | Passed. |

## Identity and Grounding

The minted source identity is internally consistent. `data/raw/gold_ecosystem_paths.tsv`
has one collapsed GOLD ecosystem concept for the exact canonical path, with
leaf label `Cave lake`, depth 5, `gold_node_count` 1, `total_assertions` 0,
and `gold_node_ids` `gold.ecosystem:8197`. The generated
`source_attestations` entry carries the same source id, source label, and
source path, with no generated assertion count or count unit because the KGX
assertion inventory has no organism assertions for this GOLD path.

The repository has additional source inventory proving GOLD did attach samples
to the concept. `data/raw/gold_path_biosamples.tsv` has two bulk-export
biosamples for the same `gold.ecosystem:8197` path, and
`data/raw/gold_studies.tsv` places those two biosamples in one study shared
with the sibling `Cave microbial mat` path. That separate biosample inventory
does not feed the generated `source_attestations` assertion count, so the
record's zero-count source attestation is faithful to the committed
`gold_ecosystem_paths.tsv` row.

The current grounding is only class-level. `curation/decisions.tsv` carries a
`CONFIRM_UNGROUNDED` row from the August 2026 class-level sweep for
`habitatmech:GOLD.d9fb6dd542`; the generated `curation_history` explicitly
notes that the sweep matched no vendored term by lexical route and did not
assess whether this exact concept is a habitat or term-request candidate. The
resulting `SEEDED` mapping status is therefore correct for an unreviewed GOLD
source concept, but the decision row is not an item-level review.

The inherited parent is suspicious. The generated parent,
`habitatmech:GOLD.22a80cbd14` `Groundwater`, is the GOLD parent path
`Environmental > Aquatic > Deep subsurface > Groundwater`; it is itself a
generated `NARROW` record beneath `ENVO:01001004` `groundwater`. ENVO defines
groundwater as underground water in pore spaces of rock or unconsolidated
deposits, while it already defines `ENVO:00000058` `underground lake` for a
lake formed under the Earth's surface. A cave lake is more plausibly an
underground-lake habitat than an is-a child of pore-space groundwater, and the
class-level Cave lake sweep did not record that broader term.

## Evidence

The generated record is traceable to maintained inputs:

| Claim | Maintained input or inventory |
|---|---|
| GOLD source id and exact source path | `data/raw/gold_ecosystem_paths.tsv` row for `gold.ecosystem:8197` |
| Stable HabitatMech identifier and slug | `data/habitats/PATHS.tsv` row for the Cave lake GOLD path |
| Class-level `CONFIRM_UNGROUNDED` status | `curation/decisions.tsv` row for `habitatmech:GOLD.d9fb6dd542` |
| Two GOLD biosamples for the path | `data/raw/gold_path_biosamples.tsv` row for `gold.ecosystem:8197` |
| One GOLD study containing those biosamples | `data/raw/gold_studies.tsv` row for study `Gs0153647` |

The record has no record-level literature entry. A spot-check of Hershey et al.
2018, DOI `10.3389/fmicb.2018.02823`, corroborates that Wind Cave lakes are
microbial habitats in directly sampled, aphotic, subterranean aquifer lakes,
but that paper is not a maintained HabitatMech input and was not linked to
`gold.ecosystem:8197` during this review.

No causal graph is attached to this record, so there are no mechanism edges
needing claim-level `EvidenceItem` support.

## Completeness

The required generated identity, category, source attestation, source-derived
parent, curation history, and seeded mapping status are present.

The empty optional slots are acceptable for the current generated record:

| Slot | Review |
|---|---|
| `environmental_parameters` | Empty. The GOLD row has no committed biosample triad or environment-parameter row in `data/raw/`, and no curator-authored parameter row is expected for a review-only generated concept. |
| `characteristic_taxa` | Empty. The GOLD aggregate does not commit organism names or taxon identities for `gold.ecosystem:8197`. |
| `evidence` | Empty. There is no item-level curation row, causal overlay, or maintained literature artifact for this record yet. |
| `causal_graphs` | Empty. Exact ignored-inclusive searches found no `curation/causal_graphs/*cave_lake*` overlay and no overlay keyed to `habitatmech:GOLD.d9fb6dd542`. |
| `discussions` and `datasets` | Empty. No maintained discussion or dataset input exists for this record. |

Exact ignored-inclusive searches for `habitatmech:GOLD.d9fb6dd542`,
`gold.ecosystem:8197`, `cave_lake`, `Cave lake`, and the GOLD source path found
the generated record, its `PATHS.tsv` row, the raw GOLD path inventories, and
the class-level decision row. They did not find a target-specific term request,
causal overlay, history record, research report, or prior YAML review report.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | None found | The generated record validates, denotes the expected GOLD source path, and carries the correct `SEEDED` status for a source concept with only a class-level decision. | Not applicable |
| Major | The class-level `CONFIRM_UNGROUNDED` row missed the vendored `ENVO:00000058` `underground lake` term and left Cave lake under a likely non-strict `Groundwater` parent. This is a real cave-lake habitat, not a lexical false positive; it needs item-level curation to decide whether `ENVO:00000058` should be a broader parent or whether the Cave lake and two GOLD `Subterranean lake` source concepts should merge around that ENVO term. | `data/raw/ontology_terms.tsv` and `data/raw/ontology_subclass_edges.tsv` include `ENVO:00000058` as a subclass of `ENVO:00000020` `lake`. `data/raw/gold_path_triads.tsv` independently assigns `ENVO:00000058` as the local triad term for both existing GOLD `Subterranean lake` paths, while exact ignored-inclusive searches found no generated HabitatMech record or curation row that uses `ENVO:00000058`. Hershey et al. 2018 also confirms that cave-accessed subterranean lakes can be direct microbial habitats rather than merely pore-space groundwater. | `curation/decisions.tsv`; probably `curation/term_requests.tsv` if Cave lake remains novel after item-level review |
| Minor | None found | The remaining empty optional slots are consistent with the absence of maintained item-level curation or a causal overlay. | Not applicable |

## Recommended Edits

| Priority | Edit | Maintained path |
|---|---|---|
| Major | Revisit `habitatmech:GOLD.d9fb6dd542` with item-level curation. Replace the class-level lexical decision with either a `GROUND_AS_PARENT` decision to `ENVO:00000058` plus a `parent_mode=REPLACE` term request, or `SAME_AS` decisions that merge the Cave lake source concept with the equivalent GOLD `Subterranean lake` records if the three source concepts prove coextensive. | `curation/decisions.tsv` and, if the Cave lake record remains novel, `curation/term_requests.tsv` |
| Major | If Cave lake stays as a distinct novel term, replace the inherited GOLD `Groundwater` parent with `ENVO:00000058` `underground lake` so the generated hierarchy does not claim every cave lake is pore-space groundwater. | `curation/term_requests.tsv` |

Do not hand-edit `data/habitats/aquatic/cave_lake.yaml`; regenerate it from the
maintained input after item-level curation changes.

## Follow-up Checks

| Edit | Narrowest proving checks |
|---|---|
| Item-level Cave lake grounding or merge | Exact ignored-inclusive `rg` for `habitatmech:GOLD.d9fb6dd542`, `gold.ecosystem:8197`, and `ENVO:00000058` should find the new maintained decision and any term request; `just seed-canary habitatmech:GOLD.d9fb6dd542`, `just term-requests-check`, `just validate data/habitats/aquatic/cave_lake.yaml`, `just validate-strict data/habitats/aquatic/cave_lake.yaml`, `just validate-history`, and `just verify-corpus --max-diffs 1` should still pass. |
| Parent replacement, if Cave lake stays distinct | The regenerated `data/habitats/aquatic/cave_lake.yaml` should list `ENVO:00000058`, not `habitatmech:GOLD.22a80cbd14`, under `parent_habitats`; run `just validate data/habitats/aquatic/cave_lake.yaml`, `just validate-strict data/habitats/aquatic/cave_lake.yaml`, and `just verify-corpus --max-diffs 1`. |

## Additional Notes

- Searches used to establish absence included ignored and hidden files. Broad
  exact content searches excluded `data/text_map/**`, `pages/**`, `build/**`,
  and `.git/**` so generated text-map, rendered-page, build-product, and git
  object matches did not dominate the results.
- `data/habitats/aquatic/subterranean_lake.yaml` and
  `data/habitats/aquatic/subterranean_lake__155f3b29.yaml` are similarly still
  class-swept. The GOLD triad rows for both Subterranean lake paths chose
  `ENVO:00000058`, but no exact `Cave lake` triad exists in
  `data/raw/gold_path_triads.tsv`.
- `data/raw/gold_path_biosamples.tsv` is derived from GOLD's bulk export,
  while `data/raw/gold_ecosystem_paths.tsv` comes from the kg-microbe KGX dump.
  The Cave lake record's missing `assertion_count` is therefore expected even
  though the bulk-export inventory has two biosamples for `gold.ecosystem:8197`.
