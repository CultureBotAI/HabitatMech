# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/other/wilt.yaml
- Started UTC: 2026-09-26T21:37:40Z
- Finished UTC: 2026-09-26T21:41:33Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/other/wilt.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:BACDIVE.4c48b5114d` |
| Label | `Wilt` |
| Category | `OTHER` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Generated or maintained | Generated from `data/raw/` plus `curation/decisions.tsv`; do not hand-edit |

The filename is pinned by `data/habitats/PATHS.tsv`, whose exact target row is:

```text
habitatmech:BACDIVE.4c48b5114d	wilt
```

The single source concept is maintained in `curation/decisions.tsv` as a
class-level sweep row:

| Field | Value |
|---|---|
| source ID | `habitatmech:BACDIVE.4c48b5114d` |
| decision | `CONFIRM_UNGROUNDED` |
| curator/date | `claude-opus-5` / `2026-08-12` |
| review depth | `CLASS` |
| target ID, target label, predicate, relation | empty |
| note | Class-level sweep; no term in the vendored slice matched this label by any search route, but whether the concept is a habitat at all was not assessed. |

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/other/wilt.yaml` | PASS; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/other/wilt.yaml` | PASS; one file scanned, zero files with errors, zero total error rows. |
| `just validate-causal-all` | PASS; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | PASS; the term-request table is current at 109 terms. |
| `just validate-history` | PASS; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | PASS; 3,206 expected records, zero missing, zero extra, zero differing. |
| `git diff --check` | PASS; no whitespace errors before this report edit. |

No validator was skipped.

## Identity and Grounding

The generated record mechanically matches the BacDive source inventory:

- `data/raw/bacdive_isolation_sources.tsv` has the exact source ID
  `bacdive.isolation_source:wilt`, label `Wilt`, normalized value `wilt`, three
  strain assertions, and three source taxa.
- `data/raw/isolation_source_groundings.tsv` keeps BacDive's `Wilt` row with an
  empty ontology target from `manual_review`, so the seeder correctly avoided
  re-grounding the source by a weak lexical candidate such as `silt` or `milt`.
- The generated `source_attestations[0]` preserves the raw source ID, label,
  and count as `assertion_count: 3` with `assertion_unit: STRAIN`.
- `data/raw/bacdive_source_taxa.tsv` has the three rows rendered under
  `characteristic_taxa`: `NCBITaxon:2035` `Curtobacterium flaccumfaciens`,
  `NCBITaxon:47879` `Pseudomonas corrugata`, and `NCBITaxon:554`
  `Pectobacterium carotovorum`, each with one BacDive strain assertion.

The current generated identity is wrong because the source concept is not a
habitat. BacDive's flat `Wilt` isolation-source tag names the wilting state of a
plant rather than a sampled plant compartment, lesion, material, or site. In
contrast to localized BacDive plant-infection labels such as `Spot-Leaf,Stem`,
`Rot-Root,Stem`, `Canker`, and `Gall`, `Wilt` does not identify a bounded
infection structure that can be represented as an environment associated with a
plant part. It should be retired from the habitat candidate set with an
item-level `NOT_APPLICABLE` decision, not promoted to a novel ENVO term request.

## Evidence

| Claim | Evidence | Status |
|---|---|---|
| The record denotes BacDive source `bacdive.isolation_source:wilt`. | `data/raw/bacdive_isolation_sources.tsv` records ID `bacdive.isolation_source:wilt`, label `Wilt`, normalized value `wilt`, three strain assertions, and three source taxa; the generated YAML repeats the source ID, source label, assertion count, assertion unit, and three BacDive taxa. | Supported exactly. |
| The upstream mapping deliberately declined an ontology target. | `data/raw/isolation_source_groundings.tsv` keeps the `Wilt` row targetless with `manual_review` provenance from `bacdive` on `2026-05-02`; `src/habitatmech/seed.py` emits the matching source-attestation note when the automatic BacDive route is `bacdive_declined_upstream`. | Supported exactly. |
| The class-level `CONFIRM_UNGROUNDED` row is only a lexical negative, not a habitat review. | `curation/decisions.tsv` has `review_depth` `CLASS` and the sweep note states that whether the concept is a habitat at all was not assessed. | Supported exactly. |
| `Wilt` should be treated as non-habitat. | `curation/samples/class_swept_unscreened-20260814.tsv` already spot-checked this exact identifier and marked it wrong as a plant disease state rather than a site; the same sample marked the `Canker` sibling as a habitat, and the `Spot-Leaf,Stem`, `Rot-Root,Stem`, and `Gall` siblings have item-level curation or definitions as localized plant-infection structures. | Supported; enough for a review finding that the source concept needs item-level `NOT_APPLICABLE`, but not applied until the maintained row changes. |

The record has no record-level `evidence` items, causal edges, environmental
parameter rows, discussions, or dataset entries to support or correct. Its three
`characteristic_taxa` rows are source-observed BacDive taxon associations only:
none carries `is_characteristic` or claims those taxa typify a wilting plant.

## Completeness

The generated YAML is complete for its current input state: it has the exact
BacDive source ID, label, strain count, source-observed taxon rows, and
generated curation-history events for the class-level `CONFIRM_UNGROUNDED` row
and the BacDive seed. Its empty definition, synonyms, parents, xrefs,
environmental parameters, causal graphs, discussions, and datasets are expected
for a flat BacDive isolation-source label that has not been item-reviewed or
defined.

Ignored/hidden-inclusive searches for `habitatmech:BACDIVE.4c48b5114d`,
`4c48b5114d`, `bacdive.isolation_source:wilt`, `Wilt`, and `wilt` covered
`data/raw/`, `data/habitats/PATHS.tsv`, `curation/`, `history/`, `research/`,
and `reports/`. They found the generated target, locked slug, raw BacDive
source and taxon rows, the empty upstream mapping row, the class-level decision
row, the class-sweep sample verdict, contextual mentions in two sibling
research reports, and one contextual prior review mention from the
`Spot-Leaf,Stem` report. They found no target-specific term request,
term-request exclusion, history record, causal overlay, committed paid research
report, or prior target-specific YAML review.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | `Wilt` is represented as an ungrounded habitat candidate even though the BacDive source concept names a whole-plant wilting state rather than a sampled site or material. | `curation/decisions.tsv` only has a class-level lexical sweep row; `curation/samples/class_swept_unscreened-20260814.tsv` already flags this identifier as a plant disease state, and the raw BacDive inventory has no narrower body-site path or material label that would turn `Wilt` itself into a habitat. | `curation/decisions.tsv`; `history/` |

No major or minor findings.

## Recommended Edits

1. Replace the class-level `habitatmech:BACDIVE.4c48b5114d` row in
   `curation/decisions.tsv` with an item-level `NOT_APPLICABLE` decision whose
   note records that `Wilt` is a plant disease or physiological state, not the
   plant compartment from which BacDive strains were isolated.
2. Do not add a `curation/term_requests.tsv` row for this identifier; there is
   no missing ENVO habitat term to request for the BacDive label itself.
3. Add an append-only history record for the curation session under `history/`.
4. Regenerate this record from maintained inputs:

```bash
just seed
just seed-canary habitatmech:BACDIVE.4c48b5114d
just seed-apply --force
```

## Follow-up Checks

After the future curation edit:

1. Re-read `data/habitats/other/wilt.yaml` and confirm it has
   `grounding_status: NOT_APPLICABLE`, `mapping_status: REVIEWED`, no
   definition, no parent added merely to rescue the disease-state label, and a
   generated item-level `NOT_APPLICABLE` curation event for
   `habitatmech:BACDIVE.4c48b5114d`.
2. Confirm `source_attestations[0]` still points at
   `bacdive.isolation_source:wilt` with three BacDive strain assertions.
3. Confirm the three `characteristic_taxa` rows remain source-observed rows
   without `is_characteristic` claims.
4. Run the narrow validators:

```bash
just validate data/habitats/other/wilt.yaml
just validate-strict data/habitats/other/wilt.yaml
just term-requests-check
just validate-history
just verify-corpus --max-diffs 1
git diff --check
```

5. Run the full corpus gate before PR:

```bash
just qc
```

## Additional Notes

- This review used ignored/hidden-inclusive exact searches when checking for
  target-specific maintained inputs, overlays, history, research, and prior
  reviews.
- The `Rot-Root,Stem` and `Spot-Leaf,Stem` research reports mention Wilt only
  to draw sibling boundaries inside BacDive's plant-infection cohort; they are
  contextual leads, not maintained inputs for this target record.
- This was a read-only review. It did not edit generated YAML, generated pages,
  source inventories, decision rows, term requests, causal overlays, or history
  records.
