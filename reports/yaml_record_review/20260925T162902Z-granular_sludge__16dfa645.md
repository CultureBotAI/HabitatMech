# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/granular_sludge__16dfa645.yaml
- Started UTC: 2026-09-25T16:29:02Z
- Finished UTC: 2026-09-25T16:30:57Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.e8d7f5a282` |
| Label | `Granular sludge` |
| Category | `ENGINEERED` |
| Source path | `Engineered > Bioreactor > Anaerobic > Sludge > Granular sludge` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv`, `curation/decisions.tsv`, and `data/habitats/PATHS.tsv`; future item-level review belongs in `curation/decisions.tsv`, and any retained minted definition or term request belongs in `curation/term_requests.tsv`. |

The target record is GOLD-only, generated, `UNGROUNDED`, and still `SEEDED`.
It has the exact GOLD path above, one source node `gold.ecosystem:8142`, no
direct GOLD occurrence assertions, and an inherited `ENVO:00002129`
`anaerobic sludge` parent from the immediate source path
`Engineered > Bioreactor > Anaerobic > Sludge`.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/habitats/engineered/granular_sludge__16dfa645.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/granular_sludge__16dfa645.yaml` | Pass: scanned 1 file with 0 files containing `ERROR` and 0 total `ERROR` rows. |
| `just validate-causal curation/causal_graphs/granular_sludge__16dfa645.yaml` | Not applicable: the target has no `causal_graphs` block, and exact gitignore-independent searches found no target-specific causal overlay. |
| `just validate-causal-all` | Pass: validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Pass: the term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: expected 3,206 records, found 3,206 on disk, and the corpus reproduces exactly from `data/raw/`. |
| `just worklist --limit 2000 --status all --out /tmp/habitatmech-granular-sludge-anaerobic-worklist.tsv` | Pass: reported 953 ungrounded records, 1,810 decisions on file, and wrote 953 rows to `/tmp/habitatmech-granular-sludge-anaerobic-worklist.tsv`. |
| `just report` | Pass: completed the corpus report for 3,206 records. |
| `git diff --check` | Pass. |
| Reference validator | Not applicable: this generated GOLD-only record has no record-level `evidence`, `characteristic_taxa`, `causal_graphs`, `discussions`, or `datasets` with citation-bearing `EvidenceItem` references. |

## Identity and Grounding

The generated identifier, label, category, source attestation, and slug agree
with the source inventory. `data/raw/gold_ecosystem_paths.tsv` has the exact
path `Engineered > Bioreactor > Anaerobic > Sludge > Granular sludge` with
`gold_node_count=1`, zero direct organism, study, biosample, and total
assertions, and `gold_node_ids=gold.ecosystem:8142`. `data/habitats/PATHS.tsv`
maps `habitatmech:GOLD.e8d7f5a282` to the stable
`granular_sludge__16dfa645` slug.

The current `curation/decisions.tsv` row for
`habitatmech:GOLD.e8d7f5a282` is a `CLASS`-depth
`CONFIRM_UNGROUNDED` decision. That row supports the lexical conclusion that no
term in the vendored slice matched the label by the sweep's search routes, but
its curation-history event explicitly says the source concept's habitat status
was not assessed. The generated `UNGROUNDED` state is therefore defensible as a
provisional state, not enough to mark this concept reviewed.

The generated parent `ENVO:00002129` `anaerobic sludge` is a plausible strict
broader parent for the GOLD child. The immediate source parent,
`Engineered > Bioreactor > Anaerobic > Sludge`, is an exact grounded
multi-source record for `anaerobic sludge`, and this target adds the
`Granular sludge` child under that already-grounded sludge path.

The vendored ontology slice has a close lexical near miss rather than a direct
identity for this source concept. `data/raw/ontology_terms.tsv` contains
`ENVO:00002212` `thermophilic granular sludge`, and
`data/raw/ontology_subclass_edges.tsv` places it under `ENVO:00002044`
`sludge`, but that term over-specifies temperature and does not match the
generic anaerobic GOLD leaf.

## Evidence

The sole `GOLD` source attestation mirrors
`data/raw/gold_ecosystem_paths.tsv`: source ID `gold.ecosystem:8142`, source
label `Granular sludge`, and source path
`Engineered > Bioreactor > Anaerobic > Sludge > Granular sludge`.

No PREGO, BacDive, MADIN, or Environments Table source concept is merged into
this record, no characteristic taxa are present, and no evidence-backed
environmental parameter or causal mechanism evidence is present. Exact
gitignore-independent searches for the target source path found no row in
`data/raw/gold_path_triads.tsv`, `data/raw/environment_parameters.tsv`,
`data/raw/gold_studies.tsv`, or `data/raw/gold_path_biosamples.tsv`; the
parent `Engineered > Bioreactor > Anaerobic > Sludge` has its own GOLD side
tables, but those belong to the parent path rather than this child.

The same-label GOLD sibling at
`Engineered > Bioreactor > UASB (Upflow anaerobic sludge blanket) > Sludge >
Granular sludge` is a distinct generated record with three direct GOLD
organism assertions. It gives useful neighboring context, but it is a separate
source concept beneath a more specific reactor path and does not by itself
support merging or grounding `habitatmech:GOLD.e8d7f5a282`.

Unsupported or over-scoped generated claims:

- None found in the generated fields themselves. The record is internally
  consistent with the raw GOLD path, the generated parent, and the current
  class-level ungrounded decision.

## Completeness

The generated record has the source-owned fields expected for this zero-count
GOLD leaf: the minted identifier, GOLD label, `ENGINEERED` category,
inherited `anaerobic sludge` parent, GOLD source attestation, class-level
grounding event, seed event, and stable slug lock.

The curation state is still materially incomplete. Exact
`rg --no-ignore --hidden` or `find` searches for the target identifier,
`gold.ecosystem:8142`, `granular_sludge__16dfa645`, and the exact source path
across `data/raw`, `data/habitats/engineered`, `curation`, `history`,
`research`, `reports/yaml_record_review`, and `conf` found only the raw GOLD
row, the generated YAML record, the slug lock, the class-level
`curation/decisions.tsv` row, and contextual mentions in earlier review
reports. A broader exact `Granular sludge` label search also found the
distinct UASB same-label sibling described below. The same
gitignore-independent searches found no target-specific term request, causal
overlay, history record, research report, or previous exact YAML review
report.

Optional characteristic-taxon, environmental-parameter, DOI/PMID/URL evidence,
dataset, discussion, and causal-graph slots are correctly absent for the
current raw inputs. The direct GOLD path contributes no organism, study,
biosample, triad, or environmental-parameter rows that could populate them.

## Findings

- **major**: `habitatmech:GOLD.e8d7f5a282` is still only class-swept, so the
  candidate anaerobic granular-sludge habitat has no item-level habitat
  decision, definition, or ENVO term request.

  **Evidence**: The GOLD source path names `Granular sludge` beneath an exact
  grounded `Engineered > Bioreactor > Anaerobic > Sludge` parent, and the
  vendored ontology slice has no exact generic granular-sludge term. The only
  maintained row for `habitatmech:GOLD.e8d7f5a282` is a `CLASS`-depth
  `CONFIRM_UNGROUNDED` decision whose generated curation-history event states
  that habitat status was not assessed. Exact gitignore-independent searches
  found no target-specific term request, research report, causal overlay, or
  item-level history record.

  **Owner**: `curation/decisions.tsv` should hold the item-level decision. If a
  curator confirms the concept is a real habitat with no exact existing term,
  `curation/term_requests.tsv` should hold the ENVO request metadata and
  definition.

No blockers found.

No minor findings.

## Recommended Edits

1. In `curation/decisions.tsv`, perform an item-level review for
   `habitatmech:GOLD.e8d7f5a282` rather than relying on the class-level sweep.

2. During that item-level review, confirm whether the path should be treated as
   anaerobic granular sludge, generic granular sludge nested in an anaerobic
   sludge context, or another narrower class; do not ground directly to
   `ENVO:00002212` unless thermophilic temperature is part of the confirmed
   identity.

3. If no exact external ontology term fits after that review, add a
   target-specific ENVO request in `curation/term_requests.tsv` with a
   definition and supporting literature, then regenerate the corpus from the
   maintained inputs.

## Follow-up Checks

After curation, rerun:

- `just term-requests-check`
- `just validate data/habitats/engineered/granular_sludge__16dfa645.yaml`
- `just validate-strict data/habitats/engineered/granular_sludge__16dfa645.yaml`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000 --status all --out /tmp/habitatmech-granular-sludge-anaerobic-worklist.tsv`

## Additional Notes

- Ignored files were included in the exact absence checks above via
  `rg --no-ignore --hidden` and `find`.
- The neighboring GOLD same-label UASB record is
  `data/habitats/engineered/granular_sludge.yaml` with identifier
  `habitatmech:GOLD.c79068da29` and `source_id: gold.ecosystem:8150`.
  Because its path is beneath `UASB (Upflow anaerobic sludge blanket)`, it
  should be read as a lead for future anaerobic granular-sludge curation, not
  as evidence that the two generated HabitatMech records are already
  interchangeable.
