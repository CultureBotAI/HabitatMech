# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/hydrocarbon.yaml
- Started UTC: 2026-09-25T09:36:45Z
- Finished UTC: 2026-09-25T09:36:45Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/engineered/hydrocarbon.yaml` |
| Identifier | `habitatmech:GOLD.3c68e33a11` |
| Label | `Hydrocarbon` |
| Category | `ENGINEERED` |
| Grounding | `NOT_APPLICABLE` |
| Mapping | `REVIEWED` |
| Maintained owner | `curation/decisions.tsv` row for `habitatmech:GOLD.3c68e33a11`; a future definition belongs in `curation/term_requests.tsv` |

The record is generated from GOLD path
`Engineered > Artificial ecosystem > Soil microcosm > Contaminated soil > Hydrocarbon`.
Its one source concept is `gold.ecosystem:6045`, the pinned slug is
`hydrocarbon` in `data/habitats/PATHS.tsv`, and the current item-level decision
marks the source as `NOT_APPLICABLE` against `CHEBI:24632` `hydrocarbon`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/hydrocarbon.yaml` | Passed; the generated record validates as a `HabitatRecord`. |
| `just validate-strict data/habitats/engineered/hydrocarbon.yaml` | Passed in closed-schema mode. |
| `just validate-causal-all` | Passed; there is no causal overlay attached to this record. |
| `just term-requests-check` | Passed; the generated ENVO ROBOT template is up to date with the current term requests. |
| `just validate-history` | Passed. |
| `just verify-corpus --max-diffs 1` | Passed; `data/raw/` reproduces all 3,206 generated records exactly. |
| `just worklist --limit 2000 --status all --out /tmp/habitatmech-hydrocarbon-worklist.tsv` | Passed and wrote 953 ungrounded backlog rows. The target is currently `NOT_APPLICABLE` and `REVIEWED`, so it is not a term-request backlog row. |
| `just report` | Passed; the current corpus report completed. |

No structural validator catches the problem below because the generated YAML is
faithful to the maintained curation decision.

## Identity and Grounding

The exact `gold.ecosystem:6045` raw row is the full path above with zero direct
organism assertions. The path has a GOLD environmental-triad row whose
`env_medium` is `ENVO:00002116` `contaminated soil`, with one of one samples and
one of one studies agreeing.

The current curation row instead reads the leaf label in isolation:
`NOT_APPLICABLE` to `CHEBI:24632` `hydrocarbon`, with a note that a chemical is
not a habitat. That would be right for a source concept denoting the chemical
class itself. It is not right for this path: under `Engineered > Artificial
ecosystem > Soil microcosm > Contaminated soil`, `Hydrocarbon` is a contaminant
qualifier on a soil microcosm habitat.

`CHEBI:24632` is the exact chemical xref for the contaminant and is valid in the
vendored slice, but it is not broader than the source concept and must not be
used as a habitat identity or parent. `ENVO:00002116` is broader than the target:
a hydrocarbon-contaminated soil microcosm is a kind of contaminated soil.

## Evidence

- **Supported:** the GOLD source attestation is exact. `data/raw/gold_ecosystem_paths.tsv`
  contains exactly one node id, `gold.ecosystem:6045`, for the target path.
- **Supported:** the generated `ENVO:00002116` parent is a true broader habitat
  for this source path. GOLD's own MIxS `medium` summary for the same path points
  to `ENVO:00002116` `contaminated soil` with full one-study support.
- **Supported but mis-scoped:** `CHEBI:24632` exactly identifies the contaminant
  class named by the `Hydrocarbon` leaf. It should remain, at most, a contaminant
  xref; it does not make the source concept non-habitat on this path.
- **Unsupported:** the current `NOT_APPLICABLE` decision says the source concept
  is not a habitat. The full GOLD path and triad support a contaminated soil
  microcosm reading.

## Completeness

Ignored-file-inclusive searches were run with `rg --no-ignore --hidden` over
`data/raw`, `curation`, `history`, `research`, `reports/yaml_record_review`,
`data/habitats/PATHS.tsv`, and `data/habitats`, plus `find` over the review
report directory.

- Exact searches for `habitatmech:GOLD.3c68e33a11` found only the pinned slug in
  `data/habitats/PATHS.tsv`, the single decision row in
  `curation/decisions.tsv`, and the generated target record. They found no
  term request, prior history record, research report, causal overlay, or prior
  YAML review.
- Exact searches for `gold.ecosystem:6045` found only
  `data/raw/gold_ecosystem_paths.tsv` and the target record.
- Exact searches for the full GOLD source path found the raw row, the GOLD
  triad row, and the target record.
- Exact searches for `CHEBI:24632` found the vendored CHEBI term, three GOLD
  `Hydrocarbon` decisions, Madin hydrocarbon source rows, and the target's
  current xref. The aquatic deep-subsurface `Hydrocarbon` row also has a GOLD
  triad pointing to `CHEBI:24632`; this target's triad points to
  `ENVO:00002116`, so the rows should not be bulk-changed by label alone.
- Exact searches for `ENVO:00002116` found the vendored `contaminated soil`
  term, its subclass edges, the current exact `contaminated soil` decisions,
  PREGO rows for contaminated soil, prior contaminated-soil review notes, and
  this target's triad and parent.

The optional `definition`, `evidence`, `environmental_parameters`,
`characteristic_taxa`, `causal_graphs`, `discussions`, and `datasets` fields
are currently empty. Empty optional slots are not themselves invalid, but a
minted habitat for this row will need a definition.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Major | `habitatmech:GOLD.3c68e33a11` is incorrectly signed off as `NOT_APPLICABLE`. GOLD's full path and matching MIxS medium make it a hydrocarbon-contaminated soil microcosm, not pure hydrocarbon as a sampled chemical. The generated record is consequently marked `REVIEWED` under a decision whose reasoning contradicts the path-local evidence. | `curation/decisions.tsv` row for `habitatmech:GOLD.3c68e33a11`; a new definition in `curation/term_requests.tsv` |

No blockers found: the YAML parses, validates, and reproduces from maintained
inputs. No minor findings found.

## Recommended Edits

1. In `curation/decisions.tsv`, replace the
   `habitatmech:GOLD.3c68e33a11` `NOT_APPLICABLE` decision with an item-level
   habitat decision that keeps the minted identifier and records
   `ENVO:00002116` `contaminated soil` as a broader parent.

2. Add a definition for this minted source concept in
   `curation/term_requests.tsv`, for example under a label such as
   `hydrocarbon-contaminated soil microcosm`. Keep both axes explicit:
   the record is narrower than `ENVO:00002116` contaminated soil and narrower
   than a microcosm.

3. Do not promote `CHEBI:24632` to `parent_habitats`. If the current one-object
   decision surface cannot retain both the `ENVO:00002116` parent and the
   chemical xref, prefer the habitat parent and document that the
   hydrocarbon-contaminant xref is being dropped because the curation row has no
   second relation slot.

4. Record an append-only history entry for the curation session that changes
   the decision or term request. Do not edit the generated
   `data/habitats/engineered/hydrocarbon.yaml` directly.

## Follow-up Checks

- `just seed`
- `just seed-canary habitatmech:GOLD.3c68e33a11`
- `just seed-apply --force`
- `just validate data/habitats/engineered/hydrocarbon.yaml`
- `just validate-strict data/habitats/engineered/hydrocarbon.yaml`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus`

After reseeding, inspect the generated record and confirm that it is `NARROW`,
keeps `ENVO:00002116` in `parent_habitats`, has no CHEBI parent, and has a
history event for the item-level decision and definition.

## Additional Notes

- The sibling
  `Engineered > Bioremediation > Hydrocarbon > Methane` and
  `Engineered > Bioremediation > Hydrocarbon > Xylene` records already document
  the same modeling pattern: GOLD contaminant leaves under an engineered
  remediation path denote the remediated matrix, not a pure chemical.
- The broader `Engineered > Bioremediation > Hydrocarbon` source and the
  `Environmental > Aquatic > Marine > Deep subsurface > Hydrocarbon` source use
  the same leaf label but need their own item-level path reviews. This report
  only reviewed `gold.ecosystem:6045`.
