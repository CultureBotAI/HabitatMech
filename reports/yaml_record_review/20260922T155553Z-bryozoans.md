# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/bryozoans.yaml`
- Started UTC: 2026-09-22T15:55:53Z
- Finished UTC: 2026-09-22T15:55:53Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.d2ef97a849` |
| Label | `Bryozoans` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Maintained owner | `curation/decisions.tsv:1165` records only a `CLASS`-depth ungrounded sweep for this GOLD source concept |
| Locked slug | `data/habitats/PATHS.tsv:2847` maps `habitatmech:GOLD.d2ef97a849` to `bryozoans` |

This is the seeded GOLD record for `Host-associated > Invertebrates > Bryozoans`. The generated YAML faithfully preserves the current inputs: it keeps the source path minted, inherits the reviewed `invertebrate-associated environment` record from the GOLD parent path, records the first of three GOLD node ids plus two organism assertions, and stays `SEEDED` because the only decision row is a class-level lexical sweep. The record still needs item-level curation because the bare host-phylum label `Bryozoans` is not itself the habitat identity, and GOLD separately has a top-level `Host-associated > Bryozoa` branch that currently mints a duplicate unreviewed bryozoan host-clade record.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/bryozoans.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/bryozoans.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The generated identifier, label, category, grounding status, parent, source attestation, and seed status are supported by maintained inputs:

- `data/raw/gold_ecosystem_paths.tsv:842` is the raw GOLD row for `Host-associated > Invertebrates > Bryozoans`. It has leaf label `Bryozoans`, depth 3, three GOLD node ids, two organism assertions, zero study assertions, zero biosample assertions, and total assertion count 2.
- `curation/decisions.tsv:1165` is a `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.d2ef97a849`, but its `review_depth` is `CLASS` and its note says whether the concept is a habitat was not assessed.
- `data/habitats/PATHS.tsv:2847` pins the generated target to `data/habitats/host_associated/bryozoans.yaml`.
- `curation/decisions.tsv:497`, `curation/term_requests.tsv:9`, and `data/habitats/host_associated/invertebrates.yaml` show that the source parent `Host-associated > Invertebrates` has already been item-reviewed and defined as `invertebrate-associated environment`.
- `data/raw/gold_ecosystem_paths.tsv:1974`, `curation/decisions.tsv:551`, `data/habitats/PATHS.tsv:1937`, and `data/habitats/host_associated/gymnolaemates.yaml` show that the generated `Gymnolaemates` child still inherits this record as its host-clade parent. That child has supplemental one-biosample and one-study rows in `data/raw/gold_path_biosamples.tsv:1011` and `data/raw/gold_studies.tsv:623`.
- `data/raw/gold_ecosystem_paths.tsv:1906`, `data/habitats/PATHS.tsv:2949`, and `reports/yaml_record_review/20260922T153519Z-bryozoa.md` show that `Host-associated > Bryozoa` is a separate seeded source concept for the same host phylum; that sibling was reviewed separately and needs the same item-level curation pass.

The current `UNGROUNDED` status is a reproducible lexical-sweep result, not a reviewed habitat decision. The source concept should likely remain minted because a whole bryozoan taxon is not the habitat identity, but the minted record should be a bryozoan-associated environment and the plural `Host-associated > Invertebrates > Bryozoans` branch should not be split from `Host-associated > Bryozoa` if both source paths assert the same host scope.

## Evidence

Every generated claim is traceable to a raw row or maintained curation input:

| Claim | Nearest source | Review |
|---|---|---|
| Minted identifier `habitatmech:GOLD.d2ef97a849` | GOLD path minting from `Host-associated > Invertebrates > Bryozoans` in `src/habitatmech/seed.py` | Supported exactly |
| Label `Bryozoans`, category `HOST_ASSOCIATED`, source label, source path, three-node note, two organism assertions, and `ORGANISM` assertion unit | `data/raw/gold_ecosystem_paths.tsv:842` | Supported exactly |
| `source_id: gold.ecosystem:3563` | first node id in `data/raw/gold_ecosystem_paths.tsv:842` | Supported exactly; `gold.ecosystem:3563\|gold.ecosystem:4798\|gold.ecosystem:4799` share the path |
| Parent `habitatmech:GOLD.4d792ac724` | GOLD parent-path emission plus the item-level review and definition for `Host-associated > Invertebrates` | Supported exactly |
| `grounding_status: UNGROUNDED` | `curation/decisions.tsv:1165` | Supported exactly |
| `mapping_status: SEEDED` | `curation/decisions.tsv:1165` has `review_depth` `CLASS`; only `ITEM` decisions count as reviewed | Supported exactly |
| Absence of a definition, synonym, xref, environmental parameter, characteristic taxon, causal graph, record-level evidence, discussion, and dataset | No maintained input row or overlay found for these slots | Supported by bounded exact searches |

The one GOLD biosample and study side-table rows under `Host-associated > Invertebrates > Bryozoans > Gymnolaemates` are correctly absent from this parent record. They belong to the generated `Gymnolaemates` descendant unless curation later decides to merge or retire that leaf.

## Completeness

The generated fields are complete for the maintained inputs that currently feed this record:

- The single GOLD source attestation captures the path, the first GOLD node id, assertion count, assertion unit, and multi-node note.
- The source-inherited `habitatmech:GOLD.4d792ac724` parent is present.
- The generated corpus keeps this node in use as the parent of the seeded `Host-associated > Invertebrates > Bryozoans > Gymnolaemates` record.
- No item-level decision, authored term request, target-specific causal graph overlay, append-only history record, dedicated deep-research report, GOLD side-table row on the exact `Host-associated > Invertebrates > Bryozoans` path, term-request exclusion row, or previous exact YAML review report exists for `habitatmech:GOLD.d2ef97a849`.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, `data/habitats/host_associated`, and the generated target, excluding generated `build`, `data/text_map`, and `pages` trees. They found the raw GOLD Bryozoans row, the Gymnolaemates child row, Gymnolaemates GOLD biosample and study side rows, the duplicate top-level `Bryozoa` source concept, the class-level decision rows, the reviewed Invertebrates parent, the reciprocal Bryozoa YAML review, and path locks; they found no Bryozoans-specific item decision, term request, term-request exclusion, causal overlay, history record, dedicated deep-research report, GOLD biosample or study side-table row on `Host-associated > Invertebrates > Bryozoans` itself, BacDive or PREGO row, or prior exact YAML review report.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Major | `habitatmech:GOLD.d2ef97a849` remains a `SEEDED` host-associated GOLD record because its only decision row is a class-level ungrounded sweep that did not assess habitat validity. The current record therefore publishes the bare host phylum label `Bryozoans` with no definition even though the branch has two organism assertions and an asserted `Gymnolaemates` child; the semantically equivalent top-level `Host-associated > Bryozoa` branch remains a separate seeded record even though both source concepts denote bryozoans as hosts. | Add `ITEM`-depth rows in `curation/decisions.tsv` for `habitatmech:GOLD.d2ef97a849` and `habitatmech:GOLD.dfaedfff09`; if retained, add one associated-environment definition in `curation/term_requests.tsv`, then regenerate `bryozoans`, `bryozoa`, and the affected children. |

## Recommended Edits

- Add an item-level `curation/decisions.tsv` row for `habitatmech:GOLD.d2ef97a849` that explicitly reviews `Host-associated > Invertebrates > Bryozoans`.
- Review `habitatmech:GOLD.dfaedfff09` for `Host-associated > Bryozoa` in the same curation pass and merge it with `habitatmech:GOLD.d2ef97a849` if both paths have the same bryozoan host-clade scope.
- If the source concepts should publish, keep the surviving identity minted and define `bryozoan-associated environment` in `curation/term_requests.tsv` with parent `ENVO:01001002` `animal-associated environment`; keep any bryozoan taxon term only as a related xref.
- After any merge or rename, confirm that `Host-associated > Invertebrates > Bryozoans > Gymnolaemates` still has the surviving bryozoan-associated environment as a strictly broader parent.
- Regenerate the target with `just seed` and preview `habitatmech:GOLD.d2ef97a849` with `just seed-canary habitatmech:GOLD.d2ef97a849` before applying the full generated corpus.

## Follow-up Checks

After item-level Bryozoans curation, re-run the same focused validation set and confirm the `Host-associated > Invertebrates > Bryozoans` and `Host-associated > Bryozoa` attestations land on one reviewed generated identity or on deliberately distinct reviewed records. If `bryozoans.yaml` is retired into `bryozoa.yaml`, validate the surviving generated record in place of the deleted target:

- `just validate data/habitats/host_associated/bryozoans.yaml`
- `just validate-strict data/habitats/host_associated/bryozoans.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

Also verify that the `Gymnolaemates`, `Reproductive system`, and `Ovicells` child records keep strictly broader parents and that their GOLD biosample and study rows remain attached to the surviving child records.

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -name '*bryozoans*' -print` found no pre-existing exact review report for this record before this file was written.
- Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.d2ef97a849`, `habitatmech:GOLD.587378d673`, `gold.ecosystem:3563`, `gold.ecosystem:3642`, and `Host-associated > Invertebrates > Bryozoans`, plus a case-insensitive hidden-inclusive search for `bryozo`, found no target-specific maintained artifact beyond the raw, curation, path-lock, generated, broader invertebrate, and reciprocal Bryozoa report rows cited above.
