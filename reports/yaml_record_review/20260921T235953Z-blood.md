# YAML Record Review: blood

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/blood.yaml`
- Started UTC: 2026-09-21T23:50:00Z
- Finished UTC: 2026-09-21T23:59:53Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `BTO:0000089` |
| Label | `blood` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Maintained inputs | `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, `data/raw/prego_habitats.tsv`, `data/raw/prego_habitat_taxa.tsv`, `data/habitats/PATHS.tsv` |

This is a generated BTO-grounded record produced from one PREGO source concept. The path lock maps `BTO:0000089` to the exact `blood` slug, the vendored BTO slice supplies the record label and definition, and PREGO directly asserts the same `BTO:0000089` source id.

The target was resolved by exact slug and CURIE. The corpus also has related and same-label siblings such as `blood__*.yaml`, `whole_blood*.yaml`, `blood_plasma*.yaml`, `blood_serum.yaml`, `arterial_blood.yaml`, and `venous_blood.yaml`; none of those was used for this review.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/blood.yaml` | Pass; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/host_associated/blood.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Pass; the committed term-request table is current with 109 terms. |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass; 3,206 expected records, 3,206 records on disk, and 0 missing, extra, or differing records. |
| `git diff --check` | Pass. |

`just validate-products` was not run. This read-only review did not add or change a grounding row, and the BTO identifier on the record was checked directly against the vendored ontology row in `data/raw/ontology_terms.tsv`.

## Identity and Grounding

The generated identity and exact grounding are supported:

- `data/habitats/PATHS.tsv` pins `BTO:0000089` to `blood`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv` labels `BTO:0000089` as `blood`, marks the term active, and carries the definition copied into the record.
- PREGO directly asserts `BTO:0000089` with `prego_synonyms` `blood|bloods|whole blood|whole bloods`, so `ingest_prego()` resolves the source concept to the same ontology CURIE with `grounding_status: EXACT`.
- The stable PREGO decision key is `habitatmech:PREGO.1f045aab09`, computed by the seeder as `sha1("PREGO:BTO:0000089")[:10]`.
- Ignored-file-inclusive searches found no `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/causal_graphs/`, `history/`, or prior `reports/yaml_record_review/` entry for `BTO:0000089`, `habitatmech:PREGO.1f045aab09`, or this record path, so the generated `mapping_status: SEEDED` is expected.
- BTO identifiers fall through `PREFIX_CATEGORY` to `HOST_ASSOCIATED`, matching the record's `habitat_category`.

`data/raw/ontology_subclass_edges.tsv` has downstream BTO children for `BTO:0000089`, including arterial and venous blood, but no exact `BTO:0000089 rdfs:subClassOf ...` parent edge. The empty `parent_habitats` slot therefore matches the vendored ontology slice.

## Evidence

Supported claims:

- `definition` and `definition_source: BTO` come verbatim from the vendored BTO row for `BTO:0000089`.
- The three generated synonyms are the pipe-delimited PREGO lexical variants after omitting `blood`, which duplicates the record label.
- The PREGO attestation's `source_id`, `source_label`, `assertion_count: 206`, `assertion_unit: TAXON`, `score: 4.0`, and `evidence_channels: annotated_genomes_isolates|environmental_samples` come from `data/raw/prego_habitats.tsv`; the record intentionally uses PREGO's distinct `taxon_count` rather than the larger `direct_assertion_count`.
- The 25 `characteristic_taxa` entries are copied from `data/raw/prego_habitat_taxa.tsv` in PREGO rank order, with `candidate_pool: 206` copied from the source-level PREGO `taxon_count`.
- The missing `taxon_label` values for `NCBITaxon:1122931` and `NCBITaxon:1707330` match blank label cells in `data/raw/prego_habitat_taxa.tsv`, not YAML truncation.
- None of the PREGO taxon associations is promoted with `is_characteristic: true`, so the record stays within the source-observed association claim that the schema assigns to seeded entries.

Unsupported or over-scoped claims: None found.

The record has no record-level evidence, environmental parameters, causal graphs, discussion links, or datasets. That absence is supported for this record: it is sourced only from PREGO, no maintained causal overlay names `BTO:0000089`, and ignored-file-inclusive searches found no exact `BTO:0000089`, `habitatmech:PREGO.1f045aab09`, or `data/habitats/host_associated/blood.yaml` entry under the curation, history, or prior review paths checked before this report was written.

## Completeness

The record is complete enough for its current generated scope:

- No authored definition or term request is expected because the record uses an existing exact BTO `blood` term.
- No source-path hierarchy, GOLD environmental triads, or environmental parameters are expected because the only contributing source is PREGO.
- No parent habitat is expected because the vendored BTO slice has no superclass row for `BTO:0000089`.
- No item-level curation decision exists yet; with one PREGO-only source concept, `mapping_status: SEEDED` is therefore correct.
- No HabitatMech-authored causal graph is expected because no maintained `curation/causal_graphs/` overlay names this habitat.
- No stronger characteristic-taxon evidence is expected because the PREGO taxa are presented only as reported associations from PREGO, not as curator-asserted typifying taxa.

All negative checks above used `rg --no-ignore --hidden` or `find`, so ignored files were included in the searched paths.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

If a future curation change touches this record, rerun:

- `just validate data/habitats/host_associated/blood.yaml`
- `just validate-strict data/habitats/host_associated/blood.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

## Additional Notes

An ignored-file-inclusive `find reports/yaml_record_review -name '*blood*' -print` found no prior report for this `blood` record before this file was written.
