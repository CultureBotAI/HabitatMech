# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/water_tank.yaml
- Started UTC: 2026-09-24T10:23:58Z
- Finished UTC: 2026-09-24T10:23:58Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `HabitatRecord` |
| Identifier | `ENVO:03501254` |
| Label | `water tank` |
| Category | `AQUATIC` |
| Source concept | `habitatmech:GOLD.b5663cad0a` |
| Source path | `Environmental > Aquatic > Freshwater > Water tank` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv`; future curation belongs in `curation/decisions.tsv` or a maintained parent-override input, not in `data/habitats/aquatic/water_tank.yaml`. |

The generated record grounds GOLD's `Water tank` leaf to ENVO's `water tank` term. The GOLD canonical path row collapses two upstream node IDs, `gold.ecosystem:5209` and `gold.ecosystem:5210`, and contributes 36 GOLD organism assertions to the record.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/habitats/aquatic/water_tank.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/water_tank.yaml` | Pass: scanned 1 file with 0 files containing `ERROR` and 0 total `ERROR` rows. |
| `just validate-causal curation/causal_graphs/<overlay>.yaml` | Not applicable: a gitignore-independent `find curation/causal_graphs history research -type f -iname '*water*tank*' -print` search found no curated causal overlay for `water_tank`, and the target record has no `causal_graphs` block. |
| `just validate-causal-all` | Pass: validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Pass: the term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: expected 3,206 records, found 3,206 on disk, and the corpus reproduces exactly from `data/raw/`. |
| `just worklist --limit 2000` | Pass: reported 0 undecided ungrounded records and 1,810 decisions on file. |
| `just report` | Pass: completed the corpus report for 3,206 records. |
| `git diff --check` | Pass. |
| Reference validator | Not applicable: this record has no record-level `evidence`, `characteristic_taxa`, `causal_graphs`, `discussions`, or `datasets` with citation-bearing `EvidenceItem` references. |

## Identity and Grounding

`ENVO:03501254` is present in `data/raw/ontology_terms.tsv` as `water tank` with definition `A container for storing water.` The same table carries no exact synonyms for the term.

The GOLD source path `Environmental > Aquatic > Freshwater > Water tank` is source concept `habitatmech:GOLD.b5663cad0a`. Its leaf label resolves to the ENVO `water tank` label by exact leaf match, so `grounding_status: EXACT` and `mapping_predicate: skos:exactMatch` follow the seeder's lexical rule. No item-level row for `habitatmech:GOLD.b5663cad0a` exists in `curation/decisions.tsv`, so `mapping_status: SEEDED` is expected.

The ontology-derived parent `ENVO:00003074` is supported: `data/raw/ontology_subclass_edges.tsv` asserts `ENVO:03501254 rdfs:subClassOf ENVO:00003074`, and `ENVO:00003074` is `manufactured product`. The source-derived `ENVO:00002011` `fresh water` parent is not a true `is-a` parent; see the major finding below.

## Evidence

The ENVO label and definition trace to `data/raw/ontology_terms.tsv`, and the manufactured-product parent traces to `data/raw/ontology_subclass_edges.tsv`.

The GOLD attestation traces to `data/raw/gold_ecosystem_paths.tsv`, where `Environmental > Aquatic > Freshwater > Water tank` has `gold_node_count=2`, `organism_count=36`, `total_assertions=36`, and `gold_node_ids=gold.ecosystem:5209|gold.ecosystem:5210`. The first node ID and "2 GOLD ecosystem node ids share this path" note in the generated record agree with that maintained row.

GOLD has a richer biosample side channel for one of the collapsed nodes: `data/raw/gold_path_biosamples.tsv` reports 54 biosamples for ecosystem path ID `5210`, and `data/raw/gold_path_triads.tsv` reports freshwater-lake triad terms for 54 samples in two studies. Those triads are contextual annotations on submitted GOLD biosamples; they do not make the container itself a lake, lake water, or fresh water.

## Completeness

- A gitignore-independent exact search for `ENVO:03501254`, `Water tank`, `water_tank`, `gold.ecosystem:5209`, and `gold.ecosystem:5210` across `data/habitats/PATHS.tsv`, `data/raw`, and `curation` found the expected path-lockfile row, GOLD source rows, GOLD study and biosample rows, and ontology rows. It found no maintained curation decision for the water-tank source concept.
- A gitignore-independent exact search for `habitatmech:GOLD.b5663cad0a`, `ENVO:03501254`, and `water_tank` across `curation`, `history`, `research`, and non-Markdown YAML-review metadata found no maintained decision row, no curated causal overlay, no session history file, and no paid research report for this record.
- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-water_tank.md' -print` found no prior top-level review report for the same record stem; `find` includes ignored files.
- Optional `environmental_parameters`, `characteristic_taxa`, `evidence`, `causal_graphs`, `discussions`, and `datasets` are correctly absent. The generated record contains only source-vocabulary provenance, and no maintained overlay adds claim-level mechanism evidence.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | `parent_habitats` contains `ENVO:00002011` `fresh water`, but a water tank is a manufactured container, not a kind of fresh water. | ENVO directly asserts only `ENVO:03501254 water tank rdfs:subClassOf ENVO:00003074 manufactured product`. The extra `fresh water` parent comes from GOLD's filing of `Water tank` under `Environmental > Aquatic > Freshwater`, and the seeder promotes the GOLD parent-path record into `parent_habitats`. That turns a source context or contents branch into a false subclass edge. | The fix needs a curator-maintained way to suppress or replace false source-path parents for ontology-grounded GOLD records. The current generated edge comes from `src/habitatmech/seed.py` applying GOLD parent paths; do not hand-edit `data/habitats/aquatic/water_tank.yaml`. |

No blocker findings found.

No minor findings found.

## Recommended Edits

1. Add a maintained parent-suppression or parent-override input for ontology-grounded GOLD records and use it to drop `ENVO:00002011` from `ENVO:03501254`. The row should name `habitatmech:GOLD.b5663cad0a`, the rejected parent `ENVO:00002011`, and the fact that `Freshwater > Water tank` is source placement or contents context rather than a broader habitat class.
2. Update `src/habitatmech/seed.py` to apply that curated override before serializing `parent_habitats`.
3. Add a regression test that preserves the ENVO-manufactured-product parent on `water tank` while rejecting the source-derived `fresh water` parent.

## Follow-up Checks

| Edit | Proof |
| --- | --- |
| Drop only the false `fresh water` parent | `just seed-canary ENVO:03501254`, followed by an inspection of `data/habitats/aquatic/water_tank.yaml` showing `ENVO:00003074` in `parent_habitats` and no `ENVO:00002011`. |
| Preserve corpus reproduction | `just seed-apply --force` and `just verify-corpus --max-diffs 1`. |
| Validate the focused record and new curated override | `just validate data/habitats/aquatic/water_tank.yaml`, `just validate-strict data/habitats/aquatic/water_tank.yaml`, and the new parent-override validator or regression test added with the maintained input. |
| Confirm no unrelated curation loss | `just term-requests-check`, `just validate-history`, `just worklist --limit 2000`, and `git diff --check`. |

## Additional Notes

- GOLD study `Gs0114675` spans six ecosystem paths, including the sibling `Environmental > Aquatic > Freshwater > Lake` path, while `Gs0164319` is specific to `Environmental > Aquatic > Freshwater > Water tank`. The study multiplicity does not change the parent finding because the generated `fresh water` edge is a type assertion on the tank record, not a statement about study context.
- The GOLD MIxS triad rows for this path are all freshwater-lake terms supported by only one agreeing study among two studies. They are relevant to a future item-level review, but they do not support replacing the `water tank` identity with a lake or lake-water identity.
