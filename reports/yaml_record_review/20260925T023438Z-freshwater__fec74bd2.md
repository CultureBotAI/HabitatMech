# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/engineered/freshwater__fec74bd2.yaml`
- Started UTC: `2026-09-25T02:34:38Z`
- Finished UTC: `2026-09-25T02:35:29Z`
- Verdict: needs curation

## Target

Reviewed the complete generated record at `data/habitats/engineered/freshwater__fec74bd2.yaml`. The record is a GOLD-only engineered habitat for `Engineered > Artificial ecosystem > Mesocosm > Freshwater`, minted as `habitatmech:GOLD.faf9f777e1`.

The generated file has `grounding_status: NARROW` and `mapping_status: SEEDED`. Its parents are `ENVO:00002011` `fresh water`, from the ambiguous-leaf match on `Freshwater`, and `ENVO:01000620` `mesocosm`, from the exact-grounded GOLD source-path parent. Its only source attestation shows `gold.ecosystem:7931`, and the note correctly points out that two GOLD ecosystem node IDs share this canonical path.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/freshwater__fec74bd2.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/freshwater__fec74bd2.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Focused causal overlay validator | Not applicable; exact ignored-inclusive searches in `curation/causal_graphs/` found no `habitatmech:GOLD.faf9f777e1`, `freshwater__fec74bd2`, `gold.ecosystem:7931`, `gold.ecosystem:7932`, or exact Mesocosm Freshwater source-path overlay. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| Reference validator | Not applicable; the target record has no record-level `evidence` entries and no causal graph edges with citation references. |
| `just term-requests-check` | Passed; the generated ENVO term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records all exist on disk, with 0 missing, 0 extra, and 0 differing records. |
| `just worklist --limit 2000` | Passed; it reported 0 still-undecided ungrounded records and 1,810 decisions on file. |
| `just report` | Passed; the corpus report completed for 3,206 records. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

The generated identifier, label, category, parents, and source attestation agree with the committed GOLD inventory. `data/raw/gold_ecosystem_paths.tsv` has the row `Engineered > Artificial ecosystem > Mesocosm > Freshwater` at depth 4 with `gold_node_count` 2, no GOLD organism, study, or biosample assertions, and node IDs `gold.ecosystem:7931|gold.ecosystem:7932`. `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.faf9f777e1` to the stable `freshwater__fec74bd2` slug.

The `NARROW` grounding is mechanically explainable but not yet item-reviewed. The seeder keeps this path distinct from generic `ENVO:00002011` `fresh water` because the source path qualifies the leaf as engineered mesocosm freshwater, then attaches the broader water material and the immediate `ENVO:01000620` `mesocosm` source-path parent. Exact ignored-inclusive searches found no `curation/decisions.tsv` row for `habitatmech:GOLD.faf9f777e1`.

This lack of item review is visible against the sibling `Engineered > Artificial ecosystem > Mesocosm > Seawater` path. That source concept, `habitatmech:GOLD.0cadaca0bf`, has an `ITEM` `GROUND_AS_PARENT` decision saying mesocosm seawater is experimental and held in an artificial ecosystem, so merging it with in-situ `ENVO:00002149` `sea water` would conflate the concepts. The freshwater sibling has the same path-qualified water leaf pattern but no analogous decision accepting `ENVO:00002011` as a broader material parent or deciding whether a novel freshwater-mesocosm term is needed.

## Evidence

- Supported: the `GOLD` source attestation is traceable to `data/raw/gold_ecosystem_paths.tsv`, including the two collapsed GOLD node IDs and the absence of an assertion count in the generated YAML because the canonical path carries zero organism, study, biosample, and total assertions.
- Supported as generated context: the `ENVO:01000620` `mesocosm` parent comes from the immediate GOLD parent path `Engineered > Artificial ecosystem > Mesocosm`; that parent path currently resolves to `data/habitats/engineered/mesocosm.yaml`.
- Supported as ambiguous-leaf context: the `ENVO:00002011` `fresh water` parent is the material term in the vendored ontology slice for low-solute water. The generated record correctly avoids exact grounding to it.
- Under-reviewed: the record has no item-level decision, authored definition, term request, record-level evidence, discussion, dataset, characteristic taxon, environmental parameter, or causal graph.

## Completeness

The zero source count is faithful to the GOLD source inventory, so the absence of `assertion_count`, `assertion_unit`, characteristic taxa, and environmental parameters is not a generated-data omission for this path. An exact ignored-inclusive search across the focused GOLD side tables for `gold.ecosystem:7931`, `gold.ecosystem:7932`, and the exact source path found only the parent Mesocosm Freshwater row in `gold_ecosystem_paths.tsv`; it found no direct `gold_path_triads.tsv`, `gold_path_biosamples.tsv`, or `gold_studies.tsv` row to audit.

Exact ignored-inclusive searches in `history/`, `research/`, `curation/causal_graphs/`, `curation/term_requests.tsv`, `curation/term_requests/`, and `conf/id_label_targets.yaml` found no target-specific history entry, research report, causal overlay, minted definition, generated term-request row, or label-correspondence residual for `freshwater__fec74bd2`, `habitatmech:GOLD.faf9f777e1`, `gold.ecosystem:7931`, `gold.ecosystem:7932`, or the exact source path.

`find reports/yaml_record_review -maxdepth 1 -type f -name '*-freshwater__fec74bd2.md' -print` found no existing `freshwater__fec74bd2` review before this report was written. `find` is not `.gitignore`-filtered, so ignored files were included in that absence check.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| Major | `Engineered > Artificial ecosystem > Mesocosm > Freshwater` remains raw seeded ambiguous-leaf output while its directly analogous Seawater sibling has already needed an item-level broader-term decision. | `curation/decisions.tsv` has no row for `habitatmech:GOLD.faf9f777e1`. The generated record asserts the path-qualified concept is narrower than both `ENVO:00002011` `fresh water` and `ENVO:01000620` `mesocosm`, but the sibling `habitatmech:GOLD.0cadaca0bf` Mesocosm Seawater record required an `ITEM` `GROUND_AS_PARENT` decision to explicitly keep generic `ENVO:00002149` `sea water` as a broader parent rather than the exact identity. | `curation/decisions.tsv`; if no exact term fits, `curation/term_requests.tsv` |

## Recommended Edits

1. Item-review `habitatmech:GOLD.faf9f777e1` in `curation/decisions.tsv` against the exact `Engineered > Artificial ecosystem > Mesocosm > Freshwater` source path, `ENVO:00002011` `fresh water`, `ENVO:01000620` `mesocosm`, and the existing item decision for the sibling Mesocosm Seawater path.
2. If `Freshwater` under the Mesocosm path denotes a real path-qualified habitat with no exact ontology term, keep the minted identity and add the appropriate `GROUND_AS_PARENT` or `CONFIRM_UNGROUNDED` item-level decision.
3. If no exact ontology term fits and the source concept needs a clearer editor-facing label such as `freshwater mesocosm`, add a `curation/term_requests.tsv` row with a true genus and the correct `ADD` versus `REPLACE` parent mode.

## Follow-up Checks

- Run `just seed`, then `just seed-canary habitatmech:GOLD.faf9f777e1`, and inspect `data/habitats/engineered/freshwater__fec74bd2.yaml` before any wider `just seed-apply --force`.
- Run `just validate data/habitats/engineered/freshwater__fec74bd2.yaml`, `just validate-strict data/habitats/engineered/freshwater__fec74bd2.yaml`, `just term-requests-check`, `just validate-history`, `just verify-corpus --max-diffs 1`, `just worklist --limit 2000`, `just report`, and `git diff --check` after the maintained input changes.

## Additional Notes

The review did not find a target causal overlay, so there was no focused `just validate-causal curation/causal_graphs/<slug>.yaml` invocation to run. The whole-overlay validator still passed.

The exact Mesocosm Freshwater path has no direct rows in the GOLD biosample, triad, or study side tables committed under `data/raw/`. The Mesocosm Seawater sibling does have later API-side biosample and study rows, so future curation should compare siblings by source-path meaning rather than assuming they have the same side-table support.
