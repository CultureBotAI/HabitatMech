# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/soil__cbcc9126.yaml
- Started UTC: 2026-09-25T03:57:19Z
- Finished UTC: 2026-09-25T03:57:19Z
- Verdict: pass

## Target

Reviewed the complete generated record at `data/habitats/engineered/soil__cbcc9126.yaml`. The record is a GOLD-only engineered habitat for `Engineered > Artificial ecosystem > Mesocosm > Soil`, minted as `habitatmech:GOLD.3e86b794ef`.

The generated file has `label: Soil`, `habitat_category: ENGINEERED`, `grounding_status: NARROW`, and `mapping_status: SEEDED`. Its parents are `ENVO:00001998` `soil`, attached by the seeded ambiguous-GOLD-leaf rule because this concept is narrower than generic soil, and `ENVO:01000620` `mesocosm`, inherited from the immediate GOLD source-path parent. Its only source attestation shows `gold.ecosystem:7694` and correctly notes that two GOLD ecosystem node IDs share this canonical path.

The file is generated from `data/raw/`; no item-level decision, authored term request, source extractor override, or causal-graph overlay currently contributes to this record. Future grounding changes belong in `curation/decisions.tsv`; future authored definition or parent-mode changes would belong in `curation/term_requests.tsv`; future causal overlays would belong under `curation/causal_graphs/`.

## Validation

All required local checks passed:

- `just validate data/habitats/engineered/soil__cbcc9126.yaml`
- `just validate-strict data/habitats/engineered/soil__cbcc9126.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

No validator was skipped.

## Identity and Grounding

The generated identifier, label, category, source attestation, and path slug agree with the maintained inputs. `data/raw/gold_ecosystem_paths.tsv` has the exact `Engineered > Artificial ecosystem > Mesocosm > Soil` row at depth 4, with two GOLD node IDs, zero `organism_count`, `study_count`, `biosample_count`, and `total_assertions` values in this bulk-export summary, and `gold.ecosystem:7694|gold.ecosystem:7695` as the node list. `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.3e86b794ef` to the stable `soil__cbcc9126` slug.

The `NARROW` grounding and `SEEDED` mapping follow from the current GOLD path and seeder rules. There is no target row in `curation/decisions.tsv`; the seeder mints `habitatmech:GOLD.3e86b794ef`, attaches `ENVO:00001998` as a broader parent, and emits `skos:narrowMatch` because the exact GOLD leaf label `Soil` is ambiguous while the mesocosm source path is narrower than plain soil.

The broader ENVO terms are present in the vendored ontology slice. `data/raw/ontology_terms.tsv` defines `ENVO:00001998` as the environmental material soil, and defines `ENVO:01000620` as a mesocosm. The generated record keeps the GOLD concept minted and uses both ontology terms only as parents, which matches the ambiguous-leaf rule and GOLD source hierarchy.

## Evidence

- Supported as generated source provenance: exact ignored-inclusive searches found `gold.ecosystem:7694`, `gold.ecosystem:7695`, and `Engineered > Artificial ecosystem > Mesocosm > Soil` in `data/raw/gold_ecosystem_paths.tsv`; `gold.ecosystem:7694` is the first node shown in the generated source attestation.
- Supported as generated source hierarchy: `ENVO:01000620` `mesocosm` is the identifier for the immediate GOLD parent path, `Engineered > Artificial ecosystem > Mesocosm`.
- Supported as a broader habitat parent: `ENVO:00001998` `soil` is a vendored ontology term whose label exactly matches the GOLD leaf; the seeder keeps this deeper GOLD path minted and attaches `soil` as a parent rather than letting multiple distinct GOLD `Soil` paths claim that generic identity.
- Supported as generated status: no contributing source concept has an item-level decision, so `mapping_status: SEEDED` follows from the current decision table.

The record has no causal graphs, environmental parameters, characteristic taxa, datasets, discussions, or claim-level `EvidenceItem` objects to inspect.

## Completeness

The generated attestation is complete for the committed GOLD ecosystem inventory. `data/raw/gold_ecosystem_paths.tsv` lists both source node IDs, and the generated record's first-node note accounts for the second node ID.

The GOLD API side tables add context but no generated-data gap for this path. Exact ignored-inclusive searches found three `data/raw/gold_studies.tsv` rows for this path, one `data/raw/gold_path_biosamples.tsv` row with 148 biosamples for node `7695`, and three one-study `data/raw/gold_path_triads.tsv` rows: broad scale `ENVO:00000446` `terrestrial biome`, local scale `ENVO:00000083` `hill`, and medium `ENVO:00001998` `soil`. These MIxS triads are ranked leads for future curation and are not automatically promoted to identity, parents, or environmental parameters.

Exact ignored-inclusive searches over `history/`, `research/`, `curation/causal_graphs/`, `curation/term_requests.tsv`, `curation/term_requests/`, `conf/id_label_targets.yaml`, and `reports/yaml_record_review/` found no target-specific append-only history record, research report, causal overlay, authored term request, generated ENVO term-request row, label-correspondence residual, or prior review for `habitatmech:GOLD.3e86b794ef`, `gold.ecosystem:7694`, `gold.ecosystem:7695`, or `Engineered > Artificial ecosystem > Mesocosm > Soil`. Before this report was written, `find reports/yaml_record_review -maxdepth 1 -type f -name '*-soil__cbcc9126.md' -print` found no prior exact review report.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

None required. If future curation changes this grounding or adds an authored definition, rerun `just seed`, a focused `just seed-canary` for the affected generated identifier, `just validate-strict` on the regenerated record path, `just term-requests-check`, `just validate-history`, `just validate-causal-all`, `just verify-corpus --max-diffs 1`, and `just report`.

## Additional Notes

All absence checks in this review used ignored-inclusive `rg --no-ignore --hidden` or `find`, bounded to maintained curation, history, research, raw inventory, generated habitat, and review-report paths. No search depended on rendered `pages/` or text-map JSON.

The exact `Soil` leaf has many sibling records in the generated corpus. This review resolved the record by exact source path and generated identifier before judging it, and did not treat sibling soil records as evidence for or against `data/habitats/engineered/soil__cbcc9126.yaml`.
