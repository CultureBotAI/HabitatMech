# YAML Record Review: Alkaline

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/alkaline.yaml
- Started UTC: 2026-09-21T12:31:29Z
- Finished UTC: 2026-09-21T12:31:29Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Path | `data/habitats/aquatic/alkaline.yaml` |
| Identifier | `habitatmech:GOLD.751e762923` |
| Label | `Alkaline` |
| Category | `AQUATIC` |
| Grounding status | `NOT_APPLICABLE` |
| Mapping status | `REVIEWED` |
| Source | GOLD |
| Source ID | `gold.ecosystem:3994` |
| Source path | `Environmental > Aquatic > Thermal springs > Hot (42-90C) > Alkaline` |

The target is a generated GOLD-only record. It has no definition, synonyms,
xrefs, environmental parameters, characteristic taxa, record-level evidence,
causal graphs, discussions, or datasets. `data/habitats/PATHS.tsv` pins the
minted identifier to the stable `alkaline` slug.

This review covers the single GOLD source concept keyed by
`habitatmech:GOLD.751e762923`. The identifier is the first ten hex characters
of `sha1("GOLD:Environmental > Aquatic > Thermal springs > Hot (42-90C) > Alkaline")`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/alkaline.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/alkaline.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just term-requests-check` | Passed; the generated term-request table is current with 109 terms. |
| Reference validator | Not checked: this reviewed `NOT_APPLICABLE` GOLD-only record has no `EvidenceItem` references or causal overlay, and this repository documents no standalone reference validator for a generated record without those objects. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records were present, with 0 missing, 0 extra, and 0 differing files. |

## Identity and Grounding

The reviewed source identity is reproducible:

| Claim | Review |
|---|---|
| Minted identifier | Supported. The seeder's `mint("GOLD", canonical_path)` helper hashes the exact source path to `habitatmech:GOLD.751e762923`. |
| GOLD source path | Supported. `data/raw/gold_ecosystem_paths.tsv` has the exact canonical path `Environmental > Aquatic > Thermal springs > Hot (42-90C) > Alkaline`. |
| Source ID | Supported. The exact raw GOLD ecosystem row records `gold.ecosystem:3994`. |
| Source label | Supported. The raw GOLD row sets `leaf_label` to `Alkaline`, matching the generated `label` and `source_label`. |
| Assertion count | Supported. The exact raw GOLD ecosystem row has `organism_count=6`, `study_count=0`, `biosample_count=0`, and `total_assertions=6`; the generated attestation stores `assertion_count: 6` with `assertion_unit: ORGANISM`. |
| Category | Supported by the first two GOLD path levels, `Environmental > Aquatic`. |
| Stable path | Supported. `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.751e762923` to `alkaline`. |
| `grounding_status: NOT_APPLICABLE` | Supported. The source concept's maintained `curation/decisions.tsv` row is an item-level `NOT_APPLICABLE` decision stating that this `Alkaline` leaf is a pH band qualifying a hot thermal-spring parent, not a habitat. |
| `mapping_status: REVIEWED` | Supported. The sole contributing GOLD source has an item-level decision, so the generated record is fully reviewed even though its source concept is rejected as a habitat. |

The non-habitat decision is sound. GOLD is using `Alkaline` as a specific-ecosystem
classifier under `Environmental > Aquatic > Thermal springs > Hot (42-90C)`.
The vendored ontology slice also confirms that bare alkalinity is a quality:
`PATO:0001430` has label `alkaline`. A pH band can describe a hot-spring
habitat, but it is not itself the habitat identity.

The generated `parent_habitats` edge is not sound. The target currently has
`habitatmech:GOLD.c085f6141a` as a parent, but that parent record is the GOLD
`Environmental > Aquatic > Thermal springs > Hot (42-90C)` node and has also
been item-reviewed `NOT_APPLICABLE` because it denotes a temperature band.
That turns one environmental qualifier into a strict child of another
environmental qualifier.

## Evidence

Supported:

| Claim | Evidence |
|---|---|
| The source concept exists and is keyed by one GOLD ecosystem node. | `data/raw/gold_ecosystem_paths.tsv` has one collapsed node, `gold.ecosystem:3994`, for the exact hot alkaline path. |
| The generated GOLD attestation preserves the upstream organism count. | The exact raw GOLD row has 6 organism assertions, and the generated attestation records 6 `ORGANISM` assertions. |
| The exact source path appears in downstream GOLD biosample and study inventories. | `data/raw/gold_path_biosamples.tsv` has 16 biosamples for the exact path, and 11 `data/raw/gold_studies.tsv` rows include the exact path. |
| The MIxS triads for the exact source support hot-spring context, not an alkaline identity. | `data/raw/gold_path_triads.tsv` has exact-path rows whose highest-count local term is `ENVO:00000051` `hot spring` and whose highest-count medium term is `ENVO:03600065` `spring water`. |
| The nearest GOLD parent is a reviewed qualifier, not a habitat. | `data/habitats/aquatic/hot_42_90c.yaml` is `habitatmech:GOLD.c085f6141a`, and `curation/decisions.tsv` marks that source concept `NOT_APPLICABLE` because `Hot (42-90C)` is a temperature band. |

Unsupported or over-scoped:

| Claim | Issue |
|---|---|
| `habitatmech:GOLD.c085f6141a` `Hot (42-90C)` is a broader habitat for this record. | Both child and parent are physicochemical qualifiers rather than habitats. `docs/CURATION.md` and `CLAUDE.md` reserve `parent_habitats` for strictly broader is-a links, so the generated edge overstates GOLD path nesting as habitat subsumption. |

No literature citations, record-level evidence, causal edges, environmental
parameters, characteristic taxa, discussions, or datasets are attached to this
record.

## Completeness

The source concept has been reviewed at item depth, and no term request is
needed because the source concept is not a habitat. The record still has one
consequential generated artifact gap:

- `src/habitatmech/seed.py` adds the next GOLD path level as a parent after
  item-level decisions are applied, so this reviewed `NOT_APPLICABLE` pH band
  still inherits a reviewed `NOT_APPLICABLE` temperature-band parent.

Bounded absence checks:

| Query | Scope | Result |
|---|---|---|
| `habitatmech:GOLD.751e762923` | Hidden/ignored-inclusive search over `curation`, `data/raw`, `data/habitats`, `data/habitats/PATHS.tsv`, `research/habitats`, and `reports/yaml_record_review`, excluding `data/text_map/**` | Found the maintained decision row, the PATHS row, and the generated target; found no term request, causal overlay, research report, or prior YAML review for this exact source concept. |
| `gold.ecosystem:3994` and `Environmental > Aquatic > Thermal springs > Hot (42-90C) > Alkaline` | Hidden/ignored-inclusive search over raw GOLD rows, maintained curation inputs, generated records, research reports, and YAML reviews | Found the exact GOLD ecosystem, biosample, MIxS triad, and study rows, the maintained decision row, the generated target, and the prior hot-acidic sibling review that named this source path as another affected sibling. |
| `habitatmech:GOLD.c085f6141a` | Hidden/ignored-inclusive search over maintained curation inputs, generated records, PATHS, research reports, and YAML reviews | Found the generated `Hot (42-90C)` parent, its `NOT_APPLICABLE` decision row, and other children under the same qualifier parent; found no maintained suppression that would prevent the false parent edge. |
| `alkaline` | Hidden/ignored-inclusive search over `curation/causal_graphs`, `research/habitats/aquatic`, and prior YAML reviews | Found only the non-marine saline-or-alkaline research report and adjacent review prose; found no target-specific overlay, research report, or prior review for this hot alkaline pH-band record. |

## Findings

### Blocker

None found.

### Major

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| M1 | `parent_habitats` keeps a generated edge from a reviewed non-habitat pH band to a reviewed non-habitat temperature band. | `alkaline.yaml` lists `habitatmech:GOLD.c085f6141a` as its parent; `hot_42_90c.yaml` and the maintained decision for `habitatmech:GOLD.c085f6141a` both say the parent source concept is `NOT_APPLICABLE`. The GOLD second pass in `src/habitatmech/seed.py` adds the parent path after decisions resolve both rows to minted identifiers, so the edge is generated reproducibly even though both concepts are environmental-parameter qualifiers. | `src/habitatmech/seed.py`, or a maintained suppression/rewiring input consumed by the GOLD parent-path pass. |

### Minor

None found.

## Recommended Edits

1. Update the maintained GOLD hierarchy pass in `src/habitatmech/seed.py`, or add a maintained suppression/rewiring table consumed by that pass, so item-reviewed `NOT_APPLICABLE` qualifier source concepts are not emitted as strict `parent_habitats`.
2. At minimum, suppress the edge from `Environmental > Aquatic > Thermal springs > Hot (42-90C) > Alkaline` to `Environmental > Aquatic > Thermal springs > Hot (42-90C)`.
3. Apply the same review to the pH-band siblings `Acidic` and `Neutral` and to real children under `Hot (42-90C)`, such as `Microbial mats` and `Sediment`, so removing the false temperature-band node does not also erase defensible hot-spring context.
4. Do not hand-edit `data/habitats/aquatic/alkaline.yaml`; it is generated from raw GOLD, curation decisions, and the seeder.

## Follow-up Checks

| Edit | Narrowest proving check |
|---|---|
| Suppress or rewire the false GOLD parent edge for this exact source. | Run `just seed-canary habitatmech:GOLD.751e762923` and confirm `data/habitats/aquatic/alkaline.yaml` no longer has `parent_habitats: habitatmech:GOLD.c085f6141a`. |
| Preserve source provenance while rejecting the pH band as a habitat. | Re-read the canary and confirm the record keeps `grounding_status: NOT_APPLICABLE`, `mapping_status: REVIEWED`, `source_id: gold.ecosystem:3994`, and the exact hot alkaline GOLD source path. |
| Avoid regressing real hot-spring descendants. | Canary `habitatmech:GOLD.086ba0a00d` and `habitatmech:GOLD.686adf1872`, then confirm the hot `Microbial mats` and `Sediment` records keep a true broader habitat instead of inheriting the `Hot (42-90C)` qualifier. |
| Prove the regenerated corpus remains stable. | Run `just validate data/habitats/aquatic/alkaline.yaml`, `just validate-strict data/habitats/aquatic/alkaline.yaml --quiet`, `just term-requests-check`, `just validate-history`, and `just verify-corpus --max-diffs 1`. |
| Prove downstream exports no longer carry the false edge. | Rebuild KGX and SSSOM products and confirm no exported edge asserts `habitatmech:GOLD.751e762923` under `habitatmech:GOLD.c085f6141a`. |

## Additional Notes

- The 16 biosamples in `data/raw/gold_path_biosamples.tsv` and the 15 samples summarized in `data/raw/gold_path_triads.tsv` are distinct from the 6 `ORGANISM` assertions in `data/raw/gold_ecosystem_paths.tsv`; the generated `source_attestations` count intentionally uses the organism count.
- The adjacent `ENVO:00002119` `alkaline hot spring` ontology record does not make this GOLD leaf an exact habitat identity. GOLD stores alkalinity here as a leaf under a temperature band, while its MIxS triads point to hot spring and spring water as contextual sample fields.
