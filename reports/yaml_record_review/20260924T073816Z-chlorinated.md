# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/aquatic/chlorinated.yaml`
- Started UTC: 2026-09-24T07:32:46Z
- Finished UTC: 2026-09-24T07:38:16Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/chlorinated.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.f278a2544f` |
| Label | `Chlorinated` |
| Category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| GOLD source-concept key | `habitatmech:GOLD.f278a2544f` |
| GOLD parent source-concept key | `habitatmech:GOLD.99a88ecb51` |
| Maintained owners | Generated from `data/raw/gold_ecosystem_paths.tsv`, `curation/decisions.tsv`, `data/habitats/PATHS.tsv`, and `src/habitatmech/seed.py`; `data/habitats/` is generated and remains read-only |

This generated GOLD-only record represents `gold.ecosystem:4059`:

```text
Environmental > Aquatic > Freshwater > Drinking water > Chlorinated
```

The record is one of the disinfection-state or treatment-state leaves under
GOLD's `Drinking water` path. This review covers only the `Chlorinated` child
source with stable minted identifier `habitatmech:GOLD.f278a2544f`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/chlorinated.yaml` | Passed: LinkML reported no issues |
| `just validate-strict data/habitats/aquatic/chlorinated.yaml` | Passed: 1 file scanned, 0 files with errors, 0 total error rows |
| `just validate-causal <overlay>` | Not applicable: the record has no `causal_graphs` slot, and ignored/hidden-inclusive searches of `curation/causal_graphs/` found no overlay for `habitatmech:GOLD.f278a2544f`, `gold.ecosystem:4059`, or the exact chlorinated GOLD path |
| `just validate-causal-all` | Passed: 32 causal-graph curation files with 32 graphs |
| `just term-requests-check` | Passed: term-request table is current with 109 terms |
| `just validate-history` | Passed: no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Passed: expected 3,206 records, found 3,206, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | Passed: 0 ungrounded records still undecided, 1,810 decisions on file |
| `just report` | Passed: corpus report completed for 3,206 records |
| Reference validator | Not applicable: this record has no DOI, PMID, URL, `EvidenceItem`, or causal-edge evidence that would need citation checking |
| `git diff --check` | Passed before this report was written |

## Identity and Grounding

| Claim | Maintained evidence | Assessment |
|---|---|---|
| The output identifier is `habitatmech:GOLD.f278a2544f`. | `data/habitats/PATHS.tsv` pins that identifier to `chlorinated`. | Supported. |
| The GOLD source ID is `gold.ecosystem:4059`. | `data/raw/gold_ecosystem_paths.tsv` has one row for `Environmental > Aquatic > Freshwater > Drinking water > Chlorinated` with one GOLD node ID, depth 5, `organism_count: 1`, `study_count: 0`, `biosample_count: 0`, and `total_assertions: 1`. | Supported. |
| The source belongs in the aquatic category. | `src/habitatmech/seed.py` infers the category from GOLD's `Environmental > Aquatic` prefix for this raw row. | Supported. |
| The source is broader than neither `Chloraminated` nor `Unchlorinated`. | The committed GOLD ecosystem inventory keeps `Chlorinated`, `Chloraminated`, `Unchlorinated`, `Untreated`, `Filtered water`, and `Delivery networks` as separate children of `Environmental > Aquatic > Freshwater > Drinking water`. | Supported. |
| `habitatmech:GOLD.99a88ecb51` `Drinking water` is a broader source-path parent for this source concept. | The GOLD parent-path pass in `src/habitatmech/seed.py` links each GOLD concept to the resolved concept of its parent path; the parent path `Environmental > Aquatic > Freshwater > Drinking water` resolves to `habitatmech:GOLD.99a88ecb51`. | Supported. |
| `grounding_status: UNGROUNDED` is generated by a class-level no-match sweep. | `curation/decisions.tsv` has `habitatmech:GOLD.f278a2544f` as `CONFIRM_UNGROUNDED` with no `object_id` and `review_depth: CLASS`. The row records that no vendored-slice term matched the label by any search route, and that whether the concept is a habitat was not assessed. | Mechanically supported, but incomplete for item-level curation. |
| `mapping_status: SEEDED` is correct for the current curation input. | `docs/CURATION.md` says `CLASS` records membership in a mechanically defined screen and does not promote a record to `REVIEWED`; `Decision.counts_as_reviewed` only returns true when `review_depth` is `ITEM`. | Supported. |

The generated record is reproducible, but its only curator decision is too weak
now that the full GOLD path has been read. GOLD's leaf label is an adjective,
yet the five-level source path scopes it to chlorinated drinking water: a real
microbial habitat that is narrower than drinking water and distinct from the
sibling chloramine-treated, untreated, filtered, and delivery-network drinking
water leaves.

## Evidence

The source attestation is an accurate transcription of the committed GOLD row:

- `source: GOLD`
- `source_id: gold.ecosystem:4059`
- `source_label: Chlorinated`
- `source_path: Environmental > Aquatic > Freshwater > Drinking water > Chlorinated`
- `assertion_count: 1`
- `assertion_unit: ORGANISM`

Additional GOLD-derived evidence supports the existence and boundary of the
source path:

| Source | Maintained row | Assessment |
|---|---|---|
| Biosamples | `data/raw/gold_path_biosamples.tsv` maps the exact path and node `4059` to 94 biosamples. | Supported; biosample counts are independent of the `ORGANISM` count in the generated source attestation and are not emitted on this record. |
| GOLD studies | `data/raw/gold_studies.tsv` has one study row, `Gs0083744`, containing only `Environmental > Aquatic > Freshwater > Drinking water > Chlorinated`, and another, `Gs0154279`, that includes the chlorinated path beside its `Drinking water` parent and its `Chloraminated` sibling. | Supports source use of the exact path and confirms that GOLD distinguishes chlorine-treated drinking water from chloramine-treated drinking water. |
| MIxS triads | `data/raw/gold_path_triads.tsv` has exact `broad`, `local`, and `medium` rows for the chlorinated path, with top terms `ENVO:00000873` `freshwater biome`, `ENVO:00000026` `well`, and `ENVO:00003064` `drinking water`. | Supports a freshwater drinking-water interpretation for the GOLD path. These rows are contextual triad evidence, not direct generated `environmental_parameters`. |

There is no claim-level evidence, causal graph, or reference-bearing assertion
on this seeded GOLD record.

## Completeness

- The generated target contains every field currently emitted from the exact
  maintained GOLD row and the class-level curation row: minted identity,
  aquatic category, ungrounded seeded status, the GOLD source attestation with
  one organism assertion, the generated parent-path parent, the class-sweep
  curation event, and one seed curation event.
- Empty `synonyms`, `environmental_parameters`, `characteristic_taxa`,
  `evidence`, `causal_graphs`, `discussion`, and `datasets` are technically
  acceptable for the current maintained inputs. The exact GOLD path has
  committed MIxS triads, but those rows are curation leads rather than direct
  generated environmental-parameter inputs; no maintained overlay supplies an
  output synonym, source parameter, taxon association, claim-level evidence
  item, causal graph, discussion item, or dataset reference for this source
  concept.
- Ignored/hidden-inclusive exact searches covered `curation/decisions.tsv`,
  `curation/term_requests.tsv`, `curation/external_xrefs.tsv`,
  `curation/causal_graphs/`, `history/`, `research/`,
  `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, and
  `reports/yaml_record_review/` for `habitatmech:GOLD.f278a2544f`,
  `gold.ecosystem:4059`,
  `Environmental > Aquatic > Freshwater > Drinking water > Chlorinated`,
  chlorinated-drinking-water wording, and `chlorinated` variants. They found
  the class-depth decision row, the source inventory rows, unrelated research
  mentions of chlorinated solvents or chlorinated potable water, and contextual
  mentions in the `Chloraminated` review, but no item-level decision,
  term-request row, external-xref row, causal overlay, history entry, dedicated
  research item, ontology identity for chlorinated drinking water, or prior
  exact YAML review report.
- A gitignore-independent `find` search covered `reports/yaml_record_review/`
  before report creation; no exact `*-chlorinated.md` report was found.

## Findings

### Major

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| `HM-CHLORINATED-001` | The source concept still needs item-level curation. It denotes chlorinated drinking water, but the only current decision is a class-depth lexical no-match row that did not assess the full GOLD path, did not confirm habitathood, and did not decide whether the concept needs a novel term request. | `curation/decisions.tsv` records `habitatmech:GOLD.f278a2544f` as `CONFIRM_UNGROUNDED` with `review_depth: CLASS`. The raw GOLD path places `Chlorinated` directly under `Environmental > Aquatic > Freshwater > Drinking water`, beside other treatment-state leaves such as `Chloraminated`, `Unchlorinated`, `Untreated`, and `Filtered water`, and the exact path has GOLD MIxS rows whose top `medium` term is `ENVO:00003064` `drinking water`. Exact ignored/hidden-inclusive searches of the vendored ontology slice and curation inputs found no chlorinated-drinking-water term, no `curation/term_requests.tsv` row, and no target-specific item-level decision. | Replace the class-depth row for `habitatmech:GOLD.f278a2544f` in `curation/decisions.tsv` with an item-level `CONFIRM_UNGROUNDED` row if no exact ontology identity exists after final review, then add a `curation/term_requests.tsv` definition for the chlorinated-drinking-water habitat. |

No blocker or minor findings found.

## Recommended Edits

1. Replace the existing `CLASS`-depth `curation/decisions.tsv` row for
   `habitatmech:GOLD.f278a2544f` with an `ITEM`-depth decision. If final item
   review confirms no exact ontology term exists, keep the minted identifier
   with `CONFIRM_UNGROUNDED`.

2. Add a `curation/term_requests.tsv` row defining the novel
   chlorinated-drinking-water concept under `ENVO:00003064` `drinking water`.
   Preserve the generated GOLD parent-path edge to
   `habitatmech:GOLD.99a88ecb51` unless item review finds that the source
   hierarchy itself is too broad.

3. Regenerate the corpus from the maintained inputs and confirm
   `data/habitats/aquatic/chlorinated.yaml` is still minted, has the same GOLD
   source attestation for `gold.ecosystem:4059`, receives the authored
   definition, and becomes `mapping_status: REVIEWED`.

## Follow-up Checks

| Scope | Command or check |
|---|---|
| Target schema | `just validate data/habitats/aquatic/chlorinated.yaml` |
| Closed-schema validation | `just validate-strict data/habitats/aquatic/chlorinated.yaml` |
| Corpus reproduction | `just verify-corpus --max-diffs 1` |
| Worklist/report sanity | `just worklist --limit 2000` and `just report` |
| Causal overlays | `just validate-causal-all` |
| Term-request exports | `just term-requests-check` |
| History | `just validate-history` |
| Manual decision check | Confirm the replacement decision is keyed to `habitatmech:GOLD.f278a2544f`, uses `review_depth: ITEM`, and does not merge this source with the sibling `Chloraminated`, `Unchlorinated`, `Untreated`, `Filtered water`, or `Delivery networks` leaves |
| Diff hygiene | `git diff --check` |

## Additional Notes

- Unlike the `Chloraminated` sibling, the exact `Chlorinated` GOLD path has
  committed MIxS triad rows. Their top terms agree that the path is freshwater
  drinking water, but the `local` top term is `ENVO:00000026` `well`, so the
  triad is not itself an exact habitat identity decision for chlorinated
  drinking water.
- GOLD records 94 biosamples for `gold.ecosystem:4059` while the KGX
  ecosystem-path aggregate has one organism assertion. Those are different
  source tables and should stay separated: the generated GOLD attestation
  correctly keeps `assertion_count: 1` with `assertion_unit: ORGANISM`.
