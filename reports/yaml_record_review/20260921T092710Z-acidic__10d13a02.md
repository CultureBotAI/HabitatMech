# YAML Record Review: Acidic

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/acidic__10d13a02.yaml
- Started UTC: 2026-09-21T09:27:10Z
- Finished UTC: 2026-09-21T09:27:10Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/acidic__10d13a02.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.90a084a03e` |
| Label | `Acidic` |
| Category | `AQUATIC` |
| Grounding status | `NOT_APPLICABLE` |
| Mapping status | `REVIEWED` |
| Maintained/generated status | Generated from `data/raw/` inventories and curator inputs; `just verify-corpus --max-diffs 1` reproduces it byte-for-byte. |

The generated record is an item-reviewed GOLD qualifier record:

- GOLD supplies the source concept `Environmental > Aquatic > Acidic`, collapsed
  from three GOLD ecosystem node IDs,
  `gold.ecosystem:7975|gold.ecosystem:8506|gold.ecosystem:8507`, with one
  `ORGANISM` assertion.
- `curation/decisions.tsv` marks the source concept `NOT_APPLICABLE` at
  `ITEM` depth because `Acidic` is a pH band qualifying its parent node, not a
  habitat in its own right.
- The stable filename comes from the lockfile row for
  `habitatmech:GOLD.90a084a03e` in `data/habitats/PATHS.tsv`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/acidic__10d13a02.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/acidic__10d13a02.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 records on disk, 0 missing, 0 extra, 0 differing. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| Reference validator | Not checked: this record has no DOI/PMID/URL `EvidenceItem`, no causal-edge evidence, and no dedicated `research/habitats/` report for `habitatmech:GOLD.90a084a03e`. |

## Identity and Grounding

| Claim | Evidence | Assessment |
|---|---|---|
| The record denotes the GOLD `Environmental > Aquatic > Acidic` source concept. | `data/raw/gold_ecosystem_paths.tsv` has exactly that canonical path, leaf `Acidic`, depth `3`, `gold_node_count=3`, `organism_count=1`, `total_assertions=1`, and node IDs `gold.ecosystem:7975\|gold.ecosystem:8506\|gold.ecosystem:8507`. | Supported exactly. |
| The stable file stem is `acidic__10d13a02`. | `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.90a084a03e` to `acidic__10d13a02`. | Supported exactly. |
| The source path justifies the broad `AQUATIC` category. | The raw GOLD path is directly under `Environmental > Aquatic`, and `data/habitats/aquatic/aquatic_biome.yaml` carries the GOLD `Environmental > Aquatic` attestation under the `AQUATIC` category. | Supported exactly. |
| `grounding_status: NOT_APPLICABLE` follows from item-level curator input. | `curation/decisions.tsv` has a `NOT_APPLICABLE` row for `habitatmech:GOLD.90a084a03e` with `review_depth=ITEM` and the exact GOLD path in its note. | Supported exactly. |
| `mapping_status: REVIEWED` follows from curator input. | The target record has one contributing source concept and its `curation/decisions.tsv` row has `review_depth=ITEM`; `docs/CURATION.md` states that item-level decisions promote records to `REVIEWED`. | Supported exactly. |
| `Acidic` should not be adopted as a habitat identity. | The decision row states that this is a pH band qualifying the parent node, and `data/raw/ontology_terms.tsv` has the same label on `PATO:0001429`, a quality term rather than a habitat term. | Supported exactly. |
| The `ENVO:00002030` `aquatic biome` parent follows from the GOLD source path. | The raw path sits immediately under `Environmental > Aquatic`, and the GOLD `Environmental > Aquatic` source concept resolves to `ENVO:00002030`. | Mechanically supported but semantically problematic; see `HM-ACIDIC-10D13A02-001`. |

## Evidence

The generated record has no record-level `evidence` entries and no
`causal_graphs`.

| Claim | Evidence | Assessment |
|---|---|---|
| GOLD is the only upstream attestation feeding this record. | Ignored/hidden-inclusive exact searches for `habitatmech:GOLD.90a084a03e`, `gold.ecosystem:7975`, `gold.ecosystem:8506`, `gold.ecosystem:8507`, and `Environmental > Aquatic > Acidic` found the target generated record, its `PATHS.tsv` row, its `curation/decisions.tsv` row, and the `data/raw/gold_ecosystem_paths.tsv` row. No BacDive, PREGO, Madin, term-request, or causal-graph curation row matched those exact source keys. | Supported exactly. |
| The one GOLD `ORGANISM` assertion belongs to the qualifier node itself. | An exact ignored/hidden-inclusive regex search for a bare `Environmental > Aquatic > Acidic` row in `data/raw/gold_ecosystem_paths.tsv`, `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, and `data/raw/gold_studies.tsv` found only the ecosystem-path summary row. The committed child path rows under `Environmental > Aquatic > Acidic > ...` belong to child habitat records. | Supported exactly. |
| The pH-quality disposition is consistent with nearby maintained decisions. | Exact ignored/hidden-inclusive searches for the target decision text found four GOLD `Acidic` source concepts item-marked `NOT_APPLICABLE` as pH bands rather than habitats. `data/raw/isolation_source_groundings.tsv` also maps BacDive `Acidic` to `PATO:0001429` `acidic`. | Supported exactly. |

## Completeness

- The target is complete enough as an item-reviewed `NOT_APPLICABLE` source
  concept: it has one source attestation, the matching item-level decision, a
  generated curation-history event for that decision, and a seeding event.
- The target correctly lacks a definition, term-request row, characteristic
  taxa, environmental-parameter rows, datasets, and causal graphs because the
  reviewed source concept is a pH quality rather than a habitat.
- Ignored/hidden-inclusive exact searches covered `data/habitats`, `data/raw`,
  `curation`, `research/habitats`, and `reports/yaml_record_review` for
  `habitatmech:GOLD.90a084a03e`, `gold.ecosystem:7975`,
  `gold.ecosystem:8506`, `gold.ecosystem:8507`, and the full GOLD path. These
  found the expected generated record, `PATHS.tsv` row,
  `curation/decisions.tsv` row, and `data/raw/gold_ecosystem_paths.tsv` row.
- Gitignore-independent `find` checks for `*acidic*` under
  `curation/causal_graphs`, `research/habitats`, and
  `reports/yaml_record_review` found no target-specific causal overlay or
  research report and no prior review report for `acidic__10d13a02`.
- The branch under the GOLD qualifier is not empty: its immediate generated
  children are `data/habitats/aquatic/acidic_lake.yaml`,
  `data/habitats/aquatic/acidic_hypersaline_lake.yaml`, and
  `data/habitats/aquatic/river__d478a13c.yaml`.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| `HM-ACIDIC-10D13A02-001` | Major | The reviewed non-habitat pH band is still emitted as a habitat-hierarchy node. `Acidic` is correctly `NOT_APPLICABLE`, but the record still lists `ENVO:00002030` `aquatic biome` in `parent_habitats`, and its immediate child records for `Acidic lake`, `Acidic hypersaline lake`, and `River` list this non-habitat `Acidic` record as a parent. That makes a pH quality participate in the is-a habitat graph even though the item-level decision says it is not a habitat in its own right. | `docs/CURATION.md` states that `parent_habitats` is an is-a claim. `curation/decisions.tsv` row `habitatmech:GOLD.90a084a03e` says `Acidic` is a pH band qualifying the parent node, not a habitat. `data/habitats/aquatic/acidic__10d13a02.yaml` lists `ENVO:00002030` in `parent_habitats`; `data/habitats/aquatic/acidic_lake.yaml`, `data/habitats/aquatic/acidic_hypersaline_lake.yaml`, and `data/habitats/aquatic/river__d478a13c.yaml` list `habitatmech:GOLD.90a084a03e` in `parent_habitats`. | `src/habitatmech/seed.py`, or an explicit maintained parent-edge suppression consumed by the seeder. |

No blocker or minor findings found.

## Recommended Edits

1. Update the seeder's GOLD path-parent handling so `NOT_APPLICABLE` source
   concepts do not become `parent_habitats` bridge nodes. A pH, salinity,
   temperature, disease, procedure, or sample-artifact record can stay citable
   without being advertised as a broader habitat.
2. Preserve source context when a non-habitat path segment is skipped. For the
   child records under `Environmental > Aquatic > Acidic`, either connect them
   to the nearest real habitat ancestor, record acidity as a qualifier through
   a maintained environmental-parameter path, or add a curator-owned
   suppression with an explicit note that the pH band is not a habitat parent.
3. Add a regression that covers both directions of this target bug:
   `data/habitats/aquatic/acidic__10d13a02.yaml` should not claim
   `ENVO:00002030` as a broader habitat while it is `NOT_APPLICABLE`, and the
   three immediate child records should not list the `NOT_APPLICABLE` `Acidic`
   concept in `parent_habitats`.
4. Re-seed the target and the immediate children, inspect all four generated
   records, then run the corpus integrity and render checks that cover parent
   links and pages.

## Follow-up Checks

| Edit | Narrowest proving check |
|---|---|
| Suppress or skip `NOT_APPLICABLE` nodes in GOLD path parentage. | `uv run python scripts/seed_from_sources.py --only habitatmech:GOLD.90a084a03e habitatmech:GOLD.41f771c2b4 habitatmech:GOLD.2a6cc2b868 habitatmech:GOLD.1d0c72d58b`, followed by inspection of the targeted dry-run resolution for those four records. |
| Regenerate the affected canaries. | `just seed-canary habitatmech:GOLD.90a084a03e habitatmech:GOLD.41f771c2b4 habitatmech:GOLD.2a6cc2b868 habitatmech:GOLD.1d0c72d58b --force`, then inspect the four generated records. |
| Add the parent-hierarchy regression. | `just test tests/test_decisions.py tests/test_corpus_integrity.py`, or the narrower new test path if the regression belongs elsewhere. |
| Validate the regenerated corpus. | `just validate data/habitats/aquatic/acidic__10d13a02.yaml`, `just validate data/habitats/aquatic/acidic_lake.yaml`, `just validate data/habitats/aquatic/acidic_hypersaline_lake.yaml`, `just validate data/habitats/aquatic/river__d478a13c.yaml`, `just validate-strict data/habitats/aquatic/acidic__10d13a02.yaml data/habitats/aquatic/acidic_lake.yaml data/habitats/aquatic/acidic_hypersaline_lake.yaml data/habitats/aquatic/river__d478a13c.yaml --quiet`, `just verify-corpus --max-diffs 1`, `just render-check`, and `just validate-history`. |

## Additional Notes

- The same pH-band `NOT_APPLICABLE` reasoning is applied to
  `Environmental > Aquatic > Artesian spring > Acidic`,
  `Environmental > Aquatic > Thermal springs > Warm (34-42C) > Acidic`, and
  `Environmental > Aquatic > Thermal springs > Hot (42-90C) > Acidic`. A
  hierarchy fix for this target should check all three sibling `Acidic`
  records.
- `data/raw/isolation_source_groundings.tsv` maps BacDive `Acidic` to
  `PATO:0001429` `acidic`, and `data/raw/ontology_terms.tsv` vendors that term.
  The GOLD decision for `habitatmech:GOLD.90a084a03e` does not keep the PATO
  quality as an xref; that is not a correctness issue, because the maintained
  note is already enough to justify `NOT_APPLICABLE`.
