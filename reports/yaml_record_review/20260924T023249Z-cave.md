# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/terrestrial/cave.yaml
- Started UTC: 2026-09-24T02:29:00Z
- Finished UTC: 2026-09-24T02:32:49Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/terrestrial/cave.yaml` |
| Class | `HabitatRecord` |
| Identifier | `ENVO:00000067` |
| Label | `cave` |
| Category | `TERRESTRIAL` |
| Grounding status | `EXACT` |
| Mapping status | `REVIEWED` |
| Maintained owner | Generated from `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, GOLD/PREGO source inventories, `curation/decisions.tsv`, and `data/habitats/PATHS.tsv`; do not edit `data/habitats/terrestrial/cave.yaml` directly. |

`data/habitats/terrestrial/cave.yaml` is the generated exact ENVO record for
`ENVO:00000067` `cave`. It merges two reviewed source concepts:
`habitatmech:GOLD.db94b84352`, recomputed from
`GOLD:Environmental > Terrestrial > Cave`, and
`habitatmech:PREGO.7020391d88`, recomputed from `PREGO:ENVO:00000067`.
`data/habitats/PATHS.tsv` locks the generated slug to `cave`.

An ignored-file-inclusive pre-report check,
`find reports/yaml_record_review -maxdepth 1 -type f -name '*-cave.md' -print`,
found no prior exact report. A second ignored-file-inclusive
`find data/habitats -type f -name 'cave.yaml' -print` found exactly this
generated YAML target.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/terrestrial/cave.yaml` | Pass - LinkML reported `No issues found`. |
| `just validate-strict data/habitats/terrestrial/cave.yaml` | Pass - 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Target causal overlay validator | Not applicable - ignored-file-inclusive `find` found no `curation/causal_graphs/*cave*` overlay, and exact ignored/hidden-inclusive `rg` found no causal overlay keyed to `ENVO:00000067`, `habitatmech:GOLD.db94b84352`, or `habitatmech:PREGO.7020391d88`. |
| Reference validator | Not applicable - the record has no `evidence` entries or causal graph edges with reference identifiers. |
| `just validate-causal-all` | Pass - validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Pass - committed ENVO batch term-request table is current with 109 terms. |
| `just validate-history` | Pass - 77 history records valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass - expected 3206 generated records, found 3206 on disk, with 0 missing, 0 extra, and 0 differing. |
| `just worklist --limit 2000` | Pass - 0 ungrounded records still undecided; 1810 decisions on file. |
| `just report` | Pass - report completed. |
| `git diff --check` | Pass - no whitespace errors before this report was written. |

## Identity and Grounding

The record identity is exact. `data/raw/ontology_terms.tsv` carries
`ENVO:00000067` with label `cave`, definition "Naturally formed, subterranean
open area or chamber.", exact synonyms `cavern`, `grotto`, and `notch`, and the
term as active. The generated `identifier`, `label`, `definition`,
`definition_source: ENVO`, ENVO exact synonyms, and exact grounding status all
follow from that row.

The GOLD source concept agrees with the ENVO identity. `data/raw/gold_ecosystem_paths.tsv`
has the collapsed path `Environmental > Terrestrial > Cave` with label `Cave`,
depth 3, 68 organism assertions, and three GOLD ecosystem node ids
`gold.ecosystem:4489|gold.ecosystem:4490|gold.ecosystem:4491`. The generated
GOLD attestation keeps the full source path, reports 68 `ORGANISM` assertions,
uses `skos:exactMatch`, and notes that the first of the three node ids is
shown.

The PREGO source concept also agrees with the ENVO identity. `data/raw/prego_habitats.tsv`
has one `ENVO:00000067` row with label `cave`, 6 distinct taxa, score 4, and
the `annotated_genomes_isolates` evidence channel; the six generated PREGO
taxon rows exactly match `data/raw/prego_habitat_taxa.tsv`. None of the six
taxa are marked `is_characteristic`, so the record keeps them as reported-from
associations rather than stronger typification claims.

The current review status is mechanically justified: `curation/decisions.tsv`
has item-level `REVIEW` rows for both `habitatmech:GOLD.db94b84352` and
`habitatmech:PREGO.7020391d88`, and generated `mapping_status: REVIEWED`
requires every contributing source concept to have an item-level decision.
Those `REVIEW` rows endorse the two exact identity decisions; they do not
separately justify every inherited parent edge.

The ontology-derived parent is supported. `data/raw/ontology_subclass_edges.tsv`
asserts `ENVO:00000067 rdfs:subClassOf ENVO:01001884`, and
`data/raw/ontology_terms.tsv` labels `ENVO:01001884` as `surface landform`, so
that generated `parent_habitats` entry follows the vendored ENVO hierarchy.

The GOLD-derived `ENVO:00000446` parent is not supported. The generated edge
comes from GOLD's `Environmental > Terrestrial` path prefix, but
`data/raw/ontology_terms.tsv` labels `ENVO:00000446` as `terrestrial biome`.
A cave is a landform within a terrestrial setting, not itself a terrestrial
biome. The three newer GOLD biosamples for `Environmental > Terrestrial > Cave`
also map the MIxS local-scale slot to `ENVO:00002144` `cave wall` and the
medium slot to `ENVO:00001995` `rock`, which corroborates the cave-wall or rock
sampling context but does not make the whole cave a biome.

## Evidence

| Claim | Source | Assessment |
|---|---|---|
| The record represents `ENVO:00000067` `cave`. | `data/raw/ontology_terms.tsv`; generated `identifier`, `label`, `definition`, and `definition_source` | Supported exactly. |
| `ENVO:00000067` is an ENVO subclass of `ENVO:01001884` `surface landform`. | `data/raw/ontology_subclass_edges.tsv`; `data/raw/ontology_terms.tsv` | Supported exactly. |
| GOLD `Environmental > Terrestrial > Cave` is an exact match for ENVO `cave`. | `data/raw/gold_ecosystem_paths.tsv`; `curation/decisions.tsv` | Supported exactly. |
| PREGO `ENVO:00000067` is an exact match for ENVO `cave`. | `data/raw/prego_habitats.tsv`; `curation/decisions.tsv` | Supported exactly. |
| The GOLD source contributes 68 organism assertions. | `data/raw/gold_ecosystem_paths.tsv`; generated `source_attestations` | Supported exactly; the assertion unit is `ORGANISM`. |
| The PREGO source contributes 6 taxa and the top 6 generated taxon associations. | `data/raw/prego_habitats.tsv`; `data/raw/prego_habitat_taxa.tsv`; generated `characteristic_taxa` | Supported as PREGO reported-from associations. |
| `ENVO:00000446` `terrestrial biome` is a strict broader parent of `cave`. | Generated `parent_habitats` via GOLD source path | Not supported. It is the broad-scale MIxS context for one GOLD cave study, not an `is-a` parent. |

Exact ignored/hidden-inclusive searches found no target-specific maintained
causal overlay, no exact report under `reports/yaml_record_review/*-cave.md`,
and no `research` or `history` filename containing `00000067` or this record's
source-concept hashes.

## Completeness

| Area | Assessment |
|---|---|
| Definition | Complete. The ENVO definition is present with `definition_source: ENVO`. |
| Synonyms | Minor cleanup needed. The ENVO exact synonyms are present, and PREGO's `cavern`, `caverns`, `caves`, `grotto`, `grottoes`, and `notches` labels are preserved, but `caveses` is a malformed PREGO synonym artifact that should not be exposed as a related synonym. |
| Parents | Needs curation. `ENVO:01001884` is a true ontology parent; `ENVO:00000446` is a false source-path parent. |
| Source attestations | Complete. The GOLD and PREGO source ids, labels, path, counts, units, score, and evidence channels agree with the raw inventories. |
| Environmental parameters | Complete as empty. No maintained parameter row exists for this record. |
| Characteristic taxa | Complete for seeded PREGO associations. The six generated entries exactly match the six PREGO taxon rows and are left as reported associations. |
| Evidence | Complete as empty for now. There are no authored claims or causal edges needing claim-level references. |
| Causal graphs | Complete as empty. No target overlay exists. |
| Status and audit | Complete for identity; incomplete for hierarchy. Both source concepts have item-level `REVIEW`, but the maintained inputs do not record the false inherited `terrestrial biome` parent. |

## Findings

| Severity | Finding | Evidence | Owner |
|---|---|---|---|
| Blocker | None found. |  |  |
| Major | **The generated `cave` hierarchy falsely asserts that every cave is a terrestrial biome.** The `ENVO:00000446` parent is derived from GOLD's `Environmental > Terrestrial` path prefix, not from ENVO, and it asserts an `is-a` relationship between a landform and a biome. Existing `curation/term_requests.tsv` `parent_mode=REPLACE` only works for HabitatMech-minted definitions, so there is no current maintained TSV that can remove a false source-path parent from an exact ontology record while retaining the GOLD source attestation. | `data/raw/gold_ecosystem_paths.tsv` places `Cave` below `Environmental > Terrestrial`; `data/raw/ontology_terms.tsv` labels `ENVO:00000446` as `terrestrial biome` and `ENVO:00000067` as `cave`; `src/habitatmech/seed.py` imports ontology parents for ontology-grounded terms and later adds source-derived parents; `curation/term_requests.tsv` has no `ENVO:00000067` override. | `src/habitatmech/seed.py`; future curation input for parent suppression |
| Minor | **The generated PREGO synonyms include malformed token `caveses`.** The raw PREGO row contains `caveses`, and the seeder surfaces it as a `RELATED_SYNONYM` even though it is a pluralization artifact rather than a meaningful cave synonym. | `data/raw/prego_habitats.tsv` lists `caveses`; generated `synonyms` includes `caveses` with `source: PREGO`. | PREGO extractor or synonym filter |

## Recommended Edits

1. Add a maintained way to suppress false source-path parents on exact
   ontology-grounded records, then remove `ENVO:00000446` from the generated
   `ENVO:00000067` `parent_habitats` while preserving the GOLD
   `Environmental > Terrestrial > Cave` attestation and the true ENVO parent
   `ENVO:01001884`. A new curation input is safer than hard-coding `cave` in
   the seeder because the same source-path/ontology-parent interaction appears
   in other exact GOLD merges.

2. Filter or curate the malformed PREGO synonym `caveses` so a reseed of
   `data/habitats/terrestrial/cave.yaml` keeps useful PREGO labels like
   `caves`, `grottoes`, and `notches` but no longer exposes the double plural.

## Follow-up Checks

After the maintained hierarchy suppression exists and the synonym artifact is
removed, run:

- `just seed`
- `just seed-canary ENVO:00000067`
- `just seed-apply --force`
- `just validate data/habitats/terrestrial/cave.yaml`
- `just validate-strict data/habitats/terrestrial/cave.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

Also rerun exact ignored/hidden-inclusive searches for `ENVO:00000067`,
`habitatmech:GOLD.db94b84352`, `habitatmech:PREGO.7020391d88`, and `caveses`
to confirm the false parent and malformed synonym are gone from the generated
record and that any new parent-suppression row is the sole maintained input
for this target.

## Additional Notes

- The `Environmental > Terrestrial > Cave` GOLD path has newer GOLD biosample
  and MIxS triad context: `data/raw/gold_path_biosamples.tsv` records 15
  biosamples for `gold.ecosystem:4491`, and `data/raw/gold_path_triads.tsv`
  records one complete three-sample study with broad `terrestrial biome`,
  local `cave wall`, and medium `rock`.
- The existing research matches for `ENVO:00000067` discuss neighboring records
  such as cave spicules, built stone, monument, Geologic, and core sample.
  They are not target-specific cave identity reviews.
- `data/habitats/terrestrial/cave__4b02055a.yaml` is a separate GOLD record
  for `Environmental > Terrestrial > Deep subsurface > Cave` and has not been
  reviewed here.
