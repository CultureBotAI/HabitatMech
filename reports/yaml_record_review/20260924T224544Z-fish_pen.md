# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/engineered/fish_pen.yaml`
- Started UTC: `2026-09-24T22:41:25Z`
- Finished UTC: `2026-09-24T22:45:44Z`
- Verdict: needs curation

## Target

Reviewed the complete generated record at `data/habitats/engineered/fish_pen.yaml`. The record is a GOLD-only engineered habitat for the canonical path `Engineered > Artificial ecosystem > Aquaculture > Fish pen`, minted as `habitatmech:GOLD.984e197faf`.

The generated file has `grounding_status: UNGROUNDED` and `mapping_status: SEEDED`. It has one inherited GOLD source-path parent, `habitatmech:GOLD.0287e1b2a9` `Aquaculture`, and one `GOLD` source attestation for the first of the two GOLD ecosystem nodes that share this path.

## Validation

| Check | Result |
|---|---|
| `find reports/yaml_record_review -maxdepth 1 -type f -name '*-fish_pen.md' -print` | Passed before this report was written; no existing exact `fish_pen` review was found. `find` is not `.gitignore`-filtered. |
| `just validate data/habitats/engineered/fish_pen.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/fish_pen.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Focused causal overlay validator | Not applicable; exact ignored-inclusive searches in `curation/causal_graphs/` found no `habitatmech:GOLD.984e197faf`, `gold.ecosystem:8511`, `gold.ecosystem:8512`, `fish_pen`, or exact source-path overlay. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| Reference validator | Not applicable; the target record has no record-level `evidence` entries and no causal graph edges with citation references. |
| `just term-requests-check` | Passed; the generated ENVO term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records all exist on disk, with 0 missing, 0 extra, and 0 differing records. |
| `just worklist --limit 2000` | Passed; it reported 0 still-undecided ungrounded records and 1,810 decisions on file. |
| `just report` | Passed; the corpus report completed for 3,206 records. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

The generated identifier, label, category, source attestation, and direct GOLD parent agree with the committed inventories. `data/raw/gold_ecosystem_paths.tsv` has the exact parent path at depth 4 with `gold_node_count` 2, no direct aggregate organism, study, or biosample assertions, and node IDs `gold.ecosystem:8511|gold.ecosystem:8512`. `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.984e197faf` to the stable `fish_pen` slug.

The `UNGROUNDED` status is supported only at `CLASS` depth. The maintained `curation/decisions.tsv` row records a class-level negative lexical sweep whose note explicitly says whether the source concept is a habitat was not assessed. Exact ignored-inclusive searches across `curation/`, `history/`, `research/`, `conf/`, `reports/yaml_record_review/`, `data/habitats/PATHS.tsv`, and focused `data/raw/` inputs found no item-level decision row, term request, history entry, research report, label-correspondence residual, or prior exact review for `habitatmech:GOLD.984e197faf`, `gold.ecosystem:8511`, `gold.ecosystem:8512`, `fish_pen`, or the exact parent source path.

The source concept itself is plausible as a real engineered habitat, but the current record has not item-reviewed nearby ontology terms. The vendored ontology slice has no exact `Fish pen` term in the bounded candidate search, but it does contain `ENVO:00000294` `fish farm` and `ENVO:03600074` `aquaculture farm` as plausible broader candidates, plus the adjacent tank terms `ENVO:03501254` `water tank` and `ENVO:00002196` `aquarium` as likely near misses that should be rejected explicitly for a pen rather than a tank.

## Evidence

- Supported: the `GOLD` source attestation is traceable to `data/raw/gold_ecosystem_paths.tsv`, including the two collapsed upstream node IDs `gold.ecosystem:8511|gold.ecosystem:8512`.
- Supported: the generated `Aquaculture` parent follows the exact GOLD source path and points at the generated record for `Engineered > Artificial ecosystem > Aquaculture`.
- Supported side-table context: the raw GOLD inventories contain a direct child path, `Engineered > Artificial ecosystem > Aquaculture > Fish pen > Sediment`, at `gold.ecosystem:8513` with 13 organism assertions.
- Supported absence: exact ignored-inclusive searches across `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, and `data/raw/gold_studies.tsv` found no row for `gold.ecosystem:8511`, `gold.ecosystem:8512`, or the exact parent source path.
- Under-reviewed: the record has no item-level decision, authored definition, term request, record-level evidence, discussion, dataset, characteristic taxon, environmental parameter, or causal graph.

## Completeness

The committed source inventories are sufficient to explain the current generated record. Exact ignored-inclusive searches covering `data/raw/`, `data/habitats/`, `curation/`, `history/`, `research/`, `conf/`, `reports/yaml_record_review/`, and `curation/causal_graphs/` found no maintained item-level decision, no term request, and no target causal overlay to audit.

The empty optional slots are appropriate for a seeded GOLD path with no curated overlay: no maintained input currently provides characteristic taxa, environmental parameters, causal edges, or record-level cited evidence for the target. The generated Fish pen sediment child at `data/habitats/engineered/sediment__591df749.yaml` is consistent with the raw child path, but it should be reviewed separately because it denotes sediment in this engineered pen rather than the pen itself.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| Major | `Fish pen` remains only class-reviewed even though the source path denotes an engineered aquaculture habitat and nearby ontology terms require an item-level broader-term decision. | The only maintained decision is a `CLASS` `CONFIRM_UNGROUNDED` row whose note says habitathood was not assessed. The vendored slice has no exact fish-pen term in the bounded candidate search, but it does have plausible broader `fish farm` and `aquaculture farm` terms and related tank terms that a curator should explicitly accept or reject before promotion from `SEEDED`. | `curation/decisions.tsv`; if no exact term fits, `curation/term_requests.tsv` |
| Major | The inherited `Aquaculture` parent is itself unreviewed, so this record's only `parent_habitats` edge is not item-reviewed as strictly broader. | `data/habitats/engineered/fish_pen.yaml` lists `habitatmech:GOLD.0287e1b2a9` as its only parent. The `Aquaculture` review found no item-level decision or term request for that parent and flagged its seeded relation to `ENVO:03600074` `aquaculture farm`. | `curation/decisions.tsv`; if the minted `Aquaculture` record remains, future definition support for `NARROW` minted records |

## Recommended Edits

1. Item-review `habitatmech:GOLD.984e197faf` in `curation/decisions.tsv` against the exact `Engineered > Artificial ecosystem > Aquaculture > Fish pen` source path, `ENVO:00000294` `fish farm`, `ENVO:03600074` `aquaculture farm`, `ENVO:03501254` `water tank`, and `ENVO:00002196` `aquarium`.
2. If no exact ontology term fits, define the minted Fish pen concept in `curation/term_requests.tsv` with a true engineered-aquaculture enclosure genus and `parent_mode=ADD`, so regeneration keeps the source-path Aquaculture parent alongside any authored ontology parent.
3. Item-review `habitatmech:GOLD.0287e1b2a9` so this record no longer depends on a class-unreviewed `Aquaculture` parent.

## Follow-up Checks

- Run `just seed`, then `just seed-canary habitatmech:GOLD.984e197faf`, and inspect `data/habitats/engineered/fish_pen.yaml` before any wider `just seed-apply --force`.
- Run `just validate data/habitats/engineered/fish_pen.yaml`, `just validate-strict data/habitats/engineered/fish_pen.yaml`, `just term-requests-check`, `just validate-history`, `just verify-corpus --max-diffs 1`, `just worklist --limit 2000`, `just report`, and `git diff --check` after maintained input changes.

## Additional Notes

The review did not find a target causal overlay, so there was no focused `just validate-causal curation/causal_graphs/<slug>.yaml` invocation to run. The whole-overlay validator still passed.

This review left generated products and curation inputs unchanged. Fish pen appears to be a habitat concept rather than a non-habitat organism, process, material, or quality, but exact grounding and strictly broader parentage still need item-level curation.
