# YAML Record Review: Blood

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/blood__070ce7aa.yaml`
- Started UTC: 2026-09-22T00:14:00Z
- Finished UTC: 2026-09-22T00:15:29Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.f46a5017f9` |
| Label | `Blood` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained inputs | `data/raw/gold_ecosystem_paths.tsv`, `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, `data/habitats/PATHS.tsv` |

This is a generated GOLD-derived record for the exact source path `Host-associated > Birds > Circulatory system > Blood`. The path lock maps `habitatmech:GOLD.f46a5017f9` to `blood__070ce7aa`, and the record intentionally stays minted because several same-leaf GOLD Blood paths are narrower host-specific children of the broader `UBERON:0000178` blood term.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/blood__070ce7aa.yaml` | Pass; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/host_associated/blood__070ce7aa.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Pass; the committed term-request table is current with 109 terms. |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass; 3,206 expected records, 3,206 records on disk, and 0 missing, extra, or differing records. |
| `git diff --check` | Pass. |

`just validate-products` was not run. This read-only review did not add or change a grounding row, and the two ontology parent labels were checked directly against `data/raw/ontology_terms.tsv`.

## Identity and Grounding

The generated minted identity and narrow grounding are supported:

- `data/habitats/PATHS.tsv` pins `habitatmech:GOLD.f46a5017f9` to `blood__070ce7aa`, matching the reviewed YAML path.
- `data/raw/gold_ecosystem_paths.tsv` has the exact canonical path `Host-associated > Birds > Circulatory system > Blood` with `leaf_label` `Blood`, depth 4, 2 collapsed GOLD node ids, 30 organisms, and 30 total assertions.
- The GOLD `Blood` leaf matches `UBERON:0000178`, labeled `blood` in `data/raw/ontology_terms.tsv`, but it is one of four same-depth GOLD Blood leaves under human, mammal, bird, and fish hosts. The ambiguous-leaf rule therefore mints this record and keeps `UBERON:0000178` as a broader parent with `mapping_predicate: skos:narrowMatch`.
- The GOLD second pass links the source path to its immediate parent, `Host-associated > Birds > Circulatory system`, which resolves to `habitatmech:GOLD.0552e4fd8a`.
- Ignored-file-inclusive searches found no exact `habitatmech:GOLD.f46a5017f9` or `data/habitats/host_associated/blood__070ce7aa.yaml` entry in `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/causal_graphs/`, `history/`, or prior `reports/yaml_record_review/` reports, so the generated `mapping_status: SEEDED` is expected.

## Evidence

Supported claims:

- The `source_attestations` row is copied from the collapsed GOLD inventory row: `gold.ecosystem:3713` is the first id in `gold_node_ids=gold.ecosystem:3713|gold.ecosystem:4153`, `assertion_count: 30` comes from `organism_count`, `assertion_unit: ORGANISM` is the unit GOLD records use, and the note correctly discloses the two collapsed upstream node ids.
- `parent_habitats: UBERON:0000178` follows from the unresolved same-depth Blood leaf match described above.
- `parent_habitats: habitatmech:GOLD.0552e4fd8a` follows from the GOLD source hierarchy; the maintained parent row is `Host-associated > Birds > Circulatory system`.
- The parent `UBERON:0000178` row exists in the vendored ontology slice as `blood`, and `UBERON:0001009`, the broader anatomy term on the generated Birds/Circulatory system parent, exists as `circulatory system`.

Unsupported or over-scoped claims: None found.

The record has no authored definition, synonyms, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussion links, or datasets. That absence is supported for this generated scope: the exact Blood path has no GOLD triad rows, no PREGO/BacDive/Madin attestation rows, no maintained term-request row, and no maintained causal overlay.

## Completeness

The record is complete enough for its current source-backed scope:

- No authored definition is expected because the source concept is still a minted GOLD path with no item-level term request.
- No item-level curation decision exists yet; with one GOLD-only source concept, `mapping_status: SEEDED` is therefore correct.
- No environmental parameter or MIxS triad values are expected because `data/raw/gold_path_triads.tsv` has triad rows for the Serum child path, not for this exact Birds/Blood path.
- No characteristic taxa are expected because this record is sourced only from GOLD, which contributes organism counts rather than ranked taxa.
- No HabitatMech-authored causal graph is expected because no maintained `curation/causal_graphs/` overlay names this habitat.

All negative checks above used `rg --no-ignore --hidden` or `find`, so ignored files were included in the searched paths.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

If a future curation change touches this record, rerun:

- `just validate data/habitats/host_associated/blood__070ce7aa.yaml`
- `just validate-strict data/habitats/host_associated/blood__070ce7aa.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

## Additional Notes

An ignored-file-inclusive `find reports/yaml_record_review -name '*blood__070ce7aa*' -print` found no prior report for this exact record before this file was written.
