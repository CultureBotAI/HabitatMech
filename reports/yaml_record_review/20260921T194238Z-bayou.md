# YAML Record Review: Bayou

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/bayou.yaml`
- Started UTC: 2026-09-21T19:41:00Z
- Finished UTC: 2026-09-21T19:42:38Z
- Verdict: pass with minor issues

## Target

| Field | Value |
|---|---|
| Identifier | `ENVO:00000234` |
| Label | `bayou` |
| Class | `HabitatRecord` |
| Category | `AQUATIC` |
| Generated or maintained | Generated from `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, `data/raw/prego_habitats.tsv`, `data/raw/prego_habitat_taxa.tsv`, and `data/habitats/PATHS.tsv`; do not hand-edit |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Source concepts | PREGO `ENVO:00000234` |
| Locked stem | `bayou` in `data/habitats/PATHS.tsv` |

The record resolves the ENVO ontology class `ENVO:00000234` `bayou`, which PREGO
also uses as its source habitat identifier. `data/habitats/PATHS.tsv` maps that
identifier to the stable corpus stem `bayou`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/bayou.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/bayou.yaml --quiet` | Pass: 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Pass: 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Pass: committed term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: 3,206 expected records, 3,206 found, 0 missing, 0 extra, 0 differing. |
| Reference validator | Not available: `justfile` exposes no reference-specific validator, and this record has no `EvidenceItem`, `EnvironmentalParameter.reference`, `CharacteristicTaxon.reference`, or causal-edge evidence to target. |

## Identity and Grounding

| Claim | Evidence | Assessment |
|---|---|---|
| The record denotes ENVO's `bayou` class. | `data/raw/ontology_terms.tsv` has `ENVO:00000234`, ontology `ENVO`, label `bayou`, the ENVO definition `A small, slow-moving stream or creek; usually located in low-lying areas.`, and `directly_referenced=TRUE`. The generated YAML uses the same identifier, label, definition, and `definition_source: ENVO`. | Supported exactly. |
| PREGO's source concept is the same ontology class. | `data/raw/prego_habitats.tsv` has `prego_id=ENVO:00000234`, ontology `ENVO`, `biolink_category=biolink:OntologyClass`, `taxon_count=1`, `direct_assertion_count=1`, `max_prego_score=3`, and `channels=annotated_genomes_isolates`. | Supported exactly. |
| The generated `source_attestations` row preserves the PREGO association volume. | The single generated attestation carries `source: PREGO`, `source_id: ENVO:00000234`, `source_label: bayou`, `assertion_count: 1`, `assertion_unit: TAXON`, `score: 3.0`, and `evidence_channels: annotated_genomes_isolates`, matching the PREGO habitat row. | Supported exactly. |
| `EXACT` grounding is mechanically and semantically appropriate. | The HabitatMech identifier and the PREGO source identifier are both `ENVO:00000234`; this is not a lexical GOLD/BacDive guess or a broader-parent mapping. | Supported exactly. |
| The `AQUATIC` category and `ENVO:00000023` parent fit the ENVO hierarchy. | `data/raw/ontology_subclass_edges.tsv` places `ENVO:00000234` under `ENVO:00000023`; `data/raw/ontology_terms.tsv` labels that parent `stream` and defines it as a linear watercourse flowing across the solid portion of a planetary surface. | Supported exactly. |

## Evidence

The record has no curator-authored `evidence`, `environmental_parameters`,
`causal_graphs`, `discussions`, or `datasets`.

| Claim | Evidence | Assessment |
|---|---|---|
| The single generated taxon row is the complete PREGO direct taxon set for this habitat. | `data/raw/prego_habitat_taxa.tsv` has exactly one `ENVO:00000234` row: rank 1, `NCBITaxon:668820`, `Burkholderia sp. JPY366`, PREGO score `3`, `direct_flag=TRUE`, and `channels=annotated_genomes_isolates`. The generated YAML carries the same taxon, score, rank, source, and `candidate_pool: 1`. | Supported exactly. |
| The taxon is an observational association, not a curated characteristic exemplar. | `src/habitatmech/schema/habitatmech.yaml` documents seeded `characteristic_taxa` as observational, and the PREGO row in the YAML has no `is_characteristic: true` or literature `reference`. | Supported exactly. |
| The generated ENVO and PREGO synonyms are source-owned. | The ENVO exact synonyms in the YAML match the vendored `data/raw/ontology_terms.tsv` synonym column; the PREGO related synonyms match `data/raw/prego_habitats.tsv` `prego_synonyms` values, excluding the preferred `bayou` label. | Supported exactly, with the synonym-scope caution called out below. |

## Completeness

The record is complete enough as a generated PREGO/ENVO seed. It preserves the
ontology identity, ENVO definition, ENVO exact synonyms, the PREGO attestation,
the sole PREGO taxon association, the ENVO parent, and the generated seed
curation event.

Empty optional fields are correct for the current maintained inputs:

- no `curation/causal_graphs/bayou.yaml` overlay exists;
- no `curation/decisions.tsv`, `curation/term_requests.tsv`,
  `curation/term_requests_excluded.tsv`, or `curation/external_xrefs.tsv` row is
  keyed by `ENVO:00000234`;
- no `history/` curation-session record names `ENVO:00000234` or the `bayou`
  slug; and
- no target-specific report exists under `research/habitats`.

Hidden- and ignored-file-inclusive searches covered `curation`, `history`,
`research/habitats`, `reports/yaml_record_review`, `data/raw`,
`data/habitats/PATHS.tsv`, and `data/habitats` for `ENVO:00000234`; `find`
lookups covered the `bayou` slug under `curation`, `history`,
`research/habitats`, and `reports/yaml_record_review`. These checks found the
expected raw ontology, raw PREGO, raw subclass, generated `PATHS.tsv`, and
generated YAML entries, and found no Bayou-owned curation decision, term
request, causal overlay, history record, research report, or previous
`*-bayou.md` YAML review.

The vendored ontology slice has no direct local child of `ENVO:00000234`.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| `HM-BAYOU-001` | Minor | Some generated synonyms for `bayou` are source-owned strings whose exactness is not locally re-reviewed. ENVO contributes broad or Bay-adjacent exact synonyms such as `bahia`, `embayment`, `lake`, and `watercourse`, while PREGO contributes mechanical related variants such as `bahial`. The generated record is faithful to the two raw rows, but consumers should not treat those exact/related scopes as a HabitatMech-curated synonym audit. | `data/raw/ontology_terms.tsv` lists the ENVO exact synonym payload for `ENVO:00000234`; `data/raw/prego_habitats.tsv` lists the PREGO `prego_synonyms`; `data/habitats/aquatic/bayou.yaml` copies them into `synonyms` with `source: ENVO` or `source: PREGO`. | Upstream ENVO/PREGO extraction into `data/raw/ontology_terms.tsv` and `data/raw/prego_habitats.tsv`; no per-record HabitatMech curation input currently owns synonym suppression. |

No blocker or major findings found.

## Recommended Edits

1. Leave `data/habitats/aquatic/bayou.yaml` generated and do not add a
   `curation/decisions.tsv` row merely to restate that PREGO `ENVO:00000234`
   is exactly ENVO `bayou`.
2. If broad or mechanically inflected synonyms create false-positive lookup
   behavior, add a maintained synonym-suppression mechanism for source-owned
   ontology/PREGO synonyms or fix the upstream vocabulary rows, then refresh the
   raw inventories and regenerate with `just seed-canary ENVO:00000234 --force`.

## Follow-up Checks

- `just seed`
- `just seed-canary ENVO:00000234 --force`
- `just validate data/habitats/aquatic/bayou.yaml`
- `just validate-strict data/habitats/aquatic/bayou.yaml --quiet`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

After any synonym-filtering change, re-read `data/habitats/aquatic/bayou.yaml`
and confirm the definition, PREGO attestation, parent `ENVO:00000023`, and sole
PREGO taxon are unchanged unless the raw PREGO or ENVO source rows changed.

## Additional Notes

This was a read-only YAML review. It did not edit generated habitat YAML, append
curation history, promote the review status, create an issue, or run paid
definition research.

`data/raw/gold_path_triads.tsv` has local triad proposals from the GOLD `Creek`
and `Creek > Sediment` paths to `ENVO:00000234`, but no generated Bayou field is
owned by those proposal rows.
