# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/fish_tank.yaml
- Started UTC: 2026-09-24T19:48:00Z
- Finished UTC: 2026-09-24T19:52:20Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.1a33387187` |
| Label | `Fish tank` |
| Category | `ENGINEERED` |
| Grounding | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Maintained source | generated from `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/PATHS.tsv`, and `curation/decisions.tsv` |

Reviewed the complete generated record at `data/habitats/engineered/fish_tank.yaml`. The record is a GOLD-only habitat for `Engineered > Artificial ecosystem > Aquaculture > Fish tank`, with displayed source ID `gold.ecosystem:8000`, 11 GOLD organism assertions, and a generated parent edge to the `Engineered > Artificial ecosystem > Aquaculture` path record. The YAML is generated output; future item-review belongs in `curation/decisions.tsv`, and any future coined definition or parent replacement belongs in `curation/term_requests.tsv`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/fish_tank.yaml` | Passed |
| `just validate-strict data/habitats/engineered/fish_tank.yaml` | Passed |
| Focused causal overlay validator | Not applicable; exact ignored-inclusive searches in `curation/causal_graphs/` found no `habitatmech:GOLD.1a33387187`, `gold.ecosystem:8000`, `gold.ecosystem:8001`, or exact Fish tank source-path overlay |
| `just validate-causal-all` | Passed; 32 files / 32 graphs |
| Reference validator | Not applicable; the generated GOLD-only record has no record-level `evidence` items, no causal graph, and no environmental-parameter references |
| `just term-requests-check` | Passed; 109 terms |
| `just validate-history` | Passed; 77 history records |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected / 3,206 found / 0 missing / 0 extra / 0 differing |
| `just worklist --limit 2000` | Passed; 0 ungrounded concepts remain undecided and 1,810 decisions are present |
| `just report` | Passed; 3,206 habitat records summarized |
| `git diff --check` | Passed |

## Identity and Grounding

The generated identifier, label, category, source path, and assertion count agree with the committed GOLD inventory. `data/raw/gold_ecosystem_paths.tsv` has the canonical path `Engineered > Artificial ecosystem > Aquaculture > Fish tank`, `depth` 4, `gold_node_count` 2, `organism_count` 11, `study_count` 0, `biosample_count` 0, `total_assertions` 11, and `gold_node_ids` `gold.ecosystem:8000|gold.ecosystem:8001`.

The source concept denotes a fish tank in the engineered aquaculture branch, so the record is a habitat rather than a leaked process, quality, chemical, disease, or whole-host taxon. Its generated source-path parent `habitatmech:GOLD.0287e1b2a9` denotes `Engineered > Artificial ecosystem > Aquaculture`, which is an appropriate broader context for the GOLD path.

The `UNGROUNDED` status is only class-level support. `curation/decisions.tsv` confirms no vendored term matched the label by the class-level lexical sweep, but its note explicitly says habitathood was not assessed. That sweep also did not weigh nearby ENVO aquaculture terms: the vendored ontology slice contains `ENVO:00002196` `aquarium`, defined as a vivarium simulating submerged portions of an aquatic environment, plus the broader facility and ecosystem terms `ENVO:00000294` `fish farm` and `ENVO:03600074` `aquaculture farm`. `Fish tank` may still deserve a distinct minted identity, but item-level review needs to decide whether it should ground to or under `ENVO:00002196` before it can be signed off as ungrounded.

## Evidence

- Supported: the source leaf `Fish tank`, full source path, first displayed `gold.ecosystem:8000` source ID, duplicate `gold.ecosystem:8001` path ID, and 11 organism assertions match `data/raw/gold_ecosystem_paths.tsv`.
- Supported: exact ignored-inclusive searches across `data/raw` for `gold.ecosystem:8000`, `gold.ecosystem:8001`, `gold.ecosystem:8005`, and `Fish tank` found only the canonical `Fish tank` row and the zero-assertion `Fish tank > Sediment` child row; no direct GOLD biosample, MIxS triad, or study side-table row needs a narrower audit for the target.
- Supported: `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.1a33387187` to stable slug `fish_tank`, and no existing ignored or unignored file in `reports/yaml_record_review/` matched `*-fish_tank.md` before this report was written.
- Under-reviewed: exact ignored-inclusive searches over `history`, `research`, `conf/id_label_targets.yaml`, `curation/term_requests.tsv`, `curation/term_requests/`, and `reports/yaml_record_review/` found no item-level history record, research report, label-correspondence exception, or maintained term request for `habitatmech:GOLD.1a33387187`.
- Under-reviewed: the vendored ontology slice already contains `ENVO:00002196` `aquarium`, `ENVO:00000294` `fish farm`, and `ENVO:03600074` `aquaculture farm`; the maintained `Fish tank` row was decided by a class-level negative lexical sweep that did not inspect these related terms.
- No record-level citations, characteristic taxa, environmental parameters, or causal edges are present, so there are no attached snippets or literature references to validate.

## Completeness

The record is structurally sparse but not missing any required generated field. It has the expected GOLD source attestation, carries `assertion_unit: ORGANISM` so the 11-count is interpretable, and correctly stays `SEEDED` because its only maintained review row is class-level.

The exact ignored-inclusive target searches covered `data/habitats/PATHS.tsv`, `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/term_requests/`, `history`, `research`, `conf/id_label_targets.yaml`, `curation/causal_graphs`, `reports/yaml_record_review`, and focused GOLD source inventories under `data/raw`. They found the class-level decision row, the stable slug row, the generated target, and the canonical raw GOLD row; they found no existing review report for this exact stem before this report was written, no target-specific causal overlay, no target-specific history entry, no term request, and no direct GOLD triad, biosample, or study side-table row.

The generated `data/habitats/engineered/sediment__cb692bfb.yaml` child is consistent with the same raw inventory: it denotes `Engineered > Artificial ecosystem > Aquaculture > Fish tank > Sediment`, hangs under both `ENVO:00002007` `sediment` and this Fish tank record, and has zero upstream assertions. That child does not provide additional evidence for the Fish tank identity.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `fish_tank.yaml` remains `UNGROUNDED` from a class-level sweep even though `Fish tank` is close enough to `ENVO:00002196` `aquarium` to require item-level grounding review. | The current `habitatmech:GOLD.1a33387187` row in `curation/decisions.tsv` is `review_depth: CLASS` and says habitathood was not assessed. The vendored slice has `ENVO:00002196` `aquarium`, `ENVO:00000294` `fish farm`, and `ENVO:03600074` `aquaculture farm`; exact ignored-inclusive searches found no item-level decision, term request, research report, history entry, label-correspondence exception, or causal overlay that adjudicates those terms for the GOLD path `Engineered > Artificial ecosystem > Aquaculture > Fish tank`. | `curation/decisions.tsv`; possibly `curation/term_requests.tsv` |

## Recommended Edits

1. Item-review `habitatmech:GOLD.1a33387187` in `curation/decisions.tsv` against the exact `Engineered > Artificial ecosystem > Aquaculture > Fish tank` source path, `ENVO:00002196` `aquarium`, `ENVO:00000294` `fish farm`, and `ENVO:03600074` `aquaculture farm`.
2. If `ENVO:00002196` is the correct exact identity or broader parent, replace the class-level `CONFIRM_UNGROUNDED` row with an item-level `GROUND` or `GROUND_AS_PARENT` decision.
3. If the aquaculture Fish tank concept remains distinct from `aquarium`, keep a minted identity with an item-level `CONFIRM_UNGROUNDED` decision and add a precise definition in `curation/term_requests.tsv` under a verified true genus.
4. Canary-regenerate `habitatmech:GOLD.1a33387187` and inspect `data/habitats/engineered/fish_tank.yaml` to confirm its `grounding_status`, parents, source attestation, and curation history follow from the maintained curation row.

## Follow-up Checks

- `just seed`
- `uv run python scripts/seed_from_sources.py --apply --force --only habitatmech:GOLD.1a33387187`
- Inspect `data/habitats/engineered/fish_tank.yaml`
- `just validate data/habitats/engineered/fish_tank.yaml`
- `just validate-strict data/habitats/engineered/fish_tank.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

## Additional Notes

- No blocker finding was found: the current generated YAML validates, the GOLD source attestation is internally consistent, and the record denotes an engineered aquaculture habitat.
- No minor findings were found.
