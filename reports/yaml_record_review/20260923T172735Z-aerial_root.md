# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/host_associated/aerial_root.yaml
- Started UTC: 2026-09-23T17:23:28Z
- Finished UTC: 2026-09-23T17:27:35Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | BTO:0001659 |
| Label | aerial root |
| Class | HabitatRecord |
| Category | HOST_ASSOCIATED |
| Grounding status | EXACT |
| Mapping status | SEEDED |
| Maintained owner | generated from the GOLD raw tables, BTO/PO ontology slices, and `data/habitats/PATHS.tsv` |

[data/habitats/host_associated/aerial_root.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/host_associated/aerial_root.yaml) is the generated GOLD/BTO record for `BTO:0001659` `aerial root`.

The target was resolved with ignored-file-inclusive searches:

- `find data/habitats -type f -name 'aerial_root.yaml' -print` found exactly one generated YAML record.
- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-aerial_root.md' -print` found no prior exact review report.
- `find curation/causal_graphs -maxdepth 1 -type f -name 'aerial_root.yaml' -print` found no causal-graph overlay for this record.
- `rg --no-ignore --hidden -n --glob '!data/text_map/**' --glob '!pages/**' --glob '!build/**' --glob '!reports/yaml_record_review/**' 'habitatmech:GOLD\.4c130cb0f9|BTO:0001659|Host-associated > Plants > Roots > Aerial root|aerial_root|gold\.ecosystem:7969|gold\.ecosystem:7970' curation history research reports data/habitats/PATHS.tsv` found only the `PATHS.tsv` slug lock; it found no item-level `curation/decisions.tsv` row, maintained causal overlay, history record, or aerial-root research report for this GOLD source concept.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/host_associated/aerial_root.yaml` | pass; no LinkML issues found |
| `just validate-strict data/habitats/host_associated/aerial_root.yaml` | pass; 1 file scanned, 0 files with ERROR, 0 total ERROR rows |
| `just validate-causal curation/causal_graphs/aerial_root.yaml` | not applicable; exact `find curation/causal_graphs -maxdepth 1 -type f -name 'aerial_root.yaml' -print` found no overlay |
| `just validate-causal-all` | pass; 32 causal-graph curation files with 32 graphs validated |
| Reference validator | not applicable; this generated GOLD/BTO record has no causal graph and no DOI, PMID, or URL evidence slots to inspect |
| `just term-requests-check` | pass; term-request table is current with 109 terms |
| `just validate-history` | pass; no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | pass; 3206 expected, 3206 found, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | pass; 0 ungrounded records still undecided; 1810 decisions on file |
| `just report` | pass; 3206 records; no global issue specific to `BTO:0001659`, `gold.ecosystem:7969`, or `gold.ecosystem:7970` surfaced |
| `git diff --check` | pass |

## Identity and Grounding

The exact identity is supported. `data/raw/ontology_terms.tsv` gives `BTO:0001659` primary label `aerial root` and definition `An aerial root is a plant's root that is produced above the ground.`, matching the generated identifier, label, definition, and `definition_source: BTO`.

The GOLD source concept is real and resolves to the same plant-root habitat. `data/raw/gold_ecosystem_paths.tsv` has the canonical path `Host-associated > Plants > Roots > Aerial root` at depth 4 with `leaf_label` `Aerial root`, 2 collapsed GOLD node ids, 1 upstream organism assertion, and first node id `gold.ecosystem:7969`. The deterministic source-concept key is `habitatmech:GOLD.4c130cb0f9`.

`mapping_status: SEEDED` is expected. Exact ignored-file-inclusive search found no item-level curation decision for `habitatmech:GOLD.4c130cb0f9`, so `src/habitatmech/seed.py` preserved the seeded exact match to `BTO:0001659`.

The host-associated category is coherent because `aerial root` denotes a plant root exposed above the ground and can act as a plant-associated microbial habitat.

The three broader parents are traceable and strictly broader:

- `BTO:0001188` and `BTO:0001658` are the two vendored BTO superclass edges of `BTO:0001659` in `data/raw/ontology_subclass_edges.tsv`.
- `PO:0009005` comes through the immediate GOLD parent path `Host-associated > Plants > Roots`, whose GOLD environment triad maps the `medium` slot to Plant Ontology `root`.

The `source_attestations` note is also faithful. The seeder collapses the two upstream GOLD nodes on the canonical path, records `gold.ecosystem:7969` as the first node id, and explains that `gold.ecosystem:7970` shares the same path in `data/raw/gold_ecosystem_paths.tsv`.

## Evidence

This record has one GOLD source attestation:

- The `GOLD` attestation mirrors `data/raw/gold_ecosystem_paths.tsv`: source id `gold.ecosystem:7969`, source label `Aerial root`, source path `Host-associated > Plants > Roots > Aerial root`, predicate `skos:exactMatch`, and 1 upstream organism assertion with unit `ORGANISM`.
- The raw row's `gold_node_count` is `2` and its `gold_node_ids` field is `gold.ecosystem:7969|gold.ecosystem:7970`, matching the generated de-duplication note.
- The child raw path `Host-associated > Plants > Roots > Aerial root > Mucilage` is present as a narrower GOLD concept and should not be emitted as a `parent_habitats` value on the broader `aerial root` record.

No evidence-backed causal mechanism evidence is present.

Unsupported or over-scoped claims:

None found.

## Completeness

The record carries all generated source-owned GOLD content for this source concept: the first GOLD ecosystem id, source label, source path, source assertion count, duplicate-node note, BTO definition, three broader parents, generated slug lock, and generated seeding history.

Optional characteristic-taxon, environmental-parameter, discussion, dataset, and causal-graph slots are correctly absent; no maintained causal overlay, history entry, or curated review row adds any of those fields.

## Findings

None found.

## Recommended Edits

None required.

## Follow-up Checks

No corrective follow-up is required. If curators later add an item-level `REVIEW` decision for `habitatmech:GOLD.4c130cb0f9`, the narrow proof path would be:

- `just seed`
- `just seed-canary BTO:0001659`
- inspect `data/habitats/host_associated/aerial_root.yaml` and confirm `mapping_status` changes from `SEEDED` to `REVIEWED` without changing the generated BTO or GOLD content
- `just validate-strict data/habitats/host_associated/aerial_root.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- ignored-file-inclusive exact searches for `BTO:0001659` and `habitatmech:GOLD.4c130cb0f9` to confirm the new decision row is the maintained owner of the reviewed status
- `git diff --check`

## Additional Notes

No additional notes.
