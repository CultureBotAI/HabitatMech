# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/abscess.yaml`
- Started UTC: 2026-09-22T22:40:00Z
- Finished UTC: 2026-09-22T22:45:42Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/host_associated/abscess.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.5d784b0218` |
| Label | `Abscess` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Record status | Generated from one GOLD source concept with no item-level curation decision |

This is the generated record for the GOLD path `Host-associated > Mammals: Human > Integumentary system > Skin > Abscess`. The GOLD source-concept key is `habitatmech:GOLD.5d784b0218`, computed from `sha1("GOLD:Host-associated > Mammals: Human > Integumentary system > Skin > Abscess")[:10]`. The seeder keeps it minted because the generic `mesh:D000038` `Abscess` leaf match is broader than this skin-specific GOLD path and is also shared by several other GOLD abscess paths.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/abscess.yaml` | Passed; LinkML validation reported no issues. |
| `just validate-strict data/habitats/host_associated/abscess.yaml` | Passed; one file scanned with 0 error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 generated records, found 3206, with 0 missing, 0 extra, and 0 differing. |
| `just worklist` filtered for `Abscess`, `abscess`, `GOLD\.5d784b0218`, and `mesh:D000038` | Completed with no matching rows, as expected for a seeded `NARROW` GOLD record that is not in the class-level ungrounded backlog. |
| `just report` | Passed; the report regenerated and left the reviewed concept out of the risky-grounding, stale-sweep, current non-habitat, same-label, GOLD-triad, unsupported-prefix, and organism-identity sections. |
| `git diff --check` | Passed; no whitespace errors. |

## Identity and Grounding

The GOLD source identity is supported:

| Raw field | Value |
|---|---|
| `canonical_path` | `Host-associated > Mammals: Human > Integumentary system > Skin > Abscess` |
| `ecosystem` | `Host-associated` |
| `ecosystem_category` | `Mammals: Human` |
| `ecosystem_type` | `Integumentary system` |
| `ecosystem_subtype` | `Skin` |
| `specific_ecosystem` | `Abscess` |
| `leaf_label` | `Abscess` |
| `depth` | `5` |
| `gold_node_count` | `1` |
| `organism_count` | `17` |
| `study_count` | `0` |
| `biosample_count` | `0` |
| `total_assertions` | `17` |
| `gold_node_ids` | `gold.ecosystem:6342` |

`source_attestations` matches `data/raw/gold_ecosystem_paths.tsv:448`: the record keeps `gold.ecosystem:6342` as the source id, records the `Abscess` leaf label and canonical GOLD path exactly, records `assertion_count: 17` with `assertion_unit: ORGANISM`, and omits a multi-node note because the raw row has a single GOLD node id.

The `habitatmech:GOLD.4b37ab4b76` parent is supported. `data/raw/gold_ecosystem_paths.tsv:49` records the immediate parent path `Host-associated > Mammals: Human > Integumentary system > Skin`, and `data/habitats/host_associated/skin__621b7871.yaml` is the generated record for that path.

The `mesh:D000038` parent is supported. `data/raw/ontology_terms.tsv:13554` defines `mesh:D000038` with label `Abscess`, and this skin abscess source concept is strictly narrower than a generic abscess. `data/raw/gold_ecosystem_paths.tsv` contains other `Abscess` leaves at abdominal/peritoneal, nasal-cavity, brain, spinal-cord, joint-capsule, and soft-tissue paths, so the seeder's ambiguous-leaf rule correctly prevents this one path from adopting `mesh:D000038` as an exact identity.

`grounding_status: NARROW` is mechanically expected for an uncurated GOLD source path whose leaf label matches a broader ontology term but is not the unique defensible claimant of that term. `mapping_status: SEEDED` is also expected: an ignored/hidden-inclusive exact search for `habitatmech:GOLD.5d784b0218` found no maintained decision, term-request, term-request-exclusion, causal-graph, or history row for the source concept.

## Evidence

| Claim | Nearest support | Review |
|---|---|---|
| The generated record represents the GOLD path `Host-associated > Mammals: Human > Integumentary system > Skin > Abscess`. | `data/raw/gold_ecosystem_paths.tsv:448` | Supported exactly. |
| The GOLD source row contributes 17 organism assertions from one GOLD ecosystem node id. | `data/raw/gold_ecosystem_paths.tsv:448` | Supported exactly. |
| `habitatmech:GOLD.4b37ab4b76` is the source-path parent. | `data/raw/gold_ecosystem_paths.tsv:49`; `data/habitats/host_associated/skin__621b7871.yaml` | Supported. A skin abscess is narrower than the parent skin GOLD path. |
| `mesh:D000038` `Abscess` is broader than the skin-specific GOLD source concept. | `data/raw/ontology_terms.tsv:13554`; `data/raw/gold_ecosystem_paths.tsv:448` | Supported. The source path supplies the skin context that makes this a narrower abscess record rather than the generic abscess identity. |
| The NARROW disposition was sampled as correct during the committed 2026-08-14 `NARROW` sample. | `curation/samples/narrow-20260814.tsv:10` | Supported as corroborating context only. The sample is not an item-level curation decision and does not promote the record to `REVIEWED`. |

Unsupported or over-scoped claims: None found. The record carries only GOLD source provenance, the generic abscess parent, the GOLD source-path parent, and seeded audit metadata. It does not claim characteristic taxa, environmental parameters, causal mechanisms, or literature-backed definition text.

## Completeness

The direct GOLD source attestation is complete for a 17-organism, one-node raw row: the record carries the source name, source id, source label, source path, mapping predicate, assertion count, and assertion unit, while correctly omitting the multi-node note used for collapsed paths.

Separate committed context does not imply a missing maintained field. The GOLD path has no current row in `data/raw/gold_path_biosamples.tsv` or `data/raw/gold_path_triads.tsv`, the lesion and tubercular deep-research reports mention Abscess only as a neighbouring concept, and no curator-authored Abscess definition or causal overlay has been committed.

Ignored/hidden-inclusive exact searches found no maintained `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/term_requests_excluded.tsv`, `curation/causal_graphs`, or `history` row for `habitatmech:GOLD.5d784b0218`; generated `data/text_map`, `pages`, and `build` outputs were excluded only from broad exact content searches.

No maintained causal overlay, history file, raw research report, or prior exact YAML review report was found by filename for `abscess` under `curation/causal_graphs`, `history`, `research/habitats`, or `reports/yaml_record_review`; the `find` search included ignored files beneath those checked directories. A broader pre-report `find reports/yaml_record_review -maxdepth 1 -type f -name '*abscess*' -print` found only the already committed `breast_abscess` report.

## Findings

None found.

No blocker findings.

No major findings.

No minor findings.

## Recommended Edits

None required.

If a future curator wants to mark the GOLD narrow leaf review-complete, add a `REVIEW` row for `habitatmech:GOLD.5d784b0218` in `curation/decisions.tsv` and rerun `just seed-canary habitatmech:GOLD.5d784b0218`. That should be a status-only change: keep the minted identifier, keep `grounding_status: NARROW`, and keep both existing parents.

## Follow-up Checks

None required beyond the validators already run for this report.

For any future status-promotion-only edit, run:

- `just seed-canary habitatmech:GOLD.5d784b0218`
- `just validate data/habitats/host_associated/abscess.yaml`
- `just validate-strict data/habitats/host_associated/abscess.yaml`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

## Additional Notes

- The pre-report exact filename check `find reports/yaml_record_review -maxdepth 1 -type f -name '*-abscess.md' -print` returned no previous exact report; `find` includes ignored files.
- `just worklist` did not list `habitatmech:GOLD.5d784b0218`, `mesh:D000038`, or `Abscess`, consistent with a seeded `NARROW` GOLD leaf rather than an undecided ungrounded source concept.
- `just report` completed successfully and did not flag `mesh:D000038` or `habitatmech:GOLD.5d784b0218` in any risky-grounding, stale-sweep, current non-habitat, same-label, GOLD-triad-compatibility, minted-GOLD-triad, unsupported-prefix, or organism-identity section.
