# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/food/utensil.yaml
- Started UTC: 2026-09-24T11:34:45Z
- Finished UTC: 2026-09-24T11:34:45Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `HabitatRecord` |
| Identifier | `ENVO:03501327` |
| Label | `utensil` |
| Category | `FOOD` |
| Source concept | `habitatmech:GOLD.35cefb585d` |
| Source path | `Engineered > Food production > Equipment/Utensils` |
| Maintained owner | The reviewed grounding is the row for `habitatmech:GOLD.35cefb585d` in `curation/decisions.tsv`; the false `FOODON:03530206` parent is inherited through GOLD source-path parent logic in `src/habitatmech/seed.py`. |

The generated record grounds GOLD's `Equipment/Utensils` node to ENVO `utensil` with `grounding_status: CLOSE`, `mapping_predicate: skos:closeMatch`, and `mapping_status: REVIEWED`.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/habitats/food/utensil.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/food/utensil.yaml` | Pass: scanned 1 file with 0 files containing `ERROR` and 0 total `ERROR` rows. |
| `just validate-causal curation/causal_graphs/<overlay>.yaml` | Not applicable: a gitignore-independent `rg --no-ignore --hidden` search for `ENVO:03501327`, `habitatmech:GOLD.35cefb585d`, `GOLD.35cefb585d`, `gold.ecosystem:8314`, `gold.ecosystem:8315`, `gold.ecosystem:8316`, `Engineered > Food production > Equipment/Utensils`, and `utensil` under `curation/causal_graphs`, `curation/term_requests.tsv`, `history`, `research`, and `reports/yaml_record_review` found no curated causal overlay for this record, and the target has no `causal_graphs` block. |
| `just validate-causal-all` | Pass: validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Pass: the term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: expected 3,206 records, found 3,206 on disk, and the corpus reproduces exactly from `data/raw/`. |
| `just worklist --limit 2000` | Pass: reported 0 undecided ungrounded records and 1,810 decisions on file. |
| `just report` | Pass: completed the corpus report for 3,206 records. |
| `git diff --check` | Pass before writing this report. |
| Reference validator | Not applicable: this record has no record-level `evidence`, `characteristic_taxa`, `causal_graphs`, `discussions`, or `datasets` with citation-bearing `EvidenceItem` references. |

## Identity and Grounding

`ENVO:03501327` is present in `data/raw/ontology_terms.tsv` as `utensil` with definition `A hand tool for domestic use, especially in the kitchen.` Its ontology parent is supported: `data/raw/ontology_subclass_edges.tsv` asserts `ENVO:03501327 rdfs:subClassOf ENVO:03501263`, and `data/raw/ontology_terms.tsv` labels `ENVO:03501263` as `hand tool`.

The GOLD source path `Engineered > Food production > Equipment/Utensils` maps to one canonical GOLD row with three GOLD node IDs, one direct organism assertion, and no committed GOLD study, biosample, or MIxS triad side-channel rows for the exact source path.

The item-level curation row for `habitatmech:GOLD.35cefb585d` grounds that source concept to ENVO `utensil` with `CLOSE` status, but its own note scopes the target too narrowly by saying "Utensils are a subset of the equipment this node covers." A subset term is not a defensible identity for the whole `Equipment/Utensils` source bin. The generated record therefore over-narrows the GOLD source to a domestic hand tool and also writes `Equipment/Utensils` as an `EXACT_SYNONYM` of `utensil`, even though the slash label includes non-utensil equipment.

The extra `FOODON:03530206` parent is also false for this record. `FOODON:03530206` is present in `data/raw/ontology_terms.tsv` as `food production` with definition `A planned process involving the rearing, manufacture or distribution of food material.` `parent_habitats` is an `is-a` assertion, and a utensil or equipment item is not a kind of planned process. The edge is generated because GOLD places `Equipment/Utensils` below `Engineered > Food production`; that source tree context is useful provenance, not a strictly broader habitat for the generated ENVO record.

## Evidence

The ENVO identifier, label, definition, and `hand tool` parent trace to committed ontology inventories: `data/raw/ontology_terms.tsv` and `data/raw/ontology_subclass_edges.tsv`.

The GOLD source attestation traces to `data/raw/gold_ecosystem_paths.tsv`, where `Engineered > Food production > Equipment/Utensils` has `gold_node_count=3`, `organism_count=1`, `total_assertions=1`, and `gold_node_ids=gold.ecosystem:8314|gold.ecosystem:8315|gold.ecosystem:8316`. The generated source attestation correctly carries the first source ID, the `Equipment/Utensils` source label, the exact source path, `assertion_count: 1`, and `assertion_unit: ORGANISM`.

The curation history traces to `curation/decisions.tsv`: source concept `habitatmech:GOLD.35cefb585d` has a `GROUND` decision, target `ENVO:03501327`, target label `utensil`, `CLOSE` status, curator `claude-opus-5`, date `2026-08-12`, and `review_depth: ITEM`.

## Completeness

- A gitignore-independent exact search for `ENVO:03501327`, `Equipment/Utensils`, `GOLD.35cefb585d`, `gold.ecosystem:8314`, `gold.ecosystem:8315`, `gold.ecosystem:8316`, and `utensil` across `data/raw`, `curation`, `data/habitats`, `history`, `research`, and `reports/yaml_record_review` found the expected path-lockfile row, GOLD source row, item-level curation decision, ontology rows, and generated target record.
- The same ignored-file-inclusive target search over `curation/causal_graphs`, `curation/term_requests.tsv`, `history`, `research`, and `reports/yaml_record_review` found no term request, curated causal overlay, dedicated history record, target-specific research report, or prior review for this target.
- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-utensil.md' -print` found no prior top-level review report for the same record stem; `find` includes ignored files.
- Optional `environmental_parameters`, `characteristic_taxa`, `evidence`, `causal_graphs`, `discussions`, and `datasets` are correctly absent. The record only has one GOLD organism-count attestation and no claim-level maintained overlay.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | `habitatmech:GOLD.35cefb585d` is grounded to `ENVO:03501327` `utensil`, but `utensil` is narrower than GOLD's `Equipment/Utensils` source concept. | The curation row acknowledges that utensils are only a subset of the equipment this node covers. ENVO defines `utensil` as a domestic hand tool; GOLD's slash label also includes broader food-production equipment. Because the `GROUND` decision makes the generated record's canonical identifier `ENVO:03501327`, the record silently turns the full source bin into only the utensil subset and adds `Equipment/Utensils` as an exact synonym. | Change the `habitatmech:GOLD.35cefb585d` row in `curation/decisions.tsv`. ENVO `utensil` and `hand tool` are both too narrow for the full source; `ENVO:00003074` `manufactured product` is a safer broader parent if the source remains a single minted equipment-or-utensil record. |
| Major | `parent_habitats` contains `FOODON:03530206` `food production`, which is a process rather than a strictly broader habitat or object. | The false edge is inherited from GOLD's `Engineered > Food production` source parent. The FoodOn term's vendored definition is `A planned process involving the rearing, manufacture or distribution of food material.`, so a utensil or equipment item is not an instance of that parent. | Add a maintained way to suppress or replace false GOLD source-path parents, or otherwise prevent the GOLD `Food production` source concept from publishing the FOODON process as a `parent_habitats` edge on food products and food-production equipment. Do not hand-edit `data/habitats/food/utensil.yaml`. |

No blocker findings found.

No minor findings found.

## Recommended Edits

1. Replace the `GROUND ENVO:03501327 utensil CLOSE` decision for `habitatmech:GOLD.35cefb585d` with a decision that keeps the GOLD source minted and retains a true broader object parent, likely `GROUND_AS_PARENT ENVO:00003074 manufactured product BROAD`; the note should state that both ENVO `utensil` and `hand tool` are narrower than the equipment side of `Equipment/Utensils`.
2. Add a maintained parent-suppression or parent-override path for false GOLD parent-path edges and use it to remove `FOODON:03530206` from `habitatmech:GOLD.35cefb585d`'s generated record.
3. Re-seed with pruning because the corrected identity will stop emitting `ENVO:03501327` and leave `data/habitats/food/utensil.yaml` stale otherwise.
4. Add a regression test that the corrected record is still attested by `gold.ecosystem:8314`, keeps `ENVO:00003074` in `parent_habitats`, and does not emit `FOODON:03530206`, `ENVO:03501263`, or `ENVO:03501327` as parents or identity.

## Follow-up Checks

| Edit | Proof |
| --- | --- |
| Replace the over-narrow ENVO `utensil` identity | `just seed-canary habitatmech:GOLD.35cefb585d --force`, followed by `rg --no-ignore --hidden -n -F 'identifier: habitatmech:GOLD.35cefb585d' data/habitats` and an inspection of the generated record showing label `Equipment/Utensils`, `grounding_status: NARROW`, a GOLD source attestation for `gold.ecosystem:8314`, and no `ENVO:03501327` identity. |
| Drop the false `food production` parent | The same forced canary should show `ENVO:00003074` in `parent_habitats` and no `FOODON:03530206`. |
| Preserve corpus reproduction after the identity move | `just seed-apply --force --prune` and `just verify-corpus --max-diffs 1`. |
| Validate the focused record and new override | `just validate data/habitats/food/<new-slug>.yaml`, `just validate-strict data/habitats/food/<new-slug>.yaml`, and the new parent-override validator or regression test added with the maintained input. |
| Confirm no unrelated curation loss | `just term-requests-check`, `just validate-history`, `just worklist --limit 2000`, and `git diff --check`. |

## Additional Notes

- The GOLD `Engineered > Food production > Equipment/Utensils` source path has no exact rows in `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, or `data/raw/gold_studies.tsv`; only the aggregate `gold_ecosystem_paths.tsv` row is available locally for its one organism assertion.
- A prior `sourdough` deep-research report already flagged `FOODON:03530206` as a process-valued parent inherited from GOLD's food-production path, so the false parent is almost certainly a systemic food-branch issue rather than a utensil-only problem.
