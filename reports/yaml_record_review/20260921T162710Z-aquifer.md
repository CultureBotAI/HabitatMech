# YAML Record Review: aquifer

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/aquifer.yaml
- Started UTC: 2026-09-21T16:26:47Z
- Finished UTC: 2026-09-21T16:27:10Z
- Verdict: needs curation

## Target

The target is the generated `HabitatRecord` at `data/habitats/aquatic/aquifer.yaml`.

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `ENVO:00012408` |
| Label | `aquifer` |
| Category | `AQUATIC` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Source concepts | GOLD `habitatmech:GOLD.9f7a6c66a7`; PREGO `habitatmech:PREGO.64c780a48c` for source ID `ENVO:00012408` |
| Locked stem | `aquifer` in `data/habitats/PATHS.tsv` |

This review covers the ENVO `aquifer` record fed by PREGO `ENVO:00012408` and GOLD `Environmental > Aquatic > Freshwater > Groundwater > Aquifer`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/aquifer.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/aquifer.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 records on disk, 0 missing, 0 extra, 0 differing. |
| Reference validator | Not checked: the generated record has no DOI/PMID/URL `EvidenceItem`, no causal-edge evidence, and no curated causal overlay. |

## Identity and Grounding

| Claim | Evidence | Assessment |
|---|---|---|
| The generated record denotes ENVO `aquifer`. | `data/raw/ontology_terms.tsv` lists active term `ENVO:00012408` with label `aquifer` and the same definition emitted in the YAML. `data/raw/ontology_subclass_edges.tsv` places the term directly under `ENVO:01001275` `solid layer`. | Supported exactly. |
| PREGO attests the same ENVO concept. | `data/raw/prego_habitats.tsv` row `ENVO:00012408` lists ontology `ENVO`, category `biolink:OntologyClass`, `taxon_count=395`, `direct_assertion_count=396`, `max_prego_score=4`, channels `annotated_genomes_isolates\|environmental_samples`, and synonyms `aquifer\|aquifers`. | Supported exactly. |
| GOLD's `Aquifer` source concept is grounded to the ENVO `aquifer` term. | `data/raw/gold_ecosystem_paths.tsv` has canonical path `Environmental > Aquatic > Freshwater > Groundwater > Aquifer`, leaf `Aquifer`, depth `5`, one GOLD node ID `gold.ecosystem:5430`, and five organism assertions. `curation/decisions.tsv` has an `ITEM` `GROUND` row for `habitatmech:GOLD.9f7a6c66a7` to `ENVO:00012408` with `EXACT` grounding. | Supported exactly. |
| The August 20 GOLD curation note uses MIxS local-scale evidence correctly. | `data/raw/gold_path_triads.tsv` records five samples across four studies for the same GOLD path with all four local-scale studies asserting `ENVO:00012408` `aquifer`; the same row records `ENVO:01001004` `groundwater` as the unanimously asserted medium, not the local feature. | Supported for the exact GOLD grounding. |
| The generated `mapping_status: SEEDED` reflects incomplete item review. | Hidden/ignored-inclusive exact search across `curation/decisions.tsv`, `history`, `curation/causal_graphs`, prior YAML review reports, `data/raw`, and `data/habitats/PATHS.tsv` found the GOLD item decision above and no decision row keyed by PREGO's minted `habitatmech:PREGO.64c780a48c` source concept. | Supported exactly. |
| `habitatmech:GOLD.7f8e268653` `Groundwater` is a broader habitat for ENVO `aquifer`. | This parent is generated from the GOLD source path parent `Environmental > Aquatic > Freshwater > Groundwater`; `src/habitatmech/seed.py` links every GOLD child concept to the resolved identifier of its parent path after identity merging. ENVO instead defines aquifer as a water-bearing solid layer, and the GOLD path's own triad row treats groundwater as the sampled medium of the aquifer habitat. | Needs curation; the generated parent is path context or containment, not a strict is-a edge. |

## Evidence

The generated record has no record-level `evidence` entries and no `causal_graphs`.

| Claim | Evidence | Assessment |
|---|---|---|
| `aquifers` is a related PREGO source synonym for `aquifer`. | `data/raw/prego_habitats.tsv` lists `aquifer\|aquifers` for `ENVO:00012408`; the generated synonym carries `RELATED_SYNONYM` with source `PREGO`. | Supported exactly. |
| The GOLD source attestation preserves the upstream GOLD leaf and count unit. | `data/raw/gold_ecosystem_paths.tsv` has `gold.ecosystem:5430` on the `Environmental > Aquatic > Freshwater > Groundwater > Aquifer` path and reports `organism_count=5`, which the generated `source_attestations` entry carries as `assertion_count: 5` and `assertion_unit: ORGANISM`. | Supported exactly. |
| The PREGO source attestation preserves the source-provided taxon count and channels. | `data/raw/prego_habitats.tsv` reports 395 taxa, a maximum PREGO score of 4, and `annotated_genomes_isolates\|environmental_samples`; those values are reflected in the generated PREGO attestation. | Supported exactly. |
| The 25 PREGO taxon rows match the maintained source ranking. | `data/raw/prego_habitat_taxa.tsv` rows for `ENVO:00012408` list ranks 1 through 25 with the same `NCBITaxon` IDs, labels, and scores emitted under `characteristic_taxa`. Rank 16 has a blank source label and is correspondingly emitted without `taxon_label`. | Supported as source associations, not as curator-asserted `is_characteristic` taxa. |

## Completeness

- The record is complete enough for the seeded ENVO/PREGO/GOLD identity: it has an ENVO definition, direct ENVO ontology parent, two source attestations, PREGO's plural source synonym, PREGO's top 25 taxon associations, a generated seed event, and the generated event for GOLD's item-level `GROUND` decision.
- The remaining status gap is explicit: PREGO's minted `habitatmech:PREGO.64c780a48c` source concept for raw source ID `ENVO:00012408` has not been item-reviewed in `curation/decisions.tsv`, so `mapping_status: SEEDED` is accurate for the partial source review.
- The only consequential semantic gap found is the inherited GOLD source-path parent: `parent_habitats` currently makes ENVO `aquifer` a child of GOLD `Groundwater`.
- A filesystem `find` search under `curation/causal_graphs` for `*aquifer*` found no maintained causal overlay; the empty generated `causal_graphs` slot is therefore expected.
- Hidden/ignored-inclusive exact searches covered `curation`, `history`, `data/raw`, `data/habitats/PATHS.tsv`, `curation/causal_graphs`, and prior `reports/yaml_record_review` reports for `ENVO:00012408`, `gold.ecosystem:5430`, `habitatmech:GOLD.9f7a6c66a7`, `habitatmech:GOLD.7f8e268653`, `habitatmech:PREGO.64c780a48c`, `Aquifer`, and `aquifer`. They found the target path lock, the GOLD grounding decision, the source inventory rows, the ontology term and subclass edge, related aquifer-matrix curation for `Rock core/Sediment`, no decision row for the minted PREGO source concept, and no prior exact YAML review report for `aquifer`.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| `HM-AQUIFER-001` | Major | The exact ENVO `aquifer` record inherits a false GOLD source-path parent. `parent_habitats` asserts `ENVO:00012408` is a child of `habitatmech:GOLD.7f8e268653` `Groundwater`, but ENVO defines an aquifer as a water-bearing solid geologic layer and makes it a subclass of `ENVO:01001275` `solid layer`; the GOLD path classifies aquifer under groundwater because groundwater is the medium held in the aquifer. | `data/habitats/aquatic/aquifer.yaml` lists `habitatmech:GOLD.7f8e268653`; `data/raw/ontology_subclass_edges.tsv` lists only `ENVO:01001275` as the direct ENVO parent of `ENVO:00012408`; `data/raw/gold_path_triads.tsv` records `local = ENVO:00012408 aquifer` and `medium = ENVO:01001004 groundwater` for the GOLD path; `src/habitatmech/seed.py` adds the GOLD parent-path edge after exact identity merging. | No existing row-level curation input can suppress this parent while retaining the correct exact GOLD grounding. The fix belongs in a GOLD parent-edge suppression path consumed by `src/habitatmech/seed.py`. |

No blocker or minor findings found.

## Recommended Edits

1. Add a curator-owned parent-edge suppression for the generated `ENVO:00012408 -> habitatmech:GOLD.7f8e268653` edge while preserving `curation/decisions.tsv` row `habitatmech:GOLD.9f7a6c66a7 -> ENVO:00012408`.
2. Regenerate the ENVO aquifer record with a canary and inspect `data/habitats/aquatic/aquifer.yaml` before any wider seed run.
3. Separately review the PREGO `habitatmech:PREGO.64c780a48c` source concept for `ENVO:00012408` and add an item-level `REVIEW` row only if the source identity is confirmed; that review should promote the generated record from `SEEDED` to `REVIEWED`.

## Follow-up Checks

| Edit | Narrowest proving check |
|---|---|
| Suppress the false GOLD parent edge. | Re-read the maintained input row for the suppression, then run `just seed` to confirm the concept counts and planned mapping are stable. |
| Regenerate the aquifer record. | `just seed-canary ENVO:00012408`, then inspect `data/habitats/aquatic/aquifer.yaml` and confirm `parent_habitats` keeps `ENVO:01001275` and drops `habitatmech:GOLD.7f8e268653`. |
| Promote PREGO's source concept to an item-level review. | Re-read the new `curation/decisions.tsv` row and confirm the regenerated `curation_history` has one generated event for GOLD and one generated event for PREGO. |
| Validate the regenerated record and corpus. | `just validate data/habitats/aquatic/aquifer.yaml`, `just validate-strict data/habitats/aquatic/aquifer.yaml --quiet`, `just term-requests-check`, `just validate-history`, and `just verify-corpus --max-diffs 1`. |

## Additional Notes

- The false parent edge is about the exact ENVO `aquifer` merge, not the GOLD `Rock core/Sediment` child. `curation/term_requests.tsv` correctly defines `habitatmech:GOLD.20fb5b0162` as `aquifer rock or sediment` under `ENVO:00010483` `environmental material` with `parent_mode=REPLACE` so it does not inherit the GOLD `Aquifer` path as an is-a parent.
- The generated `habitatmech:GOLD.7f8e268653` `Groundwater` record is itself a NARROW GOLD concept under `Environmental > Aquatic > Freshwater > Groundwater`; the false edge is therefore from ENVO `aquifer` to a source-derived GOLD context, not from ENVO `aquifer` to the ontology term `ENVO:01001004` directly.
