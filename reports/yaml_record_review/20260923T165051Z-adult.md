# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/host_associated/adult.yaml
- Started UTC: 2026-09-23T16:47:25Z
- Finished UTC: 2026-09-23T16:50:51Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | BTO:0001043 |
| Label | adult |
| Class | HabitatRecord |
| Category | HOST_ASSOCIATED |
| Grounding status | EXACT |
| Mapping status | SEEDED |
| Maintained owner | generated from the BTO ontology slice, PREGO raw tables, and `data/habitats/PATHS.tsv`; PREGO self-grounding and BTO subclass parents are added by `src/habitatmech/seed.py` |

[data/habitats/host_associated/adult.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/host_associated/adult.yaml) is the generated PREGO/BTO record for `BTO:0001043` `adult`.

The target was resolved with ignored-file-inclusive searches:

- `find data/habitats -type f -name 'adult.yaml' -print` found exactly one generated YAML record.
- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-adult.md' -print` found no prior exact review report.
- `find curation/causal_graphs -maxdepth 1 -type f -name 'adult.yaml' -print` found no causal-graph overlay for this record.
- `rg --no-ignore --hidden -n 'BTO:0001043|habitatmech:PREGO\.57e0a5faae|adult stage|adult stages' curation history research reports data/habitats/PATHS.tsv -g '!reports/yaml_record_review/**' -g '!data/text_map/**' -g '!pages/**' -g '!build/**'` found only the `PATHS.tsv` slug lock and incidental mentions of the phrase `adult stages` in unrelated pupa and embryo research reports. It found no `curation/decisions.tsv` row, maintained causal overlay, history record, or adult-specific research report for this PREGO source concept.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/host_associated/adult.yaml` | pass; no LinkML issues found |
| `just validate-strict data/habitats/host_associated/adult.yaml` | pass; 1 file scanned, 0 files with ERROR, 0 total ERROR rows |
| `just validate-causal curation/causal_graphs/adult.yaml` | not applicable; exact `find curation/causal_graphs -maxdepth 1 -type f -name 'adult.yaml' -print` found no overlay |
| `just validate-causal-all` | pass; 32 causal-graph curation files with 32 graphs validated |
| Reference validator | not applicable; this generated PREGO/BTO record has no causal graph and no DOI, PMID, or URL evidence slots to inspect |
| `just term-requests-check` | pass; term-request table is current with 109 terms |
| `just validate-history` | pass; no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | pass; 3206 expected, 3206 found, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | pass; 0 ungrounded records still undecided; 1810 decisions on file |
| `just report` | pass; 3206 records; no global issue specific to `BTO:0001043` surfaced |
| `git diff --check` | pass |

## Identity and Grounding

The current exact identity is unsupported. `data/raw/ontology_terms.tsv` gives `BTO:0001043` primary label `adult` and definition `Fully developed and mature: GROWN-UP.`; `data/raw/ontology_subclass_edges.tsv` asserts `BTO:0001043 rdfs:subClassOf BTO:0000284`, and `data/raw/ontology_terms.tsv` labels `BTO:0000284` as `organism form`. These terms describe a developmental or organism-form state rather than a microbial habitat.

The source attestation is real but not item-reviewed. `data/raw/prego_habitats.tsv` has a direct PREGO row for `BTO:0001043` with source synonyms `adult|adult stage|adult stages|adults`, `taxon_count` `378`, `direct_assertion_count` `387`, max PREGO score `4`, and channels `annotated_genomes_isolates|environmental_samples`. The source concept key is the deterministic hash `habitatmech:PREGO.57e0a5faae`, and exact ignored-file-inclusive search found no `curation/decisions.tsv` row for it.

`mapping_status: SEEDED` is therefore expected. `src/habitatmech/seed.py` self-grounds every PREGO concept to its own ontology CURIE until an item-level curation decision addresses that minted PREGO source concept.

## Evidence

This record has one PREGO source attestation and 25 emitted PREGO taxon associations:

- The `PREGO` attestation mirrors `data/raw/prego_habitats.tsv`: source id `BTO:0001043`, label `adult`, 378 distinct taxa, max score `4`, and channels `annotated_genomes_isolates|environmental_samples`. The `direct_assertion_count` value is `387`, but the seeder intentionally emits PREGO `taxon_count` as `assertion_count` with unit `TAXON`.
- `data/raw/prego_habitat_taxa.tsv` rows `1540` through `1564` are the top 25 PREGO taxa for `BTO:0001043`; each emitted row preserves the raw NCBITaxon id, rank, PREGO score `4`, and candidate pool `378`.
- The missing `taxon_label` values for `NCBITaxon:541000` and `NCBITaxon:629395` mirror blank labels in `data/raw/prego_habitat_taxa.tsv` rather than dropped generated content.

Exact ignored-file-inclusive search found no matching GOLD, BacDive, Madin, or environments-table raw rows for `BTO:0001043`, so the single PREGO attestation is the whole source inventory for this record.

No evidence-backed causal mechanism evidence is present.

Unsupported or over-scoped claims:

- Exact identity to `BTO:0001043` makes the HabitatRecord denote an adult life stage rather than a habitat.
- The `BTO:0000284` parent is an organism-form superclass, not a broader habitat.
- The PREGO taxa are faithful source associations, but they are currently attached to a record whose identity is not a habitat.

## Completeness

The record carries all generated source-owned PREGO content for this source concept: the source id, source label, PREGO taxon count, assertion unit, max score, evidence channels, PREGO synonyms, top 25 PREGO taxon rows, BTO definition, BTO superclass parent, generated slug lock, and generated seeding history.

The missing maintained item-level decision is consequential. The repository has already reviewed comparable host life-stage source concepts and decided that a larva, pupa, prepupa, or embryo is a whole organism at a stage rather than a microbial habitat identity. `adult` has the same shape but remains in the default PREGO self-grounded state.

Ignored/hidden-inclusive exact searches covered the target BTO id, the PREGO minted source-concept id, PREGO adult-stage synonyms, the BTO organism-form parent, the target slug, and the exact review-report/causal-overlay filenames. They found the cited BTO/PREGO rows, PATHS lock, generated record, and related life-stage precedent rows in `curation/decisions.tsv`, with no maintained input row that should already have changed this target.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| blocker | `adult` is exactly grounded to a BTO organism-form term, so the record denotes a life stage rather than a microbial habitat. | `data/raw/ontology_terms.tsv` defines `BTO:0001043` as `adult`, the child of `BTO:0000284` `organism form`. PREGO contributes source concept `habitatmech:PREGO.57e0a5faae`, but no item-level `curation/decisions.tsv` row has reviewed whether PREGO's adult stage should be a habitat-shaped adult-associated environment or excluded as metadata about an organism. The default PREGO self-grounding therefore publishes the life-stage term itself as a HabitatRecord identity. | Add an item-level `curation/decisions.tsv` row for `habitatmech:PREGO.57e0a5faae` that removes exact identity to `BTO:0001043`; keep `BTO:0001043` only as an xref if the source concept remains habitat-shaped. |
| major | `parent_habitats` inherits a non-habitat `organism form` parent. | `parent_habitats` contains `BTO:0000284` only because the BTO slice asserts `BTO:0001043 rdfs:subClassOf BTO:0000284`; `organism form` is not a broader microbial habitat. | The same `curation/decisions.tsv` row should redirect or exclude `BTO:0001043`; once the record no longer has BTO adult as its identity, the BTO organism-form superclass should not be emitted as a habitat parent. |

## Recommended Edits

1. Add an item-level decision for `habitatmech:PREGO.57e0a5faae` in `curation/decisions.tsv`. The review needs to decide whether PREGO's source string is a broad adult-organism context that should keep a minted identity, or non-habitat life-stage metadata that should become `NOT_APPLICABLE`. Either way, remove exact identity to `BTO:0001043` and keep the BTO life-stage term only as an xref if it remains useful.
2. Re-seed after the decision and confirm `BTO:0001043` and `BTO:0000284` disappear from `identifier` and `parent_habitats`.
3. Inspect the resulting generated PREGO taxon rows before applying a full seed. If the source concept remains habitat-shaped, the 378 PREGO taxon assertions can remain as PREGO associations on the minted adult-stage habitat; if it is `NOT_APPLICABLE`, the record should not continue to publish those taxa as characteristic of a habitat.

## Follow-up Checks

The narrowest checks for the recommended fix are:

- `just seed`
- `just seed-canary habitatmech:PREGO.57e0a5faae`
- inspect the canary output and confirm no generated record uses `BTO:0001043` as its identifier or `BTO:0000284` as a parent
- `just validate-strict <resulting-record-path>`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- ignored-file-inclusive exact searches for `BTO:0001043` and `habitatmech:PREGO.57e0a5faae` to confirm the new decision row is the maintained owner of the outcome
- `git diff --check`

## Additional Notes

This defect is not currently caught by `just report`'s non-habitat identity screen. The report's organism screen is ancestry-based and covers ontology roots that make organism-ness structurally obvious; `BTO:0001043` sits under `organism form`, so it needs item-level review.
