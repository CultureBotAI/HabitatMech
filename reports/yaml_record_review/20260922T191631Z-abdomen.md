# YAML Record Review: abdomen

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/abdomen.yaml`
- Started UTC: 2026-09-22T19:15:09Z
- Finished UTC: 2026-09-22T19:16:31Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/host_associated/abdomen.yaml` |
| Class | `HabitatRecord` |
| Identifier | `BTO:0000020` |
| Label | `abdomen` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Record status | Generated from the PREGO inventory plus the vendored ontology slice |

This is the generated BTO-grounded PREGO record for `BTO:0000020 abdomen`. PREGO names the BTO CURIE directly, so `ingest_prego()` keeps `BTO:0000020` as the record identifier with `grounding_status: EXACT`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/abdomen.yaml` | Passed; LinkML validation reported no issues. |
| `just validate-strict data/habitats/host_associated/abdomen.yaml` | Passed; one file scanned with 0 error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 generated records, found 3206, with 0 missing, 0 extra, and 0 differing. |
| `git diff --check` | Passed; no whitespace errors. |

## Identity and Grounding

The record's exact identity, host-associated category, BTO label, BTO definition, PREGO attestation, and single emitted taxon are supported by committed inputs:

- `data/habitats/PATHS.tsv:3` maps `BTO:0000020` to the locked slug `abdomen`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:22` gives `BTO:0000020` the ontology label `abdomen`, its text definition, `is_directly_referenced: TRUE`, and `label_only: FALSE`.
- `data/raw/prego_habitats.tsv:526` records PREGO source id `BTO:0000020`, vocabulary `BTO`, Biolink type `GrossAnatomicalStructure`, one taxon assertion, one direct assertion, maximum PREGO score `3`, evidence channel `annotated_genomes_isolates`, and raw synonyms `abdomen|abdomens|abdominal cavities|abdominal cavity`.
- `data/raw/prego_habitat_taxa.tsv:4` records the sole PREGO taxon for `BTO:0000020`: rank `1`, `NCBITaxon:1125980`, label `Vibrio harveyi CAIM 1792`, PREGO score `3`, `direct_flag: TRUE`, evidence channel `annotated_genomes_isolates`, and no corroborating source.

The deterministic PREGO source-concept key is `habitatmech:PREGO.d69ce95c28`, computed from `sha1("PREGO:BTO:0000020")[:10]`. An ignored/hidden-inclusive exact search found no decision row for that key in `curation/decisions.tsv`, so `mapping_status: SEEDED` is expected. `habitat_category: HOST_ASSOCIATED` is also expected because `BTO` identifiers fall through `PREFIX_CATEGORY` to host-associated when no curator override is present.

The generated record has no `parent_habitats`. An ignored/hidden-inclusive exact search for `BTO:0000020` found no `rdfs:subClassOf` row for that CURIE in `data/raw/ontology_subclass_edges.tsv`, so there is no vendored broader edge for the generator to emit.

## Evidence

| Claim | Nearest support | Review |
|---|---|---|
| Record identifier, label, definition, and `definition_source: BTO` | `data/raw/ontology_terms.tsv:22` | Supported exactly. |
| Locked filename | `data/habitats/PATHS.tsv:3` | Supported exactly. |
| PREGO `source_id`, one-taxon assertion count, score `3.0`, and evidence channel | `data/raw/prego_habitats.tsv:526` | Supported exactly. |
| PREGO `source_label` | `data/raw/ontology_terms.tsv:22` | Supported exactly; PREGO rows carry ontology CURIEs, and `ingest_prego()` emits `source_label` from `store.ontology.label(prego_id)`. |
| Related synonyms `abdomens`, `abdominal cavities`, and `abdominal cavity` | `data/raw/prego_habitats.tsv:526` | Supported exactly. The fourth raw PREGO lexical form, `abdomen`, is the BTO primary label, so `Concept.add_synonym()` does not duplicate it as a synonym. |
| The sole rank-1 `characteristic_taxa` row for `NCBITaxon:1125980` | `data/raw/prego_habitat_taxa.tsv:4` | Supported exactly. |

Unsupported or over-scoped claims: None found.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- The sole `source_attestations` entry captures the complete PREGO source id, source label, assertion count, `TAXON` unit, maximum score, and evidence channel.
- All three non-primary PREGO lexical variants are emitted as related synonyms, while the fourth raw variant is the exact BTO primary label.
- The sole PREGO taxon row for `BTO:0000020` is emitted as a `characteristic_taxa` entry and retains its taxon id, taxon label, PREGO score, rank, source, and one-taxon candidate pool.
- No authored definition or term request is expected because the record uses an existing exact BTO term.
- No `parent_habitats` entry is expected because the vendored ontology slice has no `rdfs:subClassOf` edge for `BTO:0000020`.

No maintained causal overlay, history file, raw research report, or prior exact YAML review report was found by filename for `abdomen` under `curation/causal_graphs`, `history`, `research`, or `reports/yaml_record_review`; the `find` search included ignored files beneath those checked directories.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if future curation changes the PREGO rollup, BTO vendored terms, path lock, or characteristic-taxon emission:

- `just validate data/habitats/host_associated/abdomen.yaml`
- `just validate-strict data/habitats/host_associated/abdomen.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If future review redirects `BTO:0000020` away from this direct PREGO self-grounding, add an explicit `curation/decisions.tsv` row for `habitatmech:PREGO.d69ce95c28` first, regenerate with `just seed`, and verify that the PREGO attestation and characteristic taxon remain attached to the surviving record.

## Additional Notes

- The pre-report exact filename check `find reports/yaml_record_review -maxdepth 1 -name '*abdomen*' -print` returned no previous exact report.
- The ignored/hidden-inclusive exact search for `BTO:0000020` and `habitatmech:PREGO.d69ce95c28` found the path lock, generated YAML, raw PREGO habitat row, raw PREGO taxon row, and vendored ontology term cited above; it found no maintained curation decision or term request for the PREGO source-concept key.
- The broader ignored/hidden-inclusive exact searches for `abdomen`, `abdomens`, `abdominal cavities`, `abdominal cavity`, and `NCBITaxon:1125980` found the expected PREGO rows plus unrelated abdomen-named GOLD records, ontology terms, term requests, curation decisions, and research reports for narrower or adjacent abdomen concepts.
