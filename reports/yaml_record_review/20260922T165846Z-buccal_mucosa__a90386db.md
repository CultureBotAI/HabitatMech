# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/buccal_mucosa__a90386db.yaml`
- Started UTC: 2026-09-22T16:58:46Z
- Finished UTC: 2026-09-22T16:58:47Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.1a8d5400cc` |
| Label | `Buccal mucosa` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained owner | Raw GOLD and ontology rows only; no item-level curation overlay is present |
| Locked slug | `data/habitats/PATHS.tsv:1478` maps `habitatmech:GOLD.1a8d5400cc` to `buccal_mucosa__a90386db` |

This is the generated GOLD record for `Host-associated > Mammals: Human > Digestive system > Oral cavity > Buccal mucosa`. The generated YAML faithfully preserves the GOLD human-qualified leaf as a path-qualified source concept, attaches generic `UBERON:0006956` `buccal mucosa` as a broader parent with `skos:narrowMatch`, keeps the human GOLD `Oral cavity` path as its source parent, and omits a GOLD organism assertion count because the exact primary GOLD ecosystem row carries zero `ORGANISM` assertions.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/buccal_mucosa__a90386db.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/buccal_mucosa__a90386db.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The generated identifier, label, category, grounding status, parents, source attestation, and seed status are supported by maintained inputs:

- `data/raw/gold_ecosystem_paths.tsv:2218` is the raw primary GOLD row for `Host-associated > Mammals: Human > Digestive system > Oral cavity > Buccal mucosa`. It has depth 5, one GOLD node id, zero organism assertions, zero KGX study assertions, zero KGX biosample assertions, and total primary assertion count 0.
- `data/raw/gold_path_biosamples.tsv:159` records 171 GOLD bulk-export biosamples on the same exact canonical path.
- `data/raw/gold_studies.tsv:378`, `:2102`, and `:2209` are the three GOLD bulk-export study rows that include the same exact canonical path.
- Exact ignored/hidden-inclusive searches of `data/raw` for the canonical path found no `gold_path_triads.tsv` row for this child leaf. A `find data/raw -name '*triads*' -print` search found only the committed aggregate `data/raw/gold_path_triads.tsv`, so no committed per-biosample GOLD triad inventory hides target rows.
- `data/habitats/PATHS.tsv:1478` pins `habitatmech:GOLD.1a8d5400cc` to `data/habitats/host_associated/buccal_mucosa__a90386db.yaml`.
- `data/raw/ontology_terms.tsv:13316` imports `UBERON:0006956` with label `buccal mucosa` and definition `The inner lining of the cheeks and lips.`
- `data/raw/ontology_subclass_edges.tsv:11587` keeps `UBERON:0006956` under `UBERON:0003729`; `data/raw/ontology_terms.tsv:13211` identifies that parent as `mouth mucosa`.
- `data/raw/gold_ecosystem_paths.tsv:40` is the raw GOLD parent row for `Host-associated > Mammals: Human > Digestive system > Oral cavity`; it has two GOLD node ids, 1032 organism assertions, and generates the source parent `habitatmech:GOLD.99880772e1`.

The generated record is correctly separate from the same-label generic and mammalian Buccal mucosa records:

- `data/habitats/host_associated/buccal_mucosa.yaml` is the PREGO/BTO exact anatomical record for the generic term `BTO:0003833`, with its own PREGO source row and one PREGO taxon assertion.
- `data/habitats/host_associated/buccal_mucosa__62ec5bff.yaml` is the GOLD record for `Host-associated > Mammals > Digestive system > Oral cavity > Buccal mucosa`, backed by the distinct GOLD ecosystem node `gold.ecosystem:4110`.

## Evidence

Every generated claim is traceable to a raw row or maintained transform:

| Claim | Nearest source | Review |
|---|---|---|
| Minted identifier `habitatmech:GOLD.1a8d5400cc` | GOLD path minting from `Host-associated > Mammals: Human > Digestive system > Oral cavity > Buccal mucosa` and `data/habitats/PATHS.tsv:1478` | Supported exactly |
| Label `Buccal mucosa`, category `HOST_ASSOCIATED`, source label, and source path | `data/raw/gold_ecosystem_paths.tsv:2218` | Supported exactly |
| `source_id: gold.ecosystem:3915` | sole GOLD node id in `data/raw/gold_ecosystem_paths.tsv:2218` | Supported exactly |
| Absence of `assertion_count` and `assertion_unit` | `data/raw/gold_ecosystem_paths.tsv:2218` has `organism_count` 0, and `src/habitatmech/seed.py` only emits GOLD `assertion_count` for nonzero organism counts | Supported exactly |
| `mapping_predicate: skos:narrowMatch` and parent `UBERON:0006956` | path-qualified GOLD leaf matched against imported `UBERON:0006956` | Supported exactly |
| Parent `habitatmech:GOLD.99880772e1` | GOLD parent path from `data/raw/gold_ecosystem_paths.tsv:40` | Supported exactly |
| Absence of a definition, synonym, xref, environmental parameter, characteristic taxon, causal graph, record-level evidence, discussion, and dataset | No maintained input row or overlay found for these slots | Supported by bounded exact searches |

The 171 biosamples in `data/raw/gold_path_biosamples.tsv:159` and three study IDs in `data/raw/gold_studies.tsv:378`, `:2102`, and `:2209` are supplemental GOLD bulk-export audit material. They are not read by `ingest_gold`, do not create `SourceAttestation.assertion_count`, and have no exact `gold_path_triads.tsv` row to ground an environmental-parameter candidate for this target.

## Completeness

The generated fields are complete for the maintained inputs that currently feed this record:

- The single GOLD source attestation captures the path, GOLD node id, and narrow ontology match.
- Both generated parents are present: the generic UBERON anatomical class and the source-inherited human GOLD oral-cavity path.
- No item-level decision, authored term request, target-specific causal graph overlay, append-only history record, dedicated deep-research report, term-request exclusion row, BacDive row, PREGO row, Madin row, or previous exact YAML review report exists for `habitatmech:GOLD.1a8d5400cc`.

Ignored/hidden-inclusive exact searches covered `data`, `curation`, `history`, `research`, `reports`, `.claude`, `src`, and `scripts`, excluding generated `build`, `data/text_map`, and `pages` trees for broad content searches. They found the raw GOLD row, the GOLD bulk-export biosample and study side rows, the locked path row, the exact generated record, the imported UBERON parent, the generated GOLD oral-cavity parent and children, generic PREGO/BTO and mammalian GOLD Buccal mucosa siblings, and the two prior Buccal mucosa YAML reviews that mentioned this sibling; they found no target-specific maintained artifact beyond the raw, path-lock, generated, and related report rows cited above.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

No target-specific follow-up is required. If upstream GOLD or ontology inputs change for `gold.ecosystem:3915`, regenerate the target and re-run:

- `just validate data/habitats/host_associated/buccal_mucosa__a90386db.yaml`
- `just validate-strict data/habitats/host_associated/buccal_mucosa__a90386db.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

## Additional Notes

- `find reports/yaml_record_review -name '*buccal_mucosa__a90386db*' -print` found no pre-existing exact review report for this record before this file was written.
- Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.1a8d5400cc`, `gold.ecosystem:3915`, `buccal_mucosa__a90386db`, `Host-associated > Mammals: Human > Digestive system > Oral cavity > Buccal mucosa`, `UBERON:0006956`, `habitatmech:GOLD.99880772e1`, and the sibling identifiers `BTO:0003833` and `habitatmech:GOLD.4caaf1fb4f` found no unsupported assertion, target-specific curation overlay, or prior exact review.
