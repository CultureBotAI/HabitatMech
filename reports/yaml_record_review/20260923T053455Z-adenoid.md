# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/adenoid.yaml`
- Started UTC: 2026-09-23T05:32:30Z
- Finished UTC: 2026-09-23T05:34:55Z
- Verdict: pass with minor issues

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `BTO:0000777` |
| Label | `adenoid` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `REVIEWED` |
| Maintained owner | Generated from the GOLD inventory, `curation/decisions.tsv`, and the vendored BTO slice; generated YAML remains read-only |
| Locked slug | `data/habitats/PATHS.tsv:146` maps `BTO:0000777` to `adenoid` |

This is the reviewed GOLD record for the path `Host-associated > Mammals: Human > Lymphatic system > Tonsils > Adenoid/Pharyngeal`. The source concept is keyed by `habitatmech:GOLD.b21e68a415`, computed as `sha1("GOLD:Host-associated > Mammals: Human > Lymphatic system > Tonsils > Adenoid/Pharyngeal")[:10]`, and the item-level decision grounds it exactly to BTO `adenoid`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/adenoid.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/adenoid.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | Pass; 0 ungrounded records are still undecided and 1810 decisions are on file |
| `just report` | Pass; the report completed and kept the corpus at 3206 records with 0 risky groundings, 0 stale class sweeps, 0 swept path/leaf contradictions, and 0 records claiming a non-habitat is a habitat |
| `git diff --check` | Pass; no whitespace errors |

## Identity and Grounding

The exact identity, host-associated category, BTO label, BTO definition, ontology parent, GOLD source-path parent, and source attestation are supported by committed inputs:

- `data/raw/gold_ecosystem_paths.tsv:2317` records the exact source path, leaf label `Adenoid/Pharyngeal`, `depth: 5`, one GOLD node, zero organism/study/biosample/total assertions, and `gold.ecosystem:6521`.
- `curation/decisions.tsv:1005` gives `habitatmech:GOLD.b21e68a415` an item-level `GROUND` decision to `BTO:0000777` with object label `adenoid` and relation `EXACT`.
- `data/habitats/PATHS.tsv:146` pins `BTO:0000777` to the `adenoid` slug, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:778` gives `BTO:0000777` the ontology label `adenoid`, its lymphoid-tissue definition, `is_directly_referenced: TRUE`, and `label_only: FALSE`.
- `data/raw/ontology_subclass_edges.tsv:511` records `BTO:0000777 rdfs:subClassOf BTO:0000753`, supporting the generated BTO parent `lymphoid tissue`.
- `data/raw/gold_ecosystem_paths.tsv:759` records the immediate GOLD parent path `Host-associated > Mammals: Human > Lymphatic system > Tonsils`, which hashes to `habitatmech:GOLD.3d15e0e2b7`.
- `curation/decisions.tsv:427` grounds `habitatmech:GOLD.3d15e0e2b7` exactly to `BTO:0001387` `tonsil`, supporting the generated source-path parent.

`grounding_status: EXACT` and `mapping_status: REVIEWED` are expected because the single contributing source concept has an item-level `GROUND` decision. The parent edges are also broader than `adenoid`: the vendored BTO slice says `adenoid` is a kind of lymphoid tissue, and the GOLD immediate parent plus its reviewed BTO grounding make the pharyngeal adenoid a child of tonsil.

## Evidence

Every scientific claim in this record is source-derived:

| Claim | Nearest source | Review |
|---|---|---|
| The generated record represents GOLD node `gold.ecosystem:6521` at `Host-associated > Mammals: Human > Lymphatic system > Tonsils > Adenoid/Pharyngeal`. | `data/raw/gold_ecosystem_paths.tsv:2317`; `curation/decisions.tsv:1005` | Supported exactly |
| Exact BTO identity, label, definition, and `definition_source: BTO` | `curation/decisions.tsv:1005`; `data/raw/ontology_terms.tsv:778` | Supported exactly |
| Exact synonym `Adenoid/Pharyngeal` from GOLD | `data/raw/gold_ecosystem_paths.tsv:2317` | Supported exactly; the GOLD source label is retained as an exact synonym when the record takes the BTO primary label |
| GOLD source id, source label, and source path | `data/raw/gold_ecosystem_paths.tsv:2317` | Supported exactly |
| Omitted `assertion_count` and `assertion_unit` | `data/raw/gold_ecosystem_paths.tsv:2317` | Supported exactly; the raw row has zero total assertions |
| BTO parent `BTO:0000753` | `data/raw/ontology_subclass_edges.tsv:511` | Supported exactly |
| GOLD/BTO parent `BTO:0001387` | `data/raw/gold_ecosystem_paths.tsv:759`; `curation/decisions.tsv:427` | Supported as the reviewed grounding of the immediate GOLD `Tonsils` parent |

The record has no xrefs, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussion links, datasets, or quality flags. Those absences match ignored/hidden-inclusive searches that found no target-specific term request, causal-graph overlay, history entry, research artifact, dataset row, or prior exact YAML review report for `BTO:0000777`, `habitatmech:GOLD.b21e68a415`, `gold.ecosystem:6521`, `Adenoid/Pharyngeal`, or `adenoid`.

Unsupported or over-scoped claims: None found.

## Completeness

The direct GOLD source attestation is complete for the single-node, zero-assertion raw row. It carries the source id, source label, full source path, and exact-match predicate, and correctly omits a count, unit, score, evidence channel, and duplicate-node note.

The inherited BTO and GOLD-parent hierarchy is complete for the current maintained inputs: the only direct BTO superclass for `BTO:0000777` is emitted, and the GOLD path's immediate `Tonsils` parent is emitted after the parent source concept's item-level exact grounding to `BTO:0001387`. No term request is expected because the source concept uses an existing exact BTO term.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated target, excluding generated `build`, `data/text_map`, and `pages` trees where broad searches would otherwise hit generated artifacts. They found the expected decision rows, raw GOLD row, ontology rows, subclass edges, path lock, and generated YAML, and no target-specific maintained curation or research artifacts.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| HM-ADENOID-001 | minor | The item-level decision note in `curation/decisions.tsv` truncates the final source-path segment as `Adenoi`, and the generated `curation_history` therefore carries an incomplete `Path:` string even though it also retains the correct source concept id. The generated `source_attestations` entry has the full path, so this is a provenance typo rather than a record identity defect. | `curation/decisions.tsv` row for `habitatmech:GOLD.b21e68a415` |

No blocker findings.

No major findings.

## Recommended Edits

1. Fix the `curation/decisions.tsv` note for `habitatmech:GOLD.b21e68a415` so the embedded `Path:` ends with the full `Adenoid/Pharyngeal` leaf.
2. Reseed and inspect `data/habitats/host_associated/adenoid.yaml` to confirm that the generated `curation_history` changes text carries the complete path while the reviewed BTO identity, GOLD attestation, exact synonym, and two broader parents remain unchanged.

## Follow-up Checks

After the future note repair, run:

- `just seed`
- `just seed-canary BTO:0000777`
- `just validate data/habitats/host_associated/adenoid.yaml`
- `just validate-strict data/habitats/host_associated/adenoid.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-adenoid.md' -print` found no pre-existing exact review report for this record before this file was written, and `find` included ignored files under the searched directory.
- Exact ignored/hidden-inclusive content searches for `BTO:0000777`, `habitatmech:GOLD.b21e68a415`, `gold.ecosystem:6521`, `Adenoid/Pharyngeal`, `BTO:0000753`, `BTO:0001387`, and `adenoid` found the cited GOLD inventory, BTO slice, decision, path-lock, and generated-record rows, plus non-target PREGO and BTO rows for the two broader parent terms.
