# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/blood__c1b484fe.yaml`
- Started UTC: 2026-09-22T01:12:07Z
- Finished UTC: 2026-09-22T01:12:43Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.6c6aab19d3` |
| Label | `Blood` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv`; future curation belongs in `curation/decisions.tsv`, `curation/term_requests.tsv`, or `curation/causal_graphs/*.yaml`, not in this generated YAML |
| Locked slug | `data/habitats/PATHS.tsv:2082` maps `habitatmech:GOLD.6c6aab19d3` to `blood__c1b484fe` |

The record is the GOLD habitat path `Host-associated > Mammals > Circulatory system > Blood`. The source attestation points to `gold.ecosystem:3665`, and `data/raw/gold_ecosystem_paths.tsv:141` confirms that `gold.ecosystem:3665|gold.ecosystem:4114` collapse to this exact canonical path with two GOLD node IDs and 160 organism assertions.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/blood__c1b484fe.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/blood__c1b484fe.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors |

## Identity and Grounding

The generated record identity is internally consistent:

- `identifier: habitatmech:GOLD.6c6aab19d3` is the stable minted identifier for the Mammals Blood GOLD path and matches `data/habitats/PATHS.tsv`.
- `label: Blood` matches the raw GOLD `leaf_label` at `data/raw/gold_ecosystem_paths.tsv:141`.
- `habitat_category: HOST_ASSOCIATED` matches the top-level GOLD `Host-associated` path component.
- `source_id: gold.ecosystem:3665` is the first of the two collapsed raw GOLD node IDs, exactly as described by the generated attestation note.
- `source_path: Host-associated > Mammals > Circulatory system > Blood` matches the raw `canonical_path` exactly.
- `assertion_count: 160` and `assertion_unit: ORGANISM` match the raw `organism_count`/`total_assertions` for this path.

`grounding_status: NARROW` is also supported. The vendored ontology slice labels `UBERON:0000178` as `blood`, but four same-depth GOLD leaves use the `Blood` label:

| Raw row | GOLD path | GOLD node IDs |
|---:|---|---|
| 7 | `Host-associated > Mammals: Human > Circulatory system > Blood` | `gold.ecosystem:3693|gold.ecosystem:3932` |
| 141 | `Host-associated > Mammals > Circulatory system > Blood` | `gold.ecosystem:3665|gold.ecosystem:4114` |
| 355 | `Host-associated > Birds > Circulatory system > Blood` | `gold.ecosystem:3713|gold.ecosystem:4153` |
| 591 | `Host-associated > Fish > Circulatory system > Blood` | `gold.ecosystem:3650|gold.ecosystem:3902` |

The anti-conflation rule in `docs/HARMONIZATION.md` says tied ambiguous leaves should keep minted identifiers and attach the matching ontology term as a broader parent. This record follows that rule by keeping `habitatmech:GOLD.6c6aab19d3` as its identifier, using `skos:narrowMatch`, and including `UBERON:0000178` in `parent_habitats`.

The source-path parent is sound. `data/raw/gold_ecosystem_paths.tsv:1994` is the immediate path `Host-associated > Mammals > Circulatory system`, with collapsed node IDs `gold.ecosystem:3576|gold.ecosystem:3666|gold.ecosystem:4115`. `data/habitats/PATHS.tsv:2186` locks that parent to `circulatory_system__7e4ecb0e`, and `data/habitats/host_associated/circulatory_system__7e4ecb0e.yaml` carries identifier `habitatmech:GOLD.7a7f8b9aa3`, matching the second `parent_habitats` entry here.

## Evidence

The record has one source attestation and no curator-authored evidence objects or causal graph:

| Claim | Nearest source | Review |
|---|---|---|
| GOLD attests `Host-associated > Mammals > Circulatory system > Blood` as a collapsed path | `data/raw/gold_ecosystem_paths.tsv:141` | Supported; exact path, label, first node ID, two-node note, and 160 organism assertions match |
| The record is narrower than generic `UBERON:0000178` blood | `data/raw/gold_ecosystem_paths.tsv` rows 7, 141, 355, 591 plus the ambiguous-leaf rule in `docs/HARMONIZATION.md` | Supported; the same `Blood` leaf appears in four same-depth host-associated circulatory contexts |
| The record is a child of the Mammals Circulatory system source node | `data/raw/gold_ecosystem_paths.tsv:1994`, `data/habitats/PATHS.tsv:2186`, and `data/habitats/host_associated/circulatory_system__7e4ecb0e.yaml` | Supported; the generated parent is the immediate GOLD parent path |
| Curation history says the record was seeded from raw inventories and attested by GOLD | `curation_history` in this generated record and the raw GOLD row above | Supported; there is no item-level curation row that would make `mapping_status: REVIEWED` appropriate |

An ignored/hidden-inclusive, tab-anchored search of `data/raw/gold_path_triads.tsv` found no exact first-column row for `Host-associated > Mammals > Circulatory system > Blood`. The exact path does appear in `data/raw/gold_path_biosamples.tsv:618` and `data/raw/gold_studies.tsv` rows 1323 and 4237, but those inventories do not seed MIxS triad-derived environment-parameter slots or causal evidence into this parent Blood record.

## Completeness

No consequential slot is underfilled for the generated source material currently available:

- `definition` and `definition_source` are absent, as expected for a seeded minted term with no matching `curation/term_requests.tsv` row.
- `environmental_parameters` is absent, and no exact Mammals Blood row exists in `data/raw/gold_path_triads.tsv` to seed an environmental triad.
- `causal_graphs` is absent, and bounded causal-graph filename searches for `*blood*` and `*c1b484fe*` under `curation/causal_graphs/` found no maintained overlay.
- `characteristic_taxa` is absent, as expected; GOLD occurrence counts are source attestations and do not assert characteristic taxa.
- `discussions` and `datasets` are absent; no inspected maintained row supplied them for this path.

The generated child records that cite `habitatmech:GOLD.6c6aab19d3` as a parent are expected descendants of the exact GOLD path: `Arterial`, `Plasma`, `Serum`, `Venous`, `Whole blood`, and `Peripheral blood mononuclear cells`.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if a future curation change adds a decision, definition, or causal graph overlay for this source concept:

- `just validate data/habitats/host_associated/blood__c1b484fe.yaml`
- `just validate-strict data/habitats/host_associated/blood__c1b484fe.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If the future change adds a `curation/decisions.tsv` row, also verify that `gold.ecosystem:3665|gold.ecosystem:4114` are still the only GOLD node IDs collapsed into `Host-associated > Mammals > Circulatory system > Blood`.

## Additional Notes

- `find reports/yaml_record_review -name '*blood__c1b484fe*' -print` found no pre-existing review report for this record before this file was written.
- Ignored/hidden-inclusive exact searches with `rg --no-ignore --hidden` covered `curation`, `history`, `research`, `reports`, `docs`, `.claude`, `src`, `data/raw`, `data/habitats/PATHS.tsv`, and `data/habitats/host_associated`, while excluding generated `data/text_map/**` and `pages/**`; they found no maintained decision, term request, history record, causal-graph overlay, research report, or prior YAML review report for `habitatmech:GOLD.6c6aab19d3`, `gold.ecosystem:3665`, or `blood__c1b484fe`.
- `gold.ecosystem:4114` appears only in the collapsed exact raw GOLD ecosystem row for this path among those same searched inputs.
