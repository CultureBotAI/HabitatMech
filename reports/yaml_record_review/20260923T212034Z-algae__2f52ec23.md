# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/engineered/algae__2f52ec23.yaml`
- Started UTC: `2026-09-23T21:17:00Z`
- Finished UTC: `2026-09-23T21:20:34Z`
- Verdict: `pass`

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/engineered/algae__2f52ec23.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.5b8e5eddd2` |
| Label | `Algae` |
| Definition source | Not set |
| Category | `ENGINEERED` |
| Grounding status | `NARROW` |
| Mapping status | `REVIEWED` |

This is a generated record owned by upstream inputs, not by hand edits under
`data/habitats/`. Its path is pinned by `data/habitats/PATHS.tsv`, which maps
`habitatmech:GOLD.5b8e5eddd2` to `algae__2f52ec23`.

## Validation

| Check | Result |
|---|---|
| `find reports/yaml_record_review -maxdepth 1 -type f -name '*-algae__2f52ec23.md' -print` | Passed; no prior exact report was present. |
| `find curation/causal_graphs -maxdepth 1 -type f -name '*algae__2f52ec23*' -print` | Passed; no candidate causal overlay was present. |
| `rg --no-ignore --hidden -n -e "habitatmech:GOLD\.5b8e5eddd2" -e "algae__2f52ec23" -e "\bAlgae\b" curation/causal_graphs` | Passed; no causal overlay references the identifier, slug, or label. Ignored and hidden files were included. |
| `just validate data/habitats/engineered/algae__2f52ec23.yaml` | Passed; `linkml-validate` reported no issues. |
| `just validate-strict data/habitats/engineered/algae__2f52ec23.yaml` | Passed; 1 file scanned, 0 files with errors, and 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| Focused causal-graph validation | Not applicable; the record has no maintained overlay under `curation/causal_graphs/`. |
| Reference validator | Not applicable; the record has no DOI, PMID, URL, causal-edge evidence, dataset, or discussion references to check. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; no issues found and 77 history records validated. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 on disk, 0 missing, 0 extra, and 0 differing. |
| `just worklist --limit 2000` | Passed after running outside the managed workspace sandbox; the command reported 0 ungrounded records still undecided and 1,810 decisions on file. |
| `just report` | Passed; the corpus report completed for 3,206 records. |
| `git diff --check` | Passed before report creation. |

## Identity and Grounding

The GOLD source concept is `gold.ecosystem:5981`, label `Algae`, with path
`Engineered > Lab culture > Culture media > Algae`. In
`data/raw/gold_ecosystem_paths.tsv` this path is a depth-4 collapsed GOLD leaf
covering two upstream node IDs, `gold.ecosystem:5981` and
`gold.ecosystem:8496`. It has zero upstream organism, study, biosample, or
assertion counts.

The bare source label `Algae` is ambiguous outside the GOLD path. The
item-level row in `curation/decisions.tsv` resolves the exact source concept as
an algal culture medium, keeps the minted `habitatmech:GOLD.5b8e5eddd2`
identity, attaches `BTO:0000316` `culture medium` as a strictly broader parent,
and records `NARROW` grounding. The generated record matches that decision: it
does not claim an algae organism term as identity or parent, and its only
`parent_habitats` entry is the vendored `BTO:0000316` term.

The vendored BTO slice contains `BTO:0000316` `culture medium`, defined as a
substance used for cultivation, isolation, identification, or storage of
microorganisms. That term is a defensible broader parent for GOLD's
algae-specific culture-medium path.

## Evidence

The record's generated identity, `NARROW` status, `skos:narrowMatch` predicate,
`BTO:0000316` parent, and `REVIEWED` mapping status all follow from the single
item-level decision row for `habitatmech:GOLD.5b8e5eddd2`.

The GOLD source attestation preserves `gold.ecosystem:5981`, the source label,
and the four-level source path from `data/raw/gold_ecosystem_paths.tsv`. Its
note correctly reports that two upstream GOLD ecosystem node IDs share this
path and that the first is shown in the `source_id` field.

The record has no `definition`, `definition_source`, or authored exact
synonyms, and no `curation/term_requests.tsv` row is expected for the current
broader-parent curation state. It also has no `characteristic_taxa` because the
GOLD source leaf has no organism assertions in the raw path inventory.

No causal graph, claim-level evidence block, environmental parameter,
discussion item, or dataset usage is asserted for this record.

## Completeness

The record is complete enough for its current single-source GOLD scope:

- It has one item-level grounding decision for its only source concept.
- It has the correct broader ontology parent for an algal culture-medium path.
- It removes the false algae-organism parent that would otherwise be inferred
  by the ambiguous-leaf rule.
- It intentionally emits no characteristic taxa because the GOLD source leaf
  has no upstream assertions in `data/raw/gold_ecosystem_paths.tsv`.
- It correctly leaves causal graphs, environmental parameters, discussions, and
  datasets empty because no maintained input asserts them.

Bounded, ignored-inclusive exact searches for
`habitatmech:GOLD.5b8e5eddd2`, `gold.ecosystem:5981`, `algae__2f52ec23`, and
`Engineered > Lab culture > Culture media > Algae` across curation inputs, raw
inputs, `PATHS.tsv`, history, reports, research, source, tests, and
configuration found the item-level decision, the GOLD raw rows, the `PATHS.tsv`
slug row, the generated target record, the generated contaminated-child record,
and cross-references in committed research reports for host-associated algal
records. Generated `data/text_map/`, `pages/`, and `build/` files were
excluded; ignored and hidden files were included.

## Findings

No blocker, major, or minor findings.

## Recommended Edits

None.

## Follow-up Checks

- No target-specific follow-up is required.

## Additional Notes

The contaminated child path
`Engineered > Lab culture > Culture media > Algae > Contaminated` is generated
as `data/habitats/engineered/contaminated__10ad7003.yaml` and correctly
retains `habitatmech:GOLD.5b8e5eddd2` as a broader parent.
