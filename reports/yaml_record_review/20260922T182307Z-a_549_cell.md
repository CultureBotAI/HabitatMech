# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/a_549_cell.yaml`
- Started UTC: 2026-09-22T18:22:07Z
- Finished UTC: 2026-09-22T18:23:31Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `BTO:0000018` |
| Label | `A-549 cell` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the PREGO inventory plus the vendored ontology slice; generated YAML remains read-only |
| Locked slug | `data/habitats/PATHS.tsv:2` maps `BTO:0000018` to `a_549_cell` |

This is the generated BTO-grounded PREGO record for `A-549 cell`, a human lung carcinoma cell-line term. PREGO uses the BTO CURIE directly, so the seeder keeps `BTO:0000018` as the record identifier with `grounding_status: EXACT`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/a_549_cell.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/a_549_cell.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The exact identity, host-associated category, BTO label, BTO definition, ontology parent, path lock, PREGO attestation, and emitted taxa are supported by committed inputs:

- `data/habitats/PATHS.tsv:2` pins `BTO:0000018` to slug `a_549_cell`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:20` gives `BTO:0000018` the ontology label `A-549 cell`, the human lung carcinoma definition, `is_directly_referenced: TRUE`, and `label_only: FALSE`.
- `data/raw/ontology_subclass_edges.tsv:18` records `BTO:0000018 rdfs:subClassOf BTO:0004078`, which supports the generated `parent_habitats` entry.
- `data/raw/ontology_terms.tsv:4077` labels parent `BTO:0004078` as `non-small cell lung adenocarcinoma cell line`.
- `data/raw/prego_habitats.tsv:465` records PREGO source id `BTO:0000018`, vocabulary `BTO`, Biolink type `GrossAnatomicalStructure`, two taxon assertions, two direct assertions, maximum PREGO score `3`, evidence channel `annotated_genomes_isolates`, and synonyms `A-549 cell|NCI-A549 cell|a549|a549 cell line|a549 cell lines|a549 cells`.
- `data/raw/prego_habitat_taxa.tsv:2` records rank `1`, `NCBITaxon:1194085`, label `Staphylococcus aureus subsp. aureus SA40`, PREGO score `3`, `direct_flag: TRUE`, evidence channel `annotated_genomes_isolates`, and no corroborating source.
- `data/raw/prego_habitat_taxa.tsv:3` records rank `2`, `NCBITaxon:1201010`, label `Staphylococcus aureus subsp. aureus SA957`, PREGO score `3`, `direct_flag: TRUE`, evidence channel `annotated_genomes_isolates`, and no corroborating source.

The deterministic PREGO source-concept key is `habitatmech:PREGO.5cfd683ed0`, computed from `sha1("PREGO:BTO:0000018")[:10]`. An ignored/hidden-inclusive exact search found no item-level row for that key in `curation/decisions.tsv`, so `mapping_status: SEEDED` is expected. `habitat_category: HOST_ASSOCIATED` is also expected because BTO identifiers fall through `PREFIX_CATEGORY` to host-associated when no curator override is present.

## Evidence

Every generated scientific claim in this record is source-derived:

| Claim | Nearest source | Review |
|---|---|---|
| Record identifier, label, definition, and `definition_source: BTO` | `data/raw/ontology_terms.tsv:20` | Supported exactly |
| BTO parent `BTO:0004078` | `data/raw/ontology_subclass_edges.tsv:18` | Supported exactly |
| Locked filename | `data/habitats/PATHS.tsv:2` | Supported exactly |
| PREGO `source_id`, two-taxon assertion count, score `3.0`, and evidence channel | `data/raw/prego_habitats.tsv:465` | Supported exactly |
| PREGO `source_label` | `data/raw/ontology_terms.tsv:20` | Supported exactly; PREGO rows carry ontology CURIEs, and `ingest_prego()` emits `source_label` from `store.ontology.label(prego_id)` |
| Related synonyms `NCI-A549 cell`, `a549`, `a549 cell line`, `a549 cell lines`, and `a549 cells` | `data/raw/prego_habitats.tsv:465` | Supported exactly; the remaining PREGO lexical variant, `A-549 cell`, is the ontology primary label |
| The rank-1 `characteristic_taxa` row for `NCBITaxon:1194085` | `data/raw/prego_habitat_taxa.tsv:2` | Supported exactly |
| The rank-2 `characteristic_taxa` row for `NCBITaxon:1201010` | `data/raw/prego_habitat_taxa.tsv:3` | Supported exactly |

The record has no xrefs, environmental parameters, record-level evidence, causal graphs, discussion links, datasets, or quality flags. Those absences match ignored/hidden-inclusive searches that found no maintained decision, term request, causal-graph overlay, history entry, target-specific research artifact, dataset row, or prior exact YAML review report for `BTO:0000018`, `a_549_cell`, `A-549 cell`, `NCI-A549 cell`, `NCBITaxon:1194085`, `NCBITaxon:1201010`, or `habitatmech:PREGO.5cfd683ed0`.

Unsupported or over-scoped claims: None found.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- The sole `source_attestations` entry captures the complete PREGO source id, source label, assertion count, `TAXON` unit, maximum score, and evidence channel.
- All five non-primary raw PREGO lexical variants are emitted as related synonyms, while the sixth raw PREGO lexical variant is the exact BTO primary label.
- Both PREGO taxon rows for `BTO:0000018` are emitted as `characteristic_taxa` entries and retain their taxon ids, taxon labels, PREGO scores, ranks, source, and two-taxon candidate pool.
- The only broader BTO parent edge from the vendored ontology slice is emitted as `parent_habitats: [BTO:0004078]`.
- No authored definition or term request is expected because the record uses the existing exact BTO `A-549 cell` term.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated target, excluding generated `build`, `data/text_map`, and `pages` trees where broad searches would otherwise hit generated artifacts. They found the expected PREGO habitat row, PREGO taxon rows, ontology term rows, subclass edge, path lock, generated YAML, unrelated PREGO reuse of the two Staphylococcus aureus subsp. aureus strains under other BTO terms, and one unrelated lexical match for `A549-CR cell`; they found no target-specific maintained curation or research artifacts.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if future curation changes the PREGO rollup, BTO vendored terms, path lock, or characteristic-taxon emission:

- `just validate data/habitats/host_associated/a_549_cell.yaml`
- `just validate-strict data/habitats/host_associated/a_549_cell.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If future review redirects `BTO:0000018` away from this direct PREGO self-grounding, add an explicit `curation/decisions.tsv` row for `habitatmech:PREGO.5cfd683ed0` first, regenerate with `just seed`, and verify that the PREGO attestation and both characteristic taxa remain attached to the surviving record.

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -name '*a_549_cell*' -print` found no pre-existing exact review report for this record before this file was written.
- `find curation/causal_graphs history research -name '*a_549_cell*' -print` found no target-specific causal overlay, history record, or research artifact before this file was written.
- Exact ignored/hidden-inclusive content searches for `BTO:0000018`, `a_549_cell`, `A-549 cell`, `A549 cell`, `NCI-A549 cell`, `NCBITaxon:1194085`, `NCBITaxon:1201010`, `BTO:0004078`, and `habitatmech:PREGO.5cfd683ed0` found the path lock, generated YAML, raw PREGO rows, and vendored ontology rows cited above, plus unrelated hits noted under Completeness, and no target-specific maintained curation or research artifacts.
