# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/aerobic_digester.yaml
- Started UTC: 2026-09-23T17:44:56Z
- Finished UTC: 2026-09-23T17:47:50Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | habitatmech:GOLD.03fc563fae |
| Label | Aerobic digester |
| Class | HabitatRecord |
| Category | ENGINEERED |
| Grounding status | UNGROUNDED |
| Mapping status | SEEDED |
| Maintained owner | generated from `data/raw/gold_ecosystem_paths.tsv`, `curation/decisions.tsv`, and `data/habitats/PATHS.tsv`; the false parent is generated by the GOLD parent-path pass in `src/habitatmech/seed.py` |

[data/habitats/engineered/aerobic_digester.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/engineered/aerobic_digester.yaml) is the generated GOLD record for `Engineered > WWTP > Aerobic digester`, whose deterministic source-concept key is `habitatmech:GOLD.03fc563fae`.

The target was resolved with ignored-file-inclusive searches:

- `find data/habitats -type f -name 'aerobic_digester.yaml' -print` found exactly one generated YAML record.
- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-aerobic_digester.md' -print` found no prior exact review report.
- `find curation/causal_graphs -maxdepth 1 -type f -name 'aerobic_digester.yaml' -print` found no causal-graph overlay for this record.
- `rg --no-ignore --hidden -n --glob '!data/text_map/**' --glob '!pages/**' --glob '!build/**' --glob '!reports/yaml_record_review/**' 'habitatmech:GOLD\.03fc563fae|Engineered > WWTP > Aerobic digester|gold\.ecosystem:5672|gold\.ecosystem:8256|gold\.ecosystem:8257|aerobic_digester' curation history research reports data/habitats/PATHS.tsv` found the `PATHS.tsv` slug lock and a class-level `curation/decisions.tsv` row for this GOLD source concept; it found no item-level decision, maintained causal overlay, history record, or aerobic-digester research report.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/aerobic_digester.yaml` | pass; no LinkML issues found |
| `just validate-strict data/habitats/engineered/aerobic_digester.yaml` | pass; 1 file scanned, 0 files with ERROR, 0 total ERROR rows |
| `just validate-causal curation/causal_graphs/aerobic_digester.yaml` | not applicable; exact `find curation/causal_graphs -maxdepth 1 -type f -name 'aerobic_digester.yaml' -print` found no overlay |
| `just validate-causal-all` | pass; 32 causal-graph curation files with 32 graphs validated |
| Reference validator | not applicable; this generated GOLD record has no causal graph and no DOI, PMID, or URL evidence slots to inspect |
| `just term-requests-check` | pass; term-request table is current with 109 terms |
| `just validate-history` | pass; no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | pass; 3206 expected, 3206 found, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | pass; 0 ungrounded records still undecided; 1810 decisions on file |
| `just report` | pass; 3206 records; no global issue specific to `habitatmech:GOLD.03fc563fae` surfaced |
| `git diff --check` | pass |

## Identity and Grounding

The minted identity is faithful to the GOLD source concept. `data/raw/gold_ecosystem_paths.tsv` has the canonical path `Engineered > WWTP > Aerobic digester` at depth 3 with `leaf_label` `Aerobic digester`, 3 collapsed GOLD node ids, first node id `gold.ecosystem:5672`, and zero direct organism, study, biosample, or total assertions. `src/habitatmech/seed.py` mints the source concept as the first ten SHA1 hex digits of `GOLD:Engineered > WWTP > Aerobic digester`, yielding `habitatmech:GOLD.03fc563fae`.

`mapping_status: SEEDED` is correct for the current curation input. `curation/decisions.tsv` has a `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.03fc563fae`, but its `review_depth` is `CLASS`, not `ITEM`; the row records that no vendored ontology term matched by the class-level lexical sweep and explicitly says that whether the concept is a habitat was not assessed.

The generated `parent_habitats` value is not a true broader habitat. `ENVO:00002043` is `wastewater treatment plant` in `data/raw/ontology_terms.tsv`. It reaches this record because the seeder first grounds the GOLD parent path `Engineered > WWTP` through the item-level decision for `habitatmech:GOLD.f1d23444f9`, then unconditionally links each GOLD path to its immediate parent path in the second GOLD pass. `Engineered > WWTP > Aerobic digester` means an aerobic digester is organized under a WWTP in GOLD's tree; it does not mean an aerobic digester is a kind of wastewater treatment plant. `docs/CURATION.md` and `src/habitatmech/curate/decisions.py` both reserve `parent_habitats` for strictly broader `is-a` parents, not containment or context.

No exact ontology identity is maintained for the aerobic-digester concept, and exact ignored-file-inclusive search found no item-level curation row that has manually confirmed this source concept.

## Evidence

This record has one GOLD source attestation:

- The `GOLD` attestation mirrors `data/raw/gold_ecosystem_paths.tsv`: source id `gold.ecosystem:5672`, source label `Aerobic digester`, and source path `Engineered > WWTP > Aerobic digester`.
- The raw row's `gold_node_count` is `3` and its `gold_node_ids` field is `gold.ecosystem:5672|gold.ecosystem:8256|gold.ecosystem:8257`, matching the generated de-duplication note.
- The source path has `organism_count`, `study_count`, `biosample_count`, and `total_assertions` all equal to `0`, so the generated attestation correctly omits `assertion_count` and `assertion_unit`.

The narrower raw GOLD path `Engineered > WWTP > Aerobic digester > Activated sludge` is present with 2 upstream organism assertions, but it is a child source concept and does not support the false broader edge from this record to `wastewater treatment plant`.

No evidence-backed causal mechanism evidence is present.

Unsupported or over-scoped claims:

- `parent_habitats: ENVO:00002043` overstates a containment/context path as a broader-habitat edge.

## Completeness

The record carries the generated GOLD source-owned content for this source concept: the first GOLD ecosystem id, source label, source path, duplicate-node note, class-level sweep history, generated seeding history, and generated slug lock.

The hierarchy is materially incomplete because the only generated parent is false. The current curation table can add broader terms or related xrefs through `object_id` and `relation`, but exact search found no existing maintained input that suppresses an unsound GOLD parent-path edge after `src/habitatmech/seed.py` links a child path to a parent path.

Optional characteristic-taxon, environmental-parameter, discussion, dataset, and causal-graph slots are correctly absent; no maintained causal overlay, history entry, or item-level curated review row adds any of those fields.

## Findings

- **major**: `parent_habitats` asserts `Aerobic digester` `is-a` `ENVO:00002043` `wastewater treatment plant`.

  **Evidence**: GOLD places `Engineered > WWTP > Aerobic digester` below `Engineered > WWTP`, whose item decision grounds it to `ENVO:00002043`. The seeder copies that GOLD parent-path relationship into `parent_habitats`, but an aerobic digester is a unit in a wastewater treatment plant rather than a subclass of wastewater treatment plant. The curation docs require `parent_habitats` to be a real `is-a` broader-habitat edge.

  **Maintained owner**: `src/habitatmech/seed.py` currently generates this edge with no curation-owned suppression table for false GOLD parent-path edges.

## Recommended Edits

- Add a maintained way to suppress or retype unsound GOLD parent-path edges, then remove the generated edge from `habitatmech:GOLD.03fc563fae` to the resolved parent-path record `ENVO:00002043`.
- Item-review `habitatmech:GOLD.03fc563fae` after the false parent is removable. If no vendored exact term exists and the concept is retained as a real engineered habitat, keep it minted with an item-level `CONFIRM_UNGROUNDED` row rather than promoting the class-level sweep to reviewed status.
- Regenerate only the affected source concept and inspect [data/habitats/engineered/aerobic_digester.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/engineered/aerobic_digester.yaml) to confirm `parent_habitats: ENVO:00002043` is gone and no false replacement parent was introduced.

## Follow-up Checks

The narrow proof path for a future fix is:

- add or update focused tests that prove an explicitly suppressed GOLD parent-path edge is not emitted by `src/habitatmech/seed.py`
- `just seed`
- `just seed-canary habitatmech:GOLD.03fc563fae`
- inspect [data/habitats/engineered/aerobic_digester.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/engineered/aerobic_digester.yaml) and confirm `ENVO:00002043` is no longer in `parent_habitats`
- inspect any generated child of `Engineered > WWTP > Aerobic digester` and confirm needed hierarchy was intentionally retained or removed
- `just validate-strict data/habitats/engineered/aerobic_digester.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- ignored-file-inclusive exact searches for `habitatmech:GOLD.03fc563fae`, `Engineered > WWTP > Aerobic digester`, and any curated suppression row
- `git diff --check`

## Additional Notes

The exact ignored-file-inclusive search also matched `anaerobic_digester` slug locks through the substring `aerobic_digester`; those are distinct GOLD records and do not affect this review.
