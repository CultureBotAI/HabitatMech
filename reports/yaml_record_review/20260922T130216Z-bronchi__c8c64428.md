# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/bronchi__c8c64428.yaml`
- Started UTC: 2026-09-22T13:02:16Z
- Finished UTC: 2026-09-22T13:02:16Z
- Verdict: acceptable as generated

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.f7cc0ef1b4` |
| Label | `Bronchi` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv` through the automatic GOLD ambiguous-leaf route |
| Locked slug | `data/habitats/PATHS.tsv:3135` maps `habitatmech:GOLD.f7cc0ef1b4` to `bronchi__c8c64428` |

This is the GOLD path record for `Host-associated > Birds > Respiratory system > Bronchi`. The generated YAML keeps the path as a source-specific `NARROW` record with `skos:narrowMatch` to `UBERON:0002185` because `Bronchi` is an ambiguous GOLD leaf shared by Mammals, Mammals: Human, and Birds rows. That ambiguity guard is appropriate for this record: the committed narrow-sample audit already judged the Birds `Bronchi` row correct, the source path is an avian respiratory branch rather than the species-neutral bronchus class itself, and the record has no GOLD organism, study, biosample, or triad assertions that would support a stronger maintained merge.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/bronchi__c8c64428.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/bronchi__c8c64428.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The generated identifier, label, host-associated category, ontology and GOLD parents, path lock, and GOLD source attestation are supported by maintained inputs:

- `data/raw/gold_ecosystem_paths.tsv:1901` is the raw GOLD row for this record. Its canonical path is `Host-associated > Birds > Respiratory system > Bronchi`; its leaf label is `Bronchi`; it is a depth-4 path with 2 GOLD node ids, `gold.ecosystem:7440|gold.ecosystem:7441`, and zero organism, study, biosample, or literature assertions.
- `data/habitats/PATHS.tsv:3135` pins `habitatmech:GOLD.f7cc0ef1b4` to slug `bronchi__c8c64428`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:13131` provides `UBERON:0002185` `bronchus`, whose exact synonym list includes `bronchi`; `data/habitats/PATHS.tsv:1067` pins that ontology record to `bronchus`.
- `curation/samples/narrow-20260814.tsv:38` records this exact Birds `Bronchi` row in the committed `NARROW` sampling audit with verdict `correct`.
- No `curation/decisions.tsv` row addresses `habitatmech:GOLD.f7cc0ef1b4`, so the sample audit has not been promoted into record-local `mapping_status: REVIEWED` provenance.
- The source path parent `Host-associated > Birds > Respiratory system` mints to `habitatmech:GOLD.0c74807e42`; `data/habitats/PATHS.tsv:1366` pins that parent to `respiratory_system__431eac8d`, and `data/raw/gold_ecosystem_paths.tsv:299` is the raw parent row.

The automatic grounding path is reproducible. `src/habitatmech/seed.py` groups GOLD rows by normalized leaf label, gives a matching ontology term only to the unique shallowest path for that leaf, and leaves same-depth ties unclaimed. The three same-depth `Bronchi` rows under Mammals, Mammals: Human, and Birds therefore remain separate `NARROW` children of `UBERON:0002185` instead of being silently collapsed into the reviewed `bronchus` record.

## Evidence

Every generated claim in this record is source-derived or inferred by the seeder:

| Claim | Nearest source | Review |
|---|---|---|
| Minted record identifier `habitatmech:GOLD.f7cc0ef1b4` | `src/habitatmech/seed.py`, `data/raw/gold_ecosystem_paths.tsv:1901` | Supported exactly by `mint("GOLD", canonical_path)` |
| Label `Bronchi`, host-associated category, GOLD source id, source path, 2-node note, and absent assertion count | `data/raw/gold_ecosystem_paths.tsv:1901` | Supported exactly |
| `NARROW` status, `skos:narrowMatch`, and parent `UBERON:0002185` | GOLD leaf-label matching in `src/habitatmech/seed.py`, backed by `data/raw/ontology_terms.tsv:13131` | Supported as the automatic same-depth ambiguity fallback and endorsed by the `curation/samples/narrow-20260814.tsv:38` sample |
| Parent `habitatmech:GOLD.0c74807e42` | GOLD parent-path emission in `src/habitatmech/seed.py`, backed by `data/raw/gold_ecosystem_paths.tsv:299` | Supported exactly |
| `SEEDED` mapping status and only a seed-from-sources history event | Absence of a `curation/decisions.tsv` row for `habitatmech:GOLD.f7cc0ef1b4` | Supported exactly |
| Locked filename | `data/habitats/PATHS.tsv:3135` | Supported exactly |

The record has no definition, synonyms, xrefs, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussion links, or datasets. Those absences are consistent with a zero-assertion GOLD aggregate row that has no exact GOLD biosample, study, or complete-triad side-table entries.

## Completeness

The generated slots are faithful to the maintained inputs that feed this record:

- The sole `source_attestations` entry captures the exact Birds `Bronchi` path and the first of the two upstream GOLD node ids; its note preserves the multi-node fact from `data/raw/gold_ecosystem_paths.tsv:1901`.
- The record correctly omits `assertion_count` and `assertion_unit` because the raw aggregate records zero organism assertions and no study, biosample, or literature assertions.
- The UBERON parent preserves the `Bronchi` synonym match without incorrectly treating the Birds branch as the same identity as generic `UBERON:0002185`.
- The generated Birds respiratory-system parent preserves the exact source hierarchy. `Air sacs`, `Lung`, `Nasal cavity`, `Trachea`, and `Infraorbital sinus` are sibling rows under `Host-associated > Birds > Respiratory system`; none is a child of this exact `Bronchi` source path.
- The committed sampling row provides auditable support for leaving this record `NARROW`; it does not itself make the generated record `REVIEWED`, because `src/habitatmech/seed.py` only emits reviewed mapping histories from `curation/decisions.tsv`.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated host-associated records, excluding generated `build`, `data/text_map`, and `pages` trees. They found the target raw GOLD row, path lock, generated YAML, exact `UBERON:0002185` ontology row, committed narrow-sample judgement, generated Birds respiratory-system parent, Birds respiratory-system sibling rows, and the two previously added Mammals and Mammals: Human `Bronchi` YAML reports; they found no target-specific maintained decision, term request, causal overlay, history record, research report, GOLD biosample row, GOLD study row, GOLD triad row, generated child record, or prior exact YAML review report.

## Findings

No actionable correctness findings.

## Recommended Edits

- No immediate curation edit is recommended for `data/habitats/host_associated/bronchi__c8c64428.yaml`.
- If the sampling verdict should become record-local provenance, add an `ITEM`-depth `REVIEW` row for `habitatmech:GOLD.f7cc0ef1b4` to `curation/decisions.tsv`, explaining why the Birds `Bronchi` GOLD path should stay a `NARROW` child of `UBERON:0002185`.

## Follow-up Checks

If an item-level review row is added for the Birds `Bronchi` path, re-run the same focused validation set:

- `just validate data/habitats/host_associated/bronchi__c8c64428.yaml`
- `just validate-strict data/habitats/host_associated/bronchi__c8c64428.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

Also confirm the regenerated YAML remains `NARROW`, gains a `REVIEW` curation-history event for `habitatmech:GOLD.f7cc0ef1b4`, and does not merge into the generic `data/habitats/host_associated/bronchus.yaml` record.

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -name '*bronchi__c8c64428*' -print` found no pre-existing exact review report for this record before this file was written.
- Exact ignored/hidden-inclusive content searches for `Host-associated > Birds > Respiratory system > Bronchi`, `habitatmech:GOLD.f7cc0ef1b4`, `gold.ecosystem:7440`, and `gold.ecosystem:7441` found only the maintained source, sample, path-lock, generated, and sibling-review rows cited above for this target. The broader exact `Bronchi` sibling search found the Mammals and Mammals: Human GOLD rows, but those rows have different canonical paths and do not feed this target.
