# YAML Record Review: Aquifer

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/aquifer__f620c815.yaml
- Started UTC: 2026-09-21T17:11:39Z
- Finished UTC: 2026-09-21T17:11:39Z
- Verdict: needs curation

## Target

The target is the generated `HabitatRecord` at `data/habitats/aquatic/aquifer__f620c815.yaml`.

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.d61fa6c9ee` |
| Label | `Aquifer` |
| Category | `AQUATIC` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Source concepts | GOLD `gold.ecosystem:5373` |
| Locked stem | `aquifer__f620c815` in `data/habitats/PATHS.tsv` |

This review covers the GOLD `Environmental > Aquatic > Freshwater > Aquifer` source concept grounded as a narrow child of ENVO `aquifer`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/aquifer__f620c815.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/aquifer__f620c815.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 records on disk, 0 missing, 0 extra, 0 differing. |
| Reference validator | Not checked: the generated record has no DOI/PMID/URL `EvidenceItem`, no causal-edge evidence, and no curated causal overlay. |

## Identity and Grounding

| Claim | Evidence | Assessment |
|---|---|---|
| The generated record denotes GOLD's freshwater `Aquifer` source concept. | `data/raw/gold_ecosystem_paths.tsv` has canonical path `Environmental > Aquatic > Freshwater > Aquifer`, leaf `Aquifer`, depth `4`, one GOLD node ID `gold.ecosystem:5373`, and zero organism, study, biosample, or total assertions. | Supported exactly. |
| The locked generated identifier is stable. | `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.d61fa6c9ee` to stem `aquifer__f620c815`; hidden/ignored-inclusive exact search found no curation, history, causal-graph, research, or previous YAML review entry for that identifier or its GOLD source ID. | Supported exactly. |
| The `ENVO:00012408` parent is a valid broader parent. | `data/raw/ontology_terms.tsv` lists active term `ENVO:00012408` with label `aquifer`; ENVO defines aquifer as an underground water-bearing permeable rock or unconsolidated-material layer, and `data/raw/ontology_subclass_edges.tsv` places it under `ENVO:01001275` `solid layer`. | Supported as a broader parent for GOLD's more specific freshwater aquifer bucket. |
| The `ENVO:00002011` parent is generated from the upstream GOLD `Freshwater` path parent. | `data/raw/gold_path_triads.tsv` records `Environmental > Aquatic > Freshwater` samples with `medium = ENVO:00002011 fresh water`; `data/habitats/PATHS.tsv` maps `ENVO:00002011` to `fresh_water`, and the target record lists `ENVO:00002011` in `parent_habitats`. | Needs curation; the GOLD source-path context is freshwater, but a freshwater aquifer is not a subtype of water with a low concentration of dissolved solutes. |
| The GOLD `Biofilm` and `Sediment` children confirm this path is geological, not a fresh-water subtype. | `data/raw/gold_ecosystem_paths.tsv` lists `Environmental > Aquatic > Freshwater > Aquifer > Biofilm` as `gold.ecosystem:5446` and `Environmental > Aquatic > Freshwater > Aquifer > Sediment` as `gold.ecosystem:5374`; `data/habitats/aquatic/biofilm__5f164a0f.yaml` and `data/habitats/aquatic/sediment__cd41307f.yaml` both retain this GOLD aquifer record as their source-path parent. | Supports keeping `Aquifer` as a source-derived geologic context while suppressing the inherited `fresh water` parent. |

## Evidence

The generated record has no record-level `evidence` entries and no `causal_graphs`.

| Claim | Evidence | Assessment |
|---|---|---|
| The GOLD source attestation preserves the source path and source ID. | `data/raw/gold_ecosystem_paths.tsv` has `gold.ecosystem:5373` on the `Environmental > Aquatic > Freshwater > Aquifer` path with leaf `Aquifer`; the generated `source_attestations` entry carries the same source ID, label, and path. | Supported exactly. |
| The target path has no direct sample counts in the raw GOLD path table. | The canonical GOLD row for `Environmental > Aquatic > Freshwater > Aquifer` reports `organism_count=0`, `study_count=0`, `biosample_count=0`, and `total_assertions=0`, matching the absence of generated characteristic taxa or assertion counts on the source attestation. | Supported exactly. |
| The two direct descendants are modeled separately. | The raw GOLD table lists one `Biofilm` node and one `Sediment` node under this `Aquifer` path; their generated records preserve those paths and carry `habitatmech:GOLD.d61fa6c9ee` as the source-derived parent. | Supported exactly. |

## Completeness

- The generated record is complete enough for a seeded, source-derived GOLD concept: it has the stable HabitatMech identifier, the GOLD source attestation, its generated source-path parents, and the generated seed event.
- The record correctly omits a definition, synonyms, taxon associations, curated evidence, and causal edges because the raw GOLD parent bucket has no direct assertion counts and no maintained curation or causal overlay was found.
- The consequential semantic gap is limited to one inherited source-path parent: `parent_habitats` makes GOLD's freshwater `Aquifer` concept a child of ENVO `fresh water`.
- Hidden/ignored-inclusive exact searches covered `curation`, `history`, `curation/causal_graphs`, `reports/yaml_record_review`, and `research/habitats` for `habitatmech:GOLD.d61fa6c9ee`, `gold.ecosystem:5373`, `aquifer__f620c815`, and `Environmental > Aquatic > Freshwater > Aquifer`. They found no existing curation decision, history record, causal graph, research note, or prior YAML review for this target.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| `HM-AQUIFER-F620C815-001` | Major | The GOLD `Environmental > Aquatic > Freshwater > Aquifer` record inherits a false ENVO parent. `parent_habitats` asserts `habitatmech:GOLD.d61fa6c9ee` is a child of `ENVO:00002011` `fresh water`, but the source concept is a freshwater-context aquifer and its valid direct ontology parent is `ENVO:00012408` `aquifer`; ENVO defines aquifer as a water-bearing geologic layer, not as water with a low concentration of dissolved solutes. | `data/habitats/aquatic/aquifer__f620c815.yaml` lists both `ENVO:00002011` and `ENVO:00012408`; `data/raw/gold_path_triads.tsv` shows `ENVO:00002011` is the medium MIxS term for the broader GOLD `Environmental > Aquatic > Freshwater` path, not for `Environmental > Aquatic > Freshwater > Aquifer`; `data/raw/ontology_terms.tsv` defines `ENVO:00002011` as `fresh water` and `ENVO:00012408` as `aquifer`. | Add a curated `curation/term_requests.tsv` row for `habitatmech:GOLD.d61fa6c9ee` that uses `ENVO:00012408` as the authored parent and `parent_mode=REPLACE` to drop the false inherited `ENVO:00002011` edge. |

No blocker or minor findings found.

## Recommended Edits

1. Add a curated definition and hierarchy row in `curation/term_requests.tsv` for `habitatmech:GOLD.d61fa6c9ee`, with `parent_class` set to `ENVO:00012408`, `parent_label` set to `aquifer`, and `parent_mode=REPLACE` so regeneration drops the false `ENVO:00002011` source-path parent while re-adding the valid aquifer genus.
2. Regenerate the GOLD freshwater `Aquifer` record with a canary and inspect `data/habitats/aquatic/aquifer__f620c815.yaml` before any wider seed run.
3. Re-check `data/habitats/aquatic/biofilm__5f164a0f.yaml` and `data/habitats/aquatic/sediment__cd41307f.yaml` after regeneration and confirm both still keep `habitatmech:GOLD.d61fa6c9ee` as their source-path parent.

## Follow-up Checks

| Edit | Narrowest proving check |
|---|---|
| Suppress the false fresh-water parent edge. | Re-read the new `curation/term_requests.tsv` row, run `just seed-canary habitatmech:GOLD.d61fa6c9ee`, and confirm `data/habitats/aquatic/aquifer__f620c815.yaml` keeps `ENVO:00012408` and drops `ENVO:00002011`. |
| Preserve the biofilm and sediment children. | Inspect `data/habitats/aquatic/biofilm__5f164a0f.yaml` and `data/habitats/aquatic/sediment__cd41307f.yaml` and confirm their `parent_habitats` still include `habitatmech:GOLD.d61fa6c9ee`. |
| Validate the regenerated record and corpus. | `just validate data/habitats/aquatic/aquifer__f620c815.yaml`, `just validate-strict data/habitats/aquatic/aquifer__f620c815.yaml --quiet`, `just validate-history`, `just term-requests-check`, and `just verify-corpus --max-diffs 1`. |

## Additional Notes

- The direct GOLD `Environmental > Aquatic > Freshwater > Aquifer` row has no direct organism, study, or biosample assertions in `data/raw/gold_ecosystem_paths.tsv`; it is still a real generated record because it is the parent bucket for the GOLD `Biofilm` and `Sediment` leaves.
- The valid aquifer parent is deliberately generic. This source record should remain a HabitatMech GOLD concept rather than merging into ENVO `aquifer` exactly because it represents aquifers in GOLD's freshwater branch.
