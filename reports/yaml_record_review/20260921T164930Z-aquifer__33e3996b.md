# YAML Record Review: Aquifer

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/aquifer__33e3996b.yaml
- Started UTC: 2026-09-21T16:49:30Z
- Finished UTC: 2026-09-21T16:49:55Z
- Verdict: needs curation

## Target

The target is the generated `HabitatRecord` at `data/habitats/aquatic/aquifer__33e3996b.yaml`.

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.f8b744283b` |
| Label | `Aquifer` |
| Category | `AQUATIC` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Source concepts | GOLD `gold.ecosystem:5736` |
| Locked stem | `aquifer__33e3996b` in `data/habitats/PATHS.tsv` |

This review covers the GOLD `Environmental > Aquatic > Marine > Aquifer` source concept grounded as a narrow child of ENVO `aquifer`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/aquifer__33e3996b.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/aquifer__33e3996b.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 records on disk, 0 missing, 0 extra, 0 differing. |
| Reference validator | Not checked: the generated record has no DOI/PMID/URL `EvidenceItem`, no causal-edge evidence, and no curated causal overlay. |

## Identity and Grounding

| Claim | Evidence | Assessment |
|---|---|---|
| The generated record denotes GOLD's marine `Aquifer` source concept. | `data/raw/gold_ecosystem_paths.tsv` has canonical path `Environmental > Aquatic > Marine > Aquifer`, leaf `Aquifer`, depth `4`, one GOLD node ID `gold.ecosystem:5736`, and zero organism, study, biosample, or total assertions. | Supported exactly. |
| The locked generated identifier is stable. | `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.f8b744283b` to stem `aquifer__33e3996b`; hidden/ignored-inclusive exact search found no curation, history, causal-graph, research, or previous YAML review entry for that identifier or its GOLD source ID. | Supported exactly. |
| The `ENVO:00012408` parent is a valid broader parent. | `data/raw/ontology_terms.tsv` lists active term `ENVO:00012408` with label `aquifer`; ENVO defines aquifer as an underground water-bearing permeable rock or unconsolidated-material layer, and `data/raw/ontology_subclass_edges.tsv` places it under `ENVO:01001275` `solid layer`. | Supported as a broader parent for GOLD's more specific marine aquifer bucket. |
| The `ENVO:00001999` parent is generated from the upstream GOLD `Marine` path parent. | `data/raw/gold_path_triads.tsv` records `Environmental > Aquatic > Marine` samples with `local = ENVO:00001999 marine water body`; `data/habitats/PATHS.tsv` maps `ENVO:00001999` to `marine_water_body`, and the target record lists `ENVO:00001999` in `parent_habitats`. | Needs curation; the GOLD source-path context is marine, but a marine aquifer is not a subtype of a lentic marine water body. |
| The GOLD `Formation fluid` child confirms this path is geological, not a marine water body subtype. | `data/raw/gold_ecosystem_paths.tsv` lists `Environmental > Aquatic > Marine > Aquifer > Formation fluid` under the target path as `gold.ecosystem:5737`; `data/habitats/aquatic/formation_fluid.yaml` grounds that child exactly to `ENVO:03600007` `formation fluid` with this GOLD aquifer record as its source-path parent. | Supports keeping `Aquifer` as a source-derived geologic context while suppressing the inherited `marine water body` parent. |

## Evidence

The generated record has no record-level `evidence` entries and no `causal_graphs`.

| Claim | Evidence | Assessment |
|---|---|---|
| The GOLD source attestation preserves the source path and source ID. | `data/raw/gold_ecosystem_paths.tsv` has `gold.ecosystem:5736` on the `Environmental > Aquatic > Marine > Aquifer` path with leaf `Aquifer`; the generated `source_attestations` entry carries the same source ID, label, and path. | Supported exactly. |
| The target path has no direct sample counts in the raw GOLD path table. | The canonical GOLD row for `Environmental > Aquatic > Marine > Aquifer` reports `organism_count=0`, `study_count=0`, `biosample_count=0`, and `total_assertions=0`, matching the absence of generated characteristic taxa or assertion counts on the source attestation. | Supported exactly. |
| The one asserted descendant is modeled separately. | The raw GOLD table gives the `Formation fluid` child one GOLD node; `data/raw/gold_path_biosamples.tsv` reports three biosamples for the child path, and `data/habitats/aquatic/formation_fluid.yaml` is the exact ENVO `formation fluid` record for that source concept. | Supported exactly. |

## Completeness

- The generated record is complete enough for a seeded, source-derived GOLD concept: it has the stable HabitatMech identifier, the GOLD source attestation, its generated source-path parents, and the generated seed event.
- The record correctly omits a definition, synonyms, taxon associations, curated evidence, and causal edges because the raw GOLD parent bucket has no direct assertion counts and no maintained curation or causal overlay was found.
- The consequential semantic gap is limited to one inherited source-path parent: `parent_habitats` makes GOLD's marine `Aquifer` concept a child of ENVO `marine water body`.
- Hidden/ignored-inclusive exact searches covered `curation`, `history`, `curation/causal_graphs`, `reports/yaml_record_review`, and `research/habitats` for `habitatmech:GOLD.f8b744283b`, `gold.ecosystem:5736`, `aquifer__33e3996b`, and `Environmental > Aquatic > Marine > Aquifer`. They found no existing curation decision, history record, causal graph, research note, or prior YAML review for this target.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| `HM-AQUIFER-33E3996B-001` | Major | The GOLD `Environmental > Aquatic > Marine > Aquifer` record inherits a false ENVO parent. `parent_habitats` asserts `habitatmech:GOLD.f8b744283b` is a child of `ENVO:00001999` `marine water body`, but the source concept is a marine-context aquifer and its valid direct ontology parent is `ENVO:00012408` `aquifer`; ENVO defines aquifer as a water-bearing geologic layer, not as a lentic water body composed primarily of marine water. | `data/habitats/aquatic/aquifer__33e3996b.yaml` lists both `ENVO:00001999` and `ENVO:00012408`; `data/raw/gold_path_triads.tsv` shows `ENVO:00001999` is the local MIxS term for the broader GOLD `Environmental > Aquatic > Marine` path, not for `Environmental > Aquatic > Marine > Aquifer`; `data/raw/ontology_terms.tsv` defines `ENVO:00001999` as `marine water body` and `ENVO:00012408` as `aquifer`. | Add a curated `curation/term_requests.tsv` row for `habitatmech:GOLD.f8b744283b` that uses `ENVO:00012408` as the authored parent and `parent_mode=REPLACE` to drop the false inherited `ENVO:00001999` edge. |

No blocker or minor findings found.

## Recommended Edits

1. Add a curated definition and hierarchy row in `curation/term_requests.tsv` for `habitatmech:GOLD.f8b744283b`, with `parent_class` set to `ENVO:00012408`, `parent_label` set to `aquifer`, and `parent_mode=REPLACE` so regeneration drops the false `ENVO:00001999` source-path parent while re-adding the valid aquifer genus.
2. Regenerate the GOLD `Aquifer` record with a canary and inspect `data/habitats/aquatic/aquifer__33e3996b.yaml` before any wider seed run.
3. Re-check `data/habitats/aquatic/formation_fluid.yaml` after regeneration and confirm it still keeps `habitatmech:GOLD.f8b744283b` as its source-path parent.

## Follow-up Checks

| Edit | Narrowest proving check |
|---|---|
| Suppress the false marine-water-body parent edge. | Re-read the new `curation/term_requests.tsv` row, run `just seed-canary habitatmech:GOLD.f8b744283b`, and confirm `data/habitats/aquatic/aquifer__33e3996b.yaml` keeps `ENVO:00012408` and drops `ENVO:00001999`. |
| Preserve the exact formation-fluid child. | Inspect `data/habitats/aquatic/formation_fluid.yaml` and confirm its `parent_habitats` still includes `habitatmech:GOLD.f8b744283b`. |
| Validate the regenerated record and corpus. | `just validate data/habitats/aquatic/aquifer__33e3996b.yaml`, `just validate-strict data/habitats/aquatic/aquifer__33e3996b.yaml --quiet`, `just validate-history`, `just term-requests-check`, and `just verify-corpus --max-diffs 1`. |

## Additional Notes

- The direct GOLD `Environmental > Aquatic > Marine > Aquifer` row has no direct organism, study, or biosample assertions in `data/raw/gold_ecosystem_paths.tsv`; it is still a real generated record because it is the parent bucket for the GOLD `Formation fluid` leaf.
- The valid aquifer parent is deliberately generic. This source record should remain a HabitatMech GOLD concept rather than merging into ENVO `aquifer` exactly because it represents aquifers in GOLD's marine branch.
