# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/terrestrial/agricultural_land__1576a8d1.yaml`
- Started UTC: 2026-09-24T04:35:00Z
- Finished UTC: 2026-09-24T04:42:09Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/terrestrial/agricultural_land__1576a8d1.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.41b3bd5e75` |
| Label | `Agricultural land` |
| Category | `TERRESTRIAL` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained owners | Generated from `data/raw/gold_ecosystem_paths.tsv`, `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, `data/habitats/PATHS.tsv`, and `src/habitatmech/seed.py`; `data/habitats/` is generated and remains read-only |

This generated GOLD-only record represents `gold.ecosystem:4213`:

```text
Environmental > Terrestrial > Soil > Unclassified > Agricultural land
```

The seeder currently keeps it split from the reviewed `habitatmech:GOLD.a0c9778ffa` source concept for `Environmental > Terrestrial > Soil > Agricultural land`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/terrestrial/agricultural_land__1576a8d1.yaml` | Passed: LinkML reported no issues |
| `just validate-strict data/habitats/terrestrial/agricultural_land__1576a8d1.yaml` | Passed: 1 file scanned, 0 files with errors, 0 total error rows |
| `just validate-causal <overlay>` | Not applicable: the record has no `causal_graphs` slot, and ignored/hidden-inclusive searches of `curation/causal_graphs/` found no overlay for `habitatmech:GOLD.41b3bd5e75` or `agricultural_land__1576a8d1` |
| `just validate-causal-all` | Passed: 32 causal-graph curation files with 32 graphs |
| `just term-requests-check` | Passed: term-request table is current with 109 terms |
| `just validate-history` | Passed: no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Passed: expected 3,206 records, found 3,206, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | Passed: 0 ungrounded records still undecided, 1,810 decisions on file |
| `just report` | Passed: corpus report completed for 3,206 records |
| Reference validator | Not applicable: this record has no DOI, PMID, URL, `EvidenceItem`, or causal-edge evidence that would need citation checking |
| `git diff --check` | Passed |

## Identity and Grounding

Supported generated claims:

| Claim | Maintained evidence | Assessment |
|---|---|---|
| The output identifier is `habitatmech:GOLD.41b3bd5e75`. | `data/habitats/PATHS.tsv` pins that identifier to `agricultural_land__1576a8d1`. | Supported. |
| The GOLD source ID is `gold.ecosystem:4213`. | `data/raw/gold_ecosystem_paths.tsv` has one row for `Environmental > Terrestrial > Soil > Unclassified > Agricultural land` with one GOLD node ID and 364 organism assertions. | Supported. |
| The record is a terrestrial source concept. | `src/habitatmech/seed.py` infers `TERRESTRIAL` from the leading `Environmental > Terrestrial` GOLD levels. | Supported. |
| `mapping_status` is `SEEDED`. | Ignored/hidden-inclusive exact searches found no item-level `curation/decisions.tsv` decision for `habitatmech:GOLD.41b3bd5e75` or `gold.ecosystem:4213`. | Supported. |

The generated grounding needs item-level curation before the two parents can be trusted together:

- `ENVO:00000077` `agricultural ecosystem` is attached by the GOLD ambiguous-leaf rule because ENVO lists `agricultural land` as a synonym.
- `ENVO:00001998` `soil` is attached by the GOLD parent-path pass because the structured raw row leaves the `ecosystem_subtype` column blank and therefore makes the computational parent path `Environmental > Terrestrial > Soil`.
- The same GOLD node appears in `data/raw/gold_path_biosamples.tsv` under the stripped path `Environmental > Terrestrial > Soil > Agricultural land`, not under the `Unclassified` path used as this record's `source_path`.
- The stripped path is also present as a separate `data/raw/gold_ecosystem_paths.tsv` row for `gold.ecosystem:5860|gold.ecosystem:7674`; that sibling is already item-reviewed as agreeing with `ENVO:00000077`.

No maintained row currently explains why `gold.ecosystem:4213` should remain a separate habitat from the reviewed stripped-path sibling or why a separate `Agricultural land` concept should be strictly narrower than both an ecosystem and a soil material.

## Evidence

The generated source attestation accurately transcribes the GOLD 2021 bulk-export row:

- `source: GOLD`
- `source_id: gold.ecosystem:4213`
- `source_label: Agricultural land`
- `source_path: Environmental > Terrestrial > Soil > Unclassified > Agricultural land`
- `mapping_predicate: skos:narrowMatch`
- `assertion_count: 364`
- `assertion_unit: ORGANISM`

The GOLD biosample/triad rows do not use that exact `Unclassified` path. `data/raw/gold_path_biosamples.tsv` maps node `4213` to:

```text
Environmental > Terrestrial > Soil > Agricultural land
```

The corresponding `data/raw/gold_path_triads.tsv` rows for that stripped path summarize 2,186 samples from 89 studies and rank `ENVO:00000446` `terrestrial biome` as broad scale, `ENVO:00000114` `agricultural field` as local scale, and `ENVO:00002259` `agricultural soil` as medium. Those MIxS triads support a terrestrial agricultural-soil context, but by themselves they do not prove that the `Unclassified` GOLD node is a novel child of `agricultural ecosystem`, a duplicate of the reviewed stripped source path, or exactly `agricultural soil`.

The record has no curator-authored record-level evidence, environmental parameters, characteristic taxa, or causal graph. That is acceptable for a source-seeded record, but it leaves the conflicting source-path normalization unresolved.

## Completeness

The record is complete for the current raw GOLD row. It is not complete enough to be treated as reviewed because its source concept has not been reconciled against the sibling stripped GOLD path.

| Slot | Status |
|---|---|
| Source attestation | Complete for `gold.ecosystem:4213` and its 364 `ORGANISM` assertions from `data/raw/gold_ecosystem_paths.tsv` |
| Definition | Empty because no `curation/term_requests.tsv` row defines `habitatmech:GOLD.41b3bd5e75` |
| Causal graph | Empty; ignored/hidden-inclusive searches found no target overlay |
| Environmental parameters | Empty; no maintained environment-parameter row is keyed to this minted identifier |
| Characteristic taxa | Empty; no PREGO, BacDive, or Madin row contributes taxa to this minted GOLD record |
| Evidence | Empty; no curator-authored definition, hierarchy edge, or causal edge exists that would need claim-level literature |

Bounded absence checks used ignored and hidden files:

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-agricultural_land__1576a8d1.md' -print` found no prior exact-stem review before this report was written.
- Exact searches for `habitatmech:GOLD.41b3bd5e75`, `gold.ecosystem:4213`, and `Environmental > Terrestrial > Soil > Unclassified > Agricultural land` covered `data/raw`, `data/habitats`, `curation`, and `reports/yaml_record_review`.
- Exact searches of `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/external_xrefs.tsv`, and `curation/causal_graphs` found no row for `habitatmech:GOLD.41b3bd5e75` or `gold.ecosystem:4213`.
- Exact searches for `habitatmech:GOLD.41b3bd5e75` and `agricultural_land__1576a8d1` covered `curation/causal_graphs`; no target overlay was found.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| HM-AGLAND1576-001 | major | `gold.ecosystem:4213` is not consistently identified across the maintained GOLD-derived inventories. `data/raw/gold_ecosystem_paths.tsv` preserves it as `Environmental > Terrestrial > Soil > Unclassified > Agricultural land`, while `data/raw/gold_path_biosamples.tsv` strips the filler and places the same node under `Environmental > Terrestrial > Soil > Agricultural land`. The unreviewed split lets the seeder publish a second `Agricultural land` habitat beside the already reviewed stripped-path source. | Review `gold.ecosystem:4213` at item depth in `curation/decisions.tsv` and either merge it to the existing agricultural-land source with `SAME_AS` or keep it distinct with notes explaining why the `Unclassified` level is meaningful. |
| HM-AGLAND1576-002 | major | The generated parent set mixes two readings of the source. `ENVO:00000077` comes from the `agricultural land` synonym route, while `ENVO:00001998` comes from the stripped `Soil` source parent. No inspected maintained row proves the minted concept is a strict subclass of both an agricultural ecosystem and a soil material. | After the item-level identity decision, regenerate from the maintained decision or add a definition in `curation/term_requests.tsv` with an authored genus and `parent_mode` that matches the reviewed identity. |

No blocker or minor findings were found.

## Recommended Edits

1. Add an item-level `curation/decisions.tsv` row for `habitatmech:GOLD.41b3bd5e75`.
2. If node `4213` is only the stripped `Environmental > Terrestrial > Soil > Agricultural land` source represented in a different GOLD level, record a `SAME_AS` decision to merge it with the reviewed `habitatmech:GOLD.a0c9778ffa` concept.
3. If node `4213` is a distinct agricultural-soil concept, use `GROUND` or `GROUND_AS_PARENT` against the correct verified term, or add a HabitatMech term request with an authored definition and a `REPLACE` parent mode that removes the false inherited parent.
4. Re-seed the corpus and confirm `data/habitats/terrestrial/agricultural_land__1576a8d1.yaml` either disappears into the merged target or keeps only strictly broader parents.

## Follow-up Checks

| Scope | Command |
|---|---|
| Dry-run regeneration | `just seed` |
| Target canary | `just seed-canary habitatmech:GOLD.41b3bd5e75` |
| Causal overlays | `just validate-causal-all` |
| Target schema | `just validate data/habitats/terrestrial/agricultural_land__1576a8d1.yaml` |
| Closed-schema validation | `just validate-strict data/habitats/terrestrial/agricultural_land__1576a8d1.yaml` |
| Term-request exports | `just term-requests-check` |
| History | `just validate-history` |
| Corpus reproduction | `just verify-corpus --max-diffs 1` |
| Worklist/report sanity | `just worklist --limit 2000` and `just report` |
| Diff hygiene | `git diff --check` |

Manual follow-up should verify that:

- `gold.ecosystem:4213` contributes to exactly one regenerated habitat.
- Any retained `parent_habitats` entry is strictly broader than the curated identity.
- No new row treats a MIxS triad term as the record identity merely because the term appears in the stripped path's broad, local, or medium slot.

## Additional Notes

- `habitatmech:GOLD.a0c9778ffa`, the stripped-path sibling for `gold.ecosystem:5860|gold.ecosystem:7674`, already has an item-level `REVIEW` row in `curation/decisions.tsv`; this report does not reopen that decision for the sibling source concept.
- `ENVO:00002259` `agricultural soil` is a tempting near match because it is the dominant medium term for the stripped GOLD path and already exists in HabitatMech with a causal overlay. Future curation still needs to decide source identity first; the MIxS medium slot alone is contextual evidence, not an identity assertion.
