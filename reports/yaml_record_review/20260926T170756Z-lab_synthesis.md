# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/other/lab_synthesis.yaml`
- Started UTC: 2026-09-26T17:07:56Z
- Finished UTC: 2026-09-26T17:07:56Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/other/lab_synthesis.yaml` |
| Identifier | `habitatmech:BACDIVE.0e92e77cd4` |
| Label | `Lab-synthesis` |
| Category | `OTHER` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Generated status | Generated from committed inventories and `curation/decisions.tsv`; `data/habitats/PATHS.tsv` pins the identifier to `lab_synthesis` |
| Source concept | `bacdive.isolation_source:lab-synthesis` |

The record has one BacDive source attestation and three BacDive-associated taxa.
It has no definition, synonyms, parents, xrefs, environmental parameters,
evidence items, causal graphs, discussions, or datasets.

## Validation

| Check | Result |
|---|---|
| `just worklist --limit 0 --status all --out /tmp/habitatmech-worklist.tsv` | Passed; wrote 953 ungrounded rows, with `Lab-synthesis` at row 244 |
| `just report` | Passed; current corpus has 3,206 records, 953 `UNGROUNDED` records, and 830 class-level sweeps |
| `just validate data/habitats/other/lab_synthesis.yaml` | Passed |
| `just validate-strict data/habitats/other/lab_synthesis.yaml` | Passed; 1 file scanned, 0 error rows |
| `just validate-causal-all` | Passed; 32 causal-graph curation files validated |
| `just term-requests-check` | Passed; the generated term-request table is current |
| `just validate-history` | Passed; 77 history records are valid |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 records found, no missing, extra, or differing records |
| `git diff --check` | Passed |

No separate reference validator is exposed in the HabitatMech `justfile`.
This record has no `EvidenceItem`, causal-graph edge, discussion, or dataset
references to validate.

## Identity and Grounding

The record identity is internally reproducible:

- `data/habitats/PATHS.tsv` maps `habitatmech:BACDIVE.0e92e77cd4` to
  `lab_synthesis`.
- `data/raw/bacdive_isolation_sources.tsv` records
  `bacdive.isolation_source:lab-synthesis`, label `Lab-synthesis`, slug
  `lab-synthesis`, `assertion_count` 3, and 3 distinct taxon assertions.
- `data/raw/isolation_source_groundings.tsv` has a row for `Lab-synthesis`
  whose ontology-target columns are empty, so the seeded record correctly kept
  the BacDive source concept minted and `UNGROUNDED` instead of re-guessing a
  lexical target.

The only curation decision for this source concept is:

```tsv
habitatmech:BACDIVE.0e92e77cd4	CONFIRM_UNGROUNDED				claude-opus-5	2026-08-12	Class-level sweep (see docs/HARMONIZATION.md#class-level-sweep): no term in the vendored slice matched this label by any search route. Whether the concept is a habitat at all was NOT assessed, so this is not yet a term-request candidate.	CLASS
```

That row explains why `mapping_status` remains `SEEDED`: the class-level sweep
proved no mechanical lexical route matched the BacDive label, but it explicitly
did not decide whether `Lab-synthesis` denotes a habitat at all.

The related GOLD concept `habitatmech:GOLD.e4a3c08b86` for
`Engineered > Lab synthesis` has been item-reviewed and grounded to
`ENVO:01001405` `laboratory environment` with a `BROAD` relationship. That GOLD
decision is not enough to decide the BacDive row automatically, but it is a
specific maintained comparison point that this BacDive record has not yet been
reviewed against.

The vendored ontology slice contains `ENVO:01001405` `laboratory environment`
and `ENVO:01001406` `laboratory facility`. An ignored/hidden-inclusive exact
search of `data/raw/ontology_terms.tsv` for `lab synthesis`, `lab-synthesis`,
and `laboratory synthesis` found no direct ontology term for the source label.

## Evidence

The BacDive source attestation is faithfully copied from
`data/raw/bacdive_isolation_sources.tsv`:

| Field | YAML value | Maintained row |
|---|---|---|
| `source_id` | `bacdive.isolation_source:lab-synthesis` | `bacdive.isolation_source:lab-synthesis` |
| `source_label` | `Lab-synthesis` | `Lab-synthesis` |
| `assertion_count` | 3 | 3 |
| `assertion_unit` | `STRAIN` | BacDive isolation-source counts are strains |

The `source_attestations` note is also supported: kg-microbe's
`isolation_source_groundings.tsv` has a row for `Lab-synthesis` with no
ontology target.

The three taxon associations match `data/raw/bacdive_source_taxa.tsv` exactly:

| Rank | Taxon | Label | Count |
|---:|---|---|---:|
| 1 | `NCBITaxon:1404` | `Priestia megaterium` | 1 |
| 2 | `NCBITaxon:1869344` | `Curtobacterium sp.` | 1 |
| 3 | `NCBITaxon:1883` | `Streptomyces` | 1 |

None of those taxa are marked `is_characteristic`, so the record only claims
BacDive-reported association and does not inflate single-strain observations
into characteristic-taxon evidence.

## Completeness

Ignored and hidden files were included in the exact searches for
`habitatmech:BACDIVE.0e92e77cd4`, `BACDIVE.0e92e77cd4`,
`bacdive.isolation_source:lab-synthesis`, `Lab-synthesis`, and
`lab_synthesis` across `data/habitats`, `data/raw`, `curation`, `history`,
`research`, and `reports/yaml_record_review`.

Those searches found the generated YAML, the slug row, the BacDive
isolation-source row, the empty upstream kg-microbe mapping row, the three
BacDive taxon rows, and the class-level curation decision. They found no
target-specific `curation/term_requests.tsv` row, no target-specific
`history/` record, no deep-research report, and no prior exact
`reports/yaml_record_review` report for this identifier.

`find curation/causal_graphs -maxdepth 1 -type f -name lab_synthesis.yaml
-print` found no target causal overlay. That is acceptable for this sparse,
seed-only BacDive source because no mechanism claim is present in the generated
record.

The empty definition, parents, xrefs, environmental parameters, evidence,
discussion, and dataset slots are not unsupported claims. The consequential gap
is narrower: before this record can be treated as a reviewed ungrounded habitat
or linked to a broader laboratory term, an item-level curation row must inspect
the BacDive source concept itself.

## Findings

### Blocker

None found.

### Major

1. `habitatmech:BACDIVE.0e92e77cd4` is still only class-reviewed even though a
   near-identical `Lab synthesis` GOLD concept has an item-level laboratory
   grounding.

   Owner: `curation/decisions.tsv`

   The current `CONFIRM_UNGROUNDED` row has `review_depth` `CLASS` and says
   the sweep did not assess whether the source concept is a habitat. The
   repository already has an `ITEM` decision for GOLD's `Engineered > Lab
   synthesis`, mapped to `ENVO:01001405` `laboratory environment` as broader
   than synthesis. Because the BacDive row differs only in punctuation but is
   backed by a separate kg-microbe mapping row whose target was intentionally
   left empty, a curator still needs to decide one of the substantive
   alternatives for the BacDive concept: same reviewed treatment as the GOLD
   source, a broader/xref relation to `ENVO:01001405`, a real ungrounded
   habitat that needs an ENVO request, or `NOT_APPLICABLE` if BacDive's
   `Lab-synthesis` is a procedure rather than a habitat.

### Minor

None found.

## Recommended Edits

Replace the existing class-level `curation/decisions.tsv` row for
`habitatmech:BACDIVE.0e92e77cd4` with an item-level row after comparing the
BacDive isolation-source meaning with the already-reviewed GOLD
`Engineered > Lab synthesis` concept and ENVO's `laboratory environment` and
`laboratory facility` terms.

The replacement row should use one exact decision for the source concept:

- `GROUND` or `GROUND_AS_PARENT` to `ENVO:01001405` only if the BacDive concept
  is a habitat under the same laboratory-environment scope as GOLD's Lab
  synthesis concept.
- `CONFIRM_UNGROUNDED` with `review_depth` `ITEM` if BacDive really denotes a
  microbial habitat but no vendored term fits.
- `NOT_APPLICABLE` if the BacDive label denotes laboratory synthesis as a
  process or provenance mode rather than a microbial habitat.

Only add a `curation/term_requests.tsv` row if the item review concludes that
`Lab-synthesis` is a real habitat needing a novel term.

## Follow-up Checks

After the item-level decision:

- Run `just seed`.
- Run `just seed-canary habitatmech:BACDIVE.0e92e77cd4` if the source remains
  minted, or `just seed-canary ENVO:01001405` if it merges into the laboratory
  environment record, then inspect the regenerated output for the chosen
  identity, relation, category, source attestation, and curation history.
- Run `just validate data/habitats/other/lab_synthesis.yaml` if the record
  remains minted, or the new grounded output path if the source merges into an
  ontology record.
- Run `just validate-strict <record-path>`.
- Run `just term-requests-check` if a term request is added or changed.
- Run `just validate-history`.
- Run `just verify-corpus --max-diffs 1`.
- Run `just report` and confirm the worklist no longer contains
  `habitatmech:BACDIVE.0e92e77cd4` as a class-level `Lab-synthesis` item.

## Additional Notes

This review did not launch paid deep research, contact an upstream provider, or
inspect live BacDive web pages. The generated record has enough committed
inventory to audit the current YAML shape; deciding the source concept's final
meaning belongs to a later curation edit.
