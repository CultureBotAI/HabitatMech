# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/engineered/abattoir.yaml`
- Started UTC: 2026-09-22T17:21:57Z
- Finished UTC: 2026-09-22T17:21:58Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `ENVO:01000925` |
| Label | `abattoir` |
| Class | `HabitatRecord` |
| Category | `ENGINEERED` |
| Grounding status | `EXACT` |
| Mapping status | `REVIEWED` |
| Maintained owner | `curation/decisions.tsv:1127`, `data/raw/gold_ecosystem_paths.tsv:1279`, and vendored ontology rows |
| Locked slug | `data/habitats/PATHS.tsv:866` maps `ENVO:01000925` to `abattoir` |

This is the generated, reviewed GOLD record for `Engineered > Built environment > Slaughterhouse/Abattoir`. The item-level decision in `curation/decisions.tsv` grounds the slash-joined GOLD label to the exact ENVO `abattoir` class, the record keeps the raw GOLD label as a source synonym, and the generated parents combine ENVO's `industrial building` superclass with GOLD's `Built environment` parent path.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/engineered/abattoir.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/engineered/abattoir.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The generated identifier, label, definition, category, grounding status, source attestation, and reviewed status are supported by maintained inputs:

- `data/raw/gold_ecosystem_paths.tsv:1279` is the raw GOLD row for `Engineered > Built environment > Slaughterhouse/Abattoir`. It has depth 3, three collapsed GOLD ecosystem node ids, zero primary organism/study/biosample assertions, and total primary assertion count 0.
- `curation/decisions.tsv:1127` keys the source concept `habitatmech:GOLD.cbcb2124b2` to `ENVO:01000925` with a `GROUND` / `EXACT` / `ITEM` decision and explains that the slash in `Slaughterhouse/Abattoir` names one thing.
- `data/raw/ontology_terms.tsv:8420` imports `ENVO:01000925` with label `abattoir`, definition `A facility in which non-human animals are slaughtered and processed for human consumption.`, and the `butchery`, `meat processing building`, `slaughter plant`, and `slaughterhouse` synonyms.
- `data/raw/ontology_subclass_edges.tsv:6718` keeps `ENVO:01000925` under `ENVO:00003861`; `data/raw/ontology_terms.tsv:7401` identifies that parent as `industrial building`.
- `data/raw/ontology_terms.tsv:13556` imports `mesh:D000076624` with label `Built Environment`, and `data/habitats/engineered/built_environment.yaml` is the GOLD parent-path record generated from `Engineered > Built environment`.
- `data/habitats/PATHS.tsv:866` pins `ENVO:01000925` to `data/habitats/engineered/abattoir.yaml`.

The two `gold_path_triads.tsv` rows that mention `ENVO:01000925` are unrelated to this record's identity: they annotate GOLD `Beef chop` and `Poultry processing wastewater` paths, not the exact `Engineered > Built environment > Slaughterhouse/Abattoir` source path.

## Evidence

Every generated claim is traceable to an ontology row, a curation decision, a raw GOLD row, or a maintained transform:

| Claim | Nearest source | Review |
|---|---|---|
| Identifier `ENVO:01000925`, label `abattoir`, `grounding_status: EXACT`, `mapping_status: REVIEWED`, and `mapping_predicate: skos:exactMatch` | `curation/decisions.tsv:1127` plus `data/raw/ontology_terms.tsv:8420` | Supported exactly |
| Definition and `definition_source: ENVO` | `data/raw/ontology_terms.tsv:8420` | Supported exactly |
| ENVO synonyms `butchery`, `meat processing building`, `slaughter plant`, `slaughterhouse` | `data/raw/ontology_terms.tsv:8420` | Supported exactly |
| GOLD synonym, source label, and source path `Engineered > Built environment > Slaughterhouse/Abattoir` | `data/raw/gold_ecosystem_paths.tsv:1279` | Supported exactly |
| `source_id: gold.ecosystem:8338` and the three-node notes text | `data/raw/gold_ecosystem_paths.tsv:1279` has `gold_node_ids` `gold.ecosystem:8338\|gold.ecosystem:8339\|gold.ecosystem:8340` | Supported exactly |
| Absence of `assertion_count` and `assertion_unit` | `data/raw/gold_ecosystem_paths.tsv:1279` has `organism_count` 0, and `src/habitatmech/seed.py` only emits GOLD `assertion_count` for nonzero organism counts | Supported exactly |
| Parent `ENVO:00003861` | `data/raw/ontology_subclass_edges.tsv:6718` | Supported exactly |
| Parent `mesh:D000076624` | GOLD source parent path `Engineered > Built environment` | Supported exactly |
| Absence of an xref, environmental parameter, characteristic taxon, causal graph, record-level evidence, discussion, and dataset | No maintained input row, target-specific side-table row, or overlay found for these slots | Supported by bounded exact searches |

## Completeness

The generated fields are complete for the maintained inputs that currently feed this record:

- The single GOLD source concept has an item-level curation decision, so `mapping_status: REVIEWED` is appropriate.
- The GOLD source attestation captures the first GOLD node id, full source path, exact mapping predicate, three-node collapse note, and no assertion count because the exact primary GOLD row has zero organism assertions.
- Both generated parents are present: the ENVO superclass and the exact GOLD `Built environment` parent-path record.
- No target-specific causal graph overlay, append-only history record, deep-research report, BacDive row, PREGO row, Madin row, GOLD bulk-export biosample row, GOLD bulk-export study row, exact GOLD MIxS triad row, or previous exact YAML review report exists for `abattoir`.

Ignored/hidden-inclusive exact searches covered `data`, `curation`, `history`, `research`, `reports`, `.claude`, `src`, and `scripts`, excluding generated `build`, `data/text_map`, and `pages` trees for broad content searches. They found the raw GOLD row, decision row, locked path row, generated record, imported ontology rows, generated GOLD parent, two unrelated GOLD MIxS triad rows for different paths, and unrelated research prose that uses the word `abattoir`; they found no target-specific maintained artifact beyond the raw, decision, path-lock, generated, and ontology rows cited above.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

No target-specific follow-up is required. If upstream GOLD, the ENVO slice, the MeSH slice, or the `habitatmech:GOLD.cbcb2124b2` curation decision changes, regenerate the target and re-run:

- `just validate data/habitats/engineered/abattoir.yaml`
- `just validate-strict data/habitats/engineered/abattoir.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

## Additional Notes

- `find reports/yaml_record_review -name '*abattoir*' -print` found no pre-existing exact review report for this record before this file was written.
- Exact ignored/hidden-inclusive content searches for `ENVO:01000925`, `habitatmech:GOLD.cbcb2124b2`, `gold.ecosystem:8338`, `gold.ecosystem:8339`, `gold.ecosystem:8340`, `Engineered > Built environment > Slaughterhouse/Abattoir`, `ENVO:00003861`, `mesh:D000076624`, and `abattoir` found no unsupported assertion, target-specific curation overlay, or prior exact review.
