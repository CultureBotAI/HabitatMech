# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/engineered/engineered__900b76ad.yaml`
- Started UTC: 2026-09-24T16:52:10Z
- Finished UTC: 2026-09-24T16:54:34Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.2acb39dd08` |
| Label | `Engineered` |
| Category | `ENGINEERED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Generated path | `data/habitats/engineered/engineered__900b76ad.yaml` |
| Source concept | GOLD `Engineered` |

The target is the depth-1 GOLD `Engineered` root source path. It is the source-path parent for depth-2 GOLD branches such as `Engineered > Bioremediation`, `Engineered > Bioreactor`, and `Engineered > Artificial ecosystem`, not one of those narrower child records.

The file is generated. `data/habitats/PATHS.tsv:1605` maps `habitatmech:GOLD.2acb39dd08` to the stable `engineered__900b76ad` slug, so future fixes belong in `curation/decisions.tsv` or future source-path parent suppression support rather than in this YAML.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/engineered__900b76ad.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/engineered__900b76ad.yaml` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| Target causal overlay validator | Not applicable; an ignored-inclusive exact search over `curation/causal_graphs` for the target identifier, exact source node IDs, and `Engineered`, plus `find curation/causal_graphs -maxdepth 1 -type f -name '*engineered*' -print`, found no target overlay. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files and 32 graphs validated. |
| Reference validator | Not applicable; this generated record has no record-level `evidence` or `causal_graphs`, and HabitatMech has no dedicated per-record reference check. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 records were expected, 3206 were found, and `data/habitats/` reproduced exactly from `data/raw/`. |
| `just worklist --limit 2000` | Passed; 0 ungrounded records remain undecided and 1810 decisions are on file. |
| `just report` | Passed; the corpus report regenerated successfully. |
| `git diff --check` | Passed. |

## Identity and Grounding

`data/raw/gold_ecosystem_paths.tsv:253` is the maintained GOLD row for this exact source concept. It has canonical path `Engineered`, source leaf `Engineered`, depth `1`, five collapsed GOLD node IDs, 68 direct organism assertions, no direct study or biosample assertions in that aggregate row, and `gold.ecosystem:2|gold.ecosystem:2891|gold.ecosystem:3521|gold.ecosystem:3858|gold.ecosystem:4292` as source node IDs.

The generated source attestation mirrors that row with GOLD `gold.ecosystem:2`, source label `Engineered`, source path `Engineered`, `assertion_count: 68`, `assertion_unit: ORGANISM`, and a note that five GOLD ecosystem node IDs share this path.

The exact GOLD path also appears in `data/raw/gold_path_biosamples.tsv:691` with seven direct biosamples for path ID `4292`. Ignored-inclusive exact raw searches found no exact study row in `data/raw/gold_studies.tsv` and no exact MIxS triad row in `data/raw/gold_path_triads.tsv`.

`curation/decisions.tsv:330` is the only target decision row. It is a class-level `CONFIRM_UNGROUNDED` row from the lexical sweep and explicitly says the sweep did not assess whether the concept is a habitat at all, so `UNGROUNDED` has not been confirmed by item-level review of this root source concept.

The root has no generated `parent_habitats`, but the exact target identifier appears as a generated parent on multiple depth-2 or ontology-grounded child records under `data/habitats/engineered/`. Those parent edges currently rest on source-path position alone.

## Evidence

The generated target has no record-level `evidence`, no `definition`, no `environmental_parameters`, no `characteristic_taxa`, and no target-specific `causal_graphs`.

Supported:

- The source-path identity and five-node collapse are reproducible from `data/raw/gold_ecosystem_paths.tsv:253`.
- The exact path has one direct GOLD biosample row in `data/raw/gold_path_biosamples.tsv:691`.
- The generated child parent edges to `habitatmech:GOLD.2acb39dd08` are reproducible from the `Engineered` source-path prefix.

Unsupported or over-scoped:

- No item-level decision has inspected whether GOLD's top-level `Engineered` branch denotes a habitat, an administrative source grouping, or a broad bin for human-designed source paths that are not all strict subclasses of one microbial environment.
- No maintained input establishes the GOLD root as a strict broader habitat for depth-2 children such as `Bioremediation`, `Biotransformation`, `Food production`, `Lab culture`, or `Modeled`.
- No `SAME_AS` decision resolves whether this GOLD root and `habitatmech:BACDIVE.7d4708950e` are the same `Engineered` grouping, even though `just report` flags the shared lower-case `engineered` label across 68 GOLD organism assertions and 7174 BacDive strain assertions.

Related curation:

- `data/habitats/other/engineered.yaml` is the separate BacDive `Engineered` record. It is `REVIEWED`, keeps `ENVO:01000313` `anthropogenic environment` as a broad parent, and has 7174 BacDive strain assertions.
- Recent YAML reviews for `sheet of paper` and `Bioremediation` both flagged this GOLD root as an unreviewed inherited parent.

## Completeness

Consequential gaps:

- The target has no item-level decision or term request.
- The target has no definition, environmental parameters, characteristic taxa, causal graph, or target-specific literature evidence.
- The target is a top-level GOLD source grouping and has no parent to constrain its meaning.
- `just report` lists `engineered` as the largest novel label held by records from different sources, so the same-label GOLD and BacDive root records still need explicit SAME_AS or not-same review.

Bounded searches:

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-engineered__900b76ad.md' -print` found no earlier YAML review report for this exact stem. `find` is not gitignore-filtered.
- An ignored-inclusive exact search over `curation`, `history`, `research`, `data/habitats/PATHS.tsv`, `data/habitats/engineered`, `reports/yaml_record_review`, `data/raw`, and `conf/id_label_targets.yaml` for `habitatmech:GOLD.2acb39dd08` found the path lock, the class-level decision row, generated child records that use this target as a parent, this target YAML, and earlier YAML reviews that mention the root parent; it found no target-specific term request, term-request exclusion, history record, deep-research report, id-label residual, or causal overlay.
- Ignored-inclusive exact searches over the same corpus for `gold.ecosystem:2`, `gold.ecosystem:2891`, `gold.ecosystem:3521`, `gold.ecosystem:3858`, and `gold.ecosystem:4292` found the raw aggregate row and this generated target, and found no target-specific curation inputs.
- An ignored-inclusive exact raw search found `data/raw/gold_ecosystem_paths.tsv:253` and `data/raw/gold_path_biosamples.tsv:691` for the exact `Engineered` path and found no exact `data/raw/gold_studies.tsv` or `data/raw/gold_path_triads.tsv` row.
- An ignored-inclusive exact `label: Engineered` and `source_label: Engineered` search under `data/habitats` found only this GOLD record and the separate BacDive `data/habitats/other/engineered.yaml` record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The target has only a class-level `CONFIRM_UNGROUNDED` row, so `UNGROUNDED` has not been item-reviewed against the top-level GOLD `Engineered` source path. | `curation/decisions.tsv:330` records `review_depth=CLASS` and says habitathood was not assessed. The exact path has one direct seven-biosample row in `data/raw/gold_path_biosamples.tsv:691`, so this is not an empty source-only branch in all committed raw GOLD tables. | `curation/decisions.tsv`. |
| Major | The unreviewed GOLD root is serialized as a strict parent for many depth-2 engineered GOLD records. | Exact ignored-inclusive searches found `habitatmech:GOLD.2acb39dd08` in generated records for `Built environment`, `Artificial ecosystem`, `Solid waste`, `Bioremediation`, `Bioreactor`, `Biotransformation`, and other children. `parent_habitats` means strictly broader, but the only source for this root is a class-level lexical sweep that did not assess whether it is a habitat. | `curation/decisions.tsv`; if item review rejects the source-derived edges, future parent-suppression support in `src/habitatmech/seed.py` or child-specific `curation/term_requests.tsv` rows with `parent_mode=REPLACE`. |

## Recommended Edits

1. Item-review `habitatmech:GOLD.2acb39dd08` in `curation/decisions.tsv` against the exact GOLD `Engineered` root and the GOLD `4292` biosamples.
2. Decide whether the GOLD root is a real top-level anthropogenic microbial habitat, a non-habitat administrative category, or the same concept as BacDive `habitatmech:BACDIVE.7d4708950e`.
3. If the root is retained as a real novel habitat, decide whether `ENVO:01000313` `anthropogenic environment` is a strict broader parent, an overlapping xref, or too narrow for food, lab-culture, product, and other material-heavy children.
4. If the root is only a source grouping, add maintained support for suppressing or replacing the inherited source-path root on child records before `src/habitatmech/seed.py` serializes `parent_habitats`.

## Follow-up Checks

- After adding a decision or merge, run `just seed`, `just seed-canary habitatmech:GOLD.2acb39dd08`, inspect `data/habitats/engineered/engineered__900b76ad.yaml`, then run `just seed-apply --force`.
- Run `just validate data/habitats/engineered/engineered__900b76ad.yaml` and `just validate-strict data/habitats/engineered/engineered__900b76ad.yaml`.
- Run `just term-requests-check`, `just validate-history`, `just verify-corpus --max-diffs 1`, `just worklist --limit 2000`, `just report`, and `git diff --check`.
- Run `just validate-causal-all` if any overlay changes.

## Additional Notes

- The exact ignored-inclusive miss searches included hidden and gitignored files.
- Reviewing this root would not by itself settle every depth-2 child edge; it would establish whether the top-level source prefix is eligible to be a strict parent at all.
