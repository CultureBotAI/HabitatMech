# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/mine_water__20810f85.yaml`
- Started UTC: 2026-09-26T17:37:02Z
- Finished UTC: 2026-09-26T17:37:02Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/mine_water__20810f85.yaml` |
| Identifier | `habitatmech:GOLD.943d5cba70` |
| Label | `Mine water` |
| Class | `HabitatRecord` |
| Category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Generated status | Generated from committed inventories and `curation/decisions.tsv`; `data/habitats/PATHS.tsv` pins the identifier to `mine_water__20810f85` |
| Source concept | GOLD `Environmental > Aquatic > Deep subsurface > Groundwater > Mine water` |

The record has one GOLD source attestation and one parent inherited from the
immediate GOLD parent path. It has no definition, synonyms, xrefs,
environmental parameters, characteristic taxa, evidence items, causal graphs,
discussions, or datasets.

## Validation

| Check | Result |
|---|---|
| `just worklist --limit 0 --status all --out /tmp/habitatmech-worklist.tsv` | Passed; wrote 953 ungrounded rows, with aquatic `Mine water` immediately after `Lab-synthesis` |
| `just report` | Passed; current corpus has 3,206 records, 953 `UNGROUNDED` records, and 830 class-level sweeps |
| `just validate data/habitats/aquatic/mine_water__20810f85.yaml` | Passed |
| `just validate-strict data/habitats/aquatic/mine_water__20810f85.yaml` | Passed; 1 file scanned, 0 error rows |
| `just validate-causal-all` | Passed; 32 causal-graph curation files validated |
| `just term-requests-check` | Passed; the generated term-request table is current |
| `just validate-history` | Passed; 77 history records are valid |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 records found, no missing, extra, or differing records |
| `git diff --check` | Passed |

No separate reference validator is exposed in the HabitatMech `justfile`.
This generated record has no `EvidenceItem`, causal-graph edge, discussion, or
dataset references to validate.

## Identity and Grounding

The generated identity and source attestation match the maintained GOLD
inventory:

- `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.943d5cba70` to
  `mine_water__20810f85`.
- `data/raw/gold_ecosystem_paths.tsv` records exactly
  `Environmental > Aquatic > Deep subsurface > Groundwater > Mine water` with
  one GOLD ecosystem node, `gold.ecosystem:5964`, and `organism_count` 3.
- The YAML `source_attestations` entry preserves that node ID, label, source
  path, and three GOLD organism assertions with `assertion_unit: ORGANISM`.

The class-level curation decision is the only decision for this minted source
concept:

```tsv
habitatmech:GOLD.943d5cba70	CONFIRM_UNGROUNDED				claude-opus-5	2026-08-12	Class-level sweep (see docs/HARMONIZATION.md#class-level-sweep): no term in the vendored slice matched this label by any search route. Whether the concept is a habitat at all was NOT assessed, so this is not yet a term-request candidate.	CLASS
```

That row explains why the generated record remains `UNGROUNDED` and `SEEDED`.
It does not prove that the source concept lacks a broader ontology parent.

The committed GOLD triad inventory gives a specific and internally consistent
habitat reading for all 206 biosamples from `gold.ecosystem:5964`:

| Slot | Term | Share | Studies |
|---|---|---:|---:|
| Broad | `ENVO:00000873` `freshwater biome` | 1.00 | 1 |
| Local | `ENVO:00000076` `mine` | 1.00 | 1 |
| Medium | `ENVO:01001004` `groundwater` | 1.00 | 1 |

The vendored ontology slice has `ENVO:01001004` `groundwater` as an exact term
for underground pore-space water and has `ENVO:00000076` `mine` as an exact
term for a mine feature. Read together with GOLD's source path, the triad says
this source concept is mine-localized groundwater. The mine term is local
context, not the identity of the water habitat.

The worklist candidates `ENVO:00002010` `saline water`,
`mesh:D000068357` `Saline Waters`, `ENVO:00001999` `marine water body`, and
`ENVO:01000599` `river water` are lexical or label-near terms from other water
contexts. None is supported by this GOLD path or its triad as an exact identity
or strict parent for mine-localized groundwater.

`ENVO:00001996` `mine drainage` is a closer active term with the definition
`The outflow of water from a mine.`, and it must be compared before requesting a
new mine-water term. It is already the ontology parent of two generated GOLD
`Mine drainage` records:

- `data/habitats/aquatic/mine_drainage.yaml` is GOLD
  `Environmental > Aquatic > Freshwater > Groundwater > Mine drainage`, seeded
  as `NARROW` under `ENVO:00001996` and the generated GOLD Freshwater
  Groundwater parent.
- `data/habitats/terrestrial/mine_drainage__bd3e47d7.yaml` is GOLD
  `Environmental > Terrestrial > Soil > Unclassified > Mine drainage`, seeded as
  `NARROW` under `ENVO:00001996` and `ENVO:00001998` `soil`.

Those existing records do not prove that ENVO's outflow term is an exact match,
strict parent, or overlapping neighbor for GOLD deep-subsurface Mine water; they
show that the relation must be decided explicitly before adding a new
mine-water term request under `ENVO:01001004` `groundwater`.

An ignored/hidden-inclusive exact search of `data/raw/ontology_terms.tsv` for
`mine water` and `mine-water` found no direct same-label ontology term in the
vendored slice. An ignored/hidden-inclusive exact search for `ENVO:00001996`
found the active `mine drainage` term above and its subclass edges.

## Evidence

The record has no curator-authored definition, parameter band, causal edge, or
external citation claim.

| YAML claim | Maintained evidence | Review |
|---|---|---|
| `identifier: habitatmech:GOLD.943d5cba70` | `data/raw/gold_ecosystem_paths.tsv` canonical path `Environmental > Aquatic > Deep subsurface > Groundwater > Mine water` | Supported; this is the stable hash for the source path |
| `habitat_category: AQUATIC` | GOLD top-level path `Environmental > Aquatic` | Supported |
| `grounding_status: UNGROUNDED` | `curation/decisions.tsv` class-level `CONFIRM_UNGROUNDED` row | Reproducible but incomplete; see Findings |
| `parent_habitats: habitatmech:GOLD.22a80cbd14` | Immediate GOLD parent path `Environmental > Aquatic > Deep subsurface > Groundwater`; `data/habitats/PATHS.tsv` maps the parent source to `groundwater` | Supported as a generated source-path parent |
| GOLD source attestation | `data/raw/gold_ecosystem_paths.tsv` row with node `gold.ecosystem:5964` and `organism_count` 3 | Supported |

The generated `Groundwater` parent is itself the GOLD
`Environmental > Aquatic > Deep subsurface > Groundwater` concept, minted as
`habitatmech:GOLD.22a80cbd14` and seeded as `NARROW` under `ENVO:01001004`
`groundwater`. That means the target's broader groundwater placement is already
implicit through a seeded generated parent, but it has not been promoted to an
item-level `GROUND_AS_PARENT` decision for this exact child.

## Completeness

Ignored and hidden files were included in the exact searches for
`habitatmech:GOLD.943d5cba70`, `GOLD.943d5cba70`,
`Environmental > Aquatic > Deep subsurface > Groundwater > Mine water`,
`Mine water`, `mine_water__20810f85`, `gold.ecosystem:5964`, the GOLD
Groundwater parent, and the relevant triad and candidate CURIEs across
`data/habitats`, `data/raw`, `curation`, `history`, `research`, and
`reports/yaml_record_review`.

Those searches found the generated YAML, the PATHS row, the GOLD
ecosystem-path row, the GOLD biosample row, the three GOLD triad rows, the GOLD
study containing the exact source path, the parent Groundwater source path, and
sibling groundwater-path records. They found no target-specific
`curation/term_requests.tsv` row, no target-specific `history/` entry, no
committed deep-research report, and no prior exact YAML review for
`habitatmech:GOLD.943d5cba70`.

Ignored/hidden-inclusive searches for `ENVO:00001996`, `Mine drainage`,
`habitatmech:GOLD.342d26e2ac`, `habitatmech:GOLD.acc3f2cd61`, and the GOLD
Mine drainage source paths found the ontology `mine drainage` term, two
subclasses, two generated GOLD `Mine drainage` records already seeded under
that term, the corresponding `data/raw/gold_ecosystem_paths.tsv`,
`data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, and
`data/raw/gold_studies.tsv` rows, and one prior acid mine drainage review that
discusses a generated-parent edge issue on an exact ENVO child of
`ENVO:00001996`.

`find curation/causal_graphs -maxdepth 1 -type f -name mine_water__20810f85.yaml -print`
found no target causal overlay. That is
acceptable for this seed-only GOLD review because the generated record asserts
no mechanism graph.

The generated record has no taxa because the committed GOLD source inventory
does not include per-taxon rows. It has no environmental parameter rows because
the kg-microbe environments table is keyed to ontology terms such as
`ENVO:01001004` `groundwater`, not to this narrower minted GOLD source concept.

## Findings

### Blocker

None found.

### Major

1. `habitatmech:GOLD.943d5cba70` is still only class-reviewed even though
   `ENVO:01001004` `groundwater` is a strict broader ontology parent for the
   exact GOLD concept.

   Owner: `curation/decisions.tsv`; likely `curation/term_requests.tsv` if the
   record remains a novel minted child of groundwater

   The current decision row only records that no lexical route matched `Mine
   water`; it explicitly did not inspect whether the concept is a habitat.
   GOLD places this node under `Environmental > Aquatic > Deep subsurface >
   Groundwater`, its MIxS `env_medium` row is `ENVO:01001004` `groundwater`
   with 100% agreement for the node's 206 biosamples, and its MIxS local
   feature is `ENVO:00000076` `mine`. No vendored term directly names `mine
   water`, but the source path and triad support reviewing the record as a real
   habitat under `groundwater`.

   Item-level review must first compare GOLD Mine water with `ENVO:00001996`
   `mine drainage` and the two generated GOLD `Mine drainage` records that are
   already seeded below it. The current target may be exact, narrower, or
   overlapping with ENVO's outflow-from-a-mine term. Leaving it as only `CLASS`
   depth prevents `mapping_status: REVIEWED`, omits an explicit relation to
   `ENVO:01001004` or `ENVO:00001996`, and leaves any needed definition or term
   request unrecorded.

### Minor

None found.

## Recommended Edits

Replace the class-level `curation/decisions.tsv` row for
`habitatmech:GOLD.943d5cba70` with an item-level decision. Compare the committed
GOLD path and triad against both `ENVO:01001004` `groundwater` and
`ENVO:00001996` `mine drainage`, then record whether the source-specific GOLD
identity is an exact match, narrower match, sibling, or overlapping neighbor of
the mine-drainage term.

If the source remains minted after that comparison and is a strict child of
groundwater, use `GROUND_AS_PARENT` to attach `ENVO:01001004` `groundwater`
while keeping the source-specific minted identity. Add a
`curation/term_requests.tsv` row for a `mine water` habitat term only if
`ENVO:00001996` is not sufficient as an exact match or reusable parent. Preserve
`ENVO:00000076` `mine` only as local context in the notes or as a related xref if
a future curation surface needs that relation; do not use `mine` as the identity
or as a parent of the water habitat.

Do not merge this record with `habitatmech:GOLD.6a0644fced`, the engineered
`Engineered > Wastewater > Industrial wastewater > Mine water` source concept,
unless a separate item review establishes that GOLD is using those two paths
for the same habitat rather than for natural mine groundwater versus industrial
mine wastewater.

## Follow-up Checks

After the item-level decision:

- Run `just seed`.
- Run `just seed-canary habitatmech:GOLD.943d5cba70`.
- Inspect `data/habitats/aquatic/mine_water__20810f85.yaml` to confirm it keeps
  the GOLD source attestation, has the chosen `GROUND_AS_PARENT` or equivalent
  item-level curation history, and has a supported `parent_habitats` list.
- If a term request is added, run `just term-requests-check` and confirm the
  generated request table includes the mine-water request without duplicating
  `ENVO:00001996` `mine drainage`.
- Run `just validate data/habitats/aquatic/mine_water__20810f85.yaml`.
- Run `just validate-strict data/habitats/aquatic/mine_water__20810f85.yaml`.
- Run `just validate-history`.
- Run `just verify-corpus --max-diffs 1`.
- Run `just report` and confirm the worklist no longer contains
  `habitatmech:GOLD.943d5cba70` as a class-level ungrounded `Mine water` item.

## Additional Notes

This review did not launch deep research, contact GOLD, or inspect a local
kg-microbe checkout. The committed GOLD path, biosample, and triad inventories
are enough to audit the current YAML and to identify the maintained
item-review work this sparse generated record still needs.
