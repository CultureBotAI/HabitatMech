# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/abdomen__ca3ba4b6.yaml`
- Started UTC: 2026-09-22T20:43:00Z
- Finished UTC: 2026-09-22T20:44:58Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/host_associated/abdomen__ca3ba4b6.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.f9f745629a` |
| Label | `Abdomen` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Record status | Generated from one unreviewed GOLD source concept |

This is the minted GOLD record for `Host-associated > Fish > Abdomen`. Its identifier is the deterministic source-concept id `habitatmech:GOLD.f9f745629a`, computed from `sha1("GOLD:Host-associated > Fish > Abdomen")[:10]`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/abdomen__ca3ba4b6.yaml` | Passed; LinkML validation reported no issues. |
| `just validate-strict data/habitats/host_associated/abdomen__ca3ba4b6.yaml` | Passed; one file scanned with 0 error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 generated records, found 3206, with 0 missing, 0 extra, and 0 differing. |
| `git diff --check` | Passed; no whitespace errors. |

## Identity and Grounding

The GOLD source identity is supported:

| Raw field | Value |
|---|---|
| `canonical_path` | `Host-associated > Fish > Abdomen` |
| `ecosystem` | `Host-associated` |
| `ecosystem_category` | `Fish` |
| `ecosystem_type` | `Abdomen` |
| `ecosystem_subtype` | empty |
| `specific_ecosystem` | empty |
| `leaf_label` | `Abdomen` |
| `path_depth` | `3` |
| `gold_node_count` | `1` |
| `organism_count` | `0` |
| `metagenome_count` | `0` |
| `biosample_count` | `0` |
| `total_assertions` | `0` |
| `gold_node_ids` | `gold.ecosystem:5052` |

`source_attestations` matches `data/raw/gold_ecosystem_paths.tsv:1916`: the record keeps `gold.ecosystem:5052` as the source id, records the `Abdomen` leaf label and canonical GOLD path exactly, omits `assertion_count` because the raw organism count is zero, and does not add a multi-node note because the path has only one GOLD node id.

The source-path parent is supported. `data/raw/gold_ecosystem_paths.tsv:33` records the immediate GOLD parent path `Host-associated > Fish`, whose deterministic minted id is `habitatmech:GOLD.3d529a667e`; `data/habitats/host_associated/fish.yaml` is the reviewed `fish-associated environment` record for that parent path and keeps `animal-associated environment` as a broader ENVO parent.

The seeded `NARROW` relationship to `UBERON:0000916` is supported. `data/raw/ontology_terms.tsv:12962` defines `UBERON:0000916` as the abdomen subdivision of the vertebrate body, while GOLD scopes the source path to `Fish`. The ambiguous GOLD leaf rule correctly keeps the fish-specific abdomen path minted instead of merging it into a generic ontology abdomen record: `data/raw/gold_ecosystem_paths.tsv` has four rows whose `leaf_label` is `Abdomen`, with `Host-associated > Arthropoda: Insects > Abdomen` and `Host-associated > Fish > Abdomen` tied at the shallowest depth, so `leaf_claimants()` leaves the label unclaimed and `resolve_gold()` emits a `skos:narrowMatch` parent for each path.

`mapping_status: SEEDED` is mechanically expected. Exact ignored/hidden-inclusive searches found no `curation/decisions.tsv` row for `habitatmech:GOLD.f9f745629a`, so the automatic GOLD lexical result has not been promoted by an item-level `REVIEW` or `GROUND_AS_PARENT` decision.

## Evidence

| Claim | Nearest support | Review |
|---|---|---|
| The generated record represents GOLD node `gold.ecosystem:5052` at `Host-associated > Fish > Abdomen`. | `data/raw/gold_ecosystem_paths.tsv:1916` | Supported exactly. |
| The GOLD source row has no upstream organism, metagenome, or biosample assertions. | `data/raw/gold_ecosystem_paths.tsv:1916` | Supported exactly. |
| `Host-associated > Fish > Abdomen` is a child of `Host-associated > Fish`. | `data/raw/gold_ecosystem_paths.tsv:33`; `src/habitatmech/seed.py` | Supported. `ingest_gold()` links each GOLD row to the generated record for the next path level up. |
| `Host-associated > Fish > Abdomen` is narrower than `UBERON:0000916 abdomen`. | `data/raw/gold_ecosystem_paths.tsv:1916`; `data/raw/ontology_terms.tsv:12962` | Supported. The source path is fish-scoped, and UBERON defines the target as the abdomen of vertebrates. |
| The generated record should remain `SEEDED`. | Exact ignored/hidden-inclusive search for `habitatmech:GOLD.f9f745629a` in `curation/decisions.tsv` | Supported. No item-level decision exists for this source concept. |

Unsupported or over-scoped claims: None found.

## Completeness

No direct source attestation is missing for the GOLD source row: the record carries the source name, source id, source label, canonical source path, and mapping predicate. Its omitted `assertion_count` and omitted node-count note are both expected for a raw row with `organism_count: 0` and `gold_node_count: 1`.

The immediate child row is present and attached. `data/raw/gold_ecosystem_paths.tsv:1033` records `Host-associated > Fish > Abdomen > Ascites`, and `data/habitats/host_associated/ascites__13be9a4c.yaml` keeps `habitatmech:GOLD.f9f745629a` as its GOLD source-path parent.

The target has no definition, causal graph, environmental parameters, characteristic taxa, discussions, or datasets. Those optional slots are correctly absent for an unreviewed, zero-assertion GOLD path with no maintained `curation/term_requests.tsv`, `curation/causal_graphs`, or external evidence keyed by `habitatmech:GOLD.f9f745629a`.

No maintained decision, term request, external xref, causal overlay, history file, or raw research report was found for `habitatmech:GOLD.f9f745629a`; the exact search covered `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/term_requests/envo_robot_template.tsv`, `curation/external_xrefs.tsv`, `curation/causal_graphs`, `history`, and `research` with ignored and hidden files included.

No maintained causal overlay, history file, raw research report, or prior exact YAML review report was found by filename for `abdomen__ca3ba4b6` or `abdomen-ca3ba4b6` under `curation/causal_graphs`, `history`, `research`, or `reports/yaml_record_review`; the `find` search included ignored files beneath those checked directories.

## Findings

None found.

No blocker findings.

No major findings.

No minor findings.

## Recommended Edits

None required.

If the corpus later needs this record to become `mapping_status: REVIEWED`, add an item-level `REVIEW` row for `habitatmech:GOLD.f9f745629a` to `curation/decisions.tsv` and rerun `just seed-canary habitatmech:GOLD.f9f745629a`; this would only promote the reviewed status and should not change the identifier, source attestation, or parents.

## Follow-up Checks

None required beyond the validators already run for this report.

For any future status-promotion-only edit, run:

- `just seed-canary habitatmech:GOLD.f9f745629a`
- `just validate data/habitats/host_associated/abdomen__ca3ba4b6.yaml`
- `just validate-strict data/habitats/host_associated/abdomen__ca3ba4b6.yaml`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

## Additional Notes

- The pre-report exact filename check `find reports/yaml_record_review -maxdepth 1 -type f -name '*abdomen__ca3ba4b6*' -print` returned no previous exact report; `find` includes ignored files.
- Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.f9f745629a`, `Host-associated > Fish > Abdomen`, `gold.ecosystem:5052`, `habitatmech:GOLD.3d529a667e`, `BTO:0000020`, and `UBERON:0000916` found the generated target, the raw GOLD rows, the source-path parent record, the BTO abdomen sibling record, UBERON's vertebrate abdomen term, and the related prior insect, lymph-node, and crustacean abdomen review reports cited above.
- The prior abdomen review reports that mention `Host-associated > Fish > Abdomen` use it only as an ambiguous GOLD sibling; they are not prior reviews of `data/habitats/host_associated/abdomen__ca3ba4b6.yaml`.
