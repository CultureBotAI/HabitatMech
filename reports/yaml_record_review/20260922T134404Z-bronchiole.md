# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/bronchiole.yaml`
- Started UTC: 2026-09-22T13:44:04Z
- Finished UTC: 2026-09-22T13:44:04Z
- Verdict: pass with minor issues

## Target

| Field | Value |
|---|---|
| Identifier | `BTO:0002375` |
| Label | `bronchiole` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `REVIEWED` |
| Maintained owner | `curation/decisions.tsv:260` grounds the GOLD source concept to `BTO:0002375` |
| Locked slug | `data/habitats/PATHS.tsv:339` maps `BTO:0002375` to `bronchiole` |

This is the reviewed exact record for the GOLD path `Host-associated > Mammals: Human > Respiratory system > Lung > Bronchioles`. The generated YAML correctly adopts `BTO:0002375` because the item-level decision grounds the minted GOLD source concept to `bronchiole` with `EXACT` status. The only issue found is limited to the human-readable provenance note: it says the GOLD leaf label equals the BTO label exactly, but the raw leaf is the plural `Bronchioles` and the BTO label is singular `bronchiole`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/bronchiole.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/bronchiole.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The generated BTO identity, GOLD source attestation, exact mapping predicate, host-associated category, and path lock are supported by maintained inputs:

- `data/raw/gold_ecosystem_paths.tsv:364` is the raw GOLD row for this source concept. Its canonical path is `Host-associated > Mammals: Human > Respiratory system > Lung > Bronchioles`; its leaf label is `Bronchioles`; it is a depth-5 path with one GOLD node id, `gold.ecosystem:6631`, 28 organism assertions, and no study or biosample assertions.
- `curation/decisions.tsv:260` is an item-level `GROUND` decision for `habitatmech:GOLD.1cfb2fdc59`, grounding that exact GOLD path to `BTO:0002375` `bronchiole` with `EXACT` status.
- `data/raw/ontology_terms.tsv:2375` provides `BTO:0002375` `bronchiole`, with definition `A minute thin-walled branch of a bronchus.`; `data/habitats/PATHS.tsv:339` pins that ontology record to `bronchiole`.
- The raw source path's parent is the GOLD path `Host-associated > Mammals: Human > Respiratory system > Lung`; that parent mints to `habitatmech:GOLD.5bbc13545b` and is preserved as the generated parent of this reviewed BTO record.
- `data/habitats/PATHS.tsv:1955` locks `habitatmech:GOLD.5bbc13545b` to `lung__88ff1576`; the generated parent record represents the parent GOLD path `Host-associated > Mammals: Human > Respiratory system > Lung`.

The exact identity is defensible: the GOLD plural leaf denotes the same anatomical class as BTO's singular `bronchiole`, the GOLD plural is retained as an `EXACT_SYNONYM`, the decision is item-scoped, and no other source concept resolves into `BTO:0002375` in the generated corpus.

## Evidence

Every generated claim in this record is source-derived or curated:

| Claim | Nearest source | Review |
|---|---|---|
| Identifier `BTO:0002375`, label `bronchiole`, definition, and `EXACT` status | `curation/decisions.tsv:260`, `data/raw/ontology_terms.tsv:2375` | Supported exactly |
| `mapping_status: REVIEWED` | `curation/decisions.tsv:260` has `review_depth` `ITEM` | Supported exactly |
| Synonym `Bronchioles` from `GOLD` | `data/raw/gold_ecosystem_paths.tsv:364` has leaf label `Bronchioles` | Supported exactly |
| GOLD source id `gold.ecosystem:6631`, source label, source path, and assertion count | `data/raw/gold_ecosystem_paths.tsv:364` | Supported exactly |
| `assertion_count: 28` with `assertion_unit: ORGANISM` | `data/raw/gold_ecosystem_paths.tsv:364` has 28 organism assertions and zero study or biosample assertions | Supported exactly |
| `skos:exactMatch` | `curation/decisions.tsv:260` records `grounding_status` `EXACT` | Supported exactly |
| Parent `habitatmech:GOLD.5bbc13545b` | GOLD parent-path emission in `src/habitatmech/seed.py`, backed by `data/raw/gold_ecosystem_paths.tsv:364` and the generated `lung__88ff1576` record | Supported exactly |
| Locked filename | `data/habitats/PATHS.tsv:339` | Supported exactly |

The record has no xrefs, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussion links, or datasets. Those absences are consistent with the single GOLD organism-assertion row that feeds this record and the absence of target-specific term-request, causal-graph, history, and research entries.

## Completeness

The generated slots are faithful to the maintained inputs that feed this record, aside from the singular/plural wording in the decision note:

- The sole `source_attestations` entry captures the exact child GOLD path and its single GOLD node id.
- The BTO definition and exact synonym from the GOLD source label are complete for the current ontology and source inputs.
- The item-level curation decision is sufficient to make this single-source record `REVIEWED`.
- The path-derived parent is a strictly broader source concept and correctly remains a parent rather than an equivalent xref.
- This record has no target-specific term request, causal graph overlay, research report, append-only history entry, or previous exact YAML review report.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated host-associated records, excluding generated `build`, `data/text_map`, and `pages` trees. They found the target raw GOLD row, item-level decision, BTO ontology row, path lock, parent path lock, generated YAML, and generated records that use the lung parent; they found no target-specific term request, causal overlay, history record, research report, or prior exact YAML review report.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Minor | The curated provenance note for `habitatmech:GOLD.1cfb2fdc59` says `Leaf label equals the BTO label exactly`, but the inspected source and ontology rows differ in number: GOLD uses plural `Bronchioles`, while BTO uses singular `bronchiole`. This does not change the exact identity or generated synonym, but it makes the item-level decision explanation slightly overstate the lexical match. | Reword the note in `curation/decisions.tsv:260`, then regenerate `BTO:0002375`. |

## Recommended Edits

- Reword the `curation/decisions.tsv:260` note so it says the plural GOLD leaf label denotes the singular BTO class instead of saying the labels are textually exact.
- Regenerate the target with `just seed` and preview `BTO:0002375` with `just seed-canary BTO:0002375` before applying the full generated corpus.

## Follow-up Checks

After correcting the maintained decision note, re-run the same focused validation set and confirm the generated `GROUND` history event carries the corrected singular/plural explanation:

- `just validate data/habitats/host_associated/bronchiole.yaml`
- `just validate-strict data/habitats/host_associated/bronchiole.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -name '*bronchiole*' -print` found no pre-existing exact review report for this record before this file was written.
- Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.1cfb2fdc59`, `habitatmech:GOLD.5bbc13545b`, `Host-associated > Mammals: Human > Respiratory system > Lung > Bronchioles`, `gold.ecosystem:6631`, and `BTO:0002375` found only the maintained source, decision, ontology, path-lock, generated, and generated-child-parent rows cited above for this target. Broader searches for `bronchiole` and `Bronchioles` also found unrelated ontology definitions that merely mention bronchioles.
