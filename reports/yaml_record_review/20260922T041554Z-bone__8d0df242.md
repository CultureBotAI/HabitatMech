# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/bone__8d0df242.yaml`
- Started UTC: 2026-09-22T04:15:54Z
- Finished UTC: 2026-09-22T04:15:54Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.28cfbd7dee` |
| Label | `Bone` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from the GOLD ecosystem-path inventory plus the vendored ontology slice; future item-level review belongs in `curation/decisions.tsv`, future authored definitions belong in `curation/term_requests.tsv`, and future mechanism evidence belongs in `curation/causal_graphs/*.yaml`, not in this generated YAML |
| Locked slug | `data/habitats/PATHS.tsv:1583` maps `habitatmech:GOLD.28cfbd7dee` to `bone__8d0df242` |

This is the minted GOLD record for `Host-associated > Mammals > Skeletal system > Bone`. The GOLD path scopes the generic `Bone` leaf to the mammal skeletal system and keeps it narrower than the vendored BTO `bone` term.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/bone__8d0df242.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/bone__8d0df242.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors |

## Identity and Grounding

The minted identity and its broader parents are supported by the GOLD inventory, the path lock, and the vendored ontology slice:

- `data/raw/gold_ecosystem_paths.tsv:2116` is the exact raw GOLD path `Host-associated > Mammals > Skeletal system > Bone`, gives leaf label `Bone`, records depth `4`, records two GOLD ecosystem nodes, aggregates zero organism, study, biosample, and total assertions, and names `gold.ecosystem:6373|gold.ecosystem:6374` as the upstream nodes.
- `data/habitats/PATHS.tsv:1583` pins the minted identifier `habitatmech:GOLD.28cfbd7dee` to slug `bone__8d0df242`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv:142` defines `BTO:0000140` with canonical label `bone`, a connective-tissue definition, `directly_referenced: FALSE`, and `label_only: FALSE`.
- `data/raw/gold_ecosystem_paths.tsv:2115` is the immediate GOLD parent path `Host-associated > Mammals > Skeletal system`, and `data/habitats/PATHS.tsv:1338` pins its generated identifier `habitatmech:GOLD.092eae5cd7` to `skeletal_system__e5059bff`.
- Exact ignored/hidden-inclusive searches found no maintained `curation/decisions.tsv` row for either `habitatmech:GOLD.28cfbd7dee` or its minted `Skeletal system` parent, so the target record correctly remains `mapping_status: SEEDED`.

`grounding_status: NARROW` is consistent with the path context: the source concept is mammal skeletal-system bone, which should not be merged into the generic BTO `bone` identity. Keeping the GOLD path as a minted record and attaching `BTO:0000140` as a broader term preserves the source distinction while retaining the anatomical/material parent. The other parent, `habitatmech:GOLD.092eae5cd7`, follows directly from the next broader GOLD path step.

## Evidence

Every generated scientific claim in this record is source-derived:

| Claim | Nearest source | Review |
|---|---|---|
| Minted identifier and locked filename | `data/habitats/PATHS.tsv:1583` | Supported exactly |
| GOLD `source_id: gold.ecosystem:6373`, `source_label: Bone`, `source_path`, omitted assertion count/unit, and the two-node note | `data/raw/gold_ecosystem_paths.tsv:2116` | Supported; `gold.ecosystem:6373` is the first of two node IDs that share the exact path |
| Broader BTO `bone` parent | `data/raw/ontology_terms.tsv:142` | Supported as a broader term for the mammal skeletal-system bone source concept, not as an exact identity |
| Broader GOLD `Skeletal system` parent | `data/raw/gold_ecosystem_paths.tsv:2115`, `data/habitats/PATHS.tsv:1338` | Supported exactly |

The record has no definition, synonyms, xrefs, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussion links, or datasets. Exact ignored/hidden-inclusive searches of the committed raw GOLD tables for `gold.ecosystem:6373`, `gold.ecosystem:6374`, and the full target source path found the cited `gold_ecosystem_paths.tsv` aggregate row and no exact `gold_path_biosamples.tsv`, `gold_path_triads.tsv`, or `gold_studies.tsv` side-table row.

## Completeness

No consequential slot is underfilled for the maintained inputs that feed this record:

- The sole `source_attestations` entry captures the complete target GOLD path, first upstream node id, and two-node provenance note. It correctly omits `assertion_count` and `assertion_unit` because the exact `gold_ecosystem_paths.tsv` aggregate has zero total assertions.
- Both `parent_habitats` values are expected: the BTO parent records the matched broader ontology term, and the minted `Skeletal system` parent records the next broader GOLD path.
- The record has no environmental parameter: exact committed GOLD triad rows exist only for child paths such as `Bone marrow`, not for the exact reviewed path.
- `mapping_status` stays `SEEDED` because no item-level curation decision exists for `habitatmech:GOLD.28cfbd7dee`.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `docs`, `.claude`, `src`, `tests`, `build`, `data/raw`, and `data/habitats/PATHS.tsv`, while excluding generated `data/text_map/**` and `pages/**`. They found the GOLD and BTO rows, pinned slugs, generated text-map projections, and generated child-path references, but no maintained target decision row, parent decision row, term request, history record, causal-graph overlay, target-specific research report, or prior exact YAML review report for `habitatmech:GOLD.28cfbd7dee`.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

Re-run the same focused validation set if a future curation change adds an item-level decision, authored definition, or causal graph overlay for this mammal skeletal-system bone record:

- `just validate data/habitats/host_associated/bone__8d0df242.yaml`
- `just validate-strict data/habitats/host_associated/bone__8d0df242.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

If future curation signs off the exact GOLD path, add the maintained item-level row to `curation/decisions.tsv`, run `just seed`, preview `just seed-canary habitatmech:GOLD.28cfbd7dee`, and rerun the corpus-level checks after regeneration.

## Additional Notes

- `find reports/yaml_record_review -name '*bone__8d0df242*' -print` found no pre-existing exact review report for this record before this file was written.
- `find curation history research reports -name '*bone__8d0df242*' -o -name '*28cfbd7dee*' -o -name '*6373*'` found no maintained target-specific decision, history, research, report, or causal overlay artifacts before this file was written.
- The generated text-map hits for `bone__8d0df242` and `skeletal_system__e5059bff` are search projections, not maintained inputs.
