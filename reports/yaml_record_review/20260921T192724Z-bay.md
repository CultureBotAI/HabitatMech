# YAML Record Review: Bay

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/bay.yaml`
- Started UTC: 2026-09-21T19:12:00Z
- Finished UTC: 2026-09-21T19:27:24Z
- Verdict: pass with minor issues

## Target

| Field | Value |
|---|---|
| Identifier | `ENVO:00000032` |
| Label | `bay` |
| Class | `HabitatRecord` |
| Category | `AQUATIC` |
| Generated or maintained | Generated from `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, `data/raw/prego_habitats.tsv`, `data/raw/prego_habitat_taxa.tsv`, and `data/habitats/PATHS.tsv`; do not hand-edit |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Source concepts | PREGO `ENVO:00000032` |
| Locked stem | `bay` in `data/habitats/PATHS.tsv` |

The record resolves the ENVO ontology class `ENVO:00000032` `bay`, which PREGO
also uses as its source habitat identifier. `data/habitats/PATHS.tsv` maps that
identifier to the stable corpus stem `bay`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/bay.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/bay.yaml --quiet` | Pass: 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Pass: 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Pass: committed term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: 3,206 expected records, 3,206 found, 0 missing, 0 extra, 0 differing. |
| Reference validator | Not available: `justfile` exposes no reference-specific validator, and this record has no `EvidenceItem`, `EnvironmentalParameter.reference`, `CharacteristicTaxon.reference`, or causal-edge evidence to target. |

## Identity and Grounding

| Claim | Evidence | Assessment |
|---|---|---|
| The record denotes ENVO's `bay` class. | `data/raw/ontology_terms.tsv` has `ENVO:00000032`, ontology `ENVO`, label `bay`, the ENVO definition `An area of water bordered by land on three sides.`, and `directly_referenced=TRUE`. The generated YAML uses the same identifier, label, definition, and `definition_source: ENVO`. | Supported exactly. |
| PREGO's source concept is the same ontology class. | `data/raw/prego_habitats.tsv` has `prego_id=ENVO:00000032`, ontology `ENVO`, `biolink_category=biolink:OntologyClass`, `taxon_count=132`, `direct_assertion_count=135`, `max_prego_score=4`, and `channels=annotated_genomes_isolates\|environmental_samples`. | Supported exactly. |
| The generated `source_attestations` row preserves the PREGO taxon count. | The single generated attestation carries `source: PREGO`, `source_id: ENVO:00000032`, `source_label: bay`, `assertion_count: 132`, `assertion_unit: TAXON`, `score: 4.0`, and `evidence_channels: annotated_genomes_isolates\|environmental_samples`, matching the PREGO habitat row's source identifier, distinct taxon count, maximum score, and channel set. | Supported exactly. |
| `EXACT` grounding is mechanically and semantically appropriate. | The HabitatMech identifier and the PREGO source identifier are both `ENVO:00000032`; this is not a lexical GOLD/BacDive guess or a broader-parent mapping. | Supported exactly. |
| The `AQUATIC` category and `ENVO:00000137` parent fit the ENVO hierarchy. | `data/raw/ontology_subclass_edges.tsv` places `ENVO:00000032` under `ENVO:00000137`; `data/raw/ontology_terms.tsv` labels that parent `coastal inlet` and defines it as an opening of the sea into the land. | Supported exactly. |

## Evidence

The record has no curator-authored `evidence`, `environmental_parameters`,
`causal_graphs`, `discussions`, or `datasets`.

| Claim | Evidence | Assessment |
|---|---|---|
| The 25 generated taxon rows are the first 25 PREGO-ranked taxa for this habitat. | `data/raw/prego_habitat_taxa.tsv` has `ENVO:00000032` rows with ranks 1 through 25, score `4`, and `channel=environmental_samples`; the generated YAML carries the same rank order, NCBITaxon IDs, labels, scores, and `candidate_pool: 132` on each of those 25 rows. | Supported exactly. |
| The taxa are observational associations, not curated characteristic exemplars. | `src/habitatmech/schema/habitatmech.yaml` documents seeded `characteristic_taxa` as observational, and none of the 25 PREGO rows in the YAML has `is_characteristic: true` or a literature `reference`. | Supported exactly. |
| The generated ENVO and PREGO synonyms are source-owned. | The ENVO exact synonyms in the YAML match the vendored `data/raw/ontology_terms.tsv` synonym column; the PREGO related synonyms match `data/raw/prego_habitats.tsv` `prego_synonyms` values, excluding the preferred `bay` label. | Supported exactly, with the synonym-scope caution called out below. |

## Completeness

The record is complete enough as a generated PREGO/ENVO seed. It preserves the
ontology identity, ENVO definition, ENVO exact synonyms, the PREGO attestation,
the first 25 ranked PREGO taxon associations out of the 132-taxon PREGO
candidate pool, the ENVO parent, and the generated seed curation event.

Empty optional fields are correct for the current maintained inputs:

- no `curation/causal_graphs/bay.yaml` overlay exists;
- no `curation/decisions.tsv`, `curation/term_requests.tsv`,
  `curation/term_requests_excluded.tsv`, or `curation/external_xrefs.tsv` row is
  keyed by `ENVO:00000032`;
- no `history/` curation-session record names `ENVO:00000032` or the `bay`
  slug; and
- no target-specific report exists under `research/habitats`.

Hidden- and ignored-file-inclusive searches covered `curation`, `history`,
`research/habitats`, `reports/yaml_record_review`, `data/raw`,
`data/habitats/PATHS.tsv`, and `data/habitats` for `ENVO:00000032`; `find`
lookups covered the `bay` slug under `curation`, `history`, `research/habitats`,
and `reports/yaml_record_review`. These checks found the expected raw ontology,
raw PREGO, raw subclass, generated `PATHS.tsv`, and generated YAML entries, the
existing GOLD `gulf` term request and curation history that use `ENVO:00000032`
as their new genus, and no Bay-owned curation decision, term request, causal
overlay, history record, research report, or previous `*-bay.md` YAML review.

The vendored ontology slice has no direct local child of `ENVO:00000032`; the
one maintained GOLD child, `gulf`, is curated in
`curation/term_requests.tsv` as a large, deeply indented marine bay under
`ENVO:00000032`. That existing term request is compatible with the generated Bay
seed and does not require any change to `data/habitats/aquatic/bay.yaml`.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| `HM-BAY-001` | Minor | Some generated synonyms for `bay` are source-owned strings whose exactness is not locally re-reviewed. ENVO contributes `inlet`, which is also broader ontology class `ENVO:00000475`, and PREGO contributes mechanical or broad related strings such as `bahial`, `geo`, and `gulf`. The generated record is faithful to the two raw rows, but consumers should not treat those exact/related scopes as a HabitatMech-curated synonym audit. | `data/raw/ontology_terms.tsv` lists the ENVO exact synonym payload for `ENVO:00000032`; `data/raw/prego_habitats.tsv` lists the PREGO `prego_synonyms`; `data/habitats/aquatic/bay.yaml` copies them into `synonyms` with `source: ENVO` or `source: PREGO`. | Upstream ENVO/PREGO extraction into `data/raw/ontology_terms.tsv` and `data/raw/prego_habitats.tsv`; no per-record HabitatMech curation input currently owns synonym suppression. |

No blocker or major findings found.

## Recommended Edits

1. Leave `data/habitats/aquatic/bay.yaml` generated and do not add a
   `curation/decisions.tsv` row merely to restate that PREGO `ENVO:00000032`
   is exactly ENVO `bay`.
2. If broad or mechanically inflected synonyms create false-positive lookup
   behavior, add a maintained synonym-suppression mechanism for source-owned
   ontology/PREGO synonyms or fix the upstream vocabulary rows, then refresh the
   raw inventories and regenerate with `just seed-canary ENVO:00000032 --force`.

## Follow-up Checks

- `just seed`
- `just seed-canary ENVO:00000032 --force`
- `just validate data/habitats/aquatic/bay.yaml`
- `just validate-strict data/habitats/aquatic/bay.yaml --quiet`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

After any synonym-filtering change, re-read `data/habitats/aquatic/bay.yaml` and
confirm the definition, PREGO attestation, parent `ENVO:00000137`, and 25 PREGO
taxa are unchanged unless the raw PREGO or ENVO source rows changed.

## Additional Notes

This was a read-only YAML review. It did not edit generated habitat YAML, append
curation history, promote the review status, create an issue, or run paid
definition research.
