# YAML Record Review: air

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/air/air.yaml
- Started UTC: 2026-09-21T04:35:20Z
- Finished UTC: 2026-09-21T04:38:48Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/air/air.yaml` |
| Class | `HabitatRecord` |
| Identifier | `ENVO:00002005` |
| Label | `air` |
| Category | `AIR` |
| Grounding status | `EXACT` |
| Mapping status | `REVIEWED` |
| Maintained status | generated from `data/raw/`, `curation/decisions.tsv`, and `data/habitats/PATHS.tsv` |

The generated record harmonizes one reviewed GOLD source concept with one
reviewed PREGO ontology-class source:

- ENVO supplies the identifier, canonical label, definition, and
  `ENVO:01000797` parent.
- GOLD supplies the `Environmental > Air` source attestation and a source-path
  parent through its reviewed `Environmental` ancestor.
- PREGO supplies the `ENVO:00002005` ontology-class attestation and 25
  associated taxon rows.
- No curator-owned evidence, environmental parameters, causal graphs,
  discussions, datasets, xrefs, or authored synonyms are present.

## Validation

| Check | Result |
|---|---|
| `.venv/bin/python -c "from linkml.validator.cli import cli; cli()" --schema src/habitatmech/schema/habitatmech.yaml --target-class HabitatRecord data/habitats/air/air.yaml` | Passed; no LinkML issues. |
| `.venv/bin/python scripts/validate_strict.py data/habitats/air/air.yaml --quiet` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| `.venv/bin/python scripts/build_term_requests.py --check` | Passed; term-request table is current at 109 terms. |
| `.venv/bin/python scripts/verify_corpus.py --max-diffs 1` | Passed; 3,206 expected records, 3,206 found, no missing/extra/differing records. |
| `.venv/bin/python scripts/validate_history.py` | Passed; 77 history records valid against `src/habitatmech/schema/history.yaml`. |
| Reference validator | Not checked: this record has no DOI/PMID/URL `EvidenceItem` or causal-edge evidence, and no dedicated `research/habitats/air/` report for the `ENVO:00002005` `air` target requiring `scripts/check_report_citations.py` exists. |

## Identity and Grounding

| Claim | Evidence | Assessment |
|---|---|---|
| The record denotes `ENVO:00002005` `air`. | `data/raw/ontology_terms.tsv` contains `term_id=ENVO:00002005`, ontology `ENVO`, label `air`, the same ENVO definition used in the YAML, `directly_referenced=TRUE`, no synonyms, and `label_only=FALSE`. | Supported exactly. |
| The stable file stem is `air`. | `data/habitats/PATHS.tsv` maps `ENVO:00002005` to `air`. | Supported exactly. |
| The record belongs in `habitat_category: AIR`. | `src/habitatmech/seed.py` lists `ENVO:00002005` as an `AIR` category anchor. | Supported exactly. |
| `ENVO:01000797` is the broader ontology parent. | `data/raw/ontology_subclass_edges.tsv` contains `ENVO:00002005 rdfs:subClassOf ENVO:01000797`; `data/raw/ontology_terms.tsv` labels `ENVO:01000797` as `gaseous environmental material`. | Supported exactly. |
| `habitatmech:BACDIVE.0f1e92a02c` is a broader source-path parent. | The GOLD source path is `Environmental > Air`. `curation/decisions.tsv` merges GOLD's root `Environmental` concept into `habitatmech:BACDIVE.0f1e92a02c`; `curation/term_requests.tsv` defines that parent as `free-living environment`, an environmental system that can be a portion of air, water, or land surface/subsurface and is not host-bounded or an operated artifact. | Supported exactly; `free-living environment` is broader than `air`. |
| The GOLD attestation is the reviewed source concept `Environmental > Air`. | `data/raw/gold_ecosystem_paths.tsv` has canonical path `Environmental > Air`, leaf `Air`, depth `2`, `gold_node_count=4`, `organism_count=397`, `total_assertions=397`, and node IDs `gold.ecosystem:2879\|gold.ecosystem:3479\|gold.ecosystem:3748\|gold.ecosystem:3963`. `curation/decisions.tsv` has an `ITEM` `REVIEW` row for `habitatmech:GOLD.7343a1c7d4`. | Supported exactly; the YAML carries the first GOLD node ID and preserves the four-ID caveat in `notes`. |
| The PREGO attestation is the reviewed `ENVO:00002005` ontology class. | `data/raw/prego_habitats.tsv` has `prego_id=ENVO:00002005`, `ontology=ENVO`, `biolink_category=biolink:OntologyClass`, `taxon_count=2424`, `direct_assertion_count=2436`, `max_prego_score=4`, `channels=annotated_genomes_isolates\|environmental_samples`, and synonym `air`. `curation/decisions.tsv` has an `ITEM` `REVIEW` row for `habitatmech:PREGO.994af75c57`. | Supported exactly; the PREGO ID is the same ENVO CURIE as the record ID. |
| The generated `mapping_status: REVIEWED` is justified. | `curation/decisions.tsv` has `ITEM`-depth `REVIEW` rows for both source concepts feeding this record, `habitatmech:GOLD.7343a1c7d4` and `habitatmech:PREGO.994af75c57`. | Supported exactly; the record is not being promoted from lexical evidence alone. |

## Evidence

| Claim | Evidence | Assessment |
|---|---|---|
| The 25 PREGO taxa are the top ranked taxa for `ENVO:00002005`. | `data/raw/prego_habitat_taxa.tsv` has exactly the 25 `ENVO:00002005` rows rendered in the YAML, with ranks 1-25, the same NCBITaxon CURIEs, the same labels, PREGO scores `4` for ranks 1-23 and `3` for ranks 24-25, `direct_flag=TRUE`, and the same empty `corroborated_by` cells. | Supported exactly. |
| Rank 16 has no `taxon_label`. | The rank-16 raw PREGO row is `NCBITaxon:2735898` with an empty `taxon_label` cell. | Supported exactly; the generator did not drop a known name. |
| These taxa are observational reported-from associations, not curator claims that the listed taxa typify air. | The generated YAML omits `is_characteristic` and `reference` for every taxon. `src/habitatmech/schema/habitatmech.yaml`, `README.md`, and `docs/HARMONIZATION.md` all distinguish seeded PREGO associations from the stronger curator-set `is_characteristic` flag. | Supported; no overclaim found. |

No claim-level literature evidence was present or required. The record has no
causal graph, so there are no causal edges or snippets to audit.

## Completeness

- The record is complete enough for a reviewed ENVO/GOLD/PREGO harmonization
  record.
- Empty `evidence`, `environmental_parameters`, `causal_graphs`, `discussions`,
  `datasets`, `xrefs`, and `synonyms` are acceptable here. No maintained input
  supplies those fields for this record, and the ENVO/PREGO rows supply no
  non-canonical synonym beyond `air` itself.
- Ignored/hidden-inclusive exact searches covered `data`, `curation`,
  `research`, `docs`, `src`, and `.claude`, excluding only generated text-map,
  cache, and build trees, for `ENVO:00002005`,
  `gold.ecosystem:2879`, `habitatmech:GOLD.7343a1c7d4`,
  `habitatmech:PREGO.994af75c57`, and
  `habitatmech:BACDIVE.0f1e92a02c`. They found the target, expected raw rows,
  curation rows, adjacent indoor/outdoor-air term requests and reports, and the
  generated parent record; they found no causal overlay or direct research
  report for `ENVO:00002005` `air`.
- A bounded `find reports/yaml_record_review -maxdepth 1` search and an exact
  ignored/hidden-inclusive `rg` search under `reports/yaml_record_review/`
  found no prior review for `data/habitats/air/air.yaml` or `ENVO:00002005`.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

None required. If the source inventories change later, the narrowest proof is:

- run `.venv/bin/python scripts/seed_from_sources.py`;
- run a single-record seed canary for `ENVO:00002005`;
- rerun strict validation on `data/habitats/air/air.yaml`;
- rerun `scripts/verify_corpus.py`.

## Additional Notes

- `data/raw/isolation_source_groundings.tsv` contains a lexical BacDive
  grounding candidate from `Air` to `ENVO:00002005`, but ignored/hidden-inclusive
  searches over the committed BacDive source inventories found no
  `bacdive.isolation_source:air` source row. The nearest BacDive rows are the
  distinct `bacdive.isolation_source:airways` and
  `bacdive.isolation_source:air-conditioner`, which already generate
  `data/habitats/host_associated/respiratory_airway.yaml` and
  `data/habitats/engineered/air_conditioning_unit.yaml`.
- The related `research/habitats/air/indoor-air-...` and
  `research/habitats/air/outdoor-air-...` reports correctly use
  `ENVO:00002005` as the broader genus for narrower minted air terms; they do
  not supply maintained fields to the broader `air` record.
