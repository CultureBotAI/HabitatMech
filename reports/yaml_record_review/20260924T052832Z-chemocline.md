# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/aquatic/chemocline.yaml`
- Started UTC: 2026-09-24T05:21:00Z
- Finished UTC: 2026-09-24T05:28:32Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/chemocline.yaml` |
| Class | `HabitatRecord` |
| Identifier | `ENVO:00002270` |
| Label | `chemocline` |
| Category | `AQUATIC` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| GOLD source-concept key | `habitatmech:GOLD.a1ead6c3f8` |
| Maintained owners | Generated from `data/raw/gold_ecosystem_paths.tsv`, `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, `data/habitats/PATHS.tsv`, and `src/habitatmech/seed.py`; `data/habitats/` is generated and remains read-only |

This generated GOLD-only record represents the GOLD source concept
`gold.ecosystem:8542`:

```text
Environmental > Aquatic > Freshwater > Meromictic lake > Chemocline
```

The record is grounded directly to the vendored ENVO class `ENVO:00002270`
`chemocline`, defined in `data/raw/ontology_terms.tsv` as:

```text
A cline across which the chemical composition of a fluid changes rapidly.
```

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/chemocline.yaml` | Passed: LinkML reported no issues |
| `just validate-strict data/habitats/aquatic/chemocline.yaml` | Passed: 1 file scanned, 0 files with errors, 0 total error rows |
| `just validate-causal <overlay>` | Not applicable: the record has no `causal_graphs` slot, and ignored/hidden-inclusive searches of `curation/causal_graphs/` found no overlay for `ENVO:00002270`, `habitatmech:GOLD.a1ead6c3f8`, `gold.ecosystem:8542`, `Chemocline`, or `chemocline` |
| `just validate-causal-all` | Passed: 32 causal-graph curation files with 32 graphs |
| `just term-requests-check` | Passed: term-request table is current with 109 terms |
| `just validate-history` | Passed: no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Passed: expected 3,206 records, found 3,206, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | Passed: 0 ungrounded records still undecided, 1,810 decisions on file |
| `just report` | Passed: corpus report completed for 3,206 records |
| Reference validator | Not applicable: this record has no DOI, PMID, URL, `EvidenceItem`, or causal-edge evidence that would need citation checking |
| `git diff --check` | Passed before this report was written |

## Identity and Grounding

| Claim | Maintained evidence | Assessment |
|---|---|---|
| The output identifier is `ENVO:00002270`. | `data/habitats/PATHS.tsv` pins `ENVO:00002270` to the `chemocline` stem. | Supported. |
| The output label and definition are the vendored ENVO label and definition for `ENVO:00002270`. | `data/raw/ontology_terms.tsv` lists `ENVO:00002270` with label `chemocline` and the same definition used by the generated record. | Supported. |
| The source concept is GOLD `gold.ecosystem:8542`. | `data/raw/gold_ecosystem_paths.tsv` has one row for `Environmental > Aquatic > Freshwater > Meromictic lake > Chemocline`, `gold_node_count` 1, `gold_node_ids` `gold.ecosystem:8542`, and zero organism, study, biosample, or total assertions. | Supported. |
| The record belongs in `AQUATIC`. | `src/habitatmech/seed.py` derives `AQUATIC` from GOLD's `Environmental > Aquatic` prefix for this row. | Supported. |
| `ENVO:01000258` `fluid cline` is a broader habitat for `chemocline`. | `data/raw/ontology_subclass_edges.tsv` asserts `ENVO:00002270 rdfs:subClassOf ENVO:01000258`. | Supported. |
| `ENVO:00000199` `meromictic lake` is a broader habitat for `chemocline`. | The GOLD second pass in `src/habitatmech/seed.py` links each GOLD child path to the resolved concept for its parent path. Here, `Environmental > Aquatic > Freshwater > Meromictic lake` resolves to `ENVO:00000199`. | Not supported: this is only a source-path containment/context edge. A chemocline is a cline within a stratified fluid mass, not a kind of meromictic lake. |
| `mapping_status` is `SEEDED`. | Ignored/hidden-inclusive exact searches found no item-level `curation/decisions.tsv` decision for the source-concept key `habitatmech:GOLD.a1ead6c3f8`, `ENVO:00002270`, `gold.ecosystem:8542`, `Chemocline`, or `chemocline`. | Supported. |

The exact `ENVO:00002270` grounding is sound: GOLD's `Chemocline` leaf under
`Meromictic lake` denotes the rapid chemical-gradient layer, not the lake as a
whole. The `ENVO:01000258` parent follows from ENVO's own subclass edge. The
extra `ENVO:00000199` parent is the only identity or grounding defect found.

## Evidence

The source attestation is an accurate transcription of the committed GOLD row:

- `source: GOLD`
- `source_id: gold.ecosystem:8542`
- `source_label: Chemocline`
- `source_path: Environmental > Aquatic > Freshwater > Meromictic lake > Chemocline`
- `mapping_predicate: skos:exactMatch`

The attestation correctly omits `assertion_count` and `assertion_unit`: the
maintained GOLD row has `organism_count`, `study_count`, `biosample_count`, and
`total_assertions` all set to `0`. Ignored/hidden-inclusive exact searches of
`data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, and
`data/raw/gold_ecosystem_paths.tsv` found `gold.ecosystem:8542`, `8542`, and
`Chemocline` only in `data/raw/gold_ecosystem_paths.tsv`, so there is no
committed GOLD sample or MIxS environmental-triad evidence to review for this
source concept.

## Completeness

- The generated target contains every field supported by the maintained GOLD
  and ENVO inputs: identity, ENVO definition, aquatic category, grounding
  status, two generated parents, one GOLD source attestation, and one generated
  curation event.
- Empty `synonyms`, `environmental_parameters`, `characteristic_taxa`,
  `evidence`, `causal_graphs`, `discussion`, and `datasets` are appropriate for
  the current maintained inputs. No maintained row supplies a synonym, sample
  triad, taxon association, claim-level evidence item, causal overlay,
  discussion item, or dataset reference for this zero-assertion GOLD node.
- Ignored/hidden-inclusive exact searches covered `curation/decisions.tsv`,
  `curation/term_requests.tsv`, `curation/external_xrefs.tsv`, and
  `curation/causal_graphs/` for `ENVO:00002270`,
  `habitatmech:GOLD.a1ead6c3f8`, `gold.ecosystem:8542`, `Chemocline`, and
  `chemocline`; no maintained curation row or overlay was found.
- Ignored/hidden-inclusive exact searches covered
  `reports/yaml_record_review/` before report creation; no exact
  `*-chemocline.md` report was found.

## Findings

### Major

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| `HM-CHEMOCLINE-001` | `parent_habitats` asserts `ENVO:00000199` `meromictic lake` as an is-a parent, but a chemocline is a cline within a chemically stratified fluid mass rather than a kind of lake. | `data/raw/gold_ecosystem_paths.tsv` places the source under `Environmental > Aquatic > Freshwater > Meromictic lake > Chemocline`; the GOLD parent-path second pass in `src/habitatmech/seed.py` turns that path containment into `parent_habitats`; `ENVO:00000199` is defined as a lake with layers of water which do not intermix; `ENVO:00002270` is defined as a cline across which fluid chemistry changes rapidly and is only subclassed to `ENVO:01000258` `fluid cline` in the vendored ENVO slice. | `src/habitatmech/seed.py` needs a maintained way to suppress or demote this one GOLD parent-path edge without hand-editing `data/habitats/aquatic/chemocline.yaml`. |

No blocker or minor findings found.

## Recommended Edits

1. Add a maintained curation route that can remove or demote a generated GOLD
   parent-path edge when the path parent is contextual or locative rather than
   strictly broader. Then apply it to
   `Environmental > Aquatic > Freshwater > Meromictic lake > Chemocline` so
   `ENVO:00000199` no longer appears in `parent_habitats` for
   `ENVO:00002270`.

   Maintain the fix in `src/habitatmech/seed.py` plus the relevant curation
   input, then regenerate the corpus. Do not edit
   `data/habitats/aquatic/chemocline.yaml` directly.

## Follow-up Checks

| Scope | Command or check |
|---|---|
| Target schema | `just validate data/habitats/aquatic/chemocline.yaml` |
| Closed-schema validation | `just validate-strict data/habitats/aquatic/chemocline.yaml` |
| Corpus reproduction | `just verify-corpus --max-diffs 1` |
| Worklist/report sanity | `just worklist --limit 2000` and `just report` |
| Causal overlays | `just validate-causal-all` |
| Term-request exports | `just term-requests-check` |
| History | `just validate-history` |
| Manual hierarchy check | Confirm `data/habitats/aquatic/chemocline.yaml` keeps `ENVO:01000258` and drops `ENVO:00000199` from `parent_habitats` after regeneration |
| Diff hygiene | `git diff --check` |

## Additional Notes

The false `ENVO:00000199` parent was not caused by a stale or missing decision
row. Exact ignored/hidden-inclusive searches of the maintained curation tables
found no row for this source concept, so the record is still following the
default GOLD plus ENVO generation path.
