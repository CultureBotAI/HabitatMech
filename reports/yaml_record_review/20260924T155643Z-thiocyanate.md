# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/engineered/thiocyanate.yaml`
- Started UTC: 2026-09-24T15:53:30Z
- Finished UTC: 2026-09-24T15:56:43Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.a006851e0a` |
| Label | `Thiocyanate` |
| Category | `ENGINEERED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Generated path | `data/habitats/engineered/thiocyanate.yaml` |
| Source concept | GOLD `Engineered > Bioremediation > Thiocyanate` |

The target is the depth-3 GOLD thiocyanate branch under engineered bioremediation, not the sibling `Engineered > Bioreactor > Thiocyanate-remediating` branch or the `Engineered > Bioreactor > Wastewater > Cyanide/thiocyanate` wastewater branch.

The record is generated from committed inputs. `data/habitats/PATHS.tsv:2484` maps `habitatmech:GOLD.a006851e0a` to the stable `thiocyanate` slug, and future corrections belong in `curation/decisions.tsv`, `curation/term_requests.tsv`, or the GOLD inventory extractor rather than in this generated YAML.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/thiocyanate.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/thiocyanate.yaml` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| Target causal overlay validator | Not applicable; an ignored-inclusive exact search over `curation/causal_graphs` for the target identifier, exact GOLD path, source node IDs, and `Thiocyanate` label found no target overlay. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files and 32 graphs validated. |
| Reference validator | Not applicable; this record has no record-level `evidence`, `causal_graphs`, or dedicated HabitatMech per-record reference check. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 records were expected, 3206 were found, and `data/habitats/` reproduced exactly from `data/raw/`. |
| `just worklist --limit 2000` | Passed; 0 ungrounded records remain undecided and 1810 decisions are on file. |
| `just report` | Passed; the corpus report regenerated successfully. |
| `git diff --check` | Passed. |

## Identity and Grounding

`data/raw/gold_ecosystem_paths.tsv:1237` is the maintained GOLD row for the reviewed concept. It has:

- canonical path `Engineered > Bioremediation > Thiocyanate`;
- source leaf `Thiocyanate`;
- GOLD depth `3`;
- three collapsed GOLD node IDs, `gold.ecosystem:4434|gold.ecosystem:4435|gold.ecosystem:4436`;
- zero direct organism, project, biosample, or study assertions in the ecosystem-path inventory.

The generated `source_attestations` entry mirrors this source row: it cites GOLD `gold.ecosystem:4434`, uses source label `Thiocyanate`, uses the exact source path above, and notes that three GOLD ecosystem node IDs share that path.

The record inherits one source-path parent, `habitatmech:GOLD.5f0e9e816a`. That parent is generated from `Engineered > Bioremediation`, whose exact GOLD row is `data/raw/gold_ecosystem_paths.tsv:600`; the row has four collapsed GOLD node IDs, six direct GOLD organism assertions, and the source path that is strictly the path prefix of the reviewed target.

The `UNGROUNDED` target status traces to `curation/decisions.tsv:928`, which records `CONFIRM_UNGROUNDED` for `habitatmech:GOLD.a006851e0a`. That row is explicitly only `review_depth=CLASS`, and its note says the class-level sweep did not assess whether the concept is a habitat.

## Evidence

The generated record has no record-level `evidence`, no `environmental_parameters`, no `characteristic_taxa`, and no `causal_graphs`, so the source attestation and curation history carry the only record-local evidence-like claims to review.

Supported:

- The GOLD source identity is reproducible from `data/raw/gold_ecosystem_paths.tsv:1237`.
- The generated parent is the immediate GOLD path prefix and is reproducible from `data/raw/gold_ecosystem_paths.tsv:600`.
- The `SEEDED` mapping status is consistent with the absence of an item-level decision; `docs/CURATION.md` states that a multi-source or minted record becomes `REVIEWED` only when its source concepts have item-level decisions.
- The absence of an ontology identity in this record is a faithful projection of the current `CONFIRM_UNGROUNDED` row.

Unsupported or over-scoped:

- No item-level evidence in `curation/decisions.tsv` shows whether `Engineered > Bioremediation > Thiocyanate` denotes a thiocyanate-contaminated environmental material, a thiocyanate compound class, a treatment process, or another non-habitat source artifact.
- No item-level evidence currently shows that the generated `Bioremediation` parent is itself a strictly broader microbial habitat rather than a remediation process or organizational branch.

Auxiliary GOLD support:

- An ignored-inclusive exact raw-GOLD search for the exact `Engineered > Bioremediation > Thiocyanate` path found one direct biosample row, `data/raw/gold_path_biosamples.tsv:488`, with 23 biosamples on GOLD node `4436`.
- The exact raw-GOLD search found no direct GOLD triad row for this path.
- `data/raw/gold_studies.tsv:1226` links one GOLD study, `Gs0118793`, to this exact path and to the related `Engineered > Bioreactor > Thiocyanate-remediating > Anaerobic-Aerobic` and `Engineered > Bioreactor > Wastewater > Cyanide/thiocyanate` paths.

## Completeness

Consequential gaps:

- The target has not been item-reviewed even though direct GOLD biosample support exists on its exact path.
- The only generated parent, `habitatmech:GOLD.5f0e9e816a`, has also not been item-reviewed, so the reviewed record currently sits under an unconfirmed broader source concept.
- No causal overlay exists for a thiocyanate bioremediation habitat; that is acceptable for a seeded record, but it means the corpus makes no mechanism claim to review.

Bounded searches:

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-thiocyanate.md' -print` found no earlier YAML review report for the `thiocyanate` stem. `find` is not gitignore-filtered.
- An ignored-inclusive exact search over `curation`, `history`, `research`, `data/habitats/PATHS.tsv`, `data/habitats/engineered`, `data/raw`, and `conf/id_label_targets.yaml` for `habitatmech:GOLD.a006851e0a`, `gold.ecosystem:4434`, `gold.ecosystem:4435`, `gold.ecosystem:4436`, and `Engineered > Bioremediation > Thiocyanate` found the path lock, the generated target and child records, the class-level decision row, the raw GOLD path row, the direct 23-biosample row, the shared study row, and adjacent research/report mentions; it found no target-specific term request, term-request exclusion, history record, deep-research report, id-label residual, or causal overlay.
- An ignored-inclusive exact search over `data/raw/ontology_terms.tsv`, `curation/term_requests.tsv`, `curation/term_requests_excluded.tsv`, `curation/decisions.tsv`, and `curation/samples` for `thiocyanate`, `cyanide`, `cyanate`, `SCN`, and `Bioremediation` found related bioremediation material term requests and sibling cyanide/thiocyanate sample evidence, but found no existing candidate habitat term or exact target term request/exclusion/sample for this source concept.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The target has only a class-level `CONFIRM_UNGROUNDED` row, so `UNGROUNDED` has not been item-reviewed against the full `Engineered > Bioremediation > Thiocyanate` source path. | `curation/decisions.tsv:928` records `review_depth=CLASS` and says habitathood was not assessed. The exact path has 23 direct biosamples in `data/raw/gold_path_biosamples.tsv:488` and also appears in the multi-path GOLD study row at `data/raw/gold_studies.tsv:1226`, so the concept is not merely an unattested empty branch in all raw GOLD tables. | `curation/decisions.tsv`; if item review confirms a real novel habitat, `curation/term_requests.tsv`. |
| Major | The target's only parent, `habitatmech:GOLD.5f0e9e816a`, also has only a class-level `CONFIRM_UNGROUNDED` row, so the `Thiocyanate is-a Bioremediation` edge rests on an unreviewed source-path prefix that may be a process or remediation program rather than a strictly broader habitat. | `data/raw/gold_ecosystem_paths.tsv:600` defines the inherited parent as `Engineered > Bioremediation`; `curation/decisions.tsv:583` records the same `review_depth=CLASS` "habitathood was NOT assessed" decision for that parent. | `curation/decisions.tsv`; if item review confirms a real novel habitat, `curation/term_requests.tsv`. |

## Recommended Edits

1. Item-review `habitatmech:GOLD.a006851e0a` in `curation/decisions.tsv` against the exact `Engineered > Bioremediation > Thiocyanate` path and the GOLD `4436` biosamples. Decide whether the concept is a thiocyanate bioremediation habitat with no ontology term, a chemical contaminant class needing a retained xref, or a non-habitat GOLD branch.
2. If the target is a real novel habitat, add a definition in `curation/term_requests.tsv`, probably under `ENVO:00010483` `environmental material` unless item review finds a tighter vendored parent. The adjacent methane, xylene, and persistent-organic-pollutant decisions use this material pattern for pollutant-named bioremediation paths.
3. Item-review the broader `habitatmech:GOLD.5f0e9e816a` `Engineered > Bioremediation` source concept in `curation/decisions.tsv` before relying on it as an is-a parent. If it is a process or administrative branch rather than a habitat, replace inherited bioremediation parentage in downstream term requests with supported material or device parents.
4. Review the related GOLD thiocyanate paths together: `Engineered > Bioreactor > Thiocyanate-remediating`, `Engineered > Bioreactor > Thiocyanate-remediating > Anaerobic-Aerobic`, and `Engineered > Bioreactor > Wastewater > Cyanide/thiocyanate`. The shared GOLD study `Gs0118793` links them but does not prove they are equivalent.

## Follow-up Checks

- After adding any decision or term-request row, run `just seed`, `just seed-canary habitatmech:GOLD.a006851e0a`, inspect the regenerated `data/habitats/engineered/thiocyanate.yaml`, then run `just seed-apply --force`.
- Validate the regenerated target with `just validate data/habitats/engineered/thiocyanate.yaml` and `just validate-strict data/habitats/engineered/thiocyanate.yaml`.
- Run `just term-requests-check`, `just validate-history`, and `just verify-corpus --max-diffs 1` to prove curation inputs, history, and generated corpus stay aligned.
- If a causal overlay is added, run `just validate-causal curation/causal_graphs/<slug>.yaml` and `just validate-causal-all`.
- Run `just report`, `just worklist --limit 2000`, and `git diff --check` before opening a PR.

## Additional Notes

- The sibling `Biofilm` child under this path is generated as `data/habitats/engineered/biofilm__59810f2b.yaml`. Its `parent_habitats` include both this target and `ENVO:00002034` by `skos:narrowMatch`, so a later item review of `Thiocyanate` should include a quick child-parent sanity check.
- The adjacent deep-research report for persistent-organic-pollutant bioremediation lists the thiocyanate path as a sibling lead only; it is not source evidence for this target's identity.
