# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/other/in_situ.yaml`
- Started UTC: `2026-09-26T16:37:46Z`
- Finished UTC: `2026-09-26T16:38:27Z`
- Verdict: needs curation

## Target

Reviewed the complete generated record at
`data/habitats/other/in_situ.yaml`.

| Field | Value |
|---|---|
| Identifier | `habitatmech:BACDIVE.3edb563d49` |
| Label | `In-situ` |
| Category | `OTHER` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Generated or maintained | Generated from `data/raw/` plus `curation/decisions.tsv`; do not hand-edit |
| Stable slug | `data/habitats/PATHS.tsv` maps `habitatmech:BACDIVE.3edb563d49` to `in_situ` |
| Source concept | `bacdive.isolation_source:in-situ` |

This target is BacDive's flat `In-situ` isolation-source bin, not a specific
natural habitat with retained path context. `data/raw/bacdive_isolation_sources.tsv`
has one `bacdive.isolation_source:in-situ` row with three strain assertions, and
`data/raw/isolation_source_groundings.tsv` has a deliberate empty mapping row
for BacDive label `In-situ`.

Exact ignored/hidden-inclusive searches for
`habitatmech:BACDIVE.3edb563d49`, `3edb563d49`, `bacdive.isolation_source:in-situ`,
`In-situ`, and `in_situ` across `data/habitats`, `data/raw`, `curation`,
`history`, `research`, and `reports/yaml_record_review` found the generated
record, `PATHS.tsv` slug row, class-level decision, BacDive source rows, and
unrelated textual uses of "in-situ"; they found no target-specific
`curation/term_requests.tsv` row, causal overlay, history record, deep-research
report, or prior exact YAML review report.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/other/in_situ.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/other/in_situ.yaml` | Pass: scanned 1 file with 0 files containing `ERROR` and 0 total `ERROR` rows. |
| `just validate-causal curation/causal_graphs/in_situ.yaml` | Not applicable: exact ignored/hidden-inclusive searches found no target-specific causal-graph overlay. |
| `just validate-causal-all` | Pass: validated 32 causal-graph curation files with 32 graphs. |
| Term-request validator | Pass: `just term-requests-check` reported `term-request table is current (109 terms)`. |
| Reference validator | Not applicable: this generated BacDive-only record has no citation-bearing `evidence`, `causal_graphs`, `discussions`, `datasets`, or `characteristic_taxa.reference` entries. |
| History validator | Pass: `just validate-history` reported `77 history record(s) valid`. |
| Corpus reproduction | Pass: `just verify-corpus --max-diffs 1` reported 3,206 expected records, 3,206 found records, and 0 missing, extra, or differing records. |
| Worklist | Pass: `just worklist --limit 0 --status all --out /tmp/habitatmech-worklist.tsv` wrote 953 ungrounded rows and showed this record in the three-assertion backlog. |
| Corpus report | Pass: `just report` completed for the 3,206-record corpus and still placed 830 ungrounded records in the class-level sweep. |
| Whitespace | Pass: `git diff --check` reported no whitespace errors. |

## Identity and Grounding

The generated identity is internally traceable:

| Claim | Maintained source | Assessment |
|---|---|---|
| BacDive source id | `data/raw/bacdive_isolation_sources.tsv` has `bacdive.isolation_source:in-situ` for label `In-situ`, normalized value `in-situ`, three strains, and three total assertions. | Supported. |
| BacDive upstream grounding | `data/raw/isolation_source_groundings.tsv` has an `In-situ` row with normalized label `in situ` and no ontology target. | Supported. The generated attestation note correctly preserves kg-microbe's blank mapping rather than replacing it with a weak local lexical match. |
| HabitatMech identifier and slug | `data/habitats/PATHS.tsv` pins `habitatmech:BACDIVE.3edb563d49` to slug `in_situ`. | Supported. |
| Curation status | `curation/decisions.tsv` has one row for `habitatmech:BACDIVE.3edb563d49`: `CONFIRM_UNGROUNDED` with `review_depth: CLASS`. | Supported, but not item-reviewed. The note explicitly says habitathood was not assessed. |
| Worklist candidate | `just worklist` surfaces `ENVO:00000473` via the label `mine site`. `data/raw/ontology_terms.tsv` labels that CURIE `gravel pit` and lists `mine site` only as a synonym. | Near miss only. A mine site or gravel pit is not the meaning of the flat BacDive `In-situ` label. |

The record currently treats `In-situ` as a minted, ungrounded habitat. The only
maintained curation says merely that a class-level lexical sweep found no
matching vendored term; it does not decide whether `In-situ` is a habitat
identity, a collection-location qualifier meaning "in its original place", or a
metadata placeholder too broad for a habitat record.

## Evidence

The BacDive source attestation is supported by `data/raw/bacdive_isolation_sources.tsv`:
it carries `assertion_count: 3` and `assertion_unit: STRAIN`, which the
generated YAML reproduces.

The three generated `characteristic_taxa` entries are supported as BacDive
associations, not as characteristic-taxon claims:

| Rank | Taxon | Raw support | Assessment |
|---:|---|---|---|
| 1 | `NCBITaxon:265883` `Hydrogenovibrio thermophilus` | `data/raw/bacdive_source_taxa.tsv` rank 1 for `bacdive.isolation_source:in-situ`, count 1. | Supported association. |
| 2 | `NCBITaxon:267990` `Lebetimonas acidiphila` | `data/raw/bacdive_source_taxa.tsv` rank 2 for `bacdive.isolation_source:in-situ`, count 1. | Supported association. |
| 3 | `NCBITaxon:988827` `Kocuria oceani` | `data/raw/bacdive_source_taxa.tsv` rank 3 for `bacdive.isolation_source:in-situ`, count 1. | Supported association. |

None of the taxon rows has `is_characteristic: true` or a literature reference,
so the record does not overclaim that these taxa typify an in-situ habitat. The
taxon names also should not be used to infer the habitat identity; they are a
thin, post hoc association list behind BacDive's flat label.

## Completeness

- No definition is present. That is appropriate for a class-swept minted record:
  `curation/term_requests.tsv` has no item-reviewed novel-term definition for
  `habitatmech:BACDIVE.3edb563d49`.
- No parent is present. That is preferable to inventing a broad parent while
  the record's habitathood is unresolved.
- No causal graph is present. Exact ignored/hidden-inclusive searches found no
  `curation/causal_graphs/in_situ.yaml`, and a flat `In-situ` placeholder would
  not support mechanism edges.
- No environmental parameters are present. None are asserted for this BacDive
  isolation source in the committed raw inventories.
- No datasets or discussions are present. The record has no maintained
  evidence object that would make either field actionable.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `In-situ` remains only class-swept, so HabitatMech has not item-reviewed whether this BacDive label is a habitat at all. | The only `curation/decisions.tsv` row for `habitatmech:BACDIVE.3edb563d49` is `review_depth: CLASS` and says no human assessed habitathood. The kg-microbe grounding row intentionally leaves the target empty. The label is a generic Latin location qualifier, and the three BacDive taxa provide no shared environmental identity. | Add an item-level row for `habitatmech:BACDIVE.3edb563d49` in `curation/decisions.tsv`. If item review proves this is a real broad habitat rather than a metadata placeholder, add any supported novel definition in `curation/term_requests.tsv`; if it proves the label is not a habitat, use `NOT_APPLICABLE`. |

No blockers found. No minors found.

## Recommended Edits

1. Item-review `habitatmech:BACDIVE.3edb563d49` in `curation/decisions.tsv`.
   The row should decide whether BacDive `In-situ` is a citable habitat concept
   or a non-habitat collection qualifier. Do not ground it to `ENVO:00000473`
   `gravel pit`; `mine site` is only a misleading lexical near miss.
2. If `In-situ` remains a real minted habitat after review, add a supported
   `curation/term_requests.tsv` definition and a strictly broader parent.
   If no coherent habitat identity can be recovered from BacDive's flat label,
   mark the source concept `NOT_APPLICABLE` instead of turning the generated
   YAML into a term request.

## Follow-up Checks

| Fix | Proving check |
|---|---|
| Verify the item-level decision | `rg --no-ignore --hidden -n '^habitatmech:BACDIVE\\.3edb563d49\\b' curation/decisions.tsv` should show an `ITEM` row whose `decision` matches the reviewed identity. |
| Verify any novel definition | `rg --no-ignore --hidden -n '^habitatmech:BACDIVE\\.3edb563d49\\b' curation/term_requests.tsv` should show an evidence-backed definition only if the concept remains minted. |
| Regenerate the target | Run `just seed`, then `just seed-canary habitatmech:BACDIVE.3edb563d49`, and inspect `data/habitats/other/in_situ.yaml` for the expected `grounding_status`, `mapping_status`, source attestation, parents, and curation history. |
| Keep the corpus reproducible | Run `just verify-corpus --max-diffs 1`, `just validate data/habitats/other/in_situ.yaml`, `just validate-strict data/habitats/other/in_situ.yaml`, `just validate-causal-all`, `just term-requests-check`, `just validate-history`, `just worklist --limit 0 --status all`, `just report`, and `git diff --check`. |

## Additional Notes

- The ignored/hidden-inclusive exact search used patterns for the HabitatMech
  identifier, BacDive source identifier, label, and locked slug; it covered the
  maintained curation tables, raw TSVs, generated habitat records, history,
  research artifacts, and YAML-review reports.
- A broader ignored/hidden-inclusive search for `in situ` found the phrase in
  unrelated ontology definitions and research prose, but found no vendored
  ontology label or synonym that names this target.
- The only pre-existing review report whose filename contains `in_situ` is
  `20260922T074439Z-bowens_disease_squamous_cell_carcinoma_in_situ.md`, for a
  different GOLD disease-path record.
