# YAML Record Review

- Repository: HabitatMech
- Record: `data/habitats/host_associated/remains__68525761.yaml`
- Started UTC: 2026-09-26T19:39:27Z
- Finished UTC: 2026-09-26T19:39:27Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Identifier | `habitatmech:GOLD.dbfbcdfbbd` |
| Label | `Remains` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Source concept | GOLD `Host-associated > Mammals > Remains` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/PATHS.tsv`, and `curation/decisions.tsv`; the YAML record is read-only |
| Locked slug | `data/habitats/PATHS.tsv:2921` maps `habitatmech:GOLD.dbfbcdfbbd` to `remains__68525761` |

The target was selected from the current all-status worklist immediately after
the already-reviewed `Puddle` row. The generated YAML preserves the GOLD
identity with the first collapsed source id `gold.ecosystem:6426`, leaf label
`Remains`, source path `Host-associated > Mammals > Remains`,
`assertion_count: 3`, `assertion_unit: ORGANISM`, `mapping_status: SEEDED`, and
one generated parent: `habitatmech:GOLD.e889967f4f`
`mammal-associated environment`.

`data/raw/gold_ecosystem_paths.tsv:748` lists the same canonical path with
three collapsed GOLD nodes, `gold.ecosystem:6426|gold.ecosystem:8214|gold.ecosystem:8215`,
and three GOLD organism assertions. The exact path has no direct rows in the
GOLD study, biosample, or MIxS-triad side tables committed under `data/raw/`;
those side tables only mention fossilized-remains descendants such as
`Fossilized remains > Bone` and `Fossilized remains > Coprolite`.

## Validation

| Command | Result |
| --- | --- |
| `just worklist --limit 0 --status all --out /tmp/habitatmech-worklist.tsv` | Passed; wrote 953 ungrounded rows and listed `habitatmech:GOLD.dbfbcdfbbd` as the first worklist row after the already-reviewed `Puddle` target. |
| `just report` | Passed; reported 3206 generated records, 953 `UNGROUNDED` records, and 830 class-level sweeps. |
| `just validate data/habitats/host_associated/remains__68525761.yaml` | Passed; LinkML reported no issues. |
| `just validate-strict data/habitats/host_associated/remains__68525761.yaml` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| `just validate-causal-all` | Passed; validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Passed; the term-request table is current for 109 terms. |
| `just validate-history` | Passed; 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected and found 3206 generated records, with 0 missing, 0 extra, and 0 differing files. |
| `git diff --check` | Passed before this report was written. |

No validator was skipped.

## Identity and Grounding

The raw GOLD identity supports the generated source attestation exactly.
`data/raw/gold_ecosystem_paths.tsv:748` has the canonical path
`Host-associated > Mammals > Remains`, leaf label `Remains`, three collapsed
source nodes, and three organism assertions; the generated YAML records the
first source node, the exact path, the expected assertion count and unit, and a
collapsed-node note.

The current grounding row is only a class-level lexical sweep. The maintained
row at `curation/decisions.tsv:1217` is `CONFIRM_UNGROUNDED` with no object
term, no object label, no relation, `review_depth` `CLASS`, and the standard
sweep note saying no vendored term matched the label by the automated routes.
That row did not assess the source path or habitathood at item depth.

The existing parent is supported as a GOLD hierarchy edge. The immediate GOLD
parent path `Host-associated > Mammals` appears in
`data/raw/gold_ecosystem_paths.tsv:9`, is pinned to
`habitatmech:GOLD.e889967f4f` by `data/habitats/PATHS.tsv:3021`, and has a
reviewed item-level decision plus a term-requested definition as
`mammal-associated environment`.

The vendored slice contains potentially related but non-exact terms. `BTO:0001965`
`carcass` and `ENVO:00002033` `carcass` both denote dead bodies, and the ENVO
term is already a reviewed record for BacDive `Decomposing-animal`; however,
GOLD keeps `Fossilized remains` under the Mammals `Remains` node, and that
branch contains bone, tooth, and coprolite children that are mammalian remains
without necessarily being whole dead bodies. Item review should decide whether
`carcass` is a broader parent, an xref, or only a near miss before a definition
for this record is requested.

## Evidence

| Claim | Evidence | Status |
| --- | --- | --- |
| The record denotes the GOLD path `Host-associated > Mammals > Remains`. | `data/raw/gold_ecosystem_paths.tsv:748` lists the canonical path, leaf, three source nodes, and node ids `gold.ecosystem:6426|gold.ecosystem:8214|gold.ecosystem:8215`; the generated YAML repeats the first node and exact source path. | Supported exactly. |
| The source attestation has three GOLD organism assertions. | `data/raw/gold_ecosystem_paths.tsv:748` reports `organism_count` 3 and `total_assertions` 3; the generated YAML records `assertion_count: 3` with `assertion_unit: ORGANISM`. | Supported exactly. |
| `Remains` is narrower than `mammal-associated environment`. | The source path places `Remains` directly under GOLD `Host-associated > Mammals`; the parent record is a reviewed minted term for microbial environments determined by mammals. | Supported as the generated source-path edge. |
| `Remains` has no exact vendored ontology identity. | No exact identity has been item-reviewed. The current negative decision is only a class-level lexical sweep; the vendored `carcass` terms are relevant near misses but have a narrower dead-body scope than GOLD's broader Remains branch. | Plausible but unresolved until item review. |

## Completeness

The generated YAML is complete for its current input state: it has the exact
GOLD source path, the collapsed-node note, one source-path parent, and generated
curation-history events for the class-level `CONFIRM_UNGROUNDED` row and the
source seed. Its empty definition, synonyms, xrefs, environmental parameters,
characteristic taxa, causal graphs, discussions, and datasets are expected for
a class-swept generated record that lacks an item-level term request or causal
overlay.

Ignored/hidden-inclusive exact searches for
`habitatmech:GOLD.dbfbcdfbbd`, `GOLD.dbfbcdfbbd`, `dbfbcdfbbd`,
`gold.ecosystem:6426`, `gold.ecosystem:8214`, `gold.ecosystem:8215`, and the
exact GOLD path covered `data/raw/`, `data/habitats/`, `curation/`, `history/`,
`research/`, `conf/`, `reports/habitat_research_manifest.tsv`, and
`reports/yaml_record_review/`. They found the generated target, its locked
slug, its raw GOLD row, and its class-level decision, and they found no prior
YAML review for this Mammals `Remains` target, no target-specific term request
or term-request exclusion, no history record, no research report, and no causal
overlay.

An ignored/hidden-inclusive exact-path search over
`data/raw/gold_studies.tsv`, `data/raw/gold_path_biosamples.tsv`, and
`data/raw/gold_path_triads.tsv` found no direct side-table rows for
`Host-associated > Mammals > Remains`. The populated descendant rows under the
same branch start at `Fossilized remains`, so they support the shape of the
source hierarchy but not an environmental triad, study id, or biosample count
for the exact parent record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | `Remains` is still only a class-level `CONFIRM_UNGROUNDED` even though the GOLD path denotes a real mammalian-remains habitat that needs item-level disposition. | `curation/decisions.tsv:1217` has `review_depth` `CLASS`; `data/raw/gold_ecosystem_paths.tsv:748` records three direct organism assertions for `Host-associated > Mammals > Remains`; `data/raw/gold_ecosystem_paths.tsv:2091` records a `Fossilized remains` child below it. | `curation/decisions.tsv`; if the record stays minted, `curation/term_requests.tsv`. |

No blockers or minor findings.

## Recommended Edits

1. Item-review `habitatmech:GOLD.dbfbcdfbbd` in
   `curation/decisions.tsv`. Inspect GOLD nodes `6426`, `8214`, and `8215` and
   decide whether the exact source concept denotes mammalian remains as a
   postmortem host-associated environment.
2. Compare `ENVO:00002033` `carcass` and `BTO:0001965` `carcass` against the
   direct GOLD `Remains` path and its fossilized-remains child. Retain `carcass`
   only if it is a true broader parent or a genuinely related xref; do not
   ground `Remains` exactly to a dead-body term if the source also covers
   fossilized bones, teeth, and coprolites.
3. If no existing term fits, keep `CONFIRM_UNGROUNDED` at `ITEM` depth and add
   an evidence-backed `curation/term_requests.tsv` definition for a
   mammalian-remains associated environment, parented under the reviewed
   `mammal-associated environment` record unless item review finds a tighter
   true genus.

## Follow-up Checks

After the item-level decision:

- Run an ignored/hidden-inclusive exact search for
  `habitatmech:GOLD.dbfbcdfbbd`, `gold.ecosystem:6426`, `gold.ecosystem:8214`,
  and `gold.ecosystem:8215` across `curation`, `history`, `research`,
  `data/habitats`, `data/raw`, and `reports`.
- Run `just seed`.
- Run `just seed-canary habitatmech:GOLD.dbfbcdfbbd`.
- Inspect the regenerated target and
  `data/habitats/host_associated/fossilized_remains__eec3c6e8.yaml` to confirm
  the child source path points to the resolved Remains parent.
- Run `just validate` on the regenerated target path.
- Run `just validate-strict` on the regenerated target path.
- Run `just validate-causal-all`.
- Run `just term-requests-check`.
- Run `just validate-history`.
- Run `just verify-corpus --max-diffs 1`.
- Run `git diff --check`.

## Additional Notes

Several generated records share the label `Remains` for other GOLD host clades,
including Birds, Fish, and Mammals: Human. This report covers only
`habitatmech:GOLD.dbfbcdfbbd`, the `Host-associated > Mammals > Remains` row
selected from the current worklist.
