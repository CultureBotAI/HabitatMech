# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/sludge__25c88b98.yaml
- Started UTC: 2026-09-25T18:30:44Z
- Finished UTC: 2026-09-25T18:31:09Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.33ad86bb23` |
| Label | `Sludge` |
| Category | `ENGINEERED` |
| Source path | `Engineered > Bioreactor > DHS reactor > Sludge` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv` and `data/habitats/PATHS.tsv`; future item-level review belongs in `curation/decisions.tsv`, and any retained minted definition or parent replacement belongs in `curation/term_requests.tsv`. |

The target is a GOLD-only, generated, `NARROW`, still-`SEEDED` record for the
two-node `Sludge` child under the DHS reactor path. The seeder minted a stable
HabitatMech identifier, attached `ENVO:00002044` `sludge` through the
ambiguous-leaf route, inherited `habitatmech:GOLD.24cf427e7d` `DHS reactor`
from the immediate GOLD parent path, and represented the two collapsed GOLD
ecosystem node IDs with `source_id: gold.ecosystem:5835`.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/habitats/engineered/sludge__25c88b98.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/sludge__25c88b98.yaml` | Pass: scanned 1 file with 0 files containing `ERROR` and 0 total `ERROR` rows. |
| `just validate-causal curation/causal_graphs/sludge__25c88b98.yaml` | Not applicable: the target has no `causal_graphs` block, and `find curation/causal_graphs -maxdepth 1 -type f -name 'sludge__25c88b98.yaml' -print` found no target-specific overlay. |
| `just validate-causal-all` | Pass: validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Pass: the term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: expected 3,206 records, found 3,206 on disk, and the corpus reproduces exactly from `data/raw/`. |
| `just worklist --limit 2000 --status all --out /tmp/habitatmech-dhs-sludge-worklist.tsv` | Pass: reported 953 ungrounded records, 1,810 decisions on file, and wrote 953 rows to `/tmp/habitatmech-dhs-sludge-worklist.tsv`. |
| `just report` | Pass: completed the corpus report for 3,206 records. |
| Reference validator | Not applicable: this generated GOLD-only record has no record-level `evidence`, `characteristic_taxa`, `causal_graphs`, `discussions`, or `datasets` with citation-bearing `EvidenceItem` references. |

## Identity and Grounding

The generated identifier, label, category, source attestation, and slug agree
with the raw GOLD inventory. `data/raw/gold_ecosystem_paths.tsv:1179` has the
exact path `Engineered > Bioreactor > DHS reactor > Sludge`, `gold_node_count=2`,
no direct organism, study, biosample, or total assertions in the collapsed
summary row, and
`gold_node_ids=gold.ecosystem:5835|gold.ecosystem:5836`.
`data/habitats/PATHS.tsv:1670` maps `habitatmech:GOLD.33ad86bb23` to the stable
`sludge__25c88b98` slug.

The generated `ENVO:00002044` parent is supported and appropriately broader.
`data/raw/ontology_terms.tsv:7189` labels that term `sludge` and defines it as
the residual semi-solid material left from domestic, industrial, or
wastewater-treatment processes. The target is source-specific DHS reactor
sludge, so generic sludge is broader rather than exact.

The generated `habitatmech:GOLD.24cf427e7d` parent is not supported as a strict
broader habitat. The target leaf names sludge material in the
`Engineered > Bioreactor > DHS reactor` context; that material can be contained
by a DHS reactor, but it is not a subtype of the reactor itself. The immediate
GOLD parent is itself only class-reviewed: `curation/decisions.tsv:307` records
a `CLASS`-depth `CONFIRM_UNGROUNDED` decision whose rationale explicitly says
whether the DHS reactor concept is a habitat was not assessed.

No item-level curation row exists for `habitatmech:GOLD.33ad86bb23`; the target
has only its generated seed event. The `NARROW` state is explainable from the
seeder's ambiguous-leaf and source-parent routes, but it has not been endorsed
by a curator.

## Evidence

The sole `GOLD` source attestation mirrors
`data/raw/gold_ecosystem_paths.tsv:1179`: source ID `gold.ecosystem:5835`,
source label `Sludge`, source path
`Engineered > Bioreactor > DHS reactor > Sludge`, and
`mapping_predicate: skos:narrowMatch`. The attestation note correctly records
that two GOLD ecosystem node IDs share this collapsed path and that the first
one is shown.

Two GOLD side tables also name the same path: `data/raw/gold_path_biosamples.tsv:778`
records four biosamples for path ID `5836`, and
`data/raw/gold_studies.tsv:683` records study `Gs0111456` across this sludge
path and `Engineered > Bioreactor > DHS reactor > Wastewater`. Those tables are
supporting inventory for GOLD API triad enumeration; `src/habitatmech/seed.py`
currently builds GOLD source attestations from `gold_ecosystem_paths.tsv`, and
this target has no exact path row in `data/raw/gold_path_triads.tsv` or
`data/raw/environment_parameters.tsv`.

Unsupported or over-scoped generated claims:

- `habitatmech:GOLD.24cf427e7d` as a `parent_habitats` entry is an over-scoped
  source-context edge. `habitatmech:GOLD.24cf427e7d` names a DHS reactor, not
  sludge material contained by a DHS reactor.

## Completeness

The generated record carries the expected source-owned values for the current
raw inputs: a minted identifier, GOLD label, `ENGINEERED` category,
ambiguous-leaf `sludge` parent, inherited `DHS reactor` parent, a single GOLD
source attestation covering the collapsed GOLD path, a seed event, and a stable
slug lock.

The curation state is incomplete. Exact `rg --no-ignore --hidden` searches for
`habitatmech:GOLD.33ad86bb23`, `gold.ecosystem:5835`,
`gold.ecosystem:5836`, the target source path, and `sludge__25c88b98` under
the maintained source and review areas found the generated YAML record, the raw
GOLD rows, and the slug-lock row, but no item-level decision,
`curation/term_requests.tsv` definition, target-specific history, research
report, maintained causal overlay, or prior slug-named YAML review report.
Exact `find` searches found no target-specific causal overlay and no prior
slug-named YAML review report. Ignored files were included in all of those
absence checks.

Optional characteristic-taxon, environmental-parameter, DOI/PMID/URL evidence,
dataset, discussion, and causal-graph slots are correctly absent for this GOLD
child in the generated record. The source-side biosample and study rows name
the path, but they do not currently provide a generated record field beyond the
GOLD path support already captured by the source attestation.

## Findings

- **major**: `habitatmech:GOLD.33ad86bb23` has no item-level review, so its
  `NARROW` status and source-specific DHS-sludge identity remain seeded rather
  than curated.

  **Evidence**: The target GOLD row is a distinct two-node `Sludge` child under
  `Engineered > Bioreactor > DHS reactor`, while the exact `ENVO:00002044`
  `sludge` identity is already claimed by the generic `data/habitats/other/sludge.yaml`
  ontology record. Exact gitignore-independent searches found no target-specific
  row in `curation/decisions.tsv`, no `curation/term_requests.tsv` definition,
  and no target-specific history or research report.

  **Owner**: `curation/decisions.tsv` should hold the item-level decision; if
  the item-level review keeps the minted identifier,
  `curation/term_requests.tsv` should hold its definition and strict parent.

- **major**: `parent_habitats` asserts that this DHS-reactor sludge is a kind
  of `habitatmech:GOLD.24cf427e7d` `DHS reactor`.

  **Evidence**: The inherited parent comes from GOLD's `Engineered >
  Bioreactor > DHS reactor` path segment. `habitatmech:GOLD.24cf427e7d` names
  a bioreactor context, while this child leaf names sludge material in that
  context. The record has no item-level decision, term request, MIxS triad,
  causal overlay, or source side-table row saying the sludge is itself a
  reactor.

  **Owner**: `curation/term_requests.tsv` should replace the generated
  source-path parent with `ENVO:00002044` or a tighter inspected material genus
  if the item-level decision confirms the target is a real minted habitat.

No blockers found.

No minor findings.

## Recommended Edits

1. In `curation/decisions.tsv`, add an item-level review for
   `habitatmech:GOLD.33ad86bb23` scoped to
   `Engineered > Bioreactor > DHS reactor > Sludge`, the broader
   `ENVO:00002044` `sludge` parent, and the contextual
   `habitatmech:GOLD.24cf427e7d` `DHS reactor` edge.

2. If item review keeps this as source-specific DHS-reactor sludge, define it
   in `curation/term_requests.tsv` under `ENVO:00002044` or a tighter inspected
   material genus, and use `parent_mode=REPLACE` if the review confirms that
   `habitatmech:GOLD.24cf427e7d` is contextual rather than strict.

3. Regenerate the corpus from those maintained inputs so the generated record
   records the curator decision and no longer carries a false DHS-reactor
   `is-a` edge.

## Follow-up Checks

After curation, rerun:

- `just term-requests-check`
- `just validate data/habitats/engineered/sludge__25c88b98.yaml`
- `just validate-strict data/habitats/engineered/sludge__25c88b98.yaml`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000 --status all --out /tmp/habitatmech-dhs-sludge-worklist.tsv`

## Additional Notes

- `data/raw/gold_path_biosamples.tsv` shows that the second collapsed GOLD node
  ID, `5836`, has four biosamples behind it. That support can help a future
  item-level review find the submitter context, but the current generator
  intentionally emits the first collapsed node ID and does not use the
  biosample side table as the source-attestation assertion count.
- The false `DHS reactor` edge is the generic version of the contextual-parent
  pattern already observed for zero-count DHS aerobic and anaerobic sludge
  children: source-path reactor context becomes a false parent when the leaf is
  a material rather than the reactor device.
