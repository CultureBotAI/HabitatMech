# YAML Record Review: archipelago

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/archipelago.yaml
- Started UTC: 2026-09-21T17:31:38Z
- Finished UTC: 2026-09-21T17:31:38Z
- Verdict: needs curation

## Target

The target is the generated `HabitatRecord` at `data/habitats/aquatic/archipelago.yaml`.

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `ENVO:00000220` |
| Label | `archipelago` |
| Category | `AQUATIC` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Source concepts | GOLD `gold.ecosystem:5382` |
| Locked stem | `archipelago` in `data/habitats/PATHS.tsv` |

This review covers the GOLD `Environmental > Aquatic > Marine > Archipelago` source concept grounded exactly to ENVO `archipelago`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/archipelago.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/archipelago.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 records on disk, 0 missing, 0 extra, 0 differing. |
| Reference validator | Not checked: the generated record has no DOI/PMID/URL `EvidenceItem`, no causal-edge evidence, and no curated causal overlay. |

## Identity and Grounding

| Claim | Evidence | Assessment |
|---|---|---|
| The generated record denotes GOLD's marine `Archipelago` source concept. | `data/raw/gold_ecosystem_paths.tsv` has canonical path `Environmental > Aquatic > Marine > Archipelago`, leaf `Archipelago`, depth `4`, one GOLD node ID `gold.ecosystem:5382`, and zero organism, study, biosample, or total assertions. | Supported exactly. |
| The GOLD source concept matches ENVO `archipelago`. | `data/raw/ontology_terms.tsv` lists active term `ENVO:00000220` with label `archipelago`, definition `A group of geographically or geologically associated islands.`, and exact synonyms `ISLAND CLUSTER`, `IslandArc`, `island arc`, and `island group`; the GOLD leaf is `Archipelago`. | Supported exactly. |
| The locked generated identifier is stable. | `data/habitats/PATHS.tsv` maps `ENVO:00000220` to stem `archipelago`; hidden/ignored-inclusive exact search found no curation, history, causal-graph, research, or previous YAML review entry for `ENVO:00000220`, `gold.ecosystem:5382`, `archipelago`, or `Environmental > Aquatic > Marine > Archipelago`. | Supported exactly. |
| The `ENVO:01001784` parent is a valid ontology parent. | `data/raw/ontology_subclass_edges.tsv` has `ENVO:00000220 rdfs:subClassOf ENVO:01001784`, and `data/raw/ontology_terms.tsv` labels `ENVO:01001784` as `compound astronomical body part`. | Supported exactly. |
| The `ENVO:00001999` parent is generated from the upstream GOLD `Marine` path parent. | `data/raw/gold_ecosystem_paths.tsv` has a direct parent path `Environmental > Aquatic > Marine` that resolves to `ENVO:00001999` `marine water body`; `data/raw/gold_path_triads.tsv` also records the same parent GOLD path with `local = ENVO:00001999 marine water body`. | Needs curation; the source-path context is marine, but an ENVO archipelago is a group of islands and not a subtype of a lentic body composed primarily of marine water. |
| The record has not been item-reviewed yet. | Hidden/ignored-inclusive exact search across `curation`, `history`, `curation/causal_graphs`, `reports/yaml_record_review`, and `research/habitats` found no entry for `ENVO:00000220`, GOLD node `gold.ecosystem:5382`, minted source identifier `habitatmech:GOLD.8ec9c78331`, or the canonical GOLD path. | Supported; `mapping_status: SEEDED` is expected until a maintained item-level decision exists. |

## Evidence

The generated record has no record-level `evidence` entries and no `causal_graphs`.

| Claim | Evidence | Assessment |
|---|---|---|
| The GOLD source attestation preserves the source path and source ID. | `data/raw/gold_ecosystem_paths.tsv` has `gold.ecosystem:5382` on `Environmental > Aquatic > Marine > Archipelago` with leaf `Archipelago`; the generated `source_attestations` entry carries the same source ID, label, and path. | Supported exactly. |
| The target path has no direct sample counts in the raw GOLD path table. | The canonical GOLD row for `Environmental > Aquatic > Marine > Archipelago` reports `organism_count=0`, `study_count=0`, `biosample_count=0`, and `total_assertions=0`, matching the absence of generated characteristic taxa or assertion counts on the source attestation. | Supported exactly. |
| The child sediment path is modeled separately. | The raw GOLD tables list `Environmental > Aquatic > Marine > Archipelago > Sediment` as GOLD node `gold.ecosystem:5383`, and `data/raw/gold_path_biosamples.tsv` reports 8 biosamples for that child path. | Supported; those child assertions do not belong on the parent `archipelago` source attestation. |

## Completeness

- The generated record is complete enough for an automatically grounded, source-derived GOLD concept: it has the ENVO definition, ENVO synonyms, GOLD source attestation, ontology parent, generated GOLD source-path parent, and generated seed event.
- The record correctly omits assertion counts, taxon associations, curated record evidence, and causal edges because the raw GOLD parent bucket has no direct assertion counts and no maintained curation or causal overlay was found.
- The consequential semantic gap is limited to one inherited source-path parent: `parent_habitats` makes ENVO `archipelago` a child of ENVO `marine water body`.
- Hidden/ignored-inclusive exact searches covered `data/raw`, `data/habitats/PATHS.tsv`, `curation`, `history`, `curation/causal_graphs`, `reports/yaml_record_review`, and `research/habitats` for `ENVO:00000220`, `gold.ecosystem:5382`, `habitatmech:GOLD.8ec9c78331`, `archipelago`, and `Environmental > Aquatic > Marine > Archipelago`.
- The only directly relevant maintained rows found were source inventory and ontology rows; there is no current curation input row or overlay that owns a correction for the inherited GOLD `Marine` parent edge.

## Findings

| ID | Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| `HM-ARCHIPELAGO-001` | Major | The GOLD `Environmental > Aquatic > Marine > Archipelago` record inherits a false ENVO parent. `parent_habitats` asserts `ENVO:00000220` `archipelago` is a child of `ENVO:00001999` `marine water body`, but ENVO defines the target as a group of islands and defines `marine water body` as a lentic body composed primarily of marine water. The valid direct ontology parent is already present as `ENVO:01001784` `compound astronomical body part`. | `data/habitats/aquatic/archipelago.yaml` lists both `ENVO:00001999` and `ENVO:01001784`; `data/raw/gold_ecosystem_paths.tsv` shows `Environmental > Aquatic > Marine` is the GOLD parent path of `Environmental > Aquatic > Marine > Archipelago`; `data/raw/ontology_terms.tsv` defines `ENVO:00000220` and `ENVO:00001999`; `data/raw/ontology_subclass_edges.tsv` places `ENVO:00000220` directly under `ENVO:01001784`, not under `ENVO:00001999`. | There is no current data-only curation surface that can suppress one generated GOLD source-path parent edge while preserving exact ENVO grounding. Add a maintained source-path parent override in `curation/` and teach `src/habitatmech/seed.py` to apply it during GOLD parent linking, then add a row that suppresses only `Environmental > Aquatic > Marine -> Environmental > Aquatic > Marine > Archipelago`. |

No blocker or minor findings found.

## Recommended Edits

1. Add a maintained curation input for suppressing false generated source-path parent edges, and update the GOLD second pass in `src/habitatmech/seed.py` so a curator can reject a specific parent edge without changing source identity.
2. Add one item-level override for minted source concept `habitatmech:GOLD.8ec9c78331` that suppresses `ENVO:00001999` as an inherited parent while preserving exact identity to `ENVO:00000220` and ontology parent `ENVO:01001784`.
3. Add a `curation/decisions.tsv` `REVIEW` or `GROUND` row for `habitatmech:GOLD.8ec9c78331` only after the false inherited parent is suppressible, so the record can become `REVIEWED` without endorsing the bad is-a claim.
4. Regenerate the record with a canary and inspect `data/habitats/aquatic/archipelago.yaml` before any wider seed run.

## Follow-up Checks

| Edit | Narrowest proving check |
|---|---|
| Suppress the false marine-water-body parent edge. | Re-read the new parent-edge curation input, run `just seed-canary ENVO:00000220`, and confirm `data/habitats/aquatic/archipelago.yaml` keeps `ENVO:01001784` and drops `ENVO:00001999`. |
| Preserve exact ENVO grounding and GOLD attestation. | Inspect the regenerated target and confirm `identifier: ENVO:00000220`, `grounding_status: EXACT`, `source_id: gold.ecosystem:5382`, and `mapping_predicate: skos:exactMatch` remain unchanged. |
| Promote the record only after item-level review. | Re-read the new `curation/decisions.tsv` row for `habitatmech:GOLD.8ec9c78331` and confirm its `review_depth` is `ITEM` and its notes name the inspected GOLD path and rejected parent edge. |
| Validate the regenerated record and corpus. | `just validate data/habitats/aquatic/archipelago.yaml`, `just validate-strict data/habitats/aquatic/archipelago.yaml --quiet`, `just validate-history`, `just term-requests-check`, and `just verify-corpus --max-diffs 1`. |

## Additional Notes

- `data/raw/gold_ecosystem_paths.tsv` also lists the child path `Environmental > Aquatic > Marine > Archipelago > Sediment` as `gold.ecosystem:5383`, and `data/raw/gold_path_biosamples.tsv` reports 8 biosamples for that sediment child. That supports keeping `Archipelago` as a source-path context in the corpus but does not justify copying child counts to the parent attestation.
- `curation/term_requests.tsv` can already replace generated parent sets for HabitatMech-minted terms with authored definitions, but this target is the existing ENVO class `ENVO:00000220`. Reusing the term-request table here would require pretending HabitatMech is defining `archipelago`, not simply suppressing one false source-path parent.
