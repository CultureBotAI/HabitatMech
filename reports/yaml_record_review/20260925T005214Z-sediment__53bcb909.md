# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/engineered/sediment__53bcb909.yaml`
- Started UTC: `2026-09-25T00:48:50Z`
- Finished UTC: `2026-09-25T00:52:15Z`
- Verdict: needs curation

## Target

Reviewed the complete generated record at `data/habitats/engineered/sediment__53bcb909.yaml`. The record is a GOLD-only engineered habitat for the canonical path `Engineered > Artificial ecosystem > Aquaculture > Molluscs pond > Sediment`, minted as `habitatmech:GOLD.89569f7bcc`.

The generated file has `grounding_status: NARROW` and `mapping_status: SEEDED`. It has one true ontology parent, `ENVO:00002007` `sediment`, one inherited GOLD source-path parent, `habitatmech:GOLD.236eb735d7` `Molluscs pond`, and one `GOLD` source attestation for `gold.ecosystem:8008`.

## Validation

| Check | Result |
|---|---|
| `find reports/yaml_record_review -maxdepth 1 -type f -name '*-sediment__53bcb909.md' -print` | Passed before this report was written; no existing exact `sediment__53bcb909` review was found. `find` is not `.gitignore`-filtered. |
| `just validate data/habitats/engineered/sediment__53bcb909.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/sediment__53bcb909.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| Focused causal overlay validator | Not applicable; exact ignored-inclusive searches in `curation/causal_graphs/` found no `habitatmech:GOLD.89569f7bcc`, `gold.ecosystem:8008`, `sediment__53bcb909`, or exact source-path overlay. |
| Reference validator | Not applicable; the target record has no record-level `evidence` entries and no causal graph edges with citation references. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the generated ENVO term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records all exist on disk, with 0 missing, 0 extra, and 0 differing records. |
| `just worklist --limit 2000` | Passed; it reported 0 still-undecided ungrounded records and 1,810 decisions on file. |
| `just report` | Passed; the corpus report completed for 3,206 records. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

The generated identifier, label, category, source attestation, and ontology grounding agree with the committed GOLD inventory. `data/raw/gold_ecosystem_paths.tsv` has the exact child path at depth 5 with `gold_node_count` 1, no direct organism, study, biosample, or total assertions, and source ID `gold.ecosystem:8008`. `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.89569f7bcc` to the stable `sediment__53bcb909` slug.

The `NARROW` relationship to `ENVO:00002007` `sediment` is sound. GOLD has many distinct deeper paths with the `Sediment` leaf; HabitatMech's anti-conflation rule keeps these contextual sediment paths as minted identifiers under the generic sediment material parent instead of merging engineered, marine, freshwater, and other sediments onto one ontology record.

The record is still only seeded because exact ignored-inclusive searches across `data/raw/`, `data/habitats/`, `curation/`, `history/`, `research/`, `conf/`, `reports/yaml_record_review/`, `pages/habitats/`, and `curation/causal_graphs/` found the generated target, the `PATHS.tsv` row, the canonical GOLD ecosystem-path row, the generated page, and the prior Molluscs pond review's mentions of this child, but no item-level decision row, term request, history entry, research report, label-correspondence residual, curated sample row, target causal overlay, auxiliary GOLD biosample/triad/study row, or prior exact review for `habitatmech:GOLD.89569f7bcc`, `gold.ecosystem:8008`, `sediment__53bcb909`, or the exact source path.

The second generated parent is not a strict broader material parent. The GOLD path provides Molluscs pond context for the leaf `Sediment`, while `parent_habitats` is an `is_a` claim. Sediment contained in a molluscs pond is a sediment material in an aquaculture context, not a subtype of the containing pond.

## Evidence

- Supported: the `GOLD` source attestation is traceable to `data/raw/gold_ecosystem_paths.tsv`, including the single upstream node ID `gold.ecosystem:8008`.
- Supported: the generated `ENVO:00002007` parent follows from the anti-conflation rule for a deep GOLD `Sediment` leaf; the ontology slice contains `ENVO:00002007` as a valid broader sediment material class.
- Supported side-table context: exact ignored-inclusive searches across the GOLD biosample, triad, study, and ecosystem-path side tables found only the canonical `gold_ecosystem_paths.tsv` row for `gold.ecosystem:8008` or the exact source path. No committed auxiliary GOLD side table adds target-specific biosample, study, or MIxS triad claims for this child.
- Unsupported hierarchy: the generated edge from `habitatmech:GOLD.89569f7bcc` to `habitatmech:GOLD.236eb735d7` is an inherited GOLD source-path edge. The child record denotes sediment in a pond, not a subtype of the containing pond.
- Under-reviewed: the record has no item-level decision, authored definition, term request, record-level evidence, discussion, dataset, characteristic taxon, environmental parameter, curated sample row, or causal graph.

## Completeness

The committed source inventories are sufficient to explain the current generated record. Exact ignored-inclusive searches covering `data/raw/`, `data/habitats/`, `curation/`, `history/`, `research/`, `conf/`, `reports/yaml_record_review/`, `pages/habitats/`, and `curation/causal_graphs/` found no maintained item-level decision, no term request, and no target causal overlay to audit.

The empty optional slots are appropriate for a seeded GOLD path with no curated overlay: no maintained input currently provides characteristic taxa, environmental parameters, causal edges, or record-level cited evidence for the target.

The parent `Molluscs pond` record was reviewed separately. That review found a class-level `UNGROUNDED` gap because `ENVO:03600074` `aquaculture farm` is available as a strict broader term, but no possible future grounding can make a sediment material into a subtype of the containing pond.

## Findings

| Severity | Finding | Evidence | Future owner |
|---|---|---|---|
| Major | `Sediment` inherits `Molluscs pond` as an `is_a` parent even though the child denotes sediment contained in the engineered pond, not a subtype of that pond. | The generated YAML has both `ENVO:00002007` and `habitatmech:GOLD.236eb735d7` in `parent_habitats`. The exact GOLD source path is `Engineered > Artificial ecosystem > Aquaculture > Molluscs pond > Sediment`, and the generated target already keeps the true broader `ENVO:00002007` sediment material parent. | New maintained source-path parent override |

## Recommended Edits

1. Item-review `habitatmech:GOLD.89569f7bcc` in `curation/decisions.tsv` against the exact `Engineered > Artificial ecosystem > Aquaculture > Molluscs pond > Sediment` source path, the existing `ENVO:00002007` sediment parent, and the generated `habitatmech:GOLD.236eb735d7` Molluscs pond parent.
2. Do not try to remove the false parent solely with `curation/term_requests.tsv`: `parent_mode=REPLACE` is only accepted for term-request-generated `UNGROUNDED` records, while this child is currently a `NARROW` GOLD leaf under `ENVO:00002007` and still receives its source-path parent from `ingest_gold()`.
3. Add a maintained source-path parent override that can suppress `habitatmech:GOLD.236eb735d7` for this reviewed `NARROW` child while preserving the ontology parent `ENVO:00002007`; keep Molluscs pond and aquaculture context out of `parent_habitats` unless a future maintained input has a non-`is_a` slot for local source-path context.

## Follow-up Checks

- `just seed`
- `just seed-canary habitatmech:GOLD.89569f7bcc`
- Inspect `data/habitats/engineered/sediment__53bcb909.yaml`
- `just validate data/habitats/engineered/sediment__53bcb909.yaml`
- `just validate-strict data/habitats/engineered/sediment__53bcb909.yaml`
- `just term-requests-check`
- `just validate-history`
- `just validate-causal-all`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

## Additional Notes

The review did not find a target causal overlay, so there was no focused `just validate-causal curation/causal_graphs/<slug>.yaml` invocation to run. The whole-overlay validator still passed.

This review left generated products and curation inputs unchanged. Fixing the false parent needs a maintained source-path parent override, followed by a normal seed regeneration.
