# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/abscess__c48fc20f.yaml`
- Started UTC: 2026-09-23T00:48:00Z
- Finished UTC: 2026-09-23T00:57:59Z
- Verdict: pass

## Target

| Field | Value |
| --- | --- |
| Path | `data/habitats/host_associated/abscess__c48fc20f.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.9c459b5a33` |
| Label | `Abscess` |
| Habitat category | `HOST_ASSOCIATED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Record status | Generated from one GOLD source concept with no item-level curation decision |
| GOLD source id | `gold.ecosystem:6344` |
| GOLD source path | `Host-associated > Mammals: Human > Skeletal system > Joint capsule > Abscess` |
| Locked slug | `data/habitats/PATHS.tsv:2451` maps `habitatmech:GOLD.9c459b5a33` to `abscess__c48fc20f` |

This is the generated record for the GOLD joint-capsule Abscess path. The GOLD source-concept key is `habitatmech:GOLD.9c459b5a33`, computed as `sha1("GOLD:Host-associated > Mammals: Human > Skeletal system > Joint capsule > Abscess")[:10]`. The record is correctly minted and `NARROW`: the source concept is narrower than the generic `mesh:D000038` `Abscess` term because the full GOLD path supplies the joint-capsule context.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/habitats/host_associated/abscess__c48fc20f.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/host_associated/abscess__c48fc20f.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Passed; validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Passed; the term-request table is current at 109 terms. |
| `just validate-history` | Passed; 77 history records validated. |
| `just verify-corpus --max-diffs 1` | Passed; all 3,206 generated records reproduced exactly from `data/raw/`, with 0 missing, 0 extra, and 0 differing. |
| `just worklist --limit 2000` filtered for `Abscess`, `abscess`, `GOLD\.9c459b5a33`, and `mesh:D000038` | Completed with no matching rows, as expected for a seeded `NARROW` GOLD record that is not in the class-level ungrounded backlog. |
| `just report` | Passed; the corpus summary still reports 0 risky groundings not yet reviewed, 0 contradicted class-level sweeps, and 0 current records claiming a non-habitat organism/process term as a habitat. |

## Identity and Grounding

The record identity is the GOLD leaf `Abscess` under `Host-associated > Mammals: Human > Skeletal system > Joint capsule`. `data/raw/gold_ecosystem_paths.tsv:1093` carries exactly that path with source id `gold.ecosystem:6344`, 1 organism assertion, 0 study assertions, 0 biosample assertions, and one GOLD node id. `data/habitats/host_associated/abscess__c48fc20f.yaml:1` preserves the minted `habitatmech:GOLD.9c459b5a33` identifier and `:11-17` preserve the same GOLD source id, source label, source path, `skos:narrowMatch` predicate, and 1 `ORGANISM` assertion.

The `habitatmech:GOLD.e7296678c2` parent is supported. The parent source path is `Host-associated > Mammals: Human > Skeletal system > Joint capsule` in `data/raw/gold_ecosystem_paths.tsv:2452`, and `data/habitats/host_associated/joint_capsule__3bc01568.yaml:1-15` is the generated parent record for that same GOLD path.

The `mesh:D000038` parent is also supported. `data/raw/ontology_terms.tsv:13554` labels `mesh:D000038` as `Abscess`, and the joint-capsule Abscess source concept is a joint-capsule-specific subtype of a generic abscess. Other raw GOLD paths reuse the leaf `Abscess` under skin, nasal-cavity, brain, spinal-cord, abdominal/peritoneal, and soft-tissue contexts, so the ambiguous-leaf rule correctly prevents this one GOLD path from becoming exactly `mesh:D000038`.

`grounding_status: NARROW` is correct: the target has two broader parents, but neither parent is an exact identity for a GOLD joint-capsule Abscess. `mapping_status: SEEDED` is also correct because exact ignored/hidden-inclusive searches found no maintained `curation/decisions.tsv`, `curation/term_requests`, `curation/causal_graphs`, `history`, or raw research artifact that reviews `habitatmech:GOLD.9c459b5a33` or `gold.ecosystem:6344`.

## Evidence

| Claim | Source | Assessment |
| --- | --- | --- |
| The record represents the GOLD path `Host-associated > Mammals: Human > Skeletal system > Joint capsule > Abscess`. | `data/raw/gold_ecosystem_paths.tsv:1093`; `data/habitats/host_associated/abscess__c48fc20f.yaml:11-17` | Supported exactly. |
| The path is attested by 1 GOLD organism assertion and no study or biosample assertions. | `data/raw/gold_ecosystem_paths.tsv:1093`; `data/habitats/host_associated/abscess__c48fc20f.yaml:15-17` | Supported exactly. |
| `habitatmech:GOLD.e7296678c2` is the immediate GOLD source-path parent. | `data/raw/gold_ecosystem_paths.tsv:2452`; `data/habitats/host_associated/joint_capsule__3bc01568.yaml:1-15`; `data/habitats/host_associated/abscess__c48fc20f.yaml:6-8` | Supported. A joint-capsule Abscess is narrower than the parent joint-capsule GOLD path. |
| `mesh:D000038` names the broader generic Abscess concept. | `data/raw/ontology_terms.tsv:13554`; `data/habitats/host_associated/abscess__c48fc20f.yaml:6-8` | Supported. The MeSH term is a valid broader parent but not an exact target for the path-specific GOLD record. |
| `abscess__c48fc20f` is the locked generated slug for this minted identifier. | `data/habitats/PATHS.tsv:2451` | Supported exactly. |

Unsupported or over-scoped claims: none found. The record carries only GOLD source provenance, a generic Abscess parent, the joint-capsule source-path parent, and seeded audit metadata. It does not claim characteristic taxa, environmental parameters, causal mechanisms, cross references, synonyms, or literature-backed definition text.

## Completeness

The record intentionally has no definition, synonyms, environmental parameters, characteristic taxa, causal graphs, discussion links, datasets, or record-level evidence. Exact ignored/hidden-inclusive content searches for `habitatmech:GOLD.9c459b5a33`, `gold.ecosystem:6344`, the full GOLD joint-capsule Abscess path, and `abscess__c48fc20f` found no maintained curation, history, research, test, or report artifact outside generated data, `data/raw/gold_ecosystem_paths.tsv`, and `data/habitats/PATHS.tsv`.

Filename searches also found no target-specific maintained overlay or prior report: `find curation/causal_graphs history research/habitats reports/yaml_record_review \( -name '*abscess__c48fc20f*' -o -name '*9c459b5a33*' \) -print` returned no rows before this report was written. Those `find` checks include ignored files beneath the checked directories.

## Findings

None.

## Recommended Edits

None.

No GitHub issue is needed from this YAML review because the generated record is internally valid, reproduced from maintained inputs, and semantically consistent with its raw GOLD path.

## Follow-up Checks

Re-run this focused validation set if future curation changes the GOLD joint-capsule Abscess decision, source-path parent emission, generic Abscess grounding, or path lock:

- `just validate data/habitats/host_associated/abscess__c48fc20f.yaml`
- `just validate-strict data/habitats/host_associated/abscess__c48fc20f.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

## Additional Notes

- The pre-report exact filename check `find reports/yaml_record_review -maxdepth 1 -type f -name '*-abscess__c48fc20f.md' -print` returned no previous exact report; `find` includes ignored files.
- The pre-report exact record check `find data/habitats -name 'abscess__c48fc20f.yaml' -print` found exactly one generated target; `find` includes ignored files.
- Exact ignored/hidden-inclusive searches found no maintained target-specific artifact for `habitatmech:GOLD.9c459b5a33`, `gold.ecosystem:6344`, or `abscess__c48fc20f`.
