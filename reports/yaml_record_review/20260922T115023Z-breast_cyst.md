# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/breast_cyst.yaml`
- Started UTC: 2026-09-22T11:50:23Z
- Finished UTC: 2026-09-22T11:50:23Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.7862616c64` |
| Label | `Breast cyst` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv` plus the class-level `curation/decisions.tsv` row |
| Locked slug | `data/habitats/PATHS.tsv:2176` maps `habitatmech:GOLD.7862616c64` to `breast_cyst` |

This is the GOLD path record for `Host-associated > Mammals: Human > Integumentary system > Mammary gland > Breast cyst`. The generated YAML faithfully retains it as an ungrounded, minted HabitatMech source concept because the maintained decision is still only a class-level sweep row. The leaf now needs item-level curation: the path names a pathological cyst bucket under the Mammary gland GOLD parent, so leaving the source concept class-swept is materially weaker than the evidence in the path itself.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/breast_cyst.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/breast_cyst.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The generated identifier, label, host-associated category, GOLD path parent, path lock, zero-assertion source attestation, and class-level grounding decision are supported by maintained inputs:

- `data/raw/gold_ecosystem_paths.tsv:2271` is the raw GOLD row for this record. Its canonical path is `Host-associated > Mammals: Human > Integumentary system > Mammary gland > Breast cyst`; its leaf label is `Breast cyst`; it is a depth-5 path with one GOLD node id, `gold.ecosystem:7008`, and zero organism, study, biosample, and total assertions.
- `curation/decisions.tsv:716` addresses the same minted source concept, `habitatmech:GOLD.7862616c64`, and records a `CLASS`-depth `CONFIRM_UNGROUNDED` decision by `claude-opus-5` on 2026-08-12.
- `data/habitats/PATHS.tsv:2176` pins `habitatmech:GOLD.7862616c64` to slug `breast_cyst`, matching the reviewed YAML path.
- The parent path `Host-associated > Mammals: Human > Integumentary system > Mammary gland` mints to `habitatmech:GOLD.89cf14e129`; `data/habitats/PATHS.tsv:2315` pins that parent to `mammary_gland__cf1c47fb`.
- `data/raw/gold_ecosystem_paths.tsv:684` is the parent Mammary gland path that feeds `habitatmech:GOLD.89cf14e129` and carries GOLD node ids `gold.ecosystem:6119|gold.ecosystem:6505`.

`grounding_status: UNGROUNDED` and `mapping_status: SEEDED` are reproducible generated output, but the maintained curation row is stale at item scope. A pathological-cyst GOLD leaf should be decided directly instead of left in the class-level lexical-miss bucket.

## Evidence

Every generated claim in this record is source-derived or maintained:

| Claim | Nearest source | Review |
|---|---|---|
| Minted record identifier `habitatmech:GOLD.7862616c64` | `src/habitatmech/seed.py`, `data/raw/gold_ecosystem_paths.tsv:2271` | Supported exactly by `mint("GOLD", canonical_path)` |
| Label `Breast cyst`, host-associated category, GOLD source id, and source path | `data/raw/gold_ecosystem_paths.tsv:2271` | Supported exactly |
| Omitted `assertion_count` and `assertion_unit` | `data/raw/gold_ecosystem_paths.tsv:2271`, `src/habitatmech/seed.py` | Supported; GOLD attestation counts are emitted from nonzero `organism_count`, and this exact path has `organism_count=0` |
| `UNGROUNDED` status and the first curation-history event | `curation/decisions.tsv:716` | Supported exactly |
| `SEEDED` mapping status | `curation/decisions.tsv:716`, `docs/HARMONIZATION.md` | Supported; the decision is class-depth, not item-depth |
| Parent `habitatmech:GOLD.89cf14e129` | GOLD parent-path emission in `src/habitatmech/seed.py`, backed by `data/raw/gold_ecosystem_paths.tsv:684` | Supported exactly |
| Locked filename | `data/habitats/PATHS.tsv:2176` | Supported exactly |

The record has no xrefs, definition, synonyms, environmental parameters, characteristic taxa, record-level evidence, causal graphs, discussion links, assertion count, assertion unit, or datasets. Those absences are consistent with the class-level decision and zero-organism raw GOLD row. Exact side-table lookups found no exact target row in `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_studies.tsv`, or `data/raw/gold_path_triads.tsv`.

## Completeness

The generated slots are faithful to the maintained inputs that feed this record, but one maintained input is consequentially underfilled:

- The sole `source_attestations` entry captures the complete target GOLD path and exact upstream node id.
- No `assertion_count` is emitted because `data/raw/gold_ecosystem_paths.tsv:2271` records 0 organism assertions for the canonical GOLD path.
- The parent list preserves the immediate GOLD path parent, `Mammary gland`, so this sparse leaf remains browsable in its source hierarchy.
- `mapping_status` stays `SEEDED` because the class-level row is a reproducible lexical miss, not an item-level habitat judgement; this is correct generated output, but not complete curation for a pathological GOLD leaf.
- The `Milk` and `Discharge` GOLD rows at `data/raw/gold_ecosystem_paths.tsv:210` and `:2272` are Mammary gland sibling targets with their own locked slugs and do not feed this Breast cyst record.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated target, excluding generated `build`, `data/text_map`, and `pages` trees. They found the maintained target decision, raw GOLD row, path lock, generated YAML, Mammary gland parent record, and Mammary gland siblings; they found no target-specific term request, causal overlay, history record, research report, biosample side-table row, GOLD triad row, GOLD study row, or prior exact YAML review report.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Major | `Breast cyst` remains under a `CLASS`-depth `CONFIRM_UNGROUNDED` row even though the GOLD path is a pathological branch, `Host-associated > Mammals: Human > Integumentary system > Mammary gland > Breast cyst`. The generated `UNGROUNDED` / `SEEDED` state is faithful to the table, but it leaves an obvious cyst-labeled source concept in the lexical-miss bucket instead of deciding whether it is a non-habitat pathology bucket. | Replace `curation/decisions.tsv:716` for `habitatmech:GOLD.7862616c64` with an `ITEM`-depth row, most likely `NOT_APPLICABLE`. |

## Recommended Edits

- Replace the `CLASS`-depth `curation/decisions.tsv` row for `habitatmech:GOLD.7862616c64` with an item-level curation row. The expected disposition is `NOT_APPLICABLE` if the curator confirms the GOLD leaf denotes the breast-cyst pathology bucket rather than a sampled mammary-gland habitat.
- Regenerate from maintained inputs with `just seed` and preview `data/habitats/host_associated/breast_cyst.yaml` with `just seed-canary habitatmech:GOLD.7862616c64` before applying the full generated corpus.

## Follow-up Checks

After replacing the class-level Breast cyst decision, re-run the same focused validation set and confirm the regenerated YAML is `mapping_status: REVIEWED` with a first curation-history event that points at an `ITEM`-depth `curation/decisions.tsv` row:

- `just validate data/habitats/host_associated/breast_cyst.yaml`
- `just validate-strict data/habitats/host_associated/breast_cyst.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

Also verify the GOLD attestation and Mammary gland parent stay attached to the surviving record or its replacement.

## Additional Notes

- `find reports/yaml_record_review -name '*breast_cyst*' -print` found no pre-existing exact review report for this record before this file was written.
- Exact ignored/hidden-inclusive content searches for `Breast cyst`, `breast_cyst`, `gold.ecosystem:7008`, and `habitatmech:GOLD.7862616c64` found the maintained source and decision rows cited above. The same broader GOLD Mammary gland search found sibling Milk and Discharge paths; those rows share the same source parent but do not feed this target.
