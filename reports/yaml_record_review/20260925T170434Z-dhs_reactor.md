# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/dhs_reactor.yaml
- Started UTC: 2026-09-25T17:04:34Z
- Finished UTC: 2026-09-25T17:06:20Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.24cf427e7d` |
| Label | `DHS reactor` |
| Category | `ENGINEERED` |
| Source path | `Engineered > Bioreactor > DHS reactor` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv` and `data/habitats/PATHS.tsv`; future item-level review belongs in `curation/decisions.tsv`, and any retained minted definition belongs in `curation/term_requests.tsv`. |

The target is a GOLD-only, generated, `UNGROUNDED`, still-`SEEDED` record for
the zero-count `Engineered > Bioreactor > DHS reactor` path. The seeder minted
a stable HabitatMech identifier, attached the broader `ENVO:00002123`
`bioreactor` parent, represented three collapsed GOLD ecosystem node IDs with
`source_id: gold.ecosystem:5834`, and preserved the class-level
`CONFIRM_UNGROUNDED` sweep from `curation/decisions.tsv`.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/habitats/engineered/dhs_reactor.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/dhs_reactor.yaml` | Pass: scanned 1 file with 0 files containing `ERROR` and 0 total `ERROR` rows. |
| `just validate-causal curation/causal_graphs/dhs_reactor.yaml` | Not applicable: the target has no `causal_graphs` block, and `find curation/causal_graphs -maxdepth 1 -type f -name 'dhs_reactor.yaml' -print` found no target-specific overlay. |
| `just validate-causal-all` | Pass: validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Pass: the term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: expected 3,206 records, found 3,206 on disk, and the corpus reproduces exactly from `data/raw/`. |
| `just worklist --limit 2000 --status all --out /tmp/habitatmech-dhs-reactor-worklist.tsv` | Pass: reported 953 ungrounded records, 1,810 decisions on file, and wrote 953 rows to `/tmp/habitatmech-dhs-reactor-worklist.tsv`. |
| `just report` | Pass: completed the corpus report for 3,206 records. |
| `git diff --check` | Pass. |
| Reference validator | Not applicable: this generated GOLD-only record has no record-level `evidence`, `characteristic_taxa`, `causal_graphs`, `discussions`, or `datasets` with citation-bearing `EvidenceItem` references. |

## Identity and Grounding

The generated identifier, label, category, source attestation, and slug agree
with the raw GOLD inventory. `data/raw/gold_ecosystem_paths.tsv` has the exact
path `Engineered > Bioreactor > DHS reactor` with `gold_node_count=3`, no
direct organism, study, biosample, or total assertions, and
`gold_node_ids=gold.ecosystem:5834|gold.ecosystem:8432|gold.ecosystem:8433`.
`data/habitats/PATHS.tsv` maps `habitatmech:GOLD.24cf427e7d` to the stable
`dhs_reactor` slug.

The generated `ENVO:00002123` parent is plausible and appropriately broader.
`data/raw/ontology_terms.tsv` labels that term `bioreactor` and defines it as a
biomaterial containment unit. GOLD's own parent path, `Engineered >
Bioreactor`, is exact-grounded to the same ENVO term, so the source tree and
the generated parent both treat `DHS reactor` as a kind of bioreactor.

The `UNGROUNDED` status is only a class-level ontology-match decision, not an
item-level judgement. The `curation/decisions.tsv` row records review depth
`CLASS` and explicitly says that no term in the vendored slice matched the
label by any search route, and that whether the concept is a habitat at all was
not assessed.

## Evidence

The sole `GOLD` source attestation mirrors
`data/raw/gold_ecosystem_paths.tsv`: source ID `gold.ecosystem:5834`, source
label `DHS reactor`, source path `Engineered > Bioreactor > DHS reactor`, and
a note that three GOLD node IDs share the same collapsed path.

Exact gitignore-independent searches for the target source path in
`data/raw/gold_path_triads.tsv`, `data/raw/environment_parameters.tsv`,
`data/raw/gold_studies.tsv`, and `data/raw/gold_path_biosamples.tsv` found no
target-specific side-table rows. The downstream GOLD children `Engineered >
Bioreactor > DHS reactor > Sludge` and `Engineered > Bioreactor > DHS reactor
> Wastewater` do have one study and biosample rows, but those side-table rows
are asserted against the narrower child paths, not against the parent
`Engineered > Bioreactor > DHS reactor` concept.

Unsupported or over-scoped generated claims:

- None beyond the missing item-level curation described below. The generated
  parent is a true broader habitat candidate for a DHS reactor.

## Completeness

The generated record carries the expected source-owned values for the current
raw inputs: a minted identifier, GOLD label, `ENGINEERED` category, inherited
`bioreactor` parent, a single GOLD source attestation, a class-level
`CONFIRM_UNGROUNDED` curation event, a seed event, and a stable slug lock.

The curation state is incomplete. Exact `rg --no-ignore --hidden` searches for
`habitatmech:GOLD.24cf427e7d`, `gold.ecosystem:5834`,
`gold.ecosystem:8432`, `gold.ecosystem:8433`, `DHS reactor`, `downflow`, and
`hanging sponge` under the maintained source and review areas found the class
decision, the raw GOLD row, generated target and child references, and no
item-level decision, `curation/term_requests.tsv` definition, target-specific
research report, or vendored ontology term. Exact `find` searches found no
target-specific causal overlay and no prior `dhs_reactor` YAML review report.
Ignored files were included in all of those absence checks.

Optional characteristic-taxon, environmental-parameter, DOI/PMID/URL evidence,
dataset, discussion, and causal-graph slots are correctly absent for this GOLD
parent in the generated record. The direct GOLD path contributes no organism,
study, biosample, triad, or environmental-parameter rows that could populate
those slots for `dhs_reactor`.

## Findings

- **major**: `habitatmech:GOLD.24cf427e7d` has no item-level review, so its
  source-specific DHS reactor identity remains seeded and undefined rather
  than curated.

  **Evidence**: `curation/decisions.tsv` records only a class-level
  `CONFIRM_UNGROUNDED` row for this identifier, and that row explicitly did not
  assess whether the GOLD concept is a habitat. The target GOLD row is a
  distinct `DHS reactor` child under `Engineered > Bioreactor` with three GOLD
  node IDs, but exact gitignore-independent searches found no item-level
  decision, no term request defining the minted record, no causal overlay, no
  direct GOLD side-table assertions, and no vendored ontology term for `DHS
  reactor` or the likely expanded `downflow hanging sponge` wording.

  **Owner**: `curation/decisions.tsv` should hold the item-level decision; if
  the item-level review keeps the minted identifier, `curation/term_requests.tsv`
  should hold its definition and strict parent.

No blockers found.

No minor findings.

## Recommended Edits

1. In `curation/decisions.tsv`, add an item-level review for
   `habitatmech:GOLD.24cf427e7d` scoped to
   `Engineered > Bioreactor > DHS reactor`, the broader `ENVO:00002123`
   `bioreactor` parent, and the question of whether the source path represents
   a habitat rather than only a reactor device.

2. If item review keeps this as a source-specific bioreactor habitat, define
   `DHS reactor` in `curation/term_requests.tsv` under `ENVO:00002123` or a
   tighter inspected reactor genus.

3. Regenerate the corpus from those maintained inputs so the generated record
   records the curator decision and, if retained, a definition.

## Follow-up Checks

After curation, rerun:

- `just term-requests-check`
- `just validate data/habitats/engineered/dhs_reactor.yaml`
- `just validate-strict data/habitats/engineered/dhs_reactor.yaml`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000 --status all --out /tmp/habitatmech-dhs-reactor-worklist.tsv`

## Additional Notes

- The DHS child records are downstream context, not direct support for this
  parent record. Their raw rows show DHS-specific aerobic, anaerobic, sludge,
  and wastewater subcontexts, but the only child side-table evidence belongs to
  the narrower `Sludge` and `Wastewater` paths.
- A prior review of `data/habitats/engineered/aerobic_bioreactor.yaml` found
  that `Engineered > Bioreactor > DHS reactor > Aerobic` was over-merged into
  generic `ENVO:00002126` `aerobic bioreactor`. That creates a false generated
  child edge from generic aerobic bioreactor to this DHS reactor parent, but it
  is a defect in the child source mapping rather than in
  `dhs_reactor.yaml` itself.
