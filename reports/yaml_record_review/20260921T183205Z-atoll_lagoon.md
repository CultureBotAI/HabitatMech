# YAML Record Review: Atoll lagoon

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/atoll_lagoon.yaml`
- Started UTC: 2026-09-21T18:20:00Z
- Finished UTC: 2026-09-21T18:36:30Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.603ed7e7eb` |
| Label | `Atoll lagoon` |
| Class | `HabitatRecord` |
| Category | `AQUATIC` |
| Generated or maintained | Generated from `data/raw/gold_ecosystem_paths.tsv`, `curation/decisions.tsv`, and `data/habitats/PATHS.tsv`; do not hand-edit |
| Current grounding | `UNGROUNDED` |
| Current mapping | `SEEDED` |

The record resolves the GOLD path `Environmental > Aquatic > Marine > Atoll lagoon`.
The minted identifier suffix is stable for `GOLD:Environmental > Aquatic > Marine > Atoll lagoon`
(`603ed7e7eb`), and `data/habitats/PATHS.tsv` maps that identifier to the
corpus slug `atoll_lagoon`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/atoll_lagoon.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/atoll_lagoon.yaml --quiet` | Pass: 1 file scanned, 0 files with ERROR, 0 total ERROR rows. |
| `just validate-causal-all` | Pass: 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Pass: committed term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: 3,206 expected records, 3,206 found, 0 missing, 0 extra, 0 differing. |

No documented validator was skipped.

## Identity and Grounding

The generated record denotes one minted GOLD source concept:

- `data/raw/gold_ecosystem_paths.tsv` has the exact collapsed GOLD path on line
  1506, with leaf label `Atoll lagoon`, depth 4, two node IDs
  (`gold.ecosystem:7942|gold.ecosystem:7943`), and zero organisms, studies,
  biosamples, or total assertions.
- `source_attestations[0]` carries `source: GOLD`, `source_id:
  gold.ecosystem:7942`, `source_label: Atoll lagoon`, the exact source path,
  and the generated note explaining that two GOLD ecosystem node IDs share the
  path and the first is shown.
- The path's generated source parent is
  `Environmental > Aquatic > Marine`, which resolves to `ENVO:00001999`
  `marine water body`; that is a true broader parent, not an identity claim.

The current `curation/decisions.tsv` row is only the 2026-08-12 class-level
`CONFIRM_UNGROUNDED` sweep. Hidden- and ignored-file-inclusive exact searches of
`data/raw`, `data/habitats/PATHS.tsv`, `curation`, `history`,
`curation/causal_graphs`, and `reports/yaml_record_review` found no item-level
decision, no target-specific term request, no exclusion row, no external xref,
no history record, no causal overlay, and no prior review for
`habitatmech:GOLD.603ed7e7eb`, `atoll_lagoon`, the exact GOLD path, or the
record label.

The vendored ontology slice contains nearby but non-exact terms:

- `ENVO:00000038` `lagoon` is broader than this source concept; its definition
  includes "The sheet of water within a ring or horseshoe shaped atoll."
- `ENVO:00000166` `atoll` is an island/reef landform surrounding a lagoon, not
  the lagoon water body itself.
- `ENVO:00001999` `marine water body` is the currently inherited GOLD parent
  and is broader than both `Atoll lagoon` and `lagoon`.

## Evidence

The record has no curator-authored `definition`, record-level `evidence`,
`environmental_parameters`, `characteristic_taxa`, `causal_graphs`,
`discussions`, or `datasets`.

The only positive provenance is the GOLD source-attestation row. Exact
ignored/hidden-inclusive searches found no Atoll lagoon row in
`data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, or
`data/raw/gold_studies.tsv`, which agrees with the collapsed GOLD row's zero
biosample/study/assertion counts. No MIxS triad, sample, study, taxon, or
literature claim is silently dropped from the generated YAML for this path.

## Completeness

The record is complete enough as a generated seed: it records the GOLD path, a
strictly broader GOLD-derived parent, and a curation-history entry for the class
sweep. It is not complete enough as a reviewed HabitatMech habitat:

- the only curation decision is `review_depth: CLASS`, whose documented meaning
  is that no vendored label/synonym match was found and habitathood was not
  assessed;
- no maintained row records the narrower `lagoon` parent that is visible in the
  vendored ENVO definition; and
- no maintained definition row states what differentiates an atoll lagoon from
  the already grounded broader `ENVO:00000038` `lagoon` record.

The empty optional slots are correct for the current maintained inputs. There
is no source-owned GOLD triad, biosample, or study row for the exact path, and
no target-owned causal graph to validate beyond the full-overlay corpus check.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| HM-ATOLL-LAGOON-001 | Major | `Atoll lagoon` is still represented by a class-sweep `CONFIRM_UNGROUNDED` decision, so `mapping_status: SEEDED` is expected and no curator has yet assessed the actual GOLD path. The vendored slice already supplies `ENVO:00000038` `lagoon` as the relevant broader genus and `ENVO:00000166` `atoll` as a near miss to reject, so this needs item-level review rather than another class-level sweep. | `curation/decisions.tsv` |
| HM-ATOLL-LAGOON-002 | Major | The minted record has no HabitatMech-authored definition or term-request row. If item-level review confirms GOLD's concept as the narrower lagoon within an atoll, the corpus needs a maintained `curation/term_requests.tsv` definition under `ENVO:00000038` so the generated record can explain the distinction from generic `lagoon`. | `curation/term_requests.tsv` |

No blocker findings.

No minor findings.

## Recommended Edits

1. Replace the `CLASS`-depth row for `habitatmech:GOLD.603ed7e7eb` in
   `curation/decisions.tsv` with an `ITEM`-depth decision after inspecting the
   exact GOLD concept. If the current evidence holds, keep it minted with
   `CONFIRM_UNGROUNDED` and document that `ENVO:00000038` `lagoon` is a broader
   genus while `ENVO:00000166` `atoll` denotes the surrounding island/reef
   landform rather than the water body.
2. Add a `curation/term_requests.tsv` row for
   `habitatmech:GOLD.603ed7e7eb` with requested label `atoll lagoon`, parent
   `ENVO:00000038` / `lagoon`, and `parent_mode=ADD` so the generated record
   retains the true source-derived `ENVO:00001999` marine-water-body parent
   while gaining the tighter lagoon genus.
3. Run the dry-run `just seed`, then regenerate the existing Atoll lagoon file
   with `just seed-canary habitatmech:GOLD.603ed7e7eb --force` and inspect the
   generated target before any wider `just seed-apply --force` run.

## Follow-up Checks

- `just validate data/habitats/aquatic/atoll_lagoon.yaml`
- `just seed-canary habitatmech:GOLD.603ed7e7eb --force`
- `just validate-strict data/habitats/aquatic/atoll_lagoon.yaml --quiet`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

After regeneration, re-read `data/habitats/aquatic/atoll_lagoon.yaml` and
confirm that the record is still `UNGROUNDED`, is now `REVIEWED`, contains the
authored definition and the `ENVO:00000038` parent, preserves
`ENVO:00001999`, and records a curation-history event for the item-level
decision and term request.

## Additional Notes

This was a read-only YAML review. It did not edit generated habitat YAML, append
curation history, promote the review status, create an issue, or run paid
definition research.

The generated HTML page was not used as evidence; all assertions above were
traced to maintained TSV inputs, the vendored ontology slice, or the generated
target YAML.
