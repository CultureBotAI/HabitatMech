# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/waste__ae37fc5e.yaml
- Started UTC: 2026-09-26T08:20:36Z
- Finished UTC: 2026-09-26T08:26:17Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.cea7ead8dd` |
| Label | `Waste` |
| Class | `HabitatRecord` |
| Category | `AQUATIC` |
| Generated path | `data/habitats/aquatic/waste__ae37fc5e.yaml` |
| Maintained path lock | `data/habitats/PATHS.tsv:2811` maps `habitatmech:GOLD.cea7ead8dd` to `waste__ae37fc5e` |
| Source concept | GOLD `gold.ecosystem:8417` / `gold.ecosystem:8420` |
| Source path | `Environmental > Aquatic > Marine > Waste` |
| Grounding | `UNGROUNDED` |
| Mapping | `SEEDED` |

The reviewed file is a generated GOLD-only record. It is not a maintained
curation input and should not be patched directly.

## Validation

| Command | Result |
|---|---|
| `just worklist --limit 0 --status all --out /tmp/habitatmech-worklist.tsv` | Passed; wrote 953 rows and placed `habitatmech:GOLD.cea7ead8dd` immediately after `Untreated`. |
| `just validate data/habitats/aquatic/waste__ae37fc5e.yaml` | Passed with `No issues found`. |
| `just validate-strict data/habitats/aquatic/waste__ae37fc5e.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 expected records, 3206 found, 0 missing, 0 extra, 0 differing. |
| `just report` | Passed; no stale class-level sweep, contradictory path term, or non-habitat label contradiction was reported for this record. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

The generated identifier, label, category, and source attestation agree with the
GOLD source inventory:

| Claim | Evidence | Assessment |
|---|---|---|
| The target is the GOLD `Waste` node under the marine path. | `data/raw/gold_ecosystem_paths.tsv:640` gives the canonical path `Environmental > Aquatic > Marine > Waste`, leaf label `Waste`, depth `4`, and source nodes `gold.ecosystem:8417|gold.ecosystem:8420`. | Supported. The path, not the bare label, is the identity context. |
| The generated source attestation uses the first collapsed GOLD node ID and records the duplicate-node note. | The same raw row lists two GOLD ecosystem node IDs; `data/habitats/aquatic/waste__ae37fc5e.yaml` emits `source_id: gold.ecosystem:8417` and says two GOLD ecosystem node IDs share the path. | Supported. |
| The direct GOLD row has five organism-level assertions. | `data/raw/gold_ecosystem_paths.tsv:640` has `organism_count=5`, `study_count=0`, `biosample_count=0`, and `total_assertions=5`. | Supported; the count is a GOLD `ORGANISM` aggregate and is not study or biosample evidence. |
| The only generated parent is the source-path parent `Marine`. | The target has `parent_habitats: ENVO:00001999`; `data/habitats/aquatic/marine_water_body.yaml` is the reviewed merged record for `Environmental > Aquatic > Marine` and ontology `marine water body`. | Supported as a GOLD path broader edge. Incomplete as a genus: the record also denotes a kind of waste. |
| The source concept is a waste-material habitat rather than liquid water, glue, or anatomy. | The path places `Waste` beside other marine places/materials and above `Plastic` and `Rubber`; `data/raw/ontology_terms.tsv` shows worklist candidates `ENVO:00002006` `liquid water`, `ENVO:00003076` `glue`, and `UBERON:0000945` `stomach`, which match only weak synonyms or substrings. | Supported negatively for the lexical candidates. `ENVO:00002264` `waste material` is the useful broader term but is not yet attached to this record. |
| `UNGROUNDED` is currently explained only by a class-level no-match sweep. | `curation/decisions.tsv:1140` has `CONFIRM_UNGROUNDED` with `review_depth` `CLASS`; the generated curation history repeats that no ontology term fit by any lexical route and that habitat status was not assessed. | Reproducible but incomplete. `CLASS` depth does not establish that this GOLD leaf denotes a real marine-waste microbial habitat or that no manual broader parent or novel term should be attached. |

A bounded ignored/hidden-inclusive search of `data/raw/`,
`curation/decisions.tsv`, `curation/term_requests.tsv`,
`curation/term_requests_excluded.tsv`, `curation/causal_graphs/`, `history/`,
`research/`, `reports/yaml_record_review/`, `data/habitats/PATHS.tsv`,
`data/habitats/aquatic/`, and `pages/habitats/` for
`habitatmech:GOLD.cea7ead8dd`, `gold.ecosystem:8417`,
`gold.ecosystem:8420`, `Environmental > Aquatic > Marine > Waste`, and
`waste__ae37fc5e` found the expected generated record, page, path lock, raw
GOLD aggregate row, class-level decision row, and `Plastic` and `Rubber` child
records that point at this parent. It found no target-specific term request,
term-request exclusion, causal overlay, append-only history entry, exact-path
GOLD study row, advisory sample row, or prior YAML review.

## Evidence

| Record claim | Nearest source | Assessment |
|---|---|---|
| `source: GOLD`, `source_id: gold.ecosystem:8417`, `source_label: Waste`, and the full GOLD path | `data/raw/gold_ecosystem_paths.tsv:640` | Supported exactly. |
| The two-GOLD-node collapse note | `data/raw/gold_ecosystem_paths.tsv:640` lists `gold.ecosystem:8417|gold.ecosystem:8420`. | Supported exactly. |
| `assertion_count: 5`, `assertion_unit: ORGANISM` | `data/raw/gold_ecosystem_paths.tsv:640` records 5 organism assertions. | Supported exactly. |
| Parent `ENVO:00001999` | The GOLD path places `Waste` below `Environmental > Aquatic > Marine`; `data/raw/gold_ecosystem_paths.tsv:8` records that parent path as `Marine`, with nodes `gold.ecosystem:3487|gold.ecosystem:3768|gold.ecosystem:4003`. | Supported as a source-path broader edge. |
| Absence of characteristic taxa, environmental parameters, record evidence, and causal graphs | Exact ignored/hidden-inclusive searches across `data/raw/`, `curation/causal_graphs/`, and the generated YAML found no exact-path GOLD triad/study row and no curated causal overlay. | Supported. GOLD organism aggregates are not characteristic-taxon claims, and no curated causal overlay exists. |

`ENVO:00002264` `waste material` is present in the vendored ontology slice at
`data/raw/ontology_terms.tsv:7340` with a definition broad enough to cover
discarded marine materials. The record does not include that term as a parent
because no item-level decision or term request exists for this GOLD source
concept. The more specific `marine rubber waste` child has already been defined
under `ENVO:00002264` while preserving this `Waste` record as its GOLD
source-path parent, which is the parent shape this broader record also needs.

## Completeness

The generated record preserves the existing raw GOLD aggregate row and the
class-level curation decision, and it correctly avoids unsupported
characteristic taxa, environmental parameters, record evidence, and causal graph
claims.

It is incomplete as an item-reviewed habitat. The 2026-08-12 class-level
decision only proves that no vendored term matched by the sweep's lexical
routes, and the direct GOLD row has enough path context to identify the concept
as marine waste rather than undifferentiated waste, liquid water, paste/glue, or
an anatomical `gaster` false match. A curator still needs to inspect
`gold.ecosystem:8417` and `gold.ecosystem:8420`, confirm whether the five
organism assertions all belong to marine waste material, add a strict
waste-material genus if they do, and author a label and definition that carry
the marine qualifier.

The same-label BacDive record `habitatmech:BACDIVE.36df85e4a4` is not an
automatic merge target. Its item-level decision records BacDive `Waste` as an
engineered bin spanning solid, liquid, and industrial wastes, while this GOLD
record is a marine aquatic branch with `Plastic` and `Rubber` children.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| HM-WASTE-001 | Major | `Waste` is still backed only by `review_depth` `CLASS`. The generated `UNGROUNDED` and `SEEDED` statuses are reproducible, but the maintained row explicitly says no one assessed whether the concept is a habitat. The GOLD path supports the narrower reading `marine waste material`, not the bare label's generic `waste`, and no target-specific row currently confirms that identity or asks ENVO for a novel term. | `curation/decisions.tsv`; if a novel term is requested, `curation/term_requests.tsv` |
| HM-WASTE-002 | Major | The record lacks `ENVO:00002264` `waste material` as a strict broader parent even though the GOLD path denotes a waste branch under `Marine` and its item-reviewed `Rubber` child already uses `ENVO:00002264` as the genus for marine rubber waste. The current sole parent, `ENVO:00001999` `marine water body`, preserves the source location but not the discarded-material genus. | `curation/decisions.tsv` or `curation/term_requests.tsv`, then regenerated `data/habitats/aquatic/waste__ae37fc5e.yaml` |

## Recommended Edits

1. Item-review `habitatmech:GOLD.cea7ead8dd` in
   `curation/decisions.tsv`, replacing the class-level row with an item-level
   decision.
2. Inspect GOLD nodes `gold.ecosystem:8417` and `gold.ecosystem:8420`,
   including the five organism assertions behind the aggregate row, to confirm
   whether both collapsed nodes denote marine waste material.
3. If the GOLD nodes both mean marine waste material, keep the minted identity,
   add an item-level `CONFIRM_UNGROUNDED`, and request a novel term such as
   `marine waste material` in `curation/term_requests.tsv` with
   `ENVO:00002264` as the genus and `parent_mode` `ADD` so the GOLD Marine
   parent is retained.
4. If the collapsed GOLD source concept as a whole duplicates `Plastic`,
   `Rubber`, or the generic BacDive `Waste` bin, use item-level `SAME_AS` only
   after inspecting the upstream assertions. If just one of the two raw GOLD
   ecosystem nodes is misfiled or duplicate, first split or correct the source
   inventory/extractor so each upstream node lands in the right source concept.
   Do not merge the GOLD marine waste branch with BacDive `Waste` solely because
   their bare labels match.
5. If the aggregate is a heterogeneous bucket rather than a habitat class,
   replace the class-level row with an item-level `NOT_APPLICABLE` decision
   that records that exact reading.

Do not hand-edit `data/habitats/aquatic/waste__ae37fc5e.yaml`; regenerate it
from the maintained TSVs.

## Follow-up Checks

After adding item-level curation, rerun:

- `just seed`
- `just seed-canary habitatmech:GOLD.cea7ead8dd`
- inspect `data/habitats/aquatic/waste__ae37fc5e.yaml`
- `just validate-strict data/habitats/aquatic/waste__ae37fc5e.yaml`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just report`

If a causal overlay is added later, also run
`just validate-causal curation/causal_graphs/waste__ae37fc5e.yaml` and
`just validate-causal-all`.

## Additional Notes

The GOLD `Plastic` child is still seeded and has not had the same item-level
review as `marine rubber waste`. Its generated parentage, like this record's,
should be rechecked when the marine waste branch is curated so the source-path
hierarchy and `waste material` genus stay aligned across all three records.
