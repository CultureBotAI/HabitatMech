# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/sediment__5aa3b4f2.yaml
- Started UTC: 2026-09-24T18:30:03Z
- Finished UTC: 2026-09-24T18:32:17Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.601cb8619f` |
| Label | `Sediment` |
| Category | `ENGINEERED` |
| Grounding | `NARROW` |
| Mapping status | `SEEDED` |
| Maintained source | generated from `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/PATHS.tsv`, and the Fish pond parent row in `curation/decisions.tsv` |

Reviewed the complete generated record at `data/habitats/engineered/sediment__5aa3b4f2.yaml`. The record is a GOLD-only habitat for `Engineered > Artificial ecosystem > Aquaculture > Fish pond > Sediment`, with one `gold.ecosystem:8004` source attestation and one GOLD organism assertion. The file itself is generated output; the source concept is minted from GOLD's exact source path and any future source-path parent fix belongs in the maintained parent decision row for `habitatmech:GOLD.f0461d363e`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/sediment__5aa3b4f2.yaml` | Passed |
| `just validate-strict data/habitats/engineered/sediment__5aa3b4f2.yaml` | Passed |
| Focused causal overlay validator | Not applicable; exact ignored-inclusive searches in `curation/causal_graphs/` found no `habitatmech:GOLD.601cb8619f` or exact Fish pond sediment source-path overlay |
| `just validate-causal-all` | Passed; 32 files / 32 graphs |
| Reference validator | Not applicable; the generated GOLD-only record has no record-level `evidence` items, no causal graph, and no environmental-parameter references |
| `just term-requests-check` | Passed; 109 terms |
| `just validate-history` | Passed; 77 history records |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected / 3,206 found / 0 missing / 0 extra / 0 differing |
| `just worklist --limit 2000` | Passed; 0 ungrounded concepts remain undecided and 1,810 decisions are present |
| `just report` | Passed; 3,206 habitat records summarized |
| `git diff --check` | Passed |

## Identity and Grounding

The generated identity, category, and source attestation agree with the committed GOLD inventory row. `data/raw/gold_ecosystem_paths.tsv` has a single collapsed Fish pond sediment path with leaf `Sediment`, GOLD level count `5`, one organism assertion, and one GOLD node ID, `gold.ecosystem:8004`; those values match the generated `source_attestations` entry.

The record's own `NARROW` mapping to `ENVO:00002007` `sediment` is consistent with the GOLD path: fish-pond sediment is a narrower engineered sediment setting, so the ambiguous GOLD leaf keeps a minted identity and carries generic `sediment` as a broader `parent_habitats` value rather than merging with every other GOLD path ending in `Sediment`.

The second generated parent, `habitatmech:GOLD.f0461d363e`, is weaker. It points to the direct GOLD source-path parent `Engineered > Artificial ecosystem > Aquaculture > Fish pond`; that parent is still `UNGROUNDED` under a 2026-08-12 class-level `CONFIRM_UNGROUNDED` decision whose note explicitly says habitat identity was not assessed. The local vendored slice contains `ENVO:00000056` `fishpond`, and the Fish pond review report found no item-level decision or term request that compares that candidate against the source path.

## Evidence

- Supported: the `Sediment` source path, `gold.ecosystem:8004` source ID, and one-organism assertion count match `data/raw/gold_ecosystem_paths.tsv`.
- Supported: `ENVO:00002007` `sediment` is present in `data/raw/ontology_terms.tsv`, so the parent is a valid local ontology term.
- Supported: no direct GOLD biosample, MIxS triad, or study side table adds source-path-specific claims for `gold.ecosystem:8004` or `Engineered > Artificial ecosystem > Aquaculture > Fish pond > Sediment`; exact ignored-inclusive searches found only the canonical `gold_ecosystem_paths.tsv` row.
- Under-reviewed: the inherited Fish pond parent has not been item-reviewed against `ENVO:00000056` `fishpond`, `ENVO:00000294` `fish farm`, or `ENVO:03600074` `aquaculture farm`, so the child may need a regenerated ENVO parent rather than the current minted Fish pond parent.
- No record-level citations, characteristic taxa, environmental parameters, or causal edges are present, so there are no attached snippets or literature references to validate.

## Completeness

The record is structurally sparse but not missing any required generated field. It has the expected GOLD source attestation, source-path parent, and broader sediment parent for a path-specific sediment source concept with one organism assertion and no committed GOLD biosample, triad, or study rows.

Bounded ignored-inclusive searches were run over `curation`, `history`, `research`, `reports/yaml_record_review`, `conf/id_label_targets.yaml`, `data/habitats/PATHS.tsv`, `data/habitats/engineered/sediment__5aa3b4f2.yaml`, and the focused GOLD raw side tables for `habitatmech:GOLD.601cb8619f`, `gold.ecosystem:8004`, and `Engineered > Artificial ecosystem > Aquaculture > Fish pond > Sediment`. They found the generated target, its `data/habitats/PATHS.tsv` row, the canonical GOLD ecosystem-path row, and the prior Fish pond review's follow-up references to this child; they found no existing review report for this exact stem before this report was written and no item-level decision, term request, history entry, research report, label-correspondence exception, exact auxiliary GOLD biosample/triad/study row, or target-specific causal overlay.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `sediment__5aa3b4f2.yaml` inherits the under-reviewed minted Fish pond parent, so the generated hierarchy may need to point at `ENVO:00000056` `fishpond` after item-level Fish pond review. | The generated child lists `habitatmech:GOLD.f0461d363e` as a `parent_habitats` value. The only maintained decision for that parent is a class-level `CONFIRM_UNGROUNDED` row, while `data/raw/ontology_terms.tsv` contains the likely exact `ENVO:00000056` `fishpond` term and the prior Fish pond review found no item-level parent decision. | `curation/decisions.tsv` |

## Recommended Edits

1. Item-review `habitatmech:GOLD.f0461d363e` in `curation/decisions.tsv` against the exact `Engineered > Artificial ecosystem > Aquaculture > Fish pond` path and the local candidates `ENVO:00000056` `fishpond`, `ENVO:00000294` `fish farm`, and `ENVO:03600074` `aquaculture farm`.
2. Regenerate both Fish pond and this Fish pond sediment child with a forced bounded canary so the child record's inherited parent is rewritten before inspection.
3. Inspect `data/habitats/engineered/sediment__5aa3b4f2.yaml` or its regenerated successor path from `data/habitats/PATHS.tsv` and confirm its `parent_habitats` values contain `ENVO:00002007` plus the reviewed Fish pond identity.

## Follow-up Checks

- `just seed`
- `uv run python scripts/seed_from_sources.py --apply --force --only ENVO:00000056 habitatmech:GOLD.601cb8619f` if the Fish pond decision exactly grounds to `ENVO:00000056`; otherwise `uv run python scripts/seed_from_sources.py --apply --force --only habitatmech:GOLD.f0461d363e habitatmech:GOLD.601cb8619f`
- Inspect the regenerated Fish pond record and the regenerated Fish pond `Sediment` child.
- `just seed-apply --force --prune` if exact grounding changes the Fish pond path or leaves the old minted file stale; otherwise `just seed-apply --force`
- `just validate <regenerated Fish pond Sediment path>`
- `just validate-strict <regenerated Fish pond Sediment path>`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

## Additional Notes

- No blocker finding was found: the current generated YAML validates, its GOLD source attestation is internally consistent, and the record denotes engineered fish-pond sediment rather than a non-habitat.
- No minor findings were found.
