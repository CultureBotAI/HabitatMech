# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/sediment__bcc5249a.yaml
- Started UTC: 2026-09-25T12:26:41Z
- Finished UTC: 2026-09-25T12:26:41Z
- Verdict: needs curation

## Target

Reviewed the complete generated record at `data/habitats/engineered/sediment__bcc5249a.yaml`. The record is the GOLD-only engineered child for `Engineered > Artificial ecosystem > Vivarium > Seawater aquarium > Sediment`, currently carried as the minted identifier `habitatmech:GOLD.085f791375`.

The generated file has `label: Sediment`, `habitat_category: ENGINEERED`, `grounding_status: NARROW`, and `mapping_status: SEEDED`. Its generated parents are `ENVO:00002007` `sediment`, from the repeated GOLD leaf match, and `habitatmech:GOLD.c483f43031` `Seawater aquarium`, from the immediate GOLD source-path parent. Its only source attestation is `gold.ecosystem:8038` with `mapping_predicate: skos:narrowMatch`.

The file is generated from `data/raw/` and seeded path logic. No `curation/decisions.tsv` row, authored term request, source extractor override, or causal-graph overlay currently contributes to this record. Future item review belongs in `curation/decisions.tsv`; a future path-qualified label, definition, or parent replacement belongs in `curation/term_requests.tsv`; future causal overlays belong under `curation/causal_graphs/`.

## Validation

All required local checks passed:

- `just validate data/habitats/engineered/sediment__bcc5249a.yaml`
- `just validate-strict data/habitats/engineered/sediment__bcc5249a.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000 --status all --out /tmp/habitatmech-seawater-aquarium-sediment-worklist.tsv`
- `just report`

No validator was skipped.

## Identity and Grounding

The generated identifier, label, category, source attestation, and path slug agree with the maintained inputs. `data/raw/gold_ecosystem_paths.tsv` has the exact `Engineered > Artificial ecosystem > Vivarium > Seawater aquarium > Sediment` row at depth 5, with one GOLD node ID, zero `organism_count`, `study_count`, `biosample_count`, and `total_assertions` values in this bulk-export summary, and `gold.ecosystem:8038` as the node list. `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.085f791375` to the stable `sediment__bcc5249a` slug.

The `ENVO:00002007` parent is a plausible broader term. The vendored ontology slice defines `ENVO:00002007` with canonical label `sediment`, and the GOLD source path adds a Seawater aquarium context under that generic leaf. `docs/HARMONIZATION.md` documents that only the shallowest GOLD path may claim a repeated `Sediment` leaf exactly; deeper path-specific sediment records keep minted identifiers and the matched ontology term as a broader parent.

The inherited `habitatmech:GOLD.c483f43031` Seawater aquarium parent is not yet defensible as a strict `is-a` parent. The raw row places the Sediment leaf under Seawater aquarium, but seawater-aquarium sediment is accumulated material within that engineered aquarium context, not a kind of aquarium. This is the same path-qualified Sediment pattern that earlier fish- and crustacean-system Sediment reviews flagged: the generic `ENVO:00002007` parent is the material genus, while the inherited tank, pond, raceway, pen, or aquarium source path should be replaced if item review confirms it is contextual.

This target has no item-level decision. Exact ignored-inclusive searches found no `habitatmech:GOLD.085f791375`, `gold.ecosystem:8038`, `source_id: gold.ecosystem:8038`, or exact path-specific term request in maintained curation inputs.

## Evidence

- Supported as generated source provenance: exact ignored-inclusive searches found `gold.ecosystem:8038` and the exact tab-delimited `Engineered > Artificial ecosystem > Vivarium > Seawater aquarium > Sediment` row in `data/raw/gold_ecosystem_paths.tsv`.
- Supported as broader ontology context: `ENVO:00002007` is present in the vendored ontology slice as `sediment`, and a path-qualified GOLD Sediment child is narrower than the generic ontology class.
- Supported as generated context but not as a strict parent: `habitatmech:GOLD.c483f43031` is the immediate parent source path, which explains why it is generated, but the GOLD containment/context path has not been item-reviewed into a true `parent_habitats` claim.
- Not yet supported as individually reviewed habitat identity: no item-level `curation/decisions.tsv` row decides whether this source concept should stay as a specific seawater-aquarium sediment, become a different exact ontology grounding, or be handled some other way.

The record has no definition, environmental parameters, characteristic taxa, claim-level `EvidenceItem` objects, causal graphs, discussions, or datasets to inspect.

## Completeness

The generated source attestation is complete for the committed GOLD ecosystem inventory. `data/raw/gold_ecosystem_paths.tsv` lists a single source node ID for this exact path, so no generated note is needed to account for collapsed sibling nodes.

Exact ignored-inclusive searches of the GOLD API side tables found no `gold.ecosystem:8038` and no exact tab-delimited `Engineered > Artificial ecosystem > Vivarium > Seawater aquarium > Sediment` row in `data/raw/gold_studies.tsv`, `data/raw/gold_path_biosamples.tsv`, or `data/raw/gold_path_triads.tsv`; no GOLD study, biosample, or MIxS triad context is committed for this exact path.

Before this report was written, exact ignored-inclusive searches over `history/`, `research/habitats/`, `curation/causal_graphs/`, `curation/term_requests.tsv`, `curation/term_requests/`, `curation/term_requests_excluded.tsv`, `conf/id_label_targets.yaml`, and `reports/yaml_record_review/` found no target-specific append-only history record, research report, causal overlay, authored term request, generated ENVO term-request row, term-request exclusion, or label-correspondence residual for `habitatmech:GOLD.085f791375`, `gold.ecosystem:8038`, or the exact `Seawater aquarium > Sediment` path fragment. Before this report was written, `find reports/yaml_record_review -maxdepth 1 -type f -name '*sediment__bcc5249a*.md' -print` and `find reports/yaml_record_review -maxdepth 1 -type f -iname '*seawater*sediment*.md' -print` found no prior review report for this exact generated stem or label context.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `Engineered > Artificial ecosystem > Vivarium > Seawater aquarium > Sediment` is still only a seeded repeated-leaf child, and its generated `habitatmech:GOLD.c483f43031` Seawater aquarium parent is likely a contextual container rather than a true `is-a` parent. | `data/raw/gold_ecosystem_paths.tsv` has this source concept as the `Sediment` child of `Engineered > Artificial ecosystem > Vivarium > Seawater aquarium`; `curation/decisions.tsv` has no item-level row for `habitatmech:GOLD.085f791375`; prior Sediment reviews for fish tanks, fish pens, crustacean ponds, crustacean raceways, and crustacean tanks all recommend an `ENVO:00002007` sediment genus plus `parent_mode=REPLACE` for inherited aquatic-system contexts. | `curation/decisions.tsv` and `curation/term_requests.tsv`. |

## Recommended Edits

1. Item-review `habitatmech:GOLD.085f791375` against the exact `Engineered > Artificial ecosystem > Vivarium > Seawater aquarium > Sediment` GOLD path, `ENVO:00002007` `sediment`, and the immediate `habitatmech:GOLD.c483f43031` Seawater aquarium context.
2. If the source concept is seawater-aquarium sediment, add an `ITEM` `CONFIRM_UNGROUNDED` decision in `curation/decisions.tsv` for `habitatmech:GOLD.085f791375`, because no exact term in the vendored slice currently names the path-qualified child.
3. In that same future curation session, add a `curation/term_requests.tsv` row that defines the record under `ENVO:00002007` `sediment`; use `parent_mode=REPLACE` if item review confirms the generated Seawater aquarium parent is contextual.
4. Add an append-only `history/` record for that future curation session.

## Follow-up Checks

- After updating `curation/decisions.tsv` and `curation/term_requests.tsv`, run `just seed`, `just seed-canary habitatmech:GOLD.085f791375`, and inspect the regenerated `data/habitats/engineered/sediment__bcc5249a.yaml`.
- If the canary is correct, run `just seed-apply --force`, then `just validate-strict data/habitats/engineered/sediment__bcc5249a.yaml`, `just term-requests-check`, `just validate-history`, `just validate-causal-all`, `just verify-corpus --max-diffs 1`, and `just report`.
- If the future item review adds a causal overlay, run the focused `just validate-causal curation/causal_graphs/<slug>.yaml` before `just validate-causal-all`.

## Additional Notes

All absence checks in this review used ignored-inclusive `rg --no-ignore --hidden`, exact scans of the committed raw GOLD side tables, or `find`, bounded to maintained curation, history, research, raw inventory, generated habitat, label-residual, and review-report paths. No search depended on rendered `pages/` or text-map JSON.

The immediate Seawater aquarium parent has already been reviewed with a `needs curation` verdict in `reports/yaml_record_review/20260925T114550Z-seawater_aquarium.md`. Future curation of that parent should not block item review of this child: whether Seawater aquarium later grounds to `ENVO:00002197` `saline water aquarium` or remains minted, the generated child still needs a strict-parent check of the Sediment-to-aquarium edge.
