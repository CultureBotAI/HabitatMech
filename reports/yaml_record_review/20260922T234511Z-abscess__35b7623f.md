# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/abscess__35b7623f.yaml`
- Started UTC: 2026-09-22T23:29:00Z
- Finished UTC: 2026-09-22T23:45:11Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/host_associated/abscess__35b7623f.yaml` |
| Class | `HabitatRecord` |
| Identifier | `mesh:D000038` |
| Label | `Abscess` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Record status | Generated from one GOLD source concept with no item-level curation decision |

This is the generated record for the GOLD path `Host-associated > Mammals: Human > Abdominal/Peritoneal cavity > Abscess`. The source-concept key is `habitatmech:GOLD.9c78e65e82`, computed from `sha1("GOLD:Host-associated > Mammals: Human > Abdominal/Peritoneal cavity > Abscess")[:10]`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/abscess__35b7623f.yaml` | Passed; LinkML validation reported no issues. |
| `just validate-strict data/habitats/host_associated/abscess__35b7623f.yaml` | Passed; one file scanned with 0 error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 generated records, found 3206, with 0 missing, 0 extra, and 0 differing. |
| `just worklist` filtered for `Abscess`, `abscess`, `GOLD\.9c78e65e82`, and `mesh:D000038` | Completed with no matching rows, as expected for a seeded exact GOLD grounding rather than a class-level ungrounded source concept. |
| `just report` | Passed; the record was not listed in a blocking corpus-wide diagnostic. |
| `git diff --check` | Passed after the review report was written. |

## Identity and Grounding

The GOLD source identity is supported:

| Raw field | Value |
|---|---|
| `canonical_path` | `Host-associated > Mammals: Human > Abdominal/Peritoneal cavity > Abscess` |
| `ecosystem` | `Host-associated` |
| `ecosystem_category` | `Mammals: Human` |
| `ecosystem_type` | `Abdominal/Peritoneal cavity` |
| `ecosystem_subtype` | `Abscess` |
| `specific_ecosystem` | empty |
| `leaf_label` | `Abscess` |
| `depth` | `4` |
| `gold_node_count` | `2` |
| `organism_count` | `4` |
| `study_count` | `0` |
| `biosample_count` | `0` |
| `total_assertions` | `4` |
| `gold_node_ids` | `gold.ecosystem:6346|gold.ecosystem:6347` |

`source_attestations` matches `data/raw/gold_ecosystem_paths.tsv:679`: the record keeps `gold.ecosystem:6346` as the first source id, records the full abdominal/peritoneal Abscess source path exactly, records `assertion_count: 4` with `assertion_unit: ORGANISM`, and records the multi-node note because two GOLD ecosystem node ids share the path.

The exact `mesh:D000038` identity is not supported. `mesh:D000038` labels the generic MeSH `Abscess` term; the sole active generated source attestation is narrower because GOLD places the leaf under `Abdominal/Peritoneal cavity`. The only committed BacDive line that maps `Abscess` to `mesh:D000038` is in `data/raw/isolation_source_groundings.tsv`; ignored/hidden-inclusive searches of `data/raw/bacdive_isolation_sources.tsv` and `data/raw/bacdive_source_taxa.tsv` found no active `Abscess` BacDive isolation-source row, and this generated record has no BacDive attestation.

The current `BTO:0001782` parent is valid for the abdominal/peritoneal GOLD source path but false for a generic `mesh:D000038` Abscess record. `BTO:0001782` is the reviewed record for GOLD's immediate parent path `Host-associated > Mammals: Human > Abdominal/Peritoneal cavity`, and `mesh:D013492` is the ontology parent of `mesh:D000038` in `data/raw/ontology_subclass_edges.tsv`. After an exact merge into the generic MeSH term, however, the output asserts that every `Abscess` is a kind of `peritoneal cavity`.

The source concept has no maintained override. Ignored/hidden-inclusive exact searches for `habitatmech:GOLD.9c78e65e82`, `gold.ecosystem:6346`, `gold.ecosystem:6347`, and the full GOLD source path found no row in `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/term_requests_excluded.tsv`, `curation/causal_graphs`, `history`, or `research/habitats`; broad generated `data/text_map`, `pages`, and `build` outputs were excluded.

## Evidence

| Claim | Nearest support | Review |
|---|---|---|
| The GOLD row represents `Host-associated > Mammals: Human > Abdominal/Peritoneal cavity > Abscess`. | `data/raw/gold_ecosystem_paths.tsv:679` | Supported exactly. |
| The source row contributes 4 organism assertions collapsed from two GOLD ecosystem node ids. | `data/raw/gold_ecosystem_paths.tsv:679` | Supported exactly. |
| The generated source attestation should use `gold.ecosystem:6346` as its representative id and note the second collapsed node. | `data/raw/gold_ecosystem_paths.tsv:679` | Supported. |
| `BTO:0001782` `peritoneal cavity` is the reviewed identity of the source-path parent `Host-associated > Mammals: Human > Abdominal/Peritoneal cavity`. | `curation/decisions.tsv:1178`; `data/habitats/host_associated/peritoneal_cavity.yaml` | Supported for a minted abdominal/peritoneal Abscess child, not for a generic MeSH Abscess record. |
| `mesh:D013492` `Suppuration` is the ontology parent of `mesh:D000038` `Abscess`. | `data/raw/ontology_terms.tsv:13554,13589`; `data/raw/ontology_subclass_edges.tsv:11880` | Supported. |
| The abdominal/peritoneal source path is exactly `mesh:D000038` `Abscess`. | `data/raw/ontology_terms.tsv:13554`; `data/raw/gold_ecosystem_paths.tsv:679` | Unsupported. `mesh:D000038` is generic; the GOLD path is context-specific and should retain a minted id with `mesh:D000038` as a broader parent. |
| The 2026-08-14 exact sample endorsed this generated exact mapping. | `curation/samples/exact-20260814.tsv:40` | Supported as historical sampling context only. It is not keyed by `habitatmech:GOLD.9c78e65e82`, is not a maintained item-level decision, and is contradicted by the current source-path/parent audit. |

Unsupported or over-scoped claims:

- The generated record's exact `mesh:D000038` identity is broader than its only active source concept.
- The generated `BTO:0001782` parent becomes an invalid is-a edge after the record adopts the generic MeSH Abscess identifier.

The record does not claim a definition, characteristic taxa, environmental parameters, causal mechanisms, or literature-backed evidence snippets.

## Completeness

The raw source attestation is otherwise complete for a four-organism, two-node GOLD row: the record carries the source, representative source id, source label, full path, `skos:exactMatch`, assertion count, assertion unit, and multi-node provenance note.

The exact grounding is the consequential gap. GOLD also has other plain `Abscess` leaves under `Skin`, `Nasal cavity`, `Spinal cord`, `Brain`, `Joint capsule`, and `Soft tissues`; those records keep minted ids and correctly carry `mesh:D000038` as a broader parent. The abdominal/peritoneal source path should follow the same pattern despite being the uniquely shallowest plain `Abscess` path.

No maintained causal overlay, history file, raw research report, or prior exact YAML review report was found by filename for `abscess__35b7623f` under `curation/causal_graphs`, `history`, `research/habitats`, or `reports/yaml_record_review`; the `find` checks included ignored files beneath those directories.

## Findings

| Finding | Evidence | Maintained owner |
|---|---|---|
| The generated record denotes the generic `mesh:D000038` `Abscess` term even though its only active source concept is the narrower GOLD abdominal/peritoneal Abscess path. That exact identity also leaks the source-path parent `BTO:0001782` onto generic Abscess, yielding a false parent edge. | `data/raw/gold_ecosystem_paths.tsv:679` has the context-specific path; `data/raw/ontology_terms.tsv:13554` labels the generic MeSH target; `data/habitats/host_associated/abscess__35b7623f.yaml` combines that generic target with `parent_habitats: BTO:0001782`. Exact ignored/hidden-inclusive searches found no `habitatmech:GOLD.9c78e65e82` override in maintained curation inputs. | Add an item-level row for `habitatmech:GOLD.9c78e65e82` in `curation/decisions.tsv`. |

No major findings.

No minor findings.

## Recommended Edits

Add a `GROUND_AS_PARENT` decision for `habitatmech:GOLD.9c78e65e82` to `curation/decisions.tsv`, targeting `mesh:D000038` / `Abscess` with `grounding_status: NARROW`, `review_depth: ITEM`, and notes that name `Path: Host-associated > Mammals: Human > Abdominal/Peritoneal cavity > Abscess`.

Then rerun `just seed-canary habitatmech:GOLD.9c78e65e82` and inspect the generated record. The source concept should keep a minted identifier, keep the reviewed abdominal/peritoneal source-path parent, keep `mesh:D000038` only as a broader parent, and have a `skos:narrowMatch` source attestation rather than an exact merge into the generic MeSH term.

## Follow-up Checks

For the future curation fix, run:

- `just seed`
- `just seed-canary habitatmech:GOLD.9c78e65e82`
- `just validate <new-record-path>`
- `just validate-strict <new-record-path>`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just report`
- `git diff --check`

After reseeding, search for `mesh:D000038` to confirm it remains only as a broader parent of context-specific Abscess records unless an active source attestation is curated to the generic identity.

## Additional Notes

- The pre-report exact filename check `find reports/yaml_record_review -maxdepth 1 -type f -name '*-abscess__35b7623f.md' -print` returned no previous exact report; `find` includes ignored files.
- The pre-report exact record check `find data/habitats -name 'abscess__35b7623f.yaml' -print` found exactly one generated target; `find` includes ignored files.
- `just report` completed successfully but does not ask whether the unique shallowest claimant of an ambiguous GOLD leaf still carries contextual source-path scope that makes an exact generic merge too broad.
