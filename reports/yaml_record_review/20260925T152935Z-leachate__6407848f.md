# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/engineered/leachate__6407848f.yaml`
- Started UTC: 2026-09-25T15:26:30Z
- Finished UTC: 2026-09-25T15:29:35Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/engineered/leachate__6407848f.yaml` |
| Identifier | `habitatmech:GOLD.f6f3c87964` |
| Label | `Leachate` |
| Category | `ENGINEERED` |
| Grounding | `NARROW` |
| Mapping status | `SEEDED` |
| Source | `GOLD` |
| Source path | `Engineered > Bioreactor > Anaerobic > Leachate` |

The target is a generated `HabitatRecord`. Its maintained inputs are the GOLD inventories in `data/raw/`, the slug pin in `data/habitats/PATHS.tsv`, and any future curator-authored decision or definition in `curation/decisions.tsv` and `curation/term_requests.tsv`.

The record collapses two GOLD ecosystem node IDs, `gold.ecosystem:5668` and `gold.ecosystem:5669`, under one canonical path. The generated source attestation exposes `gold.ecosystem:5668` as the first ID and points at `data/raw/gold_ecosystem_paths.tsv` for the full collapsed pair.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/engineered/leachate__6407848f.yaml` | Passed; LinkML reported no issues. |
| `just validate-strict data/habitats/engineered/leachate__6407848f.yaml` | Passed; 1 file scanned, 0 files with errors, and 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the generated term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validated. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 records on disk, no missing/extra/differing records. |
| `just worklist --limit 2000 --status all --out /tmp/habitatmech-leachate-worklist.tsv` | Passed; wrote 953 ungrounded rows. `habitatmech:GOLD.f6f3c87964` was not listed because this record is `NARROW`, not `UNGROUNDED`. |
| `just report` | Passed; the corpus-level diagnostic report completed. |

## Identity and Grounding

`data/raw/gold_ecosystem_paths.tsv` has the exact row:

| Canonical path | Leaf | Depth | Node count | Organisms | Studies | Biosamples | Total assertions | Node IDs |
|---|---|---:|---:|---:|---:|---:|---:|---|
| `Engineered > Bioreactor > Anaerobic > Leachate` | `Leachate` | 4 | 2 | 0 | 0 | 0 | 0 | `gold.ecosystem:5668\|gold.ecosystem:5669` |

The generated identifier, label, source ID, source path, and slug agree with that row and with `data/habitats/PATHS.tsv`, which pins `habitatmech:GOLD.f6f3c87964` to `leachate__6407848f`.

The NARROW parent to `ENVO:00002141` is plausible at seed time. The vendored ontology labels `ENVO:00002141` as `leachate`, and its definition is broad enough to include a more specific anaerobic-bioreactor leachate. The minted split is also explainable: GOLD has a second same-label `Engineered > Wastewater > Municipal landfill > Leachate` path in `data/habitats/engineered/leachate__2f66e051.yaml`, so claiming generic `ENVO:00002141` as the exact identity for either path would collapse distinct source contexts.

That NARROW grounding is not curator-reviewed. Ignored and hidden files were included in exact searches for `habitatmech:GOLD.f6f3c87964`, `gold.ecosystem:5668`, `gold.ecosystem:5669`, `leachate__6407848f.yaml`, and the exact source path across `curation/`, `history/`, `research/`, `reports/yaml_record_review/`, `conf/`, `data/habitats/`, and `data/raw/`. Those searches found the generated record, `PATHS.tsv`, and the raw GOLD rows described here, but no `curation/decisions.tsv` row, term request, history record, causal overlay, or prior review report for this minted identifier.

The other generated parent, `ENVO:00002124` `anaerobic bioreactor`, is inherited from the GOLD parent path. That term is strict for `Engineered > Bioreactor > Anaerobic`, which has item-level exact `GROUND` decisions to `ENVO:00002124`; it is probably contextual for this child. Leachate from an anaerobic bioreactor is a liquid produced in a reactor, not a subtype of the reactor itself.

## Evidence

The record has no `evidence` entries, no `causal_graphs`, and no source-specific study or biosample dataset objects. Its only inline support is the generated GOLD source attestation.

One committed GOLD study side row mentions the exact path:

| Study | Path count | Paths |
|---|---:|---|
| `Gs0130338` | 4 | `Engineered > Bioreactor > Anaerobic\|Engineered > Bioreactor > Anaerobic > Leachate\|Engineered > Solid waste > Food waste\|Engineered > Solid waste > Household waste` |

`data/raw/gold_path_biosamples.tsv` also has an exact row for ecosystem path ID `5669` with 3 biosamples. These rows prove GOLD uses the path in study and biosample side inventories, but they do not define the habitat tightly enough to justify the inherited `anaerobic bioreactor` parent.

An ignored/hidden-inclusive search for the exact path in `data/raw/gold_path_triads.tsv`, `data/raw/environment_parameters.tsv`, `data/raw/gold_studies.tsv`, `data/raw/gold_path_biosamples.tsv`, and `data/raw/gold_ecosystem_paths.tsv` found matches only in `gold_studies.tsv`, `gold_path_biosamples.tsv`, and `gold_ecosystem_paths.tsv`. The review found no exact-path MIxS triads or environmental-parameter rows to support a narrower grounding, parameter, or additional parent.

## Completeness

- `definition` and `definition_source` are absent. That is expected for a generated minted NARROW record, but incomplete for curator sign-off: the anaerobic-bioreactor leachate concept needs an item-level definition before it can become `REVIEWED`.
- `synonyms` and `xrefs` are absent. No exact xref or same-as decision was found during this bounded review.
- `environmental_parameters` is absent, and the exact path has no GOLD triad row or raw environment-parameter row.
- `characteristic_taxa` is absent, with no BacDive or PREGO source attestation on this generated record.
- `causal_graphs` is absent; no `curation/causal_graphs/` overlay references this identifier.
- `discussions` and `datasets` are absent even though the exact path has one GOLD study row and one 3-biosample side row that a future item-level curation pass should inspect.

The supporting absence searches used `rg --no-ignore --hidden`, so they included ignored and hidden files. `find reports/yaml_record_review -maxdepth 1 -type f -name '*leachate*' -print` found only `20260923T130355Z-borax_leachate.md`, a review of the separate `ENVO:00002142` `borax leachate` record.

## Findings

### Major: the NARROW Leachate split is unreviewed

The generated record has no `curation/decisions.tsv` row for `habitatmech:GOLD.f6f3c87964`, so no curator has confirmed that this path should remain a minted subtype rather than merge with `ENVO:00002141`, the municipal-landfill Leachate sibling, or another more specific term. The seeded answer is plausible, but `mapping_status: SEEDED` correctly shows that the identifier, `skos:narrowMatch`, same-label split, and `ENVO:00002141` parent still need item-level review.

Maintained owner: `curation/decisions.tsv`, with a supporting `curation/term_requests.tsv` definition if the item-level decision keeps this as a minted habitat.

### Major: `ENVO:00002124` is probably a contextual parent, not a strict broader habitat

The generated `parent_habitats` list contains `ENVO:00002124` only because GOLD places this Leachate leaf under `Engineered > Bioreactor > Anaerobic`. The review found no item-level decision, term request, MIxS triad, or evidence overlay saying the liquid leachate itself is a kind of anaerobic bioreactor. The strict broader term that does survive seed-time review is `ENVO:00002141` `leachate`; `ENVO:00002124` should be rechecked and probably removed with a `REPLACE` term-request parent once the child concept is defined.

Maintained owner: `curation/term_requests.tsv` with `parent_mode: REPLACE` if an item-level review confirms every inherited source parent is contextual.

## Recommended Edits

1. Inspect GOLD study `Gs0130338` and the 3 biosamples under ecosystem path ID `5669` to confirm whether the path denotes a leachate material produced by an anaerobic bioreactor, a sample location in the reactor, or a bookkeeping tag on the study.
2. Add an `ITEM`-level `curation/decisions.tsv` row for `habitatmech:GOLD.f6f3c87964` that verifies `ENVO:00002141` as a strict broader parent and records why this path should not be an exact match to the generic ENVO `leachate` term or to the municipal-landfill Leachate sibling.
3. If the item-level decision keeps the minted identifier, add a `curation/term_requests.tsv` definition for anaerobic-bioreactor leachate. Retain `ENVO:00002141` as the genus and use `parent_mode: REPLACE` if the evidence confirms `ENVO:00002124` is contextual rather than strict.
4. Rerun `just seed`, canary `just seed-canary habitatmech:GOLD.f6f3c87964`, inspect `data/habitats/engineered/leachate__6407848f.yaml`, then apply the seed and run the corpus validators.

## Follow-up Checks

After curation, run:

- `just validate data/habitats/engineered/leachate__6407848f.yaml`
- `just validate-strict data/habitats/engineered/leachate__6407848f.yaml`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just report`

Manually re-check that the generated record keeps `ENVO:00002141` as its only ontology parent unless an item-level review finds a strict additional parent, and that the same-label GOLD sibling remains separate unless direct evidence proves a same-as merge.

## Additional Notes

- The same-label GOLD sibling `data/habitats/engineered/leachate__2f66e051.yaml` represents `Engineered > Wastewater > Municipal landfill > Leachate`, carries one distinct GOLD study row and one biosample, and already has its own stable minted identifier.
- `ENVO:00002141` is itself present as `data/habitats/aquatic/leachate.yaml` because PREGO attests the generic ontology term directly. The two GOLD Leachate leaves should hang under that generic term unless curation finds a more exact ontology term for either source path.
