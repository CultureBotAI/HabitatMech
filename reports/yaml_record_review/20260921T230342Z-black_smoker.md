# YAML Record Review: black smoker

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/aquatic/black_smoker.yaml`
- Started UTC: 2026-09-21T23:03:42Z
- Finished UTC: 2026-09-21T23:03:49Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `ENVO:00000218` |
| Label | `black smoker` |
| Category | `AQUATIC` |
| Grounding status | `EXACT` |
| Mapping status | `REVIEWED` |
| Maintained inputs | `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, `data/raw/gold_ecosystem_paths.tsv`, `data/raw/prego_habitats.tsv`, `data/raw/prego_habitat_taxa.tsv`, `curation/decisions.tsv`, `data/habitats/PATHS.tsv` |

This is a generated ENVO-grounded record merged from one GOLD path and one PREGO ontology class. The path lock maps `ENVO:00000218` to `black_smoker`, the vendored ontology slice supplies the `black smoker` label, definition, and exact synonyms, GOLD maps `gold.ecosystem:4031` to `Environmental > Aquatic > Marine > Hydrothermal vents > Black smokers`, and PREGO directly asserts `ENVO:00000218`.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/aquatic/black_smoker.yaml` | Pass; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/black_smoker.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Pass; the committed term-request table is current with 109 terms. |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass; 3,206 expected records, 3,206 records on disk, and 0 missing, extra, or differing records. |
| `git diff --check` | Pass. |

`just validate-products` was not run. This review did not add or change a grounding row, and every ontology identifier on the record was checked directly in `data/raw/ontology_terms.tsv` and `data/raw/ontology_subclass_edges.tsv`.

## Identity and Grounding

The generated identity and grounding are supported:

- `data/habitats/PATHS.tsv` pins `ENVO:00000218` to `black_smoker`, matching the reviewed YAML path.
- `data/raw/ontology_terms.tsv` labels `ENVO:00000218` as `black smoker`, carries the copied definition, and lists `active chimney`, `oceanic vent`, and `sea vent` as ontology synonyms.
- `data/raw/ontology_subclass_edges.tsv` asserts `ENVO:00000218 rdfs:subClassOf ENVO:01000122`; `ENVO:01000122` is `marine hydrothermal vent` and is itself a subclass of `ENVO:00000215` `hydrothermal vent`.
- GOLD's `Environmental > Aquatic > Marine > Hydrothermal vents > Black smokers` path is an exact label/path match to `ENVO:00000218`, and `curation/decisions.tsv` records an item-level `GROUND` row for source concept `habitatmech:GOLD.a9f4ef57f4`.
- PREGO directly asserts the ontology class `ENVO:00000218`, and `curation/decisions.tsv` records an item-level `REVIEW` row for source concept `habitatmech:PREGO.ab4c7e091e`.

`parent_habitats` is also supported. `ENVO:01000122` comes from the ENVO subclass edge, and `ENVO:00000215` comes from the immediate GOLD source-path parent, `Environmental > Aquatic > Marine > Hydrothermal vents`, whose source concept is item-reviewed and exactly grounded to ENVO `hydrothermal vent`.

## Evidence

Supported claims:

- The definition and `definition_source: ENVO` come verbatim from the vendored ENVO row for `ENVO:00000218`.
- `grounding_status: EXACT` and `mapping_status: REVIEWED` follow from item-level reviewed decisions for the one GOLD source concept and the one PREGO source concept that feed the record.
- The GOLD source id, label, path, and `assertion_count: 27` with `assertion_unit: ORGANISM` follow from `data/raw/gold_ecosystem_paths.tsv`.
- The PREGO source id, label, `assertion_count: 12`, `score: 4.0`, and evidence channels follow from `data/raw/prego_habitats.tsv`.
- The twelve `characteristic_taxa` entries are copied in rank order from `data/raw/prego_habitat_taxa.tsv`; none is promoted with `is_characteristic: true`.
- The GOLD MIxS triads in `data/raw/gold_path_triads.tsv` reinforce rather than contradict the grounding: GOLD's local-scale term for this path is `ENVO:00000218` `black smoker`, its broad-scale term is `ENVO:01000051` `marine black smoker biome`, and its medium term is `ENVO:00002149` `sea water`.

Unsupported or over-scoped claims: None found.

The record has no record-level evidence, environmental parameters, causal graphs, discussion links, or datasets. That absence is supported: ignored-file-inclusive searches found no exact `ENVO:00000218`, `black_smoker`, `black smoker`, or `Black smoker` entry in `curation/term_requests.tsv`, `curation/causal_graphs/`, `history/`, or prior `reports/yaml_record_review/` reports before this report was written.

## Completeness

The record is complete enough for its current source-backed scope:

- No authored definition or term request is expected because the record uses the existing exact ENVO `black smoker` term.
- No HabitatMech-authored causal graph is expected; the only relevant existing overlay is `curation/causal_graphs/hydrothermal_vent.yaml`, and that belongs to broader `ENVO:00000215`.
- No environmental-parameter rows are expected because the committed kg-microbe environment table keys its hydrothermal rows to `ENVO:00000215`, not `ENVO:00000218`.
- No additional characteristic-taxon evidence is expected because the PREGO taxa are presented only as reported associations from PREGO, not as curator-asserted typifying taxa.

All negative checks above used `rg --no-ignore --hidden` or `find`, so ignored files were included where they were in the searched path.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

If a future curation change touches this record, rerun:

- `just validate data/habitats/aquatic/black_smoker.yaml`
- `just validate-strict data/habitats/aquatic/black_smoker.yaml`
- `just validate-causal-all`
- `just validate-history`
- `just verify-corpus --max-diffs 1`

## Additional Notes

An ignored-file-inclusive `find` search of `reports/yaml_record_review/` found no prior report for this `black_smoker` record before this file was written.

The unusual `supeRHEAted` capitalization in the generated definition is already present in `data/raw/ontology_terms.tsv` for `ENVO:00000218`; it is not introduced by HabitatMech's YAML writer.
