# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/abomasum.yaml`
- Started UTC: 2026-09-22T21:58:15Z
- Finished UTC: 2026-09-22T22:02:15Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/host_associated/abomasum.yaml` |
| Class | `HabitatRecord` |
| Identifier | `UBERON:0007358` |
| Label | `abomasum` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |
| Record status | Generated from one GOLD source concept grounded to UBERON |

This is the generated record for the GOLD path `Host-associated > Mammals > Digestive system > Stomach > Abomasum`. The GOLD source-concept key is `habitatmech:GOLD.6c776ebafd`, computed from `sha1("GOLD:Host-associated > Mammals > Digestive system > Stomach > Abomasum")[:10]`, and the generated record resolves to `UBERON:0007358` because the GOLD leaf label exactly matches the vendored UBERON `abomasum` label.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/abomasum.yaml` | Passed; LinkML validation reported no issues. |
| `just validate-strict data/habitats/host_associated/abomasum.yaml` | Passed; one file scanned with 0 error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; expected 3206 generated records, found 3206, with 0 missing, 0 extra, and 0 differing. |
| `git diff --check` | Passed; no whitespace errors. |

## Identity and Grounding

The GOLD source identity is supported:

| Raw field | Value |
|---|---|
| `canonical_path` | `Host-associated > Mammals > Digestive system > Stomach > Abomasum` |
| `ecosystem` | `Host-associated` |
| `ecosystem_category` | `Mammals` |
| `ecosystem_type` | `Digestive system` |
| `ecosystem_subtype` | `Stomach` |
| `specific_ecosystem` | `Abomasum` |
| `leaf_label` | `Abomasum` |
| `depth` | `5` |
| `gold_node_count` | `1` |
| `organism_count` | `0` |
| `study_count` | `0` |
| `biosample_count` | `0` |
| `total_assertions` | `0` |
| `gold_node_ids` | `gold.ecosystem:6391` |

The UBERON grounding is supported. `data/raw/ontology_terms.tsv:13332` defines `UBERON:0007358` as `abomasum`, with the exact synonyms `fourth stomach`, `maw`, `read`, `rennet-bag`, and `ruminant true stomach`. The generated identifier, label, definition, definition source, and five `EXACT_SYNONYM` rows all match that ontology row.

`source_attestations` matches `data/raw/gold_ecosystem_paths.tsv:2025`: the record keeps `gold.ecosystem:6391` as the source id, records the `Abomasum` leaf label and canonical GOLD path exactly, omits `assertion_count` because the raw organism count is zero, and does not add a multi-node note because the path has only one GOLD node id.

Both generated parents are supported and strictly broader:

- `UBERON:0011953` comes from `data/raw/ontology_subclass_edges.tsv:11606`, which states `UBERON:0007358 rdfs:subClassOf UBERON:0011953`; `data/raw/ontology_terms.tsv:13435` labels that parent `stomach glandular region`.
- `habitatmech:GOLD.d3869ad37f` comes from GOLD source hierarchy. `data/raw/gold_ecosystem_paths.tsv:419` records the immediate parent path `Host-associated > Mammals > Digestive system > Stomach`, and `data/habitats/host_associated/stomach__0260a3a9.yaml` is the generated `Stomach` record for that path.

`grounding_status: EXACT` is mechanically expected for an uncurated exact GOLD-to-ontology label match. `mapping_status: SEEDED` is also expected: an ignored/hidden-inclusive exact search for `habitatmech:GOLD.6c776ebafd` found no maintained `curation/decisions.tsv` row for the source concept, so there is no item-level decision to promote this record to `REVIEWED`.

## Evidence

| Claim | Nearest support | Review |
|---|---|---|
| The generated record represents the GOLD path `Host-associated > Mammals > Digestive system > Stomach > Abomasum`. | `data/raw/gold_ecosystem_paths.tsv:2025` | Supported exactly. |
| The GOLD source row has no upstream organism, study, or biosample assertions in the collapsed ecosystem inventory. | `data/raw/gold_ecosystem_paths.tsv:2025` | Supported exactly. |
| `UBERON:0007358` contributes the label, definition, and synonyms. | `data/raw/ontology_terms.tsv:13332` | Supported exactly. |
| `UBERON:0011953` is an ontology parent of `UBERON:0007358`. | `data/raw/ontology_subclass_edges.tsv:11606`; `data/raw/ontology_terms.tsv:13435` | Supported exactly. |
| `habitatmech:GOLD.d3869ad37f` is the source-path parent. | `data/raw/gold_ecosystem_paths.tsv:419`; `data/habitats/host_associated/stomach__0260a3a9.yaml` | Supported. The GOLD parent path is `Host-associated > Mammals > Digestive system > Stomach`, which is broader than the Abomasum child path. |
| GOLD biosample triads also use `UBERON:0007358` as the sampled medium for the same Abomasum path. | `data/raw/gold_path_triads.tsv:1360`; `data/raw/gold_path_biosamples.tsv:347` | Supported as corroborating context only. These newer GOLD-derived tables do not own the generated `source_attestations.assertion_count` field. |

Unsupported or over-scoped claims: None found. The record keeps only GOLD source provenance, UBERON lexical data, the UBERON asserted superclass, and the GOLD source-path parent. It does not claim organism associations, environmental parameters, characteristic taxa, or causal mechanisms.

## Completeness

The direct GOLD source attestation is complete for a zero-assertion, one-node raw row: the record carries the source name, source id, source label, source path, and exact-match predicate, while correctly omitting assertion-count and multi-node-note fields.

Separate GOLD biosample-derived inputs show downstream use of the same path but do not imply a missing Abomasum record field. `data/raw/gold_path_biosamples.tsv:347` counts 49 biosamples for ecosystem path id `6391`, `data/raw/gold_studies.tsv:3886` and `data/raw/gold_studies.tsv:3951` mention the Abomasum path, and `data/raw/gold_path_triads.tsv:1358-1360` aggregates one complete broad/local/medium triad for the path.

Ignored/hidden-inclusive exact searches found no maintained curation input for `habitatmech:GOLD.6c776ebafd` or `gold.ecosystem:6391`, and no maintained `curation/decisions.tsv` row for `UBERON:0007358`; the searches covered the repository with generated `data/text_map`, `pages`, and `build` outputs excluded only from broad exact content searches.

No maintained causal overlay, history file, raw research report, or prior exact YAML review report was found by filename for `abomasum` under `curation/causal_graphs`, `history`, `research/habitats`, or `reports/yaml_record_review`; the `find` search included ignored files beneath those checked directories.

Sibling C1 and C3 research reports mention Abomasum while discussing camelid C-compartment boundaries, but they are not Abomasum curation inputs and do not contradict this record. Those reports use `UBERON:0007358` as the ruminant abomasum sibling that should not be conflated with camelid C3, which is consistent with leaving the GOLD `Abomasum` leaf exactly grounded to `UBERON:0007358`.

## Findings

None found.

No blocker findings.

No major findings.

No minor findings.

## Recommended Edits

None required.

If a future curator wants to mark the GOLD exact match item-reviewed, add a `REVIEW` row for `habitatmech:GOLD.6c776ebafd` in `curation/decisions.tsv` and rerun `just seed-canary UBERON:0007358`. That should be a status-only change: keep the identifier at `UBERON:0007358` and keep both existing parents.

## Follow-up Checks

None required beyond the validators already run for this report.

For any future status-promotion-only edit, run:

- `just seed-canary UBERON:0007358`
- `just validate data/habitats/host_associated/abomasum.yaml`
- `just validate-strict data/habitats/host_associated/abomasum.yaml`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

## Additional Notes

- The pre-report exact filename check `find reports/yaml_record_review -maxdepth 1 -type f -name '*abomasum*' -print` returned no previous exact report; `find` includes ignored files.
- `just worklist` did not list `habitatmech:GOLD.6c776ebafd`, `UBERON:0007358`, or `Abomasum`, consistent with a GOLD leaf whose label is already generated as `EXACT`.
- `just report` completed successfully and did not flag `UBERON:0007358` or `habitatmech:GOLD.6c776ebafd` in any risky-grounding, stale-sweep, non-habitat, same-label, GOLD-triad-compatibility, minted-GOLD-triad, unsupported-prefix, or organism-identity section.
