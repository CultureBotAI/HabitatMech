# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/food/food_sample.yaml
- Started UTC: 2026-09-26T15:30:31Z
- Finished UTC: 2026-09-26T15:30:31Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.2aeb0d8390` |
| Label | `Food sample` |
| Category | `FOOD` |
| Grounding | `UNGROUNDED` |
| Mapping | `SEEDED` |
| Source | `GOLD` |
| Source path | `Engineered > Food production > Food sample` |
| Maintained owner | Target review belongs in the row for `habitatmech:GOLD.2aeb0d8390` in `curation/decisions.tsv`; if item review accepts a novel term for the generic food-sample bin, the definition and parent override belong in `curation/term_requests.tsv`. |

Reviewed the complete generated record at `data/habitats/food/food_sample.yaml`.
`data/habitats/PATHS.tsv` maps `habitatmech:GOLD.2aeb0d8390` to the stable
`food_sample` slug.

The generated record currently has one GOLD source attestation for
`gold.ecosystem:5921`, the first of three GOLD ecosystem node IDs that collapse
to the canonical path `Engineered > Food production > Food sample`.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/habitats/food/food_sample.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/food/food_sample.yaml` | Pass: scanned 1 file with 0 files containing `ERROR` and 0 total `ERROR` rows. |
| `just validate-causal curation/causal_graphs/<overlay>.yaml` | Not applicable: exact ignored/hidden-inclusive searches found no target-specific curated causal overlay for `habitatmech:GOLD.2aeb0d8390`, `food_sample`, `gold.ecosystem:5921`, `gold.ecosystem:5922`, `gold.ecosystem:5923`, or `Engineered > Food production > Food sample`. |
| `just validate-causal-all` | Pass: validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Pass: the term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: expected 3,206 records, found 3,206 on disk, and the corpus reproduces exactly from `data/raw/`. |
| `just worklist --limit 0 --status all --out /tmp/habitatmech-worklist.tsv` | Pass: wrote 953 total `UNGROUNDED` rows across all decision states; `just report` splits those into 123 individually examined term-request records and 830 class-level sweep rows. `Food sample` is the next row after the newly merged `Fetal tissue` review in the all-status ranking. |
| `just report` | Pass: completed the corpus report for 3,206 records. |
| `git diff --check` | Pass before writing this report. |
| Reference validator | Not applicable: the target has no record-level `evidence`, `characteristic_taxa`, `causal_graphs`, `discussions`, or `datasets` with citation-bearing `EvidenceItem` references. |

## Identity and Grounding

The source identity is traceable. `data/raw/gold_ecosystem_paths.tsv` has an
exact `Engineered > Food production > Food sample` row at depth 3 with three
GOLD node IDs, three organism assertions, no study or biosample assertions in
that aggregate row, and node IDs
`gold.ecosystem:5921|gold.ecosystem:5922|gold.ecosystem:5923`. The generated
`source_attestations` entry correctly carries the first source ID,
`source_label: Food sample`, the exact source path, `assertion_count: 3`, and
`assertion_unit: ORGANISM`.

The only decision row for `habitatmech:GOLD.2aeb0d8390` is the
`curation/decisions.tsv` class-level `CONFIRM_UNGROUNDED` row from
2026-08-12. Its note explicitly says the sweep only established that no term in
the vendored slice matched the label by a lexical route and did not assess
whether the concept is a habitat. That is not enough review for a generic leaf
whose label could mean either a food material sample or a sample artifact.

The only generated parent, `FOODON:03530206`, is not a broader food material or
food-sample habitat. `data/raw/ontology_terms.tsv` labels it `food production`
and defines it as "A planned process involving the rearing, manufacture or
distribution of food material." The edge is inherited from the immediate GOLD
parent path, `Engineered > Food production`, which currently resolves to
`data/habitats/food/food_production.yaml` as an exact grounding to that same
planned-process term.

The top worklist ontology candidate, `FOODON:00003366` `food (spoiled)`, is not
an identity for the GOLD source concept. Spoiled food is a narrowed quality
subclass under FoodOn's food-material-by-characteristic branch; the GOLD
`Food sample` label and path do not assert spoilage.

## Evidence

- Supported as generated source provenance: exact ignored/hidden-inclusive
  searches found `Engineered > Food production > Food sample` in
  `data/raw/gold_ecosystem_paths.tsv`, `data/raw/gold_path_biosamples.tsv`, and
  `data/raw/gold_studies.tsv`.
- Supported as generated source count: the committed aggregate
  `gold_ecosystem_paths.tsv` row reports `organism_count: 3`, matching the
  generated `assertion_count: 3`.
- Supported as side-channel evidence, not yet a generated field:
  `data/raw/gold_path_biosamples.tsv` has an exact row for
  `Engineered > Food production > Food sample`, ecosystem-path ID `5923`, and
  39 biosamples.
- Weak as source-path evidence:
  `data/raw/gold_studies.tsv` has one study row, `Gs0154282`, that contains
  `Engineered > Food production > Food sample`, but the same row spans 17
  sibling food-production paths. That row corroborates food-production context
  and does not by itself decide what `Food sample` means.
- Unsupported as a broader habitat: `FOODON:03530206` is a planned process, not
  a material, sample, site, or microbial habitat class.

## Completeness

An exact ignored/hidden-inclusive search over `history/`, `research/`,
`curation/term_requests.tsv`, `curation/term_requests/`,
`curation/causal_graphs`, `conf/`, `reports/yaml_record_review/`, and the
relevant GOLD raw support tables found no target-specific authored term
request, generated term-request row, curated causal overlay, append-only
history record, label-correspondence residual, research report, or prior review
report for `habitatmech:GOLD.2aeb0d8390`,
`gold.ecosystem:5921`, `gold.ecosystem:5922`, `gold.ecosystem:5923`, or the
exact source path.

`find reports/yaml_record_review -maxdepth 1 -type f -name '*food_sample*'
-print` found no prior top-level report with this file stem; `find` includes
ignored files.

Exact ignored/hidden-inclusive searches found no child paths under
`Engineered > Food production > Food sample >` in `data/raw/gold_ecosystem_paths.tsv`,
`data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`,
`data/raw/gold_studies.tsv`, or `data/raw/environment_parameters.tsv`.

The empty optional `environmental_parameters`, `characteristic_taxa`,
`evidence`, `causal_graphs`, `discussions`, and `datasets` blocks are not
schema defects. They correctly stay empty until a maintained input supplies
claim-level evidence.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | `Food sample` is still only class-swept, so HabitatMech has not item-reviewed whether the GOLD leaf is a real generic food habitat, a food-material bin needing a broader FoodOn parent, or a sample artifact that should be `NOT_APPLICABLE`. | The only `curation/decisions.tsv` row for `habitatmech:GOLD.2aeb0d8390` has `review_depth: CLASS` and its note says "Whether the concept is a habitat at all was NOT assessed". The worklist still ranks the record for item review with candidate `FOODON:00003366` `food (spoiled)`, which is plainly narrower than the source label. | Item-review `habitatmech:GOLD.2aeb0d8390` in `curation/decisions.tsv`; if no exact term fits a real food-sample habitat, add a definition in `curation/term_requests.tsv`. |
| Major | The generated `parent_habitats` edge says `Food sample` is a kind of `FOODON:03530206` `food production`, but that FoodOn term is a planned process, not a broader habitat or material. | `data/raw/ontology_terms.tsv` defines `FOODON:03530206` as a planned process involving the rearing, manufacture or distribution of food material. The edge is inherited from the immediate GOLD source-path parent, and an earlier `utensil` review already flagged the same inherited process parent as a false food-branch edge. | Suppress or replace the inherited parent through a maintained input. For this target, a supported `curation/term_requests.tsv` row with `parent_mode: REPLACE` could replace the false source-path parent with a real genus such as `FOODON:00002403` `food material`; a systemic food-branch fix would live in `src/habitatmech/seed.py` plus a regression test. |

No blocker findings found.

No minor findings found.

## Recommended Edits

1. Item-review `habitatmech:GOLD.2aeb0d8390` in
   `curation/decisions.tsv` against the exact
   `Engineered > Food production > Food sample` source path, `FOODON:00002403`
   `food material`, `FOODON:00001002` `food product`, `FOODON:00003366`
   `food (spoiled)`, and `FOODON:03530206` `food production`.
2. If the source denotes a real generic food-material habitat, keep the minted
   `habitatmech:GOLD.2aeb0d8390` identity with an `ITEM` decision and add a
   `curation/term_requests.tsv` row defining the term under a true broader
   material class. Use `parent_mode: REPLACE` if item review confirms the only
   inherited parent is the false `FOODON:03530206` process.
3. If the source denotes a sample artifact rather than a microbial habitat,
   replace the class-level decision with an `ITEM` `NOT_APPLICABLE` decision
   and do not mint a term request.
4. Record the decision and term-request edits in an append-only mapping history
   file with `just new-history`, then re-seed with `just seed`,
   `just seed-canary habitatmech:GOLD.2aeb0d8390`, and
   `just seed-apply --force`.

## Follow-up Checks

| Edit | Proof |
| --- | --- |
| Review the source concept | `rg --no-ignore --hidden -n '^habitatmech:GOLD\\.2aeb0d8390\\b' curation/decisions.tsv` should show one `ITEM` row whose note rejects `FOODON:00003366` unless the source path is proven to require spoiled food. |
| Add a supported novel term if `Food sample` remains a real habitat | `rg --no-ignore --hidden -n '^habitatmech:GOLD\\.2aeb0d8390\\b' curation/term_requests.tsv` should show a definition with a true broader material parent and a note explaining why GOLD's generic `Food sample` is not exact to `FOODON:00002403` or `FOODON:00001002`. |
| Remove the false `food production` parent | After the canary, inspect `data/habitats/food/food_sample.yaml` and confirm it no longer lists `FOODON:03530206` under `parent_habitats`. |
| Preserve the GOLD attestation | The canary output should still carry `source_id: gold.ecosystem:5921`, `source_path: Engineered > Food production > Food sample`, `assertion_count: 3`, and `assertion_unit: ORGANISM`. |
| Keep the generated corpus reproducible | Run `just verify-corpus --max-diffs 1`, `just validate data/habitats/food/food_sample.yaml`, `just validate-strict data/habitats/food/food_sample.yaml`, `just validate-causal-all`, `just term-requests-check`, `just validate-history`, and `git diff --check`. |

## Additional Notes

The exact raw-support absence checks above included ignored and hidden files.
They were bounded to the committed raw GOLD tables and the maintained curation
surfaces that could plausibly own this target; no search inspected uncommitted
external GOLD API exports.

The process-valued `FOODON:03530206` parent is not specific to this target.
`data/habitats/food/food_production.yaml` publishes GOLD
`Engineered > Food production` as that FoodOn process term, and any child record
that does not replace the inherited source-path parent can acquire the same
false `is-a` edge.
