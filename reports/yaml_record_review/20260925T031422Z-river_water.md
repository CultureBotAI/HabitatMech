# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/river_water.yaml
- Started UTC: 2026-09-25T03:14:22Z
- Finished UTC: 2026-09-25T03:14:22Z
- Verdict: needs curation

## Target

Reviewed the complete generated record at `data/habitats/engineered/river_water.yaml`. The record is a GOLD-only engineered habitat for `Engineered > Artificial ecosystem > Mesocosm > River water`.

The generated file has `identifier: ENVO:01000599`, `label: river water`, `habitat_category: ENGINEERED`, `grounding_status: EXACT`, and `mapping_status: SEEDED`. Its parents are `ENVO:01000620` `mesocosm`, from the immediate GOLD parent path, and `ENVO:03605006` `stream water`, from the ENVO subclass edge of `ENVO:01000599`. Its only source attestation shows `gold.ecosystem:6457` and correctly notes that two GOLD ecosystem node IDs share this canonical path.

The file is generated from `data/raw/` and has only the seed-generated `SEEDED_FROM_SOURCES` history event. Future identity or grounding fixes belong in `curation/decisions.tsv`, keyed to the deterministic source-concept ID `habitatmech:GOLD.5a98f4a82a`, not as direct edits to this YAML.

## Validation

All required local checks passed:

- `just validate data/habitats/engineered/river_water.yaml`
- `just validate-strict data/habitats/engineered/river_water.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

No validator was skipped.

## Identity and Grounding

The generated label, category, source attestation, and raw GOLD inventory row agree. `data/raw/gold_ecosystem_paths.tsv` has the exact `Engineered > Artificial ecosystem > Mesocosm > River water` row at depth 4, with two GOLD node IDs, zero aggregate organism, study, biosample, and total assertions, and `gold.ecosystem:6457|gold.ecosystem:6458` as the node list. `data/habitats/PATHS.tsv` maps `ENVO:01000599` to the stable `river_water` slug.

The exact grounding is not yet item-reviewed. The seed match adopts `ENVO:01000599` `river water`, which is present in `data/raw/ontology_terms.tsv` without a definition and has an ontology subclass edge to `ENVO:03605006` `stream water`. The vendored `stream water` definition is `Water which is part of a stream.` That inherited in-situ meaning needs curator review before HabitatMech can merge a GOLD path under `Engineered > Artificial ecosystem > Mesocosm` into the generic ontology term.

The directly analogous `Engineered > Artificial ecosystem > Mesocosm > Seawater` sibling has already been item-reviewed as `habitatmech:GOLD.0cadaca0bf` with a `GROUND_AS_PARENT` decision because mesocosm seawater is experimental and held in an artificial ecosystem. There is no matching `curation/decisions.tsv` row for `habitatmech:GOLD.5a98f4a82a`, the minted source-concept ID for this river-water path.

## Evidence

- Supported as generated source provenance: exact ignored-inclusive searches found `gold.ecosystem:6457`, `gold.ecosystem:6458`, and `Engineered > Artificial ecosystem > Mesocosm > River water` in `data/raw/gold_ecosystem_paths.tsv`; `gold.ecosystem:6457` is the first node shown in the generated source attestation.
- Supported as generated hierarchy: the immediate GOLD parent path `Engineered > Artificial ecosystem > Mesocosm` resolves to `data/habitats/engineered/mesocosm.yaml`, whose identifier is `ENVO:01000620`.
- Supported as ontology hierarchy: `data/raw/ontology_subclass_edges.tsv` makes `ENVO:01000599` a subclass of `ENVO:03605006`, and `data/raw/ontology_terms.tsv` labels that parent `stream water`.
- Unsupported as reviewed identity: no maintained item-level row explains why this engineered mesocosm source path should remain exactly `ENVO:01000599` rather than a minted concept narrower than both `river water` and `mesocosm`.

The record has no causal graphs, environmental parameters, characteristic taxa, datasets, discussions, or claim-level `EvidenceItem` objects to inspect.

## Completeness

The generated attestation is complete for the committed GOLD ecosystem inventory: `data/raw/gold_ecosystem_paths.tsv` lists both source node IDs, and the generated record's first-node note accounts for the second node ID.

Exact ignored-inclusive searches found no biosample, study, or GOLD triad rows for this exact engineered mesocosm river-water path in the committed GOLD API side tables. Exact ignored-inclusive searches over `curation/decisions.tsv`, `curation/term_requests.tsv`, `curation/term_requests/`, `curation/causal_graphs/`, `history/`, `research/`, and `conf/id_label_targets.yaml` found no occurrence of `habitatmech:GOLD.5a98f4a82a`, `gold.ecosystem:6457`, `gold.ecosystem:6458`, or the exact source path.

The absent definition is explainable from the ontology slice: the `ENVO:01000599` row in `data/raw/ontology_terms.tsv` has no definition text for the seeder to import.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `Engineered > Artificial ecosystem > Mesocosm > River water` is exact-grounded to generic `ENVO:01000599` `river water` without item-level review, even though the engineered mesocosm context may be narrower than in-situ water that is part of a river or stream. | The generated record is `EXACT` and `SEEDED`; `curation/decisions.tsv` has no row for `habitatmech:GOLD.5a98f4a82a`; `ENVO:01000599` inherits from `ENVO:03605006` `stream water`, defined as water which is part of a stream; and the sibling `Engineered > Artificial ecosystem > Mesocosm > Seawater` already required an `ITEM` `GROUND_AS_PARENT` decision to avoid merging experimental mesocosm seawater with in-situ sea water. | `curation/decisions.tsv`; if no exact term fits, `curation/term_requests.tsv` |

No blocker or minor findings.

## Recommended Edits

1. Item-review `habitatmech:GOLD.5a98f4a82a` in `curation/decisions.tsv` against the exact `Engineered > Artificial ecosystem > Mesocosm > River water` source path, `ENVO:01000599` `river water`, `ENVO:03605006` `stream water`, `ENVO:01000620` `mesocosm`, and the existing `habitatmech:GOLD.0cadaca0bf` Mesocosm Seawater decision.
2. If the source path denotes an experimental mesocosm containing river-derived water rather than water still part of a river, use `GROUND_AS_PARENT` to keep `ENVO:01000599` as a broader parent and keep the GOLD concept minted.
3. If item review finds an exact term for engineered river-water mesocosms is absent, add any needed authored definition in `curation/term_requests.tsv` instead of editing `data/habitats/engineered/river_water.yaml` directly.

## Follow-up Checks

After the curation decision:

1. Run `just seed` and a focused `just seed-canary` for the affected source concept.
2. Inspect the regenerated River water or replacement minted record and confirm the identifier, parents, source attestation predicate, `mapping_status`, and curation history follow from the decision row.
3. Run `just validate-strict` on the regenerated record path.
4. Run `just term-requests-check`, `just validate-history`, `just validate-causal-all`, and `just verify-corpus --max-diffs 1`.
5. Run `just report` and confirm the Mesocosm River water source is no longer raw seeded output.

## Additional Notes

All absence checks in this review used ignored-inclusive `rg --no-ignore --hidden` or `find`, bounded to maintained curation, history, research, raw inventory, generated habitat, and review-report paths. No search depended on rendered `pages/` or text-map JSON.

Excluding this newly added report, the exact `River water` label search over maintained curation, history, and research paths returned no rows; the lowercase record slug search found only the generated `PATHS.tsv` entry.
