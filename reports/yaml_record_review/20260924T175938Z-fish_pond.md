# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/fish_pond.yaml
- Started UTC: 2026-09-24T17:56:01Z
- Finished UTC: 2026-09-24T17:59:38Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.f0461d363e` |
| Label | `Fish pond` |
| Category | `ENGINEERED` |
| Grounding | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Maintained source | generated from `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/PATHS.tsv`, and `curation/decisions.tsv` |

Reviewed the complete generated record at `data/habitats/engineered/fish_pond.yaml`. The record is a GOLD-only habitat for `Engineered > Artificial ecosystem > Aquaculture > Fish pond`, with one source attestation representing two collapsed GOLD ecosystem node IDs, `gold.ecosystem:7998|gold.ecosystem:7999`, and 18 GOLD organism assertions. The generated file is not authoritative for future fixes; source-concept review belongs in `curation/decisions.tsv` and any regenerated path is validated with `just seed` / `just verify-corpus`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/fish_pond.yaml` | Passed |
| `just validate-strict data/habitats/engineered/fish_pond.yaml` | Passed |
| Focused causal overlay validator | Not applicable; exact ignored-inclusive overlay searches found no `curation/causal_graphs/*fish*` file and no Fish pond source path in `curation/causal_graphs/` |
| `just validate-causal-all` | Passed; 32 files / 32 graphs |
| Reference validator | Not applicable; the generated GOLD-only record has no record-level `evidence` items, no causal graph, and no environmental-parameter references |
| `just term-requests-check` | Passed; 109 terms |
| `just validate-history` | Passed; 77 history records |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected / 3,206 found / 0 missing / 0 extra / 0 differing |
| `just worklist --limit 2000` | Passed; 0 ungrounded concepts remain undecided and 1,810 decisions are present |
| `just report` | Passed; 3,206 habitat records summarized |
| `git diff --check` | Passed |

## Identity and Grounding

The record identity, category, and source path agree: `Fish pond` is an engineered aquaculture habitat under GOLD's `Engineered > Artificial ecosystem > Aquaculture` branch. Its `source_attestations` row also agrees with `data/raw/gold_ecosystem_paths.tsv`: two GOLD ecosystem node IDs share the exact collapsed path, and that path carries 18 GOLD organism assertions.

The record's current `UNGROUNDED` status is not item-reviewed enough to trust. `curation/decisions.tsv` only has a 2026-08-12 class-level `CONFIRM_UNGROUNDED` row whose note explicitly says the concept was not assessed for habitat identity. Exact ignored-inclusive searches for `habitatmech:GOLD.f0461d363e`, `gold.ecosystem:7998`, `gold.ecosystem:7999`, and `Engineered > Artificial ecosystem > Aquaculture > Fish pond` found no item-level decision, term request, history record, research report, or `conf/id_label_targets.yaml` entry that would supersede that class-level row.

The vendored ontology slice now includes one likely exact local ENVO candidate and two plausible broader candidates that need item review:

| Candidate | Local evidence | Review implication |
|---|---|---|
| `ENVO:00000056` `fishpond` | `data/raw/ontology_terms.tsv` defines it as a pond or lake used for artificial fish culture. | Likely exact for GOLD `Fish pond`; this should be checked before the record remains minted and `UNGROUNDED`. |
| `ENVO:00000294` `fish farm` | `data/raw/ontology_terms.tsv` defines it as a facility where fish are commercially raised in tanks or enclosures, usually for food. | Plausible broader parent if `fishpond` is rejected, but broader than a pond-specific path because it also covers tanks and enclosures. |
| `ENVO:03600074` `aquaculture farm` | `data/raw/ontology_terms.tsv` defines it as an agricultural aquatic ecosystem where fish, crustaceans, mollusks, aquatic plants, and algae are cultivated. | Plausible higher aquaculture parent, and relevant to the inherited `Aquaculture` source parent, but broader than a fish-specific pond. |

`habitatmech:GOLD.f0461d363e` is also a generated parent of `data/habitats/engineered/sediment__5aa3b4f2.yaml`, so the item-level grounding choice for Fish pond will propagate to the generated fish-pond sediment child.

## Evidence

- Supported: the GOLD source provenance and 18 organism-assertion count are traceable to the committed GOLD path inventory row for `Engineered > Artificial ecosystem > Aquaculture > Fish pond`.
- Supported: the note that two GOLD ecosystem node IDs share the collapsed path matches the inventory's `gold.ecosystem:7998|gold.ecosystem:7999` source-id list.
- Under-reviewed: the class-level `CONFIRM_UNGROUNDED` decision was generated from a lexical screen and is not source-path-specific evidence that no ENVO identity exists.
- Under-reviewed: no maintained row records a curator comparison against `ENVO:00000056` `fishpond`, `ENVO:00000294` `fish farm`, or `ENVO:03600074` `aquaculture farm`.
- No record-level citations, characteristic taxa, environmental parameters, or causal edges are present, so there are no attached snippets or literature references to validate.

## Completeness

The record is structurally sparse but not missing any required generated slots. Its consequential gap is semantic rather than syntactic: it lacks the item-level decision that would determine whether the `Fish pond` source path should ground exactly to `ENVO:00000056`, stay minted under a broader ENVO aquaculture/fish-farm parent, or document why those local terms are unsuitable.

Bounded ignored-inclusive searches were run over `curation`, `data/raw`, `reports`, `data/habitats`, `history`, `research`, and `conf/id_label_targets.yaml` for the HabitatMech ID, both GOLD source IDs, and the exact source path. They found the generated target, its `data/habitats/PATHS.tsv` row, the generated fish-pond sediment child, the GOLD path inventory row, the direct child path `Engineered > Artificial ecosystem > Aquaculture > Fish pond > Sediment`, and the class-level `curation/decisions.tsv` row; they found no item-level decision, term request, history entry, research report, label-correspondence exception, or causal overlay for this exact source concept.

Additional ignored-inclusive bounded searches found no exact `Fish pond` rows in `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_path_triads.tsv`, or `data/raw/gold_studies.tsv`, so there is no direct MIxS triad or exact study-path evidence in the committed GOLD auxiliary inventories.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `Fish pond` remains `UNGROUNDED` and `SEEDED` even though the vendored ontology slice contains a likely exact `ENVO:00000056` `fishpond` term and plausible broader `fish farm` / `aquaculture farm` candidates. | The only decision for `habitatmech:GOLD.f0461d363e` is a class-level `CONFIRM_UNGROUNDED`; the class-level note says habitat identity was not assessed. `data/raw/ontology_terms.tsv` has `ENVO:00000056` `fishpond`, `ENVO:00000294` `fish farm`, and `ENVO:03600074` `aquaculture farm`. Exact ignored-inclusive searches found no item-level decision, term request, history entry, research report, causal overlay, or label-correspondence exception for the target. | `curation/decisions.tsv` |

## Recommended Edits

1. Item-review `habitatmech:GOLD.f0461d363e` in `curation/decisions.tsv` against the exact `Engineered > Artificial ecosystem > Aquaculture > Fish pond` path and the local candidates `ENVO:00000056` `fishpond`, `ENVO:00000294` `fish farm`, and `ENVO:03600074` `aquaculture farm`.
2. If `ENVO:00000056` is exact for the GOLD source path, replace the class-level `CONFIRM_UNGROUNDED` with an `ITEM` `GROUND` decision to `ENVO:00000056` and regenerate the corpus so `Fish pond` stops carrying a minted `habitatmech:GOLD.*` identity.
3. If the pond path must remain minted, replace the class-level row with an `ITEM` decision that records the nearest valid broader parent and why `fishpond` is not exact.
4. After the decision, inspect `data/habitats/engineered/sediment__5aa3b4f2.yaml` in the regenerated corpus because it currently inherits `habitatmech:GOLD.f0461d363e` as its source-path parent.

## Follow-up Checks

- `just seed`
- `just seed-canary ENVO:00000056` if the decision exactly grounds Fish pond to `ENVO:00000056`; otherwise `just seed-canary habitatmech:GOLD.f0461d363e`
- Inspect the regenerated `Fish pond` and fish-pond `Sediment` records.
- `just seed-apply --force --prune` if the decision exactly grounds Fish pond to `ENVO:00000056`; otherwise `just seed-apply --force`
- `just validate data/habitats/engineered/fish_pond.yaml` or, if grounding changes its slug/category, the regenerated successor path from `data/habitats/PATHS.tsv`
- `just validate-strict <regenerated Fish pond path>`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

## Additional Notes

- No blocker finding was found: the current generated YAML validates, its GOLD source attestation is internally consistent, and the record denotes an engineered fish pond rather than a non-habitat.
- No minor findings were found.
