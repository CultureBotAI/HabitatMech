# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/brine.yaml
- Started UTC: 2026-09-23T14:41:12Z
- Finished UTC: 2026-09-23T14:45:27Z
- Verdict: pass

## Target

| Field | Value |
|---|---|
| Identifier | ENVO:00003044 |
| Label | brine |
| Class | HabitatRecord |
| Category | AQUATIC |
| Grounding status | EXACT |
| Mapping status | SEEDED |
| Maintained owner | generated from `data/raw/prego_habitats.tsv`, `data/raw/prego_habitat_taxa.tsv`, `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, and `data/habitats/PATHS.tsv` |

[data/habitats/aquatic/brine.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/aquatic/brine.yaml) is the generated PREGO/ENVO record for `ENVO:00003044`.

The exact target was resolved with ignored-file-inclusive searches:

- `find data/habitats -type f -name 'brine.yaml' -print` found exactly one generated YAML record.
- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-brine.md' -print` found no prior exact report.
- `rg --no-ignore --hidden -n 'ENVO:00003044|brine' data/habitats/PATHS.tsv data/habitats -g 'brine*.yaml'` showed that the bare `brine` slug is pinned to `ENVO:00003044`, while the two same-label GOLD children keep distinct minted identifiers and collision-resolved slugs.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/brine.yaml` | pass; no LinkML issues found |
| `just validate-strict data/habitats/aquatic/brine.yaml` | pass; 1 file scanned, 0 files with ERROR, 0 total ERROR rows |
| `just validate-causal curation/causal_graphs/brine.yaml` | not applicable; exact `find curation/causal_graphs -maxdepth 1 -type f -name 'brine.yaml' -print` found no overlay |
| `just validate-causal-all` | pass; 32 causal-graph curation files with 32 graphs validated |
| Reference validator | not applicable; this generated PREGO record has no causal graph and no DOI, PMID, or URL evidence slots to inspect |
| `just term-requests-check` | pass; term-request table is current with 109 terms |
| `just validate-history` | pass; no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | pass; 3206 expected, 3206 found, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | pass; 0 ungrounded records still undecided; 1810 decisions on file |
| `just report` | pass; 3206 records; no risky groundings not yet reviewed and no global issue specific to `brine` |
| `git diff --check` | pass |

## Identity and Grounding

The record identity is coherent:

- `data/raw/prego_habitats.tsv` contains `ENVO:00003044`, ontology `ENVO`, category `biolink:OntologyClass`, 2 taxa, 2 direct assertions, maximum PREGO score `3`, evidence channel `annotated_genomes_isolates`, and PREGO synonyms `brine|brines|brinous`.
- `data/raw/ontology_terms.tsv` supplies the canonical ENVO label `brine` and definition `Water saturated or nearly saturated with salt (NaCl).`
- `data/raw/ontology_subclass_edges.tsv` asserts `ENVO:00003044 rdfs:subClassOf ENVO:00002012`, matching the generated sole parent `hypersaline water`. Brine, as salt-saturated or nearly salt-saturated water, is strictly narrower than hypersaline water.
- `data/habitats/PATHS.tsv` pins `ENVO:00003044` to the corpus-unique slug `brine`.

PREGO habitat IDs are ontology IDs that self-ground exactly when no curation decision redirects them, so `grounding_status: EXACT` and the absent `mapping_predicate` on the PREGO attestation are correct. The `mapping_status: SEEDED` value is also expected: exact `rg --no-ignore --hidden -n 'habitatmech:PREGO\.f97fb60fb8|ENVO:00003044' curation/decisions.tsv` found no `ITEM` decision for the deterministic `mint("PREGO", "ENVO:00003044")` source key, and exact `rg --no-ignore --hidden -n 'ENVO:00003044' history curation/term_requests.tsv curation/causal_graphs research reports -g '!reports/yaml_record_review/**'` found no brine-specific maintained overlay, term request, curation-history file, research report, or auxiliary report.

The same-label GOLD `Brine` leaves are not conflated with this record:

- [data/habitats/aquatic/brine__2299825d.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/aquatic/brine__2299825d.yaml) is `habitatmech:GOLD.cc5b244380`, the non-marine saline-and-alkaline lake brine path.
- [data/habitats/aquatic/brine__791bb429.yaml](/Users/marcin/Documents/VIMSS/ontology/KG-Hub/KG-Microbe/Mechs/HabitatMech/data/habitats/aquatic/brine__791bb429.yaml) is `habitatmech:GOLD.fd7b992495`, the marine coastal brine path.
- Both correctly retain `ENVO:00003044` as a broader parent under `grounding_status: NARROW`.

## Evidence

This record has no causal graph and no curator-authored literature evidence. All material generated claims trace to maintained rows:

- The PREGO source attestation mirrors `data/raw/prego_habitats.tsv`: `ENVO:00003044`, source label `brine`, 2 upstream taxon assertions, `TAXON` assertion unit, maximum PREGO score `3.0`, and evidence channel `annotated_genomes_isolates`.
- The two `RELATED_SYNONYM` entries, `brines` and `brinous`, come from the PREGO synonym list after the canonical label `brine` is suppressed as redundant.
- The two PREGO taxon rows mirror `data/raw/prego_habitat_taxa.tsv`: `NCBITaxon:717231` / `Flexistipes sinusarabici DSM 4947` at rank 1 and `NCBITaxon:889378` / `Spirochaeta africana DSM 8902` at rank 2, both with score `3`.
- Each taxon row has `candidate_pool: 2`, matching the full PREGO taxon count for this habitat.
- No taxon row sets `is_characteristic`; this avoids upgrading PREGO associations into stronger curated characteristic-taxon claims.

## Completeness

The generated record is complete enough for its current PREGO-owned scope:

- It carries ENVO's definition and broader parent.
- It carries the one PREGO source attestation that generates the record.
- It carries all two PREGO-associated taxa, so the top-25 truncation path did not omit any raw PREGO association.
- It intentionally has no GOLD `source_path`, BacDive source ID, Madin source ID, or environmental-parameter rows. Raw GOLD triads cite `ENVO:00003044` as a submitted `medium` for three GOLD paths, but MIxS triad context is not a claim that those GOLD concepts should merge into the generic `brine` material record.
- It intentionally has no causal graph; exact ignored-file-inclusive search found no `curation/causal_graphs/brine.yaml` overlay.

## Findings

None found.

## Recommended Edits

None.

## Follow-up Checks

No corrective checks are required. If a future curation pass changes the PREGO grounding, synonym handling, or ontology parentage, the narrowest checks are:

- `just seed`
- `just seed-canary ENVO:00003044`
- `just validate data/habitats/aquatic/brine.yaml`
- `just validate-strict data/habitats/aquatic/brine.yaml`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`

## Additional Notes

`just report` lists `ENVO:00003044` as GOLD environmental-medium triad evidence for Salt crystallizer ponds. That ranked row is useful evidence for future review of the GOLD salt-crystallizer-pond concept; it is not an additional PREGO source attestation for the exact ENVO `brine` record.
