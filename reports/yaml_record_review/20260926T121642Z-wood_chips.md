# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/wood_chips.yaml
- Started UTC: 2026-09-26T12:16:42Z
- Finished UTC: 2026-09-26T12:16:57Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.0c54014f15` |
| Label | `Wood chips` |
| Class | `HabitatRecord` |
| Category | `ENGINEERED` |
| Generated path | `data/habitats/engineered/wood_chips.yaml` |
| Maintained path lock | `data/habitats/PATHS.tsv:1365` maps `habitatmech:GOLD.0c54014f15` to `wood_chips` |
| Source concept | GOLD `Engineered > Solid waste > Brown waste > Wood chips` |
| First source node | `gold.ecosystem:7648` |
| Grounding | `UNGROUNDED` |
| Mapping | `SEEDED` |

The reviewed file is generated from the GOLD ecosystem-path inventory and one
class-level curation decision. It should not be patched directly.

## Validation

| Command | Result |
|---|---|
| `just worklist --limit 0 --status all --out /tmp/habitatmech-worklist.tsv` | Passed; wrote 953 rows. The target appeared as row 234 with four GOLD organism assertions and no candidate ontology term column. |
| `just validate data/habitats/engineered/wood_chips.yaml` | Passed with `No issues found`. |
| `just validate-strict data/habitats/engineered/wood_chips.yaml` | Passed; one file scanned, zero files with `ERROR`, zero total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 expected records, 3206 found, 0 missing, 0 extra, 0 differing. |
| `just report` | Passed. |
| `git diff --check` | Passed before this report was written. |

## Identity and Grounding

The generated identifier, source path, source node collapse, top-level
category, and organism count agree with the GOLD aggregate:

| Claim | Evidence | Assessment |
|---|---|---|
| The target is the GOLD `Engineered > Solid waste > Brown waste > Wood chips` path. | `data/raw/gold_ecosystem_paths.tsv:664` records that canonical path with `ecosystem` `Engineered`, `ecosystem_category` `Solid waste`, `ecosystem_type` `Brown waste`, `ecosystem_subtype` `Wood chips`, `leaf_label` `Wood chips`, depth 4, and four organism assertions. | Supported exactly. |
| Two GOLD ecosystem node IDs share this canonical path. | `data/raw/gold_ecosystem_paths.tsv:664` lists `gold.ecosystem:7648|gold.ecosystem:7649` and `gold_node_count` 2. `src/habitatmech/seed.py` emits the first ID on the generated record and records the collapse in a note. | Supported exactly. |
| The `source_attestations` block preserves the GOLD source path, first source ID, leaf label, assertion count, and assertion unit. | The generated YAML has `source: GOLD`, `source_id: gold.ecosystem:7648`, `source_label: Wood chips`, `source_path: Engineered > Solid waste > Brown waste > Wood chips`, `assertion_count: 4`, and `assertion_unit: ORGANISM`. | Supported exactly. |
| The parent points to the GOLD `Brown waste` parent path. | The generated record lists `habitatmech:GOLD.e292616707`, which `data/habitats/PATHS.tsv:2968` maps to `brown_waste`. `data/habitats/engineered/brown_waste.yaml` is generated from `Engineered > Solid waste > Brown waste` and hangs under `mesh:D062611` `Solid Waste`. | Supported as a generated GOLD path parent. The `Brown waste` parent is itself only class-depth reviewed. |
| The record is still owned by a class-level sweep decision. | `curation/decisions.tsv:166` has `CONFIRM_UNGROUNDED` for `habitatmech:GOLD.0c54014f15` with `review_depth` `CLASS`. Its note says the sweep did not assess whether the concept is a habitat and is not yet a term-request candidate. | Reproducible but incomplete. Wood chips under GOLD brown waste are a concrete engineered solid-waste material, so the record needs item-level review rather than another generated-only pass. |

The current vendored ontology slice and local curation tables do not contain an
exact wood-chip or brown-waste target. A hidden/ignored-inclusive regex search
for `wood[-_[:space:]]*chips?|brown[-_[:space:]]*waste` across
`data/raw/ontology_terms.tsv`, `curation/external_xrefs.tsv`, and
`curation/term_requests.tsv` returned no hits.

Nearby GOLD `Brown waste` leaves show the branch is partly groundable rather
than wholesale invalid: `Engineered > Solid waste > Brown waste > Hay` already
maps exactly to `ENVO:00002869` `hay`, and `Engineered > Solid waste > Brown
waste > Rice straw` already maps exactly to `ENVO:00003870` `rice straw`.

## Evidence

| Record claim | Nearest source | Assessment |
|---|---|---|
| `source: GOLD`, `source_id: gold.ecosystem:7648`, `source_label: Wood chips`, and `source_path: Engineered > Solid waste > Brown waste > Wood chips` | `data/raw/gold_ecosystem_paths.tsv:664` | Supported exactly. |
| `assertion_count: 4` and `assertion_unit: ORGANISM` | `data/raw/gold_ecosystem_paths.tsv:664` | Supported exactly. This is a GOLD organism count; `study_count` and `biosample_count` are both zero on the aggregate row. |
| The note saying two GOLD ecosystem node IDs share this path | `data/raw/gold_ecosystem_paths.tsv:664`; the GOLD attestation writer in `src/habitatmech/seed.py` | Supported exactly as generated behavior. |
| `parent_habitats: habitatmech:GOLD.e292616707` | The source path's immediate parent is `Engineered > Solid waste > Brown waste`, which appears as generated `data/habitats/engineered/brown_waste.yaml`. | Supported as the generated GOLD hierarchy. The parent remains `UNGROUNDED` from a class-level sweep. |
| Absence of a curated definition, term request, environmental parameters, record evidence, causal graph, item-level history event, target-specific research report, and prior exact YAML review | Exact hidden/ignored-inclusive searches for `habitatmech:GOLD.0c54014f15`, `gold.ecosystem:7648`, `gold.ecosystem:7649`, `Wood chips`, `wood chips`, and `wood_chips` across `data/raw/`, `curation/`, `history/`, `research/habitats/`, `reports/yaml_record_review/`, `data/habitats/`, and `pages/habitats/` found only the expected raw aggregate, generated record, generated page, `PATHS.tsv` lock, class-level decision, and one contextual biofilm research mention of wood chips as packing material. | Supported. No maintained item-level curation artifact for this specific GOLD path was found. |

## Completeness

The generated record completely preserves the current GOLD path aggregate,
source node collapse, class-level decision, organism assertion count, and GOLD
source-path parent. It correctly avoids unsupported definitions,
environmental parameters, evidence objects, characteristic taxa, and causal
graph claims.

It is incomplete as a curation decision. `Wood chips` are a specific plant
material used in the engineered solid-waste branch, but the current
`CONFIRM_UNGROUNDED` action only says no vendored term matched the label by the
class-level lexical sweep. The exact ontology, external-xref, term-request, and
target-artifact searches above found no exact preexisting `wood chips` or
`brown waste` target to ground against, but they also found no item-level
definition or term request that would justify keeping this source as a minted
HabitatMech leaf.

The neighboring `Hay` and `Rice straw` records make the missing item review
consequential: the curation pass should decide whether this source is an exact
match to an ontology term not found by the bounded search, a narrower
plant-derived solid-waste material that needs a term request, or a source that
should be merged under a broader reviewed wood/solid-waste record.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| HM-WOOD-CHIPS-001 | Major | `Wood chips` still publishes as `UNGROUNDED` from a `CLASS`-depth `CONFIRM_UNGROUNDED` decision. The GOLD path and organism count are reproducible, but there is no item-level decision, term request, definition, or broader curated grounding that records whether wood chips should remain a minted HabitatMech habitat or merge with a missed exact/broader material term. | `curation/decisions.tsv`; if no exact ontology term exists after item review, `curation/term_requests.tsv` |

## Recommended Edits

1. Item-review `Engineered > Solid waste > Brown waste > Wood chips` against
   relevant wood, plant-material, organic-material, and solid-waste ontology
   terms.
2. If a term exactly names wood chips as a sampled environmental material,
   replace the class-level row in `curation/decisions.tsv` with an item-level
   `GROUND` decision and let regeneration merge the GOLD attestation onto that
   target.
3. If no exact term exists, replace the class-level row with an item-level
   `CONFIRM_UNGROUNDED` decision and add a `curation/term_requests.tsv` row
   with a definition and strictly broader parents for wood chips as
   plant-derived engineered solid waste.
4. Regenerate from the maintained inputs and confirm the source path,
   grounding status, mapping status, parentage, and generated history reflect
   the item-level decision.

Do not hand-edit `data/habitats/engineered/wood_chips.yaml` or generated HTML
pages.

## Follow-up Checks

After item-level curation, rerun:

- `just seed`
- `just seed-canary habitatmech:GOLD.0c54014f15`
- inspect `data/habitats/engineered/wood_chips.yaml` or the ontology-backed
  target that absorbs `Engineered > Solid waste > Brown waste > Wood chips`
- `just validate-strict` on the regenerated target
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just report`

If the source merges with an existing generated record, also check
`data/habitats/PATHS.tsv`, generated pages, and any retired stale file before
committing the curation change.

## Additional Notes

None.
