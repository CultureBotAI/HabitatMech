# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: `data/habitats/host_associated/brown_algae.yaml`
- Started UTC: 2026-09-22T14:57:33Z
- Finished UTC: 2026-09-22T14:57:33Z
- Verdict: pass with minor issues

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.3e4ecbed63` |
| Label | `brown-alga-associated environment` |
| Class | `HabitatRecord` |
| Category | `HOST_ASSOCIATED` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `REVIEWED` |
| Maintained owner | `curation/decisions.tsv:1781` confirms the GOLD source concept as ungrounded with `FOODON:03412395` as an xref; `curation/term_requests.tsv:33` defines the minted term |
| Locked slug | `data/habitats/PATHS.tsv:1757` maps `habitatmech:GOLD.3e4ecbed63` to `brown_algae` |

This is the reviewed GOLD record for `Host-associated > Algae > Brown Algae`. The generated YAML correctly keeps the host context as a minted `brown-alga-associated environment`, keeps `FOODON:03412395` only as an xref, and uses both the curated organism-determined ENVO genus and the stricter generated `alga-associated environment` parent. The only issue found is confined to one human-readable curation note: it correctly rejects `FOODON:03412395`, but it copied the red-algae sibling's definition quote while discussing the brown-algae term.

## Validation

| Command | Result |
|---|---|
| `just validate data/habitats/host_associated/brown_algae.yaml` | Pass; `linkml-validate` reported `No issues found` |
| `just validate-strict data/habitats/host_associated/brown_algae.yaml` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass; 32 causal-graph curation files with 32 graphs validated |
| `just term-requests-check` | Pass; term-request table is current with 109 terms |
| `just validate-history` | Pass; 77 history records are valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass; expected 3206 records, found 3206, with 0 missing, 0 extra, 0 differing |
| `git diff --check` | Pass; no whitespace or patch errors after this report was written |

## Identity and Grounding

The source identity, curated term, xref, and parents are supported by maintained rows:

- `data/raw/gold_ecosystem_paths.tsv:112` is the raw GOLD row for `Host-associated > Algae > Brown Algae`. It has leaf label `Brown Algae`, depth 3, three GOLD node ids, 231 organism assertions, zero study assertions, zero biosample assertions, and total assertion count 231.
- `data/raw/gold_path_biosamples.tsv:332` is the side-table row for 57 biosample assertions on the same GOLD path, and `data/raw/gold_path_triads.tsv:1226` through `:1228` preserve the screened MIxS broad, local, and medium terms for 42 samples.
- `curation/decisions.tsv:1781` records an item-level `CONFIRM_UNGROUNDED` decision for `habitatmech:GOLD.3e4ecbed63`. It deliberately rejects `FOODON:03412395` as the identity and keeps it with relation `xref`.
- `data/raw/ontology_terms.tsv:10705` provides `FOODON:03412395` `brown algae`, and `data/raw/ontology_subclass_edges.tsv:9028` places it under `FOODON:03411301` `algae`.
- `curation/term_requests.tsv:33` defines `habitatmech:GOLD.3e4ecbed63` as `brown-alga-associated environment`, with parent `ENVO:01001000`, exact synonym `Brown Algae`, `ADD` parent mode, and notes tying the wording to the committed deep-research report.
- `data/raw/ontology_terms.tsv:8495` provides `ENVO:01001000` `environmental system determined by an organism`, and `data/raw/ontology_subclass_edges.tsv:6795` confirms it is an `environmental system`.
- `data/raw/gold_ecosystem_paths.tsv:79`, `curation/decisions.tsv:1778`, `curation/term_requests.tsv:27`, `data/habitats/PATHS.tsv:1278`, and the generated `data/habitats/host_associated/algae.yaml` support the generated parent `habitatmech:GOLD.02383c20a7` `alga-associated environment`.

The record therefore denotes the microbial habitat provided by brown algae, not brown algae as organisms or food-source taxa. That is the right split for a host-associated GOLD path: the host clade is relevant to microbial habitat scope, but the organism term itself is not a place.

## Evidence

Every generated claim is traceable to a raw row or maintained curation input:

| Claim | Nearest source | Review |
|---|---|---|
| Minted identifier `habitatmech:GOLD.3e4ecbed63` | GOLD path minting from `Host-associated > Algae > Brown Algae` in `src/habitatmech/seed.py` | Supported exactly |
| Label, HabitatMech definition, definition source, exact synonym `Brown Algae`, and parent `ENVO:01001000` | `curation/term_requests.tsv:33` | Supported exactly |
| Parent `habitatmech:GOLD.02383c20a7` | GOLD path parent inference plus `ADD` mode in `curation/term_requests.tsv:33` | Supported; `alga-associated environment` is strictly broader |
| `xrefs: FOODON:03412395` and `grounding_status: UNGROUNDED` | `curation/decisions.tsv:1781` | Supported exactly |
| `mapping_status: REVIEWED` | `curation/decisions.tsv:1781` has `review_depth` `ITEM` | Supported exactly |
| `source_id: gold.ecosystem:3620`, source label, source path, and the three-node note | `data/raw/gold_ecosystem_paths.tsv:112` | Supported exactly; the first of `gold.ecosystem:3620\|gold.ecosystem:3745\|gold.ecosystem:3960` is shown inline |
| `assertion_count: 231` with `assertion_unit: ORGANISM` | `data/raw/gold_ecosystem_paths.tsv:112` | Supported exactly |
| Absence of environmental parameters and characteristic taxa | No maintained parameter rows or GOLD/PREGO/BacDive/Madin taxon rows feed this source concept | Supported by bounded exact searches |

The 57 biosample assertions and screened MIxS triads are correctly absent from this record because the current seeder preserves GOLD organism counts in the source attestation and does not promote MIxS triad terms into identity, parent, or environmental-parameter claims automatically.

## Completeness

The generated slots are complete for the maintained inputs that currently feed this record:

- The single GOLD source attestation captures the path, first GOLD node id, first-node note, organism assertion count, and `ORGANISM` unit.
- The curated definition has the expected `HabitatMech` provenance, exact synonym, xref, and both broader parents.
- The `ADD` parent mode keeps the GOLD `Host-associated > Algae` hierarchy while adding the ENVO host-associated genus, so the record does not lose its source parent.
- The record has no target-specific causal graph overlay, record-level evidence item, discussion item, dataset, append-only history record, or previous exact YAML review report.

Ignored/hidden-inclusive exact searches covered `curation`, `history`, `research`, `reports`, `data/raw`, `data/habitats/PATHS.tsv`, and the generated target, excluding generated `build`, `data/text_map`, and `pages` trees. They found the raw GOLD path and side-table rows, decision row, term-request row, ENVO and FOODON slice rows, generated parent, path lock, deep-research report, and research manifest rows cited above; they found no target-specific causal overlay, append-only history record, or prior YAML review report.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| Minor | The disposition for `FOODON:03412395` is correct, but the explanatory note in `curation/decisions.tsv:1781` quotes red-algae evidence while reviewing the brown-algae source concept. The inspected FOODON slice has no definition on `FOODON:03412395`; the committed brown-algae deep-research report separately called out that this sentence was copied from the red-algae sibling. This does not change the generated xref or ungrounded identity, but it makes the review rationale cite the wrong FOODON term. | Reword the `habitatmech:GOLD.3e4ecbed63` note in `curation/decisions.tsv:1781`, then regenerate `brown_algae`. |

## Recommended Edits

- Reword the `curation/decisions.tsv:1781` note so it says `FOODON:03412395` has no textual definition and is a FOODON algae subclass / food-source organism term, rather than saying it carries the red-algae definition.
- Regenerate the target with `just seed` and preview `habitatmech:GOLD.3e4ecbed63` with `just seed-canary habitatmech:GOLD.3e4ecbed63` before applying the full generated corpus.

## Follow-up Checks

After correcting the maintained decision note, re-run the same focused validation set and confirm the generated `CONFIRM_UNGROUNDED` history event carries the corrected brown-algae rationale:

- `just validate data/habitats/host_associated/brown_algae.yaml`
- `just validate-strict data/habitats/host_associated/brown_algae.yaml`
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `git diff --check`

## Additional Notes

- `find reports/yaml_record_review -maxdepth 1 -name '*brown*' -print` found no pre-existing review report for this record before this file was written.
- Exact ignored/hidden-inclusive searches for `habitatmech:GOLD.3e4ecbed63`, `gold.ecosystem:3620`, `Host-associated > Algae > Brown Algae`, `FOODON:03412395`, `brown-algae`, and `brown_algae` found no maintained artifact for this target beyond the raw, curation, term-request, research, and generated rows cited above.
