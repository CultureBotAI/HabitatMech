# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/aquatic/cave_stream.yaml`
- Started UTC: 2026-09-24T01:11:55Z
- Finished UTC: 2026-09-24T01:16:19Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.c0b02b63b9` |
| Label | `Cave stream` |
| File | `data/habitats/aquatic/cave_stream.yaml` |
| Category | `AQUATIC` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `SEEDED` |
| Source | `GOLD` |
| Source ID | `gold.ecosystem:7785` |
| Source path | `Environmental > Aquatic > Deep subsurface > Groundwater > Cave stream` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv`, `data/habitats/PATHS.tsv`, and `curation/decisions.tsv`; do not edit `data/habitats/aquatic/cave_stream.yaml` directly. |

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/cave_stream.yaml` | Passed; `linkml-validate` reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/cave_stream.yaml` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| Target causal overlay | Not applicable. Exact ignored-inclusive searches for `habitatmech:GOLD.c0b02b63b9`, `Cave stream`, and `cave_stream`, plus `find` checks for `*cave*` and `*stream*` causal-graph filenames, found no target overlay in `curation/causal_graphs/`. |
| Reference validator | Not applicable. This target has no record-level `evidence`, no `characteristic_taxa.reference`, and no `causal_graphs` edges requiring cited evidence. |
| `just term-requests-check` | Passed; the ENVO term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records are valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 records expected, 3,206 found, 0 missing, 0 extra, 0 differing. |
| `just worklist --limit 2000` | Passed; 0 ungrounded records are still undecided and 1,810 decisions are on file. |
| `just report` | Passed; the corpus report completed and emitted current grounding, source, GOLD triad, and curation statistics. |
| `git diff --check` | Passed before writing this report. |

## Identity and Grounding

The generated identifier is stable and correct for this GOLD path:

| Claim | Check |
|---|---|
| Minted identifier | `mint("GOLD", "Environmental > Aquatic > Deep subsurface > Groundwater > Cave stream")` returned `habitatmech:GOLD.c0b02b63b9`, matching the generated record. |
| Slug lock | `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.c0b02b63b9` to `cave_stream`. |
| GOLD source row | `data/raw/gold_ecosystem_paths.tsv` has exactly one node for the path, source id `gold.ecosystem:7785`, depth 5, label `Cave stream`, 0 GOLD KGX organism assertions, 0 GOLD KGX sample assertions, and 0 GOLD KGX study assertions. |
| Curation row | `curation/decisions.tsv` has one class-level `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.c0b02b63b9`; the row records that the lexical sweep did not assess whether the concept is a habitat or term-request candidate. |

The current direct source-path parent,
`habitatmech:GOLD.22a80cbd14` `Groundwater`, is generated from the GOLD parent
path `Environmental > Aquatic > Deep subsurface > Groundwater`. That parent is
not a strict broader class for this leaf: a cave stream is a flowing watercourse
in a cave, not a kind of pore-space groundwater.

The vendored ontology slice has no exact `cave stream` class. It does have
`ENVO:00000060` `underground stream`, defined as a stream under Earth's
surface. That term is a strictly broader parent for cave streams and is tighter
than the generic `ENVO:00000023` `stream`; it subclasses both `ENVO:00000023`
and `ENVO:00000061` `underground water body`.

## Evidence

The generated record is traceable to maintained inputs:

| Claim | Maintained input or inventory |
|---|---|
| GOLD source id and exact source path | `data/raw/gold_ecosystem_paths.tsv` row for `gold.ecosystem:7785` |
| Stable HabitatMech identifier and slug | `data/habitats/PATHS.tsv` row for `habitatmech:GOLD.c0b02b63b9` |
| Class-level `CONFIRM_UNGROUNDED` status | `curation/decisions.tsv` row for `habitatmech:GOLD.c0b02b63b9` |
| Generated source-path parent | `data/raw/gold_ecosystem_paths.tsv` row for the parent path `Environmental > Aquatic > Deep subsurface > Groundwater` plus `data/habitats/PATHS.tsv` mapping `habitatmech:GOLD.22a80cbd14` to `groundwater` |

The record has no record-level literature entry, and the committed GOLD rows do
not attach any biosamples, studies, MIxS triads, or environmental parameters to
`gold.ecosystem:7785`. Exact ignored-inclusive searches for
`gold.ecosystem:7785` found the target row in
`data/raw/gold_ecosystem_paths.tsv` and the generated target record, but no
`data/raw/gold_path_biosamples.tsv`, `data/raw/gold_studies.tsv`, or
`data/raw/gold_path_triads.tsv` rows for this source path.

No causal graph is attached to this record, so there are no mechanism edges
needing claim-level `EvidenceItem` support.

## Completeness

The required generated identity, category, source attestation, source-derived
parent, class-level curation history, and seeded mapping status are present.

The empty optional slots are acceptable for the generated record as it stands:

| Slot | Review |
|---|---|
| `environmental_parameters` | Empty. No committed GOLD or curator-authored environmental-parameter row was found for `gold.ecosystem:7785`. |
| `characteristic_taxa` | Empty. The GOLD aggregate records zero organism assertions for this path, the record has no BacDive or PREGO taxon rows, and HabitatMech does not infer characteristic taxa from a source-tree label. |
| `evidence` | Empty. The class-level sweep did not inspect cave-stream literature, and no maintained literature artifact was found for this record. |
| `causal_graphs` | Empty. Exact ignored-inclusive searches found no target overlay in `curation/causal_graphs/`. |
| `discussions` and `datasets` | Empty. No maintained discussion or dataset input exists for this record. |

Exact ignored-inclusive searches for `habitatmech:GOLD.c0b02b63b9`,
`gold.ecosystem:7785`, `cave_stream`, `ENVO:00000060`, and the GOLD source path
found the generated record, its `PATHS.tsv` row, the raw GOLD path row, the
class-level decision row, and the vendored `underground stream` term. They did
not find a target-specific term request, causal overlay, history record,
research report, generated `ENVO:00000060` habitat record, or prior exact YAML
review report. The exact report check used `find` under
`reports/yaml_record_review`, which includes ignored files.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | None found | The YAML validates, the generated identifier matches the GOLD source path, and the record correctly remains `SEEDED` because its only curation decision is class-level. | Not applicable |
| Major | The class-level `CONFIRM_UNGROUNDED` row missed `ENVO:00000060` `underground stream` as a strictly broader term for Cave stream and left the generated record under the non-strict `Groundwater` source-path parent. | `data/raw/ontology_terms.tsv` includes `ENVO:00000060` `underground stream`; `data/raw/ontology_subclass_edges.tsv` places it under `ENVO:00000023` `stream` and `ENVO:00000061` `underground water body`. `ENVO:01001004` `groundwater` names underground water in pore spaces of rock or unconsolidated deposits, while this GOLD path names a cave stream. The source path places the concept under GOLD's groundwater branch, but the leaf is a flowing underground watercourse rather than a kind of pore-space water. | `curation/decisions.tsv`; probably `curation/term_requests.tsv` if Cave stream remains a novel minted child of `ENVO:00000060` |
| Minor | None found | The remaining empty optional slots are consistent with the absence of maintained item-level curation, sample or study rows, MIxS triads, BacDive or PREGO taxa, and a causal overlay. | Not applicable |

## Recommended Edits

| Priority | Edit | Maintained path |
|---|---|---|
| Major | Replace the class-level row for `habitatmech:GOLD.c0b02b63b9` with an item-level `GROUND_AS_PARENT` decision to `ENVO:00000060` `underground stream`, yielding `grounding_status` `NARROW`. | `curation/decisions.tsv` |
| Major | If Cave stream stays distinct from generic `ENVO:00000060`, add an authored `cave stream` term request with parent `ENVO:00000060` and `parent_mode=REPLACE` so the regenerated record drops the inherited `habitatmech:GOLD.22a80cbd14` `Groundwater` parent. | `curation/term_requests.tsv` |

Do not hand-edit `data/habitats/aquatic/cave_stream.yaml`; regenerate it from
the maintained inputs after item-level curation changes.

## Follow-up Checks

| Edit | Narrowest proving checks |
|---|---|
| Item-level Cave stream grounding | Exact ignored-inclusive `rg` for `habitatmech:GOLD.c0b02b63b9`, `gold.ecosystem:7785`, and `ENVO:00000060` should find the new maintained decision and any term request; `just seed-canary habitatmech:GOLD.c0b02b63b9`, `just term-requests-check`, `just validate data/habitats/aquatic/cave_stream.yaml`, `just validate-strict data/habitats/aquatic/cave_stream.yaml`, `just validate-history`, and `just verify-corpus --max-diffs 1` should still pass. |
| Parent replacement | The regenerated `data/habitats/aquatic/cave_stream.yaml` should list `ENVO:00000060`, not `habitatmech:GOLD.22a80cbd14`, under `parent_habitats`; run `just validate data/habitats/aquatic/cave_stream.yaml`, `just validate-strict data/habitats/aquatic/cave_stream.yaml`, and `just verify-corpus --max-diffs 1`. |

## Additional Notes

- Searches used to establish absence included ignored and hidden files. Broad
  exact content searches excluded `data/text_map/**`, `pages/**`, `build/**`,
  and `.git/**` so generated text-map, rendered-page, build-product, and git
  object matches did not dominate the results.
- Exact `Cave stream` label searches also find Cave stream sediment rows. Cave
  stream sediment is the sibling GOLD path
  `Environmental > Aquatic > Deep subsurface > Groundwater > Cave stream sediment`
  with source id `gold.ecosystem:7786`, not an additional source attestation for
  this Cave stream target.
