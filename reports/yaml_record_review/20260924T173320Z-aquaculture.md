# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/engineered/aquaculture.yaml`
- Started UTC: 2026-09-24T17:29:00Z
- Finished UTC: 2026-09-24T17:33:20Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.0287e1b2a9` |
| Label | `Aquaculture` |
| Category | `ENGINEERED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Generated path | `data/habitats/engineered/aquaculture.yaml` |
| Source concept | GOLD `Engineered > Artificial ecosystem > Aquaculture` |

The target is the depth-3 GOLD `Aquaculture` source path under `Engineered > Artificial ecosystem`. It is distinct from `data/habitats/aquatic/aquaculture__f784f173.yaml`, which represents the sibling GOLD path `Environmental > Aquatic > Aquaculture` and was reviewed separately in `reports/yaml_record_review/20260921T154453Z-aquaculture__f784f173.md`.

The file is generated. `data/habitats/PATHS.tsv:1280` maps `habitatmech:GOLD.0287e1b2a9` to the stable `aquaculture` slug, so future fixes belong in `curation/decisions.tsv`, `curation/term_requests.tsv`, or future source-path parent suppression support rather than in this YAML.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/aquaculture.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/aquaculture.yaml` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| Target causal overlay validator | Not applicable; an ignored-inclusive exact search over `curation/causal_graphs` for the target source path, plus `find curation/causal_graphs -maxdepth 1 -type f -name '*aquaculture*' -print`, found no target overlay. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files and 32 graphs validated. |
| Reference validator | Not applicable; this generated record has no record-level `evidence` or `causal_graphs`, and HabitatMech has no dedicated per-record reference check. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3206 records were expected, 3206 were found, and `data/habitats/` reproduced exactly from `data/raw/`. |
| `just worklist --limit 2000` | Passed; 0 ungrounded records remain undecided and 1810 decisions are on file. |
| `just report` | Passed; the corpus report regenerated successfully. |
| `git diff --check` | Passed. |

## Identity and Grounding

`data/raw/gold_ecosystem_paths.tsv:482` is the maintained GOLD row for this exact source concept. It has canonical path `Engineered > Artificial ecosystem > Aquaculture`, source leaf `Aquaculture`, depth `3`, three collapsed GOLD node IDs, 13 direct organism assertions, no direct study or biosample assertions in that aggregate row, and `gold.ecosystem:7990|gold.ecosystem:7991|gold.ecosystem:7992` as source node IDs.

The generated source attestation mirrors that row with GOLD `gold.ecosystem:7990`, source label `Aquaculture`, source path `Engineered > Artificial ecosystem > Aquaculture`, `mapping_predicate: skos:narrowMatch`, `assertion_count: 13`, `assertion_unit: ORGANISM`, and a note that three GOLD ecosystem node IDs share this path.

`ENVO:03600074` `aquaculture farm` is a plausible exact grounding target: its vendored definition covers aquatic systems in which fish, crustaceans, mollusks, aquatic plants, and algae are cultivated, and the source path sits under GOLD's engineered artificial-ecosystem branch. The generated record leaves the source concept minted with `ENVO:03600074` as a broader parent because the seeder's ambiguous-leaf rule found two same-depth `Aquaculture` leaves: this target and `Environmental > Aquatic > Aquaculture`.

The record has no row in `curation/decisions.tsv`, no row in `curation/term_requests.tsv`, and only generated `SEEDED_FROM_SOURCES` history. Its `NARROW` grounding and two parents are therefore seeded, not item-reviewed.

The generated `habitatmech:GOLD.0acae9a1a4` `Artificial ecosystem` parent is inherited from the GOLD source path. That parent is unresolved: its only decision row is a class-level lexical sweep, and `reports/yaml_record_review/20260924T171335Z-artificial_ecosystem.md` found that the parent has not been item-reviewed as a strict broader habitat.

## Evidence

The generated target has no record-level `evidence`, no `definition`, no `environmental_parameters`, no `characteristic_taxa`, and no target-specific `causal_graphs`.

Supported:

- The source-path identity, three-node collapse, and 13 GOLD organism assertions are reproducible from `data/raw/gold_ecosystem_paths.tsv:482`.
- The displayed GOLD node ID and three-node source-attestation note are reproducible from the raw node list, where `gold.ecosystem:7990` is the first node for this exact path.
- `ENVO:03600074` is at least a relevant broader candidate for the target because the vendored ontology term names an `aquaculture farm` and lists `aquaculture` as an exact synonym.
- The generated `habitatmech:GOLD.0acae9a1a4` parent is reproducible from the `Engineered > Artificial ecosystem` prefix.

Unsupported or over-scoped:

- No item-level decision has inspected whether the exact source path should `GROUND` to `ENVO:03600074`, stay `NARROW` under that term, or merge with the same-depth `Environmental > Aquatic > Aquaculture` source.
- No maintained input defines a novel engineered-aquaculture child of `ENVO:03600074` if this source path is retained as a minted record, and `curation/term_requests.tsv` currently only owns `UNGROUNDED` minted definitions, not `NARROW` records.
- No maintained input confirms that every exact-grounded or merged child under this branch should inherit the generic `Aquaculture` parent.

## Completeness

Consequential gaps:

- The target has no item-level decision. If it remains `NARROW`, there is not yet a maintained definition path that can author its HabitatMech definition while preserving `ENVO:03600074` as a broader parent.
- The target has no definition, environmental parameters, characteristic taxa, causal graph, or target-specific literature evidence.
- The target's `Artificial ecosystem` parent remains class-level reviewed rather than item-reviewed.
- `Aquaculture` is serialized as a strict parent for several child records generated from the GOLD branch, including fish pond/tank/pen, freshwater/seawater recirculating aquaculture system, molluscs farm/pond/tank, crustaceans pond/tank/raceway, marine fish farm, algae raceway pond, and photobioreactor descendants. Several of those exact source-path edges are likely valid, but the exact-grounded `ENVO:03600077` `photobioreactor` record also carries this parent after merging the aquaculture-path source with `Engineered > Bioreactor > Photobioreactor (PBR)`.

Bounded searches:

- `find reports/yaml_record_review -maxdepth 1 -type f -name '*-aquaculture.md' -print` found no earlier YAML review report for this exact stem. `find` is not gitignore-filtered.
- An ignored-inclusive exact search over `curation`, `data/raw`, `reports`, `data/habitats`, `history`, `research`, and `conf/id_label_targets.yaml` for `habitatmech:GOLD.0287e1b2a9` found the path lock, generated child records that use this target as a parent, and this target YAML; it found no target-specific decision row, term request, history entry, deep-research report, prior YAML review, id-label residual, or causal overlay.
- Ignored-inclusive exact searches for `gold.ecosystem:7990`, `gold.ecosystem:7991`, and `gold.ecosystem:7992` found the raw aggregate row and the generated target's displayed first source ID, and found no target-specific curation inputs.
- Ignored-inclusive exact raw searches found the exact `Engineered > Artificial ecosystem > Aquaculture` row in `data/raw/gold_ecosystem_paths.tsv` and found no exact direct rows in `data/raw/gold_path_biosamples.tsv`, `data/raw/gold_studies.tsv`, or `data/raw/gold_path_triads.tsv`.
- An ignored-inclusive exact search over `curation/causal_graphs` for `Engineered > Artificial ecosystem > Aquaculture` found no target causal overlay.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The target has no item-level curation, so the relationship between the exact GOLD path, `ENVO:03600074` `aquaculture farm`, and the same-depth aquatic `Aquaculture` source remains seeded rather than reviewed. | The generated record has `mapping_status: SEEDED` and only a `SEEDED_FROM_SOURCES` history event. Ignored-inclusive exact searches found no `curation/decisions.tsv`, `curation/term_requests.tsv`, `history/`, `research/habitats/`, or causal-overlay input for `habitatmech:GOLD.0287e1b2a9`, `gold.ecosystem:7990`, `gold.ecosystem:7991`, `gold.ecosystem:7992`, or the exact source path. | `curation/decisions.tsv`; if the minted record remains narrower than `ENVO:03600074`, future definition support for `NARROW` minted records. |
| Major | The unreviewed source-path parent currently propagates to exact-grounded or merged descendants where `Aquaculture` may be a contextual branch rather than a strict broader parent. | Exact ignored-inclusive searches found `habitatmech:GOLD.0287e1b2a9` as a generated parent in many descendant records. `data/habitats/engineered/photobioreactor.yaml` is exact-grounded to generic `ENVO:03600077` and also attested by `Engineered > Bioreactor > Photobioreactor (PBR)`, but it inherits `habitatmech:GOLD.0287e1b2a9` from only the aquaculture-path source concept. `parent_habitats` must mean strictly broader. | `curation/decisions.tsv` for source-specific grounding changes; future source-path parent suppression support in `src/habitatmech/seed.py` where an exact-grounded merged ontology record should not inherit a contextual source-path parent. |
| Major | The inherited `Artificial ecosystem` parent is itself unresolved as a strict broader habitat. | `curation/decisions.tsv:160` is only a class-level `CONFIRM_UNGROUNDED` row for `habitatmech:GOLD.0acae9a1a4`, and the `Artificial ecosystem` review found no item-level decision or term request validating that GOLD branch as a real generic artificial-ecosystem habitat. | `curation/decisions.tsv`; if item review confirms a real novel habitat, `curation/term_requests.tsv`. |

## Recommended Edits

1. Item-review `habitatmech:GOLD.0287e1b2a9` in `curation/decisions.tsv` against the exact `Engineered > Artificial ecosystem > Aquaculture` path, the vendored `ENVO:03600074` `aquaculture farm` term, and the sibling `Environmental > Aquatic > Aquaculture` source.
2. If the engineered path exactly denotes `ENVO:03600074`, use a `GROUND` decision; if it denotes a narrower engineered-aquaculture subtype, keep it minted with `GROUND_AS_PARENT` and track the current absence of definition support for `NARROW` minted records.
3. Review descendant records that inherit `habitatmech:GOLD.0287e1b2a9`, starting with `ENVO:03600077` `photobioreactor`; for exact-grounded merged records, either revise a source-specific decision that grounded too broadly or use future source-path parent suppression support to drop false inherited parents without redefining ontology-owned records.
4. Item-review `habitatmech:GOLD.0acae9a1a4` so this target no longer depends on a class-level `Artificial ecosystem` parent.

## Follow-up Checks

- After adding a decision or term request, run `just seed`, `just seed-canary habitatmech:GOLD.0287e1b2a9`, inspect `data/habitats/engineered/aquaculture.yaml`, then run `just seed-apply --force`.
- Run `just validate data/habitats/engineered/aquaculture.yaml` and `just validate-strict data/habitats/engineered/aquaculture.yaml`.
- Run `just term-requests-check`, `just validate-history`, `just verify-corpus --max-diffs 1`, `just worklist --limit 2000`, `just report`, and `git diff --check`.
- Run `just validate-causal-all` if any overlay changes.

## Additional Notes

- The exact ignored-inclusive miss searches included hidden and gitignored files.
- The GOLD source path has no exact direct MIxS biosample, triad, or study rows. Rows for descendants such as `Algae raceway pond` and `Crustaceans pond > Sediment` are useful leads for those narrower records, but not direct evidence for this target.
- The child-level `Algae raceway pond` reports and the sibling aquatic `Aquaculture` review are useful leads, but they cannot automatically validate this exact engineered source or every source-derived parent edge below it.
