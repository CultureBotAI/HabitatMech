# YAML Record Review: Alkaline

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/aquatic/alkaline__02df7139.yaml
- Started UTC: 2026-09-21T13:09:05Z
- Finished UTC: 2026-09-21T13:11:41Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | `HabitatRecord` |
| Path | `data/habitats/aquatic/alkaline__02df7139.yaml` |
| Identifier | `habitatmech:GOLD.94b8786010` |
| Label | `Alkaline` |
| Category | `AQUATIC` |
| Grounding status | `NOT_APPLICABLE` |
| Mapping status | `REVIEWED` |
| Source | GOLD |
| Source ID | `gold.ecosystem:3751` |
| Source path | `Environmental > Aquatic > Non-marine Saline and Alkaline > Alkaline` |

The target is a generated GOLD-only record. It has no definition, synonyms,
xrefs, environmental parameters, characteristic taxa, record-level evidence,
causal graphs, discussions, or datasets. `data/habitats/PATHS.tsv` pins
`habitatmech:GOLD.94b8786010` to the stable `alkaline__02df7139` slug.

This review covers the single GOLD source concept keyed by
`habitatmech:GOLD.94b8786010`. The identifier is the first ten hex characters
of `sha1("GOLD:Environmental > Aquatic > Non-marine Saline and Alkaline > Alkaline")`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/aquatic/alkaline__02df7139.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/habitats/aquatic/alkaline__02df7139.yaml --quiet` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just term-requests-check` | Passed; the generated term-request table is current with 109 terms. |
| Reference validator | Not checked: this reviewed `NOT_APPLICABLE` GOLD-only record has no `EvidenceItem` references or causal overlay, and this repository documents no standalone reference validator for a generated record without those objects. |
| `just validate-history` | Passed; 77 history records validated against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records were present, with 0 missing, 0 extra, and 0 differing files. |

## Identity and Grounding

The reviewed source identity is reproducible:

| Claim | Review |
|---|---|
| Minted identifier | Supported. `mint("GOLD", canonical_path)` hashes the exact GOLD source path to `habitatmech:GOLD.94b8786010`; `data/habitats/PATHS.tsv` maps that identifier to `alkaline__02df7139`. |
| GOLD source path | Supported. `data/raw/gold_ecosystem_paths.tsv` has the exact canonical path `Environmental > Aquatic > Non-marine Saline and Alkaline > Alkaline`. |
| Source ID | Supported with disambiguation. The exact raw GOLD ecosystem row collapses two upstream IDs, `gold.ecosystem:3751|gold.ecosystem:3966`; the generated attestation shows the first and its note points readers to `data/raw/gold_ecosystem_paths.tsv` for the rest. |
| Source label | Supported. The raw GOLD row sets `leaf_label` to `Alkaline`, matching the generated `label` and `source_label`. |
| Assertion count | Supported. The exact raw GOLD row has `organism_count=6`, `study_count=0`, `biosample_count=0`, and `total_assertions=6`; the generated attestation stores 6 `ORGANISM` assertions. |
| Category | Supported by the first two GOLD path levels, `Environmental > Aquatic`. |
| `grounding_status: NOT_APPLICABLE` | Supported. The source concept's maintained `curation/decisions.tsv` row is an item-level `NOT_APPLICABLE` decision stating that this `Alkaline` node is a pH band qualifying the parent node, not a habitat. |
| `mapping_status: REVIEWED` | Supported. The sole contributing GOLD source has an item-level decision, so the generated record is fully reviewed even though the source concept is rejected as a habitat. |

The non-habitat decision is consistent with the curated parent review. The
parent record, `habitatmech:GOLD.ce244e62cd`, was redefined as the genuine
habitat `inland saline or alkaline aquatic environment` on 2026-08-25 after a
deep-research report separated the broad inland saline-or-alkaline environment
from its bare `Alkaline` pH-band child. The vendored ontology slice also carries
`PATO:0001430` `alkaline`, confirming that bare alkalinity is a quality, and
the exact source path's strongest MIxS medium term is `ENVO:01000357` `alkaline
water`, a material rather than a habitat identity.

The generated `parent_habitats` edge is not sound. The target currently has
`habitatmech:GOLD.ce244e62cd` as a parent, but a source concept curated
`NOT_APPLICABLE` as a pH qualifier cannot be a strict subtype of a HabitatRecord
even when the GOLD parent path itself denotes a real habitat.

## Evidence

Supported:

| Claim | Evidence |
|---|---|
| The source concept exists and is keyed by two GOLD ecosystem nodes collapsed onto one canonical path. | `data/raw/gold_ecosystem_paths.tsv` has the exact non-marine saline-or-alkaline `Alkaline` path with `gold_node_count=2` and `gold.ecosystem:3751|gold.ecosystem:3966`. |
| The generated GOLD attestation preserves the upstream organism count. | The exact raw GOLD row has 6 organism assertions, and the generated attestation records 6 `ORGANISM` assertions. |
| The exact source path appears in downstream GOLD biosample, triad, and study inventories. | `data/raw/gold_path_biosamples.tsv` has 25 biosamples for `gold.ecosystem:3966`; `data/raw/gold_path_triads.tsv` has exact-path broad/local/medium rows summarizing 11 samples from 6 studies; and `data/raw/gold_studies.tsv` has nine rows that include the exact path. |
| The exact-path MIxS triads support alkaline-water and inland aquatic context, not a bare alkaline identity. | The highest-count medium term for the exact path is `ENVO:01000357` `alkaline water`, while the highest-count broad term is `ENVO:00002030` `aquatic biome`. |
| The nearest GOLD parent is a curated habitat, but the child is still a qualifier. | `data/habitats/aquatic/non_marine_saline_and_alkaline__0e812745.yaml` defines `habitatmech:GOLD.ce244e62cd` as `inland saline or alkaline aquatic environment`; `curation/decisions.tsv` marks this `Alkaline` source concept `NOT_APPLICABLE` because it is a pH band. |

Unsupported or over-scoped:

| Claim | Issue |
|---|---|
| `habitatmech:GOLD.ce244e62cd` `inland saline or alkaline aquatic environment` is a broader habitat for this record. | The parent source is a habitat, but this child source has been item-reviewed as a pH qualifier rather than a habitat. `docs/CURATION.md` and `CLAUDE.md` reserve `parent_habitats` for strictly broader is-a links; this generated edge says the rejected pH-band node is itself a habitat underneath the curated inland saline-or-alkaline habitat. |

No literature citations, record-level evidence, causal edges, environmental
parameters, characteristic taxa, discussions, or datasets are attached to this
record.

## Completeness

The source concept has been reviewed at item depth, and no term request is
needed because the source concept is not a habitat. The record still has one
consequential generated artifact gap:

- `src/habitatmech/seed.py` adds the next GOLD path level as a parent after
  item-level decisions are applied, so this reviewed `NOT_APPLICABLE` pH band
  still inherits a real HabitatRecord as a strict broader habitat.

The broader GOLD subtree confirms why this should be fixed at the maintained
GOLD hierarchy pass instead of on the generated target. The same `Alkaline`
qualifier is also the GOLD parent path for descendants such as `Sediment`,
`Microbial mats`, `Mine pit pond`, and `Carbonate`; future rewiring needs to
preserve or intentionally replace each descendant's broader habitat context
without inheriting it through a node already curated as a non-habitat pH band.

Bounded absence checks:

| Query | Scope | Result |
|---|---|---|
| `habitatmech:GOLD.94b8786010` | Hidden/ignored-inclusive search over `curation`, `data/raw`, `data/habitats`, `research/habitats`, `reports/yaml_record_review`, and `curation/causal_graphs` | Found the maintained decision row, the PATHS row, the generated target, generated children that inherit this node as a parent, and one parent-record research mention; found no term request, causal overlay, or prior YAML review for this exact source concept. |
| `gold.ecosystem:3751`, `gold.ecosystem:3966`, and `Environmental > Aquatic > Non-marine Saline and Alkaline > Alkaline` | Hidden/ignored-inclusive search over raw GOLD rows, maintained curation inputs, generated records, research reports, and YAML reviews | Found the exact GOLD ecosystem, biosample, MIxS triad, and study rows, plus generated descendants under the source path; found no maintained suppression that would prevent the false parent edge. |
| `alkaline__02df7139` | Hidden/ignored-inclusive search over `data/habitats/PATHS.tsv`, `data/habitats`, `reports/yaml_record_review`, `research/habitats`, and `curation` | Found only the slug row before this report was created. |
| `PATO:0001430` and `ENVO:01000357` | Hidden/ignored-inclusive search over `data/raw/ontology_terms.tsv`, `data/raw/gold_path_triads.tsv`, maintained curation inputs, and the generated target and parent records | Found `PATO:0001430` `alkaline`, the parent record's note intentionally not carrying that partial BacDive quality xref, and exact-path GOLD triads for `ENVO:01000357` `alkaline water`; found neither term as an xref or grounding target on this exact generated record. |

## Findings

### Blocker

None found.

### Major

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| M1 | `parent_habitats` keeps a generated edge from a reviewed non-habitat pH band to a real habitat, asserting that the pH qualifier is itself a habitat subtype. | `alkaline__02df7139.yaml` lists `habitatmech:GOLD.ce244e62cd` as its parent even though the maintained decision for `habitatmech:GOLD.94b8786010` says the child source concept is `NOT_APPLICABLE`. The GOLD second pass in `src/habitatmech/seed.py` adds the parent path after decisions resolve both rows, so the edge is generated reproducibly even though the child is an environmental-parameter qualifier. | `src/habitatmech/seed.py`, or a maintained suppression/rewiring input consumed by the GOLD parent-path pass. |

### Minor

None found.

## Recommended Edits

1. Update the maintained GOLD hierarchy pass in `src/habitatmech/seed.py`, or add a maintained suppression/rewiring table consumed by that pass, so item-reviewed `NOT_APPLICABLE` qualifier source concepts are not emitted as strict `parent_habitats`.
2. At minimum, suppress the edge from `Environmental > Aquatic > Non-marine Saline and Alkaline > Alkaline` to `Environmental > Aquatic > Non-marine Saline and Alkaline`.
3. Canary the descendants under this pH band, including `Sediment`, `Microbial mats`, `Mine pit pond`, and `Carbonate`, so the future fix either preserves defensible inland alkaline context or keeps curated parent replacement without routing any hierarchy through a source concept curated as a pH qualifier.
4. Do not hand-edit `data/habitats/aquatic/alkaline__02df7139.yaml`; it is generated from raw GOLD, curation decisions, and the seeder.

## Follow-up Checks

| Edit | Narrowest proving check |
|---|---|
| Suppress or rewire the false GOLD parent edge for this exact source. | Run `just seed-canary habitatmech:GOLD.94b8786010` and confirm `data/habitats/aquatic/alkaline__02df7139.yaml` no longer has `parent_habitats: habitatmech:GOLD.ce244e62cd`. |
| Preserve source provenance while rejecting the pH band as a habitat. | Re-read the canary and confirm the record keeps `grounding_status: NOT_APPLICABLE`, `mapping_status: REVIEWED`, `source_id: gold.ecosystem:3751`, the two-node source note, and the exact non-marine alkaline GOLD source path. |
| Avoid regressing descendants below this qualifier. | Canary `habitatmech:GOLD.fec2625866`, `habitatmech:GOLD.af1854714b`, `habitatmech:GOLD.0946b92f8f`, and `habitatmech:GOLD.1208484062`, then confirm they either keep a defensible broader habitat or intentionally replace the inherited `Alkaline` pH-band node. |
| Prove the regenerated corpus remains stable. | Run `just validate data/habitats/aquatic/alkaline__02df7139.yaml`, `just validate-strict data/habitats/aquatic/alkaline__02df7139.yaml --quiet`, `just term-requests-check`, `just validate-history`, and `just verify-corpus --max-diffs 1`. |
| Prove downstream exports no longer carry the false edge. | Rebuild KGX and SSSOM products and confirm no exported edge asserts `habitatmech:GOLD.94b8786010` under `habitatmech:GOLD.ce244e62cd`. |

## Additional Notes

- `gold_ecosystem_paths.tsv` reports 6 organism assertions on the collapsed
  exact path, while `gold_path_biosamples.tsv` reports 25 biosamples for
  `gold.ecosystem:3966` and `gold_path_triads.tsv` summarizes 11 samples for
  the exact path. Those inventories have different assertion units; the
  generated GOLD `source_attestations` count intentionally uses the organism
  count.
- The exact-path MIxS triads include four distinct local terms and five
  distinct medium terms across six studies, which is too diffuse to promote
  one contextual triad term as this source concept's identity. The reviewed
  `NOT_APPLICABLE` decision avoids overfitting the bare pH-band source node to
  `alkaline water`.
