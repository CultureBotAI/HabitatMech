# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/wastewater__49e4fa74.yaml
- Started UTC: 2026-09-25T16:49:36Z
- Finished UTC: 2026-09-25T16:51:13Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.39ec66994a` |
| Label | `Wastewater` |
| Category | `ENGINEERED` |
| Source path | `Engineered > Bioreactor > Anaerobic > Wastewater` |
| Maintained owner | Generated from `data/raw/gold_ecosystem_paths.tsv` and `data/habitats/PATHS.tsv`; future item-level review belongs in `curation/decisions.tsv`, and any retained minted definition or parent replacement belongs in `curation/term_requests.tsv`. |

The target is a GOLD-only, generated, `NARROW`, still-`SEEDED` record for the
zero-count anaerobic-bioreactor `Wastewater` path. The seeder minted a stable
HabitatMech identifier, attached `ENVO:00002001` `waste water` through the
ambiguous-leaf rule, inherited `ENVO:00002124` `anaerobic bioreactor` from the
immediate GOLD parent path, and represented two collapsed GOLD ecosystem node
IDs with `source_id: gold.ecosystem:5678`.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/habitats/engineered/wastewater__49e4fa74.yaml` | Pass: LinkML reported `No issues found`. |
| `just validate-strict data/habitats/engineered/wastewater__49e4fa74.yaml` | Pass: scanned 1 file with 0 files containing `ERROR` and 0 total `ERROR` rows. |
| `just validate-causal curation/causal_graphs/wastewater__49e4fa74.yaml` | Not applicable: the target has no `causal_graphs` block, and `find curation/causal_graphs -maxdepth 1 -type f -name 'wastewater__49e4fa74.yaml' -print` found no target-specific overlay. |
| `just validate-causal-all` | Pass: validated 32 causal-graph curation files with 32 graphs. |
| `just term-requests-check` | Pass: the term-request table is current with 109 terms. |
| `just validate-history` | Pass: 77 history records were valid against `src/habitatmech/schema/history.yaml`. |
| `just verify-corpus --max-diffs 1` | Pass: expected 3,206 records, found 3,206 on disk, and the corpus reproduces exactly from `data/raw/`. |
| `just worklist --limit 2000 --status all --out /tmp/habitatmech-anaerobic-wastewater-worklist.tsv` | Pass: reported 953 ungrounded records, 1,810 decisions on file, and wrote 953 rows to `/tmp/habitatmech-anaerobic-wastewater-worklist.tsv`. |
| `just report` | Pass: completed the corpus report for 3,206 records. |
| `git diff --check` | Pass. |
| Reference validator | Not applicable: this generated GOLD-only record has no record-level `evidence`, `characteristic_taxa`, `causal_graphs`, `discussions`, or `datasets` with citation-bearing `EvidenceItem` references. |

## Identity and Grounding

The generated identifier, label, category, source attestation, and slug agree
with the raw GOLD inventory. `data/raw/gold_ecosystem_paths.tsv` has the exact
path `Engineered > Bioreactor > Anaerobic > Wastewater` with
`gold_node_count=2`, no direct organism, study, biosample, or total assertions,
and `gold_node_ids=gold.ecosystem:5678|gold.ecosystem:5679`.
`data/habitats/PATHS.tsv` maps `habitatmech:GOLD.39ec66994a` to the stable
`wastewater__49e4fa74` slug.

The generated `ENVO:00002001` parent is plausible and appropriately broader.
`data/raw/ontology_terms.tsv` labels that term `waste water` and defines it as
water affected in quality by anthropogenic influence, and GOLD's high-volume
shallower `Engineered > Wastewater` concept already claims it exactly. This
target is not identical to generic waste water because the GOLD path narrows it
to a wastewater child of anaerobic bioreactor, but the target is still a kind
of waste water.

The generated `ENVO:00002124` parent is not supported as a strict broader
habitat. The exact grounded source parent, `Engineered > Bioreactor >
Anaerobic`, is an anaerobic-bioreactor record with item-level `GROUND`
decisions; that establishes only the source-tree context. A `Wastewater` leaf
under that context denotes wastewater in, entering, or processed by an
anaerobic bioreactor, not a kind of anaerobic bioreactor.

No item-level curation row exists for `habitatmech:GOLD.39ec66994a`; the target
has only its generated seed event. That means the `NARROW` state is explainable
from the seeder's ambiguous-leaf and source-parent routes, but it has not been
endorsed by a curator.

## Evidence

The sole `GOLD` source attestation mirrors
`data/raw/gold_ecosystem_paths.tsv`: source ID `gold.ecosystem:5678`, source
label `Wastewater`, source path
`Engineered > Bioreactor > Anaerobic > Wastewater`,
`mapping_predicate: skos:narrowMatch`, and a note that two GOLD node IDs share
the same collapsed path.

Exact gitignore-independent searches for the target source path in
`data/raw/gold_path_triads.tsv`, `data/raw/environment_parameters.tsv`,
`data/raw/gold_studies.tsv`, and `data/raw/gold_path_biosamples.tsv` found no
target-specific side-table rows. The parent paths `Engineered > Wastewater`
and `Engineered > Bioreactor > Anaerobic` do carry GOLD assertions, and the
generic `ENVO:00002001` `waste water` record also has PREGO, MADIN,
Environments Table, environmental-parameter, taxon, and causal-graph content,
but those rows belong to their own exact records rather than to this specific
zero-count GOLD child.

Unsupported or over-scoped generated claims:

- `ENVO:00002124` as a `parent_habitats` entry is an over-scoped
  source-context edge. `ENVO:00002124` is the reviewed parent-path identity,
  not a strict genus for wastewater.

## Completeness

The generated record carries the expected source-owned values for the current
raw inputs: a minted identifier, GOLD label, `ENGINEERED` category, inherited
`waste water` and `anaerobic bioreactor` parents, a single GOLD source
attestation, a seed event, and a stable slug lock.

The curation state is incomplete. Exact `rg --no-ignore --hidden` searches for
`habitatmech:GOLD.39ec66994a` under `curation`,
`data/habitats/engineered`, `data/habitats/PATHS.tsv`, `history`, `research`,
`reports/yaml_record_review`, and `conf` found only the generated YAML record
and the slug-lock row. Exact searches for `gold.ecosystem:5678`,
`gold.ecosystem:5679`, and the target source path found the raw GOLD row and
the generated target only. Exact `find` searches found no target-specific
causal overlay or prior YAML review report. Ignored files were included in all
of those absence checks.

Optional characteristic-taxon, environmental-parameter, DOI/PMID/URL evidence,
dataset, discussion, and causal-graph slots are correctly absent for this GOLD
child in the generated record. The direct GOLD path contributes no organism,
study, biosample, triad, or environmental-parameter rows that could populate
those slots for `wastewater__49e4fa74`.

## Findings

- **major**: `habitatmech:GOLD.39ec66994a` has no item-level review, so its
  NARROW status and source-specific wastewater identity remain seeded rather
  than curated.

  **Evidence**: The target GOLD row is a distinct zero-count `Wastewater` child
  under `Engineered > Bioreactor > Anaerobic`, while the exact `waste water`
  identity is already claimed by the shallower high-volume `Engineered >
  Wastewater` source concept. Exact gitignore-independent searches found no
  target-specific row in `curation/decisions.tsv`, no `curation/term_requests.tsv`
  definition, and no target-specific history or research report.

  **Owner**: `curation/decisions.tsv` should hold the item-level decision; if
  the item-level review keeps the minted identifier, `curation/term_requests.tsv`
  should hold its definition and strict parent.

- **major**: `parent_habitats` asserts that this anaerobic-bioreactor
  wastewater is a kind of `ENVO:00002124` `anaerobic bioreactor`.

  **Evidence**: The inherited parent comes from GOLD's
  `Engineered > Bioreactor > Anaerobic` path segment. `ENVO:00002124` names a
  bioreactor in which the contained material is not oxygenated, while this
  child leaf names the wastewater material in the anaerobic-bioreactor context.
  The record has no item-level decision, term request, MIxS triad, causal
  overlay, or source side-table row saying the wastewater is itself a reactor.

  **Owner**: `curation/term_requests.tsv` should replace the generated
  source-path parent with a strict material or waste-water genus if the
  item-level decision confirms the target is a real minted habitat.

No blockers found.

No minor findings.

## Recommended Edits

1. In `curation/decisions.tsv`, add an item-level review for
   `habitatmech:GOLD.39ec66994a` scoped to
   `Engineered > Bioreactor > Anaerobic > Wastewater`, the broader
   `ENVO:00002001` `waste water` parent, and the contextual `ENVO:00002124`
   `anaerobic bioreactor` edge.

2. If item review keeps this as a source-specific wastewater habitat, define it
   in `curation/term_requests.tsv` under `ENVO:00002001` or a tighter
   inspected water/material genus, and use `parent_mode=REPLACE` if the review
   confirms that `ENVO:00002124` is contextual rather than strict.

3. Regenerate the corpus from those maintained inputs so the generated record
   records the curator decision and no longer carries a false
   anaerobic-bioreactor `is-a` edge.

## Follow-up Checks

After curation, rerun:

- `just term-requests-check`
- `just validate data/habitats/engineered/wastewater__49e4fa74.yaml`
- `just validate-strict data/habitats/engineered/wastewater__49e4fa74.yaml`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000 --status all --out /tmp/habitatmech-anaerobic-wastewater-worklist.tsv`

## Additional Notes

- The target should stay separate from the exact `ENVO:00002001` `waste water`
  aggregate unless an item-level review decides the anaerobic-bioreactor
  context is not identity-defining. `Engineered > Wastewater` is a high-volume
  exact waste-water concept with 1,140 GOLD organism assertions, whereas this
  deeper child has none.
- The same contextual-parent pattern has already been observed for the sibling
  `Engineered > Bioreactor > Anaerobic` material leaves `Inoculum`,
  `Leachate`, `Manure`, and `Digestate`.
