# YAML Record Review

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/engineered/activated_sludge__cd285164.yaml
- Started UTC: 2026-09-23T10:27:05Z
- Finished UTC: 2026-09-23T10:27:05Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `habitatmech:GOLD.032b7dd40b` |
| Label | `Activated sludge` |
| Path | `data/habitats/engineered/activated_sludge__cd285164.yaml` |
| Category | `ENGINEERED` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| Generated or maintained | Generated from `data/raw/gold_ecosystem_paths.tsv`, lexical ENVO grounding, and `data/habitats/PATHS.tsv`; not a maintained curation input |
| Source concept | GOLD `gold.ecosystem:5528` for `Engineered > WWTP > Activated sludge` |
| Locked slug | `data/habitats/PATHS.tsv:1285` maps `habitatmech:GOLD.032b7dd40b` to `activated_sludge__cd285164` |

I read the full generated record. It contains generated identity, category,
grounding status, two ontology parents, one GOLD source attestation with
organism-level assertion count, and the initial `SEEDED_FROM_SOURCES` history
event. It has no generated definition, synonyms, environmental parameters,
characteristic taxa, evidence items, causal graphs, discussions, or dataset
links.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/engineered/activated_sludge__cd285164.yaml` | Pass: `No issues found` |
| `just validate-strict data/habitats/engineered/activated_sludge__cd285164.yaml` | Pass: 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows |
| `just validate-causal-all` | Pass: 32 causal-graph curation files and 32 graphs validated |
| `just term-requests-check` | Pass: `term-request table is current (109 terms)` |
| `just validate-history` | Pass: `No issues found`; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Pass: expected 3,206 records, found 3,206 on disk, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | Pass: 0 ungrounded records still undecided; 1,810 decisions on file |
| `just report` | Pass: corpus report completed for 3,206 records |
| `git diff --check` | Pass |
| Reference validator | Not applicable: the target has no `evidence`, `causal_graphs`, or reference-bearing maintained overlay to validate |

## Identity and Grounding

The target is a minted GOLD-path record for `Engineered > WWTP > Activated
sludge`. `data/raw/gold_ecosystem_paths.tsv:538` contains exactly that path,
gives it the label `Activated sludge`, records depth `3`, records three GOLD
ecosystem node IDs `gold.ecosystem:5528|gold.ecosystem:5529|gold.ecosystem:5530`,
and supplies the generated organism-level `assertion_count: 9` and
`assertion_unit: ORGANISM`.

The source path is backed by GOLD study and biosample inventories.
`data/raw/gold_studies.tsv` lists `Engineered > WWTP > Activated sludge` in 16
rows, including single-path studies `Gs0151925`, `Gs0151936`, `Gs0153768`,
`Gs0153782`, `Gs0153966`, `Gs0154155`, `Gs0154229`, and `Gs0154256`.
`data/raw/gold_path_biosamples.tsv:112` lists node `5530` with 270 biosamples.

The generic activated-sludge ontology parent is supported.
`data/raw/ontology_terms.tsv:7191` gives `ENVO:00002046` the canonical label
`activated sludge`, and `data/raw/ontology_subclass_edges.tsv:5293` places it
under `ENVO:00002044`; `data/raw/ontology_terms.tsv:7189` labels
`ENVO:00002044` as `sludge`. That supports keeping the GOLD path minted and
carrying generic activated sludge as a broader parent.

The wastewater-treatment-plant ontology parent is supported for the standalone
GOLD parent path but not for this child edge. `data/raw/gold_ecosystem_paths.tsv:575`
records `Engineered > WWTP` with four GOLD ecosystem node IDs, and
`curation/decisions.tsv:1336` grounds that source concept
`habitatmech:GOLD.f1d23444f9` to `ENVO:00002043` `wastewater treatment plant`
with item-level `GROUND`. The generated
`data/habitats/engineered/wastewater_treatment_plant.yaml` record is therefore
valid as a reviewed GOLD/PREGO wastewater-treatment-plant record. Activated
sludge is a sludge material inside some wastewater treatment plants, not a
subtype of the plant itself.

No item-level curation decision currently reviews
`habitatmech:GOLD.032b7dd40b`. An exact ignored/hidden-inclusive search for the
child identifier found only the generated record and `PATHS.tsv`; exact
ignored/hidden-inclusive searches for the child source nodes and full GOLD path
found only the generated record and raw GOLD inventory rows under the searched
repository paths.

## Evidence

The generated record carries no literature evidence or mechanism evidence, and
none is required for the raw GOLD attestation itself.

| Claim | Checked evidence | Assessment |
|---|---|---|
| GOLD attests `Engineered > WWTP > Activated sludge`. | `data/raw/gold_ecosystem_paths.tsv:538` | Supported. The path, label, depth, three-node identity, and organism count agree with the generated `source_attestations` item. |
| GOLD backs the target path with studies and biosamples. | `data/raw/gold_studies.tsv`; `data/raw/gold_path_biosamples.tsv:112` | Supported. Sixteen study rows include the exact path, and the biosample table lists node `5530` with 270 biosamples. |
| The target path can use generic activated sludge as a broader parent. | `data/raw/ontology_terms.tsv:7191`; `data/raw/ontology_subclass_edges.tsv:5293` | Supported. `ENVO:00002046` is `activated sludge` and is a subclass of sludge. |
| The target path is a kind of `ENVO:00002043` `wastewater treatment plant`. | Generated from the GOLD `WWTP` parent path after the maintained `habitatmech:GOLD.f1d23444f9` grounding. | Unsupported. `WWTP` correctly denotes a wastewater treatment plant, but the activated-sludge child denotes material in the plant rather than a narrower facility type. |
| The `Engineered > WWTP` parent source is raw-backed and item-reviewed. | `data/raw/gold_ecosystem_paths.tsv:575`; `curation/decisions.tsv:1336`; `data/habitats/engineered/wastewater_treatment_plant.yaml:19-27` | Supported as a source concept, not as a reviewed broader parent of this activated-sludge child. |

## Completeness

- No generated definition, curator-authored term request, environmental
  parameter, characteristic taxon, discussion, dataset, or causal graph is
  expected for a purely seeded GOLD record that has not yet received item-level
  review.
- Exact ignored/hidden-inclusive searches for
  `habitatmech:GOLD.032b7dd40b`, `activated_sludge__cd285164`,
  `gold.ecosystem:5528`, `gold.ecosystem:5529`, `gold.ecosystem:5530`,
  `Engineered > WWTP > Activated sludge`, `habitatmech:GOLD.f1d23444f9`,
  `gold.ecosystem:4669`, `Engineered > WWTP`, `ENVO:00002046`,
  `ENVO:00002044`, and `ENVO:00002043` found the cited raw, path-lock,
  maintained parent curation, ontology-slice, and generated-record rows
  outside `build/`, `pages/`, and `data/text_map/`.
- The target identifier had no hits in `curation/`, `research/`, `history/`, or
  pre-existing `reports/yaml_record_review/`, with ignored and hidden files
  included.
- No exact prior report existed for `activated_sludge__cd285164` before this
  report was written: `find reports/yaml_record_review -maxdepth 1 -type f
  -name '*-activated_sludge__cd285164.md' -print` returned no paths.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| HM-ACTSLUDGE-CD285164-001 | major | `parent_habitats` asserts `habitatmech:GOLD.032b7dd40b` is a kind of `ENVO:00002043` `wastewater treatment plant`, but the child is `Activated sludge` and the supported material parent is `ENVO:00002046` `activated sludge` under `ENVO:00002044` `sludge`. `parent_habitats` is an is-a relation, and this path edge turns GOLD's `WWTP` facility context into the false claim that activated sludge is the treatment plant itself. | Add an item-level decision for `habitatmech:GOLD.032b7dd40b` in `curation/decisions.tsv`; if the concept stays minted, add a `curation/term_requests.tsv` definition with `parent_mode=REPLACE` so the generated record keeps `ENVO:00002046` and drops the inherited `ENVO:00002043` wastewater-treatment-plant parent. |

## Recommended Edits

1. Resolve `habitatmech:GOLD.032b7dd40b` at item depth in
   `curation/decisions.tsv`.
2. If `Engineered > WWTP > Activated sludge` denotes generic activated sludge
   rather than a wastewater-treatment-plant-specific subtype, ground it
   directly to `ENVO:00002046` so the GOLD attestation merges into the existing
   activated-sludge identity.
3. If the WWTP context is materially narrower than generic activated sludge,
   keep the source concept minted, use a `GROUND_AS_PARENT` decision to
   `ENVO:00002046`, and add a definition in `curation/term_requests.tsv` with
   `parent_mode=REPLACE` so the next seed no longer inherits the false
   `ENVO:00002043` parent.
4. Regenerate through the seeder rather than editing
   `data/habitats/engineered/activated_sludge__cd285164.yaml` directly.

## Follow-up Checks

- `just seed`
- `just seed-canary habitatmech:GOLD.032b7dd40b`
- Inspect `data/habitats/engineered/activated_sludge__cd285164.yaml` or
  confirm the record intentionally merged away after an exact `GROUND`.
- `just seed-apply --force`
- `just validate-strict data/habitats/engineered/activated_sludge__cd285164.yaml`
  if the record stays minted; otherwise validate the record that absorbed its
  source attestation.
- `just validate-causal-all`
- `just term-requests-check`
- `just validate-history`
- `just verify-corpus --max-diffs 1`
- `just worklist --limit 2000`
- `just report`
- `git diff --check`

## Additional Notes

- The false parent is not caused by a hand edit: `just verify-corpus --max-diffs
  1` reproduced the generated YAML exactly from committed raw inputs.
- The parent decision at `curation/decisions.tsv:1336` correctly reviews the
  GOLD `WWTP` source concept as an exact wastewater treatment plant. It does
  not validate the source-path edge from an activated-sludge material child to
  that plant parent.
- A reference validator was recorded as not applicable because this record has
  no references or causal-graph overlay; the edge that needs curation is a
  generated hierarchy edge, not a reference-backed mechanism claim.
