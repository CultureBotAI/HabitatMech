# YAML Record Review: indoor air

- Repository: CultureBotAI/HabitatMech
- Record: data/habitats/air/indoor_air.yaml
- Started UTC: 2026-09-21T05:49:19Z
- Finished UTC: 2026-09-21T05:50:41Z
- Verdict: pass with minor issues

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/air/indoor_air.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.40979f5751` |
| Label | `indoor air` |
| Category | `AIR` |
| Grounding status | `UNGROUNDED` |
| Mapping status | `REVIEWED` |
| Maintained status | generated from `data/raw/`, `curation/decisions.tsv`, `curation/term_requests.tsv`, and `data/habitats/PATHS.tsv` |

The generated record is an item-reviewed, authored-definition GOLD term request:

- GOLD supplies one `Environmental > Air > Indoor Air` source attestation with
  72 `ORGANISM` assertions.
- An item-level decision confirms this is a real habitat whose best available
  vendored ontology parent is `ENVO:00002005` `air`.
- A term-request row authors a definition, keeps `air` as the genus, and adds
  `room air` and `interior air` as exact synonyms.
- No characteristic taxa, environmental parameters, claim-level evidence,
  causal graphs, discussions, datasets, or xrefs are present.

## Validation

| Check | Result |
|---|---|
| `.venv/bin/python -c "from linkml.validator.cli import cli; cli()" --schema src/habitatmech/schema/habitatmech.yaml --target-class HabitatRecord data/habitats/air/indoor_air.yaml` | Passed; no LinkML issues. |
| `.venv/bin/python scripts/validate_strict.py data/habitats/air/indoor_air.yaml --quiet` | Passed; 1 file scanned, 0 files with errors, 0 total error rows. |
| `.venv/bin/python scripts/build_term_requests.py --check` | Passed; term-request table is current at 109 terms. |
| `.venv/bin/python scripts/verify_corpus.py --max-diffs 1` | Passed; 3,206 expected records, 3,206 found, no missing/extra/differing records. |
| `.venv/bin/python scripts/validate_history.py` | Passed; 77 history records valid against `src/habitatmech/schema/history.yaml`. |
| Reference validator | Not checked: this record has no DOI/PMID/URL `EvidenceItem` or causal-edge evidence. `scripts/check_report_citations.py` only samples or checks the full research DOI corpus, not one target report. |

## Identity and Grounding

| Claim | Evidence | Assessment |
|---|---|---|
| The record denotes the GOLD `Environmental > Air > Indoor Air` source concept. | `data/raw/gold_ecosystem_paths.tsv` has exactly that canonical path, leaf `Indoor Air`, depth `3`, `gold_node_count=3`, `organism_count=72`, `total_assertions=72`, and node IDs `gold.ecosystem:3477\|gold.ecosystem:3746\|gold.ecosystem:3961`. | Supported exactly. |
| The stable file stem is `indoor_air`. | `data/habitats/PATHS.tsv` maps `habitatmech:GOLD.40979f5751` to `indoor_air`. | Supported exactly. |
| `grounding_status: UNGROUNDED` is correct. | `curation/decisions.tsv` has `habitatmech:GOLD.40979f5751` as `CONFIRM_UNGROUNDED`, `review_depth=ITEM`, and `object_id=ENVO:00002005`, `object_label=air`. `data/raw/ontology_terms.tsv` verifies `ENVO:00002005` as `air` and the rejected kg-microbe mapping target `ENVO:01000855` as `area of mixed forest`. | Supported exactly. |
| `ENVO:00002005` `air` is a true parent. | The committed indoor-air research report proposes `ENVO:00002005` as the genus and distinguishes indoor air from rooms, buildings, HVAC artifacts, cleanroom air, humid air, contaminated air, and outdoor atmospheric terms. `curation/term_requests.tsv` row `habitatmech:GOLD.40979f5751` uses `parent_mode=ADD`. | Supported exactly. |
| `mapping_status: REVIEWED` is correct. | The sole source concept feeding this record has an item-level decision row, and the authored term-request row defines the minted concept. | Supported exactly. |
| `room air` is a supported exact synonym. | The indoor-air research report lists `room air` as a name in real use and ties it to MIxS `room air exchange rate`. | Supported. |
| `interior air` is a supported exact synonym. | The indoor-air research report lists `interior air` only under names in real use with the note `common usage`; no citation or inspected source is attached. | Weakly supported; see `HM-INDOOR-AIR-001`. |

## Evidence

No `EvidenceItem` rows or causal graphs are present, so there are no attached
claim-level snippets or causal edges to audit.

The curator-authored definition and the `ENVO:00002005` parent are supported by
the committed indoor-air research report's proposed definition, near-miss table,
and GOLD-child scope discussion. The synonym evidence is uneven: `room air` is
traced to a specific MIxS phrase in the report, but `interior air` is present
only as an uncited common-usage assertion.

## Completeness

- The record is schema-valid, reproducible, and item-curated.
- Empty `characteristic_taxa`, `evidence`, `environmental_parameters`,
  `causal_graphs`, `discussions`, `datasets`, and `xrefs` are technically
  acceptable: no maintained input currently supplies them for this record.
- `data/raw/gold_path_triads.tsv` and `data/raw/environment_parameters.tsv` have
  no row for the exact `Environmental > Air > Indoor Air` path,
  `habitatmech:GOLD.40979f5751`, or `gold.ecosystem:3477`,
  `gold.ecosystem:3746`, or `gold.ecosystem:3961`.
- Ignored/hidden-inclusive exact searches covered `data`, `curation`,
  `research`, `docs`, `src`, `.claude`, and `reports`, excluding only generated
  text-map, cache, and build trees, for `habitatmech:GOLD.40979f5751`, its GOLD
  ecosystem node IDs, `Indoor Air`, the exact GOLD path, and `indoor_air`.
  These found the target, its `PATHS.tsv` row, the raw GOLD path, the item-level
  decision row, the term-request row, generated term-request output, the
  dedicated indoor-air research report, the successful research-manifest row,
  and adjacent reports or records that mention indoor air; they found no
  environmental-parameter row, exact MIxS triad row, or causal-graph overlay for
  this target.
- A bounded `find curation/causal_graphs -maxdepth 1 -type f -name '*indoor*'`
  search and a bounded `find curation/causal_graphs -maxdepth 1 -type f -name '*air*'`
  search found no candidate overlay.
- A bounded `find research/habitats/air -maxdepth 1 -type f -name '*indoor*'`
  search found exactly one dedicated indoor-air report.
- An exact ignored/hidden-inclusive `rg` search under
  `reports/yaml_record_review/` found no prior report with
  `- Record: data/habitats/air/indoor_air.yaml`. It found only adjacent
  cattle-barn and composting-facility reviews that mention indoor air as the
  inherited GOLD parent.

## Findings

### Minor

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| `HM-INDOOR-AIR-001` | The generated `interior air` exact synonym needs source-backed synonym support. Exact synonyms are usage claims, but the term-request row supports `interior air` only by saying the committed report attests it, and the report itself lists `interior air, ambient indoor air` as `common usage` without a cited source. | `curation/term_requests.tsv` row `habitatmech:GOLD.40979f5751` includes `interior air` in `exact_synonym`. The only exact `interior air` hit in the dedicated research report is the uncited line `interior air, ambient indoor air (common usage)`. An ignored/hidden-inclusive exact search across the repository, excluding generated pages/text-map and cache/build trees, found no other direct support beyond the generated record, the generated ENVO robot template, and an unrelated subway report phrase. | `curation/term_requests.tsv` |

## Recommended Edits

1. Re-review the `interior air` exact synonym in
   `curation/term_requests.tsv`. Either add durable inspected support for that
   phrase to the term-request notes or remove it from `exact_synonym`.
2. Regenerate the ENVO robot table and the single-record canary for
   `habitatmech:GOLD.40979f5751`, then inspect
   `data/habitats/air/indoor_air.yaml` and confirm that the generated
   synonyms, definition, parent, source attestation, and curation history all
   follow from the maintained rows.

## Follow-up Checks

- `.venv/bin/python scripts/build_term_requests.py`
- `.venv/bin/python scripts/seed_from_sources.py`
- `.venv/bin/python scripts/seed_from_sources.py --apply --only habitatmech:GOLD.40979f5751`
- `.venv/bin/python -c "from linkml.validator.cli import cli; cli()" --schema src/habitatmech/schema/habitatmech.yaml --target-class HabitatRecord data/habitats/air/indoor_air.yaml`
- `.venv/bin/python scripts/validate_strict.py data/habitats/air/indoor_air.yaml --quiet`
- `.venv/bin/python scripts/build_term_requests.py --check`
- `.venv/bin/python scripts/verify_corpus.py --max-diffs 1`

## Additional Notes

- The target report discusses `Dust` and `Air scrubber > Biofilm` as GOLD
  `Indoor Air` children that are not themselves air. That boundary is handled
  on the child records: the committed term-request rows for `indoor dust` and
  scrubber biofilm both use `parent_mode=REPLACE` to drop false inherited GOLD
  parents.
- The target has two failed deep-research attempts and one successful committed
  attempt in `reports/habitat_research_manifest.tsv`; the successful row points
  at `research/habitats/air/indoor-air-habitatmech-gold-40979f5751-deep-research-claude_code.md`.
