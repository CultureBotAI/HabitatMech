# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/terrestrial/agricultural_land__d3a12788.yaml`
- Started UTC: 2026-09-24T08:48:42Z
- Finished UTC: 2026-09-24T08:51:15Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/terrestrial/agricultural_land__d3a12788.yaml` |
| Class | `HabitatRecord` |
| Identifier | `habitatmech:GOLD.9f684e44fb` |
| Label | `Agricultural land` |
| Category | `TERRESTRIAL` |
| Grounding status | `NARROW` |
| Mapping status | `SEEDED` |
| GOLD source-concept key | `habitatmech:GOLD.9f684e44fb` |
| GOLD parent source-concept key | `habitatmech:GOLD.ceceb38473` |
| Maintained owners | Generated from `data/raw/gold_ecosystem_paths.tsv`, `data/raw/ontology_terms.tsv`, `data/raw/ontology_subclass_edges.tsv`, `data/habitats/PATHS.tsv`, and `src/habitatmech/seed.py`; `data/habitats/` is generated and remains read-only |

This generated GOLD-only record represents `gold.ecosystem:4230`:

```text
Environmental > Terrestrial > Soil > Clay > Agricultural land
```

The source concept is one of the collision-pinned GOLD `Agricultural land`
leaves. This review covers only the `Clay`-scoped source concept with stable
minted identifier `habitatmech:GOLD.9f684e44fb`.

## Validation

| Check | Result |
|---|---|
| `just validate data/habitats/terrestrial/agricultural_land__d3a12788.yaml` | Passed: LinkML reported no issues |
| `just validate-strict data/habitats/terrestrial/agricultural_land__d3a12788.yaml` | Passed: 1 file scanned, 0 files with errors, 0 total error rows |
| `just validate-causal <overlay>` | Not applicable: the record has no `causal_graphs` slot, and ignored/hidden-inclusive exact searches found no `curation/causal_graphs/` overlay for `habitatmech:GOLD.9f684e44fb`, `gold.ecosystem:4230`, or `agricultural_land__d3a12788` |
| `just validate-causal-all` | Passed: 32 causal-graph curation files with 32 graphs |
| `just term-requests-check` | Passed: term-request table is current with 109 terms |
| `just validate-history` | Passed: no issues found; 77 history records valid against `src/habitatmech/schema/history.yaml` |
| `just verify-corpus --max-diffs 1` | Passed: expected 3,206 records, found 3,206, 0 missing, 0 extra, 0 differing |
| `just worklist --limit 2000` | Passed: 0 ungrounded records still undecided, 1,810 decisions on file |
| `just report` | Passed: corpus report completed for 3,206 records |
| Reference validator | Not applicable: this record has no DOI, PMID, URL, `EvidenceItem`, or causal-edge evidence that would need citation checking |
| `git diff --check` | Passed before and after this report was written |

## Identity and Grounding

| Claim | Maintained evidence | Assessment |
|---|---|---|
| The output identifier is `habitatmech:GOLD.9f684e44fb`. | `data/habitats/PATHS.tsv` pins that identifier to `agricultural_land__d3a12788`. | Supported. |
| The GOLD source ID is `gold.ecosystem:4230`. | `data/raw/gold_ecosystem_paths.tsv` has one row for `Environmental > Terrestrial > Soil > Clay > Agricultural land` with one GOLD node ID, depth 5, `organism_count` 11, `study_count` 0, `biosample_count` 0, and `total_assertions` 11. | Supported. |
| The source belongs in the terrestrial category. | `src/habitatmech/seed.py` infers the category from GOLD's `Environmental > Terrestrial` prefix for this raw row. | Supported. |
| `ENVO:00000077` `agricultural ecosystem` is a broader habitat for this source concept. | The reused GOLD leaf `Agricultural land` matches a synonym on the vendored `ENVO:00000077` row; the seeder keeps the exact ontology term as a parent because other GOLD paths reuse the same leaf. | Supported. |
| `habitatmech:GOLD.ceceb38473` `Clay` is a broader habitat for this source concept. | `src/habitatmech/seed.py` links each GOLD concept to the resolved concept of its parent path; the parent source path `Environmental > Terrestrial > Soil > Clay` hashes to `habitatmech:GOLD.ceceb38473`, and `data/habitats/PATHS.tsv` pins that identifier to `clay__0b0ee455`. | Not supported: no inspected maintained row proves that agricultural land in a clay-soil context is a strict subclass of the generated `Clay` parent. |
| The source attestation has `assertion_count: 11` and `assertion_unit: ORGANISM`. | The exact `data/raw/gold_ecosystem_paths.tsv` row records `organism_count` 11 for `gold.ecosystem:4230`. | Supported. |
| `mapping_status` is `SEEDED`. | Ignored/hidden-inclusive exact searches found no item-level `curation/decisions.tsv` decision for `habitatmech:GOLD.9f684e44fb`, `gold.ecosystem:4230`, `Environmental > Terrestrial > Soil > Clay > Agricultural land`, or `agricultural_land__d3a12788`. | Supported. |

The generated minted-child identity is internally consistent, but the
source-path parent is too strong. GOLD uses `Clay` as path context for this
`Agricultural land` leaf, and `parent_habitats` turns that containment into an
is-a edge to the generated `Clay` habitat. The exact MIxS triad rows classify
the target samples as a terrestrial biome at broad scale, rank `farm` as the
local-scale term, and rank `ultisol` as the medium term; those rows reinforce a
soil/agricultural context but do not support making agricultural land a subtype
of the upstream `Clay` bin.

## Evidence

The source attestation is an accurate transcription of the committed GOLD row:

- `source: GOLD`
- `source_id: gold.ecosystem:4230`
- `source_label: Agricultural land`
- `source_path: Environmental > Terrestrial > Soil > Clay > Agricultural land`
- `mapping_predicate: skos:narrowMatch`
- `assertion_count: 11`
- `assertion_unit: ORGANISM`

Additional GOLD-derived evidence is consistent with an agricultural-land source
whose samples came from a clay-soil context:

| Source | Maintained row | Assessment |
|---|---|---|
| Biosamples | `data/raw/gold_path_biosamples.tsv` maps the same exact path and node `4230` to 46 biosamples. | Supported; biosample counts are independent of the `ORGANISM` count in the generated source attestation and are not emitted on this record. |
| GOLD studies | `data/raw/gold_studies.tsv` has three rows that include `Environmental > Terrestrial > Soil > Clay > Agricultural land`. | Supports multi-study use of the exact GOLD source path. |
| MIxS broad scale | `data/raw/gold_path_triads.tsv` summarizes 46 samples from 3 studies and ranks `ENVO:00000446` `terrestrial biome` as the broad-scale top term with 100% sample share. | Supports the generated terrestrial category. |
| MIxS local scale | The same triad block ranks `ENVO:00000078` `farm` as the local-scale top term with 78% sample share from 1 study. | Supports an agricultural local context. |
| MIxS medium | The same triad block ranks `ENVO:01001397` `ultisol` as the medium top term with 78% sample share from 1 study. | Supports a soil material context, but not a strict `Clay` is-a parent. |

There is no claim-level evidence, causal graph, or reference-bearing assertion
on this seeded GOLD record.

## Completeness

- The generated target contains every slot currently emitted from the
  maintained exact GOLD row: minted identity, terrestrial category, seeded
  narrow grounding, the GOLD source attestation, the generated parent set, and
  one generated curation event.
- Empty `synonyms`, `environmental_parameters`, `characteristic_taxa`,
  `evidence`, `causal_graphs`, `discussion`, and `datasets` are appropriate
  for the current maintained inputs. The reviewed GOLD triad rows provide
  contextual evidence for this source path; no maintained input emits them as
  `environmental_parameters`, and no maintained input supplies an output
  synonym, taxon association, claim-level evidence item, causal overlay,
  discussion item, or dataset reference for this source concept.
- Ignored/hidden-inclusive exact searches covered `curation/decisions.tsv`,
  `curation/term_requests.tsv`, `curation/external_xrefs.tsv`,
  `curation/causal_graphs/`, `history/`, `reports/yaml_record_review/`, and
  `research/` for `habitatmech:GOLD.9f684e44fb`, `gold.ecosystem:4230`,
  `Environmental > Terrestrial > Soil > Clay > Agricultural land`, and
  `agricultural_land__d3a12788`; no maintained curation row, overlay, history
  entry, prior exact report, or research item was found.
- A gitignore-independent `find` search covered `reports/yaml_record_review/`
  before report creation; no exact `*-agricultural_land__d3a12788.md` report
  was found.

## Findings

### Major

| ID | Finding | Evidence | Maintained owner |
|---|---|---|---|
| `HM-AGR-LAND-D3A12788-001` | `parent_habitats` asserts `habitatmech:GOLD.ceceb38473` `Clay` as an is-a parent, but the source concept is agricultural land in GOLD's clay-soil context, not a kind of clay habitat. | `data/raw/gold_ecosystem_paths.tsv` places `gold.ecosystem:4230` under `Environmental > Terrestrial > Soil > Clay > Agricultural land`; the GOLD parent-path second pass in `src/habitatmech/seed.py` turns that source path into a `parent_habitats` edge; `data/raw/gold_path_triads.tsv` points the local context at `ENVO:00000078` `farm` and the sampled material at `ENVO:01001397` `ultisol`, which is subclassed to `ENVO:00001998` `soil`. | Add an item-level definition for `habitatmech:GOLD.9f684e44fb` in `curation/term_requests.tsv` with a strict agricultural parent and `parent_mode: REPLACE`, or add a maintained GOLD path-parent override in `src/habitatmech/seed.py` so this source path keeps `ENVO:00000077` without inheriting the false `Clay` parent. |

No blocker or minor findings found.

## Recommended Edits

1. Decide `habitatmech:GOLD.9f684e44fb` at item level against the exact
   `Environmental > Terrestrial > Soil > Clay > Agricultural land` path. If
   the source concept is an agricultural-land habitat scoped by clay soil, add
   a `curation/decisions.tsv` row that keeps the minted identity and
   `ENVO:00000077` as a broader agricultural parent.

2. Add a maintained `curation/term_requests.tsv` row for
   `habitatmech:GOLD.9f684e44fb` with an authored definition, a strict
   agricultural parent, and `parent_mode=REPLACE`, or add a maintained
   path-parent override that removes the contextual
   `habitatmech:GOLD.ceceb38473` parent from this child. Regenerate the corpus;
   do not edit
   `data/habitats/terrestrial/agricultural_land__d3a12788.yaml` directly.

## Follow-up Checks

| Scope | Command or check |
|---|---|
| Target schema | `just validate data/habitats/terrestrial/agricultural_land__d3a12788.yaml` |
| Closed-schema validation | `just validate-strict data/habitats/terrestrial/agricultural_land__d3a12788.yaml` |
| Corpus reproduction | `just verify-corpus --max-diffs 1` |
| Worklist/report sanity | `just worklist --limit 2000` and `just report` |
| Causal overlays | `just validate-causal-all` |
| Term-request exports | `just term-requests-check` |
| History | `just validate-history` |
| Manual hierarchy check | Confirm the regenerated child keeps `ENVO:00000077` and drops `habitatmech:GOLD.ceceb38473` from `parent_habitats` |
| Diff hygiene | `git diff --check` |

## Additional Notes

- `ENVO:00000077` is not the hierarchy defect in this target. It is a broader
  agricultural-ecosystem term for GOLD's reused `Agricultural land` leaf and
  belongs as a `NARROW` parent unless a future item-level review chooses a more
  specific source identity.
- The exact path's top medium triad, `ENVO:01001397` `ultisol`, has `red clay
  soil` as a synonym and is subclassed to `ENVO:00001998` `soil` in the vendored
  slice. That is compatible with GOLD using `Clay` as a soil-context bin, but
  it still supports a sampled-soil context rather than a strict is-a edge from
  agricultural land to the generated `Clay` parent record.
