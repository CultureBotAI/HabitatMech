# YAML Record Review

- Repository: `CultureBotAI/HabitatMech`
- Record: `data/habitats/aquatic/brine_pool.yaml`
- Started UTC: `2026-09-23T22:42:28Z`
- Finished UTC: `2026-09-23T22:44:56Z`
- Verdict: `pass with minor issues`

## Target

| Field | Value |
|---|---|
| Path | `data/habitats/aquatic/brine_pool.yaml` |
| Class | `HabitatRecord` |
| Identifier | `ENVO:00000369` |
| Label | `brine pool` |
| Definition source | `ENVO` |
| Category | `AQUATIC` |
| Grounding status | `EXACT` |
| Mapping status | `SEEDED` |

This is a generated ENVO/PREGO record owned by upstream inventories, ontology
slice rows, and the generator, not by hand edits under `data/habitats/`. Its
path is pinned by `data/habitats/PATHS.tsv`, which maps `ENVO:00000369` to
`brine_pool`.

## Validation

| Check | Result |
|---|---|
| `find reports/yaml_record_review -maxdepth 1 -type f -name '*-brine_pool.md' -print` | Passed; no prior exact report was present. |
| `find curation/causal_graphs -maxdepth 1 -type f -name '*brine*' -print` | Passed; no candidate causal overlay was present. |
| `rg --no-ignore --hidden -n --fixed-strings ... -g '!data/text_map/**' -g '!pages/**' -g '!build/**'` | Passed; bounded exact searches found the ENVO ontology rows, direct parent edges, PREGO raw rows, the `PATHS.tsv` slug row, the generated target record, nearby non-target brine notes, and no maintained curation decision, term request, causal overlay, history record, or auxiliary report for `ENVO:00000369`. Ignored and hidden files were included. |
| `just validate data/habitats/aquatic/brine_pool.yaml` | Passed; `linkml-validate` reported no issues. |
| `just validate-strict data/habitats/aquatic/brine_pool.yaml` | Passed; 1 file scanned, 0 files with errors, and 0 total error rows. |
| `just validate-causal-all` | Passed; 32 causal-graph curation files with 32 graphs validated. |
| Focused causal-graph validation | Not applicable; the record has no maintained overlay under `curation/causal_graphs/`. |
| Reference validator | Not applicable; the record has no DOI, PMID, URL, causal-edge evidence, dataset, or discussion references to check. |
| `just term-requests-check` | Passed; the term-request table is current with 109 terms. |
| `just validate-history` | Passed; no issues found and 77 history records validated. |
| `just verify-corpus --max-diffs 1` | Passed; 3,206 expected records, 3,206 on disk, 0 missing, 0 extra, and 0 differing. |
| `just worklist --limit 2000` | Passed; the command reported 0 ungrounded records still undecided and 1,810 decisions on file. |
| `just report` | Passed; the corpus report completed for 3,206 records. |
| `git diff --check` | Passed before report creation. |

## Identity and Grounding

The record denotes `ENVO:00000369` `brine pool`. Its identifier, label,
definition, exact ENVO synonym `marine brine pool`, and `definition_source:
ENVO` are seeded from the matching row in `data/raw/ontology_terms.tsv`.

The only source concept feeding this record is PREGO `ENVO:00000369`. PREGO
concepts already use ontology CURIEs, so `src/habitatmech/seed.py` self-grounds
this source concept to `ENVO:00000369` through the `prego_self_grounded` route.
No item-level `curation/decisions.tsv` row exists for the PREGO curator key
`habitatmech:PREGO.b176e6ce66`, so `mapping_status: SEEDED` is expected even
though the generated grounding is exact.

Both generated parents are direct ENVO parents. `data/raw/ontology_terms.tsv`
labels `ENVO:01000617` as `lentic water body` and `ENVO:01001308` as
`hydroform`; `data/raw/ontology_subclass_edges.tsv` asserts both
`ENVO:00000369 rdfs:subClassOf ENVO:01000617` and
`ENVO:00000369 rdfs:subClassOf ENVO:01001308`.

## Evidence

The PREGO source attestation is faithful to `data/raw/prego_habitats.tsv`. The
raw row has `taxon_count` 2, `direct_assertion_count` 2, `max_prego_score`
1.10314, `channels` `environmental_samples`, and six PREGO synonym strings,
five of which differ from the ENVO label and are emitted as `RELATED_SYNONYM`.

The two generated taxon associations match `data/raw/prego_habitat_taxa.tsv`
rows 4718 and 4719:

- rank 1 is `NCBITaxon:54121` `Halanaerobium congolense`, with PREGO score
  1.10314, direct flag `TRUE`, and channel `environmental_samples`;
- rank 2 is `NCBITaxon:53245` `Desulfonatronovibrio hydrogenovorans`, with
  PREGO score 1.08264, direct flag `TRUE`, and channel
  `environmental_samples`;
- each generated row keeps `candidate_pool: 2`;
- neither generated row carries `is_characteristic: true`, so the PREGO
  occurrence ranking is not upgraded to a curated characteristic claim.

The record has no `environmental_parameters`, causal graph, claim-level
evidence block, discussion item, or dataset usage. No maintained input
currently asserts those fields.

## Completeness

The record is complete enough as a generated PREGO/ENVO seed. It preserves the
source-owned ENVO definition, both direct ENVO ontology parents, the PREGO
source attestation, the complete two-taxon PREGO ranking, and the generated
`SEEDED_FROM_SOURCES` history event.

The generated empty fields are expected for the current maintained inputs:

- no item-level curation decision is keyed by `habitatmech:PREGO.b176e6ce66`;
- no term request is keyed by `ENVO:00000369`;
- no causal overlay file matches `brine`;
- no record-specific history record or auxiliary YAML review report exists for
  this exact source concept.

Bounded, ignored-inclusive exact searches for `ENVO:00000369`,
`habitatmech:PREGO.b176e6ce66`, `brine pool`, `brine_pool`,
`ENVO:01000617`, `ENVO:01001308`, `NCBITaxon:54121`, and
`NCBITaxon:53245` across curation inputs, raw inputs, generated YAML, history,
reports, research, source, tests, and configuration found the expected ontology,
PREGO, path-lock, and generated-record rows. Generated `data/text_map/`,
`pages/`, and `build/` files were excluded; ignored and hidden files were
included.

## Findings

### Minor

| Finding | Evidence | Maintained owner |
|---|---|---|
| Some generated PREGO related synonyms are mechanically inflected strings whose lexical scope is not locally re-reviewed. | `data/raw/prego_habitats.tsv` preserves PREGO synonym strings such as `brine poolic` and `marine brine poolic`; `data/habitats/aquatic/brine_pool.yaml` copies them as `RELATED_SYNONYM` with `source: PREGO`. The record is faithful to the raw row, but downstream synonym lookup should not treat these strings as HabitatMech-curated exact synonyms. | `data/raw/prego_habitats.tsv`; `src/habitatmech/extract.py` |

No blocker or major findings found.

## Recommended Edits

1. Leave `data/habitats/aquatic/brine_pool.yaml` generated and do not add a
   `curation/decisions.tsv` row merely to restate that PREGO `ENVO:00000369`
   is exactly ENVO `brine pool`.
2. If mechanically inflected PREGO related synonyms create false lookup
   behavior, add a maintained synonym-suppression mechanism for source-owned
   PREGO synonyms or fix the upstream PREGO vocabulary rows, then refresh the
   raw inventories.

## Follow-up Checks

- Run `just seed`.
- Run `just seed-canary ENVO:00000369` and inspect
  `data/habitats/aquatic/brine_pool.yaml` if PREGO synonym handling changes.
- Run `just validate data/habitats/aquatic/brine_pool.yaml`.
- Run `just validate-strict data/habitats/aquatic/brine_pool.yaml`.
- Run `just verify-corpus --max-diffs 1`.
- Run `just validate-history` after any future curation session that changes a
  maintained input.

## Additional Notes

The ignored-inclusive exact search found research mentions of
`ENVO:00000369` in a non-marine saline and alkaline habitat report; those
mentions are exclusionary notes explaining that marine brine pools overlap but
do not define that separate minted habitat. They do not add curation evidence
or an unresolved conflict for this generated PREGO/ENVO `brine pool` record.

The same search also found two existing review reports for same-label GOLD
`Brine` records. Those reports explicitly note that `brine_pool.yaml` is a
nearby but unrelated ENVO/PREGO record and are not prior exact reports for this
file.
